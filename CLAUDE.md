# CLAUDE.md — คู่มือสำหรับ Claude Code

> **อ่านไฟล์นี้ให้จบก่อนแก้โค้ดในโปรเจกต์นี้ทุกครั้ง**
> โปรเจกต์นี้แปลงมาจากมอคอัปแบบตรงตัว มีกฎเฉพาะตัวหลายข้อที่ถ้าไม่รู้แล้วจะพังเงียบ ๆ
>
> 🛑🛑🛑 **ก่อนอื่นใดทั้งหมด — อ่าน [`.claude/skills/working-with-gun/SCOLDING-LOG.md`](.claude/skills/working-with-gun/SCOLDING-LOG.md)**
>    **ทุกครั้งที่เริ่มโปรเจกต์ และทุกครั้งที่เริ่มคุยกัน** (พี่กันสั่ง 26 ส.ค. 2569)
>    ไฟล์นั้นเก็บคำจริงทุกคำที่พี่กันเคยด่า พร้อมว่าทำอะไรผิด และกฎที่ห้ามละเมิด
>    **พี่กันอารมณ์เสียเมื่อไหร่ = หยุดทุกอย่างทันที + บอกให้ได้ว่าทำอะไรผิด + ถามว่าจะให้ทำอะไรต่อ**
>
>    🚨 **งานเสร็จต้องถามทุกครั้งว่า "จะให้ push ไหมคะ"** ห้าม push เอง ห้ามเงียบรอ
>    🚨 **ห้ามตีความเอง ไม่ชัดเมื่อไหร่ = ถามก่อนเสมอ**
>    🚨🔴🔴 **กฎตัดสิน push — ประโยคที่พี่กันเพิ่งพิมพ์ มีคำว่า push หรือ พุช ไหม**
>    **ไม่มี = ห้าม push จบ** (*เอาเลย · ทำเลย · โอเค · ได้* ไม่ใช่คำสั่ง push ต้องย้อนถาม)
>    **มีแต่พูดถึงในบริบทอื่น = ห้าม push ย้อนถามก่อน** · **เป็นคำสั่งตรง ๆ เท่านั้น = push ได้**
>    🚨 **ห้ามถามขอบเขต push ในโปรเจกต์เดียวกัน** push ทีเดียวขึ้นทั้งโปรเจกต์อยู่แล้ว
>    🚨🔴 **ทุกประโยคที่มีคำว่า push หรือ พุช ต้องหยุดอ่านให้ดีก่อนว่าเป็นคำสั่งหรือไม่**
>    ตัวอย่างที่พี่กันยกเอง: *ถามทุกครั้งว่าจะให้พุชมั้ย* = สั่งให้ตั้งกฎว่าต้องถาม **ไม่ใช่สั่งให้ push**
>
> **ก่อนหน้านั้นต้องอ่าน 2 ไฟล์นี้ให้จบก่อน**
> 1. [`.claude/skills/working-with-gun/SKILL.md`](.claude/skills/working-with-gun/SKILL.md) — กฎการทำงานกลาง (ตัวตน · ภาษา · push/version · ข้อผิดพลาดห้ามซ้ำ)
> 2. [`docs/CLAIRE-CONTEXT.md`](docs/CLAIRE-CONTEXT.md) — บริบทเฉพาะโปรเจกต์นี้ + การตัดสินใจที่พี่กันเคาะแล้ว

---

## 0. กฎเหล็ก 16 ข้อ ห้ามละเมิด

1. **ตาราง `public.drugs` ใช้ร่วมกับ TB Calculator และ ME-DRP ที่รันจริงอยู่ — แก้ผิดพังทั้งสามที่**
   · **แก้ค่าในแถวได้** ผ่านหน้าคลังยา (ดูข้อ 3.15) และ **เพิ่มคอลัมน์ได้** (ราคากับสีเม็ดยาย้ายเข้ามาแล้ว ดูข้อ 3.33)
   · **ยังห้าม**ลบคอลัมน์ · ย้ายข้อมูลออกจากคอลัมน์เดิม · ลบแถว (ใช้ `hidden` แทน)
2. **ห้ามให้ `PATCH /api/returns/[id]` แตะ `unit_price`** — ราคาถูกแช่แข็งไว้ในแถว แก้ได้แค่ `qty` กับ `disposition` ไม่งั้นตัวเลข KPI ย้อนหลังจะขยับ
3. **ห้ามใช้เครื่องหมาย `?` ในข้อความบนหน้าเว็บ** ทุกที่ (ปุ่ม/หัวข้อ/ป้าย/หน้าต่างเด้ง) — ❌ "ยืนยันลบ?" ✅ "ยืนยันลบรายการนี้"
4. **ข้อความบนหน้าเว็บใช้ภาษาทางการ ไม่มี ค่ะ/นะคะ** และห้ามใช้ `alert()` / `confirm()` ของเบราว์เซอร์ ต้องเป็นหน้าต่างในแอปเท่านั้น
5. **ห้ามรัน `npm run build` ขณะ `npm run dev` เปิดอยู่** — `.next` ชนกัน ขึ้น `Cannot find module './xxx.js'`
6. **ห้ามใส่ `NEXT_PUBLIC_` กับกุญแจ Supabase** — เบราว์เซอร์ต้องไม่เคยถือกุญแจฐานข้อมูล
7. **การกระทำที่ย้อนยาก (ลบ) ต้องมีหน้าต่างยืนยันก่อนเสมอ** — ปุ่ม **"ยืนยันลบ" อยู่ซ้าย** "ยกเลิก" อยู่ขวา (สลับโดยตั้งใจ กันเผลอกด) และ **กดพื้นหลังไม่ปิด**
8. **แก้สไตล์ให้แก้ในข้อความ CSS ตรง ๆ อย่าแปลงเป็น object เอง** — ดูข้อ 3 ด้านล่าง
9. **ห้ามเปลี่ยน `ignoreDuplicates: true` เป็น `false` ใน `POST /api/returns`** — จะกลายเป็น `DO UPDATE` แล้วทับ `unit_price` ด้วยราคาปัจจุบัน = ผิดกฎแช่ราคาทันทีโดยไม่มีอะไรเตือน
10. **ห้ามเปลี่ยนชื่อ `middleware.js` เป็น `proxy.ts`** — Cloudflare build ไม่ผ่าน (เคยพลาดที่ TB Dashboard)
11. **ห้ามลบแถวจริงใน `mr_return`** — ใช้ `deleted_at` (ถังขยะ) เท่านั้น · ฟังก์ชันสรุป/ประวัติกรอง `deleted_at is null` อยู่แล้ว
12. **`mr_backfill_price` เติมได้เฉพาะแถวที่ `unit_price = 0`** — ห้ามแก้ให้ทับแถวที่มีราคาแล้วเด็ดขาด
13. **กล่องนอกสุดของทุกหน้าต้องมี `width:100%`** — ดูข้อ 3.1 ด้านล่าง ลบแล้วหน้าจะหดทันทีโดยไม่มี error
14. **`app.persist()` ห้ามเขียน localStorage ตอนอยู่ในโหมดดูตัวอย่าง** — ดูข้อ 13
15. 🚨 **เปิดเซิร์ฟเวอร์ทดสอบ — เช็คพอร์ตก่อนเสมอ ถ้าไม่ว่างให้เปิดพอร์ตอื่น**
    พี่กันสั่ง 10 ส.ค. 2569: *"เอาแค่อันเดียว อย่าเปิดสองโลคอลสิวะ"*
    แก้เป็นกฎใหม่ 5 ก.ย. 2569: *"จะรันโลคอลมา ต้องดูด้วยว่าพอร์ตนั้นมีใครใช้รึยัง ถ้ามีก็เปิดพอร์ตอื่น"*

    ```bash
    netstat -ano | grep LISTENING | grep -E ":30[0-9][0-9]"
    ```

    | เจออะไร | ทำยังไง |
    |---|---|
    | ไม่มีอะไรรัน | เปิด `med-return` (พอร์ต 3000) ได้เลย |
    | มีเว็บยาคืนรันอยู่แล้ว | **ใช้ตัวนั้น ห้ามเปิดซ้ำ** |
    | มีเว็บโปรเจกต์อื่นรันอยู่ | เปิด `med-return-3002` แทน · **ห้ามปิดของเขา** |

    🚨 **ห้ามปิดเซิร์ฟเวอร์ของโปรเจกต์อื่นเด็ดขาด** พี่กันแก้หลายโปรเจกต์พร้อมกันได้
    เคยพลาด 5 ก.ย. 2569 — พอร์ต 3000 เป็น ME-DRP ที่พี่กันกำลังแก้อยู่
    แคลร์เสนอให้ปิด พี่กันตอบ *"จะปิด drp เราทำไม เราแก้โปรเจกต์นั้นอยู่ ก็รันโลคอลใหม่สิ"*

    🚨 **ต้องเช็คด้วยว่าพอร์ตนั้นเป็นเว็บอะไร** ไม่ใช่แค่ว่ามีอะไรรันอยู่ไหม
    ดูจาก `document.title` — พอร์ตเดิมอาจถูกโปรเจกต์อื่นยึดไปแล้ว

    🚨🔴 **วันใหม่ = พอร์ตปิดหมดแล้ว ต้องเปิดใหม่เสมอ** (พี่กันสั่ง 5 ก.ย. 2569)
    *"วันใหม่ละนะ พอร์ตมันปิดหมดแล้ว เราปิดเครื่องมันก็หายแล้ว ซึ่งเธอต้อง detect ได้"*
    - เทียบวันที่ปัจจุบันกับวันที่ในบันทึก session ล่าสุด คนละวัน = เครื่องปิดไปแล้ว
    - เซิร์ฟเวอร์ที่เปิดไว้เมื่อวานไม่มีทางยังรันอยู่ **อย่าไปสมมติว่ายังอยู่**
    - **ห้ามพูดว่า "เซิร์ฟเวอร์เมื่อวานดับไปแล้ว" เหมือนเป็นเรื่องผิดปกติ** มันเป็นเรื่องธรรมดา

    🚨 **ใช้ `preview_start` เท่านั้น ห้ามรัน dev server ด้วย Bash**
    ค่าตั้งต้นอยู่ใน `C:\Users\PKH\.claude\launch.json` (ไฟล์รวมของทุกโปรเจกต์ ไม่ได้อยู่ในโปรเจกต์)

    | ชื่อค่าตั้งต้น | พอร์ต |
    |---|---|
    | `med-return` | 3000 |
    | `med-return-3002` | 3002 (สำรอง ใช้ตอน 3000 ไม่ว่าง) |
16. 🚨🔴 **ทำงานบนคลาว push ขึ้น `main` เท่านั้น** (พี่กันสั่ง 26 ส.ค. 2569)
    session บนคลาวมักตั้งสาขา `claude/xxx` มาให้ — **ห้าม push ขึ้นสาขานั้นแล้วถือว่าจบ**
    Cloudflare ดู `main` อย่างเดียว · งานที่ค้างบนสาขาอื่นเท่ากับไม่ได้ขึ้นเว็บจริง
    และเครื่องคลาวเป็นเครื่องชั่วคราว ปิด session แล้วหายทั้งหมด
    - ใช้ `git push -u origin HEAD:main` — สาขาในเครื่องชื่ออะไรก็ได้ ปลายทางต้องเป็น `main`
    - push แล้วต้องยืนยันด้วย `git rev-list --left-right --count origin/main...HEAD` ต้องได้ `0 0`
    - ถ้า setup บล็อกจน push ไม่ผ่าน ให้บอกพี่กันตรง ๆ ทันที ห้ามเงียบแล้ว push สาขาอื่นแทน

---

## 0.1 เลขเวอร์ชัน — โปรเจกต์นี้แคลร์ตั้งเองได้

พี่กันสั่งไว้ **5 ส.ค. 2569**: *"โปรเจคนี้เราให้อิสระหมายเลขเวอร์ชั่นตามที่เธอเห้นสำควรเลย ซึ่งนี่คือกฎ"*

**ตั้งเลขเองได้เลย ไม่ต้องถาม** (ต่างจากโปรเจกต์อื่นของพี่กันที่ต้องรอสั่ง) แต่ยังต้องทำครบทุกขั้น

| หลักใน `X.Y.Z.W` | ขยับเมื่อ |
|---|---|
| `W` | แก้บั๊ก · ขัดหน้าตาเล็กน้อย |
| `Z` | ฟีเจอร์ย่อย · ขัดหน้าตาชุดใหญ่ |
| `Y` | ระบบใหม่ทั้งก้อน · รื้อโครง |
| `X` | เปิดใช้จริงกับผู้ป่วย |

- รูปแบบ **4 ตำแหน่งเสมอ** ห้ามขาดห้ามเกิน
- แก้ที่ `components/helpers.js` (`APP_VERSION`) **และ** `package.json` ต้องตรงกัน
- 🚨🚨 **แก้ `APP_LAST_UPDATE` ในไฟล์เดียวกันด้วยทุกครั้ง** — เป็นวันที่ที่หน้าตั้งค่าโชว์ว่า "อัปเดตล่าสุด"
  พลาดมาแล้ว 25 ส.ค. 2569: push v0.7.1.0 ไปแล้วแต่หน้าจอยังบอก "5 สิงหาคม 2569" พี่กันจับได้เอง
  เดิมวันที่ฝังตายอยู่ใน `pages/about.jsx` คนละไฟล์กับเลขเวอร์ชันจึงลืมทุกที
  ย้ายมาไว้ติดกับ `APP_VERSION` แล้ว — เห็นพร้อมกันตอนแก้ ไม่ต้องจำ
- ก่อน bump รัน `git log --oneline -10` เช็คเลขที่ push จริง อย่าเชื่อเลขในโค้ด
- subject ของ commit ใส่ `vX.Y.Z.W:` **เฉพาะ commit ที่ bump จริง** ไม่งั้น changelog สร้างหัวข้อปลอม

---

## 1. โปรเจกต์นี้คืออะไร

เว็บแอปห้องยา รพ.ปรางค์กู่ — แปลงยาที่ผู้ป่วยคืนมาเป็นตัวเลขเงิน แล้วสรุปเป็น KPI ทั้งปีงบประมาณ
ไม่มีระบบเข้าสู่ระบบ · เปิดเว็บใช้ได้เลย

**ต้นฉบับ:** มอคอัปของ Claude Design ไฟล์ `Med Return App.dc.html` (1453 บรรทัด)
**กติกาที่พี่กันสั่งไว้:** *"แปรมาให้เหมือนเว็บนั้นทั้งหมด แล้วเราค่อยมาแก้กัน"* — ห้ามออกแบบใหม่ ห้ามตัดฟีเจอร์ ห้ามปรับ UX ให้ "ดีขึ้น" เอง

---

## 2. สถาปัตยกรรม — ทำไมแบ่งไฟล์แบบนี้

มอคอัปเป็นคลาสเดียว state ก้อนเดียว + ฟังก์ชัน `renderVals()` ที่คำนวณค่าทุกอย่างสำหรับวาดจอ
**โครงนี้ถูกเก็บไว้เหมือนเดิมเป๊ะ** แค่ผ่าเป็นไฟล์ย่อยเพื่อให้แก้ง่าย

```
MedReturnApp.jsx                 คลาสเดียว · state ก้อนเดียว · ไม่มี useState ที่ไหนเลย
   ├─ constructor → installHandlers(this)     ติดเมธอดทั้งหมดเข้ากับ this
   └─ render()    → renderShell(this)

shell.jsx           → renderVals(app)  แล้วส่ง V ก้อนเดียวให้ทุกหน้า
   ├─ vals/*.js     คำนวณค่าที่จะโชว์ (ไม่แตะ DOM ไม่ยิง API)
   └─ pages/*.jsx   วาดจออย่างเดียว (ไม่คำนวณอะไรเลย นอกจาก .map())

handlers/*.js       เมธอดทั้งหมด — setState · ยิง API · จับเวลา
```

**สัญญา 3 ข้อของโครงนี้ ห้ามฝ่าฝืน**

| ชั้น | ทำอะไรได้ | ห้ามทำ |
|---|---|---|
| `handlers/*` | `app.setState` · `fetch` · ตั้ง timer | คืนค่า JSX |
| `vals/*` | อ่าน `app.state` แล้วคำนวณค่าที่จะโชว์ · ส่งต่อ `app.method` เป็น callback | `setState` · `fetch` |
| `pages/*` | อ่าน `V.xxx` แล้ววาด | คำนวณเงื่อนไข/ตัวเลข |

รูปแบบไฟล์
```js
// handlers/xxx.js
export function xxxActions(app) {
  app.doThing = (arg) => { ... };
}

// vals/xxx.js
export function xxxVals(app, d) {   // d = ผลจาก derive(app)
  return { keyThatPagesUse: ... };
}

// pages/xxx.jsx
export function renderXxx(V) { return (<div>...</div>); }
```

**เพิ่มไฟล์ใหม่ในโฟลเดอร์ไหน ต้องไปลงทะเบียนที่ `index.js` ของโฟลเดอร์นั้นด้วย**
(`handlers/index.js` และ `vals/index.js`) ไม่งั้นเรียกใช้ไม่ได้ และจะเงียบ ไม่มี error

`helpers.js` **ห้าม import ไฟล์อื่นใน `components/`** — เป็นตัวล่างสุด ถ้าไป import จะวนกันเอง

---

## 3. `s()` — ตัวแปลง CSS (สำคัญที่สุดในโปรเจกต์นี้)

มอคอัปมีสไตล์ฝังในแท็กราว 600 จุด ถ้าแปลงเป็น object ด้วยมือทีละจุดจะเพี้ยนแน่นอน
เลยใส่ตัวแปลงหนึ่งตัวใน `components/helpers.js` แล้ว **ก๊อปข้อความ CSS จากมอคอัปมาทั้งดุ้น**

```js
s('display:flex;gap:7px;color:#2f7d5d')     // → {display:'flex',gap:'7px',color:'#2f7d5d'}
sx('padding:8px', { background: V.bg })     // ต่อสไตล์คงที่เข้ากับสไตล์ที่เปลี่ยนตามข้อมูล
sx('', { color: V.fg })                     // ใช้ได้ ถ้าไม่มีส่วนคงที่
```

ได้ 3 อย่าง: โค้ดใหม่เทียบกับต้นฉบับได้ตรง ๆ ทีละตัวอักษร · เรื่องฟอนต์แก้ที่เดียว · **ไม่มีรหัสสีไหนถูกแตะเลย**

- ผลลัพธ์ถูก cache + `Object.freeze` → **ห้ามแก้ object ที่ `s()` คืนมา** ถ้าต้องเพิ่มให้ใช้ `sx()`
- `s()` แปลงชื่อฟอนต์ระหว่างทาง: `Sarabun` → `var(--font-sarabun),Sarabun` และ `'IBM Plex Sans Thai'` → `var(--font-plex),...` (ฟอนต์ฝังมากับเว็บ ไม่โหลดจาก Google)
- มอคอัปใช้ `style-hover="..."` ซึ่ง React ทำไม่ได้ → ย้ายเป็นคลาสใน `app/globals.css` ชื่อ `.hv-*` (ทุกตัวมี `!important`) เช่น `.hv-bg-f6` `.hv-bg-e3f` `.hv-del` `.hv-bd-green` `.hv-teal`
  **เพิ่ม hover ใหม่ = เพิ่มคลาส `.hv-*` ใน globals.css อย่าเขียน onMouseEnter**

---

## 3.1 🚨 กับดัก flex ที่เคยทำหน้าเว็บหดไป 347px

กล่องครอบเนื้อหาใน `shell.jsx` เป็น **flex คอลัมน์** (ต้องเป็น เพื่อดันท้ายเว็บลงล่างสุด)
กล่องนอกสุดของทุกหน้าใช้ `max-width:1400px;margin:0 auto` เพื่อจัดกึ่งกลาง

**สองอย่างนี้ตีกัน** — ใน flex ตัว `margin:auto` บนแกนขวางจะ **ยกเลิกการยืดเต็มความกว้าง**
แล้วย่อกล่องลงเท่าเนื้อหา (fit-content) แทน หน้าเว็บเลยหดโดยไม่มี error ไม่มีคำเตือนอะไรเลย

**กฎ: กล่องนอกสุดของทุกหน้าต้องมี `width:100%` เสมอ**

```jsx
// ❌ หดเหลือ ~1013px บนจอ 1360
<div style={s('max-width:1400px;margin:0 auto;padding:20px 26px 26px')}>
// ✅
<div style={s('width:100%;max-width:1400px;margin:0 auto;padding:20px 26px 26px')}>
```

ใช้กับกล่อง `max-width:520px` ของฝั่งมือถือด้วย (`renderRecordNarrow` · `renderHistoryNarrow` · `renderSummaryNarrow`)
**ข้อยกเว้น:** กล่องที่พ่อไม่ใช่ flex ไม่ต้องใส่ เช่น `max-width:520px` ใน `renderSaveBar` กับ `renderNavNarrow`
(พ่อของมันคือ `<div style="flex:none;background:#fff">` ซึ่งเป็นบล็อกธรรมดา)

---

## 3.2 การตรึงหน้าจอ — สองแบบ ใช้คนละวิธี

### แบบ A — หน้าบันทึกคอม: ล็อกความสูงเท่าจอ ทั้งหน้าไม่เลื่อน

พี่กันสั่ง: *"มันจะเลื่อนได้แค่ตรงกรอบ ยังไม่มีรายการในครั้งนี้"*

```
shell.jsx  พื้นที่เลื่อน (overflow-y:auto · flex คอลัมน์)
  └─ กล่องครอบ    V.fitScreen ? 'flex:1;min-height:0'  ← หดได้ = ความสูงแน่นอน
                              : 'flex:1 0 auto'         ← ห้ามหด = หน้าอื่นเลื่อนตามปกติ
       └─ renderRecordWide  flex:1 · min-height:440px · display:flex · align-items:stretch
            ├─ คอลัมน์ซ้าย   flex:1 · min-width:0 · min-height:0 · flex คอลัมน์
            │    ├─ ช่องกรอกยา / ข้อความใบ้ / ยาที่คืนบ่อย   ← flex:none ทุกตัว
            │    └─ กรอบขาว  flex:1 · min-height:0 · flex คอลัมน์
            │         ├─ หัวคอลัมน์          flex:none
            │         └─ 🎯 กรอบรายการ       flex:1 · min-height:0 · overflow-y:auto
            └─ แผงขวา       width:296px · flex:none · min-height:0 · overflow-y:auto
```

🚨 **`V.fitScreen` คือหัวใจ** — ถ้ากล่องครอบเป็น `flex:1 0 auto` (ห้ามหด) กรอบรายการจะ
**ไม่รู้ว่าตัวเองสูงได้แค่ไหน** เพราะพ่อไม่มีความสูงที่แน่นอน แล้วมันจะยืดตามจำนวนแถวไปเรื่อย ๆ
สุดท้ายทั้งหน้าเลื่อนแทนที่จะเลื่อนในกรอบ (เคยพลาดมาแล้ว วัดได้ว่ากรอบสูง 700px บนจอ 640px)

`min-height:440px` เป็นทางหนีตอนจอเตี้ยมาก — ถ้าที่ว่างไม่พอ ให้กลับไปเลื่อนทั้งหน้าแทนที่จะบีบจนอ่านไม่ได้

### แบบ B — หน้าประวัติคอม: ตรึงด้วย `position:sticky` (หน้ายังเลื่อนทั้งหน้า)

| ส่วน | คลาส | ติดที่ | z-index |
|---|---|---|---|
| แถบกรอง (ชื่อหน้า + ช่องค้น + ปุ่มช่วงเวลา + ช่วงวัน + ถังขยะ + ส่งออก) | `.hist-head` | `top:0` | 5 |
| หัวคอลัมน์ | `.sticky-head` | `top:var(--histhead)` | 3 |

- `--histhead` ถูกเขียนจาก **ResizeObserver** ใน `MedReturnApp.jsx` (`this.histHeadRef` เป็น ref แบบฟังก์ชัน)
  **ตั้งเลขตายตัวไม่ได้** เพราะแถบกรองขึ้นบรรทัดใหม่เองเมื่อจอแคบ (`flex-wrap`)
- `.hist-head` ใช้ `margin:-20px -26px 0` คู่กับ `padding:20px 26px 2px` เพื่อ **แผ่ทับระยะขอบของหน้า**
  ไม่งั้นจะเห็นแถวลอดผ่านตรงร่องขอบตอนเลื่อน
- กรอบตารางห้ามใส่ `overflow:hidden` ไม่งั้น sticky ตาย (มีคอมเมนต์เตือนไว้ในไฟล์แล้ว)

---

## 3.3 นำเข้าราคาจากไฟล์ HIS — กฎ 5 ข้อ

ไฟล์: `lib/hisMatch.js` (สมองการจับคู่) · `handlers/himport.js` · `vals/himport.js` · `pages/himport.jsx`

1. 🚨 **ระบบเสนอ คนตัดสิน ห้ามนำเข้าอัตโนมัติทั้งดุ้น** — ราคาถูกแช่แข็งลงแถวตอนบันทึก
   จับคู่ผิดแล้วตัวเลข KPI ที่เอาไปเสนอผู้บริหารจะผิดถาวร แก้ทีหลังไม่ได้
2. 🚨 **ต้องตรวจความแรงเสมอ ไม่ใช่เฉพาะตอนมีหลายตัวเลือก** — เคยพลาดมาแล้ว
   ในไฟล์มี `ACYCLOVIR 400 mg` แถวเดียว ยา `Acyclovir 500 mg` ในเว็บก็จับคู่กับแถวนั้น
   แล้วขึ้นว่า "มั่นใจ" ติ๊กให้อัตโนมัติ = ได้ราคาของยาคนละความแรงมาใส่แบบเงียบ ๆ
   **ถ้าไม่มีแถวไหนความแรงตรงเลย ต้องตกไปกลุ่ม "ต้องเลือก" เสมอ**
3. 🚨 **ไฟล์ถูกอ่านในเบราว์เซอร์ล้วน ๆ ห้ามอัปโหลดขึ้นเซิร์ฟเวอร์** — ไฟล์มีบัญชียาทั้งโรงพยาบาล
   สิ่งเดียวที่ส่งขึ้นไปคือ (รหัสยา, ราคา) ของแถวที่ติ๊กยืนยันแล้ว
4. **ตัวอ่าน Excel วางไว้เองที่ `public/vendor/xlsx.full.min.js`** (SheetJS 0.20.3 จากเว็บทางการ)
   ไม่ได้ลงผ่าน npm เพราะรุ่นบน npm เก่าและมีประกาศช่องโหว่ค้างอยู่
   โหลดแบบขอเมื่อใช้ (952 KB) ตอนเปิดหน้านี้เท่านั้น ไม่ถ่วงตอนเปิดเว็บครั้งแรก
5. **คัดยาเลิกใช้ (`no_use = 1`) กับเวชภัณฑ์ออกก่อนจับคู่เสมอ**

**เรื่องที่ต้องรู้เกี่ยวกับไฟล์ HIS** (ไม่รู้แล้วจับคู่พลาดเป็นร้อยตัว)

| เรื่อง | ตัวอย่างจริง |
|---|---|
| เขียนชื่อยาแบบ Tall Man Lettering กันยาชื่อพ้อง | `CefTRI-axone` · `ceFA-zolin` · `CefTAZ-idime` · `CeFO-taxime` |
| ใช้ชื่อการค้าและตัวย่อ | `Artane` = Benzhexol · `CPM` = Chlorpheniramine · `CPZ` = Chlorpromazine |
| มีคำสะกดผิดในต้นทาง | `bisacodil` (ที่ถูกคือ bisacodyl) |
| มีเวชภัณฑ์ปนมา | `Aluminium Splint` · สายยาง · ถุงมือ |
| ยาตัวเดียวมีหลายบรรทัด ราคาต่างกัน | `CeFO-taxime` นอกบัญชี 40 ฿ · ยาเดิมผู้ป่วย 25 ฿ |
| **ราคา 1 บาท = ยาที่โรงพยาบาลไม่มีในคลัง** | `Ampicillin/Sulbactam (2+1 g/vial)` = 1 ฿ |

### 🧠 ราคา 1 บาท ไม่ใช่การจับคู่ผิด — อย่าไปทัก

พี่กันอธิบายไว้ 10 ส.ค. 2569 หลังแคลร์ทักว่าราคาดูไม่สมเหตุสมผล:

> *"ampi/sal ไม่มีในโรงพยาบาล เรามีแต่ code ยาไว้เผื่อเป็นยาจาก refer เลยใส่ 1 บาทเฉย ๆ"*

**ยาที่ตั้งราคา 1 บาทในไฟล์ HIS = ยาที่โรงพยาบาลไม่ได้สต๊อก** เก็บรหัสยาไว้เฉย ๆ
เผื่อผู้ป่วยส่งตัวมาจากโรงพยาบาลอื่น (refer) แล้วถือยาตัวนั้นติดมาด้วย จะได้มีรหัสให้บันทึก

**ไม่ใช่ราคาจริง · ไม่ใช่ความผิดพลาดของการจับคู่ · ไม่ต้องแก้ ไม่ต้องทักซ้ำ**
(เจอราคาแปลก ๆ ที่ไม่ใช่ 1 บาทค่อยทัก)

🚨 **Tall Man Lettering คือเหตุผลที่ต้องเทียบชื่อแบบ "บีบติดกัน" (`squashName`)**
ถ้าแปลงขีดเป็นช่องว่างจะได้ `cef triaxone` ซึ่งไม่มีวันตรงกับ `ceftriaxone`

ผลจับคู่กับไฟล์จริง (972 รายการ): คัดออก 225 เลิกใช้ + 124 เวชภัณฑ์ → เหลือ 654
เทียบกับยาในเว็บ 417 ตัว ได้ **มั่นใจ 254 · ต้องเลือก 97 · ไม่เจอ 66** · ความแรงไม่ตรง **0 ตัว**

### 3.3.1 ราคาถูกใส่ไว้ในฐานให้แล้ว — เภสัชกรแค่กดเลือก

พี่กันบอกว่าไม่อยากยุ่งกับไฟล์เอง *"เธอใส่ราคาเลยย เเล้วเราจะกดเลือกเองง"*
แคลร์เลยจับคู่ให้ล่วงหน้าแล้วเขียนผลลง `mr_drug_price` เรียบร้อย (5 ส.ค. 2569)

| ช่องใหม่ | เก็บอะไร |
|---|---|
| `needs_check` | `true` = จับคู่ไม่ชัวร์ รอเภสัชกรกดเลือก · **ราคาต้องเป็น 0** |
| `suggestions` | `[{name, price, unit}]` ตัวเลือกที่ระบบเสนอ สูงสุด 5 ตัว |
| `note` | ที่มาของราคา เช่น `HIS 5 ส.ค. 69 · CefTRI-axone 1 g/vial` |

🚨 **กติกาที่ห้ามฝ่าฝืน: ยาที่ `needs_check = true` ราคาต้องเป็น 0 เท่านั้น**
ถ้าใส่ราคาที่อาจผิดลงไปแล้วเภสัชกรมองข้าม ตัวเลข KPI จะผิดแบบเงียบ ๆ
⚠️ **ข้อความนี้ล้าสมัยตั้งแต่ v0.7.1.0** — ตอนนี้บันทึกยาที่ราคา 0 ได้แล้ว (ดูข้อ 3.21)
ยังปลอดภัยอยู่เพราะแถวนั้นมูลค่า 0 จนกว่าจะใส่ราคา แล้วระบบตีราคาย้อนหลังให้เอง

ตัวอย่างจริงที่เกือบพลาด: `Amoxicillin + Clavulanic acid 500 + 125 mg` ระบบจับไปเจอ
`Amoxicillin 250 mg = 2 ฿` ซึ่งเป็นยาเดี่ยว ราคาต่างกันหลายเท่า

**หน้าจัดการราคา** มีแท็บ **"รอกดเลือก"** พร้อมตัวเลขงานค้าง กดตัวเลือกแล้วเติมราคาลงช่องให้เลย
พอกดบันทึก `handlers/prices.js` จะเคลียร์ `needs_check` + ล้าง `suggestions`
แล้วเปลี่ยน `note` เป็น `ยืนยันโดยเภสัชกร · <ชื่อ>` เพื่อแยกจากที่ระบบเดามาเอง

---

## 3.4 กล่อง "Lot นี้" ในแผงขวาหน้าบันทึก

พี่กันเลือกจากมอคอัป 3 แบบ (สรุป Lot นี้ / Lot ล่าสุด / แค่ดันขึ้นให้ชิด)
เหตุผลที่ตัด "Lot ล่าสุด" ทิ้ง: *"รายการยามีฝั่งซ้ายอยู่แล้ว"* ไม่ต้องโชว์ซ้ำ

**กฎ 3 ข้อ**

1. 🚨 **ห้ามรวมจำนวนข้ามหน่วยนับ** — เอาเม็ดไปบวกขวดบวกหลอดแล้วเขียนว่า "414 หน่วย"
   เป็นตัวเลขที่ไม่มีความหมายอะไรเลย (พี่กันทักว่า "หน่วยอะไร")
   ต้องแยกตามหน่วยจริง `396 เม็ด · 14 แคปซูล · 2 กระบอก · และอีก 1`
2. 🚨 **ห้ามเดาเลข Lot ล่วงหน้า** — ฐานข้อมูลออกให้ตอนกดบันทึก (`mr_next_lot_no`)
   เครื่องอื่นอาจกดแทรกก่อน เลขที่เดาไว้จะผิด · โชว์เลขจริงเฉพาะหลังบันทึกสำเร็จ
   (โหมดดูตัวอย่างเป็นข้อยกเว้น ใส่เลขตัวอย่างไว้ให้เห็นภาพ)
3. 🚨 **ปุ่มบันทึกต้องเห็นเสมอ** — ใช้ `position:sticky;bottom:-14px` ตรึงไว้ก้นแผง
   เคยเจอ: จอ 640px + แถบเตือนโหมดตัวอย่าง = ปุ่มตกขอบแผง ต้องเลื่อนหา

**กับดัก flex ที่เจอตอนทำ** — กล่องนี้ต้องเป็น `flex:1 1 auto` **ไม่ใช่** `flex:1`
`flex:1` ย่อมาจาก `1 1 0%` = ฐานความสูงเป็นศูนย์ การคำนวณการหดเลยได้ศูนย์ตาม
กล่องไม่ยอมหดแม้ใส่ `min-height:0` แล้วไปดันปุ่มบันทึกตกขอบ

**แผงขวาแน่นมาก** — วัดแล้วก่อนใส่กล่องนี้ แผงกิน 502px ในพื้นที่ 503px
ระยะห่างถูกรีดเป็น padding 14/16 · gap 11 · ตัวเลขใหญ่ 20px แล้ว **อย่าเพิ่มระยะกลับ**

---

## 3.4.1 หน้าประวัติ — แบ่งกลุ่มด้วยสีจางตาม Lot

พี่กันเลือกจากมอคอัป 3 แบบ: สีจางอย่างเดียว / **สีจาง + คอลัมน์ Lot** / แถวหัวคั่น
และสั่งว่า **ห้ามตัดคอลัมน์ HN ออก** ตารางคอมจึงมี 10 คอลัมน์

| | คอม | มือถือ |
|---|---|---|
| แสดง Lot | คอลัมน์ที่ 10 ท้ายตาราง | ป้ายเขียวข้างสถานะ |
| แบ่งกลุ่ม | พื้นสลับ ขาว ↔ `#f4faf7` | แถบซ้าย 3px เข้ม ↔ จาง |
| กด | กรองดูเฉพาะ Lot นั้น (`app.viewLot`) | เหมือนกัน |

🚨 **สีจางต้องทำงานเฉพาะตอนเรียงตามวันที่** (`bandOn` ใน `vals/history.js`)
ถ้ากดเรียงตามราคา/ชื่อยา แถวจาก Lot ต่าง ๆ จะสลับกันมั่ว
สีจะกลายเป็นลายพร้อยที่ **หลอกตาว่าเป็นกลุ่มทั้งที่ไม่ใช่** — อันตรายกว่าไม่มีสี

🚨 **แถว "ทำลาย" คงสีแดงจาง `#fdf7f5` ไว้เสมอ** ไม่ให้สีกลุ่มมาทับ
เพราะเป็นสัญญาณที่สำคัญกว่าการจับกลุ่ม

**ทำไมมือถือใช้แถบซ้ายแทนพื้นสี** — การ์ดมือถือมีพื้นขาวกับกรอบอยู่แล้ว เปลี่ยนพื้นอีกจะเลอะ

**การเรียงไม่ต้องแก้อะไร** — `mr_history` เรียง `return_date desc, id desc`
แถวใน Lot เดียวกันบันทึกพร้อมกัน จึงติดกันเองอยู่แล้ว

---

## 3.13 🔍 ระบบค้นหายา — อยู่ที่ `lib/drugSearch.js`

**สมองของการค้นหาทั้งหมดอยู่ในไฟล์เดียว** ตั้งใจให้เป็นโมดูลอิสระ
ไม่พึ่ง React · ไม่พึ่ง Next.js · ไม่พึ่งไฟล์อื่นในโปรเจกต์ → **ก๊อปไปโปรเจกต์อื่นได้ทั้งไฟล์**

```js
import { searchDrugsEx, moveHi, thaiToEnglish } from '@/lib/drugSearch';
const { list, used, swapped } = searchDrugsEx(drugs, q, { limit: 14, hotIds: st.hotIds });
```

| ความสามารถ | รายละเอียด |
|---|---|
| เรียงชื่อก่อนความแรง | 🚨 เดิมเรียงความแรงข้ามยา ทำ **Metformin หลุดจากผลค้นหา** ทั้งที่คืนบ่อยที่สุด |
| พิมพ์หลายคำ | `amox clav` · สลับลำดับก็เจอ |
| **ลืมสลับแป้นพิมพ์** | `ทำะดนพทรื` → `metformin` |
| ค้นด้วยตัวย่อ | `cpm` · `hctz` · `inh` (127 ตัว) |
| ค้นด้วยชื่อการค้า | `augmentin` |
| ยาที่คืนบ่อยขึ้นก่อน | จาก `mr_hot_drug_ids` ส่งมาตอน bootstrap |
| ลูกศรวนรอบ | `moveHi()` |

**ลำดับการจัดอันดับ (ห้ามสลับ)**
```
1) เจอที่ต้นชื่อ  2) ตำแหน่งที่เจอ  3) ยาที่ใช้บ่อย
4) ชื่อยาตามตัวอักษร   ← กันความแรงเรียงข้ามยา
5) ความแรงน้อย→มาก     เฉพาะยาชื่อเดียวกัน
```
ตำแหน่งที่เจอถูกถ่วงตามที่มา — ชื่อสามัญ `0+` · **ตัวย่อ `500+`** · ชื่อการค้า `900+`

🚨 **ลืมสลับแป้นต้องทำ 2 อย่างคู่กันเสมอ**
- ไฮไลต์ด้วย `used` (คำที่แปลแล้ว) ไม่ใช่คำที่ผู้ใช้พิมพ์ — ไม่งั้นเจอยาแต่ไม่ไฮไลต์อะไรเลย
- โชว์ป้าย `ค้นว่า metfo` ข้างปุ่ม ✕ — ไม่งั้นผู้ใช้งงว่าทำไมพิมพ์ไทยแล้วเจอยาอังกฤษ

🚨 **กดลูกศรแล้วกรอบต้องเลื่อนตาม** (`componentDidUpdate` ใน `MedReturnApp.jsx`)
กรอบสูง ~290px เห็นราว 5 แถว · ถ้าไม่เลื่อนตาม ผู้ใช้กดเลือกแบบตาบอด
ทำใน `componentDidUpdate` **ไม่ใช่ ref callback** เพราะ ref ถูกเรียกทุกครั้งที่วาดจอ
จะดึงกรอบกลับตอนผู้ใช้เลื่อนดูเองด้วยเมาส์

---

## 3.14 🚨 สถิติจับกลุ่มด้วย "รหัสยา" ไม่ใช่ชื่อ

ชื่อยาใน `mr_return` เป็น snapshot ณ วันบันทึก · แก้ชื่อยาในคลังแล้วยาตัวเดียวกันจะถูกนับเป็นสองตัว

```sql
case when r.drug_id is null then 'txt:' || r.drug_name
     else 'id:' || r.drug_id::text end as gkey
```
มีรหัสใช้รหัส · ไม่มีรหัส (ยาพิมพ์ชื่อเอง) ใช้ชื่อ · **ป้ายที่โชว์ใช้ชื่อล่าสุดจากคลังเสมอ**

แก้ไว้ใน `scripts/008_group_by_drug_id.sql` → `mr_summary` · `mr_top_returned`
ME-DRP ทำเรื่องนี้ไปแล้วตั้งแต่ v0.9.10.2

---

---

## 3.15 💊 หน้าคลังยา — แก้ตาราง `drugs` ของกลางจากเว็บนี้ได้

ไฟล์: `handlers/catalog.js` · `vals/catalog.js` · `pages/catalog.jsx`
หลังบ้าน: `app/api/catalog/route.js` (GET/PUT/POST) · `app/api/catalog/audit/route.js`

**เข้าจากแท็บที่ 4** ในแถบเมนู (บันทึก · ประวัติ · สรุป · คลังยา)
แท็บนี้เรียก `app.openCatalog` ไม่ใช่ `pickScreen('catalog')` เฉย ๆ เพราะต้องสั่งโหลดคลังยาดิบด้วย

🚨 **กฎเหล็กข้อ 1 บอกว่า "ห้ามแก้ตาราง drugs" — ข้อนั้นยังจริงสำหรับการแก้โครงสร้าง**
สิ่งที่หน้านี้ทำคือแก้ **ค่าในแถว** ผ่านหลังบ้านของเว็บเอง ซึ่งพี่กันสั่งให้ทำได้ (13 ส.ค. 2569)
สิ่งที่ยังห้ามคือเพิ่ม/ลบคอลัมน์ หรือย้ายข้อมูลออกจากคอลัมน์เดิม

🚨 **ไม่มี DELETE ใน API โดยตั้งใจ** — ลบยาไม่ได้ ใช้ `hidden = true` แทน
รายการยาคืนเก่าที่อ้างถึงยาตัวนั้นยังต้องแสดงชื่อได้อยู่ (มี snapshot ในแถวตัวเอง)
ลบจริงทำได้ทางเดียวคือเข้า Supabase เอง

🚨 **ต้องเรียก `syncDrugs(true)` หลังบันทึกทุกครั้ง** ไม่ใช่แค่ `loadCatalog(true)`
เพราะ `st.catalog` (ตารางคลังยา) กับ `st.drugs` (รายการที่ช่องค้นหาใช้) เป็นคนละก้อน
แก้แล้วโหลดแค่ก้อนเดียว หน้าบันทึกจะยังค้นเจอชื่อเก่า

**คอลัมน์ที่ 14 — ชื่อที่เห็นตอนค้นหา (กดซ่อน/ขยายได้)**

พี่กันสั่งไว้ 13 ส.ค. 2569 — เป็น**คอลัมน์เดียวในตาราง**ที่ทำแบบนี้
ชื่อประกอบด้วย `buildDrugNames()` ตัวเดียวกับที่ `/api/drugs` ใช้ จึงตรงกับของจริงเสมอ

🚨 **ต้องส่งยา "ทุกตัว" เข้า `buildDrugNames()` รวมตัวที่ซ่อนไว้ด้วย**
ตัวแยกชื่อซ้ำต้องเห็นยาครบถึงจะรู้ว่าต้องต่อท้ายด้วยรูปแบบยาไหม
ถ้าส่งเฉพาะตัวที่กรองแล้ว ชื่อที่ได้จะไม่ตรงกับที่โผล่จริงตอนค้น

**ประวัติการแก้** อ่านจาก `drug_audit` ที่ ME-DRP สร้างไว้ (v0.9.10.0)
trigger เป็น `SECURITY DEFINER` จึง **จับได้แม้แก้ตรงในหน้า Supabase** = ไม่มีทางแก้แล้วไม่ทิ้งร่องรอย
`vals/catalog.js` → `diffRow()` แปลงเป็นภาษาคน ("ชื่อยา — เก่า → ใหม่")

---

## 3.16 🔄 คลังยาซิงก์ข้ามเว็บ — ไม่ใช้ Supabase realtime

ไฟล์: `app/api/drugs/rev/route.js` · `handlers/data.js` → `app.syncDrugs()`

**ทำไมไม่ใช้ realtime ตรง ๆ** — ต้องเอากุญแจ Supabase ไปฝังในหน้าเว็บ
ซึ่งขัดกฎเหล็กข้อ 6 ของโปรเจกต์นี้ · ME-DRP ทำได้เพราะใช้ `NEXT_PUBLIC_` อยู่แล้ว แต่เว็บนี้ตั้งใจไม่มี

**ลายเซ็นคลังยา** = `max(id)` ของ `drug_audit` + จำนวนยา
ใช้ `max(id)` ไม่ใช่ `max(changed_at)` เพราะเป็นตัวเลขเรียงขึ้นอย่างเดียว เทียบง่ายกว่าเวลา

🚨 **`/api/bootstrap` ต้องส่ง `drugRev` มาคู่กับ `drugs` เสมอ**
ถ้าให้ `syncDrugs` ครั้งแรกไปถามลายเซ็นเอง แล้วบังเอิญมีคนแก้ยาระหว่างเปิดเว็บกับการถามครั้งแรก
เว็บจะจำลายเซ็นใหม่ไปเลยทั้งที่ยังถือรายการยาชุดเก่า = **พลาดการแก้ครั้งนั้นถาวร**

🚨 **ต้องล้างแคช `mrv.drugs` ด้วยตอนโหลดใหม่** ไม่งั้นรีเฟรชแล้วของเก่ากลับมา (แคชอายุ 12 ชม.)

**เรียกเมื่อไหร่** — `visibilitychange` (กลับมาที่แท็บ) · `online` (เน็ตกลับมา) · หลังแก้ยาจากหน้าคลังยา

**ไม่มีตัวจับเวลาถามเป็นระยะ** พี่กันทักเองว่าไม่คุ้ม — แก้ชื่อยาปีละไม่กี่ครั้ง
แต่เว็บเปิดค้างทั้งวัน ถามทุก 30 วินาทีจะได้คำขอเปล่าวันละพันกว่าครั้ง

⚠️ **ทดสอบ `visibilitychange` ใน Browser pane ของ Claude ไม่ได้** — ไม่ยิงสัญญาณตอนสลับแท็บ
(ข้อจำกัดเดียวกับ `resize_window`) ใช้ `window.dispatchEvent(new Event('online'))` แทนได้ เส้นทางเดียวกัน

---

## 3.17 🎨 สีความแรง — ยาชื่อเดียวกันหลายความแรงต้องคนละสี

ไฟล์: `vals/record.js` → `makeStColorOf()` · วาดที่ `pages/record.jsx`

**พี่กันสั่ง 13 ส.ค. 2569:** *"ยาที่ตัวเดียวกันและคนละความแรงก็ต้องแยกสี"*
`Morphine 10 · 20 · 30 mg` หยิบสลับกันแล้วอันตรายถึงชีวิต ตาต้องจับได้ตั้งแต่กวาดผ่าน

```js
const ST_COLORS = ['#0b62d6', '#b04a00', '#00808f', '#5b34c9', '#6b6b52'];
//                 น้ำเงินสด   ส้มอิฐ    เทอร์ควอยซ์  ม่วงคราม   เขียวมะกอก
```

- **ทำงานเฉพาะตอนผลค้นหามียาชื่อเดียวกันตั้งแต่ 2 รายการ** — เจอตัวเดียวคงเทาเหมือนเดิม
  (ไม่งั้นทั้งจอเป็นสีรุ้ง สีเลยหมดความหมาย)
- **ทาสีเฉพาะตัวเลข หน่วยคงเทาเดิม** — เน้นจุดที่ต่างกันจริง
- จับกลุ่มด้วย `splitDrugName().base` ไม่ใช่ชื่อเต็ม (ชื่อเต็มมีความแรงติดอยู่)

🚨 **ห้ามใช้สีที่จองไว้แล้ว** — เทล (ชื่อการค้า + ไฮไลต์คำค้น) · ม่วง (ตัวย่อ) · ส้ม (เปอร์เซ็นต์) ·
แดงอมชมพู (ER/IR) · แดง (ทำลาย/ยังไม่ใส่ราคา)
**ต้องอ่านออกทั้งบนพื้นขาวและพื้น `#eef6f1`** (สีแถวตอนถูกลูกศรเลือก)

---

## 3.18 📐 การเรียงผลค้นหา — จัดกลุ่มตามรูปแบบยา

`lib/drugSearch.js` → `FORM_ORDER` + `formOf()`

ยาชื่อเดียวกันที่มีหลายรูปแบบ เรียงตามความแรงล้วนจะกระจัดกระจาย
(Morphine เม็ด 10 mg กับเม็ด 30 mg อยู่คนละปลาย โดยมียาน้ำกับยาฉีดคั่นกลาง)

**เรียงตามรูปแบบก่อน** เพราะเวลารับยาคืน สิ่งแรกที่เห็นคือ**รูปของยาในมือ** ไม่ใช่ตัวเลขความแรง
ลำดับ: ยากินแข็ง → ยากินน้ำ → ยาฉีด → ยาพ่น → ยาหยอด → ยาใช้ภายนอก

**ลำดับการจัดอันดับฉบับสมบูรณ์ (ห้ามสลับ)**
```
1) เจอที่ต้นชื่อ   2) ตำแหน่งที่เจอ   3) ยาที่ใช้บ่อย
4) ชื่อยาตามตัวอักษร   5) รูปแบบยา   6) ความแรงน้อย→มาก   7) ชื่อเต็ม
```

---

## 3.19 ✂️ ก้อนในบรรทัดชื่อยาห้ามถูกตัดขาดกลาง

`vals/record.js` → `fits()` · `pages/record.jsx` → `nw()`

**อาการเดิม:** `Trimethoprim Sulfamethoxazole (Co-trimoxazole) (40 + 200 mg/5 mL)`
ถูกตัดเป็น `(40 + 200` ค้างบรรทัดบน แล้ว `mg/5 mL)` ตกบรรทัดล่าง — สับสนว่าความแรงเท่าไหร่

**วิธี:** ก้อนที่ยาวไม่เกิน **26 ตัวอักษร** ใส่ `white-space:nowrap`
เว้นวรรคระหว่างก้อนทำด้วย `margin-left` อยู่แล้ว จึงยังขึ้นบรรทัดใหม่ระหว่างก้อนได้

🚨 **ก้อนที่ยาวเกินเกณฑ์ต้องยอมให้ตัด** ไม่งั้นล้นออกนอกจอมือถือ
ยาวสุดในคลังคือความแรง 42 ตัวอักษร (`25 + 2 + 5000 mcg/1 mL + mg/1 mL + IU/1 mL`)
วัดบนจอ 375px แล้ว: ก้อนสั้นไม่ขาด · ก้อนยาวตัดได้ · ไม่มีแถบเลื่อนแนวนอนโผล่

---

## 3.20 🐛 ยาชื่อซ้ำ — วงเล็บต่อท้ายทำให้ตัวแยกชื่อพัง

`components/helpers.js` → `splitDrugName()`

ยา 6 คู่ในคลังที่ชื่อกับความแรงซ้ำกันพอดี ถูก `drugName.js` ต่อรูปแบบยาไว้ท้ายชื่อ
→ `Trimethoprim Sulfamethoxazole (Co-trimoxazole) (80 + 400 mg) (injection)`

ความแรงจึงไม่ได้อยู่ท้ายสุดอีกต่อไป เดิมจับไม่ได้เลยเหมาว่าทั้งก้อนคือชื่อยา
**ผลคือความแรงกลายเป็นสีเข้มเหมือนชื่อ และตัวย่อไปโผล่ท้ายสุดแทนที่จะติดชื่อ**
= ยาตัวเดียวกันหน้าตาไม่เหมือนกันในรายการเดียว (พี่กันทักเอง 13 ส.ค. 2569)

`splitDrugName()` จึงคืน 3 ส่วนแล้ว — `{ base, strength, tail }`
กลุ่มที่ 3 รับวงเล็บต่อท้ายที่ **ไม่ได้ขึ้นต้นด้วยตัวเลข** ไว้ต่างหาก

---

## 3.21 💰 ยาที่ยังไม่ใส่ราคา — บันทึกได้ แล้วตีราคาย้อนหลังทีหลัง

**พี่กันสั่งเปลี่ยน 25 ส.ค. 2569:** *"อยากให้กรอกได้ แม้ราคาของยาจะยังไม่เติม"*

เดิม `app.blockNoPrice` กันไม่ให้เพิ่มยาที่ราคาเป็น 0 เข้ารายการเลย (ดูข้อ 3.3.1)
เหตุผลตอนนั้นถูกต้อง — ราคาถูกแช่ไว้ในแถว บันทึกตอนราคา 0 แล้วแถวนั้นมูลค่า 0 ค้างอยู่

**แต่บริบทเปลี่ยน** — ห้องยาเริ่มใช้งานจริงทั้งที่ยังใส่ราคาไม่ครบ (เหลือ 43 ตัวจาก 417)
การกั้นไว้จึงกลายเป็น "บันทึกงานประจำวันไม่ได้" ซึ่งแย่กว่าตัวเลขที่ยังไม่สมบูรณ์

**ตอนนี้ทำงานยังไง**

```js
// components/handlers/record.js
app.blockNoPrice = () => false;      // ไม่กั้นแล้ว
```

```js
// components/handlers/prices.js → savePrices()
body: JSON.stringify({ items: items, backfill: true, by: app.state.recorder || '' })
```

🚨 **`savePrices` ต้องส่ง `backfill: true` เสมอ** — ถ้าลืม แถวที่บันทึกไปตอนยังไม่มีราคา
จะค้างเป็น 0 ตลอดกาล ต้องไปตีราคาย้อนหลังเองทีหลังซึ่งไม่มีใครจำได้

🚨 **กฎแช่ราคายังอยู่ครบ** — `mr_backfill_price` เติมเฉพาะแถวที่ `unit_price = 0`
ไม่ทับแถวที่คิดมูลค่าไว้แล้ว (กฎเหล็กข้อ 12) · ตัวเลข KPI ที่รายงานไปแล้วไม่มีทางขยับ

**ป้าย "ยังไม่ใส่ราคา" ยังอยู่ทุกที่** — ผลค้นหา · รายการในครั้งนี้ · หน้าคลังยา
ผู้ใช้ยังเห็นว่าตัวไหนรอราคาอยู่ แค่ไม่ถูกกั้นไม่ให้บันทึก

---

## 3.22 🐛 วันที่ค้างวันเก่าจากร่างที่กู้มา

**อาการที่พี่กันเจอ (25 ส.ค. 2569):** เปิดเว็บวันที่ 25 แต่ช่องวันที่ยังเป็น 10 ส.ค.
= ยาที่รับคืนวันนี้จะถูกบันทึกเป็นวันเก่าทั้งหมด

**ต้นเหตุ** — ร่างใน localStorage เก็บ `date` ไว้ด้วย พอกู้ร่างมาแล้ว `checkDayRollover`
เช็คแค่ `wasToday = (date === today)` ซึ่งเป็นเท็จ (10 ≠ 25) เลยไม่ยอมอัปเดต
เงื่อนไขนั้นตั้งใจไว้กันการทับวันที่ผู้ใช้เลือกเอง แต่ไม่ได้เผื่อกรณีร่างค้างข้ามวัน

**วิธีแก้**

```js
const wasToday = app.state.date === app.state.today;
const isPast = app.state.date && app.state.date < now;   // ← เพิ่ม
app.setState({ today: now, date: (wasToday || isPast) ? now : app.state.date });
```

และเรียก `app.checkDayRollover()` ท้าย `boot()` ทันที **ไม่รอตัวจับเวลา 1 นาที**
ไม่งั้นผู้ใช้เห็นวันเก่าค้างอยู่นาทีแรกแล้วกดบันทึกไปก่อน

⚠️ **ยังเลือกวันย้อนหลังเองได้ปกติ** — เงื่อนไขนี้ทำงานเฉพาะตอนทวนวันข้ามคืน/ข้ามวันเท่านั้น
ไม่ได้ทับค่าที่ผู้ใช้เพิ่งเลือกในรอบการใช้งานเดียวกัน

---

## 3.23 🐛 ป๊อปยืนยันต้องปิดที่จุดเดียว

**อาการ:** กด "ยืนยันลบ" ในหน้าบันทึกแล้วป๊อปค้าง ดูเหมือนกดไม่ติด (จริง ๆ ลบไปแล้ว)

**ต้นเหตุ** — `confirmRun` เรียก `run()` เฉย ๆ แล้วปล่อยให้แต่ละ handler ปิดป๊อปเอง
`handlers/history.js` ปิดเอง (`confirm: null` ในฟังก์ชันลบ) แต่ `app.removeRow` ลืมปิด

**วิธีแก้ — ปิดที่ปุ่มยืนยันจุดเดียว** (`vals/history.js` → `confirmRun`)

```js
confirmRun: () => {
  const c = app.state.confirm;
  if (!c) return;
  app.setState({ confirm: null }, () => c.run());   // ปิดก่อน แล้วค่อยทำงาน
},
```

🚨 **handler ที่ใช้ป๊อปยืนยันไม่ต้องปิดเองอีก** — ตรงกับกฎ "อะไรที่คล้ายกันรวมเป็นระบบกลางตัวเดียว"
เพิ่มป๊อปใหม่ทีหลังก็ทำงานถูกทันทีโดยไม่ต้องจำ

---

## 3.24 ✍️ ช่องผู้บันทึก — ห้ามติ๊กชื่อไว้ให้

**พี่กันสั่ง 25 ส.ค. 2569:** *"ค่าตั้งต้น มันจะไม่เลือกชื่อไว้ และจุดนี้ต้องกรอกเท่านั้นถึงกดส่งได้"*

เดิม `boot()` ติ๊กคนที่เซ็นล่าสุดไว้ให้เพื่อความเร็ว

```js
recorder: app.state.recorder || data.setting.lastRecorder || '',   // ❌
recorder: app.state.recorder || '',                                 // ✅
```

**เหตุผล** — คอมห้องยาเป็นเครื่องกลางที่ใช้ร่วมกันทั้งเวร คนถัดไปที่มานั่ง
จะเห็นชื่อคนก่อนติ๊กอยู่แล้วกดบันทึกโดยไม่ทันสังเกต = ล็อตนั้นเซ็นในชื่อคนอื่น สืบกลับผิดคน

`lastRecorder` ยังถูกจำไว้ในฐานเหมือนเดิม แค่ไม่เอามาติ๊กให้
การบังคับกรอกอยู่ใน `app.save()` มาตั้งแต่แรกแล้ว (กดบันทึกโดยไม่เลือกชื่อไม่ได้)

---

## 3.25 📐 หน้าคลังยา — ตรึงแถบค้นหากับหัวตาราง

🚨🚨 **ห้ามมี `overflow-x:auto` หรือ `max-height` ครอบ `<table>`**

`overflow-x` ทำให้ `overflow-y` กลายเป็น `auto` โดยปริยาย → `position:sticky` ของ `<th>`
ไปผูกกับ**กรอบนั้น**แทนพื้นที่เลื่อนของเว็บ พอกรอบไม่จำกัดความสูงก็ไม่มีที่ให้ติด = ไม่ตรึง
(ME-DRP เจอปัญหาเดียวกันแล้วหัวตารางลอยไปโผล่กลางตาราง)

**พื้นที่เลื่อนของเว็บนี้คือ `app.scrollRef` ใน `shell.jsx` ไม่ใช่ทั้งหน้า**
→ `window.scrollTo` ใช้ไม่ได้ · `top:0` ของ sticky หมายถึงขอบบนกรอบนั้น

```js
// handlers/catalog.js
app.catToTop = () => {
  const el = app.scrollRef.current;
  if (el) el.scrollTo({ top: 0, behavior: 'smooth' });
};
```

**หัวตารางตรึงใต้แถบค้นหา** — ระยะวัดจริงด้วย `ResizeObserver` เขียนลง `--cathead`
(`catHeadRef` ใน `MedReturnApp.jsx` · แบบเดียวกับ `--histhead` ของหน้าประวัติ)
**ตั้งเลขตายตัวไม่ได้** เพราะชิปตัวกรองขึ้นบรรทัดใหม่เองเมื่อจอแคบ

วัดจากโครมจริง (จอ 1517×641): แถบค้นหา top 59 · หัวตาราง top 224 — ต่อกันพอดี ไม่มีช่องว่าง

**คอลัมน์ราคา** — `/api/catalog` join `mr_drug_price` มาด้วย
ยาที่ยังไม่ใส่ราคาโชว์ป้ายแดง ไม่ใช่ `0.00` (กันเข้าใจผิดว่าราคาศูนย์บาทจริง)
เรียงตามราคาต้องเทียบแบบตัวเลข ไม่ใช่ `localeCompare`

---

## 3.26 ✏️ แก้ไขล็อตย้อนหลัง + เก็บร่องรอยทุกการแก้

**พี่กันสั่ง 25 ส.ค. 2569:** *"ตรงรายการ lot เราอยากให้แก้ไขได้หน่อย แบบแก้ชื่อคน แก้ว่ากรอกที่ไหน
หรือจำนวน ให้มันเปิดมาแล้วเป็นทุกตัวในลอตเลย"* + *"เอาเก็บร่องรอยด้วย"*

**ไฟล์ที่เกี่ยวข้อง**

| ชั้น | ไฟล์ |
|---|---|
| ฐานข้อมูล | `scripts/010_lot_edit_audit.sql` — ตาราง `mr_lot_audit` + `mr_lot_rows()` + `mr_lot_log()` |
| หลังบ้าน | `app/api/lots/[lot]/route.js` — GET อ่านล็อต+ประวัติ · PATCH แก้ |
| ตรรกะ | `components/handlers/lots.js` (ท้ายไฟล์) |
| ค่าหน้าจอ | `components/vals/lots.js` → `lotEditVals()` |
| หน้าจอ | `components/pages/lotedit.jsx` (ไฟล์ของตัวเอง เพราะยาวพอ ๆ กับทั้งหน้ารายการล็อต) |

**แก้ได้ 2 ระดับในคำขอเดียว**

```
ระดับล็อต → recorded_by · source · return_date · hn   (อัปเดตทุกแถวที่ lot_no ตรงกัน)
ระดับแถว  → qty · disposition                          (อัปเดตทีละ id)
```

🚨 **ห้ามแตะ `unit_price` เด็ดขาด** — ไม่มีช่องในหน้าจอ และหลังบ้านก็ไม่รับ
ทดสอบแล้วด้วยการยิง `{"items":[{"id":52,"unit_price":999}]}` → ราคายังเป็น `2.5000` เท่าเดิม
มูลค่าคำนวณจาก `qty × unit_price` อยู่แล้ว แก้จำนวนก็พอให้ตัวเลขถูกต้อง

🚨 **แก้ระดับล็อตก่อน แล้วค่อยรายแถว** — ถ้าสลับกัน การอัปเดตทั้งล็อตจะทับค่ารายแถวที่เพิ่งตั้ง
(ตอนนี้คนละคอลัมน์กันจึงยังไม่ชนจริง แต่กันไว้เผื่อวันหน้าเพิ่มช่องที่ซ้อนกัน)

🚨 **เขียนประวัติหลังแก้สำเร็จเท่านั้น** — ถ้าเขียนก่อนแล้วการแก้ล้ม ประวัติจะโกหก

**ทำไมต้องเก็บร่องรอย**

การแก้ชื่อผู้บันทึกย้อนหลัง = เปลี่ยนหลักฐานว่าใครเซ็นรับล็อตนั้น
ถ้าไม่เก็บไว้ จะไม่มีทางตอบผู้ตรวจได้ว่าตัวเลขที่เห็นวันนี้เคยเป็นอะไรมาก่อน
เก็บเป็น**รายช่อง** ไม่ใช่ทั้งแถวแบบ `drug_audit` เพราะต้องตอบให้ได้ว่า
"ใครเปลี่ยนแหล่งที่มาจากอะไรเป็นอะไร เมื่อไร"

**บทเรียนที่เสียเวลาพี่กันไปจริง**

เวอร์ชันแรกผูกชื่อผู้แก้กับ `app.state.recorder` ซึ่งเป็นช่องผู้บันทึกใน**หน้าบันทึก**
คนที่มาแก้ล็อตย้อนหลังไม่จำเป็นต้องเปิดหน้าบันทึกแล้วเลือกชื่อตัวเองก่อน — มันคนละงานกัน
ผลคือพี่กันกดยืนยันแล้วโดนตีกลับเงียบ ๆ เพราะข้อความเตือนหายใน 2 วินาที **ดูเหมือนปุ่มเสีย**

→ ย้ายมาเป็น**ช่อง "ผู้ที่แก้ไข" ในหน้าต่างยืนยันเอง** และ**ปิดปุ่มยืนยันไว้จนกว่าจะเลือก**
พร้อมป้ายแดง "ต้องเลือกก่อนยืนยัน" — ไม่มีทางกดแล้วเงียบอีก

**⚠️ กติกาที่ใช้ร่วมกับหน้าบันทึก** — ช่องแก้จำนวนในหน้าต่างนี้ใช้กฎเดียวกับหน้าบันทึกทุกข้อ
(พิมพ์สูตรได้ · Enter 2 จังหวะ · ปุ่ม ✓ ปิดเมื่อค่าเดิม · โครงคงที่กรอบไม่เลื่อน)
ถ้าแก้ที่หนึ่ง ต้องแก้อีกที่ให้ตรงกันเสมอ ไม่งั้นมือจะจำผิดสลับไปมา

---

## 3.27 🧮 ช่องจำนวนคิดเลขได้ (เฉพาะคอม)

**พี่กันสั่ง 25 ส.ค. 2569:** *"ใส่ 25+25 แล้วเอนเทอร์ นอกจากจะเปลี่ยนช่อง อยากให้มันบวกให้ด้วย"*
แล้วสั่งเพิ่มทีหลังว่า *"พิมพ์ + - * / ได้เลย และวงเล็บได้ด้วย"*

**ที่มาของโจทย์** — นับยาคืนจริงมักได้เป็นกอง ๆ แผงละ 10 สามแผง กับเศษอีก 4
เดิมต้องคิดในหัวก่อนแล้วค่อยพิมพ์ผลรวม เป็นจุดที่พลาดง่ายและตรวจย้อนไม่ได้

**ตัวคิดเลขอยู่ที่ `components/helpers.js`**

```
cleanQtyExpr()  ยอมให้เครื่องหมายกับวงเล็บค้างในช่องระหว่างพิมพ์
evalQty()       คิดสูตร — คูณหารก่อน วงเล็บก่อนทุกอย่าง
isQtyExpr()     ยังเป็นสูตรที่รอคิดอยู่ไหม
splitResolved() แยก "25+25=50" เป็นสูตรกับคำตอบ
exprText()      แปลง 25*4 → 25 × 4 สำหรับโชว์
```

🚨 **ห้ามใช้ `eval()` หรือ `new Function()` เด็ดขาด** แม้จะสั้นกว่ามาก
ทั้งสองตัวรันโค้ดอะไรก็ได้ที่อยู่ในข้อความ และ CSP ของเว็บจริงบล็อกไว้อยู่แล้ว
(`next.config.js` ใส่ `'unsafe-eval'` เฉพาะตอน dev — **เว็บจริงจะพังเงียบ ๆ หาสาเหตุยากมาก**)
วิธีที่ใช้คือไล่อ่านทีละชั้น บวกลบ → คูณหาร → ตัวเลข/วงเล็บ ชั้นล่างถูกเรียกก่อนเสมอ

🚨 **ต้องทนกับสูตรที่ยังพิมพ์ไม่จบ** เพราะผลลัพธ์ถูกคิดใหม่ทุกตัวอักษรที่พิมพ์
`"25+"` · `"(25+25"` · `"3*("` ต้องไม่พังและไม่กระพริบเป็นค่าอนันต์

🚨 **ห้ามเอาไปใช้กับป๊อปอัปฝั่งมือถือ** — พี่กันสั่งชัดว่าทำเฉพาะคอม
`vals/sheet.js` ยังใช้ `cleanQty`/`qtyNum` ตัวเดิม ไม่ต้องแก้อะไรเลย
(แป้นตัวเลขมือถือไม่มีเครื่องหมาย + − × ÷ ให้กดอยู่แล้ว)

**Enter 2 จังหวะ**

```
พิมพ์ 25+25 → Enter ครั้งแรก → ช่องกลายเป็น "25+25=50" เลข 50 เขียวเด่น
             → Enter ครั้งที่สอง → เพิ่มรายการ
```

พี่กันสั่งเปลี่ยนกลางทางจาก "Enter ทีเดียวเพิ่มเลย" เพราะอยากเห็นผลลัพธ์ก่อนของเข้ารายการ

🚨 **เก็บสูตรไว้ในช่องด้วย ต่อ `=ผลลัพธ์` ท้าย ไม่ใช่แทนที่ด้วยตัวเลขเปล่า**
`"25+25=50"` ตรวจย้อนได้ว่ามาจากการบวกอะไร ถ้าเหลือแค่ `"50"` จะไม่รู้ที่มา
และ `evalQty` **คิดจากสูตรฝั่งซ้ายของ `=` เสมอ** — กันคนไปแก้ตัวเลขหลัง `=` เอง
(ทดสอบแล้ว: `"25+25=999"` ยังคืน 50)

🚨 **ช่องกรอกทำตัวหนาเฉพาะบางส่วนไม่ได้** จึงวาดข้อความซ้อนทับแล้วทำตัวอักษรจริงโปร่งใส
ต้องใช้ฟอนต์ ขนาด และระยะขอบชุดเดียวกันทั้งสองชั้น ไม่งั้นขีดกะพริบจะไม่ตรงตัวอักษร

**แป้นเครื่องคิดเลข** — `CALC_KEYS` ใน `vals/record.js` · 4 คอลัมน์ 5 แถว
แป้นไม่ได้คิดเลขเอง มันแค่พิมพ์ตัวอักษรลงช่อง แล้ว `evalQty` คิดให้ตอนกด Enter
ทำแบบนี้เพื่อให้ "พิมพ์เอง" กับ "กดแป้น" เดินทางเดียวกัน ไม่มีทางให้ผลต่างกัน

🚨 **ต้องมีปุ่ม `+` ครบ** — รอบแรกลืมใส่ ทั้งที่ `25+25` คือโจทย์แรกสุดของพี่กัน

---

## 3.28 ✎ แก้จำนวนในตารางรายการครั้งนี้

**พี่กันสั่ง 25 ส.ค. 2569:** *"กดเปลี่ยนจำนวนได้ในนี้เลย แม้ว่าจะกดเอนเทอร์ลงมาแล้ว"*
แล้วสั่งเพิ่มว่า *"ไม่อยากให้เอาเมาส์ไปจิ้มแล้วกดที่เลข อยากให้มีปุ่มกดที่สื่อตรง ๆ"*

**กฎ 4 ข้อของช่องนี้ — ต้องเหมือนกันทั้งหน้าบันทึกและหน้าต่างแก้ไขล็อต**

1. **ปุ่มดินสอบอกชัด ๆ ว่ากดแก้ได้** ไม่ใช่ให้เดาว่าจิ้มที่ตัวเลขได้
2. **กดแล้วไฮไลต์ตัวเลขไว้ให้** (`onFocus={(e) => e.target.select()}`) พิมพ์ทับได้เลย
3. **ปุ่ม ✓ ปิดเมื่อค่ายังเท่าเดิม** — กดได้ทั้งที่ไม่มีอะไรเปลี่ยน ผู้ใช้จะไม่รู้ว่าตกลงไปแล้วได้อะไร
4. 🚨 **โครงคงที่ ปุ่ม 2 ช่องเสมอ** ตอนไม่แก้ช่องที่สองเป็นที่ว่างเปล่า

```
ปกติ: [เลข 150px][✎ 26px][ที่ว่าง 26px]
แก้:  [ช่อง 150px][✓ 26px][✕ 26px]
```

ถ้าปล่อยให้จำนวนปุ่มไม่เท่ากัน **กรอบตัวเลขจะเลื่อนซ้ายตอนกดแก้** ตาต้องไล่หาใหม่ทุกครั้ง
วัดจริงหลังแก้: ขอบขวาอยู่ที่ 618px เท่ากันทั้งสองสถานะ

🚨 **ความสูงแถวต้องคงที่ 50px** ทั้ง 3 สถานะ — ป้ายผลลัพธ์สูตรใช้ `position:absolute` จึงไม่ดันแถว

---

## 3.29 💊 ชื่อยาเต็มยศทุกที่ + สีเหมือนตอนค้นหา

**พี่กันสั่ง 25 ส.ค. 2569:** *"ชื่อยาที่แสดง ใส่แบบเต็มยศ ไม่งั้นถ้าเก็บทั้งยากินกับฉีด
มันแยกไม่ออกเลย"* + *"ชื่อเต็ม เราอยากได้เหมือนตอนค้นหา พวกสีต่าง ๆ ควรมาด้วย"*

**`lib/drugName.js`** — เอาชั้นแรก (ชื่อเปล่า ๆ) ออก ให้เริ่มที่ชั้นที่มี `form` เลย

```js
const LEVELS = [
  (d, base) => (t(d.form) ? base + ' (' + t(d.form) + ')' : base),   // ← ชั้นแรกมี form แล้ว
  ...
];
```

⚠️ **มีผลกับของใหม่เท่านั้น** — แถวที่บันทึกไปแล้วยังเป็นชื่อเดิม เพราะชื่อถูกแช่ไว้ในแถว
ไม่ใช่บั๊ก แต่เป็นกฎแช่ข้อมูล · Top 10 หน้าสรุปจับกลุ่มด้วย `drug_id` จึงไม่กระทบ

**`vals/record.js` → `nameParts()`** — แยกชิ้นส่วนชื่อยาสำหรับวาดพร้อมสี
ใช้ร่วมกันระหว่างผลค้นหา · ตารางรายการครั้งนี้ · หน้าต่างแก้ไขล็อต
ชื่อยาจะได้หน้าตาเหมือนกันทุกที่ ไม่ต้องเขียนซ้ำ 3 รอบ

**`handlers/data.js` → `refreshDraftNames()`** — ทวนชื่อในร่างให้ตรงกับคลังยาทุกครั้งที่เปิดเว็บ

🚨 **ทำเฉพาะแถวที่ยังไม่บันทึกเท่านั้น** แถวที่เข้าฐานแล้วห้ามแตะเด็ดขาด
🚨 **อัปเดตแค่ชื่อกับหน่วย ไม่แตะราคา** — ราคาถูกล็อกตั้งแต่ตอนกดเลือกยา
ถ้าอัปเดตราคาด้วย ของที่กรอกค้างข้ามวันจะเปลี่ยนมูลค่าเองโดยผู้ใช้ไม่รู้ตัว

---

## 3.30 🏥 แหล่งที่มา — เพิ่ม IPD เอา ward ออก

`lib/format.js` → `SOURCES` เป็น**จุดกลางจุดเดียว** ทั้งช่องเลือก · ตัวกรอง · โดนัทหน้าสรุป ·
หัวคอลัมน์ไฟล์ส่งออก และ allowlist ฝั่งเซิร์ฟเวอร์ดึงไปใช้หมด

🚨 **เพิ่มตัวใหม่ต้องแก้ check constraint ในฐานด้วย** ไม่งั้นบันทึกไม่ผ่าน
(`scripts/009_source_ipd.sql` — `mr_return.source` และ `mr_setting.default_source`)

**เอา `ward` (หอผู้ป่วย) ออก** — พี่กันบอกซ้ำซ้อนกับห้องยา IPD
ยาที่คืนจากผู้ป่วยในเดินผ่านห้องยา IPD เสมอ แยกไว้ทำให้เลือกลังเล
✅ ตรวจก่อนลบว่าไม่มีแถวไหนในฐานใช้ค่านี้เลย (`ncd` 33 แถว · `pcu` 3 แถว) จึงไม่กระทบของเก่า

⚠️ **check constraint ยังยอมรับ `ward` อยู่** ตั้งใจคงไว้ — วันหน้าอยากได้กลับมาจะได้แก้ไฟล์เดียว

⚠️ **ลำดับในรายการคุมสีในโดนัทหน้าสรุป** (`greens[i*2]`) แทรกตรงกลางแล้วสีของตัวที่อยู่หลังจะขยับ

---

## 3.31 🐛 บั๊กหน้าตา 3 ตัวที่เจอ 25 ส.ค. 2569

**1. ปุ่มที่เปลี่ยนสีตามสถานะ ต้องสลับคลาส hover ด้วย**

`pages/history.jsx` ปุ่ม "กลับไปดูรายการปกติ" ใช้ `className="hv-bg-f6"` ตายตัว
ตอนอยู่ในถังขยะปุ่มเป็นพื้นเขียวตัวหนังสือขาว → ชี้เมาส์แล้วพื้นกลายเป็นขาวนวล **ตัวหนังสือขาวกลืนหายไป**

```jsx
className={(V.histTrash ? 'hv-teal' : 'hv-bg-f6') + ' tap'}
```

🚨 **ตระกูลเดียวกับบั๊ก `border-color` เดี่ยวใน hover ของ ME-DRP** — เขียน hover ครึ่งเดียวแล้วอีกครึ่งค้างของเก่า

**2. ป๊อปที่กดพื้นหลังไม่ปิด ต้องมี Esc เสมอ**

ใบสรุป Lot กดพื้นหลังไม่ปิดโดยตั้งใจ (กันปิดพลาดตอนกำลังจะสั่งพิมพ์)
แต่ไม่มี Esc ด้วย → เหลือทางเดียวคือเล็งปุ่มปิดมุมบนขวา
เพิ่ม `slipLot` เข้า `_onKey` แล้ว พร้อมกับหน้าต่างแก้ไขล็อต 3 ชั้น

**3. วันที่อัปเดตล่าสุดค้างที่ 5 สิงหาคม** — ดูข้อ 0.1

---

## 3.32 📐 หัวคอลัมน์ต้องตรงกับข้อมูลข้างใน

พี่กันสั่งแยกรายคอลัมน์ ไม่ใช่จัดแบบเดียวกันทั้งหมด

| คอลัมน์ | หัว | ข้อมูล | เหตุผล |
|---|---|---|---|
| จำนวน | **ตรงเลข** | ชิดขวา | ข้างในมีปุ่มดินสอต่อท้าย หัวจึงต้องถอยเข้ามา `padding-right:69px` (26+26+ช่องไฟ 5×2 +7) |
| ราคา/หน่วย | กลาง | **กลาง** | ทุกตัวมีทศนิยม 2 ตำแหน่งเสมอ หลักจุดเยื้องกันน้อยมาก |
| มูลค่า | ชิดขวา | ชิดขวา | ตัวเลขยาวไม่เท่ากัน (15.00 กับ 1,200.00) ต้องชิดขวาให้หลักตรง |
| สถานะ · ล้าง | กลาง | กลาง | — |

🚨 **`padding-right:69px` ผูกกับความกว้างปุ่ม** เปลี่ยนขนาดปุ่มเมื่อไหร่ต้องแก้เลขนี้ตาม

**หน้ารายการ Lot** — กล่องซ้ายต้องเป็น**ความกว้างคงที่ ไม่ใช่ `min-width`**
ไม่งั้นกล่องยืดตามความยาวชื่อคน (วัดจริง 157px กับ 180px) แล้วคอลัมน์ "X รายการ" เหลื่อมกัน 23px
ตั้ง `width:208px` · ชื่อยาวเกินให้ขึ้นบรรทัดใหม่ ไม่ตัดทิ้ง เพราะชื่อผู้บันทึกเป็นข้อมูลสืบกลับ
## 3.4.2 ชื่อการค้า (`drugs.brand`) · ตัวย่อ (`drugs.abbrev`)

มี 37 ตัวจาก 417 · แสดงในวงเล็บสีเทล `#2f7d5d` ตัวหนา **เฉพาะตัวที่มี**
ทำตามแบบ ME-DRP ที่พี่กันชี้ให้ดู · โชว์ทั้งผลค้นหาหน้าบันทึกและหน้าตั้งราคายา

**ค้นด้วยชื่อการค้าได้** (`rankResults` ใน `vals/derive.js`) — เภสัชกรจำ `Augmentin`
ได้ก่อน `Amoxicillin + Clavulanic acid` · จัดอันดับให้ที่เจอในชื่อสามัญมาก่อน

🚨 **บรรทัดชื่อยาห้ามใช้ `white-space:nowrap` + `text-overflow:ellipsis`**
ยาชื่อยาวอย่าง `Gramicidin + Neomycin sulfate + Polymyxin b sulfate` จะทำให้
**ความแรงโดนตัดและชื่อการค้าหายทั้งอัน** ทั้งที่ผู้ใช้ค้นด้วยชื่อการค้าอยู่แท้ ๆ
ความแรงเป็นข้อมูลความปลอดภัย (`25 + 2 + 5000` สามค่าคนละหน่วย) ซ่อนไม่ได้
ใช้ `overflow-wrap:anywhere` ยอมให้แถวสูงขึ้นเฉพาะยาชื่อยาว

---

## 3.4.3 บรรทัดชื่อยา — ลำดับและสี (ทำตาม ME-DRP)

พี่กันเทียบภาพกับ ME-DRP แล้วสั่งให้ทำให้เหมือน · ลำดับนี้ห้ามสลับ

```
Chlortetracycline  10 mg/1 g  (1%)  ointment  (Aureomycin®)
      ชื่อ           ความแรง     %     รูปแบบ      ชื่อการค้า
     #1e2420        #6b746e   #96650f  #414a44     #2f7d5d
                                 ↑ส้ม              ↑เทลหนา
IV · หลอด
↑#6b746e  ↑#9aa19c
```

| ส่วน | มาจาก | แสดงเมื่อ |
|---|---|---|
| ความแรง | `strength` + `unit` | มีค่า |
| `(1%)` **ส้มอำพัน** | `percent` | มีค่า (19 ตัว) |
| รูปแบบยา | `form` | มีค่า |
| ชื่อการค้า **เทล** | `brand` | มีค่า (37 ตัว) |
| ทางให้ยา | `route` | มีค่า (409 ตัว) |

**ทำไมสีส้ม `#96650f`** — เทลถูกใช้กับชื่อการค้าและคำที่ค้นเจอแล้ว · แดงใช้กับ "ทำลาย/ยังไม่ใส่ราคา"
ส้มเป็นสีที่ยังว่างและตัดกับพื้นขาวได้ชัด

### 🚨 ยาสูตรผสม — ความแรงต้องอยู่ในวงเล็บ

```
❌ Ampicillin + Sulbactam 2 + 1 g      ← เครื่องหมาย + สองที่ แยกไม่ออกว่าตรงไหนคืออะไร
✅ Ampicillin + Sulbactam (2 + 1 g)
```

ทำใน `lib/drugName.js` → `baseName()` เมื่อ `strength` มี `+` · มีผลกับยา **49 ตัว**
ME-DRP ทำแบบนี้มาตลอด (`lib/helpers.ts` → `drugFlatLine`)

🚨 **`splitDrugName()` ใน `helpers.js` ต้องรับวงเล็บด้วย และในวงเล็บต้องขึ้นต้นด้วยตัวเลขเท่านั้น**

```js
/^(.*?)\s(\(\d[^)]*\)|\d[\d\w./+\-% ]*)$/
```

ถ้าไม่บังคับให้ขึ้นต้นด้วยตัวเลข ยาชื่อซ้ำที่ระบบต่อท้ายด้วย `(vial)` `(injection)`
(ดูข้อ 9 · `LEVELS` ใน `drugName.js`) จะถูกจับวงเล็บนั้นมาเป็น "ความแรง" แทน

⚠️ **ก่อนแก้สูตรชื่อยาทุกครั้ง ต้องเช็คว่ากระทบ snapshot เก่าไหม**
`mr_return.drug_name` แช่ชื่อ ณ วันบันทึกไว้ · ถ้าชื่อใหม่ไม่ตรงกับชื่อเก่า
**Top 10 ในหน้าสรุปจะแยกยาตัวเดียวกันเป็นสองก้อน** (จับกลุ่มด้วยชื่อ)

```sql
select count(*) from mr_return where deleted_at is null and drug_name like '%+%';
```

**ข้อจำกัดที่รู้อยู่:** ยาชื่อซ้ำ 6 คู่ที่ถูกต่อท้ายด้วย `(vial)` `(injection)`
จะไม่ถูกแยกสีชื่อ/ความแรง (ทั้งก้อนเป็นสีเข้ม) — เป็นแบบนี้มาตั้งแต่แรก แจ้งพี่กันแล้ว

---

## 3.33 💰 ราคากับสีเม็ดยาย้ายเข้าคลังยากลาง

**พี่กันสั่ง 25 ส.ค. 2569:** *"ราคายาเก็บตอนไหน ตอนนี้ Supabase ไหม เราบอกแล้วนี่
คลังยาทั้งหมดคือส่วนกลางที่ทุกเว็บใช้ด้วยกัน ทุกอย่าง"*

**คอลัมน์ใหม่ในตาราง `drugs`**

| คอลัมน์ | เก็บอะไร |
|---|---|
| `unit_price` | ราคาต่อหน่วย `numeric(12,4)` — ยาถูก ๆ จาก HIS มาเป็น 0.4567 |
| `unit_th` | หน่วยนับไทยเฉพาะตัวนี้ (ทับค่าเริ่มต้นจาก `form`) |
| `price_note` · `price_needs_check` | ที่มาของราคา · ธงว่ายังรอเภสัชกรตรวจ |
| `pill_color` · `pill_color_hex` | ชื่อสีเม็ดยาไทย · รหัสสี (เว้นว่างได้ ระบบเติมจากชื่อสีให้) |

🚨 **`mr_drug_price` ไม่มีโค้ดไหนอ่านแล้ว** แต่ยังไม่ลบทิ้ง เก็บเป็นสำเนาสำรอง
ก่อนย้ายตรวจทีละแถวแล้ว **ไม่มีตัวไหนคลาดเคลื่อนเลยสักตัวจาก 417**

🚨 **`/api/prices` เปลี่ยนจาก `upsert` เป็น `update` รายแถว**
`upsert` ใช้กับ `drugs` ไม่ได้ เพราะแถวมีอยู่แล้วและมีคอลัมน์อื่นอีกยี่สิบกว่าช่อง
ถ้า upsert ด้วยชุดช่องไม่ครบ ช่องที่ไม่ได้ส่งจะโดนล้างเป็น NULL (บั๊กตระกูลเดียวกับข้อ 3.6)
ตอนนี้ยิงทีละ 20 แถวขนานกัน แล้วรวมผล

**`lib/drugPillColors.js`** — `pillColorOf(drug)` คืน `{label, color}` · `COLOR_HEX` แปลงชื่อสีไทยเป็นรหัสสี
🚨 **ไฟล์นี้ก๊อปไปวางที่ ME-DRP ด้วยแล้ว (`lib/drugPillColors.ts`) แก้ที่ไหนต้องก๊อปไปอีกที่เสมอ**
(กฎเดียวกับ `drugSearch.js`)

**ทำไมต้องมีสีเม็ดยา** — Warfarin 2 mg ส้ม · 3 mg น้ำเงิน · 5 mg ชมพู
เภสัชกรกับคนไข้จำยาพวกนี้ด้วยสีมากกว่าตัวเลข หน้าจอต้องพูดภาษาเดียวกับของจริงที่อยู่ในมือ
🚨 **เก็บในฐานไม่ฝังในโค้ด** — เพิ่มยาตัวใหม่แค่กรอกในหน้าคลังยา ไม่ต้องแก้โค้ดทั้งสามเว็บ

---

## 3.33 💰 ราคากับสีเม็ดยาย้ายเข้าคลังยากลาง

**พี่กันสั่ง 25 ส.ค. 2569:** *"คลังยาทั้งหมดคือส่วนกลางที่ทุกเว็บใช้ด้วยกัน ทุกอย่าง"*

ตาราง `drugs` มีคอลัมน์ `unit_price` `unit_th` `price_note` `price_needs_check`
`pill_color` `pill_color_hex` แล้ว · ย้ายมาจาก `mr_drug_price` ครบ 417 แถว ตรวจแล้วไม่คลาดเคลื่อน

🚨 **`mr_drug_price` ไม่มีโค้ดไหนอ่านแล้ว** แต่ยังไม่ลบ เก็บเป็นสำเนาสำรอง
🚨 **`/api/prices` ใช้ `update` รายแถว ไม่ใช่ `upsert`** — upsert กับตารางที่มี 20+ คอลัมน์
จะล้างช่องที่ไม่ได้ส่งไปเป็น NULL (บั๊กตระกูลเดียวกับข้อ 3.6)

**`lib/drugPillColors.js`** — ก๊อปไป ME-DRP แล้ว (`.ts`) 🚨 แก้ที่ไหนต้องก๊อปไปอีกที่เสมอ

---

## 3.34 🏷 ชื่อยา — ดึงจากคลังเสมอ และวาดพร้อมสีด้วยตัวกลางตัวเดียว

**พี่กันสั่ง 25 ส.ค. 2569:** *"เราอยากให้ชื่อเดียวมันลิงก์ไปทุกอย่างในเว็บ แม้ชื่อจะเปลี่ยนไปก็ตาม
เรามีระบบ id อยู่นี่นา"* · *"ชื่อจัดเต็ม มียี่ห้อด้วยนะ"* · *"ไหนสีแบบที่ช่องค้นหา"*

### ชื่อมาจากไหน

| ที่ | ประกอบด้วย |
|---|---|
| ฝั่งฐาน | `mr_drug_label(d, fallback)` — รับทั้งแถวจาก `drugs` (ดู `scripts/011`) |
| ฝั่งเว็บ | `vals/record.js` → `nameParts(drug, stColor)` |
| ตัววาด | `components/pages/drugname.jsx` → `renderDrugName(parts, opts)` |

**ลำดับ (ห้ามสลับ):** ชื่อสามัญ (ตัวย่อ) (สีเม็ด) ความแรง (%) รูปแบบยา (ER/IR) (ยี่ห้อ)
🚨 **รูปแบบยาไม่ใส่วงเล็บ ที่เหลือใส่** — ตามภาพผลค้นหาที่พี่กันชี้ให้ดู
🚨 **แก้สูตรที่ไหน ต้องแก้อีกสองที่ให้ตรงกันเสมอ** (`lib/drugName.js` · `mr_drug_label` · `nameParts`)

### 🚨 ห้ามเดาชิ้นส่วนชื่อจากข้อความ

`nameParts` ใช้**ข้อมูลดิบรายช่อง**เป็นหลัก (`generic` `strength` `strengthUnit` `percent`
`release` `form` `brand` `abbrev`) ทางเดาจากข้อความเหลือไว้เป็นทางสำรองสำหรับยานอกบัญชีเท่านั้น

บทเรียน 25 ส.ค. 2569: เดิมเดาจากข้อความ พอสูตรชื่อเปลี่ยนก็เดาผิดทันที
ได้ `Morphine sulfate 10 mg/mL injection injection` ซ้ำสองรอบ และ `(IR)` หายไป

🚨 **`lib/drugName.js` คืนแค่ชื่อพื้นฐาน** (ชื่อสามัญ + ความแรง + %)
รูปแบบยา · ER/IR · ยี่ห้อ · สีเม็ด **ไม่อยู่ในนั้นโดยตั้งใจ** เพราะหน้าจอวาดเองพร้อมทาสี
ถ้าใส่ลงไปด้วยจะถูกวาดซ้ำสองรอบ

### หน้าไหนใช้ชื่อจากคลัง

ประวัติ · สรุป (Top 10 ทั้งสองการ์ด) · แก้ไขล็อต · ใบสรุปพิมพ์ — ทุกที่หา `drugId` ในคลังก่อน
ไม่เจอค่อยใช้ชื่อที่แช่ไว้ · `mr_history` ค้นได้ทั้งชื่อเก่าและชื่อใหม่

---

## 3.35 ✅ ป๊อปยืนยันก่อนส่ง + ส่งซ้ำอัตโนมัติ

**พี่กันสั่ง 25 ส.ค. 2569:** *"ตอนจะกดส่งข้อมูล เราอยากให้มันมีป๊อปอัปถามยืนยันอีกครั้ง...
และทำระบบไว้ด้วย ถ้าเกิดเน็ตมันพัง มันต้องขึ้นบอกด้วย ไม่ใช่ส่งแล้วพังโดยไม่เห็น"*

- `app.askSave()` ใน `handlers/record.js` — ตรวจช่องบังคับ **ก่อน** เปิดป๊อป
  ไม่งั้นผู้ใช้กดยืนยันแล้วเพิ่งมาบอกว่ากรอกไม่ครบ
- ป๊อปกลาง (`pages/confirm.jsx`) รองรับ 2 แบบแล้ว
  `danger` = ลบ ปุ่มแดงซ้าย · `normal` = ยืนยันปกติ ปุ่มเขียวขวา
- **ลองส่งใหม่หลังเน็ตหลุดไม่ต้องยืนยันซ้ำ** (`onSave: st.saveFailed ? app.save : app.askSave`)
- `retryFailedSave()` ใน `MedReturnApp.jsx` — ส่งซ้ำเองเมื่อ `online`/กลับเข้าแท็บ
  🚨 หน่วง 1.2 วิก่อนยิง เพราะสัญญาณ online มาถึงก่อนที่เน็ตจะใช้ได้จริง

---

## 3.36 💸 แก้ราคาย้อนหลัง — ประตูเดียวที่มีกุญแจ

**เคสจริง 25 ส.ค. 2569:** MTV tab ถูกใส่ราคา 20 บาท (เอาราคายาน้ำทั้งขวดมาใส่เป็นราคาต่อเม็ด)
บันทึกไปแล้ว 30 เม็ด = 600 บาท ทั้งที่ควรเป็น 15 · ยอดรวมทั้งปีเพี้ยน 8.8% จากแถวเดียว

ไฟล์: `scripts/012_price_fix.sql` · `app/api/price-fix/route.js` ·
`handlers/catalog.js` (`checkPriceFix`/`doPriceFix`) · `pages/catalog.jsx` (`renderPriceFix`)

🚨 **กฎแช่ราคายังอยู่ครบ** ใช้เฉพาะ "ราคาผิดตั้งแต่ต้น" ไม่ใช่ "ราคาที่เปลี่ยนตามเวลา"

| กรณี | ทำยังไง |
|---|---|
| ยาขึ้นราคากลางปี | **ห้ามแตะ** ของเก่าต้องคงราคาเดิม |
| กรอกผิด/จับคู่ผิดมาแต่ต้น | แก้ได้ พร้อมบันทึกว่าใครแก้และทำไม |

🚨 **ระบบไม่แก้ให้เอง** — ถามก่อนเสมอ โชว์ว่ากระทบกี่รายการ มูลค่าเปลี่ยนเท่าไร ช่วงวันไหน Lot ไหน
🚨 **บังคับเลือกชื่อผู้แก้ + กรอกเหตุผล** ถึงกดยืนยันได้ (บทเรียนจากหน้าต่างแก้ไขล็อต)

---

## 3.37 🛡 โหมดดูตัวอย่างแตะข้อมูลจริงไม่ได้แล้ว

**ผลตรวจข้อ ว-1 (วิกฤต):** ข้อมูลตัวอย่างใช้เลขแถว 1, 2, 3, … **ชุดเดียวกับแถวจริง**
เปิดโหมดตัวอย่างให้ผู้บริหารดูแล้วกด "ลบ" เพื่อโชว์วิธีลบ → `DELETE /api/returns/57` จริง
→ แถวจริงหมายเลข 57 หายจากฐาน · กด "แก้" ยิ่งแย่กว่าเพราะไม่มีร่องรอยเลย

**วิธีแก้ — ดักที่ทางออกทางเดียว** (`handlers/index.js` → `installDemoGuard`)

```js
app.fetchT = (url, opts, ms) => {
  if (app.state.demo && method !== 'GET') { แจ้งเตือน + ตีกลับ 403 }
  return fetchT(url, opts, ms);
};
```

🚨 **ทุก handler ต้องเรียก `app.fetchT` ไม่ใช่ `fetchT` ตรง ๆ** (ตอนนี้ 29 จุด)
เดิมกันทีละฟังก์ชัน 8 จุด ขาดไป 6 จุด เพราะต้องจำใส่เองทุกครั้งที่เขียนฟังก์ชันใหม่
🚨 **ห้ามเปลี่ยนให้ปล่อยผ่าน** — ความปลอดภัยของโหมดตัวอย่างอยู่ที่ "ไม่มีอะไรออกไปถึงฐานจริง" ข้อเดียว

---

## 3.38 📅 ปีงบย้อนหลัง

**ผลตรวจข้อ ส-4:** เลือกปีงบเก่า → กราฟว่างทั้ง 12 แท่ง · ป้ายเขียนปีผิด · **CSV ได้ข้อมูลผิดปีทั้งไฟล์**

- `vals/summary.js` → `fyViewing` มาจาก `sum.fyYear` ที่เซิร์ฟเวอร์ส่งมา (เดิมไม่เคยหยิบไปใช้)
  ปีงบ 2569 เริ่ม ต.ค. 2568 → ปี ค.ศ. ที่เริ่ม = `2569 - 543 - 1 = 2025`
- `/api/returns` รับ `fy=2568` แล้ว (`fyRangeOf`) · ปุ่มส่งออกส่งปีที่กำลังดูไปด้วย

---

## 3.39 🔒 ประตูตรวจรหัส — "พังแล้วปิด" ไม่ใช่ "พังแล้วเปิด"

**ผลตรวจข้อ ส-2 · ก-6 · ก-13 · ส-1** แก้พร้อมกัน 26 ส.ค. 2569 เพราะเป็นเรื่องประตูเดียวกัน

### `authEnabled()` แยกจาก `authConfigured()`

```js
authConfigured()  ตั้ง MRV_PASSWORD ไว้แล้วหรือยัง
authEnabled()     ต้องล็อกไหม  →  production = true เสมอ
```

🚨 **เว็บจริงล็อกเสมอ ไม่ว่าจะตั้งรหัสไว้หรือไม่** — ของเดิมถ้าอ่าน `MRV_PASSWORD` ไม่ได้
(ลบทิ้งตอนจัดการตัวแปรบน Cloudflare · พิมพ์ชื่อผิด · secret ไม่ผูกเข้า Worker)
`middleware.js` จะปล่อยผ่านทันที **ทั้งเว็บเปิดโล่งให้อินเทอร์เน็ตโดยไม่มีสัญญาณเตือน**
สัญญาณเดียวคือปุ่ม "ออกจากระบบ" หายไป ซึ่งไม่มีทางสังเกต — อาจเปิดโล่งเป็นเดือน

ตอนนี้ต้องล็อกแต่ไม่มีรหัส = **ตอบ 503 ปิดตายทั้งเว็บรวมหน้าเข้าสู่ระบบ** สังเกตเห็นในนาทีเดียว

🚨 **`/api/auth` ต้องกันรหัสว่างด้วย** — ถ้าลืม `tokenOf('')` จะตรงกับ `expectedToken()` พอดี
= กรอกช่องว่างแล้วเข้าเว็บได้เลย กลายเป็นรูที่ใหญ่กว่าเดิม

⚠️ **ก่อน deploy ต้องมั่นใจว่า `MRV_PASSWORD` ตั้งไว้บน Cloudflare แล้ว** ไม่งั้นเว็บจริงจะ 503 ทั้งใบ

### `matcher` ต้องมี `/api/:path*` แยกออกมา

ตัวกรองไฟล์รูปเทียบนามสกุลกับ**เส้นทางทั้งเส้น** → `/api/returns/12.png` ข้ามประตูไปทั้งดุ้น
ตอนนี้ยังไม่รั่วเพราะ `Number('12.png')` ไม่ใช่ตัวเลขจึงตอบ 400 **แต่เป็นระเบิดเวลา**
เพิ่มเส้นทางแบบ `[id]` ใหม่วันไหน จะไม่ถูกตรวจรหัสตั้งแต่วันแรกโดยไม่มีอะไรเตือน

### `safeEqual()` แทนการเทียบด้วย `===`

ปกติเรื่องเวลาในการเทียบแทบไม่มีผลผ่านอินเทอร์เน็ต **แต่เว็บนี้ต่างออกไป**
เพราะค่าที่เอามาเทียบคือ**ค่าเดียวกับที่ฝังเป็นคุกกี้** — เดาออกแล้วเอาไปตั้งเป็นคุกกี้เข้าเว็บได้เลย
โดยไม่ต้องรู้รหัสจริง · ใช้ทั้งใน `middleware.js` และ `/api/auth`

### `npm run dev` ผูกกับ `127.0.0.1`

**ผลตรวจข้อ ส-1** — `next dev` เปล่า ๆ เปิดรับจากทุกอินเทอร์เฟซ คู่กับ `MRV_DEV_OPEN=1`
ที่ไม่ถามรหัส และ `.env.local` ที่ชี้ฐาน**ตัวจริง** = ระหว่างเปิดเซิร์ฟเวอร์ทดสอบค้างไว้
**ใครก็ตามที่ต่อไวไฟโรงพยาบาลได้** เปิด `http://<ไอพีเครื่องพี่กัน>:3000` จะเห็น HN ทั้งปีงบ
กดส่งออก Excel ได้ กดลบได้ · เทสผ่าน `localhost` เหมือนเดิมทุกอย่าง

---

## 3.40 🧾 จุดเล็กที่ทำให้ตัวเลขกับเอกสารไม่ตรงกัน

แก้พร้อมกัน 26 ส.ค. 2569 · คนละเรื่องกันแต่อาการเหมือนกันคือ "เชื่อตัวเลขบนจอไม่ได้"

| ข้อ | อาการ | แก้ที่ |
|---|---|---|
| **ก-4** | ใบสรุป Lot ตัดที่ 60 แถวเงียบ ๆ · รับคืน 80 รายการแล้วสั่งพิมพ์ ได้กระดาษ 60 แถว ยอดท้ายใบไม่ตรงกับหน้ารายการ Lot | `handlers/lots.js` เติม `limit=all` |
| **ต-7** | `invalidate()` ไม่ล้างแคชรายการ Lot · บันทึก Lot ใหม่แล้วไม่เห็นอีก 60 วินาที นึกว่าบันทึกไม่ติดแล้วกดซ้ำ | `handlers/data.js` |
| **ต-6** | `deleted_by` ว่างทุกแถว ทั้งที่หลังบ้านรับ `by` มาตั้งแต่แรก | `handlers/history.js` |
| **ต-16** | `/api/drugs/rev` ส่งข้อความผิดพลาดดิบกลับเบราว์เซอร์ (เส้นเดียวใน 13 เส้น) | `app/api/drugs/rev/route.js` |
| **ก-7** | HN ค้างใน `localStorage` ตลอดกาลบนเครื่องกลาง คนเวรถัดไปเห็นของคนก่อน (PDPA) | `handlers/demo.js` → `doLogout` |

🚨 **ก-7 ล้างเฉพาะช่อง HN ไม่แตะรายการยาในร่าง** — ออกจากระบบไม่ควรทำให้งานที่กรอกค้างหาย
✅ **ต-6 บังคับกรอกชื่อผู้ลบแล้ว 2 ก.ย. 2569 — ดูข้อ 3.72**
ป๊อปยืนยันมีช่อง "ผู้ที่ลบ" และปุ่มปิดไว้จนกว่าจะเลือก

---

## 3.41 🧹 เก็บกวาดระดับต่ำ 9 ข้อ

แก้ 26 ส.ค. 2569 · ไม่แตะหน้าตาสักพิกเซล ทุกข้อมองไม่เห็นบนจอ

| ข้อ | เดิมพังยังไง | แก้ที่ |
|---|---|---|
| **ต-3** | หน้านำเข้าราคาใช้ `fetch` ดิบ ไม่มีตัวจับเวลา · เน็ตค้างแล้วปุ่มล็อกที่ "กำลังบันทึก" ตลอดกาล ต้องเลือกไฟล์ใหม่ทั้งชุด · และหลุดจากตัวกันโหมดตัวอย่างด้วย | `handlers/himport.js` → `app.fetchT` |
| **ต-4** | `loadMoreHistory` ไม่มีเลขลำดับ · กด "ดูเพิ่ม" แล้วรีบพิมพ์ค้น คำตอบชุดเก่ากลับมาต่อแถวที่ไม่ตรงเงื่อนไขเข้าไป | `handlers/history.js` |
| **ต-5** | `Math.floor` คู่กับ `toFixed` ปัดคนละทาง · `999.999` โชว์ `999.00` **หายไป 1 บาท** | `vals/summary.js` |
| **ต-8** | `app.loadLots` ประกาศซ้ำ 2 ไฟล์ ตัวใน `history.js` เป็นโค้ดตาย (ติดตั้งก่อนเลยโดนทับ) | ลบทิ้ง |
| **ต-9** | ตัวตรวจคีย์ซ้ำตรวจแค่ 8 ไฟล์ ขาด `himport` `lots` `catalog` · และเรียก vals ซ้ำอีกรอบทั้งชุด | `vals/index.js` |
| **ต-11** | ปุ่มลอย "ขึ้นบนสุด" z-index 40 ลอยทับหน้าต่างซ้อน 6 ตัวและกดโดนได้ | `pages/catalog.jsx` → 15 |
| **ต-15** | ไม่มี HSTS · `X-Powered-By` ยังเปิด | `next.config.js` |
| **ต-18** | โค้ดตาย 3 จุด — `qtyText` สองข้างเหมือนกัน · `(x ? 0 : 0)` · `histOffset` เขียน 4 จุดไม่มีใครอ่าน | 4 ไฟล์ |
| **ต-19** | ดึงข้อมูลทั้งปีพร้อม HN ได้โดยไม่มีร่องรอย | `api/returns` เพิ่ม log ฝั่งเซิร์ฟเวอร์ |

🚨 **ตัวตรวจที่ตรวจไม่ครบ แย่กว่าไม่มีตัวตรวจ** (ต-9) — ให้ความมั่นใจแบบผิด ๆ
ตอนนี้รายชื่อไฟล์กับตัวที่เอาไปรวมร่างเป็นชุดเดียวกัน เพิ่มไฟล์ใหม่แล้วได้รับการตรวจอัตโนมัติ

🚨 **`z-index` ของปุ่มลอยต้องต่ำกว่าหน้าต่างซ้อนที่ต่ำที่สุดเสมอ** (ต-11)
ตอนนี้ต่ำสุดคือป๊อปใส่จำนวนที่ 20 · ปุ่มลอยจึงต้องไม่เกิน 19 (ตั้งไว้ 15)
เพิ่มหน้าต่างซ้อนใหม่ที่ต่ำกว่า 15 เมื่อไหร่ ต้องขยับเลขนี้ตาม

✅ **ต-12 (ปุ่มเล็กกว่า 44px) แก้แล้ว 2 ก.ย. 2569 — ดูข้อ 3.72**
หน้าสรุป · คลังยา · ประวัติ เหลือปุ่มเล็กเกิน 0 จุด · หน้าบันทึกไม่แตะเพราะพี่กันสั่งให้เล็กเอง

---

## 3.42 🏥 รพ.สต. ต้นทาง — เลือกได้ว่ายาคืนมาจากแห่งไหน

**พี่กันสั่ง 26 ส.ค. 2569:** *"ตอนที่เลือกติ๊กแหล่งที่มา แล้วลง รพ.สต. ให้มีดรอปดาวน์เลือกได้เพิ่ม"*
และสั่งเพิ่มว่า *"ถ้าได้ รพ.สต. มา ก็ทำให้ตรงนี้มันแก้ รพ.สต. ได้ด้วยนะ ให้แก้ย้อนหลังได้"*

**ปัญหาเดิม** — แหล่งที่มา `pcu` บอกได้แค่ว่า "มาจาก รพ.สต." แต่อำเภอปรางค์กู่มี **13 แห่ง**
ยาที่คืนมาทั้งหมดถูกเหมารวมเป็นก้อนเดียว จะตอบว่าแห่งไหนคืนเยอะสุด หรือส่งใบสรุปกลับไปให้
ต้นทาง ก็ทำไม่ได้เลยเพราะข้อมูลไม่เคยถูกเก็บ

### ไฟล์ที่เกี่ยวข้อง

| ชั้น | ไฟล์ |
|---|---|
| ฐานข้อมูล | `scripts/014_pcu_site.sql` — `mr_return.pcu_site` · `mr_setting.pcu_sites` |
| รายชื่อจริง + แหล่งอ้างอิง | **`docs/PCU-SITES.md`** — รหัส 5 หลัก · ตำบล · หมู่ที่ · เว็บที่ใช้อ้างอิง |
| หลังบ้าน | `/api/settings` · `/api/bootstrap` · `/api/returns` (POST) · `/api/lots/[lot]` (GET+PATCH) |
| ตัววาดกลาง | **`components/pages/pcufield.jsx`** → `renderPcuField(V)` |
| หน้าจอ | `pages/record.jsx` (คอม+มือถือ) · `pages/lotedit.jsx` |

🚨 **ตัววาดตัวเดียวใช้ 3 ที่** — หน้าบันทึกคอม · หน้าบันทึกมือถือ · หน้าต่างแก้ไขล็อต
`vals/lots.js` สร้างก้อน `lotEditPcu` ที่มี**รูปร่างเหมือนกับที่หน้าบันทึกส่งให้เป๊ะ ๆ**
(`pcuOn` · `pcuSite` · `pcuSites` · `onPcuSite`) ตัววาดจึงไม่ต้องรู้ว่าถูกเรียกจากหน้าไหน

### กฎ 5 ข้อ

1. 🚨 **รายชื่ออยู่ในฐานข้อมูล ไม่ฝังในโค้ด** (`mr_setting.pcu_sites`)
   เปิดแห่งใหม่หรือเปลี่ยนชื่อ แก้ในฐานได้เลย ไม่ต้องแก้โค้ดแล้วขึ้นเว็บใหม่
2. 🚨 **เก็บเป็นชื่อ ไม่ใช่รหัส** — เป็น snapshot ณ วันบันทึกเหมือน `drug_name` และ `recorded_by`
   รพ.สต. เปลี่ยนชื่อวันหน้า รายการเก่ายังต้องอ่านออกว่าตอนนั้นเขียนว่าอะไร
3. 🚨 **ย้ายแหล่งที่มาออกจาก รพ.สต. ต้องล้างชื่อทิ้งทันที** ทั้งฝั่งจอ (`vals/record.js` → `pick`)
   และฝั่งเซิร์ฟเวอร์ (`/api/lots/[lot]` PATCH) ไม่งั้นจะเหลือชื่อ รพ.สต. ค้างในแถวที่เป็น OPD
   = ข้อมูลขัดกันเองซึ่งไม่มีอะไรจับได้เลย
4. 🚨 **บังคับเลือกก่อนบันทึก เมื่อรายชื่อถูกตั้งไว้ในระบบแล้ว** (พี่กันสั่ง 29 ส.ค. 2569)
   *"กฎบ้านั่นทำตอนยังไม่ใส่รายชื่อ รพ.สต. ไง ดังนั้น พอใส่แล้ว ต้องบังคับกรอก"*
   · ด่านอยู่ที่ `handlers/record.js` → `app.pcuSiteMissing()` ใช้ทั้ง `askSave` และ `save`
     (ต้องมีทั้งสองที่ เพราะเส้นทาง "ลองส่งใหม่" หลังเน็ตหลุดข้าม `askSave` ไปเลย)
   · ป้ายในช่องเปลี่ยนเป็น **"ต้องเลือกก่อนบันทึก"** เฉพาะตอนถูกเรียกจากหน้าบันทึก
     (`renderPcuField(V, { required: true })`) หน้าต่างแก้ไขล็อตยังเป็น "ยังไม่ได้เลือก" ตามเดิม
   ⚠️ **รายชื่อว่างเปล่า = ยังปล่อยผ่าน** ไม่มีตัวเลือกให้กดแล้วยังบังคับ
     เท่ากับปิดประตูไม่ให้ห้องยาบันทึกงานประจำวันได้เลย ซึ่งแย่กว่าข้อมูลที่ยังไม่ครบ
   🚨 **ต้องตรวจถึงระดับแถว ไม่ใช่ดูแค่ปุ่มแหล่งที่มาที่เลือกค้างอยู่**
     แถวเก็บ `source` ไว้ตั้งแต่ตอนกดเพิ่ม (คนไข้ 2 คนในล็อตเดียวใช้คนละแหล่งได้)
     เพิ่มยาตอนเลือก รพ.สต. ไว้ แล้วสลับไป OPD ก่อนกดส่ง = แถวนั้นยังเป็น รพ.สต.
     ที่ไม่มีชื่อแห่งติดไปด้วย ซึ่งไม่มีอะไรจับได้เลยหลังเข้าฐานแล้ว
   🚨 **โหมดดูตัวอย่างต้องมีรายชื่อ รพ.สต. ด้วย** (`enterDemo` ส่ง `DEMO_PCU`)
     ไม่งั้นเปิดโหมดนี้แล้วช่องขึ้นว่า "ยังไม่ได้ตั้งรายชื่อ" = เห็นการบังคับไม่ได้เลย (ข้อ 3.12)
5. 🚨 **แก้ย้อนหลังแล้วต้องเก็บร่องรอย** — `mr_lot_audit` field `pcu_site`
   ทดสอบแล้ว: แก้ หนองเชียงทูน → ตูม แล้วประวัติขึ้น `รพ.สต. ต้นทาง — หนองเชียงทูน → ตูม`

### ชื่อสั้น ไม่ใช่ชื่อเต็ม

พี่กันสั่ง *"ชื่อเอามาเดี๋ยว ๆ เลย เช่น พิมาย กู่ หนองเชียงทูน"*
ดรอปดาวน์จึงใช้ชื่อหมู่บ้านล้วน — `กู่` `กระดึ` `กำแมด` `หนองเชียงทูน` …

🚨 **ตรวจแล้วว่าชื่อสั้นทั้ง 13 ไม่ซ้ำกันสักคู่** เพิ่มแห่งใหม่ที่ชื่อชนกับของเดิมเมื่อไหร่
**ต้องเติมชื่อตำบลต่อท้ายทันที** ไม่งั้นเภสัชกรเลือกผิดแห่งโดยไม่มีทางรู้

### แสดงที่ไหนบ้าง

| ที่ | รูปแบบ |
|---|---|
| หน้าประวัติ | ต่อท้ายคอลัมน์แหล่งที่มา — `รพ.สต. กู่` (ไม่เพิ่มคอลัมน์ที่ 11 · ตารางแน่นอยู่แล้ว) |
| หน้ารายการ Lot | ต่อท้ายชื่อผู้บันทึก — `26 ส.ค. 2569 · ภญ.… · รพ.สต.กู่` |
| ใบสรุปที่พิมพ์ | บรรทัดหัวใบ `รพ.สต.: กู่` |
| ไฟล์ส่งออก CSV | คอลัมน์ใหม่ **"รพ.สต. ต้นทาง"** แทรกหลังแหล่งที่มา |
| ป๊อปยืนยันก่อนส่ง | บรรทัดย่อยใต้แหล่งที่มา · แดงถ้ายังไม่เลือก |

---

## 3.43 ✍️ ฟอนต์ทั้งเว็บต้องเป็นแบบมีหัว

**พี่กันสั่ง 26 ส.ค. 2569:** *"ในเว็บ ฟอนต์ไม่มีหัว เปลี่ยนเป็นมีหัวให้หมดนะ ไปไล่มา"*

เดิมเว็บใช้ 2 ฟอนต์ — **Sarabun (มีหัว)** กับ **IBM Plex Sans Thai (ไม่มีหัว)**
ตัวหลังถูกใช้กับตัวเลข ช่องกรอก และป้ายเล็ก ๆ รวม **115 จุด** (ยกมาจากมอคอัปตั้งแต่แรก)

ฟอนต์ไม่มีหัว (loopless) อ่านภาษาไทยยากกว่า โดยเฉพาะชื่อหมู่บ้านและชื่อยาที่ไม่คุ้น
ไล่เปลี่ยนเป็น `Sarabun` ครบทุกจุดแล้ว ตรวจในเบราว์เซอร์จริงได้ **0 element ที่ยังใช้ตัวเก่า**

🚨 **`components/helpers.js` → `fontify()` เหลือบรรทัดแปลงไว้เป็นตาข่ายกันตกหล่น**
```js
.replace(/'IBM Plex Sans Thai'/g, "Sarabun")
```
ถ้ามีจุดไหนหลุดรอด หรือมีคนเผลอเขียนกลับมาใหม่ ก็ยังได้ฟอนต์มีหัวอยู่ดี

✅ **ถอดฟอนต์ IBM Plex ออกแล้ว 2 ก.ย. 2569** — ไม่มีใครใช้ตั้งแต่ v0.13.0.0
ตรวจ HTML ที่เสิร์ฟจริงแล้วเหลือ 3 ฟอนต์ ไม่มีร่องรอย IBM Plex (ดูข้อ 3.72)

### ฟอนต์ชื่อเว็บ — Charmonman

พี่กันไล่ลองเอง 3 ตัวแล้วเคาะ **Charmonman** (ลายมือไทยมีหัว) — ลอง Charm กับ Thasadith แล้วไม่เอา

🚨 **ใช้กับ "ชื่อเว็บ" 3 จุดเท่านั้น** (`nav.jsx` · `record.jsx` · `about.jsx`) **ห้ามลามไปที่อื่น**
ฟอนต์ลายมืออ่านตัวเลขกับชื่อยายาก ซึ่งเป็นข้อมูลความปลอดภัยที่ต้องอ่านง่ายที่สุด

⚠️ **ขนาดต้องใหญ่กว่าตัวพิมพ์ทั่วไป** เพราะเส้นบางกว่า — 20px (แถบเมนู) · 21px (แถบบนคอม) · 31px (หน้าเกี่ยวกับ)

🚨 **`<option>` ในดรอปดาวน์ต้องใส่สไตล์ที่ตัวมันเอง ไม่ใช่แค่ที่ `<select>`**
วินโดวส์วาดรายการที่กางออกด้วยระบบของตัวเอง ไม่สืบทอดฟอนต์จากตัวแม่
ใส่แค่ที่ `<select>` จะได้ฟอนต์ระบบตัวบางสีจางเหมือนเดิม (พี่กันจับได้ทันที)

---

## 3.44 🐛🔴 วันที่ค้างวันเก่า — ของจริงที่ข้อ 3.22 แก้ไม่ตรงจุด

**เจอ 26 ส.ค. 2569** ระหว่างทดสอบเรื่องอื่น: นาฬิกาเครื่องเป็น **26 ส.ค.** แต่ช่องวันที่ยังเป็น **25 ส.ค.**
= ยาที่รับคืนวันนี้จะถูกบันทึกเป็นเมื่อวานทั้งล็อต โดยไม่มีอะไรเตือนเลย

**อาการเดียวกับข้อ 3.22 เป๊ะ ๆ ทั้งที่ "แก้ไปแล้ว" เมื่อ 19 ส.ค.** — เพราะแก้ผิดที่

```js
app.checkDayRollover = () => {
  const now = todayISO();
  if (!app.state.today || app.state.today === now) return;   // ← 🚨 ตีกลับตรงนี้เสมอ
  ...โค้ดที่แก้ไว้เมื่อ 19 ส.ค. อยู่ข้างล่างนี้ ไม่เคยได้ทำงานเลย...
};
```

ตอนกู้ร่างจาก localStorage มีบรรทัด `patch.today = todayISO()` อยู่แล้ว
แปลว่า `state.today` เท่ากับวันนี้ **เสมอ** ตั้งแต่วินาทีแรกที่เปิดเว็บ เงื่อนไขด่านแรกจึงจริงทุกครั้ง
**การแก้เมื่อ 19 ส.ค. เลยเป็นโค้ดตายมาตลอด**

### วิธีแก้ — ต้องเช็คสองอย่าง ไม่ใช่อย่างเดียว

```js
const stale = !app.state.dateTouched && !!app.state.date && app.state.date < now;
if (app.state.today === now && !stale) return;
```

🚨 **`dateTouched` เป็นหัวใจของการแก้นี้ ห้ามตัดออก**
ตัวจับเวลาเรียก `checkDayRollover` **ทุก 60 วินาที** ถ้าไม่มีธงนี้ เภสัชกรที่ตั้งใจเลือก
วันย้อนหลัง (บันทึกยาที่รับคืนเมื่อวานให้ครบ) จะถูกดึงกลับเป็นวันนี้เงียบ ๆ ภายในนาทีเดียว
**ซึ่งแย่กว่าบั๊กเดิมอีก** เพราะเกิดหลังจากผู้ใช้ตั้งใจทำอะไรบางอย่างไปแล้ว

🚨 **`dateTouched` ห้าม persist ลง localStorage เด็ดขาด**
ต้องเป็นเท็จเสมอตอนเปิดเว็บใหม่ ไม่งั้นร่างที่ค้างข้ามวันจะติดธงไว้แล้วไม่ถูกทวนวันอีกเลย
= บั๊กเดิมกลับมาแบบถาวร

**บทเรียน:** แก้บั๊กแล้วต้องพิสูจน์ด้วยพฤติกรรมจริง ไม่ใช่แค่เห็นว่าโค้ดที่เขียนดูถูกต้อง
ครั้งก่อนโค้ดถูก แต่ไม่มีใครไปถึงมัน

---

## 3.45 🔒 บัตรผ่านมีวันหมดอายุจริง (ผลตรวจข้อ ก-15)

**ปัญหาเดิม:** ค่าในคุกกี้คือ `expectedToken()` ซึ่งเป็น**ค่าคงที่ตลอดกาล**
`maxAge: 30 วัน` เป็นเพียง "คำขอ" ที่ส่งไปให้เบราว์เซอร์ช่วยลบให้ ไม่ใช่การตรวจฝั่งเซิร์ฟเวอร์

ใครก๊อปค่าคุกกี้ออกไปได้ครั้งเดียว (ยืมเครื่อง · ส่วนขยายเบราว์เซอร์ · สำรองข้อมูลเครื่อง)
เอาไปตั้งเองในเครื่องอื่นแล้วเข้าเว็บได้ **ตลอดชีวิต** จนกว่าจะมีคนเปลี่ยน `MRV_PASSWORD`
ซึ่งอาจไม่เกิดขึ้นเลย

**วิธีแก้** (`lib/auth.js` → `issueAuthCookie` / `authCookieValid`)

```
รูปแบบ  <เวลาที่ออกบัตร>.<ลายเซ็น>
ลายเซ็น = sha256(รหัสที่คาดหวัง + '|' + เวลาที่ออกบัตร)
```

เวลาที่ออกบัตรแก้เองไม่ได้ — ขยับตัวเลขเมื่อไหร่ลายเซ็นไม่ตรงทันที
และเซิร์ฟเวอร์ตัดสินอายุเอง ไม่ต้องพึ่งเบราว์เซอร์อีก · เวลาในอนาคตก็ไม่รับ

🚨 **คุกกี้แบบเก่า (ค่าคงที่ไม่มีจุดคั่น) ใช้ไม่ได้แล้วโดยตั้งใจ**
ทุกเครื่องต้องกรอกรหัสใหม่ครั้งเดียวหลังอัปเดต ซึ่งเป็นสิ่งที่ต้องการอยู่แล้ว
เพราะบัตรใบเก่าที่เคยรั่วออกไปจะใช้ไม่ได้อีกต่อไปทันที

### `needAuth` ที่ไม่เคยมีใครอ่าน

ประตูตรวจรหัสตอบ 401 พร้อมธง `needAuth` มาตั้งแต่แรก แต่ฝั่งจอไม่เคยอ่าน
พอบัตรหมดอายุ เภสัชกรจะเห็นแค่ "โหลดข้อมูลไม่สำเร็จ" ซ้ำ ๆ ทุกปุ่มที่กด
เหมือนเว็บพังทั้งที่แค่ต้องกรอกรหัสอีกครั้งเดียว

ตอนนี้ `installFetchGuard` (เดิมชื่อ `installDemoGuard`) ดักให้แล้ว — ขึ้นข้อความแล้วพาไป `/login`
🚨 **ต้องอ่านสำเนาของคำตอบ (`res.clone()`) ไม่ใช่ตัวจริง** เพราะเนื้อหาอ่านได้ครั้งเดียว
ถ้าอ่านตัวจริง ฝั่งที่เรียกจะได้กล่องเปล่าไปแทน

---

## 3.46 ♿ โปรแกรมอ่านจอใช้งานได้ (ผลตรวจข้อ ต-13 · ต-14)

เว็บทั้งใบเคยไม่มี `aria-label` แม้แต่จุดเดียว และไม่มี `<h1>` / `<main>` / `<nav>`
โปรแกรมอ่านจอจะอ่านปุ่ม ✕ ว่า "กากบาท" เฉย ๆ ไม่รู้ว่ากดแล้วเกิดอะไร

🚨 **แก้ด้วยการเติม `role=` บนของเดิม ไม่เปลี่ยนแท็ก**
`<h1>` กับ `<main>` มีสไตล์ตั้งต้นของเบราว์เซอร์ติดมาด้วย (ระยะขอบ ขนาดตัวอักษร)
เปลี่ยนแท็กแล้วหน้าตาจะขยับทันที ซึ่งผิดกติกาว่างานเก็บกวาดต้องไม่แตะหน้าตาสักพิกเซล

| ทำอะไร | กี่จุด |
|---|---|
| `role="main"` · `role="navigation"` · `role="status"` | 4 |
| `role="heading" aria-level="1"` บนหัวข้อหน้า | 5 |
| `role="dialog" aria-modal="true"` บนหน้าต่างซ้อน | 8 |
| `aria-label` บนปุ่มที่มีแต่ไอคอน (✕ ✎ ⚙ ℹ ✓ ←) | 36 |
| `aria-hidden="true"` บนลูกศรที่เป็นแค่เครื่องหมายประกอบ | 3 |

ตรวจในเบราว์เซอร์จริงแล้ว: **ปุ่มทั้ง 25 ตัวบนหน้าบันทึกมีชื่อครบทุกตัว ไม่เหลือปุ่มไร้ชื่อ**

---

## 3.47 🗑 เหตุผลที่ต้องทำลาย — 12 ข้อพร้อมคำอธิบาย (ผลตรวจข้อ ส-8)

**พี่กันเคาะ 26 ส.ค. 2569:** *"เอาทุกข้อเลย แต่ให้มีใส่ความหมายไว้ด้วยนะ หาที่ใส่"*
และเลือกแบบ **ก) เด้งถามทันทีที่กดปุ่มทำลาย**

**ปัญหาเดิม** — ช่องเหตุผลมีอยู่ แต่กรอกได้ทางเดียวคือป๊อปใส่จำนวนฝั่งมือถือ
เส้นทางหลักบนคอม (พิมพ์ชื่อยา Enter ใส่จำนวน Enter) ไม่มีช่องให้กรอกเลย
ตอบผู้บริหารไม่ได้ว่า "ทำลายไป 40,000 บาท เพราะอะไร" ซึ่งเป็นเหตุผลเดียวที่ใส่ช่องนี้มา

| ไฟล์ | ทำอะไร |
|---|---|
| `components/helpers.js` | `DESTROY_REASONS` 12 ข้อ · แต่ละข้อมี `label` กับ `help` |
| `components/pages/reason.jsx` | **ไฟล์ใหม่** หน้าต่างเลือกเหตุผล ใช้ร่วมทุกที่ |
| `handlers/record.js` | `askDestroyReason(label, next)` · `pickDestroyReason` |
| `vals/record.js` | ดักปุ่มทำลายทั้ง 2 ทาง — ก่อนกดเพิ่ม และสลับแถวที่เพิ่มแล้ว |

🚨 **กด "ทำลาย" ถามเหตุผลเสมอ · กด "ใช้ต่อได้" ไม่ถาม**
ใช้ต่อได้คือสภาพปกติ ไม่มีอะไรต้องอธิบาย

🚨 **คำอธิบายต้องอยู่ในแถวเลย ไม่ใช่ซ่อนให้เอาเมาส์ชี้** — คนที่รับยาคืนหน้าเคาน์เตอร์
อาจไม่ใช่เภสัชกร และบนมือถือไม่มีเมาส์ให้ชี้
(ส่วนป๊อปใส่จำนวนฝั่งมือถือใช้ชิป คำอธิบายโผล่ใต้แถวชิปของตัวที่เลือก)

🚨 **กดพื้นหลังปิดได้** ต่างจากป๊อปยืนยันลบ เพราะไม่ใช่การกระทำที่ย้อนยาก
ปิดทิ้งแล้วของยังเป็น "ใช้ต่อได้" เหมือนเดิม

⚠️ **ข้อ 11 ยาเสพติด วัตถุออกฤทธิ์** มีระเบียบทำลายเฉพาะของตัวเอง ต้องมีคณะกรรมการร่วม
ใส่ไว้เพื่อให้บันทึกได้ครบ แต่คำอธิบายเตือนว่าต้องทำตามระเบียบแยก

---

## 3.48 🧾 ใบสรุป Lot แบบเอกสารทางการ + บันทึก PDF

**พี่กันสั่ง 26 ส.ค. 2569:** *"เราต้องการแบบทางการ เธอออกแบบมาให้เราเลย"* · *"ใช่มันต้องสร้าง pdf ได้"*

### ของที่เพิ่มเข้าไป

| ส่วน | รายละเอียด |
|---|---|
| หัวกระดาษ | ชื่อหน่วยงานเต็มยศกึ่งกลาง แล้ว "ใบสรุปรายการยาคืน" ตัวใหญ่ใต้ลงมา |
| เลขที่เอกสาร | `ยค. L690826-01` — ใช้เลข Lot เป็นฐาน 1 Lot = 1 ใบ ไม่มีทางซ้ำ |
| วันเวลาที่พิมพ์ | โผล่เฉพาะตอนสั่งพิมพ์ · ยอดอาจเปลี่ยนได้ถ้ามีคนแก้ล็อตย้อนหลัง |
| ช่องลงนาม **3 ฝ่าย** | ผู้ส่งมอบยา · ผู้บันทึกรับคืน · ผู้ตรวจสอบ |

🚨 **ห้ามยุบช่องลงนามเหลือ 2 ช่อง** — ผู้บันทึกเป็นผู้ปฏิบัติ ส่วนผู้ตรวจสอบเป็นผู้รับรองยอด
เป็นคนละบทบาทกัน เอกสารยาคืนของโรงพยาบาลต้องมีทั้งสาม

### ทำไมไม่ลงตัวสร้าง PDF เพิ่ม

ปุ่ม **"บันทึก PDF"** ใช้ตัวแปลงที่มีในเบราว์เซอร์อยู่แล้ว ต่างจากปุ่ม "พิมพ์" แค่ตั้งชื่อไฟล์ให้ก่อน
(เบราว์เซอร์ตั้งชื่อไฟล์ PDF จากชื่อหน้าเว็บ — เปลี่ยนเป็นชื่อใบชั่วคราวแล้วคืนค่าเดิม)

🚨 **ห้ามเปลี่ยนไปใช้ตัวสร้าง PDF ฝั่งจาวาสคริปต์** เหตุผล 3 ข้อ
1. ต้องฝังฟอนต์ไทยเข้าไปในไฟล์เอง — Sarabun ชุดเต็มหนักราว 300 KB ต่อน้ำหนัก
   ฝัง 5 น้ำหนักคือ 1.5 MB ถ่วงเว็บทั้งใบเพื่อฟีเจอร์ที่ใช้วันละไม่กี่ครั้ง
2. สระวรรณยุกต์ไทยมักลอยผิดตำแหน่งในตัวสร้าง PDF ฝั่งจาวาสคริปต์
3. ตัวแปลงของเบราว์เซอร์ใช้ฟอนต์ที่หน้าเว็บใช้อยู่แล้ว จึงตรงเป๊ะ 100%

### 🚨 CSS พิมพ์เขียนใหม่ — เลิกใช้ตัวเลือก `:has()`

**อาการที่พี่กันเจอ:** กด Ctrl+P แล้วได้หน้าเว็บทั้งหน้าออกมา มีปุ่ม "แก้ไข" "ใบสรุป"
ติดไปในกระดาษด้วย แทนที่จะได้เฉพาะใบสรุป

ของเดิมซ่อนทุกอย่างที่เป็นลูกชั้นแรกของ `body` แล้วเปิดเฉพาะสายที่มี `.slip-sheet`
ซึ่งพึ่งพา **2 อย่าง** — เบราว์เซอร์ต้องรองรับ `:has()` และเนื้อหาต้องเป็นลูกชั้นแรกพอดี
ถ้าเครื่องมือสร้างเว็บห่อชั้นเพิ่ม หรือมีสคริปต์แทรกกล่องเข้ามา
ตัวเลือกจะไม่ตรงแล้ว **ทุกอย่างโผล่ออกมาหมด**

**ท่าใหม่ใช้ `visibility`** — ซ่อนทั้งหน้าก่อน แล้วเปิดเฉพาะใบกับลูกหลานของมัน
ไม่พึ่ง `:has()` ไม่สนใจว่าใบถูกห่ออยู่ลึกกี่ชั้น

⚠️ **`visibility` ต่างจาก `display` ตรงที่ของที่ถูกซ่อนยังกินที่อยู่**
จึงต้องดึงใบออกมาวางมุมบนซ้ายด้วย `position:absolute` ไม่งั้นได้กระดาษเปล่านำหน้าหลายหน้า

**ของที่เพิ่มมาด้วย** — `thead { display: table-header-group }` ทำให้หัวตารางซ้ำทุกหน้า
(ใบที่มี 80 รายการกินกระดาษ 2-3 แผ่น หน้าสองเป็นต้นไปจะเห็นแต่ตัวเลขลอย ๆ)
· `.slip-sign { break-inside: avoid }` กันช่องลงนามตกไปอยู่หน้าใหม่ลำพัง

---

## 3.49 🔓 ถอดทางลัดปลดล็อกตอนรันในเครื่องทิ้งถาวร

**พี่กันทัก 26 ส.ค. 2569:** *"เอาปุ่ม log out เราออกทำไม"* แล้วตามด้วย
*"จะมีระบบบ้านี้ไว้ทำไม แล้วถ้าเป็นงี้แล้วเราจะกด log out ยังไง"*

**ต้นเหตุ** — ปุ่มออกจากระบบผูกกับ `authEnabled()` ซึ่งเป็นเท็จเมื่อเปิดทางลัดปลดล็อก
เครื่องทดสอบจึงไม่เห็นปุ่มเลย (เว็บจริงไม่เคยได้รับผลกระทบ เพราะ production ล็อกเสมอ)

**ทางที่ลองแล้วผิด** — เปลี่ยนไปผูกกับ `authConfigured()` เพื่อให้ปุ่มโผล่บนเครื่องทดสอบด้วย
พี่กันทักทันทีว่าเครื่องที่ไม่ได้ล็อก กดออกจากระบบไปก็ไม่มีคุกกี้ให้ลบ รีเฟรชก็เข้าได้เหมือนเดิม
**= ปุ่มหลอก ซึ่งแย่กว่าปุ่มที่ไม่มี**

**ทางที่ถูก — แก้ที่ต้นเหตุ** ถอดตัวแปร `MRV_DEV_OPEN` ทิ้งทั้งระบบ

เหตุผลเดิมที่ใส่ไว้คือแคลร์กรอกรหัสผ่านลงฟอร์มเองไม่ได้ (ข้อห้ามติดตัว)
แต่ไม่จำเป็นตั้งแต่แรก — **พี่กันกรอกครั้งเดียว คุกกี้อยู่ 30 วัน แคลร์เทสต่อได้ทั้งหมด**

🚨 **ห้ามใส่กลับมาอีก** — ถ้าเทสไม่ได้เพราะไม่มีคุกกี้ ให้ขอพี่กันกรอกรหัสในเบราว์เซอร์
ไม่ใช่เปิดประตูทิ้งไว้ · เครื่องทดสอบที่ไม่เหมือนเว็บจริงคือบ่อเกิดของบั๊ก
"ในเครื่องผ่าน เว็บจริงพัง" และประตูรหัสผ่านเป็นของที่ต้องทดสอบมากที่สุด

---

## 3.50 🐛 คำตอบจากเซิร์ฟเวอร์ทับสิ่งที่ผู้ใช้กำลังพิมพ์

**อาการที่พี่กันเจอ 26 ส.ค. 2569:** พิมพ์ชื่อหน่วยงานยาว ๆ แล้วตัวอักษรหายเป็นท่อน
เหมือนระบบตัดจำนวนคำ ทั้งที่ไม่มีการตัดที่ไหนเลย

**กลไก** — ช่องชื่อหน่วยงานรอให้หยุดพิมพ์ 0.5 วินาทีแล้วค่อยส่งขึ้นเซิร์ฟเวอร์

```
1. พิมพ์ "กลุ่มงานเภสัชกรรม" แล้วหยุดคิดแป๊บหนึ่ง → ยิงขึ้นเซิร์ฟเวอร์
2. พิมพ์ต่อ "และคุ้มครองผู้บริโภค" ระหว่างที่คำขอยังเดินทางอยู่
3. คำตอบกลับมาว่าชื่อคือ "กลุ่มงานเภสัชกรรม" → setState ทับ
4. ที่พิมพ์ไปในข้อ 2 หายหมดต่อหน้าต่อตา
```

ยิ่งชื่อยาว ยิ่งหยุดพิมพ์บ่อย ยิ่งโดนบ่อย · ชื่อสั้น ๆ จึงไม่เคยมีปัญหา

**วิธีแก้** (`handlers/settings.js` → `pushSetting`) — ถ้าค่าบนจอไม่ตรงกับค่าที่เพิ่งส่งไป
แปลว่าผู้ใช้พิมพ์ต่อแล้ว ให้คงของบนจอไว้ ไม่เอาคำตอบมาทับ

🚨 **ตระกูลเดียวกับ "กันคำตอบเก่าทับคำตอบใหม่" ที่หน้าประวัติใช้เลขลำดับ**
ทุกที่ที่มีการหน่วงแล้วยิงเซิร์ฟเวอร์ ต้องคิดเรื่องนี้เสมอ

**ชื่อหน่วยงานเต็มยศ** เก็บที่ `lib/format.js` → `DEFAULT_ORG` เป็นจุดกลางจุดเดียว
🚨 หน้าเข้าสู่ระบบดึงชื่อจากฐานข้อมูลไม่ได้ เพราะยังไม่ผ่านประตูรหัสผ่าน จึงต้องมีค่าตั้งต้นฝังไว้

---

## 3.51 🎨 หน้าเข้าสู่ระบบ + ฉากหลัง three.js

**พี่กันสั่ง 26 ส.ค. 2569:** *"สวยแล้วเหรอ ออกแบบหน้านี้ใหม่เลย ขอ three js ทำแอนิเมชั่นฉากหลังด้วย"*
แล้วสั่งเพิ่ม *"เราขอ polygon ละเอียดระดับ 4k"* และ *"ตอนนี้มันเป็นแคปซูล เราขอแบบ tablet ด้วย แทรกสัก 30%"*

ไฟล์: `app/login/page.js` · **`app/login/Backdrop.js`** (ไฟล์ใหม่)

**ทำไมเป็นเม็ดยาลอยขึ้น** — เว็บนี้ชื่อ "มูลค่ายาคืน" สิ่งที่มันทำคือรับยาที่ลอยกลับมา
แล้วแปลงเป็นมูลค่า · ฉากหลังจึงลอยขึ้น ไม่ใช่ลอยลง

### กฎ 5 ข้อของฉากหลัง ห้ามละเมิด

1. **ห้ามแย่งความสนใจจากช่องกรอกรหัส** — ความทึบต่ำ เคลื่อนช้ามาก ไม่มีอะไรกะพริบ
2. **เครื่องที่เปิด "ลดการเคลื่อนไหว" ต้องได้ภาพนิ่ง** เจ้าหน้าที่บางคนเวียนหัวกับภาพเคลื่อนไหว
3. **เครื่องที่ไม่มี WebGL ต้องไม่พังทั้งหน้า** — เหลือพื้นไล่สีของ CSS ซึ่งทำงานเองได้
4. **หยุดวาดเมื่อสลับแท็บออกไป** คอมห้องยาเปิดค้างทั้งวัน ไม่ควรกินซีพียูฟรี ๆ
5. **โหลด three.js แบบขอเมื่อใช้ เฉพาะหน้านี้หน้าเดียว** ไฟล์ราว 600 KB
   ถ้าโหลดมาพร้อมทั้งเว็บจะถ่วงหน้าบันทึกที่ใช้งานจริงทุกวัน

🚨 **ใช้ `InstancedMesh` ตัวเดียวต่อทรง ไม่ใช่ `Mesh` แยกตัว**
ความละเอียด 96 ส่วนรอบวง = ราว 6,000 สามเหลี่ยมต่อเม็ด ทำได้เพราะการ์ดจอสั่งวาดรอบเดียวต่อเฟรม
ถ้าแยกเป็น Mesh 34 ตัว เครื่องห้องยาค้างทันที

🚨 **เม็ดกลมแบนทำจากทรงกลมแล้วบีบ ไม่ใช้ทรงกระบอก** — ทรงกระบอกได้ขอบคมเป็นเหลี่ยม
ส่วนทรงกลมที่บีบแล้วได้ขอบมนโค้งเหมือนเม็ดยาปั๊มจริง

🚨 **สุ่มตำแหน่งด้วยเมล็ดคงที่** ห้ามใช้ตัวสุ่มของระบบ — กฎเดียวกับข้อมูลโหมดดูตัวอย่าง
เปิดกี่ครั้งก็ได้ภาพเดิม เทียบหน้าจอกันได้

🚨 **ต้องคืนหน่วยความจำการ์ดจอเองตอนออกจากหน้า** three.js ไม่เก็บกวาดให้

⚠️ **ฟอนต์ Charmonman มีหางลากยาวกว่าฟอนต์ทั่วไป** — ตั้งระยะบรรทัด 1.2 แล้วหางไปทับ
ข้อความบรรทัดล่าง ต้องใช้ 1.5 ขึ้นไป

---

---

## 3.52 📋 หน้ารายการ Lot เป็นตาราง — ค้นได้ กรองได้ เรียงได้

**พี่กันสั่ง 26 ส.ค. 2569:** *"หน้านี้เราว่าช่องไฟเยอะมาก เราอยากให้มันมีหัวคอลัมน์ กดเรียงได้
มีช่องให้ค้นหาได้ และแบ่งคอลัมน์วันที่ คอลัมน์ชื่อคนที่ลง คอลัมน์แหล่งที่มา เพราะจะได้กรองได้
ว่าห้องยาไหน และ รพ.สต. ไหน ... คิดซะว่ามันคือตารางประวัติแบบนึง"*

**ปัญหาเดิม** — เป็นการ์ดที่ยัดวันที่ ชื่อผู้บันทึก และ รพ.สต. รวมอยู่ในบรรทัดเดียวใต้เลข Lot
จึงกรองไม่ได้เลยสักอย่าง และช่องไฟกลางการ์ดเหลือเยอะมากบนจอกว้าง

### โครงใหม่

| จอ | หน้าตา |
|---|---|
| กว้าง (`d.wide`) | ตาราง 9 คอลัมน์ · หัวคอลัมน์กดเรียงได้ · แถวสลับสีจาง |
| แคบ | การ์ดเหมือนเดิม แต่แถบค้นหากับตัวกรองยังอยู่ครบ |

คอลัมน์: วันที่ · เลข Lot · ผู้บันทึก · แหล่งที่มา · **รพ.สต.** · รายการ · ใช้ต่อได้ · ทำลาย · จัดการ

🚨 **รพ.สต. เป็นคอลัมน์ของตัวเอง ไม่ใช่บรรทัดล่างของแหล่งที่มา** (พี่กันสั่ง *"รพ.สต. สร้างคอลัมน์แยกด้วย"*)
แยกแล้วจึงกดเรียงเพื่อจับกลุ่มยาที่คืนจากแห่งเดียวกันมาอยู่ติดกันได้
ล็อตที่มาจาก รพ.สต. แต่ไม่ได้เลือกแห่ง ขึ้น "ยังไม่ได้ระบุ" สีแดง · ล็อตที่ไม่ได้มาจาก รพ.สต. ขีดกลางจาง

🚨 **การเรียงใช้ท่าเดียวกับหน้าประวัติเป๊ะ ๆ** (`setLotsSort` ลอกมาจาก `setHistSort`)
กดหัวเดิมซ้ำ = สลับทิศ · กดหัวใหม่ = เริ่มจากมากไปน้อย · ลูกศร ▲▼↕ ชุดเดียวกัน
ถ้าทำคนละแบบ มือผู้ใช้จะจำผิดสลับไปมาระหว่างสองหน้า

🚨 **คอลัมน์ตัวเลขต้องเทียบแบบตัวเลข** (`SORT_VAL`) ไม่ใช่ `localeCompare`
ไม่งั้น 1,402.50 จะมาก่อน 550.00 เพราะเทียบทีละตัวอักษร

🚨 **ค่าเท่ากันต้องมีตัวตัดสินรอง (เลข Lot)** ไม่งั้นแถวสลับที่กันเองทุกครั้งที่วาดจอใหม่

### ช่องค้นกับตัวกรอง — ทำงานในเครื่องล้วน

ไม่ยิงเซิร์ฟเวอร์เลย เพราะรายการ Lot ทั้งช่วงเวลาถูกโหลดมาแล้วตั้งแต่แรก (ไม่กี่ร้อยแถว)
ค้นได้ทั้งเลข Lot · ชื่อผู้บันทึก · ชื่อ รพ.สต. ในช่องเดียว · พิมพ์หลายคำได้ (ต้องเจอครบทุกคำ)

**ตัวกรองเลือกสองชั้น** — พี่กันสั่งแก้ทันทีที่เห็นรอบแรก

> *"ใครบ้าให้มันยาวเอามาทุก รพ.สต. ... ก็ให้เลือกสองรอบไม่เข้าใจรึไง
> กดเลือกแหล่งที่มาก่อนว่ามาจากไหน ถ้าเลือก รพ.สต. แล้วก็มีกล่องให้เลือก รพ.สต. เพิ่มว่าที่ไหน"*

```
ช่องที่ 1  ทุกแหล่งที่มา / OPD ทั่วไป / OPD NCD / ห้องยา IPD / เยี่ยมบ้าน / รพ.สต.
ช่องที่ 2  โผล่เฉพาะเมื่อช่องแรกเป็น รพ.สต. → ทุกแห่ง / กู่ (2) / ตูม / ...
```

รอบแรกแคลร์ยัดทั้ง 13 แห่งลงดรอปดาวน์เดียวกับแหล่งที่มาโดยใช้ `<optgroup>`
ด้วยเหตุผลว่า "แยกสองช่องจะกินที่ และผู้ใช้ต้องเดาเองว่าช่องไหนคุมช่องไหน" — **ผิดทั้งสองข้อ**
รายการยาวเป็นหางว่าว 19 บรรทัด และช่องที่สองที่โผล่เฉพาะตอนเลือก รพ.สต. ชัดเจนในตัวเองอยู่แล้ว

🚨 **เปลี่ยนช่องแรกไปเป็นอย่างอื่น ต้องล้างช่องที่สองทิ้งด้วย** (`setLotsSrc`)
ไม่งั้นเหลือเงื่อนไขซ่อนอยู่ที่มองไม่เห็นบนจอ แล้วผลลัพธ์ว่างโดยไม่รู้สาเหตุ

🚨🔴 **ห้ามเขียน `background:#fff` แบบรวบในช่องเลือก**
กฎกลางใน `globals.css` วาดลูกศร ▾ ด้วย `background-image` (`select{appearance:none}`)
การเขียนแบบรวบจะล้างรูปลูกศรทิ้ง ช่องเลยดูไม่ออกว่ากดได้ พี่กันทักทันที: *"ไหนตัว v"*
**ต้องเขียน `background-color` แยกเสมอ** — ตระกูลเดียวกับบั๊ก `border-color` เดี่ยวใน hover

🚨🔴 **รายชื่อ รพ.สต. ต้องมาจากการตั้งค่า (ทั้ง 13 แห่ง) ไม่ใช่จากที่โผล่ในข้อมูล**
รอบแรกเอาเฉพาะแห่งที่เคยคืนยาจริง ผลคือรายการว่างทั้งหมด เพราะยังไม่มีล็อตไหนกรอกชื่อแห่งไว้เลย
พี่กันทัก: *"แล้วทำไมเลือก รพ.สต. ไม่ได้ ว่าที่ไหน"*
**ตัวกรองที่เลือกอะไรไม่ได้เลย แย่กว่าตัวกรองที่มีตัวเลือกซึ่งยังไม่มีข้อมูล**
ต่อท้ายด้วยจำนวน Lot ในวงเล็บให้เห็นว่าแห่งไหนมีของ แห่งไหนยังว่าง

### ช่วงวันที่ · ปุ่มล้างค่า · ส่งออก CSV

พี่กันสั่งเพิ่มทีหลังทั้งสามอย่าง (26 ส.ค. 2569)

| สั่งว่า | ทำอะไร |
|---|---|
| *"เอาช่องเลือกวันที่อยู่ตรงแถวเดียวกันกับพวกนี้"* | ช่องวันที่อยู่แถวเดียวกับปุ่มช่วงเวลา ทำเหมือนหน้าประวัติทุกอย่าง |
| *"ปุ่มล้างค่า"* | ปุ่มล้างทุกเงื่อนไขในคราวเดียว โผล่เฉพาะตอนมีตัวกรองอยู่จริง |
| *"ขอปุ่มส่งออก CSV ด้วย"* | ใช้ `renderExportBtn` ตัวเดียวกับทั้งเว็บ |

🚨 **กุญแจแคชต้องรวมช่วงวันที่ที่เลือกเองด้วย** (`custom|from|to`)
ไม่งั้นเปลี่ยนวันแล้วได้รายการชุดเดิมกลับมาโดยไม่มีอะไรบอก

🚨 **กรอกวันแล้วต้องสลับปุ่มช่วงเวลาเป็น `custom` ทันที** และยิงเซิร์ฟเวอร์ต่อเมื่อกรอกครบสองช่อง
ส่วนกดปุ่มช่วงเวลาสำเร็จรูปต้องล้างวันที่ที่กรอกเองทิ้ง ไม่งั้นเหลือวันค้างในช่องที่ไม่ได้ใช้

🚨 **`/api/lots` รับ `from`/`to` เฉพาะเมื่อ `range=custom`** และตรวจรูปแบบ `ปปปป-ดด-วว` ก่อนเสมอ

🚨 **ไฟล์ CSV ส่งออกเฉพาะแถวที่กรองอยู่ (`sorted`) ไม่ใช่ทั้งหมด และไม่ใช่แค่ 40 แถวที่วาดอยู่**
ผู้ใช้กรอง รพ.สต. แห่งหนึ่งแล้วกดส่งออก ต้องได้ไฟล์ของแห่งนั้น ไม่ใช่ทั้งเดือน
หัวไฟล์เขียนบอกด้วยว่ากรองอะไรไว้ (`filterLabel`) เปิดไฟล์ทีหลังจะได้รู้ว่าตัวเลขนี้มาจากเงื่อนไขไหน
`lotsToCsv` ใน `lib/csv.js` เป็นคนละตัวกับ `recordsToCsv` โดยตั้งใจ — อันนั้นรายตัวยา อันนี้ราย Lot

⚠️ **เบราว์เซอร์ที่ถูกสั่งงานอัตโนมัติบล็อกการดาวน์โหลด** — เทสปุ่มส่งออกด้วยการกดแล้วดูไฟล์ไม่ได้
ใช้วิธีดัก `URL.createObjectURL` ในหน้าเว็บแล้วอ่านเนื้อไฟล์ออกมาแทน (ทำได้จริง ทดสอบแล้ว)

### จุดที่ต้องระวัง

🚨 **"ไม่มี Lot เลย" กับ "มี Lot แต่ตัวกรองคัดออกหมด" ต้องเป็นคนละข้อความ** (`lotsFilteredOut`)
ใช้ข้อความเดียวกันแล้วผู้ใช้จะไปไล่เปลี่ยนช่วงเวลา ทั้งที่ปัญหาอยู่ที่ช่องค้น

🚨 **ตัวนับกับยอดรวมต้องนับจากผลที่กรองแล้ว** ไม่ใช่ทั้งหมด
กรอง รพ.สต. แห่งหนึ่งแล้วต้องรู้ทันทีว่าเป็นเงินเท่าไร — เป็นเหตุผลหลักที่พี่กันขอตัวกรองนี้

🚨 **ชื่อผู้บันทึกยาวให้ขึ้นบรรทัดใหม่ ห้ามตัดทิ้ง** เป็นข้อมูลสืบกลับ (กฎเดิมจากข้อ 3.32)

🚨 **ปุ่มทุกปุ่มต้องมีข้อความกำกับ ห้ามย่อเหลือแต่ไอคอน**
รอบแรกย่อปุ่มแก้ไขกับดูรายการเหลือแต่ไอคอนเพื่อประหยัดที่ในตาราง
พี่กันสั่งเอากลับทันที: *"ไม่ย่อ เอากลับเหมือนเดิม"*
ไอคอนเปล่า ๆ ต้องเดาความหมาย และคนที่ไม่ได้ใช้ทุกวันจะไม่กล้ากด
คอลัมน์จัดการจึงกว้าง 262px และคอลัมน์อื่นถูกรีดลงมาให้พอดี

---

## 3.53 🖨 ใบสรุปต้องแขวนที่ `body` โดยตรง — แก้ข้อ 3.48 ที่ยังไม่จบ

**อาการที่พี่กันเจอ:** กดพิมพ์แล้วได้หน้ารายการ Lot ออกมาทั้งหน้า พร้อมปุ่มทุกปุ่ม

ข้อ 3.48 เคยบอกว่าแก้ด้วยการเปลี่ยนไปใช้ `visibility` แล้ว แต่ของจริงในไฟล์ยังเป็น

```css
body > * { display: none !important; }
body > div:has(.slip-paper) { display: block !important; }   /* ❌ */
```

**ต้นเหตุ** — Next.js ห่อทั้งเว็บไว้ในกล่องเดียวที่เป็นลูกชั้นแรกของ `body`
ตัวเลือกบรรทัดที่สองจึงไปตรงกับ**กล่องที่ครอบทั้งเว็บ** แล้วเปิดกลับมาทั้งหน้า
ไม่ใช่เรื่องที่เบราว์เซอร์รองรับ `:has()` หรือไม่ — ตัวเลือกมันตรงจริง ๆ แค่ตรงกับของผิดตัว

**วิธีแก้ที่ถูก — ย้ายใบไปแขวนที่ `body` ด้วย portal**

```jsx
// components/pages/lots.jsx → renderLotSlip
if (typeof document === 'undefined') return null;   // กันตอนเซิร์ฟเวอร์วาดจอ
return createPortal(sheet, document.body);
```
```css
body > * { display: none !important; }
body > .slip-backdrop { display: block !important; }   /* ✅ ตรงกับใบตัวจริงแน่นอน */
html, body { background: #fff !important; }            /* 🚨 กันแถบเทาก้นกระดาษ */
```

🚨 **ห้ามกลับไปใช้ `:has()` เดาโครงกล่องอีก** ไม่ว่าจะเขียนให้ซับซ้อนแค่ไหน
🚨 **ต้องมี `html, body { background: #fff }`** — กระดาษสูงเท่าเนื้อหา ส่วนที่เหลือใต้ใบ
จะเห็นพื้นหลังเว็บโผล่ออกมาเป็นแถบเทาก้นกระดาษ (เห็นได้จากไฟล์ PDF จริงเท่านั้น)

### หัวเอกสาร — สิ่งที่พี่กันสั่งเพิ่ม 26 ส.ค. 2569

| สั่งว่า | ทำอะไร |
|---|---|
| *"ขอเดือนชื่อเต็ม"* + *"ใส่ พ.ศ. ด้วย"* | `thaiDateFull()` ใน `lib/format.js` → `26 สิงหาคม พ.ศ. 2569` |
| *"มันซ้ำกันไหม"* | ตัดบรรทัด "เลข Lot" ทิ้ง เพราะเลขที่เอกสารคือ `ยค. + เลข Lot` อยู่แล้ว |
| *"ชื่อ รพ.สต. ต้องมีด้วย"* | บรรทัด **หน่วยบริการต้นทาง** ใช้ชื่อเต็มตามทะเบียน |

🚨 **ในใบพิมพ์ใช้ชื่อเต็มตามทะเบียน ไม่ใช่ชื่อสั้นที่ใช้บนหน้าจอ**
ใบนี้ถูกส่งกลับไปให้ รพ.สต. ต้นทางเก็บเป็นหลักฐาน เอกสารที่ออกนอกห้องยาต้องเรียกชื่อให้ถูกตามทะเบียน
map ชื่อสั้น → ชื่อเต็ม เก็บที่ `mr_setting.pcu_full` (ดู `scripts/015_pcu_full.sql`)
ไม่มีใน map ให้เติมคำนำหน้า `โรงพยาบาลส่งเสริมสุขภาพตำบลบ้าน` เป็นค่าสำรอง

🚨 **หน่วยบริการต้นทางต้องเป็นบรรทัดเต็มความกว้าง ห้ามยัดลงคอลัมน์**
ชื่อเต็มยาวเกินครึ่งกระดาษ ยัดลงคอลัมน์แล้วตกบรรทัด ดันบรรทัดอื่นเลื่อนตามจนหัวเอกสารสองฝั่งไม่ตรงกัน

---

## 3.54 🔬 เครื่องมือตรวจงานที่สร้างไฟล์ — ห้ามจำลอง

**พี่กันตั้งเป็นกฎ 26 ส.ค. 2569:** *"ถ้าเป็นการสร้างไฟล์ ต้องดูของจริง"*
· *"อย่าจำลอง ถ้าจำลองก็ไม่เห็นของจริงสิ"* · *"ทำออกมาจนกว่าจะดี เทสทุกครั้ง"*

**กฎนี้ถูกยกเป็นสกิลกลางแล้ว** — `C:\Users\PKH\.claude\skills\making-documents\SKILL.md`

| เครื่องมือ | ทำอะไร |
|---|---|
| `scripts/print-check.mjs` | สั่ง Chrome ในเครื่องสร้าง PDF จริงจากเว็บที่รันอยู่ → `out/` |
| `scripts/pdf-look.mjs` | แปลงหน้า PDF เป็นภาพ เพื่อเปิดดูด้วยตา (เครื่องนี้อ่าน PDF ตรง ๆ ไม่ได้) |
| `scripts/pdf-font.mjs` | คลายก้อนบีบอัดในไฟล์ PDF อ่านว่าฝังฟอนต์อะไรมาจริง |
| `scripts/ui-check.mjs` | ถ่ายภาพหน้าเว็บจริง + รายงานข้อผิดพลาดในคอนโซล |

```bash
node scripts/print-check.mjs && node scripts/pdf-look.mjs out/ใบสรุป-ล็อตล่าสุด.pdf
node scripts/ui-check.mjs lots        # จอคอม
node scripts/ui-check.mjs lots 430    # จอมือถือ
SHOW=1 node scripts/ui-check.mjs lots # เปิดหน้าต่าง Chrome ให้เห็นด้วยตา
```

🚨 **`print-check.mjs` ต้องตรวจก่อนเสมอว่าใบเปิดอยู่จริง** (`.slip-paper` มีในหน้า)
ไม่งั้นจะได้ PDF ของหน้าเว็บทั้งหน้าโดยไม่รู้ตัว แล้วเข้าใจผิดว่าเอกสารพัง
ทั้งที่จริงคือสคริปต์กดปุ่มไม่ติด

🚨 **`SHOW=1` เปิดหน้าต่างเบราว์เซอร์ให้เห็น** — พี่กันขอ 26 ส.ค. 2569
*"นี่เธอเทสในเว็บโครมปะ เราไม่เห็นเธอเปิดดูเลย"*
ปกติสคริปต์รันแบบไม่เปิดหน้าต่างเพื่อความเร็ว แต่พี่กันต้องเห็นได้เมื่อขอ

⚠️ **Browser pane ของ Claude ใช้ถ่ายภาพไม่ได้** — ไม่ compositing เมื่อแผงไม่แสดง
ต้องใช้ puppeteer ยิง Chrome ในเครื่องเอาเอง

---

## 3.55 🌙 ชื่อยาหายในโหมดมืด + ปุ่มบนจอสัมผัส + สีข้อความจาง

### 🔴 ชื่อยาหายในโหมดมืด (พี่กันเจอเอง 26 ส.ค. 2569)

การ์ด "ยาที่คืนมูลค่าสูงสุด 10 อันดับ" ในหน้าสรุปโหมดมืด **เห็นแต่ความแรงกับรูปแบบยา**
ชื่อสามัญหายไปทั้งบรรทัด อ่านไม่ออกว่ายาอะไร ซึ่งเป็นข้อมูลความปลอดภัย

**ต้นเหตุ** — `components/pages/drugname.jsx` ใช้ `color:#1e2420` (เกือบดำ) ตายตัว
พอพื้นการ์ดเป็นสีมืดก็กลืนหายไปหมด

**วิธีแก้** — `renderDrugName(parts, { dark })` เลือกชุดสีตามพื้นหลัง

| ส่วน | พื้นขาว | พื้นมืด |
|---|---|---|
| ชื่อสามัญ | `#1e2420` | `#e8ece9` |
| รูปแบบยา | `#414a44` | `#b9c2bc` |
| ความแรง | `#6b746e` | `#9aa5a0` |
| ไฮไลต์คำค้น | เขียวบนเขียวอ่อน | เขียวสว่างบนเขียวโปร่ง |

🚨 **เปลี่ยนเฉพาะสีที่เข้มจนกลืนพื้นมืด** — ม่วง (ตัวย่อ) · ส้ม (%) · แดงอมชมพู (ER/IR)
เป็นสีกลางที่อ่านออกทั้งสองพื้น **ห้ามเปลี่ยน** เพราะแต่ละสีมีความหมายเฉพาะตัว (ดูข้อ 3.4.3)

🚨 **หน้าสรุปเป็นหน้าเดียวที่มีโหมดมืด** — `vals/summary.js` ส่ง `sumDark` ให้ทั้ง 3 จุดที่วาดชื่อยา
หน้าอื่นไม่ต้องส่ง เพราะพื้นขาวเสมอ

### ต-12 · ปุ่มบนจอสัมผัส — วัดก่อนแก้เสมอ

พี่กันสั่ง *"ต12 ต้องทำ แล้วค่อย ๆ แก้ไป และอย่าไปหลงแก้ในเดสก์ท็อป ต้องเช็คเสมอ"*

เกณฑ์ 44×44 เป็นเรื่อง**นิ้วบนจอสัมผัส** ไม่ใช่เมาส์บนคอม ปุ่มเล็กบนเดสก์ท็อปไม่ใช่ปัญหา

**เครื่องมือ `scripts/tap-check.mjs`** วัดทุกหน้าที่จอ 390px แล้วรายงาน 2 อย่าง

1. ปุ่มที่เล็กกว่าเกณฑ์
2. **พื้นที่กดที่ทับกัน** ซึ่งอันตรายกว่าข้อแรกมาก

🚨🔴 **ปุ่มคู่ที่วางติดกันห้ามใช้คลาส `.tap`**
`.tap::before` ขยายพื้นที่กดออกด้านละ 11px ปุ่มที่ห่างกันน้อยกว่า 22px จึงมีพื้นที่กดทับกัน
วัดจริงเจอ: ปุ่ม แก้/ลบ ในการ์ดประวัติมือถือ 36×29 ห่างกัน 7px
**เล็งกด "แก้" แต่โดน "ลบ"** = ข้อมูลหายไปถังขยะโดยไม่ตั้งใจ
ต้องขยายตัวปุ่มเองให้ถึงเกณฑ์แทน (`min-height:44px`) แล้วถอด `.tap` ออก

| ที่แก้แล้ว | เดิม | ใหม่ |
|---|---|---|
| ปุ่ม แก้/ลบ/กู้คืน ในการ์ดประวัติ | 36×29 ห่าง 7px | สูง 44px ห่าง 12px |
| ชิปช่วงเวลาฝั่งมือถือทั้งแถว | สูง 30px ห่าง 6px | สูง 44px ห่าง 8px |

ผลวัดหลังแก้: หน้าประวัติจาก 116 จุดเหลือ 52 · พื้นที่กดทับกันในส่วนที่แก้เหลือ 0 คู่

### ก-12 · สีข้อความจางเกินเกณฑ์

`#9aa19c` → `#6f7873` ทั้งเว็บ **51 จุดใน 18 ไฟล์**

| สี | บนพื้นขาว | บนพื้นเทาอ่อน |
|---|---|---|
| เดิม | 2.36:1 ❌ | 2.2:1 ❌ |
| ใหม่ | 5.0:1 ✅ | 4.7:1 ✅ |

🚨 **ก่อนทำสีให้เข้มขึ้น ต้องตรวจว่าสีนั้นถูกใช้บนพื้นมืดด้วยหรือเปล่า**
ถ้าใช้ การทำให้เข้มขึ้นจะกลับแย่ลงอีกด้าน · ตรวจแล้วสีนี้ไม่ได้ใช้ในหน้าสรุปเลยสักจุด

⚠️ เหลือ 1 จุดที่ `app/login/page.js` ตั้งใจไว้ — สีตัวหนังสือบนปุ่มตอนยังกดไม่ได้

---

## 3.60 ⏳ โครงจางระหว่างรอข้อมูล (skeleton)

**พี่กันสั่ง 27 ส.ค. 2569:** *"หน้าโหลดทุกหน้า ทำ skeleton ไว้ให้หมด และต้องโครงเหมือนกับเว็บ"*

**กฎเต็มอยู่ในสกิลกลาง `loading-skeleton`** (`.claude/skills/loading-skeleton/SKILL.md`)
ตรงนี้เก็บเฉพาะเรื่องที่ผูกกับโปรเจกต์นี้

| ไฟล์ | ทำอะไร |
|---|---|
| `components/pages/skeleton.jsx` | ตัววาดทั้งหมด 6 ตัว |
| `app/globals.css` → `.skel` | แถบจางกับคลื่นแสง |
| `handlers/ui.js` → `toggleSkelDemo` | สวิตช์ค้างโครงไว้ดู |
| `vals/index.js` | ล้างข้อมูลทุกหน้าเมื่อเปิดสวิตช์ |

### 🚨 กับดัก 4 ข้อที่เจอในโปรเจกต์นี้

**① คลาส `.col-head` มี `position:sticky`**
โครงจางใช้คลาสเดียวกับตารางจริงเพื่อให้ความกว้างตรงกัน แต่หัวจะลอยไปติดขอบบนของกรอบ
แล้วไปโผล่กลางแถวจาง · **ต้องเขียน `position:static` ทับ**

**② หน้าประวัติกับหน้าคลังยาวาดหัวตารางของตัวเองอยู่แล้ว**
ต้องส่ง `noHead:true` ไม่งั้นหัวซ้อนกันสองแถว

**③ หน้าคลังยาใช้ `<table>` จริง ไม่ใช่กล่อง flex**
ใช้ `skelTable` ไม่ได้ ความกว้างจะไม่ตรงเพราะ `<colgroup>` คุมคนละแบบ
ต้องใช้ `skelTableTag` ที่วาดด้วยแท็กเดียวกัน

**④ ของที่ต้องซ่อนไม่ใช่แค่แถวในตาราง**
ปุ่ม "ดูเพิ่มอีก 60 รายการ" · ข้อความ "แสดง 60 จาก 84 รายการ" · ตารางจริงทั้งบล็อก
ถ้าลืมจะลอยอยู่ใต้โครงจาง

### หน้าสรุป — จางเฉพาะเนื้อหา

พี่กันทัก: *"สรุปควรเป็นหน้านี้เหรอ ต้อง skeleton ทั้งเว็บเหรอ"*

ปุ่มเลือกปีงบ · ชื่อหน้า · ปุ่มส่งออก · สวิตช์ธีม **ไม่ต้องรอเซิร์ฟเวอร์** จึงวาดของจริงเลย
จางเฉพาะตัวเลขกับกราฟที่รอข้อมูลจริง

### 🔴 บทเรียน

แคลร์ทำเสร็จแล้วดูแค่หน้าเดียว พี่กันเปิดดูเองทุกหน้าแล้วส่งภาพมา 4 ภาพ
**เจอปัญหาคนละอย่างทั้ง 4 ภาพ** — เรื่องเดียวกับที่พลาดตอนฟอนต์

**เปิดดูทุกหน้าจริง ๆ ก่อนบอกว่าเสร็จ**

---

## 3.61 ✅❌ หน้าผลหลังกดส่ง + ของค้างต้องส่งเองจนสำเร็จ

**พี่กันสั่ง 29 ส.ค. 2569:** *"ตอนที่กดส่งเสร็จแล้ว มันเปลี่ยนหน้าเลย แบบโปรเจกต์ DRP
แล้วกดตกลง มันถึงเปลี่ยนกลับเป็นหน้ากรอกจริง"* · *"จุดส่งไม่สำเร็จต้องดี ๆ เลย เอาเหมือนกัน
เพราะส่งไม่สำเร็จสำคัญมาก ระบบต้องเก็บไว้ในเครื่องก่อนถ้าไม่สำเร็จ และส่งให้เองถ้าเน็ตมา"*

เลือกแบบ ข จากมอคอัป 3 แบบ (มีการ์ดสรุปยอด) และสั่งว่าปุ่มใบสรุปให้ใช้ **ใบเดียวกับที่กดดูในเว็บ**

| ไฟล์ | ทำอะไร |
|---|---|
| `components/pages/result.jsx` | วาดทั้งหน้าสำเร็จและหน้าไม่สำเร็จ |
| `components/vals/result.js` | แปลง `state.result` เป็นข้อความ + นับถอยหลัง |
| `handlers/record.js` | ตั้ง `result` · `closeResult` · `openResultSlip` · `resultRetry` |
| `MedReturnApp.jsx` | ตัวตั้งเวลาถอยห่าง · เตือนก่อนปิดหน้าต่าง · กู้ของค้างตอนเปิดเว็บ |

### กฎ 7 ข้อ

1. 🚨 **ยอดบนหน้าผลคิดจาก `sending` ไม่ใช่ `st.rows`** — ระหว่างรอเซิร์ฟเวอร์ตอบ
   เภสัชกรพิมพ์ยาเพิ่มได้ นับจากของบนจอตัวเลขจะเกินจริง
2. 🚨 **`state.result` ห้าม persist ลงเครื่อง** — เปิดเว็บใหม่ต้องไม่เจอหน้าผลของล็อตเมื่อวานค้าง
   (กล่องแดงในหน้าบันทึกทำหน้าที่เตือนของค้างแทนอยู่แล้ว)
3. 🚨 **ระบบส่งเองห้ามเด้งหน้าเต็มจอ ทั้งตอนสำเร็จและตอนล้ม** — ใช้ `app.save({ auto: true })`
   เภสัชกรอาจกำลังกรอกล็อตถัดไป จู่ ๆ มีจอเต็มเด้งคือการขัดจังหวะ
   และถ้าล้มซ้ำทุก 5 นาทีจะกลายเป็นจอเด้งทั้งวันจนคนเลิกอ่าน
   ⚠️ `app.save` ถูกใช้เป็นตัวรับการกดปุ่มด้วย จึงได้ event object มาเป็น `opts`
      **ต้องเทียบ `opts.auto === true` เท่านั้น** ห้ามเช็คแค่ว่ามีค่าไหม
4. 🚨 **จังหวะลองส่งเองต้องถอยห่างขึ้น** `RETRY_WAITS = [20, 40, 80, 160, 300]` วินาที
   ค้างที่ 5 นาทีไปเรื่อย ๆ **ไม่หยุดเอง** — หยุดเมื่อไหร่แปลว่าต้องมีคนมากด ซึ่งคือสิ่งที่เลี่ยงอยู่
   ทำไมรอสัญญาณ "เน็ตกลับมา" อย่างเดียวไม่พอ: เบราว์เซอร์ยิงสัญญาณนั้นเมื่อ**หลุดจากเครือข่าย**
   แต่อาการจริงในโรงพยาบาลคือไวไฟครบทุกขีดแต่ออกเน็ตไม่ได้ ไม่มีสัญญาณไหนยิงเลย
5. 🚨 **ธง `saveFailed` + `failedBy` ต้องลงไปนอนใน localStorage ด้วย**
   เดิมเก็บแต่ยา ปิดแท็บแล้วเปิดใหม่ ยาอยู่ครบแต่เว็บนึกว่าเป็นร่างธรรมดา ไม่ลองส่งเองอีกเลย
   และคนเวรถัดไปที่เห็นรายการค้างอาจกดล้างทิ้งเพราะไม่มีอะไรบอกว่านี่คือของที่ยังไม่ได้ส่ง
6. 🚨🔴 **`failedBy` ห้ามเอาไปเติมในช่องผู้บันทึกเด็ดขาด** (กฎข้อ 3.24 ยังอยู่ครบ)
   ล็อตที่ค้างถูกเซ็นไปแล้วตอนกดส่งครั้งแรก การส่งซ้ำคือส่งของเดิมที่เซ็นไว้ จึงใช้ชื่อที่เก็บคู่ไว้
   แต่ **ช่องผู้บันทึกต้องว่างเปล่าเสมอตอนเปิดเว็บ** คนเวรถัดไปต้องเลือกชื่อตัวเองสำหรับล็อตใหม่
   `save()` จึงใช้ `const by = st.recorder || (st.saveFailed ? st.failedBy : '')`
7. 🚨 **ปุ่มคู่บนหน้าผลห้ามใส่คลาส `.tap`** — ห่างกัน 10px ส่วน `.tap` ขยายพื้นที่กดด้านละ 11px
   = พื้นที่กดทับกัน เล็ง "ตกลง" แล้วโดน "ดูใบสรุป" (กฎข้อ 3.55) ปุ่มสูง 50/44 ผ่านเกณฑ์อยู่แล้ว

### หน้าส่งไม่สำเร็จต้องตอบ 3 คำถาม

ไม่งั้นคนจะกดซ้ำรัว ๆ หรือปิดทิ้งแล้วลืม

| คำถามในใจคนใช้ | หน้าตอบว่า |
|---|---|
| ของหายไหม | ไม่หาย เก็บอยู่ในเครื่องนี้แล้ว ยังไม่ขึ้นระบบส่วนกลาง |
| ระบบทำอะไรอยู่ | **นับถอยหลังจริงทุกวินาที** จนกว่าจะลองส่งเองครั้งถัดไป + ลองไปแล้วกี่ครั้ง |
| ต้องทำอะไรต่อ | ส่งอีกครั้งเดี๋ยวนี้ · หรือเก็บไว้ส่งทีหลังแล้วทำงานอื่นต่อ |

🚨 **นาฬิกาถอยหลังเดินเฉพาะตอนหน้านั้นเปิดอยู่** (`componentDidUpdate` ตั้ง/เคลียร์ `_tickTimer`)
เว็บเปิดค้างทั้งวัน ตัวจับเวลาที่เดินเปล่าทุกวินาที = วาดจอทิ้งวันละหมื่นรอบ

🚨 **ข้อความลอย ๆ ว่า "ระบบจะลองใหม่ให้" ใช้ไม่ได้** ไม่มีอะไรพิสูจน์ว่าระบบยังทำงานอยู่
คนจะไม่เชื่อแล้วกดเองรัว ๆ · ต้องเห็นตัวเลขเดินจริง

### เตือนก่อนออกจากหน้า

**พี่กันสั่งเปลี่ยน 31 ส.ค. 2569:** *"ป้องกันการกดรีเฟรช มันจะถามก่อนว่าจะออกจากหน้าเว็บนี้ไหม
ถ้ายังไม่กดบันทึก"*

ตอนนี้ถามทุกครั้งที่**มีรายการค้างอยู่และยังไม่ได้กดบันทึก** ครอบคลุมทั้งกดรีเฟรช
ปิดแท็บ และปิดทั้งหน้าต่าง

⚠️ **กฎเดิมเขียนตรงข้าม** — เคยเตือนเฉพาะตอนส่งไม่สำเร็จ ด้วยเหตุผลว่าร่างที่กรอกค้าง
เป็นเรื่องปกติของงานประจำวัน เตือนบ่อยแล้วคนจะเลิกอ่าน · พี่กันสั่งเปลี่ยนแล้ว

🚨 **โหมดดูตัวอย่างไม่ถาม** ของในนั้นเป็นข้อมูลปลอมที่หายได้ไม่เสียหาย

🚨 **ข้อความที่โชว์เป็นของเบราว์เซอร์เอง แก้ไม่ได้** สิ่งเดียวที่เว็บทำได้คือสั่งให้ถามหรือไม่ถาม

ตรวจด้วย `scripts/leave-check.mjs` (4 ข้อ)

---

## 3.66 🚨🔴 ปุ่มยืนยันไม่มีฟังก์ชันผูก — บันทึกไม่ได้ทั้งเว็บ (v0.15.1.0)

**พี่กันเจอเอง 1 ก.ย. 2569** หลังใช้งานจริง
> เรากดส่งแล้วมันเด้งให้เลือกคนส่ง จากนั้นเรากดบันทึก มันไม่ไปเลย
> ในเว็บเดสก์ท็อป ก็กดส่งไม่ได้

### อาการ

ป๊อปยืนยันขึ้นครบถ้วนสวยงาม กดปุ่ม **"ยืนยันบันทึก"** แล้ว**เงียบสนิท**
ไม่มีข้อความผิดพลาด ไม่มีอะไรในคอนโซล ไม่มีคำขอออกไปหาเซิร์ฟเวอร์

### ต้นเหตุ

`confirmRun` (ตัวสั่งทำงานของปุ่มยืนยัน) อยู่ใน `components/vals/history.js`
แต่ข้อ 3.62 (ก-8) ใส่ระบบข้ามการคำนวณค่าของหน้าที่ไม่ได้เปิดไว้

```js
history: sc === 'history',          // ❌ อยู่หน้าบันทึก = ไม่คำนวณ = ไม่มี V.confirmRun
history: sc === 'history' || !!stt.confirm,   // ✅
```

**ป๊อปยืนยันถูกใช้ข้ามหน้า** — หน้าบันทึกใช้ทั้งตอนยืนยันก่อนส่ง และตอนลบรายการ
แต่ตัวสั่งทำงานอยู่ในไฟล์ของหน้าประวัติ พอไฟล์นั้นถูกข้าม ปุ่มก็กลายเป็นปุ่มตาย

🚨🔴 **ค่าที่ถูกใช้ข้ามหน้าห้ามอยู่ในไฟล์ที่ถูกข้ามการคำนวณได้**
ถ้าจำเป็นต้องอยู่ ต้องเติมเงื่อนไขให้คำนวณตอนของนั้นถูกใช้ด้วยเสมอ

⚠️ **CLAUDE.md ข้อ 3.62 เตือนเรื่องนี้ไว้เองแล้ว** ว่า "ตรวจก่อนตัดทุกครั้งว่าไม่มีหน้าอื่น
ใช้ค่าข้ามไฟล์" และเขียนว่าไล่ดูครบ 334 คีย์แล้ว — **แต่พลาดตัวนี้ไป**
การไล่ดูด้วยตาไม่พอ ต้องเทสเส้นทางจริงทุกเส้นหลังตัดการคำนวณ

### บทเรียนเรื่องการเทส

สคริปต์เทสเดิมกดบันทึกด้วย `setState` แล้วเช็คว่า state ถูกตั้ง ซึ่งผ่านหมด
บั๊กนี้จับได้เมื่อ**กดปุ่มจริงบนหน้าจอตามลำดับที่พี่กันทำ**เท่านั้น

| วิธีเทส | จับบั๊กนี้ได้ไหม |
|---|---|
| `setState` แล้วอ่าน state | ❌ ผ่านหมด |
| เรียก `app.save()` ตรง ๆ | ❌ ผ่าน (ข้ามป๊อป) |
| **กดปุ่มจริงทีละขั้นเหมือนคนใช้** | ✅ **เจอ** |

เครื่องมือ: `scripts/mobile-flow-check.mjs` — เดินตามอาการที่พี่กันเล่าเป๊ะ ๆ
กดปุ่มบันทึก → เลือกผู้บันทึกจากเมนู → กดบันทึกอีกครั้ง → กดยืนยัน → ดูว่ายิงคำขอไหม

🚨 **ชื่อปุ่มในป๊อปคือ "ยืนยันบันทึก" ไม่ใช่ "บันทึก"** — ตัวตรวจที่หาผิดชื่อจะรายงานว่า
"ไม่ยิงอะไรเลย" ทั้งที่โค้ดถูกแล้ว (เสียเวลาไล่ผิดทางไปหนึ่งรอบ)

### แถบบันทึกฝั่งมือถือ

พี่กันสั่งเพิ่ม: *"ไปบีบจุดนี้หน่อย เพราะตอนใช้ในมือถือ มันกินเนื้อที่มาก"*

205px → **186px** (24% ของจอ 780px) · ตัวเลขยอด 25 → 21px · ปุ่ม 52 → 48px (ยังเกิน 44)
🚨 **ไม่ตัดข้อมูลอะไรทิ้ง** ยอดประหยัด สูญเสีย แถบสัดส่วน อยู่ครบ บีบแค่ขนาดกับระยะห่าง

---

## 3.70 ✅ ตารางในเว็บนี้ยังไม่เป็นตารางจริง — ปิดแล้ว 4 ก.ย. 2569 (ดูข้อ 3.75)

**พี่กันสั่ง 1 ก.ย. 2569 หลังเห็นว่าตารางสามหน้าหน้าตาไม่เหมือนกัน**

> ตั้งเป็นกฎ ต้องสร้างตารางจริงเท่านั้น
> รู้ละที่ตอนนั้นด่าไปเยอะเพราะอะไร
> **เราจะรื้อให้เป็นตารางจริงให้หมด แต่ไม่ใช่ตอนนี้**

### สถานะตอนนี้

| ตาราง | สร้างด้วยอะไร | เส้นแบ่งคอลัมน์ | ไฮไลต์แถวตอนชี้ |
|---|---|---|---|
| รายการ Lot | กล่องเรียงกัน (`.col-head` / `.col-row`) | ✅ | ✅ |
| ประวัติ | กล่องเรียงกัน (`.sticky-head` + แถวเขียนเอง) | ❌ | ❌ |
| คลังยา | **ตารางจริง** (`<table>` + `.cat-row`) | ❌ | ❌ |

### สิ่งที่ต้องทำ — ✅ ทำครบแล้ว 4 ก.ย. 2569 ใน v0.19.0.0 (รายละเอียดอยู่ข้อ 3.75)

รื้อ **รายการ Lot** กับ **ประวัติ** ให้เป็น `<table>` จริงเหมือนหน้าคลังยา
แล้วเส้นแบ่งคอลัมน์กับไฮไลต์แถวจะได้มาฟรีทั้งสามหน้าโดยไม่ต้องเขียนกฎสองชุด

🚨 **ที่ต้องระวังตอนรื้อ**
- การตรึงหัวตารางเคยพังมาแล้วทั้งสองหน้า (ข้อ 3.2 · 3.25 · 3.59)
  ตารางจริงใช้ `thead` + `position:sticky` ได้ตรง ๆ ไม่ต้องวัดด้วย ResizeObserver อีก
  แต่ **กรอบที่ครอบตารางห้ามมี `overflow`** ไม่งั้น sticky ตายทันที
- หน้าประวัติกับรายการ Lot สลับเป็นการ์ดบนมือถือ — การ์ดคงไว้เหมือนเดิม
  รื้อเฉพาะฝั่งคอม (ข้อยกเว้นของกฎกลาง)
- หน้าคลังยามี 417 แถวที่ทยอยวาดตามการเลื่อน (ข้อ 3.62) ห้ามทำให้ช้าลง

🔑 **กฎกลางอยู่ในสกิล `working-with-gun` หัวข้อ 6.1** — ใช้กับทุกเว็บ

---

## 3.76 🗂 แยกสกิลกลางเป็น 6 ตัว + เก็บกวาด (v0.19.1.0 · 5 ก.ย. 2569)

### ที่แก้ในเว็บ

| ที่ | แก้อะไร |
|---|---|
| `pages/catalog.jsx` | กรอบขาวหน้าคลังยาใส่ `min-width: fit-content` — จอแคบกว่าตารางแล้วตารางไม่ทะลุกรอบอีก |
| `vals/shell.js` | แท็บ "ตั้งค่า" กับ "เกี่ยวกับ" เคยไฮไลต์พร้อมกันทั้งคู่ · แยกเป็น `settingsOn` / `aboutOn` |
| `scripts/*.mjs` **30 ไฟล์** | รับ `process.env.PORT` แทนพอร์ต 3000 ตายตัว — พอร์ตไม่ว่างก็ยังเทสได้ |

### 🔴🔴🔴 บทเรียนใหญ่ของรอบนี้ — จดกฎผิดที่แล้วรายงานว่าจดแล้ว

พี่กันถามว่ากฎไอคอนแว่นขยายกับกฎฟอนต์ลูกศรอยู่ในสกิลไหม ค้นแล้ว **ไม่มี**
ทั้งที่รอบก่อนรายงานไปว่า "เก็บเป็นกฎแล้ว"

```bash
git log --all -S "Roboto Mono" -- .claude/skills/    # → ว่างเปล่าทุก commit
```

ของจริงคือ **จดลง `CLAUDE.md` ของ ME-DRP** ซึ่งเว็บอื่นไม่มีทางเห็น

🔑 **`CLAUDE.md` = คู่มือของโปรเจกต์เดียว · สกิล = ของที่ทุกโปรเจกต์อ่าน**
กฎเต็มอยู่ในสกิล `working-with-gun` **ข้อ 4.9** (ตั้งใหม่วันนี้) — รวมถึงข้อบังคับว่า
**แก้สกิลเสร็จต้องส่งไฟล์ให้พี่กันเปิดดูเอง** ไม่ใช่บอกว่าจดแล้ว

### สกิลกลางแยกเป็น 6 ตัว

ไฟล์เดิม 2 ตัวยาว 1,323 กับ 1,614 บรรทัด **ท้ายไฟล์ถูกตัดตอนบทสนทนายาว**
กฎที่เขียนไว้ครบจึงไม่เคยถูกอ่าน

| สกิล | บรรทัด | ครอบคลุม |
|---|---|---|
| `working-with-gun` | 1,005 | วิธีทำงาน · push · ข้อผิดพลาดห้ามซ้ำ · **ข้อ 4.9 จดที่ไหน** |
| `pharmacy-web-logic` | 1,044 | ฐาน · สถิติ · ลบ · เน็ตหลุด · ความปลอดภัย · คลังยา |
| `search-system` 🆕 | 567 | ระบบค้นหา 17 ข้อ · ช่องค้นหา · วรรณยุกต์ไทย |
| `web-craft` 🆕 | 359 | ตารางจริง + CSS กลาง · **ลูกศรเรียงลำดับ** · ชั้นหน้าต่างซ้อน · คีย์บอร์ด |
| `drug-display` 🆕 | 287 | ชื่อยาบนหน้าจอ · สี · **สีเม็ดยา Warfarin** |
| `medical-data-safety` 🆕 | 151 | ห้ามเดาข้อมูลยา |

🚨 **แยกไฟล์แล้วไม่ได้แปลว่าอ่านตัวเดียวจบ** — แยกเพื่อให้ **หาง่ายขึ้น ไม่ใช่ให้อ่านน้อยลง**

### เอกสารที่แก้

`docs/AUDIT-STATUS.md` — หัวไฟล์เคยเขียนว่า `⬜ 0` ทั้งที่เนื้อไฟล์ยังมี ⬜ 3 ข้อ
ตอนนี้นับจากเนื้อไฟล์จริงแล้ว **✅ 41 · ◐ 3 · ⬜ 2** พร้อมคำสั่งนับกำกับไว้ในไฟล์
📌3 (`drugs` ที่ `anon` แก้ได้) **ตรวจฐานจริงแล้วปิดอยู่แล้ว** — RLS เปิด ไม่มี policy สักข้อ
และทุกเว็บเขียนตารางนี้ผ่านหลังบ้านด้วย `service_role` ทั้งหมด

---

## 3.75 📊 ตารางจริงทั้งเว็บ + กรอบขาวใบเดียว (v0.19.0.0 · 4 ก.ย. 2569)

**พี่กันสั่งเมื่อ 1 ก.ย. 2569 ว่า "ตั้งเป็นกฎ ต้องสร้างตารางจริงเท่านั้น" แต่ยังไม่ได้ทำ**
วันนี้พี่กันสั่ง *"เรามาแก้ตารางกันเถอะ จะได้เสร็จสิ้นซะที"* — ข้อ 3.70 ปิดแล้ว

### ต้นแบบคือหน้ารายการ Lot ไม่ใช่หน้าคลังยา

พี่กันย้ำกลางทาง: *"ย้ำ เอาต้นแบบมาจาก lot และปุ่มกดขึ้นลงตามนี้"*
ค่าทุกตัว — ระยะขอบ 15 จุด · เส้นแบ่งคอลัมน์ · ไฮไลต์แถว · ลูกศรเรียง — ยกมาจากที่นั่น

### ① CSS กลางชุดเดียว — `app/globals.css` คลาส `.tbl`

| คลาส | ใช้กับ |
|---|---|
| `.tbl` | โครงเต็ม — หน้าบันทึก · ประวัติ · รายการ Lot |
| `.tbl-grid` | เฉพาะเส้นแบ่ง + ไฮไลต์แถว — หน้าคลังยา (หัวเป็นสีเขียวเข้มของตัวเอง ไม่เปลี่ยน) |
| `.tbl-zebra` | แถวสลับสีจาง — หน้ารายการ Lot |
| `.wrap` | คอลัมน์ที่ยอมให้ขึ้นบรรทัดใหม่ (ชื่อผู้บันทึก) |
| `.ta-r` · `.ta-c` | จัดตำแหน่งรายคอลัมน์ |

🚨 **ค่าเริ่มต้นของทุกช่องคือ `white-space:nowrap`**
เดิมใส่ `overflow-wrap:anywhere` ให้ทุกช่อง ผลคือปุ่มกลายเป็น "แ ก้" และมูลค่าตกสองบรรทัด
คอลัมน์ที่ต้องการให้ตัดบรรทัดต้องใส่คลาส `wrap` เอง

🚨 **เส้นแบ่งของหน้าคลังยาใช้ `box-shadow: inset` ไม่ใช่ `border-right`**
ตารางนั้นไม่มี `table-layout:fixed` เส้นขอบจะไปเพิ่มความกว้างอีก 14 จุด (1px × 14 คอลัมน์)
ทำให้ตารางที่ล้นอยู่แล้วยิ่งล้น จนปุ่มขวาสุดลอยทับแถบด้านบน

### ② ลูกศรเรียงลำดับ — เหมือนกันครบ 4 หน้าแล้ว

```
↑↓  ยังไม่ได้เรียงด้วยคอลัมน์นี้   16px
↑   น้อยไปมาก                    19px
↓   มากไปน้อย                    19px
```

พี่กันสั่งเรื่องนี้ไว้ตั้งแต่ **27 ส.ค. 2569** แต่ตอนนั้นแก้แค่หน้ารายการ Lot
อีกสามหน้าค้างของเก่า (สามเหลี่ยม `▲▼↕` ขนาด 9px) มาจนวันนี้

🚨 **หน้าคลังยาใช้ลูกศร 12px ไม่ใช่ 16/19**
หน้านั้นมี 15 คอลัมน์ ลูกศรใหญ่เท่าหน้าอื่นทำให้หัวคอลัมน์ดันตารางจนล้นขอบ

### ③🔴 ลูกศรต้องบอกความจริง

หน้าประวัติเรียงวันที่ใหม่ไปเก่ามาตั้งแต่เปิดหน้า (ฐานส่งมาแบบนั้น)
แต่ `histSortKey` เป็นค่าว่าง ลูกศรจึงแสดง `↑↓` ทั้งที่เรียงอยู่จริง — **ลูกศรโกหก**

```js
const on = sortKey ? sortKey === c.key : c.key === 'date';
const dir = sortKey ? st.histSortDir : 'desc';
```

🚨 **กดคอลัมน์วันที่ครั้งแรกต้องสลับเป็นเก่าไปใหม่ทันที** ไม่ใช่ตั้ง `desc` ซ้ำ
ไม่งั้นกดแล้วหน้าจอไม่เปลี่ยนอะไรเลย ดูเหมือนปุ่มเสีย

### ④ กรอบขาวใบเดียวครอบทั้งหน้า

พี่กันสั่ง *"ทำหน้านี้ให้เหมือนเพื่อน"* — เดิมหน้าประวัติกับรายการ Lot
วางแถบเครื่องมือบนพื้นเทานอกกรอบ แล้วตารางมีกรอบของตัวเองอีกใบ
ซึ่งไม่มีหน้าไหนในเว็บทำแบบนั้น (หน้าคลังยาใช้กรอบใบเดียวมาตลอด)

🚨🔴 **แถบที่ตรึงไว้ในกรอบที่มีระยะขอบใน ต้องทำครบ 3 อย่างพร้อมกัน**
ขาดข้อใดข้อหนึ่งจะเห็นเป็นบั๊กคนละแบบ

| ต้องทำ | ถ้าไม่ทำ |
|---|---|
| `top` ติดลบเท่าระยะขอบใน (`-16px`) | ติดต่ำลง 16 จุด เกิดร่องให้แถวลอดผ่าน |
| ระยะขอบติดลบ + ระยะในชดเชย | เห็นแถวลอดตรงร่องซ้ายขวา |
| มุมมนตรงกับกรอบ (`14px 14px 0 0`) | มุมเหลี่ยมทับมุมมนของกรอบ |

🚨 **หัวตารางต้องติดที่ `calc(ความสูงแถบหัว - 16px)`**
ตัววัด `--histhead` / `--lotshead` วัดความสูงเต็มซึ่งรวมระยะขอบในไว้ด้วย
เอามาใช้ตรง ๆ หัวตารางจะติดต่ำกว่าที่ควร 16 จุด

🚨 **กรอบขาวต้องมี `min-width: fit-content`**
ตารางล็อกความกว้างคอลัมน์ไว้ตายตัวราว 1,300 จุด จอที่แคบกว่านั้นถ้ากรอบไม่ยอมกว้างตาม
ตารางจะทะลุออกไปนอกกรอบ เห็นเป็นปุ่มกับหัวตารางลอยทับกัน

### ⑤ หน้าบันทึกใช้ตาราง 2 ก้อน ไม่ใช่ก้อนเดียว

หัวต้องตรึงอยู่**นอก**พื้นที่เลื่อน ส่วนแถบล็อตค้างต้องอยู่**ใต้หัว**และเลื่อนหายได้
(พี่กันสั่งไว้ว่าไม่ต้องตรึงแถบนั้น) ถ้ารวมเป็นตารางเดียว สองอย่างนี้จะสลับที่กัน

🚨 **ทั้งสองก้อนต้องใช้ `<colgroup>` ชุดเดียวกันเป๊ะ** ไม่งั้นคอลัมน์เหลื่อม
🚨 **ช่องที่มีป้ายผลลัพธ์สูตรต้องเป็น `position:relative`** ป้ายใช้ `absolute` เกาะอยู่

---

## 3.74 🧩 ช่องค้นหากับหัวเว็บ รวมเป็นตัวกลางตัวละตัว (v0.18.0.0 · 4 ก.ย. 2569)

พี่กันไล่ดูหน้าจอจริงทั้งวันแล้วสั่งทีละจุด · จบลงที่การรื้อสองอย่างให้เหลือตัวกลางตัวเดียว

### ① ช่องค้นหา 8 ช่อง → `components/pages/thaibox.jsx`

**พี่กันสั่ง:** *"ทำกรอบค้นหา ให้เหมือนกันที คือมีไอค่อนเเว่นขยาย ทำเป็นกฎ"*

เดิม 8 ช่องวาดกันคนละแบบ — บางช่องมีแว่นขยาย บางช่องไม่มี · บางช่องตัดวรรณยุกต์ ·
ปุ่มล้างคำค้นขนาดไม่เท่ากันสักช่อง

| ตัวใน `thaibox.jsx` | ทำอะไร |
|---|---|
| `renderThaiInput` | โครงสามชั้นกันวรรณยุกต์โดนตัด (ชั้นล่างสุด) |
| `renderSearchBox` | ช่องค้นหาเต็มรูปแบบ — แว่นขยาย · ปุ่มล้าง · ป้าย "ค้นว่า" |

🚨 **ช่องค้นหาทุกช่องต้องเรียก `renderSearchBox` ห้ามวาดเอง**
🚨 **ขนาดทุกอย่างคำนวณจาก `h` ตัวเดียว** — ไอคอน ปุ่มล้าง ระยะขอบ ระยะบรรทัด

### 🚨🔴 ตัวอักษรในชั้นวาดต้องนั่งตรงกับช่องกรอก

พี่กันส่งภาพมา 3 รอบ — *"คำที่ค้น อยู่กลางกรอบยัง"* · *"เราว่ามันอยู่บนๆไป"*

**`<input>` จัดตัวอักษรกึ่งกลางด้วยกลไกของตัวเอง โดยไม่สนระยะบรรทัดที่สั่ง**
ส่วนกล่องธรรมดาเคารพระยะบรรทัด → เส้นฐานสองชั้นอยู่คนละที่

⚠️ **วัดกล่องแล้วจะบอกว่าถูก** — กล่องตัวอักษรอยู่กลางกรอบเป๊ะ เยื้อง 0 จุด
สิ่งที่เยื้องคือ**ตัวอักษรข้างในกล่อง** ซึ่งการวัดกรอบจับไม่ได้

**แก้:** ระยะบรรทัดของชั้นวาด = ความสูงในกรอบ (`innerH`) ไม่ใช่ 1.75 เท่า
ยังเหลือที่ว่างบนล่างข้างละ 7 จุดให้วรรณยุกต์เท่าเดิม

🔑 **ตรวจด้วยไอคอนในช่องเดียวกัน** — แว่นขยายจัดกึ่งกลางด้วย `top:50%` ซึ่งกลางจริงเสมอ
ตัวอักษรต้องอยู่ระดับเดียวกับไอคอน · เร็วและเชื่อได้กว่าการวัดตัวเลข

### ② หัวสามหน้า → `components/pages/pagehead.jsx`

**พี่กันวัดด้วยตาแล้วจับได้:** *"สองปุ่มนี้ ขยับตลอดทั้งสามหน้า"*

วัดจริงยืนยัน — ปุ่มขอบบน **11 / 18 / 15** · ช่องไฟระหว่างปุ่ม **7 / 10 / 10** ·
ปุ่ม ⚙ หน้าสรุป **ล้นออกนอกขอบจอ** เพราะแถวหัวยัดสวิตช์ธีมไว้ด้วย

| ค่าที่ตัวกลางคุม | ค่า |
|---|---|
| `HEAD_PAD` | `padding:11px 20px 11px` — **ทุกหน้าต้องใช้ค่านี้** |
| ระยะห่างใต้หัว | 12px (อยู่ในตัวกลาง) |
| ปุ่ม ℹ ⚙ | 38×38 · ช่องไฟ 7 · พื้น `#fff` |
| โลโก้ ฿ | 30×30 radius 8 |

**ค่าทุกตัวลอกจากหน้าบันทึกมาเป๊ะ** เพราะพี่กันบอกว่าหน้านั้นคือหน้าหลักที่ตรึงไว้แล้ว
(*"หน้านี้คือหน้าหลักที่ fix วันที่ปุ่มกดต้องอยู่ระดับเดียวกันเท่านั้น"*)

🚨 **พื้นปุ่มต้องระบุ `#fff` ตรง ๆ ห้ามปล่อยโปร่งใส**
ปล่อยโปร่ง = สีที่ตาเห็นขึ้นกับพื้นของหน้านั้น หน้าสรุปไม่มีแถบขาวรอง ปุ่มเลยกลายเป็นเทา
(พี่กันสั่ง *"เอาสีเหมือนกันสิ สีขาว"*)

🚨 **ความสูงกลุ่มซ้ายห้ามเกิน 38 จุด** ไม่งั้นแถวสูงขึ้น ปุ่มที่จัดกึ่งกลางแถวเลื่อนลงตาม
ชื่อเว็บ 18×1.2 + บรรทัดรอง 11×1.45 = 37.6 พอดี

**บรรทัดรองของแต่ละหน้า** — บันทึก `v0.18.0.0` (เทล) · ประวัติ `ประวัติ · N รายการ · ยอด` · สรุป `สรุป`
🚨 **ห้ามซ้ำกับปุ่มที่อยู่ในแถวเดียวกัน** (พี่กันสั่ง *"ไม่เอา ซ้ำ"*) — หน้าสรุปจึงไม่เขียนปีงบต่อท้าย
เพราะปุ่มปีงบอยู่แถวถัดไปแล้ว

🚨 **เลขรุ่นดึงจาก `APP_VERSION` ตรง ๆ ห้ามใช้ `V.appVersion`**
ค่านั้นอยู่ใน `vals/settings.js` ซึ่งถูกข้ามการคำนวณตอนไม่ได้เปิดหน้าตั้งค่า (ข้อ 3.62)
ใช้แล้วจะได้ `vundefined` โผล่บนหัวเว็บ

### ③🔴 ปุ่มปีงบหายตอนรีเฟรช

**พี่กันเจอเอง:** *"เรารีเฟรชเเล้ว ปี หาย"*

`fyPicks` รอรายชื่อปีจากเซิร์ฟเวอร์ ระหว่างรอมีปีเดียว แล้วตัววาดมีเงื่อนไข
`if (V.fyPicks.length < 2) return null` → หายทั้งแถว แล้วโผล่เองตอนข้อมูลถึง = หน้ากระโดด

**แก้** — ระหว่างรอใส่ `[ปีนี้, ปีที่แล้ว]` ไว้ก่อน ปีงบคำนวณจากนาฬิกาเครื่องได้ ไม่ต้องถามใคร

⚠️ **ข้อ 3.60 เขียนเตือนไว้เองแล้ว** ว่าปุ่มปีงบ "ไม่ต้องรอเซิร์ฟเวอร์ จึงวาดของจริงเลย"
**แต่โค้ดจริงรออยู่** — กฎเขียนไว้แล้วแต่ของไม่ได้ทำตาม (ตระกูลเดียวกับข้อ 3.44 · 3.72)

### 🚨🔴🔴 บทเรียนใหญ่ที่สุดของรอบนี้ — วัดคนละสภาพกับที่พี่กันดู

แคลร์วัดแล้วรายงานว่า "ปุ่มปีงบอยู่ครบ" สองรอบ ทั้งที่พี่กันเห็นว่าหาย

| แคลร์วัดยังไง | พี่กันทำยังไง |
|---|---|
| เดินจากหน้าบันทึก (ข้อมูลโหลดมาแล้ว) แล้วกดสรุป | **รีเฟรชสด** แล้วกดสรุปทันที |
| ไม่มีแถบเลื่อนกินที่ | ดูใน F12 ซึ่งมีแถบเลื่อนแบบเดสก์ท็อป |

**ตัวตรวจที่วัดคนละสภาพ = ตัวตรวจที่โกหก** · ต้องเทสตามลำดับที่พี่กันเล่าเป๊ะ ๆ เสมอ
(ตระกูลเดียวกับบทเรียนข้อ 3.66 — กดปุ่มจริงทีละขั้นเท่านั้นถึงจับบั๊กได้)

รอบหนึ่งตัวตรวจฟ้องว่า "ปุ่มหายตอนโหลด" ซึ่งก็ผิดอีก เพราะรีเฟรชแล้วเว็บกลับไปหน้าบันทึก
แคลร์เลยไปวัดหน้าที่ไม่มีปุ่มปีงบอยู่แล้ว — **เกือบรายงานผิดสองทางในเรื่องเดียวกัน**

### 🚨🔴 แทรกโค้ดลงฝั่งคอมที่ห้ามแตะ ไปแล้วสองรอบ

ใช้ `indexOf` หาข้อความเพื่อระบุจุดแทรก **แต่ข้อความชุดเดียวกันมีทั้งฝั่งคอมและฝั่งมือถือ**
ตัวแรกที่เจอคือฝั่งคอม โค้ดจึงไปลงผิดฝั่ง — จับได้ตอนถ่ายภาพเทียบ ไม่ใช่ตอนเขียน

**คืนไฟล์ด้วย `git checkout` แล้วทำใหม่โดยอ้างเลขบรรทัดในช่วงของฟังก์ชันมือถือเท่านั้น**
🚨 **แก้ไฟล์ที่มีสองฝั่งในไฟล์เดียว ห้ามค้นด้วยข้อความ** ต้องจำกัดช่วงบรรทัดก่อนเสมอ

### เครื่องมือตรวจใหม่ 2 ตัว

| สคริปต์ | ตรวจอะไร |
|---|---|
| `scripts/head-align-check.mjs` | วัดตำแหน่งกับสีปุ่มหัวทั้งสามหน้า เทียบว่าตรงกันไหม (13 ค่า) |
| `scripts/head-compare.mjs` | ต่อภาพหัวสามหน้าซ้อนกันพร้อมเส้นไกด์ → `out/ui/head-compare.png` |

### เก็บกวาดรอบเดียวกัน

| สั่งว่า | ทำอะไร |
|---|---|
| *"มันกินที่ เเละไม่สวย"* (หน้ารายการ Lot) | หัวจาก 6 แถวเหลือ 3 · ตัวกรองเข้าแผ่นเลื่อนขึ้นจากล่าง · หัวกินจาก 62% เหลือ 20% |
| *"เป็นโทนเดียวกันด้วย เเต่ให้ปุ่มดู lot เด่น"* | หน้าประวัติทำตาม + ปุ่มรายการ Lot เป็นปุ่มเขียวเต็มแถว |
| *"ควรกำกับว่า เกี่ยวกับ ส่วนตั้งค่าก็ตั้งค่า"* | หัวเรื่องตรงกับปุ่มที่กดมา · ทั้งสองหน้าปุ่มกลับเหลือลูกศร หัวเรื่องอยู่ข้าง ๆ |
| *"ในเพจตั้งค่า มันสีแปลกๆ"* | หน้าตั้งค่ามีกรอบขาวชุดเดียวกับหน้าเกี่ยวกับ · ปุ่มธีม "สว่าง" จากดำเป็นเขียวของเว็บ |
| *"ตัดบรรทัดดีๆ"* | คำอธิบายหน้าเกี่ยวกับแยกสองบรรทัดเอง ไม่ปล่อยให้ตัดเป็น "ตัวชี้วัดราย" |
| *"ปุ่มส่งออก CSV ย่อลงหน่อย เอาไปไว้ข้างๆโหมดมืด"* | แถบเขียวเต็มความกว้างหายไป เหลือปุ่มเล็กข้างสวิตช์ธีม |

🚨 **`scrollbar-gutter: stable` ในไฟล์มือถือ** — จองที่ให้แถบเลื่อนเสมอ
มือถือจริงใช้แถบลอยทับจึงไม่เสียที่สักจุด (วัดแล้วได้ 0 ทั้งสองสภาพ)
แต่ตอนพี่กันเปิด F12 ดูงานจะเห็นตรงกัน

### ตัวตรวจที่ฟ้องผิด แก้แล้ว 2 ตัว

- **`tap-check.mjs`** ไม่ตั้งชื่อเครื่องก่อนวัด → หน้าต่างถามชื่อเครื่องบังจอ
  แล้วไปวัดปุ่มในหน้าต่างนั้น ฟ้องว่าพื้นที่กดทับกัน 3 คู่ทั้งที่โค้ดถูก
- **`ui-check.mjs`** หาปุ่มไปหน้ารายการ Lot ด้วยข้อความ พอปุ่มเปลี่ยนชื่อเป็น
  "ดูเป็นรายการ Lot" ก็กดไม่ติด แล้ว**ถ่ายหน้าผิดมาให้เงียบ ๆ** — ตอนนี้หาด้วย `aria-label`

---

## 3.73 🔊 ไม่มีอะไรพังเงียบอีก + บัตรผ่านต่ออายุเอง (v0.17.2.0 · 2 ก.ย. 2569)

**พี่กันสั่ง "ทำหมดเลยนะ"** กับ 3 เรื่องที่ได้จากการอ่านสกิลกลางทั้งไฟล์
และย้ำกฎสำคัญว่า **"สกิลกลาง คือกฎนะ"** — ทุกอย่างในสกิลเป็นกฎ ไม่ใช่แค่บันทึกว่าเว็บอื่นทำอะไร

### ① ตัวช่วยกลางต้องทนก้อนที่ไม่ใช่ `Error`

`supabase-js` **ไม่ได้โยน `Error`** แต่โยนก้อน `{ message, code, details, hint }`
`String(e)` จะได้ `"[object Object]"` — **ร้องแล้วอ่านไม่รู้เรื่อง = ไร้ประโยชน์พอ ๆ กับเงียบ**
แต่หลอกให้คิดว่าทำเสร็จแล้ว (ME-DRP เจอของจริง)

**3 ตัวใหม่ใน `lib/maskHn.js`** (ไม่ใช่ `apiError.js` เพราะไฟล์นั้น import `next/server`
ฝั่งเบราว์เซอร์เรียกไม่ได้)

| ตัว | ทำอะไร |
|---|---|
| `errParts(e)` | แยกเป็น `{ code, body }` |
| `errText(e)` | ประกอบกลับเป็นข้อความเดียว ไม่ปิดบัง |
| `safeErrLine(e)` | ปิดบังเนื้อความ **แต่คงรหัสฐานไว้** |

### 🚨🔴 รหัสของ Postgres เป็นเลข 5 ตัว — ตัวปิดบังจะกลบทิ้ง

```
รหัส 23505 · duplicate key   →   รหัส *** · duplicate key      ❌ ทดสอบแล้วเกิดขึ้นจริง
```

`23505` บันทึกซ้ำ · `42P01` ไม่มีตาราง · `23503` ผิดกฎความสัมพันธ์
**เป็นกุญแจดอกแรกของการไล่บั๊ก** ปิดบังไปแล้วเปิดบันทึกมาก็ไม่รู้ว่าพังเพราะอะไร

🚨 **ต้องแยก `code` ออกก่อนปิดบัง แล้วค่อยประกอบกลับ** ไม่ใช่ปิดบังทั้งก้อน

⚠️ ทุก route ในเว็บนี้ดึง `.message` ออกมาก่อนโยนอยู่แล้วครบทุกจุด
ตัวนี้จึงเป็น **ตาข่ายกันอนาคต** — วันหน้ามีคนเขียน `throw res.error` ตรง ๆ ก็ยังอ่านออก

---

### ② `lib/clientLog.js` — ฝั่งเบราว์เซอร์ก็ต้องร้อง

**เว็บนี้มีหลังบ้านครบ แต่ยังมี 6 จุดที่พังเงียบ** เพราะ `catch {}` ที่อยู่ในโค้ดฝั่งจอ
**ทำงานบนเครื่องของเภสัชกร ไม่เคยผ่าน worker เลยสักครั้ง** → Workers Logs ไม่เห็นอะไรทั้งนั้น

| จุดที่เคยเงียบ (`handlers/data.js`) | เกิดอะไรถ้าล้ม |
|---|---|
| `loadServerDrafts` | แถบล็อตค้างไม่ขึ้น โดยไม่มีใครรู้ |
| `syncDrugs` · `pullDrugs` | แก้ชื่อยาที่เว็บอื่นแล้วที่นี่ยังเห็นของเก่า |
| `pullSetting` | รายชื่อ รพ.สต. หายไปเฉย ๆ |
| `refreshFy` (2 จุด) | ตัวเลขยอดสะสมค้างของเก่า |

`logFail` = ผู้ใช้เสียประโยชน์จริง · `logWarn` = ล้มได้ตามปกติ
🚨 **แยกให้ชัด** ใช้ `logFail` กับทุกอย่าง คอนโซลจะเต็มไปด้วยเสียงรบกวนจนเรื่องสำคัญจมหาย

### 🚨 catch ที่กลืนเงียบ "ถูกต้อง" มีจริง — เหลือไว้ 3 จุดโดยตั้งใจ

| ที่ | ทำไมเงียบได้ |
|---|---|
| `handlers/demo.js` → `localStorage` | โหมดส่วนตัว · พื้นที่เต็ม เป็นเรื่องปกติ |
| `handlers/record.js` → `setSelectionRange` | ช่องกรอกยังไม่พร้อม |
| `handlers/ui.js` → `navigator.vibrate` | เครื่องไม่มีตัวสั่น |

**เกณฑ์:** ทุกอย่างที่**คุยกับเซิร์ฟเวอร์**ต้องร้อง · ของที่เป็นความสามารถของเครื่องล้วน ๆ เงียบได้

🚨 **ตัวตรวจ `api-log-check` ต้องยกเว้นตัวช่วยกลางทั้งสองตัว** (`apiError` · `clientLog`)
รอบแรกมันฟ้อง `clientLog.js` เอง — **ตัวตรวจที่ฟ้องผิดแย่กว่าไม่มีตัวตรวจ**
เพราะคนแก้จะไปปิดเสียงตัวช่วยกลางทิ้ง ซึ่งตรงข้ามกับเจตนาทั้งข้อ

---

### ③ บัตรผ่านต่ออายุเอง — นับจากครั้งสุดท้ายที่ใช้

เดิมออกบัตรครั้งเดียวแล้วจบ **ใช้ทุกวันก็ยังถูกเด้งออกเมื่อครบ 30 วัน**
เภสัชกรที่กำลังกรอกยาคืนค้างอยู่ต้องหยุดไปตามหารหัส ซึ่งไม่มีใครจำได้

`lib/auth.js` → `authCookieStale()` + `authCookieOpts()` · ต่อใน `middleware.js` ตอนบัตรผ่านด่าน

🚨 **ต่อเมื่อใช้เกินครึ่งอายุ (15 วัน) ไม่ใช่ต่อทุกคำขอ**
ต่อทุกคำขอ = เขียนคุกกี้ใหม่หลายสิบครั้งต่อนาที และตามรอยยากเพราะเวลาที่ออกบัตรขยับตลอด

🚨 **ตัวเลือกคุกกี้ต้องอยู่ที่เดียว** — มีสองที่ที่เขียนคุกกี้ (ตอนล็อกอิน กับ ตอนต่ออายุ)
เขียนแยกแล้วแก้ที่หนึ่งลืมอีกที่ = บัตรที่ออกกับที่ต่อมีคุณสมบัติไม่เหมือนกัน **ไม่มีอะไรจับได้เลย**

🚨 **ทดสอบด้วยบัตรที่ "แก่" จริง ๆ** สร้างบัตรย้อนเวลาแล้วดูว่าระบบตัดสินยังไง
`scripts/auth-renew-check.mjs` — 1 วันไม่ต่อ · 20 วันต่อให้ · 31 วันหมดอายุจริง ·
ลายเซ็นมั่วไม่ผ่าน · บัตรรูปแบบเก่าใช้ไม่ได้

---

### เครื่องมือตรวจใหม่ 2 ตัว

| สคริปต์ | ตรวจอะไร |
|---|---|
| `scripts/client-log-proof.mjs` | เรียกตัวร้องจริงแล้วดูข้อความที่ออก (5 ข้อ) |
| `scripts/auth-renew-check.mjs` | บัตรย้อนเวลา 3 ช่วงอายุ + บัตรปลอม (8 ข้อ) |

`scripts/api-fail-proof.mjs` เพิ่มเคสก้อนของ `supabase-js` → รวม **8 ข้อ**

### ยกเป็นกฎกลางในสกิลแล้ว

พี่กันย้ำว่า **"สกิลกลาง คือกฎ"** — ของที่เคยเขียนไว้เป็นแค่บันทึกของ ME-DRP ถูกยกเป็นกฎ

| ข้อ | ที่เพิ่ม |
|---|---|
| 31 | "ต้องมีครบ 4 อย่าง" → **5 อย่าง** · อย่างที่ 5 = ฝั่งเบราว์เซอร์ก็ต้องร้อง |
| 24 | บัตรผ่านต้องนับอายุจากครั้งสุดท้ายที่ใช้ |

---

## 3.72 🧹 เก็บกวาด 4 เรื่องที่ค้างมานาน (v0.17.1.0 · 2 ก.ย. 2569)

พี่กันสั่ง **"ทำเลย"** กับรายการเก็บกวาดที่แคลร์รายงานว่ายังค้าง

### ① ต-12 · ปุ่มบนจอสัมผัสถึงเกณฑ์ 44px

ค้างมาตั้งแต่ 26 ส.ค. 2569 เพราะข้อ 3.41 เขียนไว้ว่า *"ต้องวัดในเบราว์เซอร์จริงทีละคู่ก่อน
ไม่ใช่เติมทั้ง 79 จุดรวดเดียว"*

| หน้า | ก่อน | หลัง |
|---|---|---|
| สรุป | เล็กเกิน 6 · ทับกัน 1 คู่ | **0 · 0** |
| คลังยา | เล็กเกิน 6 · ทับกัน 1 คู่ | **0 · 0** |
| ประวัติ | เล็กเกิน 4 | **0** |
| บันทึก | เล็กเกิน 11 · ทับกัน 1 คู่ | **ไม่แตะเลย** (ดูด้านล่าง) |

**วิธี — คลาสเดียวในไฟล์มือถือ**

```css
.mrv-mobile .mrv-hit { min-width:44px; min-height:44px; display:flex; align-items:center; justify-content:center }
.mrv-mobile .mrv-hit-input { min-height:44px }
```

🚨 **`min-width`/`min-height` ชนะ `width`/`height` ที่เขียนฝังในแท็ก** จึงไม่ต้องไล่แก้ทีละจุด
🚨 **ช่องกรอกใช้คลาสแยก ห้ามใส่ `display:flex` ให้ `<input>`** เบราว์เซอร์วาดช่องกรอกด้วยกลไกของตัวเอง
🚨 **อยู่ในไฟล์มือถือ เดสก์ท็อปจึงไม่โดน** พิสูจน์ด้วย `desktop-untouched-check` แล้ว

**ที่แก้ 7 จุด** — ปีงบ 2569/2568 · ปุ่มธีมสว่าง/เข้ม · ปุ่ม ℹ ⚙ ในหัวหน้าสรุปกับหน้าประวัติ
· ช่องกรอกวันที่ 4 ช่องในหน้าประวัติ

### 🔴 ปีงบ 2569 กับ 2568 เคยกดพลาดกันได้จริง

สองปุ่มนี้มีคลาส `.tap` (ขยายพื้นที่กดออกด้านละ 11px) แต่วางห่างกันแค่ **6px**
พื้นที่กดจึงทับกัน **เล็งกดปีหนึ่งแล้วโดนอีกปี = ดูตัวเลขผิดปีงบโดยไม่รู้ตัว**

ตรงกับกฎข้อ 3.55 ที่เขียนเตือนไว้เองว่าปุ่มคู่ที่ห่างกันน้อยกว่า 22px ห้ามใช้ `.tap`
**แต่ปุ่มคู่นี้หลุดรอดมา** — ถอด `.tap` ออกแล้วขยายตัวปุ่มเองแทน

### 🚨 หน้าบันทึกห้ามแตะ — ปุ่มที่เล็กคือสิ่งที่พี่กันสั่งเอง

| ปุ่ม | ทำไมเล็ก |
|---|---|
| ชิปแหล่งที่มา 5 อัน (66×30) | พี่กันสั่ง *"บีบตรงนี้โว้ย"* จาก 44 เหลือ 30 (ข้อ 3.68) |
| ช่องค้นยา (สูง 40) | ความสูงผูกกับระบบแก้วรรณยุกต์ แตะแล้วไม้โทกลับมาโดนตัด (ข้อ 3.69) |
| วันที่ · ผู้บันทึก (สูง 38-40) | สามช่องบังคับชุดเดียวที่พี่กันเคาะหน้าตาแล้ว |
| ปุ่มหัวเว็บ (38×38) | มีคอมเมนต์เดิมในไฟล์ว่าลดตามที่พี่กันถาม *"บีบได้อีกมั้ย"* |

### ✅ ชื่อเว็บกับช่องวันที่ที่เหลื่อมกัน — แก้แล้ว (พี่กันสั่ง)

ชื่อเว็บฝั่งมือถือใช้ `margin:0 -7px;padding:5px 7px` — **ระยะขอบขวาติดลบดึงตัวถัดไป
เข้ามาในระยะในของตัวเอง กรอบจึงยื่นไปทับช่องวันที่เท่ากับเลขที่ติดลบเสมอ**

```
ก่อน  ชื่อเว็บ 52-160 · วันที่ 155-280   เหลื่อม 5 จุด
หลัง  ชื่อเว็บ 52-153 · วันที่ 155-280   ห่าง 2 จุด
```

🚨 **ถอดทั้งระยะขอบขวาและระยะในขวาพร้อมกัน** `margin:0 0 0 -7px;padding:5px 0 5px 7px`
ความกว้างของกล่องรวมระยะขอบเท่าเดิมเป๊ะ **ตัวอักษรกับของทุกชิ้นหลังจากนั้นไม่ขยับเลยสักจุด**
(วัดยืนยันแล้ว — วันที่ · ปุ่ม ℹ · ปุ่ม ⚙ อยู่ตำแหน่งเดิมทุกตัว)

สิ่งเดียวที่หายคือแถบไฮไลต์ตอนชี้ที่เคยยื่นไปใต้ปุ่มวันที่ **ซึ่งไม่ควรมีอยู่แต่แรก**

⚠️ **ถอดแค่ระยะขอบอย่างเดียวไม่ได้** จะดันช่องวันที่กับปุ่มไอคอนไปทางขวา 7 จุด
ปุ่ม ⚙ อยู่ที่ 370 บนจอ 390 อยู่แล้ว เหลือขอบขวาแค่ 13 จุด

### ② ต-6 · บังคับเลือกชื่อผู้ลบ

`deleted_by` เคยว่างทุกแถว ตอบผู้ตรวจไม่ได้ว่าใครลบ · ข้อ 3.40 เขียนว่า *"รอพี่กันเคาะ"*

**ป๊อปยืนยันกลางรับ `who` แล้ว** — ป๊อปไหนตั้งไว้จะขึ้นช่องเลือกชื่อ + ปิดปุ่มยืนยันจนกว่าจะเลือก

```js
confirm: { title: 'ยืนยันลบรายการนี้', who: 'ผู้ที่ลบ', run: (by) => app.deleteRecord(r, by) }
```

🚨 **ยกแพตเทิร์นจากหน้าต่างแก้ไขล็อตทั้งดุ้น** (ข้อ 3.26) ไม่ได้ออกแบบใหม่
🚨 **ป้องกันสองชั้น** — ปุ่มปิดไว้ (ตาเห็น) และ `confirmRun` ตีกลับอีกชั้น (เผื่อเรียกจากที่อื่น)
🚨 **ค่าเริ่มต้นมาจาก `st.recorder` ซึ่งว่างเปล่าเสมอตอนเปิดเว็บ** (กฎข้อ 3.24) จึงไม่มีทางเซ็นในชื่อคนก่อน
🚨 **ป๊อปที่ไม่ได้ตั้ง `who` ไม่มีอะไรเปลี่ยน** ช่องไม่โผล่ ปุ่มไม่ถูกกั้น

### ③ ถอดฟอนต์ IBM Plex Sans Thai

ข้อ 3.43 เขียนไว้เองว่า *"ยังโหลดอยู่ทั้งที่ไม่มีใครใช้ ถอดออกได้เลย แต่ยังไม่ได้ถามพี่กัน"*
ตรวจ HTML ที่เสิร์ฟจริงแล้วเหลือ 3 ฟอนต์ ไม่มีร่องรอย IBM Plex
🚨 **บรรทัดแปลงใน `fontify()` ต้องคงไว้เป็นตาข่ายกันตกหล่น**

### ④ กรอบผลค้นยากันหน้าเพจไหลตาม (สกิลข้อ 28)

ใส่ `overscroll-behavior:contain` 3 จุด — ผลค้นยามือถือ · ผลค้นยาคอม · กรอบรายการคอม
ไม่เปลี่ยนหน้าตาสักพิกเซล เปลี่ยนแค่พฤติกรรมการเลื่อน

⚠️ **ระหว่างทางวางคอมเมนต์ JSX ผิดที่จนหน้าเว็บ 500** — เป็นกับดักที่ข้อ 3.68 เตือนไว้เอง
คอมเมนต์วางเป็นลูกตัวแรกของ `{เงื่อนไข && (` ไม่ได้ ต้องวางไว้เหนือบรรทัดเงื่อนไข

### ✅ ปุ่มที่กดไม่ได้ต้องบอกเหตุผลในตัวเอง (พี่กันเลือกแบบ ก)

ป๊อปใส่จำนวนฝั่งมือถือ พิมพ์เกินเพดาน 100,000 แล้วปุ่มเป็นสีเทากดไม่ได้ถูกแล้ว
**แต่บนปุ่มเทายังเขียนยอดเงินจากจำนวนที่เกิน**

```
ก่อน   เพิ่ม +1,350,000,000.00 ฿      ← Metformin เม็ดละ 0.45 × 3,000,000,000
หลัง   เกินจำนวนสูงสุด                ← ไม่มีตัวเลขเลย
```

🚨 **ตัวเลขหลักพันล้านสะดุดตากว่าบรรทัดเตือนสีแดงเสียอีก** คนเห็นแวบแรกตกใจว่าระบบคิดเงินผิด
ทั้งที่เป็นยอดที่ไม่มีวันบันทึกได้จริง

แก้ที่ `components/vals/sheet.js` — `sheetCta` กับ `sheetValueLabel` เช็ค `qty > MAX_QTY` ทั้งคู่
🚨 **ต้องเช็คทั้งสองค่า** ซ่อนแค่ยอดแล้วปุ่มจะเหลือคำว่า "เพิ่ม" ลอย ๆ ไม่บอกว่าทำไมกดไม่ได้
· จำนวนปกติกลับมาโชว์ `เพิ่ม +13.50 ฿` เหมือนเดิมทุกอย่าง (ถ่ายภาพยืนยันทั้งสองสถานะแล้ว)

### เครื่องมือตรวจใหม่ — `scripts/delete-who-check.mjs`

กดปุ่มจริงทีละขั้นเหมือนคนใช้ 12 ข้อ (บทเรียนข้อ 3.66 — เทสแบบอ่านค่าผ่านหมดแต่ปุ่มตาย)

🚨 **ทำในโหมดดูตัวอย่าง + ดัก `DELETE` ไว้อีกชั้น** ข้อมูลจริงไม่ถูกแตะเลย
🚨 **เปิดตัวดักคำขอ "หลัง" เข้าสู่ระบบเท่านั้น** เปิดตั้งแต่แรกแล้วคุกกี้ไม่ติด ล็อกอินไม่ผ่าน
🚨 **ต้องรอ 900ms ให้หน้าเข้าสู่ระบบทำงานเสร็จก่อนพิมพ์** และ**ห้ามตัดสินว่าสำเร็จจาก URL**
   คุกกี้ถูกตั้งจาก `/api/auth` ไม่ใช่จากการเปลี่ยนหน้า
🚨 **ต้องล้างชื่อผู้บันทึกก่อนเทสด่าน** โหมดตัวอย่างเลือกชื่อไว้ให้แล้ว ค่าเริ่มต้นในป๊อปจึงถูกเติม

`scripts/ui-check.mjs` เพิ่มการตั้งชื่อเครื่องทดสอบ (ไม่งั้นหน้าต่างถามชื่อเครื่องบังทุกภาพ)
และเพิ่มสาขา `summary` กับ `record`

---

## 3.71 📋 บันทึกของเซิร์ฟเวอร์ — รู้ว่าเว็บพังเพราะอะไร (2 ก.ย. 2569)

**พี่กันสั่งจากอีกแชท** ให้ทำตามสกิลกลาง `pharmacy-web-logic` **ข้อ 31** ให้ครบทั้ง 4 อย่าง
พร้อมเงื่อนไข *"ตรวจให้เราก่อนว่าเว็บนี้มีข้อมูลคนไข้ในฐานไหม ถ้ามี ห้ามข้ามข้อ 2 เด็ดขาด
และห้ามให้เราเปิดสวิตช์ก่อนที่ข้อ 2 จะเสร็จ"*

### ผลตรวจข้อมูลคนไข้ (ตรวจจากฐานจริง)

| ตาราง | แถว | ช่อง HN |
|---|---|---|
| `mr_return` | 218 | **ว่างทุกแถว** |
| `mr_draft` | 4 | **ว่างทุกแถว** |

ช่องที่ระบุตัวคนได้ — `mr_return.hn` · `mr_draft.hn` · `recorded_by` · `deleted_by` ·
`failed_by` · `price_fixed_by` · `changed_by`

🚨 **ยังไม่มีใครกรอก HN จริงสักแถว แต่ช่องมีอยู่และกรอกได้ทุกเมื่อ**
จึงทำตัวปิดบังให้เสร็จก่อนเปิดสวิตช์ ตามที่พี่กันสั่ง

### ไฟล์ที่เพิ่มเข้ามา

| ไฟล์ | ทำอะไร |
|---|---|
| `lib/maskHn.js` | ปิดบังเลขผู้ป่วยในข้อความ · `maskHn()` · `maskAny()` |
| `lib/apiError.js` | `apiFail()` เขียนบันทึก + ตอบผู้ใช้ · `apiWarn()` เขียนอย่างเดียว |
| `scripts/mask-hn-test.mjs` | ชุดทดสอบตัวปิดบัง 19 เคส |
| `scripts/api-fail-proof.mjs` | พิสูจน์ว่า apiFail **เรียกใช้** ตัวปิดบังจริง ทั้งสองทางออก |
| `scripts/api-log-check.mjs` | ตรวจว่าไม่เหลือ catch ที่ยังไม่ใช้ตัวช่วยกลาง |

`wrangler.jsonc` เพิ่ม `observability.logs.enabled: true` + `invocation_logs: true`

### 🚨 กฎที่ห้ามละเมิด

**① ทุก catch ใน `app/api/` ต้องจบด้วย `apiFail` หรือ `apiWarn`**
ตอนนี้ครบ 28 จุดใน 17 ไฟล์ · เขียนกระจายแล้วจะมีจุดที่ลืม แล้วจุดนั้นเงียบตลอดกาล
`boom()` เดิมที่เคยมีอยู่ไฟล์เดียวถูกลบทิ้งแล้ว

**② ป้ายใน `apiFail` ห้ามซ้ำกันทั้งเว็บ** ใช้รูปแบบ `เส้นทาง.วิธี` เช่น `returns.POST`
เปิดบันทึกมาแล้วต้องรู้ทันทีว่าพังที่ไหน · ตอนนี้มี 27 แบบ ไม่ซ้ำกันสักคู่

**③🔴 ต้องปิดบัง "ทั้งสองทางออก" ไม่ใช่แค่ตอนเขียนบันทึก**

`apiFail` ส่งข้อความออกไปสองทาง — ลงบันทึกฝั่งเซิร์ฟเวอร์ กับ กลับไปหาผู้ใช้
ทางที่สองเคยรั่ว เพราะข้อความที่มีตัวอักษรไทยปน **แม้ตัวเดียว** จะถูกส่งกลับทั้งดุ้น
พร้อมเลขผู้ป่วยที่ฐานใส่มาเอง ไปโผล่บนหน้าจอและในบันทึกของเบราว์เซอร์

```js
const msg = เป็นภาษาไทย(raw) ? maskHn(raw) : fallback;   // ต้องมี maskHn ตรงนี้ด้วย
```

🚨 **`scripts/mask-hn-test.mjs` จับข้อนี้ไม่ได้** เพราะตัวปิดบังทำงานถูกทุกเคส
สิ่งที่พลาดคือ **ลืมเรียก** · จึงต้องมี `scripts/api-fail-proof.mjs` แยกอีกตัว

**④ ห้ามใส่ `\b` ปิดท้าย `hn` ในตัวปิดบัง**
`HN14820` ตัว N ติดกับ 1 ซึ่งเป็นอักขระคำทั้งคู่ จึงไม่มีขอบเขตคำให้จับ แล้วเลขจะหลุด

**⑤ เลขบรรทัดกับเลขรุ่นต้องอ่านได้ ห้ามโดนปิดบังไปด้วย**
ตัวตรวจหน้าหลัง `(?<![0-9a-zA-Z_.\-:/])` กัน `route.js:1430:9` กับ `0.16.3.0` ไว้แล้ว
ปิดบังหมดแล้วเปิดบันทึกมาก็ไล่ต้นเหตุไม่ได้

### วิธีทำให้เกิดข้อผิดพลาดจริงเพื่อดูบันทึก

การตรวจข้อมูลฝั่ง API ดักคำขอที่จงใจส่งผิดไว้ก่อนถึงฐานเสมอ (ซึ่งถูกแล้ว) จึงเข้า `catch` ไม่ได้เลย
ลองมา 7 เส้นทางแล้วโดนตีกลับด้วยข้อความไทยหมด — **ต้องทำให้ฐานพังแทน**

1. ทำเซิร์ฟเวอร์เล็ก ๆ ที่ตอบข้อความผิดพลาดแบบที่ PostgREST ส่งจริง แล้ว `listen` พอร์ตว่าง
2. ปิดเซิร์ฟเวอร์ทดสอบ เปิดใหม่โดยชี้ `SUPABASE_URL` มาที่เซิร์ฟเวอร์ปลอมตัวนั้น
3. ยิงคำขอธรรมดา แล้วเปิดบันทึกดู

ผลจริงที่ได้

```
ฐานส่งมา    duplicate key value violates unique constraint "mr_return_hn_idx" (hn)=(1482055) HN 14820
บันทึกเขียน  [returns.GET] duplicate key value violates unique constraint "mr_return_hn_idx" (HN ***) HN ***
ผู้ใช้เห็น    อ่านประวัติไม่สำเร็จ
```

🚨 **ทำเสร็จต้องคืนค่าเดิมทันที** ทั้งปิดเซิร์ฟเวอร์ปลอมและเปิดเซิร์ฟเวอร์ทดสอบใหม่ด้วยค่าจริง

### คำสั่งตรวจ

```bash
node scripts/mask-hn-test.mjs      # ตัวปิดบัง 19 เคส
node scripts/api-fail-proof.mjs    # apiFail เรียกตัวปิดบังจริงทั้งสองทางออก
node scripts/api-log-check.mjs     # ไม่เหลือ catch ที่ยังไม่ใช้ตัวช่วยกลาง
```

⚠️ **ทำเป็น `.js` ไม่ใช่ `.ts` ตามที่สกิลเขียนไว้** เพราะโปรเจกต์นี้เป็น JavaScript ล้วน
(`jsconfig.json` ไม่มีไฟล์ `.ts` เลยสักไฟล์) ใส่ `.ts` แล้ว build พัง

✅ **พี่กันกดเปิดสวิตช์ในหน้า Cloudflare แล้ว 2 ก.ย. 2569** ครบทั้ง 4 อย่างของสกิลข้อ 31

---

## 3.69 🇹🇭 วรรณยุกต์ไทยโดนตัดในช่องกรอก (v0.16.2.0)

**พี่กันเจอเอง 1 ก.ย. 2569 แล้วสั่งซ้ำสี่รอบ** — "ไม้โทโดนหั่นเหลือครึ่งตัว"
และให้คำว่า **"ก้"** มาเทียบ เพราะไม้โทบนพยัญชนะเปล่าอยู่ต่ำ ไม่โดนตัด
จะได้เห็นว่าไม้โทเต็มตัวหน้าตาเป็นยังไง เทียบกับของ **นี้ นั้น** ที่โดนหั่นครึ่ง

🔑 **กฎเต็มอยู่ในสกิลกลาง `working-with-gun` หัวข้อ 9** — เว็บอื่นเจอแน่นอน
ตรงนี้เก็บเฉพาะเรื่องที่ผูกกับโปรเจกต์นี้

### ต้นเหตุ

ช่องกรอกทุกชนิดตัดตัวอักษรที่ล้นออกนอกตัวเองเสมอ **ปิดไม่ได้**
สั่ง `overflow: visible` ก็ถูกเบราว์เซอร์เปลี่ยนเป็นตัดอัตโนมัติ
(Chromium bug 339052 · W3C bug 17473) · `<textarea>` ก็โดนเหมือนกัน
วัดค่าจริงได้ `auto` ทั้งที่สั่ง `visible`

### วิธีที่ใช้ — ช่องค้นยาทั้งสองฝั่ง

| ชั้น | ทำอะไร |
|---|---|
| กล่องนอก | กรอบที่ตาเห็น สูง 42 (มือถือ) · 46 (คอม) เท่าเดิมเป๊ะ |
| ชั้นวาด (`V.searchDrawRef`) | กล่องธรรมดาวาดตัวอักษร สูงเกินกรอบ 7 จุดบนล่าง หักคืนด้วยตำแหน่งติดลบ |
| ช่องกรอกจริง | ตัวอักษรโปร่งใส เหลือแต่ขีดกะพริบ (`caret-color`) |

🚨 **ฟอนต์ ขนาด ระยะขอบ ต้องตรงกันเป๊ะทั้งสองชั้น** ไม่งั้นขีดกะพริบไม่ตรงตัวอักษร
🚨 **`V.onSearchScroll` / `V.onSearchBlur` ห้ามลบ** — พิมพ์ยาวเกินช่องแล้วต้องเลื่อนตามกัน
🚨 **ท่าเดียวกับช่องจำนวนฝั่งคอมที่โชว์สูตรสองสี** (ข้อ 3.27) แก้ที่หนึ่งควรดูอีกที่ด้วย

### ข้อความธรรมดาแก้ง่ายกว่า

กล่องธรรมดาปล่อยล้นได้ แค่ตั้งระยะบรรทัดไม่ต่ำกว่า **1.5 เท่า**ของขนาดตัวอักษร
รอบนี้ไล่แก้ทั้งเว็บ **476 จุด** ใน 25 ไฟล์ · ตรวจด้วย `scripts/thai-clip-check.mjs`

⚠️ **ตัวตรวจกับภาพแคปหน้าจอเชื่อเป็นคำตอบสุดท้ายไม่ได้** วรรณยุกต์เล็กระดับ 1-2 จุด
เคยฟ้องผิดว่าชิป "เยี่ยมบ้าน" มีปัญหาทั้งที่ปกติ และเคยบอกว่าผ่านทั้งที่พี่กันเห็นว่าตัด
**พี่กันบอกว่ายังโดนตัด = ยังโดนตัด**

---

## 3.68 🪟 ระบบชั้นหน้าต่างซ้อน + ฉากหลังห้ามเลื่อน (v0.16.1.0)

**พี่กันเจอเอง 1 ก.ย. 2569:** กดดูรายการล็อต แล้วกดลบ
> *"เจ้า popup ลบมันดันอยู่ด้านหลัง popup ก่อนหน้า มันควรอยุ่บนสุดสิ"*
> *"เราบอกเเล้วว่าพอมี popup เเล้วฉากหลังห้ามเลื่อน"*

### ตารางชั้นกลาง — `components/helpers.js` → `Z`

ต้นเหตุคือแต่ละหน้าต่างตั้งเลขชั้นเองตามใจ (20 · 25 · 35 · 40 · 45 · 50 · 60 · 80 · 82)
ป๊อปยืนยันอยู่ 40 ส่วนหน้าต่างรายการล็อตอยู่ 60 จึงถูกทับ

| ชั้น | ใช้กับ |
|---|---|
| `bar: 6` | แถบเมนู · แถบบันทึก · หัวเว็บที่ตรึงไว้ |
| `float: 15` | ปุ่มลอย |
| `sheet: 20` | ป๊อปใส่จำนวนฝั่งมือถือ |
| `menu: 29` | เมนูเล็กที่กางจากช่องในหน้า |
| `panel: 32` | หน้าต่างซ้อนทั่วไป — นำเข้าราคา · แก้ล็อต · ป๊อปในหน้าคลังยา |
| `over: 52` | หน้าต่างที่เปิดทับหน้าต่างอื่นได้ — ใบสรุป · ล็อตค้าง · ถามชื่อเครื่อง · ผลบันทึก |
| `pick: 70` | ตัวเลือกที่เปิดจากในหน้าต่างซ้อน เช่น เลือกเหตุผลทำลาย |
| `confirm: 85` | 🚨 ป๊อปยืนยัน — บนสุดของหน้าต่างทั้งหมดเสมอ |
| `toast: 95` | ข้อความเด้ง — เหนือทุกอย่าง |

🚨 **ทุกหน้าต่างต้องใช้เลขจากตารางนี้ ห้ามเขียนเลขเองอีก**
🚨 **ฉากหลังใช้เลขตรง ๆ · ตัวกล่องใช้ +1 · ของที่ซ้อนในกล่องใช้ +2**
🚨 **ยืนยันต้องสูงกว่าหน้าต่างทุกตัว** มันคือด่านสุดท้ายก่อนของหาย

### ฉากหลังห้ามเลื่อนเมื่อมีหน้าต่างซ้อน

ธง `anyModalOpen` มีมาแต่เดิมแต่**นับแค่ 3 ตัวและไม่มีใครเอาไปใช้เลย** (โค้ดตาย)
ตอนนี้นับครบทุกตัวและถูกใช้จริง 2 ที่

| ที่ | ทำอะไร |
|---|---|
| `shell.jsx` | พื้นที่เลื่อนเปลี่ยนเป็น `overflow-y: hidden` |
| `MedReturnApp.jsx` → `_blockBgScroll` | ห้าม `touchmove` / `wheel` ที่ไม่ได้อยู่ในตัวหน้าต่างเอง |

🚨 **ต้องปล่อยให้เลื่อนได้ถ้านิ้วอยู่ในหน้าต่างซ้อนเอง** — ตรวจด้วย `closest('[role="dialog"]')`
ไม่งั้นหน้าต่างที่มีเนื้อหายาว (รายการล็อต · นำเข้าราคา) เลื่อนดูข้างในไม่ได้เลย
🚨 **ปิด overflow ที่พื้นที่เลื่อนอย่างเดียวพอสำหรับคอม แต่มือถือไม่พอ**
เบราว์เซอร์ส่งการเลื่อนต่อไปให้ตัวที่อยู่ข้างนอก ต้องห้ามที่เหตุการณ์ด้วย
🚨 **เพิ่มหน้าต่างใหม่ต้องเติมชื่อใน 2 ที่** — `vals/shell.js` และ `MedReturnApp.jsx` → `_syncModalFlag`

### หน้าตั้งค่าเป็นหน้าเต็มจอแล้ว ไม่ใช่หน้าต่างซ้อน

**พี่กันสั่ง:** *"ตั้งค่า เกี่ยวกับ เอาให้เหมือนรายละเอียดเว้บ คือมีปุ่มกลับ"*

เดิมตั้งค่าเป็นหน้าต่างซ้อนที่เลื่อนขึ้นจากล่าง ส่วนเกี่ยวกับเป็นหน้าเต็มจอ
ทั้งที่ปุ่มสองอันอยู่ข้างกันบนหัวเว็บและเป็นของประเภทเดียวกัน

🚨 **ต้องเป็น "แทนที่" ไม่ใช่ "วาดเพิ่ม"** — `{V.settingsOpen ? renderSettings(V) : (<>หน้าอื่น</>)}`
วาดคู่กับหน้าบันทึกแล้วของสองหน้าอยู่ใน DOM พร้อมกัน ปุ่มซ้ำ ตัวตรวจนับของเจอสองเท่า
🚨 **ต้องเอาออกจาก `anyModalOpen`** ไม่งั้นฉากหลังถูกล็อกทั้งที่ไม่มีอะไรซ้อน
🚨 **`recordNarrow` ต้องเช็ค `!st.settingsOpen`** ไม่งั้นแถบบันทึกลอยทับหน้าตั้งค่า

### เก็บกวาดหน้าจอมือถือรอบเดียวกัน

| สั่งว่า | ทำอะไร |
|---|---|
| "บีบตรงนี้โว้ย" | ชิปแหล่งที่มาหน้าบันทึก 44 → 30px · หน้าตั้งค่า 44 → 27px |
| "วันที่ HN ย้ายงี้หน่อยเป็นเเถวเดียว" | ป้ายย้ายมาข้างช่องในกรอบเดียวกัน เหลือบรรทัดเดียว |
| "ผู้บันทึก เอาตรึงไว้ตรงกดส่ง" | ย้ายจากตัวเลือกเพิ่มเติมมาอยู่เหนือปุ่มส่ง |
| "เอาอันนี้ออก" (แถบสัดส่วน) | เอาแถบ + บรรทัด "ใช้ต่อได้ N%" ออกจากแถบบันทึกมือถือ |
| "เอาอันนี้ออกจากมือถือ" (ท้ายเว็บ) | ท้ายเว็บโผล่เฉพาะฝั่งคอม (เนื้อหาอยู่ในหน้าเกี่ยวกับแล้ว) |
| "เอาระบบตั้งราคาออก" | ปุ่มตั้งราคายาโผล่เฉพาะฝั่งคอม |
| "เอาไว้ตรงกลาง เเละใส่ฟ้อน" | หัวเรื่องหน้าตั้งค่าใช้ฟอนต์ลายมือกึ่งกลางแบบเดียวกับหน้าเกี่ยวกับ |
| "เรียงสวยๆหน่อย เอาสองคอลัม" | ชิปเทคโนโลยีในหน้าเกี่ยวกับเป็นตาราง 2 คอลัมน์ |

⚠️ **`{/* คอมเมนต์ */}` วางเป็นลูกตัวแรกของ `{เงื่อนไข && (` ไม่ได้**
JSX รับ element ได้ชิ้นเดียว คอมเมนต์นับเป็นอีกชิ้น — ต้องวางไว้ก่อนบรรทัดเงื่อนไข
(พลาดมาแล้ว หน้าเว็บขึ้น 500 ทั้งเว็บ)

### เครื่องมือตรวจใหม่ — `scripts/modal-check.mjs`

ตรวจ 16 ข้อ ทั้งมือถือและคอม — ฉากหลังหยุดเลื่อนทุกหน้าต่าง + ป๊อปยืนยันอยู่บนสุดเสมอ
🚨 ตัวตรวจต้องอัปเดตตามเมื่อหน้าต่างเปลี่ยนสถานะเป็นหน้าเต็มจอ ไม่งั้นฟ้องผิด

---
## 3.67 📱 ระบบมือถือแยกไฟล์ + หน้าไม่ไถล + ดึงลงโหลดใหม่ (v0.16.0.0)

**พี่กันสั่ง 1 ก.ย. 2569:** *"เรามาออกแบบหน้าจอมือถือกันใหม่ ให้มันเหมือนใช้ในแอปได้"*
แล้วสั่งย้ำ *"ห้ามเปลี่ยนเดสสักนิดเดียว เเม้เเต่ขยับ 1 px ก็ห้าม สร้างไฟล์เเยกได้ไหม"*

🔑 **กฎเต็มอยู่ในสกิลกลาง `mobile-web-app`** (`.claude/skills/mobile-web-app/SKILL.md`)
ตรงนี้เก็บเฉพาะเรื่องที่ผูกกับโปรเจกต์นี้ · แผนงาน 6 ชุดอยู่ที่ `docs/MOBILE-PLAN.md`

### ของมือถืออยู่ไฟล์ไหน

| ไฟล์ | ทำอะไร |
|---|---|
| `app/mobile.css` | กฎ CSS ของมือถือทั้งหมด — **ทุกบรรทัดขึ้นต้นด้วย `.mrv-mobile`** |
| `MedReturnApp.jsx` → `syncMobileClass` | ใส่/ถอดคลาสที่ตัวหน้าเว็บตามธง wide |
| `MedReturnApp.jsx` → `_pinLeft` | ดีดตำแหน่งเลื่อนแนวนอนกลับ 0 |
| `MedReturnApp.jsx` → `pullRefresh` · `lockWidth` · `_onGesture` | ดึงลงโหลดใหม่ · ล็อกความกว้างตามขอบจอ · ห้ามซูม |
| `handlers/ui.js` → `viewSwap` | เปลี่ยนหน้าแบบไล่จาง |
| `vals/shell.js` → `tabsNarrow` | แท็บฝั่งมือถือ (ไม่มีคลังยา) |
| `pages/nav.jsx` → `navIcon` | ไอคอนแท็บ 3 ตัว |

### 🚨 กฎที่ห้ามฝ่าฝืน

**① ทุกกฎใน `mobile.css` ต้องขึ้นต้นด้วย `.mrv-mobile`** ลืมเมื่อไหร่รั่วไปโดนเดสก์ท็อปทันที

**② ห้ามใช้ `@media (max-width)` ตัดสินว่าเป็นมือถือ** — พี่กันกดปุ่ม "มือถือ" บนคอมได้
ซึ่งตอนนั้นจอยังกว้าง 1366px อยู่ · ตัวตัดสินจริงคือธง `wide` ใน `vals/derive.js`
⚠️ ข้อยกเว้นเดียว: กฎที่ไม่กระทบหน้าตาเลย (overflow) ใช้ `max-width: 1179px` เป็นตาข่ายได้

**③ แตะไฟล์มือถือเมื่อไหร่ ต้องรัน `scripts/desktop-untouched-check.mjs`**
วัด 96 ชิ้นสองทิศ — ถอดคลาสแล้วต้องเหมือนเดิม **และ** ใส่คลาสแล้วต้องเปลี่ยนจริง

### 🏆 หน้าไถลซ้ายขวา — บทเรียนที่แพงที่สุด

พี่กันบ่นเรื่องเดียวกัน **7 รอบ** ส่งคลิปมา 5 คลิป · กฎ CSS ทุกข้อที่ลอง
(`overflow-x` · `touch-action` · `max-width` · `position:fixed` · ล็อกความกว้าง · ห้ามซูม)
**ได้ผลบนคอมแต่ไม่ได้ผลบนมือถือจริงสักข้อ**

ที่แก้ได้จริงคือ **เลิกห้าม แล้วดีดตำแหน่งเลื่อนกลับ 0 ทุกครั้ง** (`_pinLeft`)
ดักทั้ง `scroll` (capture) · `touchmove` · `touchend` — ทุกตัว passive จึงไม่ถ่วงการเลื่อนขึ้นลง

🚨 **เช็ค `.mrv-mobile` ก่อนเสมอ** — ตารางคลังยาฝั่งคอมต้องเลื่อนดูข้าง ๆ ได้จริง

🔑 **วิธีไล่สาเหตุที่ควรทำตั้งแต่รอบแรก:** ดูว่า **อะไรขยับ อะไรไม่ขยับ** ในคลิปเดียวกัน
ของนอกพื้นที่เลื่อนนิ่ง + ข้างในไถล = พื้นที่เลื่อนเลื่อนข้างได้ (ดีดกลับได้)
ทุกอย่างขยับพร้อมกัน = กรอบหน้าเว็บถูกลาก · ตัวอักษรขนาดเปลี่ยน = ซูม

### ห้ามซูม (พี่กันยืนยันหลังแคลร์ทักท้วง)

*"ห้ามซูม เหมือนจุดที่ตรึงไว้ของปุ่มกดส่งยา เพราะแอปทั่วไป มันซูมไม่ได้ นี่นา"*

ล็อก 3 ชั้น — `maximum-scale=1` + `user-scalable=no` · `touch-action: pan-y` · ดัก `gesturestart`
⚠️ แคลร์ทักท้วงเรื่องคนสายตายาวไปแล้ว พี่กันยืนยัน (เว็บใช้ในห้องยา ไม่ใช่เว็บสาธารณะ
และช่องกรอกทุกช่องถูกบังคับเป็น 16px แล้วซึ่งอ่านง่ายอยู่)

🚨 **ช่องกรอกฝั่งมือถือต้องไม่ต่ำกว่า 16px เด็ดขาด** ไม่งั้น iPhone ซูมเองแล้วไม่ซูมกลับ
ตรวจด้วย `scripts/ios-zoom-check.mjs` · ยกเว้นช่องจำนวนตัวโต (`.mrv-qty-big`)

### ดึงลงเพื่อโหลดใหม่

`PULL_FIRE = 62` (ถึงเท่านี้แล้วปล่อย = โหลด) · `PULL_MAX = 92` · หารระยะนิ้วครึ่งหนึ่งให้รู้สึกฝืด
🚨 ทำงานเฉพาะตอนอยู่บนสุดของพื้นที่เลื่อน · ตั้งเวลาขั้นต่ำ 420ms ไม่งั้นตัวหมุนแวบเดียว
🚨 ระหว่างลากต้องเขียน `transition:none` ทับ ไม่งั้นภาพตามนิ้วไม่ทัน

### หัวเว็บมือถือตรึงอยู่บนสุด

พี่กันถามเอง: *"ทำไมกรอบปุ่มกดส่งยาตรึงได้ ทำไมเอาระบบนั้นมาไม่ได้"*
แถบล่างตรึงได้เพราะอยู่ **นอก** พื้นที่เลื่อน · หัวเว็บอยู่ข้างในจึงไถลไปกับเนื้อหา
แก้ด้วย `position:sticky;top:0;z-index:6` ที่กล่องหัวใน `renderRecordNarrow`
🚨 z-index ต้องต่ำกว่าหน้าต่างซ้อนทุกตัว (ต่ำสุดคือป๊อปใส่จำนวนที่ 20)

### เครื่องมือตรวจ 6 ตัว

`desktop-untouched-check` · `drag-proof` · `overflow-check` · `ios-zoom-check` ·
`parked-width-check` · `mobile-shot` — รายละเอียดแต่ละตัวอยู่ในสกิลกลาง

---
## 3.65 🧹 เก็บกวาดหน้าบันทึก + ดูตัวอย่างหน้าจอที่เรียกดูไม่ได้ (v0.15.0.0)

พี่กันไล่ดูหน้าจอจริงทีละจุด 31 ส.ค. 2569 แล้วสั่งแก้รวดเดียว 8 เรื่อง

### 🚨🔴🔴 บทเรียนใหญ่ที่สุดของรอบนี้ — วัดตัวเลขไม่ใช่การเทส

แคลร์เทสด้วยการอ่านค่าจากโค้ด (ความสูงกล่อง ตำแหน่ง สี) แล้วรายงานว่าผ่านหมด
พี่กันเปิดดูเองแล้วเจอว่า **อ่านไม่ออก** ทั้งที่ตัวเลขทุกตัวบอกว่าถูก

| ตัวเลขบอกว่า | ตาเห็นจริง |
|---|---|
| กรอบตาราง 424px | เห็นยาแค่ 3 แถว (แถวสูง 50px กินหมด) |
| ผลลัพธ์อยู่ในกรอบ ไม่ล้น | `15+15+15+ = 105·15+` ผลลัพธ์ทับสูตร |
| span 2 ชิ้นเรียงถูกลำดับ | `90 15+15+15…` ผลลัพธ์ไปโผล่หน้าสูตร |

🚨 **แตะหน้าจอเมื่อไหร่ ต้องเปิดภาพดูก่อนรายงานเสมอ**
ใช้ `computer screenshot` หรือ `zoom` ผ่าน Claude in Chrome
การวัดค่าใช้ยืนยัน**หลัง**เห็นภาพแล้วว่าถูก ไม่ใช่ใช้แทนการดู

### แถบล็อตที่กรอกค้างไว้

| ไฟล์ | ทำอะไร |
|---|---|
| `pages/parked.jsx` | แถบบรรทัดเดียว + `parkedItems()` `parkedTone()` ใช้ร่วมกับหน้าต่างซ้อน |
| `pages/parkedsheet.jsx` | หน้าต่างซ้อน — รายการล็อต กดดูยาทีละล็อต |

🚨 **แถบต้องกินที่คงที่ ห้ามยืดตามจำนวนล็อต**
คอลัมน์ซ้ายของหน้าบันทึกคอมถูกล็อกความสูงเท่าจอ (กฎข้อ 3.2)
ของที่กางออกในนั้นจะดันเนื้อหาทะลุกล่องไปทับท้ายเว็บ — พี่กันเห็นเองแล้วทัก
รายละเอียดจึงต้องไปอยู่ในหน้าต่างซ้อนที่ลอยเหนือหน้าเว็บ ไม่กินที่ในผังหน้าเลย

🚨 **แถบอยู่ใน "กรอบที่เลื่อนได้" ไม่ใช่นอกกรอบ** (พี่กันสั่ง "ไม่ต้องตรึงตัวนี้")
เลื่อนดูยาแล้วแถบเลื่อนหายไปเอง กรอบตารางได้ที่คืนเต็ม

🚨 **สีปุ่มต้องอยู่ในโทนของแถบ** — แถบแดงต้องใช้ปุ่มแดง (`c.btn` / `c.hv`)
เขียวบนพื้นแดงคือสิ่งที่พี่กันทักว่า "สีเข้ากันแล้วเหรอ" · คลาส `.hv-crim` ใน globals.css

### ช่องคิดเลข — สูตรยาวต้องอ่านออก

| ค่าใน vals/record.js | ทำอะไร |
|---|---|
| `qtyShowSum` | โชว์ป้าย "รวม" ตั้งแต่ตอนพิมพ์ ไม่ต้องรอกด Enter |
| `qtyLong` | สูตรเกิน 14 ตัวอักษร = ยาวเกินช่อง ขึ้นปุ่มกางดูเต็ม |
| `qtyExprLeft` / `qtyAnswerTail` | แยกสูตรกับผลลัพธ์เพื่อทาสีคนละสี |
| `onQtyScroll` / `onQtyBlur` | ชั้นที่วาดเลื่อนตาม input · เลิกโฟกัสแล้วเลื่อนไปท้าย |

🚨🔴 **ห้ามวาดผลลัพธ์แบบ position:absolute ตรึงขวา** — มันทับสูตรที่ไหลมาชนพอดี
ต้องเรียงต่อจากสูตรตามปกติ (`flex:none` ทั้งคู่) แล้วเลื่อนดูเอา

🚨 **ห้ามใส่ text-overflow:ellipsis ในชั้นที่วาดสูตร** — ตัดตัวเลขที่พิมพ์มาทิ้ง
แล้วเลื่อนดูของที่ถูกตัดไม่ได้เลย (พี่กันสั่ง "ต้องเลื่อนดูตัวเลขทั้งหมดได้")

🚨 **ข้อความที่วาดต้องตรงกับค่าใน input ทุกตัวอักษร** ไม่งั้นขีดกะพริบเหลื่อม
ตรวจด้วย `spans.map(x=>x.textContent).join("") === app.state.qtyInput`

🚨 **เลิกโฟกัสแล้วต้องเลื่อนไปท้ายเสมอ** ท้ายสุดคือที่ที่คำตอบอยู่
ปล่อยค้างที่ต้นสูตร คนที่หันมามองทีหลังจะไม่เห็นคำตอบเลย

### จอเตี้ยต้องบีบแถว ไม่ใช่รีดช่องไฟ

`vals/derive.js` → `tight = (st.vh || 900) < 700`

จอเตี้ยกว่า 700px บีบแถวยาจาก 11px เหลือ 6px (สูง 50 → 40) เห็นเพิ่มอีก 2 แถว

🚨 **ต้องวัดความสูงจอด้วย ไม่ใช่แค่ความกว้าง** — `st.vh` ใน MedReturnApp
⚠️ **รีดช่องไฟรอบ ๆ ทีละ 5px ไม่ช่วยอะไร** ตัวที่กินที่จริงคือแถวยาที่คูณจำนวนแถว

### ออกจากระบบไม่ได้

ตัวเตือน "จะออกจากหน้านี้ไหม" (ข้อ 3.61) ทำงานตอนกดออกจากระบบด้วย
เบราว์เซอร์เด้งถามซ้อนอีกชั้นหลังยืนยันในเว็บไปแล้ว = กดออกไม่ได้

🚨 `app._leaving` ยกธงก่อนเปลี่ยนหน้า · `_onBeforeUnload` เช็คธงนี้เป็นข้อแรก
คืนธงกลับใน 3 วินาที เผื่อเบราว์เซอร์ไม่เปลี่ยนหน้าให้ ไม่งั้นตัวเตือนตายถาวร

### โหมดดูตัวอย่าง — ปุ่มดูหน้าจอที่เรียกดูไม่ได้

| ตัวสั่งใน handlers/demo.js | เปิดดูอะไร |
|---|---|
| `previewResult("ok"/"fail")` | หน้าผลหลังกดส่ง |
| `previewLoadFail()` / `clearPreviewLoadFail()` | หน้าโหลดไม่สำเร็จครบ 5 หน้า |
| `previewHisImport()` | หน้านำเข้าราคาจาก HIS |

🚨 **ทุกตัวตรวจ `app.state.demo` อีกชั้นในตัวเอง** เผื่อมีใครเรียกจากที่อื่น
ไม่งั้นเภสัชกรกดเล่นตอนใช้งานจริงแล้วเจอจอ "ส่งไม่สำเร็จ" ปลอม ๆ

🚨🔴 **ธงโหมดดูตัวอย่างใน vals ชื่อ `V.demo` (มาจาก vals/shell.js)**
แคลร์เดาเป็น `V.demoOn` ปุ่มเลยไม่ถูกวาดออกมาเลยทั้งชุด — กดแล้วเงียบสนิท

🚨🔴 **ข้อมูล HIS ตัวอย่างต้องมีรูปร่างตรงกับที่ `matchAll()` คืนมาเป๊ะ**
ช่องที่ขาดไม่ได้: `level` (sure/pick/none) · `candidates` · `pickedIndex` · `checked`
ผิดรูปแบบแล้วหน้าจอนับกลุ่มไม่ได้ ขึ้นว่างทั้งสามแท็บ

### โหมดดูตัวอย่างต้องเป็นของปลอมทั้งหมด

🚨🔴 **คลังยากับหน้าจัดการราคาเคยโหลดยาจริง 417 ตัวพร้อมราคามาโชว์**
`loadCatalog` กับ `loadPrices` ไม่รู้จักโหมดนี้เลย (ตัวกัน fetch ดักเฉพาะที่ไม่ใช่ GET)
ตอนนี้ทั้งคู่ `return` ทันทีเมื่ออยู่ในโหมดตัวอย่าง และ `enterDemo` ตั้ง
`demoCatalog()` `demoPrices()` `demoParked()` `demoServerDrafts()` `demoTrash()` ให้ครบ

🚨🔴 **ต้องเช็ค `app.state.demo` อีกครั้ง "หลัง await" ก่อน setState**
คำขอที่ยิงไปก่อนเข้าโหมดตัวอย่าง เดินทางกลับมาทีหลังแล้วทับข้อมูลปลอมจนหายเกลี้ยง
เจอจากภาพถ่ายหน้าจอ ไม่ใช่จากสคริปต์เทส (`loadServerDrafts`)

### เครื่องมือตรวจใหม่

| สคริปต์ | ตรวจอะไร |
|---|---|
| `scripts/demo-check.mjs` | โหมดดูตัวอย่างมีข้อมูลครบทุกหน้า (25 ข้อ) |
| `scripts/parked-check.mjs` | แถบล็อตค้าง + หน้าต่างดูรายละเอียด + วัดว่าไม่มีอะไรทับกัน |
| `scripts/demo-shot.mjs` | ถ่ายภาพหน้าจอจริงลง out/ |

---

## 3.64 ☁️ ร่างที่กรอกค้างเก็บขึ้นเซิร์ฟเวอร์ + ชื่อเครื่อง

**พี่กันเล่าความกลัวที่แท้จริง 31 ส.ค. 2569:**
> สิ่งที่เรากลัวที่สุด คือกรอกไปชั่วโมงนึง แล้วเน็ตหลุด และกรอกไปแล้วคอมรีสตาร์ต
> กรอกไปแล้วไปทำอย่างอื่น พอมากดที่แท็บนั้นมันดันรีเฟรชใหม่

ของเดิมรอดทั้ง 3 อย่างนั้นอยู่แล้ว (ร่างอยู่ในเครื่อง) **แต่ไม่รอด 3 อย่างนี้**
ฮาร์ดดิสก์เสีย · มีคนล้างข้อมูลเบราว์เซอร์ · ย้ายไปทำต่อเครื่องอื่น

### ชั้นต่าง ๆ

| ชั้น | ไฟล์ |
|---|---|
| ฐานข้อมูล | `scripts/017_draft_on_server.sql` — ตาราง `mr_draft` + `mr_draft_put/list/drop` |
| หลังบ้าน | `app/api/drafts/route.js` — GET · PUT · DELETE |
| รายชื่อเครื่อง | `lib/format.js` → `COMPUTERS` (8 เครื่อง) · `MOBILE_PREFIX` |
| ตรรกะ | `handlers/data.js` → `pushDraft` · `loadServerDrafts` · `pickDevice` |
| เอามาทำต่อ | `handlers/record.js` → `takeServerDraft` · `askDropServerDraft` |
| หน้าจอ | `pages/device.jsx` (ถามชื่อเครื่อง) · `pages/parked.jsx` (แถบร่างค้าง) |

### 🚨 กฎที่ห้ามละเมิด

**① ร่างในตาราง `mr_draft` ยังไม่ใช่ข้อมูลจริง**
ไม่เข้าหน้าประวัติ ไม่เข้ายอด KPI ไม่กินเลข Lot จนกว่าจะกดบันทึกแล้วเข้า `mr_return`

**② ยังเก็บในเครื่องเหมือนเดิมทุกอย่าง** ตรงนี้เป็นสำเนาสำรองอีกชั้น
เน็ตหลุดก็กรอกต่อได้ปกติ แล้วค่อยส่งขึ้นตอนเน็ตกลับมา `pushDraft` หน่วง 2 วินาทีเสมอ

**③ แยกรายหน้าต่าง ไม่ใช่แค่รายเครื่อง** (`unique (device_id, tab_id)`)
เครื่องเดียวเปิดหลายหน้าต่างเพื่อกรอกคนไข้คนละคนได้ ถ้าผูกกับเครื่องอย่างเดียว
หน้าต่างจะเขียนทับกันบนเซิร์ฟเวอร์ = ยกปัญหาที่เพิ่งแก้ไปวางไว้บนเซิร์ฟเวอร์แทน

**④🔴 ต้องกรองร่างของหน้าต่างที่ยังเปิดอยู่ออก** (`loadServerDrafts` → `tabAlive`)
เซิร์ฟเวอร์ไม่มีทางรู้ว่าหน้าต่างไหนยังเปิดอยู่ (ทะเบียนอยู่ในเครื่อง) การกรองจึงต้องทำฝั่งเบราว์เซอร์
**เทสจับได้** ตอนเปิดหน้าต่างที่ 4 แล้วเห็นร่างของ 3 หน้าต่างที่กำลังกรอกอยู่

**⑤ เอามาทำต่อแล้วต้องลบต้นทางทันที** ไม่งั้นสองเครื่องมีของชุดเดียวกัน แล้วบันทึกซ้ำ

**⑥ มีของในมืออยู่แล้วห้ามดึงมาทับ** ยาที่กำลังกรอกอยู่สำคัญกว่าของเก่าเสมอ

### ชื่อเครื่อง — เลือก 2 ชั้น

| ชนิด | ระบุด้วย | เพราะ |
|---|---|---|
| คอมพิวเตอร์ | หมายเลขเครื่อง (8 เครื่อง) | เป็นเครื่องกลาง สลับคนนั่งได้ |
| มือถือ · แท็บเล็ต | **ชื่อคน** (จาก `mr_setting.staff`) | เป็นของส่วนตัว พกไปเยี่ยมบ้านได้ |

🚨 **ต้องเลือกจากรายการ ห้ามพิมพ์เอง** พิมพ์ `computer OPD เครื่องที่1` (ไม่มีเว้นวรรค)
= ระบบมองเป็นคนละเครื่อง แล้วหาร่างเก่าไม่เจอ ซึ่งเป็นตอนที่ต้องการมันที่สุด

🚨 **ไม่มีปุ่มข้าม** (พี่กันสั่ง) ปุ่มยืนยันปิดไว้จนกว่าจะเลือกครบทั้งสองชั้น

⚠️ **เรื่องนี้ต่างจากช่องผู้บันทึก** ช่องนั้นยังต้องเลือกใหม่ทุกครั้งที่บันทึกตามกฎข้อ 3.24
อันนี้แค่บอกว่าเครื่องนี้เป็นของใคร ตั้งครั้งเดียวจบ แก้ได้ในหน้าตั้งค่า

### อายุร่าง 7 วัน

พี่กันเคาะ 7 วัน เผื่อวันหยุดยาวติดกัน · นับจาก**ครั้งสุดท้ายที่แตะ** ไม่ใช่วันที่สร้าง

🚨 **ล้างตอนมีคนเรียก `mr_draft_list` ไม่ใช่ตัวตั้งเวลาแยก** เว็บนี้ไม่มีระบบสั่งงานตามเวลา
🚨 **เตือนล่วงหน้า 1 วัน** แถบเปลี่ยนเป็นสีแดงพร้อมข้อความ "จะถูกล้างพรุ่งนี้"
ล้างเงียบ ๆ แล้วไม่มีใครรู้ว่าเคยมีของอยู่

### 🚨🔴 สคริปต์เทสต้องใช้เครื่องชื่อ "เครื่องทดสอบอัตโนมัติ"

ห้ามใช้ชื่อ 8 เครื่องจริง ไม่งั้นร่างขยะจากการเทสจะไปโผล่ในเครื่องที่ห้องยาใช้จริง
และต้องล้างร่างเก่าของเครื่องทดสอบก่อนเริ่มทุกครั้ง (ดู `scripts/tab-check.mjs`)

### เครื่องมือตรวจ

| สคริปต์ | ตรวจอะไร |
|---|---|
| `scripts/device-check.mjs` | ตั้งชื่อเครื่อง 2 ชั้น + ร่างขึ้นเซิร์ฟเวอร์ (11 ข้อ) |
| `scripts/crash-check.mjs` | เน็ตหลุด · คอมรีสตาร์ต · แท็บโหลดใหม่ (7 ข้อ) |
| `scripts/leave-check.mjs` | ถามก่อนออกจากหน้าเมื่อยังไม่บันทึก (4 ข้อ) |
| `scripts/tab-story-check.mjs` | 3 สถานการณ์ที่พี่กันถามเรื่องหลายหน้าต่าง |

---

## 3.63 🪟 ทุกหน้าต่างเป็นเอกเทศ — ร่างแยกรายหน้าต่าง (ส-7)

**พี่กันสั่ง 31 ส.ค. 2569:**
> ที่กรอก 100 เครื่องก็ต้องแยกกัน และกรอกโครมเดียวกัน แต่คนละคนต้องแยกกัน
> ให้ทุกอย่างมันเอกเทศกัน

### ปัญหาเดิม

ร่างที่กรอกค้างเก็บไว้ก้อนเดียวใช้ร่วมทั้งเครื่อง (คีย์ `mrv.session`)
ทุกหน้าต่างเขียนทับกล่องใบเดียวกัน จึงเกิด 2 อย่างนี้เสมอเมื่อเปิดมากกว่าหนึ่งหน้าต่าง

1. ยาที่หน้าต่างหนึ่งเพิ่งเพิ่ม หายเงียบ ๆ เมื่ออีกหน้าต่างแตะอะไรก็ตาม
2. กดบันทึกจากสองหน้าต่าง ยาชุดเดียวกันเข้าฐานสองรอบ **มูลค่าถูกนับซ้ำ**

🔴 **ทางที่เคยคิดไว้แล้วทิ้ง — ให้ทุกหน้าต่างเห็นรายการชุดเดียวกัน**
พี่กันจับได้เองว่าพัง: *"ถ้าเปิด 100 แท็บ ถ้ากรอกมาพร้อมกันมันก็ขึ้นทั้งร้อยตัวสิ"*
เปิด 3 หน้าต่างเพื่อกรอก 3 คนไข้พร้อมกัน ยาจะไหลไปกองรวมเป็นล็อตเดียว **แย่กว่าเดิมอีก**

### วิธีที่ใช้

| ตัวช่วยใน `components/helpers.js` | ทำอะไร |
|---|---|
| `myTabId()` | รหัสประจำหน้าต่าง เก็บใน sessionStorage |
| `draftKeyOf(id)` | คีย์ร่างของหน้าต่างนั้น (`mrv.session.<รหัส>`) |
| `touchTab()` | บอกทะเบียนว่าหน้าต่างนี้ยังเปิดอยู่ (ทุก 15 วินาที) |
| `releaseTab()` | ปล่อยรหัสคืนก่อนหน้าหาย (ผูกกับ `pagehide`) |
| `tabAlive(id)` · `orphanDrafts()` | แยกร่างที่ยังมีเจ้าของ ออกจากร่างที่ไม่มี |

🚨🔴 **การรีเฟรชกับการทำสำเนาแท็บ หน้าตาเหมือนกันเป๊ะจากมุมของโค้ด**
ทั้งคู่คือ "มีรหัสเดิมใน sessionStorage และทะเบียนบอกว่ารหัสนั้นยังมีคนถืออยู่"
สิ่งที่แยกสองอย่างนี้ออกจากกันคือ **หน้าต่างที่กำลังจะรีเฟรชปล่อยรหัสคืนก่อนหายไป**
ส่วนต้นฉบับที่ถูกทำสำเนายังเปิดอยู่ ไม่ได้ปล่อยอะไร

**เคยพลาดมาแล้ว** ตอนแรกไม่มีการปล่อยคืน ทุกการรีเฟรชจึงได้รหัสใหม่
แล้วยาที่กรอกค้างกลายเป็นของหน้าต่างที่ไม่มีตัวตน = **กด F5 ทีของหายทั้งล็อต**

### ของค้างจากหน้าต่างที่ปิดไปแล้ว

| ประเภท | ทำยังไง |
|---|---|
| **ส่งไม่สำเร็จ** | **ดึงกลับมาให้เองทันที** ไม่ต้องรอใครกด |
| ร่างธรรมดา | ขึ้นแถบ `components/pages/parked.jsx` ให้กดเอากลับมา หรือทิ้ง |

🚨 **ของที่ส่งไม่สำเร็จต้องดึงกลับมาเอง** มันคือยาที่รับคืนจากคนไข้ไปแล้วแต่ยังไม่ขึ้นระบบ
ส่วนกลาง ถ้ารอให้คนสังเกตแล้วกด อาจไม่มีใครกดเลยจนของหายทั้งล็อต

🚨 **ร่างธรรมดาห้ามดึงมาเอง** คนที่เปิดหน้าต่างใหม่อาจเป็นคนละคนกับที่กรอกค้างไว้
ของที่โผล่มาเองในหน้าจอคนอื่นคือต้นเหตุของปัญหาเดิมทั้งหมด

🚨 **ห้ามแตะร่างของหน้าต่างที่ยังเปิดอยู่เด็ดขาด** นั่นคือของที่คนอื่นกำลังกรอกอยู่

🚨 **เอากลับมาแล้วต้องลบต้นทางทิ้ง** ไม่งั้นหน้าต่างที่สามที่เปิดทีหลังจะเห็นล็อตเดียวกันอีก

🚨 **มีของในมืออยู่แล้วห้ามดึงมาทับ** — ยาที่กำลังกรอกอยู่สำคัญกว่าของเก่าเสมอ

### เครื่องมือตรวจ

| สคริปต์ | ตรวจอะไร |
|---|---|
| `scripts/tab-check.mjs` | 3 หน้าต่างแยกกัน · รีเฟรช · ปิดแล้วเปิดใหม่ · แถบเอากลับมา (11 ข้อ) |
| `scripts/tab-failed-check.mjs` | ของค้างส่งไม่สำเร็จกลับมาเองข้ามหน้าต่าง (6 ข้อ) |

🚨🔴 **สคริปต์เทสห้ามส่งของขึ้นฐานจริงเด็ดขาด** ทั้งสองตัวดัก `POST /api/returns` ไว้แล้ว
เคยพลาด: ระบบทำงานถูกต้องเกินคาด (ดึงของค้างกลับมาแล้วส่งต่อจนสำเร็จ)
ผลคือมีล็อตขยะเข้าฐานจริง 4 ล็อต ต้องตามลบทีหลัง

⚠️ **เครื่องมือทดสอบปิดหน้าต่างโดยไม่ยิง `pagehide`** ทะเบียนจึงยังคิดว่าหน้าต่างนั้นเปิดอยู่
(เบราว์เซอร์จริงยิงเสมอ) สคริปต์เทสจึงต้องจำลองว่าเวลาผ่านไปเกินเกณฑ์เอง

---

## 3.62 💾 เก็บของไว้ในแท็บ + อัปเดตสดข้ามเครื่อง (กลุ่ม 3 · ก-1 · ก-8 · ก-9 · ก-14)

**พี่กันสั่ง 27 ส.ค. 2569 คำต่อคำ** (เก็บครบใน `docs/AUDIT-STATUS.md`)

> ก1 เอาตามนั้น และเราอยากให้มีระบบว่า ถ้าครั้งนี้โหลดมาแล้ว แล้วรีเฟรชเว็บแบบปกติ
> มันจะไม่โหลดใหม่ คือให้มันเก็บในสตอเรจเว็บเลย และระบบนี้ควรจะทำกับข้อมูลทุกอย่างในเว็บ
> ... แต่ก็ต้องลบทิ้งถ้าปิดแท็บนะ
> ยังไม่พอ ... ถ้าเราเปิดตารางประวัติ แล้วมีอีกคนส่งข้อมูลมา มันจะขึ้นอัปเดตให้เราเลย

### ที่เก็บของแท็บ (sessionStorage)

| ตัวช่วยใน `components/helpers.js` | ทำอะไร |
|---|---|
| `SS` | รายชื่อคีย์ — hist · sum · lots · catalog · prices · rev |
| `readSS/writeSS/clearSS` | อ่าน เขียน ลบ ทีละคีย์ |
| `clearAllSS()` | ล้างทุกคีย์ที่ขึ้นต้นด้วย `mrv.s.` — ไล่ตามคำนำหน้า ไม่ใช่รายชื่อ |
| `app.boxGet/boxSet` | ตัวห่อที่เก็บสองที่พร้อมกัน (หน่วยความจำ + แท็บ) |

🚨 **สามอย่างนี้ต้องอยู่ที่เก็บถาวรต่อไป ห้ามย้ายมา** — ร่างที่ยังไม่บันทึก (ของค้าง
ส่งไม่สำเร็จต้องรอดข้ามการปิดแท็บ ดูข้อ 3.61) · ธีม · ฟอนต์

🚨 **ของในที่เก็บของแท็บไม่มีวันหมดอายุด้วยเวลา** ตัวจับเวลา 60 วินาทีถูกถอดทิ้งแล้ว
ตัวล้างคือลายเซ็นข้อมูลกับการกระทำของผู้ใช้เอง ซึ่งบอกความจริงได้ตรงกว่านาฬิกา

🚨 **โหมดดูตัวอย่างห้ามเขียนลงที่เก็บของแท็บ** (`boxGet/boxSet` ดักไว้แล้ว)
ข้อมูลปลอมที่ค้างข้ามการรีเฟรชคือบั๊กตระกูลเดียวกับผลตรวจข้อ ว-1 ระดับวิกฤต

### ลายเซ็นข้อมูล — `/api/rev`

| ชั้น | ไฟล์ |
|---|---|
| ฐานข้อมูล | `scripts/016_rev_signature.sql` — `mr_return.updated_at` + trigger + `mr_rev()` |
| หลังบ้าน | `app/api/rev/route.js` — คืน 4 ค่า rows · lot · setting · drug |
| ฝั่งจอ | `handlers/data.js` → `pulse()` · `pullDrugs` · `pullSetting` · `refreshCurrent` · `refreshFy` |
| ตัวจับเวลา | `MedReturnApp.jsx` → `_revTimer` ทุก 20 วินาที |

🚨 **ต้องมี `updated_at` + trigger ในฐาน ไม่ใช่แค่นับแถว**
การนับแถวจับได้แค่เพิ่มกับลบ จับ "แก้จำนวน" หรือ "สลับใช้ต่อเป็นทำลาย" ไม่ได้เลย
ซึ่งกระทบมูลค่าโดยตรง · trigger ทำงานในฐานจึงจับได้แม้แก้ตรงในหน้า Supabase

🚨 **แท็บที่ซ่อนอยู่ไม่ถาม** คอมห้องยาเปิดเว็บค้างทั้งวันโดยไม่ได้มอง
ถามตลอดจะได้คำขอเปล่าวันละสองพันกว่าครั้งต่อเครื่อง · กลับมาที่แท็บเมื่อไหร่ถามทันที

🚨🔴 **ต้องแยกให้ออกว่าใครเป็นคนแก้** — `app.invalidate()` ประทับ `_ownAt` ไว้
แล้วยิง `pulse({quiet:true})` ตามหลัง 1.2 วินาที เพื่อจำลายเซ็นชุดใหม่ทันที
ถ้าไม่ทำ กดบันทึกเองแล้วจะเด้งข้อความ "มีข้อมูลใหม่จากเครื่องอื่น" ทุกครั้ง
เผื่อเวลาไว้ 45 วินาที (ตัวจับเวลาถามทุก 20 ถ้าเผื่อเท่ากันจะชนขอบพอดี)

### โหลดไม่สำเร็จ ต้องไม่เหมือน "ไม่มีข้อมูล" (ก-1 ส่วนแรก)

ตัววาดกลาง `components/pages/loadfail.jsx` ใช้ทั้ง 5 หน้า · ธงอยู่ที่ `state.loadErr`

🚨 **หน้าสรุปต้องไม่วาดตัวเลขเลยตอนโหลดไม่สำเร็จ** ไม่ใช่แค่ขึ้นกล่องแจ้งไว้ข้างบน
฿0.00 ที่ค้างอยู่ข้างล่างคือคำตอบที่ผิด และนี่คือหน้าที่เอาไปเสนอผู้บริหาร

### เลิกคำนวณค่าของหน้าที่ไม่ได้เปิด (ก-8)

`vals/index.js` → `need` คำนวณเฉพาะหน้าที่กำลังเปิดอยู่

🚨🔴 **ธง `isPrices · isLots · isCatalog` ต้องเติมใน `vals/index.js` เสมอ**
เดิมธงพวกนี้อยู่ในไฟล์ของหน้าตัวเอง ซึ่งกลายเป็นวงจรอุบาทว์ทันทีที่ข้ามการคำนวณ
(ไม่คำนวณหน้าราคา → ไม่มีธง → กดเข้าหน้าราคาแล้วจอว่างถาวร)

🚨 **หน้าที่ถูกข้ามได้ค่าเป็นว่างเปล่า ห้ามคืนค่าเก่าที่จำไว้**
ธงอย่าง `slipOpen` จะค้างเป็นจริง แล้วใบสรุปจะเด้งขึ้นมาเอง

🚨 **ตรวจก่อนตัดทุกครั้งว่าไม่มีหน้าอื่นใช้ค่าข้ามไฟล์** — ไล่ดูแล้วทั้ง 334 คีย์จาก 6 ไฟล์

**จำผลของงานหนักไว้** `memo1()` ใน helpers ใช้กับการประกอบชื่อยา 417 ตัว
🚨 ใช้เฉพาะจุดที่ของนำเข้าชัดเจนจริง ๆ ขาดไปตัวเดียว = หน้าจอค้างของเก่าโดยไม่มีอะไรเตือน

### คลังยาวาดครบ 417 แถว แต่ทยอยตามที่เลื่อนถึง (ก-9)

`state.catDraw` เริ่ม 60 · เพิ่มทีละ 80 เมื่อแถวเปล่าท้ายตารางเข้าใกล้สายตา
(`app.catMoreRef` + IntersectionObserver เผื่อระยะ 600px)

🚨 **ตัวสังเกตต้องเป็นเมธอดตัวเดิมตลอด** ห้ามสร้างใหม่ทุกครั้งที่วาดจอ
ไม่งั้น React ถอดแล้วต่อใหม่ทุกเฟรม (บทเรียนเดียวกับ `--bottombar`)

🚨 **เปลี่ยนเงื่อนไขค้น กรอง เรียง ต้องรีเซ็ตกลับเป็น 60 เสมอ**

⚠️ **สองวิธีที่ลองแล้วไม่ช่วย ห้ามเสียเวลาซ้ำ** — วัดจริงทั้งคู่
`content-visibility:auto` (ยังเหลือกฎ `.cat-row` ไว้ ช่วยตอนเลื่อนผ่านแถวที่วาดแล้ว)
และ `table-layout:fixed` (ถอยออกแล้ว) — คอขวดจริงคือ React สร้าง element 5,838 ตัวใหม่

### ตาข่ายกันโค้ดแปลกปลอม (ก-14)

CSP ย้ายจาก `next.config.js` ไปออกที่ `middleware.js` รายคำขอ
เว็บจริงได้ `script-src 'self' 'nonce-xxx' 'strict-dynamic'` ไม่มี unsafe-inline แล้ว

🚨🔴 **หน้าเว็บต้องเป็น dynamic ทุกหน้า** (`export const dynamic = 'force-dynamic'`)
หน้าที่ Next สร้างล่วงหน้าตอน build จะได้ HTML ก้อนเดิมที่ไม่มีใบอนุญาตติดมา
ผลคือเบราว์เซอร์บล็อกสคริปต์ทั้งหมด **ได้จอขาวทั้งใบ** — เจอตอนเทสเว็บจริงแล้วจริง ๆ
· `app/page.js` ใส่ตรง ๆ · `app/login/` ใส่ที่ `layout.js` (หน้านั้นเป็น client component)

🚨 **style-src ยังต้องมี `'unsafe-inline'` ต่อไป** (กฎเหล็กข้อ 8 ของโปรเจกต์)

🚨🔴 **แตะเรื่อง CSP เมื่อไหร่ ต้องเทส production build เสมอ**
ตอนรันในเครื่องมี `'unsafe-eval'` อยู่ จึงไม่มีทางเจอปัญหานี้

### เครื่องมือตรวจใหม่ 5 ตัว

| สคริปต์ | ตรวจอะไร |
|---|---|
| `scripts/cache-check.mjs` | เก็บของในแท็บ + อัปเดตสดข้ามเครื่อง (20 ข้อ) |
| `scripts/fail-check.mjs` | โหลดไม่สำเร็จแยกจากไม่มีข้อมูล (14 ข้อ) |
| `scripts/nav-check.mjs` | ทุกหน้าและหน้าต่างซ้อนยังใช้ได้ (10 ข้อ) |
| `scripts/cat-scroll-check.mjs` | คลังยาเลื่อนแล้วครบ 417 แถว (5 ข้อ) |
| `scripts/perf-check.mjs` | วัดเวลาแยกตามไฟล์ · เติม catalog ต่อท้ายเพื่อวัดหน้าคลังยา |

🚨 **วัดก่อนแก้เสมอ** ผลตรวจบอกว่าคลังยาหนักที่สุด วัดแล้วจริง (1.81 ms ต่อการวาดจอ)
แต่ผลตรวจไม่ได้บอกว่า 95% ของเวลาอยู่ที่การวาด DOM ไม่ใช่การคำนวณ ซึ่งเห็นได้จากการวัดเท่านั้น

---
## 3.58 🔤 ฟอนต์ตัวอักษรอังกฤษและตัวเลข — Roboto Mono

**พี่กันสั่ง 27 ส.ค. 2569:** *"เราอยากได้ฟอนต์ roboto เหมือนใน hcv ทำให้เราหน่อย
เอาใส่ให้เลือกในหน้าตั้งค่า และเปลี่ยนพวกตัวเลข และภาษาอังกฤษให้หมด เป็นฟอนต์นี้"*

### กลไก — ยกมาจากเว็บ HCV ทั้งชุด

```
font-family: var(--font-en), var(--font-sarabun), Sarabun, sans-serif
                    ↑ Roboto Mono          ↑ ตัวไทยตกมาใช้ตัวนี้เอง
```

เบราว์เซอร์ไล่ตามลำดับ — ตัวไหนมีในฟอนต์แรกใช้ตัวนั้น ไม่มีก็ตกมาตัวถัดไป
Roboto Mono ไม่มีตัวอักษรไทย ภาษาไทยจึงได้ Sarabun เหมือนเดิมทุกตัวโดยไม่ต้องเขียนเงื่อนไข

| ไฟล์ | ทำอะไร |
|---|---|
| `app/layout.js` | โหลด Roboto Mono ฝังมากับเว็บ → ตัวแปร `--font-mono` |
| `app/globals.css` | `:root { --font-en: var(--font-mono) }` ค่าเริ่มต้น |
| `components/helpers.js` → `fontify()` | แทรก `var(--font-en)` ไว้หน้า Sarabun ทุกจุด (~600 จุด) |
| `handlers/ui.js` → `applyEnFont` · `setEnFont` | สลับ + เก็บลง `mrv.enfont` |
| `pages/settings.jsx` | ปุ่มเลือก 2 แบบ พร้อมตัวอย่าง |

### 🚨 กฎ 4 ข้อ

1. 🚨 **ใบพิมพ์เอกสารราชการห้ามเปลี่ยนตาม** (พี่กันสั่งย้ำ 27 ส.ค. 2569)
   ปลอดภัยอยู่แล้วเพราะ `.slip-paper` เขียน `"TH Sarabun New"` ตรง ๆ ใน `@media print`
   ไม่ผ่าน `fontify()` · **ตรวจทุกครั้งที่แตะเรื่องฟอนต์** ด้วยการสร้าง PDF จริงแล้วคลายดู
   ```bash
   node scripts/print-check.mjs && node scripts/pdf-font.mjs "out/ใบสรุป-ล็อตล่าสุด.pdf"
   ```
   ต้องเจอแค่ `THSarabunNew` กับ `THSarabunNew-Bold` ห้ามมี Roboto Mono

2. 🚨 **โหมด "แบบปกติ" ต้องชี้ `--font-en` กลับไปที่ Sarabun ห้ามตั้งเป็นค่าว่าง**
   ค่าว่างทำให้ประโยค `font-family` พังทั้งบรรทัด ตัวอักษรกลายเป็นฟอนต์ระบบ

3. 🚨🔴 **จุดที่เขียน `var(--font-sarabun)` ตรง ๆ ไม่ได้ผลจาก `fontify()`**
   ตัววาดชื่อยา (`pages/drugname.jsx`) เขียนแบบนี้ ผลคือ Sarabun มาก่อนแล้วชนะ
   **ชื่อยาทั้งเว็บจึงไม่เปลี่ยนฟอนต์เลย** ทั้งที่ตัวเลขรอบข้างเปลี่ยนหมดแล้ว
   พี่กันจับได้เอง — แคลร์เปิดดูแล้วแต่ดูไม่ออกเพราะไม่ได้เทียบตัวอักษร
   **แก้: ใส่ `var(--font-en)` ไว้หน้าสุดทุกจุดที่เขียนฟอนต์เอง**

4. 🚨 **ตัวอย่างในปุ่มเลือกต้องเขียนฟอนต์ตายตัว ไม่ใช้ `--font-en`**
   ไม่งั้นทั้งสองปุ่มจะเปลี่ยนตามที่เลือกอยู่ = เทียบกันไม่ได้

### ⚠️ ผลข้างเคียงที่ต้องรู้

**ฟอนต์นี้ตัวอักษรกว้างกว่า Sarabun ราว 15%** ทำให้ของที่เคยพอดีล้นได้

| ที่ | อาการ | แก้ยังไง |
|---|---|---|
| คอลัมน์วันที่ หน้ารายการ Lot | ตกบรรทัดเป็นสองแถว | 92px → 116px |
| ตัวเลขใหญ่หน้าสรุป | ล้นออกนอกกรอบ | `clamp(44,9.4vw,132)` → `clamp(38,7.8vw,108)` |
| ชื่อผู้บันทึกหน้าประวัติ | ถูกตัดท้ายด้วยจุดไข่ปลา | เลิกใช้ ellipsis ให้ขึ้นบรรทัดใหม่แทน |

**จุด `.` ในคำไทยกว้างขึ้น** (`รพ.สต.` ดูเหมือน `รพ . สต .`) เพราะจุดมีในฟอนต์ทั้งสอง
เบราว์เซอร์เลยหยิบของ Roboto Mono มาใช้ · พี่กันดูแล้วบอกว่าไม่ซีเรียส

### 🔴 บทเรียน — เปิดดูอย่างเดียวไม่พอ ต้องเทียบ

แคลร์เปิดดูทุกหน้าแล้วรายงานว่าเรียบร้อย แต่**ชื่อยายังเป็นฟอนต์เดิมทั้งเว็บ**
พี่กันเห็นเองแล้วทัก — เพราะแคลร์ดูว่า "หน้าเว็บพัง/ไม่พัง" ไม่ได้ดูว่า
"ของที่สั่งให้เปลี่ยน เปลี่ยนจริงหรือยัง"

**ก่อนบอกว่าเสร็จ ต้องไล่ดูของที่สั่งทีละอย่างว่าเปลี่ยนจริง ไม่ใช่ดูว่าหน้าเว็บยังใช้ได้**

---

## 3.59 📌 หน้ารายการ Lot — ตรึงหัว 2 ชั้น

ทำเหมือนหน้าประวัติทุกอย่าง (พี่กันสั่ง 27 ส.ค. 2569)

```
.lots-head   position:sticky · top:0                  ← แถบหัวเรื่อง + ค้นหา + ตัวกรอง
.col-head    position:sticky · top:var(--lotshead)    ← หัวตาราง ติดใต้แถบบน
```

`--lotshead` เขียนจาก **ResizeObserver** ใน `MedReturnApp.jsx` (`lotsHeadRef`)
🚨 **ตั้งเลขตายตัวไม่ได้** เพราะปุ่มช่วงเวลากับช่องวันที่ขึ้นบรรทัดใหม่เองเมื่อจอแคบ
🚨 **กรอบตาราง `.col-tab` ห้ามใส่ `overflow` เด็ดขาด** — sticky ตายทันที

### ช่องไฟในตาราง

พี่กันสั่ง: *"เราให้เธอใส่เส้นแบ่งคอลัมน์ทำไม เพราะจะได้เห็นว่าเส้นกับตัวอักษรที่ชิดซ้าย
มันโคตรชิดเลย ดังนั้นเราควรจะมีระยะห่างพอประมาณที่มันมีช่องไฟ"*

```css
.col-head > span, .col-row > span { box-sizing:border-box; padding-left:12px; padding-right:12px }
.col-head > span { padding-left:12px !important; padding-right:12px !important }  /* ← ต้องมี */
```

🚨🔴 **หัวคอลัมน์ต้องเขียนซ้ำด้วย `!important`** — หัวเขียน `padding` ฝังในแท็ก ซึ่งชนะกฎ CSS
แคลร์เคยใส่กฎให้ `.col-head > span` แล้วนึกว่าได้ผล แต่**วัดจริงได้ 0px** ส่วนแถวได้ 12px
พี่กันทัก: *"เธอใส่ช่องไป 12 แล้ว ดี แต่ลืมอะไรไหม แล้วหัวคอลัมน์ไม่ใส่ให้เราหน่อยเหรอ
บ่งบอกเลยว่าเธอไม่ได้เปิดเทสดู"*

**บทเรียน: กฎ CSS ที่เขียนแล้วต้องวัดค่าจริงในเบราว์เซอร์ว่าใช้ผลได้จริง**
ดูภาพผ่าน ๆ แล้วเห็นว่า "ดูมีช่องไฟ" ไม่ใช่การตรวจ

---

## 3.56 🖱 สีตอนเอาเมาส์ชี้ — กฎ 6 ข้อ และเครื่องมือตรวจ 2 ตัว

**พี่กันตั้งเป็นกฎการออกแบบ 26 ส.ค. 2569:** *"ทุกปุ่มควรเปลี่ยนสีตอนเอาเมาส์ไปชี้
ตั้งเป็นกฎการออกแบบ เราขี้เกียจสั่งเธอให้ทำอันนี้แล้ว"*

### กฎ 6 ข้อ

1. 🚨 **ห้ามใช้กฎกลางครอบทุกปุ่ม** (เช่น `filter: brightness()`) — พี่กันสั่งตรง ๆ ว่า
   *"อันนี้งานละเอียด ห้ามสร้างโค้ดแล้วให้ทุกปุ่มเปลี่ยน ต้องแก้มือทุกปุ่มเท่านั้น"*
   เพราะสีที่ถูกต้องขึ้นกับพื้นของปุ่มนั้น กฎกลางทำให้บางปุ่มดูจางลงแทนที่จะเด่นขึ้น
2. 🚨 **สีตอนชี้ต้องอยู่ในโทนของปุ่มนั้น** — ปุ่มแดงชี้แล้วต้องแดงเข้มขึ้น ไม่ใช่เขียว
   (พี่กันทัก 27 ส.ค. 2569: *"กรอบมันสีแดงนะ จุ้มไปต้องสีแดงสิ"*)
3. 🚨 **ห้ามตั้งสีตอนชี้เป็นสีเดียวกับพื้นปุ่มเอง** — ชี้แล้วไม่เปลี่ยนอะไรเลย
4. 🚨 **ปุ่มที่พื้นเข้มอยู่แล้วไม่ต้องเข้มเพิ่มมาก** และปุ่มที่ทำงานถูกอยู่แล้วห้ามไปแก้
5. 🚨 **ปุ่มที่เปลี่ยนสีตามสถานะ ต้องสลับคลาสตอนชี้ด้วย** (ดูข้อ 3.31)
6. 🚨 **รายงานเรื่องสีให้พี่กันต้องเป็นภาพหรือชื่อสีภาษาคน ห้ามเป็นรหัสสี**
   (*"เรามองเห็นสีเหรอ ตั้งกฎเลย"*)

### เครื่องมือตรวจ 2 ตัว — ตัวเดียวไม่พอ

```bash
node scripts/hover-check.mjs   # ทุกปุ่มเปลี่ยนสีตอนชี้จริงไหม
node scripts/hue-check.mjs     # สีที่เปลี่ยนอยู่ในโทนของปุ่มนั้นไหม
```

🚨 **`hover-check.mjs` ต้องอ่านค่าสีที่เบราว์เซอร์คำนวณ ห้ามเทียบภาพ**
ของเดิมถ่ายภาพปุ่มก่อน/หลังชี้มาเทียบไบต์ต่อไบต์ โดยรอแค่ 0.045 กับ 0.16 วินาที
ทั้งที่การไล่สีใช้เวลา 0.12–0.15 วินาที **ภาพจึงถ่ายติดตอนสียังไหลไม่ถึงที่**
ผลคือรายงานไม่ตรงกันทุกรอบที่รัน — รอบหนึ่งฟ้องปุ่มกลับ อีกรอบฟ้องชิปแหล่งที่มา
พอตรวจทีละปุ่มด้วยมือแล้วทุกตัวเปลี่ยนสีจริงหมด **ตัวตรวจที่รายงานผิดแย่กว่าไม่มีตัวตรวจ**

🚨 **`hue-check.mjs` มีจุดบอดที่ตัดสินแทนคนไม่ได้ ต้องอ่านเอง**
ปุ่มที่ตอนปกติเป็นขาวหรือเทาไม่มีโทนให้เทียบ มันจึงแยกออกมาเป็นรายการ
"ต้องอ่านเอง" แทนที่จะเดา — ต้องตัดสินจาก**ความหมายของปุ่ม** (ทำลาย=แดง · บันทึก=เขียว · รอ=อำพัน)

**บทเรียน 27 ส.ค. 2569** — ปุ่ม "ทำลาย" ในช่องกรอกหน้าบันทึกใช้คลาส `hv-txt` (เขียว)
ตอนที่ยังไม่ได้เลือก ทั้งที่ปุ่มทำลายต้องเป็นแดง `hover-check` ไม่ฟ้องเพราะสีเปลี่ยนจริง
`hue-check` ก็ไม่ฟ้องเพราะพื้นปุ่มตอนปกติใสไม่มีโทน **พี่กันเห็นเองแล้วทัก**
ตอนนี้ใช้ `hv-del` (พื้นแดงจาง ตัวหนังสือแดงอิฐ) แล้ว

### ปุ่มพิเศษ 3 กลุ่ม

| คลาส | ใช้กับ | ตอนชี้ |
|---|---|---|
| `.hv-wait` | ปุ่มบันทึกตอนยังไม่เลือกยา | ครีมอมเหลืองเข้มขึ้น + สั่นซ้ายขวา |
| `.hv-sun` | ปุ่มธีม "สว่าง" | แสงอุ่นเปล่งจากกลางปุ่ม |
| `.hv-moon` | ปุ่มธีม "เข้ม" | ผืนมืดคืบจากขวา + ดาว 5 ดวง |

🚨 **ปุ่มธีมห้ามเปลี่ยนสี** พี่กันสั่งไว้ตรง ๆ — แอนิเมชันจึงทำเป็นชั้นซ้อนทับ (`::before`/`::after`)
และ**ต้องจางหายเมื่อจบ ห้ามค้าง** เพราะชั้นที่ค้างอยู่ก็คือการเปลี่ยนสีถาวรนั่นเอง

🚨 **แอนิเมชันทุกตัวต้องมี `@media (prefers-reduced-motion: reduce)`**
เจ้าหน้าที่บางคนเวียนหัวกับภาพเคลื่อนไหว (กฎเดียวกับฉากหลังหน้าเข้าสู่ระบบ ข้อ 3.51)

---

## 3.57 📏 เส้นแบ่งคอลัมน์ในตาราง — เต็มความสูงแบบ Excel

**พี่กันสั่ง 27 ส.ค. 2569:** *"ทำแบบตาราง excel นี่มันยากนักเหรอ"*

```css
.col-head > span, .col-row > span {
  align-self: stretch !important;   /* 🚨 ต้องมี !important */
  display: flex; align-items: center;
}
.col-head > span:not(:last-child),
.col-row > span:not(:last-child) { border-right: 1px solid rgba(30,36,32,.06); }
.col-row { padding-top: 0 !important; padding-bottom: 0 !important; }
.col-row > span { padding-top: 9px; padding-bottom: 9px; }
```

🚨 **`!important` จำเป็น** — แถวเขียน `align-items:center` ฝังในแท็ก ซึ่งชนะกฎในไฟล์ CSS เสมอ
ช่องจึงสูงแค่ 34px ในแถวที่สูง 53px เส้นแบ่งเลยขาดเป็นช่วง ๆ ลอยกลางแถว

🚨🔴 **เปลี่ยน `display` เป็น `flex` ทำให้ `text-align` ไม่มีผลทันที**
ตัวเลขที่เคยจัดกึ่งกลางกับชิดขวาไหลไปชิดซ้ายหมด **โดยไม่มี error อะไรเลย**
ต้องเพิ่ม `justify-content` คู่กับ `text-align` เดิมทุกช่องที่ไม่ได้ชิดซ้าย
(พี่กันทัก: *"ไปเปลี่ยนการตั้งค่าข้อมูลเราทำไม อันไหนชิดซ้าย อันไหนกึ่งกลาง"*)

🚨 **เส้นแบ่งคอลัมน์ = `border-right` ของแต่ละช่อง ไม่ใช่ `outline` ล้อมสี่ด้าน**
ใส่ `outline` แล้วตารางเต็มไปด้วยกรอบสี่เหลี่ยมจนดูเละ

### ไฮไลต์แถวตอนชี้

```css
.col-row:hover { background: #eef6f1 !important; }
.col-row:hover .hv-cream { background: #fbeed4 !important; }
.col-row:hover .hv-bg-f6 { background: #e8f2ec !important; border-color: rgba(47,125,93,.30) !important; }
.col-row:hover .hv-teal  { background: #24614a !important; }
```

🚨 **ปุ่มในแถวต้องเปลี่ยนสีตามแถวด้วย** ไม่งั้นแถวเปลี่ยนสีแต่ปุ่มยังขาว ดูเหมือนปุ่มลอยไม่เข้าพวก
แต่ละปุ่มเข้มขึ้นในโทนของตัวเอง ไม่ใช่โทนเดียวกันหมด

⚠️ **หน้าประวัติยังไม่มีทั้งเส้นแบ่งคอลัมน์และไฮไลต์แถว** เป็นตารางเหมือนกันแต่ยังไม่ได้ทำ รอพี่กันเคาะ

---

## 3.8 `kb()` — ปุ่มทุกอันต้องกดด้วยคีย์บอร์ดได้

มอคอัปทำปุ่มทุกอันเป็น `<div onClick>` ทั้งเว็บมี **107 จุด** ซึ่ง Tab ไม่ถึงและ Enter ไม่ทำงาน
หน้าบันทึกเดิมกด Tab ได้แค่ 4 ชิ้น ทั้งที่กดด้วยเมาส์ได้ 15 ชิ้น

```jsx
// ❌ เดิม                          // ✅ ตอนนี้
<div onClick={V.save} style={…}>   <div {...kb(V.save)} style={…}>
```

`kb()` อยู่ใน `components/helpers.js` คืน `{onClick, role:'button', tabIndex:0, onKeyDown}` ทีเดียวครบ

🚨 **ปุ่มใหม่ทุกปุ่มต้องใช้ `kb()` ห้ามเขียน `onClick` ตรง ๆ อีก**
(ตอนนี้ในโฟลเดอร์ `pages/` ไม่เหลือ `onClick=` แล้วแม้แต่จุดเดียว — ยกเว้นชื่อพารามิเตอร์ของ `renderExportBtn`)

Space ต้อง `preventDefault` ไม่งั้นหน้าเลื่อนลงตอนกด · ผลลัพธ์: หน้าบันทึกแบบคอม Tab ถึง **29 จุด**

---

## 3.9 หน้ารายการ Lot + ใบสรุปพิมพ์

ไฟล์: `handlers/lots.js` · `vals/lots.js` · `pages/lots.jsx` · CSS พิมพ์อยู่ท้าย `app/globals.css`

**ทำไมต้องมี** — หน้าประวัติเป็นรายแถวยา มองไม่ออกว่า "รอบบ่ายวันนี้รับคืนไปเท่าไหร่" ต้องไล่บวกเอง

| ส่วน | หมายเหตุ |
|---|---|
| เข้าจาก | หน้าประวัติ ปุ่ม **"รายการ Lot"** (มีทั้งคอมและมือถือ) |
| ข้อมูล | `/api/lots` → `mr_lots()` คืน `lot · date · by · items · qty · saved · lost` |
| ช่วงเวลา | 4 ปุ่ม แยกจากหน้าประวัติ (`st.lotsRange`) |
| แสดงทีละ | 40 Lot + ปุ่มดูเพิ่ม — ปีงบหนึ่งมีหลายร้อย วาดหมดทีเดียวจอค้าง |
| ใบสรุป | กด "ใบสรุป" → ดึงแถวใน Lot นั้นก่อน (`/api/returns?lot=…`) แล้วค่อยวาด |

🚨 **ยอดบนใบสรุปคิดจากแถวจริงที่ดึงมา ไม่ใช้ยอดจากหน้ารายการ**
ถ้ามีคนลบแถวไปหลังเปิดหน้ารายการ ตัวเลขบนใบต้องตรงกับรายการที่พิมพ์ออกมา

🚨 **ห้ามรวมจำนวนข้ามหน่วยนับในหน้ารายการ Lot** — ฐานส่ง `qty` รวมมาก็จริงแต่ไม่เอามาโชว์ (ดูข้อ 3.4)

🚨 **`printLotSlip()` ต้องรอ 2 เฟรมก่อนสั่ง `window.print()`**
กดพิมพ์ทันทีหลังข้อมูลมาถึง React ยังไม่ทันวาด = ได้กระดาษเปล่า

**CSS พิมพ์** ใช้ `body > *{display:none}` แล้วเปิดเฉพาะสายที่พาไปถึง `.slip-sheet` ด้วย `:has()`
· `.slip-hide` = แถบปุ่มที่ไม่ต้องติดไปในกระดาษ
· `print-color-adjust:exact` บังคับพิมพ์สี ไม่งั้นแยกไม่ออกว่าอันไหนใช้ต่อ อันไหนทำลาย

---

## 3.10 ส่วนหัวความปลอดภัย (security headers)

อยู่ใน `next.config.js` → `headers()` · ยกชุดเดียวกับ TB Dashboard มาปรับ

🚨 **`'unsafe-inline'` ใน `style-src` ตัดออกไม่ได้** — ทั้งเว็บวาดด้วยสไตล์ฝังในแท็ก (`s()`) ราว 600 จุด
ตัดออกหน้าเว็บจะกลายเป็นข้อความเปล่าไม่มีสีไม่มีเลย์เอาต์

`'unsafe-eval'` กับ `ws:` ใส่**เฉพาะตอนรันในเครื่อง** (`NODE_ENV === 'development'`)
เครื่องมือรีเฟรชอัตโนมัติของ Next ต้องใช้ · เว็บจริงไม่มีทั้งคู่

---

## 3.11 `--bottombar` — ข้อความเด้งไม่ทับแถบล่าง

มอคอัปตรึงข้อความเด้งไว้ที่ `bottom:96px` ตายตัว แต่หน้าบันทึกแบบมือถือมี**แถบบันทึกซ้อนอยู่เหนือแถบเมนู**
รวมกันสูงกว่า 200px ข้อความ "บันทึกสำเร็จ" จึงไปทับตัวเลขมูลค่ารวมพอดี

วัดด้วย ResizeObserver ใน `MedReturnApp.jsx` — **สองกล่องแยกกัน** (`navBarRef` + `saveBarRef`)
แล้วบวกเขียนลง `--bottombar` · แถบบันทึกโผล่เฉพาะบางหน้า กล่องที่หายไปนับความสูงเป็น 0

ref ถูกสร้างไว้ตายตัวใน constructor — สร้างใหม่ทุกครั้งที่วาดจอ React จะถอดแล้วต่อตัววัดใหม่ทุกเฟรม

---

## 3.12 โหมดดูตัวอย่างต้องมีข้อมูลครบเท่ายาจริง

🚨 **`DEMO_DRUGS` ใน `lib/demo.js` ต้องมีช่อง `brand` · `form` · `route` ครบ**
ไม่งั้นพี่กันเปิดโหมดตัวอย่างแล้ว "ไม่เห็นสิ่งที่เพิ่งแก้" ทั้งที่โค้ดถูกแล้ว (เจอจริง 10 ส.ค. 2569)

และ `handlers/demo.js` → `enterDemo()` ต้องส่งช่องเหล่านั้นต่อเข้า `st.drugs` ด้วย

🚨 **ชื่อยาตัวอย่างต้องเขียนตามรูปแบบที่ระบบจริงประกอบขึ้นมา**
`ชื่อสามัญ + ความแรง + หน่วย + เปอร์เซ็นต์ท้ายสุด` · ยาผสมความแรงอยู่ในวงเล็บ
เขียนผิดตำแหน่ง ตัวแยกชื่อ/ความแรง/% จะแยกไม่ออก แล้วสีไม่ขึ้น

**กฎทั่วไป: แก้อะไรที่เห็นได้บนหน้าจอ ให้เช็คด้วยว่าโหมดตัวอย่างมีข้อมูลพอให้เห็นของนั้นไหม**

---

## 3.6 🚨 บันทึกหลายแถวพร้อมกัน — ต้องแยกกลุ่มตามชุดช่อง

**บั๊กที่เคยเจอ (10 ส.ค. 2569):** กดบันทึกราคาทีละตัวได้ แต่กดหลายตัวพร้อมกันพัง
```
null value in column "needs_check" of relation "mr_drug_price" violates not-null constraint
```

**กลไก:** PostgREST รวมรายชื่อช่องจากทุกแถวเป็นชุดเดียว
แถวไหนไม่มีช่องนั้นถูกเติม `NULL` → ชนกับ `NOT NULL` → ตีกลับทั้งก้อน

**ห้ามแก้ด้วยการเติมค่าเริ่มต้นให้ทุกแถว** — จะไปล้าง `note`/`suggestions`
ของยาที่ผู้ใช้ไม่ได้ตั้งใจแตะ = ข้อมูลหาย

**วิธีที่ใช้:** จัดกลุ่มตาม `Object.keys(row).sort().join(',')` แล้ว upsert ทีละกลุ่ม
(ดู `app/api/prices/route.js`) · รองรับช่องใหม่ในอนาคตเองโดยไม่ต้องแก้ซ้ำ

**กฎทั่วไป:** ทุกที่ที่ส่ง array ของ object เข้า `upsert()` ถ้าแถวมีชุดช่องไม่เท่ากัน
ต้องแยกกลุ่มก่อนเสมอ

---

## 3.7 ค่าว่างมีได้หลายความหมาย — อย่าเดาจากความยาว

**บั๊กที่เคยเจอ (10 ส.ค. 2569):** กดเลือกยาสำเร็จแล้ว กล่อง "ไม่พบยาชื่อนี้" เด้งขึ้น

```js
const picked  = !!st.pending && st.query === st.pending.name;
const results = (!picked && q.length >= 2) ? rankResults(...) : [];  // เลือกแล้ว → []
noResults: d.q.length >= 2 && d.results.length === 0                 // ← นึกว่าหาไม่เจอ
```

`results = []` หมายถึงได้ทั้ง **"ยังหาไม่เจอ"** และ **"เลือกไปแล้วเลยปิดรายการ"**
ต้องแยกด้วยธง `picked` ต่างหาก → `noResults: !d.picked && ...`

---

## 3.5 คำศัพท์ — เขียน `Lot` เป็นภาษาอังกฤษ ห้ามทับศัพท์

พี่กันสั่ง: *"เอาคำว่า Lot ไม่ทับศัพท์"* = เขียน `Lot` ด้วยตัวอักษรอังกฤษ
**ไม่ใช่** สะกดไทยว่า "ล็อต" และ **ไม่ใช่** แปลเป็น "ชุด" (แคลร์เคยแปลผิดมาแล้ว)

| ❌ ห้ามใช้ | ✅ ใช้ |
|---|---|
| ล็อตนี้ · ชุดนี้ | `Lot นี้` |
| เลขล็อต · เลขที่ชุด | `เลข Lot` |
| ล็อต L690806-01 | `Lot L690806-01` |

รหัสในฐานข้อมูลยังขึ้นต้นด้วย `L` เหมือนเดิม (ย่อจาก Lot) ไม่ต้องแก้

---

## 4. ฐานข้อมูล

Supabase โปรเจกต์ `tb-calculator` (ref `ryewggkhunpuipgkgbfv`) — **dev กับ production เป็นตัวเดียวกัน** รัน SQL ครั้งเดียวพอ

| ตาราง | เก็บอะไร |
|---|---|
| `mr_drug_price` | ⚠️ **เลิกใช้แล้ว 25 ส.ค. 2569** ราคาย้ายเข้า `drugs` — เก็บไว้เป็นสำเนาสำรอง ไม่มีโค้ดไหนอ่านแล้ว |
| `mr_return` | รายการยาคืน — **มี snapshot ของ ชื่อยา/หน่วย/ราคา อยู่ในแถวเอง** · `qty` เป็น `numeric(12,2)` (รับทศนิยม) |
| `mr_setting` | ตั้งค่าห้องยา + รายชื่อผู้บันทึก (`staff`) + **รายชื่อ รพ.สต. (`pcu_sites`)** + คนล่าสุด (`last_recorder`) · มีแถวเดียวเสมอ (`id = 1`) |
| `mr_lot_seq` | ตัวนับล็อตต่อวัน — ใช้ upsert ให้ atomic (ถ้าใช้ `max()+1` สองเครื่องกดพร้อมกันได้เลขซ้ำ) |

**คอลัมน์สำคัญใน `mr_return`**

| คอลัมน์ | ใช้ทำอะไร |
|---|---|
| `lot_no` | เลขล็อตที่ระบบสร้าง `L690805-01` · 1 รอบกดบันทึก = 1 ล็อต · กดลองส่งใหม่ใช้เลขเดิม |
| `recorded_by` | ชื่อผู้บันทึกล็อต — เลือกจาก `mr_setting.staff` |
| `deleted_at` / `deleted_by` | ถังขยะ — ลบแล้วแค่ประทับเวลา กู้คืนได้ |
| `price_fixed_at` / `price_fixed_by` | หลักฐานว่าแถวไหนถูกตีราคาย้อนหลัง |
| `destroy_reason` | เหตุผลการทำลาย (5 ตัวเลือกใน `helpers.js` → `DESTROY_REASONS`) |
| `pcu_site` | ชื่อ รพ.สต. ต้นทาง — มีค่าเฉพาะแถวที่ `source = 'pcu'` (ดูข้อ 3.42) |
| `drug_id` | **เป็น null ได้** สำหรับยานอกบัญชี รพ. ที่พิมพ์ชื่อเอง |

**`mr_return.drug_id` ไม่ผูก FK โดยตั้งใจ** — แถวมี snapshot ครบแล้วไม่ต้อง join · FK จะไปขวางการแก้ตาราง `drugs` ของเว็บอื่น · ตรงกับที่ ME-DRP ทำไว้แล้ว

**ฟังก์ชันปัจจุบันอยู่ใน `scripts/005_functions_v2.sql`** (แทนที่ `002` ทั้งหมด)
- `mr_summary(p_from, p_to)` → jsonb ของหน้าสรุปทั้งก้อน + `zeroPriced` + `byReason`
- `mr_history(p_q, p_from, p_to, p_limit, p_trash, p_lot, p_offset)` → แถวประวัติ/ถังขยะ + ยอดรวม
- `mr_lots(p_from, p_to, p_limit)` → รายการล็อต
- `mr_next_lot_no(p_date)` → ออกเลขล็อตถัดไปของวันนั้น (atomic)
- `mr_zero_price_count(p_drug_ids)` → นับแถวเก่าที่ราคายังเป็น 0
- `mr_backfill_price(p_items, p_by)` → ตีราคาย้อนหลัง **เฉพาะแถวที่ราคาเป็น 0**
- `mr_top_returned(p_from, p_to, p_limit)` → ยาที่ถูกคืนบ่อยที่สุด (เรียงตามจำนวนครั้ง)

⚠️ **กฎการนับที่ต้องตรงกับมอคอัปเป๊ะ:** `byDrug` กับ `byMonth` **นับเฉพาะ `reuse`** แต่ `bySrc` **นับทั้ง reuse และ destroy** (มอคอัปบรรทัด 1086–1088) · ยอดรวมรายวันในหน้าประวัติมือถือก็นับเฉพาะ reuse

**ไฟล์ .sql:** สร้างเก็บไว้ใน `scripts/` เสมอ แล้ว **รันเองผ่าน Supabase MCP** (`execute_sql`) ไม่ต้องให้พี่กันรัน

RLS เปิดทั้ง 3 ตาราง **โดยไม่มี policy สักข้อ** = ปฏิเสธทุกคำขอจากภายนอก · เข้าถึงได้ทางเดียวคือ API route ที่ใช้ `service_role`

---

## 5. API — 7 เส้นทาง

ทุกไฟล์ใช้แพตเทิร์นเดียวกัน: `export const dynamic = 'force-dynamic'` · ครอบ try/catch · คืน `NextResponse.json(...)` หรือ `{error}` status 500

| เส้นทาง | เมธอด | หมายเหตุ |
|---|---|---|
| `/api/auth` | POST · DELETE | ตรวจรหัสผ่านร่วม → ฝังคุกกี้ 30 วัน · DELETE = ออกจากระบบ |
| `/api/bootstrap` | GET | โหลดครั้งเดียวตอนเปิดเว็บ: ยา + การตั้งค่า + รายชื่อผู้บันทึก + ยอดสะสมปีงบ |
| `/api/drugs` | GET | รายการยาพร้อมราคา/หน่วย/`hasPrice` |
| `/api/prices` | GET · PUT | PUT รับหลายตัว + `backfill:true` เพื่อตีราคาย้อนหลัง |
| `/api/settings` | GET · PUT | รวม `staff` (รายชื่อ) กับ `lastRecorder` |
| `/api/returns` | POST · GET | POST = บันทึกทั้งล็อต (ออกเลขล็อต) · GET รับ `trash` `lot` `offset` `from` `to` |
| `/api/returns/[id]` | PATCH · DELETE | **PATCH ห้ามแตะ `unit_price`** · `action:'restore'` = กู้คืน · DELETE = ย้ายเข้าถังขยะ |
| `/api/summary` | GET | รับ `fy` เพื่อดูปีงบย้อนหลัง |
| `/api/lots` | GET | รายการล็อต |
| `/api/top-returned` | GET | ยาที่ถูกคืนบ่อยที่สุด |
| `/api/drugs/rev` | GET | ลายเซ็นคลังยา — ถามว่ามีใครแก้ยาไปหรือยัง (ดูข้อ 3.16) |
| `/api/catalog` | GET · PUT · POST | หน้าคลังยา — **ไม่มี DELETE โดยตั้งใจ** ลบไม่ได้ ใช้ซ่อนแทน |
| `/api/catalog/audit` | GET | ประวัติการแก้ยารายตัว จาก `drug_audit` |

**ประตูตรวจรหัสผ่าน** อยู่ที่ `middleware.js` — กั้นทุกหน้าและทุก API ยกเว้น `/login` กับ `/api/auth`

🔓 **`MRV_DEV_OPEN=1` ปลดล็อกเฉพาะตอนรันในเครื่อง** (`lib/auth.js` → `devOpen()`)
ต้องเข้าเงื่อนไข **ทั้งสองข้อ** จึงจะปลดล็อก — `NODE_ENV === 'development'` **และ** `MRV_DEV_OPEN === '1'`
Cloudflare build เป็น production เสมอ ตัวแปรนี้จึงไม่มีทางทำให้เว็บจริงเปิดโล่ง
🚨 **ห้ามตั้งบน Cloudflare** และห้ามแก้ให้เหลือเงื่อนไขเดียว
เหตุผลที่ต้องมี: แคลร์กรอกรหัสผ่านลงฟอร์มไม่ได้ (ข้อห้ามติดตัว) ถ้าไม่มีตัวนี้ก็เทสหน้าจริงเองไม่ได้เลย
API ที่ยังไม่เข้าสู่ระบบตอบ **401 พร้อม `needAuth: true`** ไม่ใช่พาไปหน้า login
(ฝั่งจอจะได้ไม่ได้ HTML มาแล้วแปลง JSON พัง)

**Next.js 15: `params` เป็น Promise** → ต้อง `const params = await ctx.params;` ไม่ใช่ `ctx.params.id` ตรง ๆ

**ไม่มี API ค้นหายา โดยตั้งใจ** — โหลดยาทั้ง 417 ตัวทีเดียวตอนเปิดเว็บ (~7 KB) แล้วค้นในเครื่อง
เพราะมอคอัปค้นแบบทันที ไม่มีดีเลย์ ไม่มีสถานะกำลังโหลด · ถ้าเปลี่ยนไปยิงเซิร์ฟเวอร์ทุกตัวอักษรจะรู้สึกช้าลง และเน็ตโรงพยาบาลไม่ไหว

**การกันบันทึกซ้ำ:** `POST /api/returns` ใช้ `upsert(..., { onConflict: 'batch_id,client_rid', ignoreDuplicates: true })`
คู่กับดัชนี unique `(batch_id, client_rid)` · เวลาเน็ตหลุดแล้วกด "ลองส่งใหม่" **ต้องใช้ `batchId` เดิม** ไม่งั้นได้ข้อมูลซ้ำสองชุด
(กรณีที่เจอจริงบนเน็ตโรงพยาบาลคือ "ข้อมูลเข้าฐานไปแล้วแต่คำตอบหายกลางทาง")

---

## 6. ข้อมูลฝั่งเบราว์เซอร์

| ข้อมูล | เก็บที่ | อายุ | ล้างเมื่อ |
|---|---|---|---|
| รายการยา 417 ตัว | localStorage `mrv.drugs` | 12 ชม. | แก้ราคา |
| การตั้งค่า | localStorage `mrv.setting` | 12 ชม. | แก้การตั้งค่า |
| ธีมเข้ม/สว่าง | localStorage `mrv.dark` | ตลอด | ผู้ใช้กด |
| **ร่างที่ยังไม่บันทึก** | localStorage `mrv.session` | ตลอด | บันทึกสำเร็จ |
| ยอดสรุป · ประวัติ · รายการ Lot · คลังยา · ราคา | **ที่เก็บของแท็บ** `mrv.s.*` | ไม่หมดอายุด้วยเวลา | ลายเซ็นเปลี่ยน · บันทึก/แก้/ลบ · ปิดแท็บ |

🚨 **ยอดสรุปกับประวัติยังห้ามเก็บลงที่เก็บถาวร (localStorage)** ตัวเลข KPI เก่าค้างข้ามวันแย่กว่ารอโหลด
แต่ตั้งแต่ v0.12.0.0 เก็บใน **ที่เก็บของแท็บ** ได้แล้ว (พี่กันสั่ง) เพราะเบราว์เซอร์ล้างให้เองตอนปิดแท็บ
และมีลายเซ็นข้อมูลจาก `/api/rev` คอยตรวจว่าของในมือเก่าหรือยังทุก 20 วินาที (ดูข้อ 3.62)

ทุกครั้งที่บันทึก/แก้/ลบ **ต้องเรียก `app.invalidate()`** ไม่งั้นตัวเลขหน้าสรุปกับหน้าประวัติจะหลุดจากกัน

**Optimistic UI ในหน้าประวัติ:** เปลี่ยนบนจอทันที → ยิง API → ถ้าล้มเหลวย้อนกลับเป็น `before` + ขึ้นข้อความแจ้ง

**กันคำตอบเก่าทับคำตอบใหม่:** `loadHistory` ใช้ `app._histSeq` เป็นหมายเลขลำดับ ถ้าคำตอบที่กลับมาไม่ใช่หมายเลขล่าสุดให้ทิ้ง

---

## 6.1 โหมดดูตัวอย่าง — กฎ 4 ข้อ

ไฟล์: `lib/demo.js` (สร้างข้อมูล) + `components/handlers/demo.js` (เปิด/ปิด + ตัวแทนการโหลด)

1. **ห้ามแตะ Supabase เด็ดขาด** ข้อมูลถูกสร้างในเบราว์เซอร์ทั้งหมด ทุกหน้าอ่านจาก `app._demo`
   `demoLoadHistory` / `demoLoadMore` ทำหน้าที่แทน `loadHistory` ตอนเปิดโหมด
2. 🚨 **`app.persist()` ต้อง `return` ทันทีเมื่อ `st.demo` เป็นจริง** — หน้าบันทึกมีล็อตตัวอย่างค้างอยู่
   ถ้าปล่อยให้เขียนลง `localStorage` **ยาที่ผู้ใช้กรอกค้างไว้จริงจะถูกทับหายถาวร กู้ไม่ได้**
3. **ร่างจริงถูกเก็บไว้ที่ `app._realDraft`** ตอน `enterDemo` แล้วคืนกลับตอน `exitDemo`
   (อ่านจาก localStorage ตอนปิดไม่ได้ เพราะ `boot()` ไม่ได้กู้ร่าง มีแต่ `componentDidMount` ที่กู้)
4. **สุ่มด้วยเมล็ดคงที่** (`rng(20690805)` / `rng(760815)`) เปิดกี่ครั้งก็ได้ข้อมูลชุดเดิม
   ห้ามใช้ `Math.random()` ไม่งั้นเทียบหน้าจอกันไม่ได้

ความหนาแน่นข้อมูล: วันธรรมดา 88% ของวัน · วันละ 2-5 ล็อต · ล็อตละ 2-6 รายการ
(ชุดแรกเบาไป 60%/1-3/1-4 พี่กันบอกว่าเห็นแค่สิบกว่าแถว มองไม่ออกว่าใช้จริงจะแน่นแค่ไหน)

---

## 7. มือถือกับคอม

มอคอัปเขียน markup **แยกกันจริง ๆ** ไม่ใช่ responsive ตัวเดียว → โครงนี้เก็บไว้เหมือนเดิม
**1 ไฟล์ต่อ 1 หน้า มี 2 ฟังก์ชันข้างใน** เช่น `renderRecordNarrow` / `renderRecordWide`

สลับด้วย `wide = st.vw >= 1180 && !st.forceNarrow` คำนวณใน `vals/derive.js`
(เดิม 960 แต่คอลัมน์ในตารางบีบจนเหลือ 34px เลยขยับขึ้น)

**สวิตช์บังคับดูแบบมือถือ (`forceNarrow`)** อยู่ในแถบบนของคอม และในแถบล่างของโหมดมือถือ
โผล่เมื่อ `showLayoutSwitch = st.vw >= 960 && st.hasMouse` เท่านั้น

- `hasMouse` วัดด้วย `matchMedia('(pointer: fine)')` ใน `componentDidMount`
- 🚨 **ต้องมีสวิตช์ในแถบล่างของโหมดมือถือด้วย** ไม่งั้นกดสลับเป็น "มือถือ" แล้วสวิตช์หายไปกับแถบบน
  = ติดอยู่ในโหมดมือถือถาวร กลับไม่ได้
- เครื่องสัมผัสล้วนต้องไม่เห็นสวิตช์นี้ (พี่กันสั่ง — ขึ้นมาแล้วบังจอ) ความกว้างอย่างเดียวไม่พอ
  แท็บเล็ตแนวนอน 1024px จะหลุดขึ้นมา

**ตอนเซิร์ฟเวอร์วาดจอยังไม่รู้ความกว้างจอ** ถ้าวาดเลยจะเห็นหน้ามือถือแวบหนึ่งบนคอม
→ `loading: true` ตอนเริ่ม โชว์วงกลมหมุน แล้ววัดจอใน `componentDidMount` (ดู `renderLoading()` ใน `shell.jsx`)

⚠️ **ตอนทดสอบใน Browser pane ของ Claude:** `resize_window` **ไม่ยิง event `resize`** → ต้องสั่ง `window.dispatchEvent(new Event('resize'))` เองด้วย ไม่งั้นหน้าจอไม่สลับ (เบราว์เซอร์จริงยิงเองปกติ)

---

## 8. เรื่องวันที่และปีงบ

- **ปีงบไทย ต.ค.→ก.ย.** · `fyOf(iso) = (เดือน >= 10 ? ปี+1 : ปี) + 543` — 30 ก.ย. กับ 1 ต.ค. ต้องอยู่คนละปีงบ
- **`mr_return.return_date` ไม่มี default โดยตั้งใจ** เพราะนาฬิกาฐานข้อมูลเป็น UTC จะเพี้ยนวันสำหรับเวลาไทยตอนดึก → วันที่ต้องส่งมาจากเบราว์เซอร์เสมอ
- วันที่ที่โชว์บนจอใช้ `thaiDate()` ใน `lib/format.js` (พ.ศ.)

---

## 9. หน่วยนับยา

สูตรตัดสิน: `unit_th ?? FORM_UNIT[form] ?? 'หน่วย'` คำนวณฝั่งเซิร์ฟเวอร์ (`lib/units.js` → `resolveUnit`)

- ค่าเริ่มต้นตามรูปแบบยาอยู่ใน `FORM_UNIT` — แก้ทีเดียวเปลี่ยนทั้งกลุ่ม
- แก้รายตัวเก็บที่ `mr_drug_price.unit_th` — แก้ได้จากหน้าจัดการราคา
- `vial` → ขวด · `amp` → หลอด
- ยาฉีด (`injection`, 48 ตัว) ค่าเริ่มต้นเป็น **หลอด** ยกเว้น 15 ตัวที่พี่กันระบุว่าเป็น **ขวด** (ตั้งไว้แล้วใน `003_seed_prices.sql`)

**ชื่อยาที่โชว์:** ประกอบจาก `generic + strength + unit` ใน `lib/drugName.js` ถ้าชื่อซ้ำกันจะต่อท้ายด้วย `form` เฉพาะตัวที่ซ้ำ
ตั้งชื่อจากยา **ทุกตัวก่อน** แล้วค่อยกรองตัวที่ซ่อนออก ไม่งั้นชื่อยาจะขยับเวลามีคนไปซ่อนยาอีกตัวในเว็บอื่น

---

## 10. เวลาแก้อะไร ให้ไปไฟล์ไหน

| อยากแก้ | ไปที่ |
|---|---|
| หน้าตา/สี/ระยะห่างของหน้าใดหน้าหนึ่ง | `components/pages/<หน้า>.jsx` |
| ตัวเลข/ข้อความที่โชว์ผิด | `components/vals/<หน้า>.js` |
| พฤติกรรมตอนกดปุ่ม | `components/handlers/<หน้า>.js` |
| เพิ่ม/แก้ช่องข้อมูลในฐาน | `scripts/*.sql` → `app/api/*` |
| หน่วยนับยา | `lib/units.js` (ทั้งกลุ่ม) หรือหน้าจัดการราคา (รายตัว) |
| ชื่อยาที่โชว์ | `lib/drugName.js` |
| หัวตาราง/รูปแบบไฟล์ส่งออก | `lib/csv.js` |
| สีตอนเอาเมาส์ชี้ | `app/globals.css` คลาส `.hv-*` |
| หัวเรื่องหน้า (ทุกหน้าฝั่งคอม) | `components/pages/pagetitle.jsx` → `renderPageTitle` — 22px เขียว ห้ามเขียนเอง |
| ปุ่มล้างการเรียง | `components/pages/sortclear.jsx` → `renderSortClear` — ใช้ 3 หน้า |
| หน้าตาตารางทั้งเว็บ | `app/globals.css` คลาส `.tbl` · `.tbl-grid` · `.tbl-zebra` (ดูข้อ 3.75) |
| ช่องค้นหา (ทุกช่องในเว็บ) | `components/pages/thaibox.jsx` → `renderSearchBox` — ห้ามวาดเอง (ดูข้อ 3.74) |
| หัวเว็บฝั่งมือถือ (3 หน้า) | `components/pages/pagehead.jsx` → `renderPageHead` + `HEAD_PAD` |
| ปุ่มส่งออก CSV (ทุกที่) | `components/pages/exportbtn.jsx` — ตัวเดียวใช้ 3 จุด แก้ที่นี่เปลี่ยนครบ |
| ท้ายเว็บ (ลิขสิทธิ์/เลขรุ่น) | `components/pages/footer.jsx` |
| หน้าเกี่ยวกับ | `components/pages/about.jsx` |
| หน้ารายการ Lot + ใบสรุปพิมพ์ | `components/pages/lots.jsx` · `vals/lots.js` · `handlers/lots.js` |
| หน้าตาตอนสั่งพิมพ์ | `app/globals.css` ท้ายไฟล์ (`@media print`) |
| ทำปุ่มใหม่ให้กดด้วยคีย์บอร์ดได้ | ใช้ `{...kb(ฟังก์ชัน)}` แทน `onClick=` (ดูข้อ 3.8) |
| ส่วนหัวความปลอดภัย | `next.config.js` |
| ข้อมูลยาในโหมดตัวอย่าง | `lib/demo.js` → `DEMO_DRUGS` (ต้องมี brand/form/route ครบ ดูข้อ 3.12) |
| ข้อมูลโหมดดูตัวอย่าง | `lib/demo.js` (ตัวข้อมูล) · `components/handlers/demo.js` (เปิด/ปิด) |
| การตรึงแถบ/หัวตาราง | `app/globals.css` (`.hist-head` `.sticky-head`) + `V.fitScreen` ใน `vals/shell.js` |
| แถบเมนู 3 แท็บ (กรอบ/สีตอนชี้) | `app/globals.css` คลาส `.tab-btn` + `cls` ใน `vals/shell.js` |
| หน้าคลังยา (ตาราง · ป๊อปแก้ไข · ประวัติ) | `components/pages/catalog.jsx` · `vals/catalog.js` · `handlers/catalog.js` |
| สีความแรงตอนยาชื่อซ้ำ | `components/vals/record.js` → `ST_COLORS` |
| สีเม็ดยาจริง (Warfarin) | `lib/drugPillColors.js` **และก๊อปไป ME-DRP ด้วย** (ดูข้อ 3.33) |
| ราคายา | ตาราง `drugs` คอลัมน์ `unit_price` — แก้ได้จากหน้าคลังยาหรือหน้าจัดการราคา |
| ลำดับรูปแบบยาตอนเรียงผลค้นหา | `lib/drugSearch.js` → `FORM_ORDER` |
| แก้ไขล็อต | `app/api/lots/[lot]/route.js` + `pages/lotedit.jsx` + `vals/lots.js` → `lotEditVals` (ดูข้อ 3.26) |
| ตัวคิดเลขในช่องจำนวน | `components/helpers.js` → `evalQty` (ดูข้อ 3.27) |
| แหล่งที่มา | `lib/format.js` → `SOURCES` **และ** check constraint ในฐาน (ดูข้อ 3.30) |
| ตรึงแถบ/หัวตารางหน้าคลังยา | `--cathead` + `catHeadRef` ใน `MedReturnApp.jsx` (ดูข้อ 3.25) |
| เลขเวอร์ชัน | `components/helpers.js` (`APP_VERSION`) **และ** `package.json` — ต้องตรงกันทั้งสองที่ |
| วันที่อัปเดตล่าสุด | `components/helpers.js` (`APP_LAST_UPDATE`) — อยู่ติดกับ `APP_VERSION` **ต้องแก้ทุกครั้งที่ bump** |
| ช่องเลือก รพ.สต. (ทั้ง 3 ที่) | `components/pages/pcufield.jsx` — ตัวเดียวใช้ทุกที่ (ดูข้อ 3.42) |
| รายชื่อ รพ.สต. | ตาราง `mr_setting` คอลัมน์ `pcu_sites` · รายละเอียดใน `docs/PCU-SITES.md` |
| ฟอนต์ทั้งเว็บ | `components/helpers.js` → `fontify()` + `app/layout.js` (ดูข้อ 3.43) |
| ฟอนต์ชื่อเว็บ (Charm) | `pages/nav.jsx` · `pages/record.jsx` · `pages/about.jsx` |
| การทวนวันข้ามคืน | `components/handlers/data.js` → `checkDayRollover` (ดูข้อ 3.44) |
| หน้าผลหลังกดส่ง (สำเร็จ/ไม่สำเร็จ) | `components/pages/result.jsx` · `vals/result.js` (ดูข้อ 3.61) |
| จังหวะลองส่งของค้างเอง | `components/MedReturnApp.jsx` → `RETRY_WAITS` · `scheduleRetry` |
| บังคับเลือก รพ.สต. ก่อนบันทึก | `components/handlers/record.js` → `app.pcuSiteMissing()` |
| พื้นที่กดบนจอสัมผัส (44px) | `app/mobile.css` → `.mrv-hit` · `.mrv-hit-input` (ดูข้อ 3.72) |
| บังคับเลือกชื่อผู้ทำในป๊อปยืนยัน | ตั้ง `who` ในก้อน `confirm` · ตัววาดอยู่ `pages/confirm.jsx` |
| ร้องข้อผิดพลาดฝั่งเบราว์เซอร์ | `lib/clientLog.js` → `logFail` · `logWarn` (ดูข้อ 3.73) |
| ถอดข้อความจากก้อนที่ฐานโยนมา | `lib/maskHn.js` → `errParts` · `safeErrLine` |
| อายุบัตรผ่าน + การต่ออายุ | `lib/auth.js` → `AUTH_DAYS` · `authCookieStale` · `authCookieOpts` |

---

## 11. วิธีตรวจว่าใช้ได้จริง

1. `npm run dev` แล้วเปิดบน **Chrome จริง** ไม่ใช่ preview headless (จับอาการค้าง/วาดจอไม่ทันไม่ได้)
2. ทดสอบขนาดคอมจริง ~1360px และมือถือจริง — ไม่ใช่แค่ย่อหน้าต่าง
3. **เส้นทางหลัก:** ค้นยา → Enter → ใส่จำนวน → Enter → แถวเพิ่ม เลขนับขึ้น → กดบันทึก → เปิดประวัติเจอ → เปิดสรุปเลขตรงกัน
4. **กรณีขอบ:** ยาไม่มีราคา (ต้องขึ้นป้ายเตือน) · ยาชื่อซ้ำ · จำนวน 0 หรือติดลบ (ต้องกดเพิ่มไม่ได้) · รีเฟรชกลางคัน (ร่างต้องอยู่) · ปิดเน็ตแล้วกดบันทึก (กล่องแดง + ลองใหม่ + ห้ามได้ข้อมูลซ้ำ) · 30 ก.ย. กับ 1 ต.ค. ต้องคนละปีงบ
5. **ตรวจกฎแช่ราคา:** บันทึกรายการหนึ่ง → ไปแก้ราคายาตัวนั้นในหน้าจัดการราคา → ตัวเลขในหน้าสรุปต้อง **ไม่ขยับ**
6. **ตรวจความปลอดภัย:** เปิด view-source ของหน้าเว็บ ต้องไม่เจอกุญแจ Supabase เลย

---

## 12. เรื่องขึ้นเว็บจริง

- ขึ้น **Cloudflare Workers** ผ่าน OpenNext (`npm run cf:deploy`) — **ไม่ใช่ Pages** เพราะ Next.js มีฝั่งเซิร์ฟเวอร์
- ต้องตั้ง `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` บน Cloudflare ให้ตรงกับ `.env.local`
- **push ขึ้น GitHub ≠ Cloudflare deploy เสร็จ** — ต้องเช็คสองชั้นแยกกันก่อนบอกว่าเว็บจริงอัปเดตแล้ว
- push ขึ้น `main` ตรง ๆ ไม่ใช้ feature branch
