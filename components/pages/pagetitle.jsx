import { s, sx } from '../helpers';

// หัวเรื่องหน้า — ใช้ร่วมกันทุกหน้าฝั่งคอม
// พี่กันสั่ง 4 ก.ย. 2569: "เอาขนาดเท่ากัน และสีตามเว็บ"
// 🚨 ไม่ใส่กรอบและไม่ใส่พื้น — ลองแล้วพี่กันบอก "ไม่เอากรอบดีกว่า ไม่สวย"
//    เหลือแค่ตัวอักษรเปล่า ขนาดกับสีเท่ากันทุกหน้า
//
// ก่อนหน้านี้ 5 หน้าใช้ขนาดต่างกัน 4 แบบ — 600/19px · 700/19px · 700/18px · 700/17px
// สีก็ไม่ตรงกัน บางหน้าเขียว บางหน้าดำ
//
// 🚨 ทุกหน้าต้องเรียกตัวนี้ ห้ามเขียนหัวเรื่องเอง
// 🚨 ฝั่งมือถือไม่ใช้ตัวนี้ — ที่นั่นหัวเรื่องอยู่คู่ปุ่มกลับเป็นชุดของตัวเอง (พี่กันย้ำห้ามแตะ)
export function renderPageTitle(text, opts) {
  const o = opts || {};
  return (
    <div role="heading" aria-level="1"
      style={sx('font:700 22px/1.35 Sarabun,sans-serif;color:#24614a;white-space:nowrap', o.style || {})}>
      {text}
    </div>
  );
}
