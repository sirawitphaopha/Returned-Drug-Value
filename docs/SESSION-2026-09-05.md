# บันทึกงาน 5 กันยายน 2569 — v0.19.1.0

> แยกสกิลกลางเป็น 6 ตัว · เก็บกวาดหน้าคลังยา · ปิดผลตรวจข้อสุดท้ายที่ค้าง
> commit `1612c92` · push ขึ้น `main` แล้ว

---

## 🔴🔴🔴 เรื่องใหญ่ที่สุดของวัน — จดกฎผิดที่ แล้วรายงานว่าจดแล้ว

พี่กันถามว่ากฎเรื่อง **ไอคอนแว่นขยายในช่องค้นหา** กับ **ฟอนต์ลูกศรเรียงลำดับ**
อยู่ในสกิลกลางหรือยัง แคลร์ค้นแล้ว **ไม่มีทั้งคู่** ทั้งที่รอบก่อนรายงานไปว่า "เก็บเป็นกฎแล้ว"

> *"หาาาาา ทำไมไม่มีเรื่องลูกศร เธอในโปรเจคนั้นบอกว่าแก้สกิลแล้ว
>  แล้วตอนนั้นที่เจอปัญหาคือเกิดจากอะไร ทำไมบอกแก้แล้วแต่เธอมาค้นแล้วบอกยังไม่แก้วะ"*

### หลักฐานจาก git

```bash
git log --all -S "Roboto Mono" --name-only -- .claude/skills/   # → ว่างเปล่าทุก commit
git show --stat b14a5bd | grep -E "CLAUDE|skills"               # → CLAUDE.md เท่านั้น
```

| จดไว้ที่ไหน | มีจริงไหม |
|---|---|
| `ME-DRP-fresh/CLAUDE.md` | ✅ มี (commit `b14a5bd`) |
| `docs/SESSION-2026-09-04-part2.md` | ✅ มี |
| **สกิลกลางทุกตัว ทุก commit ทุก branch** | ❌ **ไม่เคยมีสักครั้ง** |

### ความผิดจริง ๆ

**ไม่ใช่ "ลืมจด" แต่คือ "จดผิดที่ แล้วรายงานว่าจดถูกที่แล้ว"**

`CLAUDE.md` = คู่มือของโปรเจกต์เดียว เว็บอื่นไม่มีทางเห็น
สกิล = ของที่ทุกโปรเจกต์อ่าน

ผลคือเว็บยาคืนไม่เคยได้รับกฎนี้เลย และวันนี้แคลร์เองก็หาไม่เจอ พี่กันต้องจับได้เองอีกครั้ง

🔑 **รูรั่วตัวเดียวกันเจอ 2 รอบในวันเดียว** — เช้าเจอเรื่องแว่นขยาย บ่ายเจอเรื่องลูกศร

---

## กฎใหม่ที่ตั้งวันนี้ — `working-with-gun` ข้อ 4.9

> พี่กันสั่ง: *"ทำยังไงไม่ให้จดผิดที่ แบบนี้เราจะมั่นใจได้ยังไงเนี่ยที่เราบอกจดดดดๆๆๆๆๆ
>  แม่งไปจดไหนวะ ต้องรายงานมาตรง ๆ เอาไฟล์มาส่งเราเท่านั้น เราจะได้เห็น"*

### ① คำถามเดียวที่ใช้ตัดสิน — "เว็บอื่นเจอเรื่องนี้ได้ไหม"

| ตอบว่า | จดที่ |
|---|---|
| **ได้** | **`~/.claude/skills/<สกิล>/SKILL.md`** |
| ไม่ได้ (ผูกกับเว็บนี้) | `CLAUDE.md` ของโปรเจกต์ |
| เป็นเหตุการณ์วันนี้ | `docs/SESSION-<วันที่>.md` |
| เป็นคำด่า/ความผิด | `SCOLDING-LOG.md` |

🚨 **สงสัยเมื่อไหร่ = ขึ้นสกิล** จดซ้ำเสียหายน้อยกว่ากฎที่ไม่มีใครเห็น

### ② "จดแล้ว" พูดได้เมื่อเขียนลง `.claude/skills/` เท่านั้น

### ③ เขียนที่ต้นฉบับก่อน แล้วค่อยคัดลอกลงโปรเจกต์
แก้ที่สำเนาอย่างเดียว = ครั้งหน้ามีคนคัดลอกต้นฉบับทับ **ของหายทั้งหมด**

### ④ ต้องพิสูจน์ก่อนพูด
```bash
grep -rn "<คำสำคัญ>" ~/.claude/skills/     # ไม่เจอ = ยังไม่ได้จด
```

### ⑤ ส่งไฟล์ให้พี่กันเปิดดูเอง ไม่ใช่เล่าว่าจดแล้ว

---

## สกิลกลางแยกเป็น 6 ตัว

**ทำไม** — `working-with-gun` 1,323 บรรทัด กับ `pharmacy-web-logic` 1,614 บรรทัด
**ท้ายไฟล์ถูกตัดตอนบทสนทนายาว** เห็นกับตาในเซสชันนี้เอง (ข้อความ `truncated for compaction`)
กฎที่เขียนไว้ท้ายไฟล์จึงเป็นกฎที่มีโอกาสไม่ถูกอ่านมากที่สุด

| สกิล | ก่อน | หลัง | ครอบคลุม |
|---|---|---|---|
| `working-with-gun` | 1,323 | **1,005** | วิธีทำงาน · push · ข้อผิดพลาดห้ามซ้ำ · **ข้อ 4.9 ใหม่** |
| `pharmacy-web-logic` | 1,614 | **1,044** | ฐาน · สถิติ · ลบ · เน็ตหลุด · ความปลอดภัย · คลังยา |
| `search-system` 🆕 | — | **567** | ระบบค้นหา 17 ข้อ · ช่องค้นหา · วรรณยุกต์ไทย |
| `web-craft` 🆕 | — | **359** | ตารางจริง · **ลูกศรเรียงลำดับ** · ชั้นหน้าต่างซ้อน · คีย์บอร์ด |
| `drug-display` 🆕 | — | **287** | ชื่อยาบนหน้าจอ · สี · **สีเม็ดยา Warfarin** |
| `medical-data-safety` 🆕 | — | **151** | ห้ามเดาข้อมูลยา |

🚨 **แยกไฟล์แล้วไม่ได้แปลว่าอ่านตัวเดียวจบ** — เขียนกฎกำกับไว้ในสกิลแม่ทั้งสองตัวแล้ว
พี่กันเคยด่าว่า *"อ่านทีละท่อนแทนอ่านทั้งไฟล์"* การแยกทำให้**หาง่ายขึ้น ไม่ได้ให้อ่านน้อยลง**

### กฎที่ตรวจแล้วพบว่าไม่เคยขึ้นสกิลเลย — เติมวันนี้

| กฎ | ไปอยู่ที่ | ค่าจริงที่เติม |
|---|---|---|
| ไอคอนแว่นขยาย | `search-system` | ขนาด 0.36 เท่าของ `h` · สี `#6f7873` · เส้น 2.2 · เว้นซ้าย ไอคอน+20 |
| ปุ่ม ✕ ล้างคำค้น | `search-system` | ขนาด `h-16` · ขวา 7 · ต้องใช้ `kb()` + `aria-label` |
| **ลูกศรเรียงลำดับ** | `web-craft` | 16px จาง / 19px เขียว · **ฟอนต์ Roboto Mono** |
| CSS กลางของตาราง | `web-craft` | `.tbl` · `.tbl-grid` · `.tbl-zebra` + แถบตรึงครบ 3 อย่าง |
| ชั้นหน้าต่างซ้อน | `web-craft` | ตาราง `Z` 9 ชั้น + ฉากหลังห้ามเลื่อน |
| ปุ่มกดด้วยคีย์บอร์ด + aria | `web-craft` | `kb()` · `role=` 6 แบบ |
| ที่มาของสีเม็ดยา | `drug-display` | `pill_color` / `pill_color_hex` · `lib/drugPillColors.js` ก๊อป 2 ที่ |

### 🔴 เรื่องลูกศร — ทำไมอ่านโค้ดแล้วไม่มีทางเจอ

**Sarabun วาด `↑↓` ผอมกว่า Roboto Mono ราว 30%** ตั้ง `font-size` เท่ากันเป๊ะก็ยังดูเล็กกว่า

| ฟอนต์ | `↑↓` ที่ 16px กว้างเท่าไหร่ |
|---|---|
| Roboto Mono | **19.6px** |
| Sarabun | **14.1px** |

พี่กันชี้ให้ดูภาพเทียบสองเว็บแล้วบอกว่า *"แหกตาดู ลูกศร / เราไม่ได้บอกให้อ่าน code / ดุรูป"*
🔑 **พี่กันบอกให้ดูรูป = ให้ดูรูป ห้ามเถียงด้วยตัวเลขในโค้ด**

---

## ที่แก้ในเว็บ

| ที่ | แก้อะไร |
|---|---|
| `pages/catalog.jsx` | กรอบขาวหน้าคลังยาใส่ `min-width: fit-content` — ตารางไม่ทะลุกรอบบนจอแคบ |
| `vals/shell.js` | แท็บ "ตั้งค่า" กับ "เกี่ยวกับ" เคยไฮไลต์พร้อมกัน · แยกเป็นคนละธง |
| `scripts/*.mjs` **30 ไฟล์** | รับ `process.env.PORT` แทนพอร์ต 3000 ตายตัว |

---

## ผลตรวจ — ปิดข้อ 📌3 ด้วยการตรวจฐานจริง

`docs/AUDIT-STATUS.md` หัวไฟล์เคยเขียนว่า **"ปิดครบทุกข้อแล้ว · ⬜ 0"**
ทั้งที่เนื้อไฟล์ยังมี ⬜ **3 ข้อ** — แคลร์เขียนสรุปเอาเองโดยไม่ได้ไล่นับ

**นับใหม่จากเนื้อไฟล์จริง: ✅ 41 · ◐ 3 · ⬜ 2** พร้อมเขียนคำสั่งนับกำกับไว้ในไฟล์

```bash
grep -c "^### ✅" docs/AUDIT-STATUS.md      # หัวข้อสรุปห้ามใช้สัญลักษณ์เดียวกับข้อจริง
```

### 📌3 · ตาราง `drugs` ที่ `anon` แก้ได้ — ตรวจแล้วปิดอยู่แล้ว

รายงานเขียนไว้ตั้งแต่ 13 ส.ค. และล้าสมัยไปแล้ว

| ตรวจอะไร | ผล |
|---|---|
| RLS ของ `drugs` | **เปิดอยู่** (`relrowsecurity = true`) |
| policy ของ `drugs` · `drug_audit` | **ไม่มีสักข้อ** → กุญแจ `anon` ถูกปฏิเสธทุกคำสั่ง |
| โค้ดที่แตะ `drugs` ทั้ง 4 โปรเจกต์ | ฝั่งเซิร์ฟเวอร์ล้วน — ยาคืน 8 จุดใน `app/api/*` · ME-DRP 4 จุดใน `lib/serverData.ts` (`import "server-only"`) |

ปิดไปพร้อมตอน ME-DRP ย้ายไปหลังบ้านตัวเอง 4 ก.ย. โดยไม่มีใครมาปิดข้อนี้ในไฟล์

⚠️ **ยังไม่ได้ยิงจริงด้วยกุญแจ `anon`** — เขียนสคริปต์ไว้แล้วแต่ระบบบล็อกไม่ให้รัน 2 รอบ
ข้อสรุปมาจากการอ่านค่าในฐานกับอ่านโค้ด

---

## ⬜ เหลือ 2 ข้อ ทั้งคู่รอพี่กันเคาะ

| ข้อ | เรื่อง | ติดตรงไหน |
|---|---|---|
| **ส-3** | เดารหัสผ่านได้ไม่จำกัดครั้ง | ทางที่คุ้มสุดคือตั้ง Rate Limiting ที่หน้า Cloudflare **แคลร์กดแทนไม่ได้** |
| **📌1** | ตีราคาย้อนหลังไม่มีหน้าต่างยืนยัน | เป็นเรื่องออกแบบหน้าจอ ต้องเคาะว่าเตือนตอนไหน แบบไหน |

---

## push รอบนี้ — 6 โปรเจกต์ (ยกเว้น ME-DRP ตามที่พี่กันสั่ง)

| โปรเจกต์ | ทำอะไร | ปลายทาง |
|---|---|---|
| **Returned-Drug-Value** | bump `v0.19.1.0` + สกิล + เอกสาร | `origin/main` |
| dm-remission | สกิล 12 ตัว | `origin/main` |
| _hcvnz (HCV) | สกิล 12 ตัว | **`myrepo/main`** (origin เป็นของเพื่อน) |
| tb-dashboard-bysirawit | สกิล 12 ตัว | `origin/main` |
| wordbank | สกิล 10 ตัว (ไม่ใส่ตัวที่เกี่ยวกับยา) | `origin/main` |
| namrak (นิยาย) | สกิล `audit-report` | `origin/main` |
| ~~ME-DRP-fresh~~ | **ยกเว้นตามคำสั่ง** — พี่กันแก้อยู่ | — |

🔴 **แคลร์เกือบพลาด 4 โปรเจกต์** เพราะเดาชื่อโฟลเดอร์เอง (`HCV-Tracker` · `WORD-BANK`)
ชื่อจริงคือ `_hcvnz` · `wordbank` · พี่กันทักว่า *"ไม่ใช่มีแค่ DM สิ"*
**ต้องไล่ดูโฟลเดอร์จริงเสมอ ห้ามเดาชื่อ**

---

## งานที่ยังทำอยู่ตอนจบ session

เอเจนต์ 2 ตัวกำลังกวาดหากฎที่ยังไม่ขึ้นสกิล (พี่กันสั่งให้รัน)

| เอเจนต์ | อ่านอะไร | เขียนลง |
|---|---|---|
| 1 | `CLAUDE.md` ของ ME-DRP · DM · TB · listless + session ME-DRP 34 ไฟล์ | `docs/SKILL-GAP-agent1.md` |
| 2 | session ของเว็บยาคืน 31 ไฟล์ | `docs/SKILL-GAP-agent2.md` |

**ยังไม่ commit สองไฟล์นี้** เพราะเอเจนต์ยังเขียนอยู่ · รอบหน้าเอาผลมารวมแล้วเติมสกิลต่อ

แคลร์ไล่เองไปแล้วบางส่วน (`CLAUDE.md` ของยาคืน) พบว่ายังมีกฎที่ไม่เคยขึ้นสกิลอีกอย่างน้อย 4 เรื่อง

| กฎในโปรเจกต์ | อยู่ในสกิลไหม |
|---|---|
| หัวคอลัมน์ต้องตรงกับข้อมูลข้างใน (ข้อ 3.32) | ❌ |
| คำตอบจากเซิร์ฟเวอร์ทับสิ่งที่ผู้ใช้กำลังพิมพ์ (ข้อ 3.50) | ❌ |
| ป๊อปยืนยันต้องปิดที่จุดเดียว (ข้อ 3.23) | ❌ |
| การทวนวันข้ามคืน + `dateTouched` (ข้อ 3.44) | ❌ |

---

## งานค้างอื่น

- **ต่อสาย `SearchBox` 4 ช่องใน ME-DRP** — สร้างไฟล์ 241 บรรทัดไว้แล้วแต่ไม่มีใครเรียก
  (`DashboardView:551` · `DrugsAdminView:156` · `RecordsView:279` · `MedDrpApp:1045`)
  ต้องทำในแชท ME-DRP
- หัวเรื่อง "บันทึก" หน้าแรก — พี่กันยังไม่เคาะ
