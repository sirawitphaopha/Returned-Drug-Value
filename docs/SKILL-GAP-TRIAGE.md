# ผลคัดกรอง 240 รายการ — รอบที่ 4

> เขียน 5 กันยายน 2569 · คัดด้วยเกณฑ์ที่พี่กันตั้งเองวันเดียวกัน
> *"จะตั้งเป็นสกิลกลาง สกิลนั้นต้องสามารถปรับใช้กับเว็บทุกเว็บได้ระดับหนึ่ง ไม่ใช่ใส่ทุกสิ่งทุกอย่าง มันก็จะบวม"*
> เกณฑ์ฉบับเต็มอยู่ใน `working-with-gun` ข้อ ②.5

**ก = มาจากไฟล์เอเจนต์ 1 (108 รายการ) · ข = มาจากไฟล์เอเจนต์ 2 (132 รายการ)**
เลขตรงกับเลขในไฟล์ดิบ `SKILL-GAP-agent1.md` และ `SKILL-GAP-agent2.md` เปิดดูข้อความจริงได้ทุกข้อ

## ผลรวม

| กอง | จำนวน | แปลว่า |
|---|---:|---|
| 🅰 **ยกขึ้นสกิลรอบนี้** | **87** | ผ่านครบ 3 ด่าน · เป็นเรื่องที่พังแล้วเสียหายจริง |
| 🅴 **ทิ้ง ไม่ยกขึ้นสกิล** | **91** | ข้อยุบยิบ — **พี่กันสั่งตัดทิ้ง 5 ก.ย. 2569** *"มันเยอะเกินไป เกินกว่าที่เราจะคุมได้"* · ของยังอยู่ในไฟล์ดิบ ค้นได้เสมอ |
| 🅳 **ยกไปแล้วในรอบ 1-3** | **62** | ไม่ต้องทำอะไร · ตรวจของจริงในสกิลแล้ว |

**ไม่มีรายการไหนตกด่าน 1 หรือ 2 เลย** เพราะเอเจนต์กรองชั้นแรกมาแล้วว่าเป็นกฎที่เว็บอื่นเจอได้
สิ่งที่คัดออกในรอบนี้จึงไม่ใช่ "ไม่ใช่กฎกลาง" แต่เป็น **"กฎกลางที่ไม่คุ้มกับที่ที่มันกิน"**

## กองไหนไปสกิลไหน

| สกิล | 🅰 ยกเต็ม | 🅴 (ทิ้งแล้ว) | ที่จะโตขึ้นจริง |
|---|---:|---:|---:|
| `pharmacy-web-logic` | 30 | 14 | **44** |
| `web-craft` | 29 | 36 | **65** |
| `working-with-gun` | 16 | 24 | **40** |
| `mobile-web-app` | 3 | 5 | 8 |
| `medical-data-safety` | 2 | 4 | 6 |
| `search-system` | 2 | 3 | 5 |
| `button-feedback` | 2 | 1 | 3 |
| `making-documents` | 1 | 2 | 3 |
| `drug-display` · `loading-skeleton` · `audit-report` | 1 · 0 · 1 | 0 · 2 · 0 | 4 |

🚨 **`web-craft` กับ `pharmacy-web-logic` จะรับของมากที่สุด** — ทั้งคู่เกิน 40 เรื่อง
ถ้ายกเข้าไปตรง ๆ จะกลายเป็นสกิลก้อนใหญ่แบบเดิมอีกรอบ **ควรแยกสกิลย่อยก่อนยก**

| สกิลที่ควรแตกออกมา | รับเรื่องอะไร |
|---|---|
| **`form-input`** (แยกจาก `web-craft`) | ฟอร์ม · ช่องกรอก · ค่าตั้งต้น · เลื่อนไปหาช่องที่ขาด — ราว 20 เรื่อง |
| **`db-safety`** (แยกจาก `pharmacy-web-logic`) | ความปลอดภัยฐาน · ประตูรหัสผ่าน · ตัวแปรลับ — ราว 15 เรื่อง |

---

# 🅰 กอง A — ยกขึ้นสกิลรอบนี้ (87 เรื่อง)

| # | เรื่อง | ปลายทางที่เอเจนต์เสนอ |
|---|---|---|
| ก5 | 🔴 บันทึกทับสถานะถังขยะเงียบ ๆ | `pharmacy-web-logic` ข้อ 19 (ถังขยะ) |
| ก6 | 🔴 ตัวลบต้องตอบว่าโดนจริงกี่แถว | `pharmacy-web-logic` ข้อ 19 |
| ก7 | 🔴 หน้าแก้ไขใช้ตัวควบคุมคนละชนิดกับหน้ากรอก → ข้อมูลหาย | `web-craft` (ฟอร์ม) |
| ก8 | 🔴 ช่องบังคับที่ไม่มีช่องให้กรอกในหน้าแก้ไข = กดบันทึกไม่ได้ตลอดกาล | `web-craft` |
| ก9 | 🔴 ข้อมูลสดข้ามเครื่องห้ามทับสิ่งที่ผู้ใช้กำลังแก้อยู่ | `web-craft` (realtime) · `pharmacy-web-logic` |
| ก10 | 🔴 สลับประเภทฟอร์ม — ช่องที่ปลายทางไม่มีต้องล้างทิ้งจริง ห้ามซ่อนไว้ | `web-craft` (ฟอร์ม) |
| ก13 | 🔴 ห้ามยัดก้อนข้อมูลจากเบราว์เซอร์ลงฐานตรง ๆ | `pharmacy-web-logic` (ความปลอดภัย) |
| ก14 | 🔴 เส้นทางที่มีรหัสในที่อยู่ ต้องยึดรหัสจากเส้นทาง ไม่เชื่อรหัสในตัวข้อมูล | `pharmacy-web-logic` (ความปลอดภัย) |
| ก15 | 🔴 หน้าตรวจสุขภาพระบบห้ามคืนค่ากุญแจ | `pharmacy-web-logic` (ความปลอดภัย) |
| ก17 | 🟠 ไม่ได้ตั้งรหัสผ่าน = ต้องพังแบบเห็นชัด + ห้ามมีทางลัดปลดล็อกตอนรันในเครื่อง | `pharmacy-web-logic` (ประตูรหัสผ่าน) |
| ก19 | 🔴 `performance.getEntriesByType("resource")` = วิธีแยก "ไม่ได้เรียก" ออกจาก "เรียกแล้วพัง" | `working-with-gun` ข้อ 3.6 (ไล่บั๊กเครื่องคนอื่น) |
| ก20 | 🔴 `stateRef.current` ยังไม่ commit ตอนถูกเรียกจาก mount effect → ข้ามงานเงียบ ๆ | `web-craft` (React) |
| ก22 | 🔴 `useMemo` ห้ามใส่ state ทั้งก้อน + ตัวคำนวณห้ามอ่านค่าที่วิ่งอนิเมชัน | `web-craft` (ความเร็ว) |
| ก24 | 🔴 ตัวกรองต้องเทียบค่าจริง ห้ามยัดลงช่องค้นหาข้อความ | `search-system` · `web-craft` |
| ก25 | 🔴 ตัวกรองกับกราฟต้องใช้กติกาเดียวกันเสมอ | `web-craft` |
| ก29 | 🟠 ค้นหาต้องกวาดเฉพาะช่องที่ผู้ใช้เห็น ห้ามกวาดทั้งก้อนข้อมูล | `search-system` |
| ก33 | 🔴🔴 กฎ 5 ข้อของระบบคำแนะนำทางคลินิก | `medical-data-safety` |
| ก34 | 🔴 ระบบต้องเตือน ไม่ใช่ห้าม | `medical-data-safety` · `web-craft` |
| ก35 | 🔴 กรอกไม่ครบต้องบันทึกได้ | `web-craft` (ฟอร์ม) |
| ก39 | 🔴 ทุกอย่างใน `public/` เสิร์ฟออกไปโดยไม่ผ่านการลงชื่อเข้าใช้ | `web-craft` · `medical-data-safety` |
| ก42 | 🔴 ห้ามเขียนรหัสสีลงในไฟล์ส่วนประกอบ | `web-craft` |
| ก47 | 🔴 เส้นแบ่ง "เตือนสีส้ม" กับ "กั้นสีแดง" | `medical-data-safety` · `web-craft` |
| ก48 | 🔴 ข้อจำกัดของยาจริงต้องสะท้อนในการคำนวณ และต้องเขียนกำกับว่าตั้งใจ | `medical-data-safety` |
| ก54 | 🔴 ตัวเลขสองที่บนจอห้ามขัดกันเงียบ ๆ — ต่างได้แต่ต้องเขียนอธิบาย | `web-craft` |
| ก55 | 🔴 กฎการนับตัวตนต้องมีชุดเดียวในระบบ | `web-craft` · `pharmacy-web-logic` |
| ก56 | 🔴🔴 แคชต้องมีป้ายบอกว่าเป็นของเก่า และห้ามเด้งกลับหน้าล็อกอินตอนเน็ตล้ม | `working-with-gun` ข้อ 5.6 (ข้อมูลต้องเก็บในเครื่อง) |
| ก58 | 🔴 แก้ข้อมูลให้ผู้ใช้ ต้องเก็บค่าเดิมไว้และแสดงว่าแก้แล้ว | `pharmacy-web-logic` · `medical-data-safety` |
| ก59 | 🔴🔴 เขียนทับข้อมูลจริงอัตโนมัติ ต้องมีด่าน 3 ชั้นต่อหนึ่งช่อง | `pharmacy-web-logic` (ความปลอดภัย) |
| ก60 | 🔴 ห้ามเพิ่มคอลัมน์ในตารางที่คนอื่นใช้งานอยู่ · ของเดิมเก็บตารางแยก | `pharmacy-web-logic` (ตารางร่วม) |
| ก62 | 🔴 ไฮไลต์ต่างตัวอักษรในข้อความไทย ต้องลากพยัญชนะฐานมาด้วย | `search-system` (ภาษาไทย) |
| ก64 | 🔴 เอกสารที่พิมพ์ออกกระดาษต้องมีค่าเดียวต่อช่องเสมอ | `making-documents` |
| ก65 | 🔴 ทุกช่องทางที่คุยกับหลังบ้านต้องมีตัวลองใหม่ และตัวใหม่ที่เพิ่มก็ต้องมีด้วย | `pharmacy-web-logic` ข้อ 30 (เน็ตหลุด) |
| ก66 | 🔴 ส่งแล้วต้องถามกลับว่าลงจริงไหม — คำตอบที่ resolve ไม่ได้แปลว่าสำเร็จ | `pharmacy-web-logic` ข้อ 20-21 |
| ก67 | 🔴 แยก "บั๊กของเรา" ออกจาก "ปัญหาเครื่องผู้ใช้" ด้วยรายการสัญญาณ | `working-with-gun` ข้อ 3.6 (ไล่บั๊กเครื่องคนอื่น) |
| ก69 | 🔴🔴 ค่าที่บันทึกลงฐาน ห้ามเป็นข้อความที่โชว์บนจอ | `pharmacy-web-logic` (ฐานข้อมูล) |
| ก70 | 🔴 `NEXT_PUBLIC_*` ต้องมีตอน build ไม่ใช่ตอนรัน | `web-craft` (deploy) |
| ก72 | 🟠 คำสั่ง build บน Cloudflare ของ Next.js ต้องเป็นตัวแปลง ไม่ใช่ `npm run build` | `web-craft` (deploy) |
| ก73 | 🔴 ค่าที่ระบบเติมให้อัตโนมัติ ต้องแยกจากค่าที่ผู้ใช้ตั้งเอง | `web-craft` (ฟอร์ม) |
| ก74 | 🔴 คิวที่เก็บในเครื่องต้องแก้ทีละรายการ ห้ามเขียนทับทั้งชุด | `pharmacy-web-logic` ข้อ 30 (เน็ตหลุด) |
| ก89 | 🔴🔴 ฟังก์ชันฝั่งอ่านต้องโยนข้อผิดพลาด ห้ามคืนค่าว่าง | `pharmacy-web-logic` |
| ก90 | 🔴 สำรองข้อมูลก่อนแตะฐาน และโฟลเดอร์สำรองต้องไม่ขึ้น git | `pharmacy-web-logic` · `medical-data-safety` |
| ก91 | 🔴 ค่าตั้งค่าที่เปลี่ยนได้ ห้ามเอามาผสมในลายเซ็นบัตรผ่าน | `pharmacy-web-logic` (ประตูรหัสผ่าน) |
| ก92 | 🔴 หน้าจอผู้ใช้ไม่บอกสาเหตุโดยตั้งใจ = เว็บจริงพังต้องดูบันทึกเสมอ | `working-with-gun` ข้อ 3.6 · `pharmacy-web-logic` ข้อ 31 |
| ก93 | 🔴 เลื่อนไปหาช่องที่กรอกไม่ครบ — ชุดกฎครบชุด | `web-craft` (ฟอร์ม) |
| ก97 | 🟠 เพิ่มคอลัมน์ใหม่ต้องเติมในรายการคอลัมน์ที่ส่ง ไม่งั้นบันทึกเงียบ | `pharmacy-web-logic` |
| ก103 | 🟠 รันชุดทดสอบทั้งหมดหลังแก้ ไม่ใช่แค่ชุดที่เกี่ยวกับงานรอบนี้ | `working-with-gun` ข้อ 4.8 |
| ข3 | ขอบเขตที่เจ้าของงานระบุชัด ชนะกฎ "อะไรคล้ายกันทำให้เหมือนกัน" | working-with-gun (ข้อ 3 กฎทอง ข้อ 10 มีกฎ "อะไรที่คล้ายกัน ต้องทำให้เหมือนกัน" แต่ไม่มีข้อยกเว้นนี้) |
| ข7 | 🔴🔴 ตัวตรวจที่วัดคนละสภาพกับที่เจ้าของงานดู = ตัวตรวจที่โกหก | working-with-gun (ข้อ 4.8 "เทสด้วยการอ่านโค้ด" มีแล้ว แต่ยังไม่มีข้อนี้) |
| ข13 | เขียนกฎไว้เองแล้วโค้ดจริงไม่ได้ทำตาม — ต้องไล่ตรวจว่าโค้ดทำตามกฎที่จดไว้จริงไหม | working-with-gun (ข้อ 4.8 เช็คลิสต์อ่านโค้ด) |
| ข19 | 🔴 ปุ่มที่กดไม่ได้ ต้องบอกเหตุผลบนตัวปุ่มเอง และห้ามโชว์ตัวเลขที่คำนวณจากค่าที่เกินเพดาน | button-feedback (หัวข้อ "🚫 ปุ่มที่กดไม่ได้") |
| ข24 | 🔴 ก่อนขยายปุ่มให้ถึงเกณฑ์ 44px ต้องอ่านคอมเมนต์ในไฟล์ก่อนว่าที่เล็กอยู่นั้นเจ้าของงานสั่งเอง | mobile-web-app (หัวข้อ "พื้นที่กดทับกัน" · ต่อจากทางแก้ที่ถูก) |
| ข28 | 🔴 กฎที่เขียนไว้แล้วไม่ได้แปลว่าของทั้งหมดทำตาม ต้องวัดจริงเสมอ | working-with-gun (ข้อ 4.8 เช็คลิสต์อ่านโค้ด) — เรื่องเดียวกับข้อ 13 ในรายงานนี้ |
| ข32 | 🔴🔴 การแอบตัดค่าที่เกินเพดานอันตรายกว่าไม่กั้น — ต้องบอกให้เห็น ห้าม `Math.min` เงียบ ๆ | web-craft หรือ pharmacy-web-logic (กฎเรื่องข้อมูลที่เปลี่ยนเองโดยผู้ใช้ไม่รู้) |
| ข33 | 🔴 `{...kb(...)}` ต้องวางก่อน `role=` เสมอ ไม่งั้นทับ `role="dialog"` แล้วระบบกันเลื่อนพัง | web-craft ข้อ 6.5 (ที่นิยาม `kb()` และ `role="dialog"` อยู่ด้วยกันพอดี) |
| ข34 | ฉากหลังห้ามเลื่อนบนมือถือ — ดัก `touchmove` ยังไม่พอ ต้องตรึง `body` ด้วย `position:fixed` และจำตำแหน่งเลื่อนไว้คืน | web-craft ข้อ 6.4 "ฉากหลังห้ามเลื่อน" · และ mobile-web-app |
| ข42 | 🔴 การป้องกันที่ผูกกับคลาสซึ่งใส่ตอน `componentDidMount` = ช่วงแรกหลังโหลดไม่มีการป้องกันเลย | mobile-web-app (หัวข้อ "แยกไฟล์มือถือ · กฎ 3 ข้อ") |
| ข43 | `touch-action` ไม่สืบทอดลงไปยังลูก — ใส่ที่ `html`/`body` อย่างเดียวไม่พอ | mobile-web-app (หัวข้อห้ามซูม / หน้าไถลซ้ายขวา) |
| ข44 | React โหมดเข้มงวดสร้างคอมโพเนนต์สองรอบ — ตัวดักที่ผูกใน `constructor` ต้องมีธงกันผูกซ้ำ | web-craft (ทักษะ web dev) |
| ข49 | 🔴🔴🔴 ค่าที่ถูกใช้ข้ามหน้า ห้ามอยู่ในไฟล์ที่ถูกข้ามการคำนวณได้ | web-craft (หัวข้อ Performance — ต่อจาก lazy render / useMemo) |
| ข51 | 🔴 เทสด้วยการเรียกฟังก์ชัน/อ่าน state ผ่านหมด — ต้องกดปุ่มจริงทีละขั้นเหมือนคนใช้ | working-with-gun ข้อ 4.8 (คู่กับ "เทสด้วยการอ่านโค้ด") |
| ข55 | 🔴 ต้องเช็คสถานะอีกครั้ง หลัง `await` ก่อน `setState` — คำขอเก่าเดินทางกลับมาทับของใหม่ | web-craft (ทักษะ web dev — ต่อจาก optimistic UI / กันคำตอบเก่าทับใหม่) |
| ข56 | โหมดดูตัวอย่าง/ข้อมูลสาธิต ต้องเป็นของปลอมทั้งหมด ทุกหน้า ไม่ใช่แค่หน้าที่นึกออก | pharmacy-web-logic หรือ web-craft |
| ข59 | 🔴 ร่าง/ของที่กรอกค้างต้องแยกรายหน้าต่าง ไม่ใช่รายเครื่อง — พร้อมกลไกทะเบียนหน้าต่างครบชุด | pharmacy-web-logic (ข้อ 30 ชุดรับมือเน็ตหลุด — ร่างเก็บในเครื่อง) หรือ web-craft |
| ข60 | 🔴🔴 รีเฟรชกับทำสำเนาแท็บ แยกไม่ออกจากมุมโค้ด — ตัวแยกคือการปล่อยรหัสคืนตอน `pagehide` | web-craft (ทักษะ web dev) — คู่กับข้อ 59 |
| ข64 | 🔴 อัปเดตสดข้ามเครื่องด้วย "ลายเซ็นข้อมูล" — ต้องมี `updated_at` + trigger ในฐาน ไม่ใช่แค่นับแถว | pharmacy-web-logic (ข้อ 29 มีลายเซ็นคลังยาแล้ว — ข้อนี้คือฉบับทั่วไปสำหรับข้อมูลทำรายการ) |
| ข65 | 🔴 ต้องแยกให้ออกว่าใครเป็นคนแก้ — และเผื่อเวลาต้องไม่เท่ากับคาบของตัวจับเวลา | pharmacy-web-logic หรือ web-craft (คู่กับข้อ 64) |
| ข67 | 🔴🔴 แตะ CSP เมื่อไหร่ ต้องเทส production build เสมอ — หน้าที่ Next สร้างล่วงหน้าได้ HTML ไม่มีใบอนุญาต = จอขาวทั้งใบ | web-craft (Security + Deploy) |
| ข68 | 🔴 วัดก่อนแก้เสมอ — ผลตรวจบอกว่าอะไรหนัก แต่ไม่บอกว่าเวลาไปอยู่ที่ไหน | web-craft (Performance) · audit-report ข้อ 11 |
| ข70 | 🟠 ธง/ค่าที่ใช้ตัดสินว่าจะคำนวณหน้าไหน ห้ามอยู่ในไฟล์ของหน้านั้นเอง (วงจรอุบาทว์) | web-craft (Performance) — คู่กับข้อ 49 |
| ข73 | 🚨 ระบบที่ทำงานเองห้ามเด้งหน้าเต็มจอ · และตัวรับปุ่มที่ถูกเรียกอัตโนมัติต้องเทียบ `opts.auto === true` | web-craft (ทักษะ web dev) หรือ pharmacy-web-logic ข้อ 30 |
| ข74 | ก่อนออกแบบของที่เว็บอื่นมีอยู่แล้ว ให้ไปโคลนโค้ดมาดูของจริง ห้ามเดาจากคำอธิบาย | working-with-gun (ข้อ 3 กฎทอง) |
| ข88 | 🔴 จุดที่ควรหยุดแล้วไม่หยุด — ยิ่งทำต่อยิ่งแย่ลงทุกรอบ | working-with-gun ข้อ 0 (จับสัญญาณให้ได้) |
| ข89 | 🟠 repo ของเว็บโรงพยาบาลที่เปิดสาธารณะ — มีชื่อเภสัชกรจริง ชื่อโรงพยาบาล และบันทึกคำด่า | medical-data-safety หรือ working-with-gun ข้อ 4 (ก่อน commit ต้องกวาดหากุญแจ) |
| ข94 | 🔴🔴 ซากฟังก์ชันทับถมกันในฐาน — เพิ่มพารามิเตอร์แล้ว `create or replace` ไม่ทับตัวเดิม | pharmacy-web-logic (หัวข้อฐานข้อมูล / กฎการแก้ฟังก์ชัน) |
| ข97 | 🔴🔴 เปิด session บนคลาวต้อง `git fetch` ก่อนเสมอ ห้ามเชื่อสิ่งที่เห็นในเครื่อง | working-with-gun (ข้อ 4 push/commit — ตอนนี้ `git fetch` ถูกใช้เฉพาะหลัง push เพื่อ verify) |
| ข98 | 🔴 อย่าเชื่อคำว่า "เติมอย่างเดียวจบ" ในผลตรวจ ต้องดูของจริงก่อน — ผลตรวจบอก 12 จุด ของจริง 79 จุด | audit-report (ข้อ 11 / ข้อ 9 ห้ามทำ) |
| ข100 | 🚨 แก้ประตูรหัสผ่านต้องกัน "รหัสว่าง" ด้วย — ไม่งั้นได้รูที่ใหญ่กว่าเดิม · และ `matcher` มีรูให้เลี่ยงด้วยนามสกุลไฟล์ | pharmacy-web-logic ข้อ 24 (เบราว์เซอร์ห้ามถือกุญแจ / ประตูรหัสผ่าน) |
| ข101 | 🔴🔴 `upsert` กับตารางที่มีคอลัมน์เยอะ ล้างช่องที่ไม่ได้ส่งเป็น NULL — ต้องใช้ `update` รายแถว | pharmacy-web-logic (หัวข้อ 🚨 กฎการแก้ตาราง `drugs` — บรรทัด 569-582) |
| ข102 | 🔴 อย่าเดาชิ้นส่วนของข้อมูลจากการอ่านข้อความ — ให้ส่งข้อมูลดิบรายช่องมาแล้ววาดทีละส่วน | drug-display (หัวข้อ "ตัววาดชื่อยาต้องเป็นตัวกลางตัวเดียว") |
| ข106 | 🔴🔴 ห้ามใช้ `eval()` / `new Function()` เด็ดขาด — CSP ของเว็บจริงบล็อก แล้วพังเงียบ | web-craft (Security — ต่อจากบรรทัด 34 ที่บอกว่า CSP ไม่มี unsafe-eval) |
| ข107 | 🔴 ปุ่มที่กดแล้วโดนตีกลับ + ข้อความเตือนหายใน 2 วินาที = ผู้ใช้เห็นเป็นปุ่มเสีย | button-feedback (หัวข้อปุ่มที่กดไม่ได้) — เรื่องเดียวกับข้อ 19 |
| ข110 | 🔴🔴 `anon` แก้ตาราง `drugs` ได้ — ใครเปิด F12 ก็แก้คลังยาของ 3 เว็บได้จากที่ไหนก็ได้ | pharmacy-web-logic (ข้อ 24 เบราว์เซอร์ห้ามถือกุญแจ · และหัวข้อตาราง `drugs` ที่ใช้ร่วมกัน) |
| ข113 | 🟠 ตีความคำสั่งกลับด้าน — *"RIF สิยังจะถามอีก"* ถูกอ่านเป็น "ลบสิ" | working-with-gun (ข้อ 3 กฎทอง — ไม่ชัด = ถามก่อน) |
| ข114 | 🔴 ไอคอนต้องบอก "สถานะตอนนี้" ไม่ใช่ "สิ่งที่จะเกิดถ้ากด" | button-feedback หรือ web-craft 6.6 |
| ข115 | 🔴 กราฟที่ข้อมูลอาจไม่ครบ อันตรายกว่าไม่มีกราฟ — เหตุผลของพี่กันเอง | web-craft 6.6 (กฎที่ตกผลึกจากงานจริง — ต่อจากข้อ 2 "ค่าอะไรวัดซ้ำได้ต้องมีกราฟ") |
| ข123 | 🔴🔴 ห้ามทำระบบที่โยนงานกลับไปให้เจ้าของงาน ในเมื่อทำเองได้อยู่แล้ว | working-with-gun (ข้อ 3 กฎทอง) · medical-data-safety (มีบรรทัด 109 *"การให้ไปไล่นับเองคือการโยนงานกลับ"* แล้ว แต่เป็นคนละบริบท) |
| ข128 | 🔴🔴 ช่องในฐานที่มีข้อมูลอยู่แล้วแต่ไม่มีโค้ดหยิบมาใช้เลย — เจอ 3 รอบในไฟล์เดียว | pharmacy-web-logic (หัวข้อตาราง `drugs`) หรือ drug-display |

---

# 🅴 กอง E — ทิ้ง ไม่ยกขึ้นสกิล (91 เรื่อง)

**กฎของกองนี้ — ห้ามตั้งหัวข้อใหม่ ห้ามเขียนเกิน 2 บรรทัดต่อเรื่อง**
เพราะรายละเอียดเต็มอยู่ในไฟล์ดิบแล้ว ที่ต้องการคือ **ให้คนอ่านสกิลรู้ว่ามีเรื่องนี้อยู่** เท่านั้น

| # | เรื่อง | หัวข้อที่ควรไปเติม |
|---|---|---|
| ก11 | 🟠 dropdown ต้องเริ่มที่ค่าว่าง ห้ามค้างค่าแรก | `web-craft` (ฟอร์ม) |
| ก12 | 🟠 ค่าว่างต้องแปลงเป็น `null` ก่อนส่งฐาน | `pharmacy-web-logic` |
| ก18 | 🔴 rAF และ `scrollIntoView` แบบนุ่ม ไม่ทำงานเมื่อแท็บถูกซ่อน | `web-craft` (กับดักเบราว์เซอร์) |
| ก21 | 🟠 `const` ไม่ hoist — helper ต้องอยู่นอกคอมโพเนนต์ | `web-craft` (React) |
| ก23 | 🟠 ตัวช่วยที่เก็บผลไว้ใช้ซ้ำ ห้ามแก้ object ที่ได้คืนมา | `web-craft` |
| ก26 | 🟠 กดกราฟแล้วกรอง — ต้องสืบทอดตัวกรองเดิม · แถว 0 กดไม่ได้ · กราฟที่กรองไม่ได้ต้องกดไม่ได้ | `web-craft` |
| ก27 | 🟠 ค่าตั้งต้นของเครื่องนี้ — จำได้บางช่อง ห้ามจำช่องที่ต้องคิดใหม่ทุกครั้ง | `web-craft` (ฟอร์ม) |
| ก28 | 🟠 เตือนว่าอาจกรอกซ้ำ — เตือนอย่างเดียว ห้ามบล็อก | `web-craft` · `pharmacy-web-logic` |
| ก30 | 🟠 ย่อเฉพาะตอนแสดงในตาราง ค่าจริงต้องเต็มเสมอ | `web-craft` |
| ก31 | 🟠 ส่งออก CSV ต้องกันสูตรฝังในไฟล์ | `making-documents` · `pharmacy-web-logic` |
| ก32 | 🟠 มอคอัปห้ามวางใน `public/` | `web-craft` (มอคอัป) · `medical-data-safety` |
| ก37 | 🟠 ฐานเดียวใช้หลายเว็บ ต้องแยกด้วยคำนำหน้าชื่อตาราง | `pharmacy-web-logic` (ฐานข้อมูล) |
| ก38 | 🟠 ชื่อ Worker ใน `wrangler.jsonc` ต้องตรงเป๊ะ | `web-craft` (deploy) |
| ก40 | 🟠 เก็บข้อมูลส่วนบุคคลเท่าที่จำเป็น และตัดออกอัตโนมัติเมื่อส่งออก | `medical-data-safety` |
| ก41 | 🟠 ทุกบันทึกติดผู้บันทึกอัตโนมัติ ผู้ใช้แก้ไม่ได้ | `pharmacy-web-logic` |
| ก43 | 🟠 ทาสีทับพื้นที่มีสีอยู่แล้ว ห้ามใช้สีโปร่งบนตัวเดียวกัน | `web-craft` · `button-feedback` |
| ก44 | 🟠 ตัวเลขสำคัญต้องใช้ตัววาดกลาง เพื่อให้เรียงหลักตรงกัน | `web-craft` |
| ก45 | 🟠 ฉากสามมิติ/ผืนผ้าใบต้องรอเบราว์เซอร์จัดวางเสร็จหนึ่งรอบก่อนสร้าง | `web-craft` |
| ก46 | 🔵 ตัวบล็อก push ด้วยเครื่อง — วิธีทำงานที่มีอยู่ที่เดียว | `working-with-gun` ข้อ 4 (push) |
| ก49 | 🟠 ค่าที่ "แสดงผลเท่านั้น" ต้องเขียนแยกจากค่าที่ใช้ตัดสินใจ | `medical-data-safety` · `web-craft` |
| ก50 | 🟠 ค่าที่ผู้ใช้กรอกต้องรอดข้ามการวาดจอใหม่ | `web-craft` (ฟอร์ม) |
| ก51 | 🟡 สถานะล็อกต้องตั้งใหม่ทุกครั้งที่โหลดหน้า เพราะเบราว์เซอร์เติมค่าให้เอง | `web-craft` (ฟอร์ม) |
| ก53 | 🔴🔴 ทำความสะอาดข้อความที่ต้นเหตุ ตอนรับเข้า ไม่ใช่ตอนแสดงผล | `search-system` · `web-craft` |
| ก63 | 🟠 ของที่ตาแยกไม่ออก ห้ามไฮไลต์ ต้องอธิบายเป็นข้อความไทย | `web-craft` |
| ก68 | 🟠 ทำความสะอาดข้อมูลที่ประตูเดียว ก่อนอะไรก็ตามแตะมัน | `web-craft` · `pharmacy-web-logic` |
| ก75 | 🟠 ตัวเลือกในช่องกรอกต้องดึงจากค่าที่มีจริงในฐาน ไม่ให้พิมพ์เอง | `web-craft` · `pharmacy-web-logic` |
| ก76 | 🟠 ร่องรอยการแก้ต้องทำที่ฐาน ไม่ใช่ที่แอป | `pharmacy-web-logic` |
| ก77 | 🟠 เสียงเตือนบน iPhone ต้องปลดล็อกในจังหวะที่นิ้วแตะ | `mobile-web-app` |
| ก78 | 🟠 ป้องกันสูตรฝังในไฟล์ส่งออก ทำยังไง | `making-documents` |
| ก79 | 🟠 รื้อโครงโค้ดต้องทำเป็นเฟส เรียงจากเสี่ยงน้อยไปมาก และพิสูจน์ว่าไม่เปลี่ยนพฤติกรรมทุกเฟส | `working-with-gun` (วิธีทำงาน) |
| ก80 | 🟡 typecheck จับ import ที่ตายแล้วไม่ได้ | `working-with-gun` ข้อ 4.8 (เทสด้วยการอ่านโค้ด) |
| ก82 | 🟡 ห้าม `pkill -f` ด้วยคำที่ตรงกับคำสั่งของตัวเอง | `working-with-gun` ข้อ 5 (ไฟล์+เครื่องมือ) |
| ก83 | 🟡 เครื่องอาจล้าหลังหลายสิบ commit — ต้องเทียบก่อนเริ่มงาน | `working-with-gun` ข้อ 5 |
| ก84 | 🟡 มอคอัปห้ามเอาของคนละหน้ามารวมกัน | `web-craft` (มอคอัป) |
| ก85 | 🟡 รายการหมวดทางวิชาการต้องเช็คว่าครบตามตำราตั้งแต่แรก | `medical-data-safety` |
| ก86 | 🟡 ประกาศชนิดของไฟล์ที่แชร์ข้ามโปรเจกต์ ต้องเป็น generic | `search-system` · `drug-display` |
| ก94 | 🟠 ตัวอย่างในหัวข้อช่อง ต้องเรียกฟังก์ชันจริงมาสร้าง ห้ามพิมพ์ตายตัว | `web-craft` (ฟอร์ม) |
| ก95 | 🟠 รายการตัวเลือกที่ใกล้กัน ต้องเขียน "เส้นแบ่ง" กำกับ | `medical-data-safety` · `web-craft` |
| ก98 | 🟠 แทรกช่องข้อมูลต้องตรงลำดับกับที่ประกาศคอลัมน์ไว้ | `web-craft` (ตาราง) |
| ก99 | 🟠 แก้ปัญหา "ทำให้เหมือนกัน" ด้วยการกลับด้าน แทนที่จะไล่ทำให้คล้าย | `working-with-gun` กฎทอง 10 · `web-craft` |
| ก100 | 🟠 ตัวอักษรพิเศษในไฟล์ไอคอน ต้องแปลงเป็นเส้นวาด | `web-craft` · `mobile-web-app` |
| ก101 | 🟠 แท็บที่ถูกซ่อนทำให้เครื่องมือทดสอบวัดขนาดหน้าต่างเป็น 0 | `working-with-gun` (การเทส) · `web-craft` |
| ก102 | 🟠 พิสูจน์ว่าปุ่มเลื่อนหน้าจอทำงาน ตอนแท็บซ่อน | `working-with-gun` (การเทส) |
| ก104 | 🟡 บัตรผ่านของเครื่องที่ใช้ร่วมกันต้องอายุสั้น | `pharmacy-web-logic` (ประตูรหัสผ่าน) |
| ก105 | 🟡 ประตูรหัสผ่านปิดหน้าเว็บ ไม่ได้ปิดฐานข้อมูล | `pharmacy-web-logic` |
| ก106 | 🟡 วิธียืนยันว่าตั้งตัวแปรลับสำเร็จ — ดูรหัสตอบกลับ | `pharmacy-web-logic` |
| ก107 | 🟡 ตัวรันในเครื่องเปิดอยู่ ให้ใช้ `tsc` ไม่ใช่ `npm run build` | `web-craft` ข้อ 6 |
| ก108 | 🟡 ตั้งค่าให้ตัวเปิดเซิร์ฟเวอร์หาพอร์ตว่างเอง | `working-with-gun` ข้อ 5.9 |
| ข1 | เทสตารางต้องครบ 3 สภาพ ไม่ใช่แค่เปิดดูในเบราว์เซอร์ | web-craft (ต่อท้าย 6.1/6.2 เรื่องตาราง) |
| ข4 | คำสั่งที่ชี้กลับไปข้อตกลงเดิม ต้องอ่านทั้งประโยค ห้ามอ่านแค่ท่อนแรก | working-with-gun (ข้อ 3 กฎทอง / ข้อ 7 ข้อผิดพลาดห้ามซ้ำ) |
| ข5 | ห้ามใช้ตัวแปรชื่อภาษาไทยใน bash (สร้างโฟลเดอร์ขยะ) | working-with-gun (ข้อ 5 ไฟล์+เครื่องมือ) |
| ข8 | แก้ไฟล์ที่มีทั้งฝั่งคอมและฝั่งมือถือในไฟล์เดียว ห้ามค้นด้วยข้อความ ต้องจำกัดช่วงบรรทัดก่อน | mobile-web-app (หัวข้อ "แยกไฟล์มือถือ" · กฎ 3 ข้อ) |
| ข11 | พื้นปุ่มต้องระบุสีตรง ๆ ห้ามปล่อยโปร่งใส | button-feedback |
| ข12 | `scrollbar-gutter: stable` — จองที่ให้แถบเลื่อนเสมอ เพื่อให้ที่พี่กันเปิด F12 ดูตรงกับมือถือจริง | mobile-web-app |
| ข15 | แตะสกิลเมื่อไหร่ต้องซิงก์สำเนาในโปรเจกต์ทุกไฟล์ ไม่ใช่แค่ไฟล์ที่แก้ | working-with-gun ข้อ 5.4 (มีกฎ "แก้ที่สำเนาอย่างเดียว = ของหาย" แล้ว แต่ทิศตรงข้ามยังไม่มี) |
| ข18 | สกิลอาจเขียนผิดได้ ไม่ใช่แค่ขาด — ต้องตรวจสกิลเทียบโค้ดจริงเป็นรอบ ๆ | working-with-gun ข้อ 5.4 หรือ audit-report |
| ข20 | ระยะขอบติดลบดึงตัวถัดไปเข้ามาทับ — ต้องถอดทั้งระยะขอบขวาและระยะในขวาพร้อมกัน | web-craft (หัวข้อกับดัก CSS ข้อ 6) |
| ข21 | ถามว่า "ออกมายังไง" = ถ่ายภาพ ห้ามอธิบายเป็นตัวหนังสือ | working-with-gun (ข้อ 10 "อ้างว่าทำแล้ว ต้องแนบภาพครอปเฉพาะจุด") |
| ข22 | เปิดหน้าต่างซ้อนเพื่อถ่ายภาพ ให้ตั้ง state ตรง ๆ ผ่าน React fiber ไม่ต้องไล่กดทีละขั้น | search-system (ข้อ 11 วรรณยุกต์ — เตือนผลข้างเคียงตอนเทส) + working-with-gun (วิธีถ่ายภาพ) |
| ข25 | `min-width`/`min-height` ชนะ `width`/`height` ที่เขียนฝังในแท็ก — แก้จุดเดียวได้ทั้งเว็บ | mobile-web-app (หัวข้อ "พื้นที่กดทับกัน") |
| ข27 | ยกแพตเทิร์นที่เจ้าของงานเคาะหน้าตาแล้วมาทั้งดุ้น ห้ามออกแบบใหม่ | working-with-gun (ข้อ 3 กฎทอง) หรือ web-craft 6.6 |
| ข29 | ตัวอย่างโค้ดในสกิลกลางเองก็ผิดได้ — เจอแล้วต้องแก้ต้นฉบับทันที ไม่ใช่แก้แต่ในเว็บตัวเอง | working-with-gun ข้อ 5.4 (การดูแลสกิล) |
| ข31 | ก่อนเปิดบันทึกของเซิร์ฟเวอร์ ต้องตรวจก่อนว่าฐานมีข้อมูลระบุตัวคนไหม แล้วไล่ชื่อช่องออกมา | pharmacy-web-logic ข้อ 31 (เป็นขั้นตอนที่ 0 ก่อนอย่างที่ 1) |
| ข35 | ป๊อปฝั่งมือถือต้องเลื่อนขึ้นจากขอบล่าง พร้อมค่าจังหวะที่พี่กันเคาะแล้ว | mobile-web-app (หัวข้อ "ท่ามาตรฐานที่ทำให้รู้สึกเป็นแอป") |
| ข36 | เล่นแอนิเมชันซ้ำต้องบังคับคำนวณใหม่ด้วย `void el.offsetWidth` คั่น | button-feedback (หัวข้อแอนิเมชัน) หรือ web-craft |
| ข41 | 🟠 ตัวตรวจวรรณยุกต์เชื่อเป็นคำตอบสุดท้ายไม่ได้ — ทั้งฟ้องผิดและบอกผ่านผิด | search-system ข้อ 11 (ท้ายหัวข้อวรรณยุกต์) |
| ข45 | เปลี่ยนหน้าต่างซ้อนเป็นหน้าเต็มจอ ต้องเป็น "แทนที่" ไม่ใช่ "วาดเพิ่ม" | web-craft ข้อ 6.4 (ต่อจากตารางชั้นหน้าต่าง) |
| ข47 | 🔑 คำถามของเจ้าของงานคือคำตอบ — เขาชี้จุดถูกก่อนแคลร์เสมอ | working-with-gun (ข้อ 3 กฎทอง หรือข้อ 3.5 กฎ 10 ข้อ) |
| ข53 | 🟠 ห้ามเดาว่าข้อมูลในฐานที่ไม่รู้จักเป็นขยะจากการเทสของตัวเอง แล้วเสนอลบ | medical-data-safety (ต่อจาก "ห้ามแก้ข้อมูลในฐานจริงโดยไม่ได้สั่ง") |
| ข57 | จอเตี้ยต้องบีบแถว ไม่ใช่รีดช่องไฟ · และต้องวัดความสูงจอ ไม่ใช่แค่ความกว้าง | web-craft (ตาราง) หรือ mobile-web-app |
| ข58 | 3 ทางที่ลองแล้วผิดสำหรับข้อความยาวเกินช่องที่ต้องเห็นท้ายสุด | search-system (ข้อ 11-12 โครงสามชั้น — เป็นท่าเดียวกัน) |
| ข66 | แท็บที่ซ่อนอยู่ไม่ต้องถามเซิร์ฟเวอร์ | web-craft (Performance / Realtime) |
| ข75 | 🟠 วิธีทดสอบเมื่อเครื่องไม่มีกุญแจฐานข้อมูล (เครื่องคลาว) — 3 ท่า | working-with-gun (ข้อ 5 ไฟล์+เครื่องมือ / หัวข้อเครื่องคลาว) |
| ข76 | 🟠 ล้างข้อมูลจริงต้องทำที่ จุดรวมค่าจุดเดียว ห้ามไปดักทีละหน้า | loading-skeleton (กฎข้อ 1-2) |
| ข77 | 🟠 เครื่องมือถ่ายภาพโครงจางต้องหน่วงเฉพาะคำขอข้อมูล ไม่แตะไฟล์เว็บ | loading-skeleton (ต่อจากหัวข้อ 🎛 สวิตช์) |
| ข83 | 🚨 ฟอนต์เว็บห้ามลามเข้าใบพิมพ์เอกสารราชการ — พิสูจน์ 2 ชั้นจากไฟล์จริง | making-documents (มี `pdf-font.mjs` และกฎฟอนต์ราชการแล้ว แต่ยังไม่มีข้อนี้) |
| ข85 | 🔴 มอคอัปที่ส่งให้ดูต้องตรงกับโค้ดจริงเสมอ — แก้โค้ดหลังทำมอคอัปแล้วไม่อัปเดตไฟล์ | web-craft 6.6 ข้อ 8 (เรื่องมอคอัป) หรือ working-with-gun ข้อ ๖ |
| ข86 | 🟠 ไฟล์มอคอัปที่วางไว้ในโฟลเดอร์สาธารณะถูกเสิร์ฟบนเว็บจริง ใครมีลิงก์เปิดดูได้ | web-craft 6.6 ข้อ 8 (มอคอัป) · medical-data-safety |
| ข93 | 🟠 ตรวจก่อนแก้สีให้เข้มขึ้นว่าสีนั้นถูกใช้บนพื้นมืดด้วยไหม | drug-display (มีตารางสีสองพื้นแล้ว) หรือ web-craft |
| ข96 | 🟠 ไม่สร้าง UI ใหม่ที่ไม่เข้าชุดกับของเดิม — แก้ข้อความให้ตรงความจริงแทน | web-craft 6.6 (กฎที่ตกผลึกจากงานจริง) |
| ข99 | 🟠 อ่านบันทึกหลายรอบในวันเดียวกัน ต้องเอาสถานะจากไฟล์รอบสุดท้ายเสมอ | working-with-gun (ข้อ 4 บันทึก session) หรือ audit-report |
| ข104 | 🟠 การกันสิ่งไม่พึงประสงค์ต้องดักที่ ทางออกทางเดียว ไม่ใช่ไล่กันทีละฟังก์ชัน | web-craft (ทักษะ web dev) หรือ pharmacy-web-logic |
| ข108 | 🚨 แถวที่มีปุ่มสลับสถานะ ต้องมีจำนวนช่องปุ่มคงที่เสมอ ไม่งั้นกรอบเลื่อนตอนกด | web-craft (ตาราง) หรือ button-feedback |
| ข109 | 🟠 ค่าที่ผูกกับขนาดของอย่างอื่น ต้องเขียนกำกับว่ามาจากไหน | web-craft (ตาราง / CSS) |
| ข111 | 🟠 ยาตัวเดียวมีหลายตัวย่อ ให้เลือกตัวเดียวและใช้ชื่อสากล (INN) | drug-display (หัวข้อตัวย่อ) หรือ medical-data-safety |
| ข119 | 🟠 ค่าที่คำนวณไว้แต่ไม่มีใครวาด = ฟีเจอร์ที่มีแต่ใช้ไม่ได้ | working-with-gun ข้อ 4.8 (เช็คลิสต์อ่านโค้ด ③ "ของที่สร้างไว้แต่ไม่มีใครเรียก") |
| ข120 | 🟠 เทคนิคที่ยกไปใช้ได้ทันที — โดนัท SVG รัศมี 15.9 · CSV ต้องมี BOM · เทสประตูรหัสโดยไม่ให้รหัสผ่านตา | web-craft (SVG/กราฟ · CSV) · working-with-gun (วิธีเทสประตูรหัส) |
| ข121 | 🟠 เพิ่มช่องที่ต้องสืบกลับได้ ต้องเพิ่มตั้งแต่วันแรก — เพิ่มทีหลังต้องย้ายข้อมูล | pharmacy-web-logic (ออกแบบฐานข้อมูล) — คู่กับกฎ *"เก็บข้อมูลเกินดีกว่าขาด"* ใน `web-craft:94` |
| ข127 | 🟠 ชื่อ Worker ใน `wrangler.jsonc` ต้องตรงกับของจริงบน Cloudflare ไม่งั้น deploy สร้างโปรเจกต์ใหม่ | web-craft (Deploy) |
| ข130 | 🟠 แปลงโค้ดจำนวนมากด้วย `sed` ต้องเติม `import` ให้ทุกไฟล์ที่แตะ | working-with-gun (ไฟล์+เครื่องมือ) · web-craft ข้อ 6.5 (ที่นิยาม `kb()`) |
| ข131 | 🟠 ทำไมไม่เปิดหน้าต่างใหม่แล้วสั่งพิมพ์ · และกราฟห้ามปัดหลักพัน | making-documents (การพิมพ์) · web-craft 6.6 (ตัวเลขต้องบอกความหมาย) |

---

# 🅳 กอง D — ยกไปแล้วในรอบ 1-3 (62 เรื่อง · ไม่ต้องทำ)

ตรวจของจริงในไฟล์สกิลแล้ว ไม่ได้เชื่อจากบันทึก

| # | เรื่อง | อยู่ที่ |
|---|---|---|
| ก1 | 🔴🔴🔴 heredoc กลืนแบ็กสแลช — และสกิลกลางกลับแนะนำให้ใช้ heredoc | `working-with-gun` ข้อ 5 (ไฟล์+เครื่องมือ) |
| ก2 | 🔴 ดึงข้อมูลได้ไม่เกิน 1,000 แถวแบบเงียบ ๆ | `pharmacy-web-logic` (ฐานข้อมูล) |
| ก3 | 🔴 ลำดับปล่อยงานที่แตะฐาน — ห้ามสลับ | `working-with-gun` ข้อ 4 (push) |
| ก4 | 🔴 ตัวแปรที่เพิ่งเพิ่มบน Cloudflare ไม่มีผลจนกว่าจะ deploy ใหม่ + สองที่ต้องใส่แยกกัน | `working-with-gun` ข้อ 4 |
| ก16 | 🔴 `run_worker_first: true` — ไม่ตั้งแล้วมีทางเข้าที่ไม่ผ่านหน้าล็อกอิน | `pharmacy-web-logic` (ประตูรหัสผ่าน) |
| ก36 | 🔴 `rls_enabled_no_policy` เป็นสถานะที่ตั้งใจ ห้ามไป "แก้" | `pharmacy-web-logic` (ความปลอดภัย) |
| ก52 | 🔴🔴🔴 `normalize('NFC')` ไม่แก้ลำดับวรรณยุกต์ไทย — ต้องเรียงเครื่องหมายเอง | `search-system` (ภาษาไทย) |
| ก57 | 🔴 ปฏิทิน พ.ศ. บนเครื่องผู้ใช้ทำให้ช่องวันที่มาตรฐานส่งปีผิด (บั๊ก 3112) | `web-craft` (ไทยเฉพาะ) |
| ก61 | 🔴 ระบบเสนอ คนตัดสิน — และต้องบอกก่อนเขียนว่าจะกระทบอะไรบ้าง | `pharmacy-web-logic` · `web-craft` |
| ก71 | 🔴 ตัวแปรบน Cloudflare ต้องเป็นชนิด Secret ไม่ใช่ข้อความธรรมดา | `working-with-gun` ข้อ 4 · `pharmacy-web-logic` |
| ก81 | 🟡 เซิร์ฟเวอร์ทดสอบค้างแบบ "พอร์ตเปิดแต่ไม่ตอบ" | `working-with-gun` ข้อ 5.9 (เปิดเซิร์ฟเวอร์ทดสอบ) |
| ก87 | 🔴🔴🔴 เอกสารที่จดค่าไว้ผิด ทำให้โค้ดผิดตามไปเรื่อย ๆ — ค่าที่คัดลอกมาจากอีกเว็บ ต้องวัดจากของที่รันจริง | `working-with-gun` (วิธีทำงาน) · `drug-display` |
| ก88 | 🔴🔴 "ไปดูเว็บนั้นมาว่าเป็นยังไง" = ไปดูแล้วทำให้เหมือน จบในรอบเดียว | `working-with-gun` ข้อ 9 (งานที่สั่งแล้วห้ามถามซ้ำ) |
| ก96 | 🟠 ขนาดตัวอักษรของช่องกรอกใช้กฎกลางได้ · สีตอนชี้ปุ่มใช้ไม่ได้ — เส้นแบ่งอยู่ตรงไหน | `web-craft` · `button-feedback` |
| ข2 | ย่อหน้าต่างด้วยเครื่องมือเบราว์เซอร์ไม่ได้ผล ต้องบีบความกว้างพื้นที่เลื่อนด้วยสคริปต์ | web-craft หรือ working-with-gun (หัวข้อวิธีเทส) |
| ข6 | คอมเมนต์ JSX วางผิดที่ทำเว็บพัง — เพิ่มอีก 2 รูปแบบที่สกิลยังไม่ได้ระบุ | button-feedback บรรทัด 372 มีกฎนี้แล้ว แต่ยังไม่ครบ 2 กรณีนี้ · ควรย้ายไป web-craft ด้วยเพราะไม่ใช่เรื่องปุ่ม |
| ข9 | กับดักเครื่องมือ 4 ข้อที่ไม่ผูกกับโปรเจกต์ | working-with-gun (ข้อ 5 ไฟล์+เครื่องมือ) |
| ข10 | ตัวตรวจต้องหาปุ่มด้วย `aria-label` ไม่ใช่ข้อความบนปุ่ม | working-with-gun (หัวข้อเครื่องมือตรวจ) หรือ audit-report ข้อ 11 |
| ข14 | 🔴🔴 "สกิลกลาง คือกฎ" — ทุกอย่างที่อยู่ในสกิลคือกฎ ไม่ว่าเขียนในรูปแบบไหน | working-with-gun (ควรอยู่บนสุด ข้อ 5.4 สกิลกลาง) |
| ข16 | ตัวตรวจขึ้น "ตก" ต้องอ่านสาเหตุก่อน อย่าเพิ่งไปแก้โค้ด | working-with-gun (ข้อ 4.8 / หัวข้อเครื่องมือตรวจ) |
| ข17 | 🟠 สีในสกิล `drug-display` ยังขาด "พื้นหลังตัวย่อสีม่วงอ่อน" | drug-display (หัวข้อ "สีที่ใช้ (ห้ามสลับ)") |
| ข23 | สคริปต์ชั่วคราวต้องวางในโปรเจกต์ ไม่ใช่โฟลเดอร์ชั่วคราวนอกโปรเจกต์ · และต้องลบทิ้ง | working-with-gun (ข้อ 5 ไฟล์+เครื่องมือ) |
| ข26 | กับดัก 4 ข้อของสคริปต์เทสที่ต้องล็อกอินก่อน | working-with-gun (หัวข้อเครื่องมือตรวจ) หรือสกิลใหม่เรื่องเขียนสคริปต์เทส |
| ข30 | สกิลเขียนนามสกุลไฟล์เป็น `.ts` — ต้องแปลงตามภาษาของโปรเจกต์ ไม่ใช่ลอกทั้งดุ้น | pharmacy-web-logic (ข้อ 31 อย่างที่ 1-2 · หัวไฟล์) |
| ข37 | 🟠 ห้ามใช้ `width` คงที่กับปุ่มที่มีข้อความ · ข้อความที่ขาดกลางให้แยกก้อนตามจุดคั่นแล้ว `nowrap` · กล่องขาวที่มีมุมมนต้องมี `overflow:hidden` | button-feedback (ปุ่ม) · web-craft (กล่อง/ข้อความ) |
| ข38 | 🔴 เปลี่ยนฟอนต์แล้วต้องไล่ระยะทุกจุดที่เคยเผื่อไว้ให้ฟอนต์เก่า | web-craft (หัวข้อฟอนต์) |
| ข39 | ลูกศร ▾ ของช่องเลือกต้องลอยทับขอบขวา ไม่กินที่ในแถว | web-craft (ช่องกรอก/ช่องเลือก) |
| ข40 | สคริปต์ถ่ายภาพต้องรอสิ่งที่ต้องการโผล่จริง ไม่ใช่รอเป็นเวลาตายตัว | working-with-gun (หัวข้อเครื่องมือตรวจ) — รวมกับข้อ 26 |
| ข46 | ตัวตรวจต้องอัปเดตตามเมื่อของเปลี่ยนสถานะ · แทนที่บล็อกด้วยสคริปต์ต้องนับวงเล็บให้ครบ | working-with-gun (เครื่องมือตรวจ + ไฟล์/เครื่องมือ) |
| ข48 | 🟠 ตัวตรวจ "ตก" เพราะฐานไม่มีข้อมูล ไม่ใช่โค้ดพัง — ต้องแยกให้ออก | working-with-gun (หัวข้อเครื่องมือตรวจ) — รวมกับข้อ 16 |
| ข50 | 🔴 บั๊กที่ "โค้ดไม่ได้พัง มันแค่ไม่มีอะไรให้เรียก" — เงียบที่สุด ไม่มีสัญญาณเตือนเลย | working-with-gun ข้อ 4.8 (เช็คลิสต์อ่านโค้ด — มีข้อ ③ *"ของที่สร้างไว้แต่ไม่มีใครเรียก"* แต่ไม่มีทิศตรงข้าม) |
| ข52 | 🟠 ตัวตรวจต้องหาปุ่มด้วยชื่อที่ตรงกับของจริง ไม่ใช่ชื่อที่เดาเอง | working-with-gun (หัวข้อเครื่องมือตรวจ) — รวมกับข้อ 10 |
| ข54 | 🔴🔴 กฎ "แตะหน้าจอต้องเปิดภาพดูก่อนรายงาน" ถูกเก็บไว้ในสกิลมือถืออย่างเดียว ทั้งที่เกิดกับหน้าคอม | working-with-gun (ข้อ 7 หรือ 4.8) — ตอนนี้อยู่ใน mobile-web-app เท่านั้น |
| ข61 | เครื่องมือทดสอบปิดหน้าต่างโดยไม่ยิง `pagehide` — สคริปต์เทสต้องจำลองเวลาผ่านไปเอง | working-with-gun (หัวข้อเครื่องมือตรวจ) — รวมกับข้อ 26 · 40 · 46 |
| ข62 | 🟠 บั๊กหน้าตาเล็ก ๆ เห็นได้จากภาพเท่านั้น — ผลเทสไม่มีทางจับ | working-with-gun — หลักฐานเสริมของข้อ 54 |
| ข63 | 🔴🔴 "โหลดไม่สำเร็จ" ต้องไม่หน้าตาเหมือน "ไม่มีข้อมูล" และหน้าที่รายงานตัวเลขต้องไม่วาดตัวเลขเลย | loading-skeleton (เป็นสถานะที่สามคู่กับ "กำลังโหลด" และ "มีข้อมูล") |
| ข69 | 🔴🔴 ห้ามแก้สคริปต์เทสให้ผ่านโดยไม่รู้ว่าทำไม — รอบเดียวเจอวัดผิด 4 ครั้ง รวมทั้ง "ผ่านแบบหลอกตัวเอง" | working-with-gun (หัวข้อเครื่องมือตรวจ) — หัวใจของชุดข้อ 10 · 16 · 26 · 40 · 46 · 48 · 52 · 61 |
| ข71 | 🔴🔴🔴 รอสัญญาณ "เน็ตกลับมา" อย่างเดียวไม่พอ — ต้องลองส่งเองเป็นระยะแบบถอยห่าง และห้ามหยุดเอง | pharmacy-web-logic ข้อ 30 (แก้ของเดิม) |
| ข72 | 🔴 หน้า "ส่งไม่สำเร็จ" ต้องตอบ 3 คำถาม และต้องนับถอยหลังจริงทุกวินาที | pharmacy-web-logic ข้อ 30 (อย่างที่ 6) |
| ข78 | 🟠 กฎ "เปิดดูทุกหน้าจริง ๆ" ถูกจดไว้ 2 สกิล แต่ไม่ได้อยู่ในสกิลกลาง | working-with-gun (ข้อ 4.8 / ข้อ 10) |
| ข79 | 🔴🔴 คำสั่งของเจ้าของงานต้องเก็บคำต่อคำ ห้ามจดเป็นสรุป — จดย่อแล้วเงื่อนไขหาย | working-with-gun (ข้อ 4.5 เอเจนต์ต้องบันทึกลงไฟล์ / ข้อ 3 กฎทอง) · audit-report |
| ข80 | 🔴🔴 ฟอนต์อังกฤษ/ตัวเลขแยกจากฟอนต์ไทยด้วยลำดับ fallback ตัวเดียว — ไม่ต้องเขียนเงื่อนไขแยกภาษา | web-craft (หัวข้อฟอนต์ / ไทยเฉพาะ) |
| ข81 | 🔴🔴 จุดที่เขียนชื่อฟอนต์ตรง ๆ ในโค้ด ไม่ผ่านตัวแปลงกลาง — ของที่สั่งเปลี่ยนจะไม่เปลี่ยน | web-craft (ฟอนต์) · drug-display (ตัววาดชื่อยาเป็นจุดที่พลาด) |
| ข82 | เปลี่ยนฟอนต์แล้วต้องไล่ตรวจของที่ล้น — Roboto Mono กว้างกว่า Sarabun ~15% | web-craft (ฟอนต์) — คู่กับข้อ 38 |
| ข84 | 🔴 ปุ่มที่กดไม่ได้ต้องใช้สีของปุ่มตัวเองที่จางลง ห้ามยกชุดจากปุ่มอื่น · แอนิเมชันต้องได้รับคำสั่งรายปุ่ม | button-feedback — ✅ ยกขึ้นสกิลแล้ว (ตรวจจริง: บรรทัด 195 "ใส่แอนิเมชันได้เฉพาะปุ่มที่พี่กันสั่งเอง" · ตาราง "ใช้สีของปุ่มตัวเองที่จางลง") · แต่คลาส `.hv-off-green` ยังไม่มีในสกิล |
| ข87 | 🟠 ตัวตรวจสีมี "จุดบอดที่ตัดสินแทนคนไม่ได้" ต้องแยกเป็นรายการให้คนอ่านเอง ห้ามเดา | button-feedback — ✅ มีในสกิลแล้ว (`button-feedback:311-322`) · แต่หลักการ "ตัวตรวจต้องแยกสิ่งที่ตัดสินไม่ได้ออกมาแทนที่จะเดา" ยังไม่ถูกยกเป็นกฎกลาง |
| ข90 | 🔴 เขียนคุณสมบัติ CSS แบบรวบ ล้างค่าที่กฎกลางตั้งไว้ทิ้ง — ต้องเขียนแบบแยกเสมอ | web-craft (กับดัก CSS) หรือ button-feedback (มีเคส `border-color` อยู่แล้ว) |
| ข91 | 🔴 ปุ่มห้ามย่อเหลือแต่ไอคอน · ตัวกรองที่เลือกอะไรไม่ได้เลย แย่กว่าตัวกรองที่ยังไม่มีข้อมูล · รายการตัวเลือกต้องมาจากการตั้งค่า ไม่ใช่จากที่โผล่ในข้อมูล | web-craft 6.6 (เกณฑ์ 4 ข้อ / กฎที่ตกผลึกจากงานจริง) |
| ข92 | 🚨 เครื่องมือสร้างไฟล์ต้องตรวจก่อนว่า "ของที่จะพิมพ์เปิดอยู่จริง" ไม่งั้นไล่ผิดทาง | making-documents — ✅ มีในสกิลแล้ว (ตรวจจริง `making-documents` หัวข้อเครื่องมือ) |
| ข95 | 🚨 `<option>` ในดรอปดาวน์ต้องใส่สไตล์ที่ตัวมันเอง ไม่สืบทอดจาก `<select>` | web-craft (ช่องกรอก/ช่องเลือก) — คู่กับข้อ 39 · 90 |
| ข103 | 🚨 ช่องกรอกกับกล่องธรรมดาคิดความสูงจาก padding คนละแบบ — ต้องกำหนด `height` ตายตัว | web-craft (กับดัก CSS) หรือ search-system (แถวช่องค้นหา+ตัวกรอง) |
| ข105 | 🟠 ตั้ง git ไม่ให้เด้งหน้าต่างถามบัญชี — ไม่งั้นค้างรอตลอดกาลตอนพี่กันไม่อยู่ | working-with-gun (ข้อ 4 push/commit หรือข้อ 5 เครื่องมือ) |
| ข112 | 🟠 ข้อจำกัดเครื่องมือ — `visibilitychange` ทดสอบใน Browser pane ไม่ได้ ใช้ `online` แทน | working-with-gun (เครื่องมือ) — รวมกับข้อ 2 (`resize_window` ไม่ได้ผล) |
| ข116 | 🚨 ของที่ "ยุบได้ตอนที่ว่างไม่พอ" ต้องเช็คว่ายุบแล้วยังอ่านได้ — ซ่อนเนื้อหาในกล่องจิ๋วแย่กว่าปล่อยให้ทั้งแผงเลื่อน | web-craft (กับดัก flex/overflow — ต่อจากบรรทัด 29) |
| ข117 | 🔑 วิธีหาบั๊ก "กล่องยุบแล้วซ่อนเนื้อหา" — วัดพื้นที่เลื่อนทุกอันในหน้าแล้วหาตัวแปลกปลอม | web-craft (คู่กับข้อ 116) |
| ข118 | 🚨 `flex:1` ย่อจาก `1 1 0%` — ฐานความสูงเป็นศูนย์ กล่องจึงไม่ยอมหด ต้องใช้ `flex:1 1 auto` | web-craft (กับดัก flex) |
| ข122 | 🔴 บั๊กเลื่อนหน้าค้างข้ามแท็บ — พื้นที่เลื่อนก้อนเดียวใช้ร่วมทุกหน้า · แก้ 2 ชั้น | web-craft (ทักษะ web dev / กับดัก CSS) |
| ข124 | 🔴 ชัวร์ → ใส่ค่าเลย · ไม่ชัวร์ → ปล่อยเป็นศูนย์แล้วแนบตัวเลือกไว้ให้กด (ห้ามใส่ค่าที่อาจผิด) | medical-data-safety (ต่อจาก "ห้ามเดา") |
| ข125 | 🔴🔴 ตรวจความแรงเสมอ ไม่ใช่ตรวจเฉพาะตอนมีหลายตัวเลือก — เคสจับคู่ผิดที่ระบบบอกว่า "มั่นใจ" | medical-data-safety (การจับคู่ข้อมูลยา) |
| ข126 | 🚨 Tall Man Lettering ในไฟล์ HIS — ต้องเทียบชื่อแบบ "บีบติดกัน" ห้ามแปลงขีดเป็นช่องว่าง | medical-data-safety หรือ search-system (การเทียบชื่อยา) |
| ข129 | 🔧 dev server ค้างแบบ "พอร์ตยังจองอยู่แต่ไม่ตอบ" — ต้องแยกจาก "พี่กันทำอะไรผิด" | working-with-gun ข้อ 5.9 (เปิดเซิร์ฟเวอร์ทดสอบ) หรือ web-craft |
| ข132 | 🟠 ค่าว่างมีได้หลายความหมาย — ห้ามเดาจากความยาวของผลลัพธ์ | search-system (ข้อ 7 หรือท้ายส่วนที่ 3 ผลค้นหา) |

---

# 📋 แผนรอบ 4 — ติ๊กเมื่อทำเสร็จ

- [ ] แยก `form-input` ออกจาก `web-craft` แล้วย้ายของเดิมที่เกี่ยวกับฟอร์มไปด้วย
- [ ] แยก `db-safety` ออกจาก `pharmacy-web-logic`
- [ ] ยกกอง A เข้าสกิล — `pharmacy-web-logic`/`db-safety` 30 เรื่อง
- [ ] ยกกอง A เข้าสกิล — `web-craft`/`form-input` 29 เรื่อง
- [ ] ยกกอง A เข้าสกิล — `working-with-gun` 16 เรื่อง
- [ ] ยกกอง A เข้าสกิลที่เหลือ 12 เรื่อง
- [x] ~~เติมกอง E~~ — **ยกเลิก** พี่กันสั่งตัดข้อยุบยิบทิ้งทั้งกอง 5 ก.ย. 2569
- [ ] วัดจำนวนบรรทัดทุกสกิลหลังจบ · เกิน 1,300 บรรทัดเมื่อไหร่ = ต้องแยกอีก
