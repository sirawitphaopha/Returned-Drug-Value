# มูลค่ายาคืน — session 6 ส.ค. 2569 · v0.4.1.0 → v0.4.2.0 (โดนัทหน้าสรุป · 10 อันดับเต็มกล่อง · Lot ในหน้าประวัติ)

ต่อจาก [SESSION-2026-08-05.md](SESSION-2026-08-05.md) (`2978cf0` v0.4.0.0)

## 1. ตั้งรหัสผ่านร่วม + เทสประตูล็อก

พี่กันตั้ง `MRV_PASSWORD` ทั้งบน Cloudflare และใน `.env.local` แล้ว

| ด่าน | ในเครื่อง | Cloudflare |
|---|---|---|
| เปิดหน้าแรก | เด้งไป `/login` | เด้งไป `/login` |
| ยิง API ตรง ๆ ไม่ล็อกอิน | `401 needAuth` | `401 needAuth` |
| รหัสผิด | ปฏิเสธ | — |
| รหัสถูก | เข้าได้ · โหลดยา 417 ตัว | — |
| ออกจากระบบ | ล้างคุกกี้จริง | — |

เทสด้วยสคริปต์ที่อ่านรหัสจาก `.env.local` เอง **รหัสไม่เคยผ่านตาแคลร์และไม่โผล่ในแชท**

**ยืนยันว่า Cloudflare ใช้โค้ด v0.4.0.0 แล้ว** — เช็คจากไฟล์ที่เพิ่งเพิ่ม
`/vendor/xlsx.full.min.js` เสิร์ฟได้ 951,904 ไบต์ เท่ากับในเครื่องเป๊ะ

⚠️ **รหัสตอนนี้เป็นของชั่วคราวไว้เทส พี่กันจะเปลี่ยนก่อนเปิดใช้จริง** (ROADMAP ข้อ 2.1)

## 2. ปุ่มดวงตาในหน้าล็อกอิน

พี่กันขอ *"เผื่อพิมพ์ผิด"* · วาดด้วย SVG ไม่ใช้ตัวอักษรพิเศษ

🚨 **ทำกลับด้านตอนแรก** — ใส่ขีดทับตอนที่กำลังโชว์รหัส (ไอคอนบอก "สิ่งที่จะเกิดถ้ากด")
พี่กันทักว่ากลับด้าน → เปลี่ยนเป็นไอคอนบอก **"สถานะตอนนี้"**

| สถานะ | ไอคอน |
|---|---|
| ซ่อนอยู่ | ตามีขีดทับ = มองไม่เห็น |
| เห็นรหัส | ตาปกติ = มองเห็นอยู่ |

## 3. ปุ่มออกจากระบบ

อยู่ที่ ตั้งค่า → ล่างสุด · มีป๊อปอัปยืนยัน 1 ชั้น

- โผล่เฉพาะตอนเว็บล็อกอยู่จริง — `/api/bootstrap` คืน `authOn` มาบอก
  ตอนรันในเครื่องที่ไม่ได้ตั้งรหัส ปุ่มไม่ขึ้นเลย เพราะกดไปก็ไม่มีความหมาย
- `DELETE /api/auth` ล้างคุกกี้ แล้วพาไป `/login`

## 4. กล่อง "Lot นี้" แทนช่องว่างในแผงขวา

พี่กันทักว่าแผงขวามีช่องว่างใต้ผู้บันทึก → ทำมอคอัป 3 แบบให้เลือก
พี่กันเลือก **แบบ ก (สรุป Lot นี้)** และตัดแบบ ข ทิ้งเพราะ *"รายการยามีฝั่งซ้ายอยู่แล้ว"*

### บั๊ก/ปัญหาที่เจอระหว่างทาง

**ก. กล่องไม่ยอมยุบตอนจอเตี้ย** — ใส่ `flex:1` = `1 1 0%` ฐานความสูงเป็นศูนย์
การคำนวณการหดเลยได้ศูนย์ตาม กล่องดันปุ่มบันทึกตกขอบ → เปลี่ยนเป็น `flex:1 1 auto`

**ข. แผงแน่นมาตั้งแต่เดิม** — วัดได้ว่าก่อนเพิ่มอะไร แผงกิน **502px ในพื้นที่ 503px**
รีดระยะห่าง: padding 18/20→14/16 · gap 16→11 · ตัวเลขใหญ่ 22→20px

**ค. ปุ่มบันทึกยังตกขอบที่จอ 640 + แถบเดโม** → ตรึงด้วย `position:sticky;bottom:-14px`

### สิ่งที่พี่กันทักเพิ่มหลังเห็นของจริง

1. **เดโมต้องมีเลข Lot ด้วย** → `enterDemo` ใส่ `lastLot` จากข้อมูลตัวอย่าง
2. **"414 หน่วย" หน่วยอะไร** → ถูกต้อง เอาเม็ดไปบวกขวดบวกหลอด ไม่มีความหมาย
   เปลี่ยนเป็นแยกตามหน่วยจริง `396 เม็ด · 14 แคปซูล · 2 กระบอก · และอีก 1`
3. **"เอาคำว่า Lot ไม่ทับศัพท์"** → แคลร์อ่านผิดรอบแรก ไปแปลเป็น "ชุด"
   พี่กันแก้ให้ว่าหมายถึง **เขียน `Lot` ด้วยตัวอักษรอังกฤษ** ไม่ใช่แปล → แก้ทั้งเว็บแล้ว

## 5. ผลเทส (Chrome จริง)

| เช็คอะไร | ผล |
|---|---|
| ปุ่มดวงตา สลับ ซ่อน/เห็น | ✅ `password ↔ text` · ไอคอนตรงกับสถานะ |
| ปุ่มอยู่ในกรอบ ไม่ทับตัวอักษร | ✅ เว้นที่ 46px |
| ประตูล็อก 6 ด่าน | ✅ ผ่านหมด ทั้งในเครื่องและ Cloudflare |
| กล่อง Lot นี้ ตอนว่าง | ✅ ขึ้นข้อความชวนแทนกล่องเปล่า |
| กล่อง Lot นี้ ตอนมี 14 รายการ | ✅ 14 ตัว · ใช้ต่อ 11 · ทำลาย 3 · แยกหน่วยถูก |
| เลข Lot ในโหมดตัวอย่าง | ✅ `L690806-04` |
| ปุ่มบันทึก จอ 700 | ✅ เห็นครบ |
| ปุ่มบันทึก จอ 640 | ✅ เห็นตลอด |
| ปุ่มบันทึก จอ 640 + แถบเดโม | ✅ เห็นตลอด (เดิมตกขอบ) |
| เหลือคำทับศัพท์ไหม | ✅ ไม่มี ทั้งหน้าบันทึกและหน้าประวัติ |

**ยังไม่ได้เทส:** ปุ่มออกจากระบบบนหน้าจอจริง (ต้องล็อกอินก่อน ซึ่งแคลร์ไม่ควรใช้รหัสพี่กัน)
เทสผ่าน API ครบแล้วว่าล้างคุกกี้จริง

## 6. ค้างอยู่

- เปลี่ยนรหัสผ่านก่อนเปิดใช้จริง (ROADMAP ข้อ 2.1)
- กดเลือกราคายา 96 ตัว + พิมพ์ราคาเอง 66 ตัว
- เทสบนมือถือจริง

## 7. 🚨 บั๊กที่พี่กันจับได้ที่จอ 768 (แก้แล้ว)

*"เราว่า หน้าจอ 768 มันมีปัญหา ลองเปิดดู เเละใส่ข้อมูลเดโม่ เธอจะเห้น"*

**อาการ:** กล่อง "Lot นี้" ถูกบีบจนเหลือ 23px แล้วซ่อนบรรทัดแยกหน่วยนับไว้ข้างใน
กลายเป็นกล่องจิ๋วที่มีแถบเลื่อนเล็ก ๆ อ่านไม่ได้ ดูเหมือนเว็บพัง — ทั้งที่แผงยังมีที่ว่างเหลือ

**วิธีหา:** วัดพื้นที่เลื่อนทุกอันในหน้า แล้วเจอตัวแปลกปลอม
`{ box: "411→434", w: 238, ซ่อนอยู่: 34 }` = กล่องกว้าง 238 สูง 23 แต่ซ่อนเนื้อหาไว้ 34px

**ต้นเหตุ:** แคลร์ใส่ `overflow-y:auto` + `min-height:0` ไว้ในกล่องเพื่อให้ยุบได้ตอนที่ว่างไม่พอ
เจตนาดีแต่ผลตรงกันข้าม — มันยุบจนเนื้อหาหายแทนที่จะบอกว่าไม่พอ

**แก้:** เอาการยุบออก ให้กล่องสูงเท่าเนื้อหาเสมอ ถ้าที่ไม่พอค่อยให้ "ทั้งแผง" เลื่อนแทน
ปลอดภัยแล้วเพราะปุ่มบันทึกถูกตรึงก้นแผงด้วย `position:sticky`

**รีดพื้นที่เพิ่มให้พอดีจอ 768**
- แยกหน่วยนับเหลือ 2 ตัว `396 เม็ด · 14 แคปซูล · และอีก 2` (3 ตัวตกบรรทัดที่สอง)
- `ใช้ต่อได้ 22% ของมูลค่าที่คืนมา 1,603.12 ฿` → `ใช้ต่อได้ 22% จาก 1,603.12 ฿`

| สถานการณ์ | แผงเลื่อน | ปุ่มบันทึก | กล่องเลื่อนจิ๋ว |
|---|---|---|---|
| 768 ใช้งานปกติ | ไม่เลื่อนเลย | เห็น | ไม่มี |
| 768 + โหมดตัวอย่าง | 34px (แถบเตือนกิน 35px) | เห็น | ไม่มี |
| 640 + โหมดตัวอย่าง | 162px | เห็นตลอด | ไม่มี |

**บทเรียน:** เวลาทำของให้ "ยุบได้ตอนที่ว่างไม่พอ" ต้องเช็คด้วยว่ายุบแล้ว **เนื้อหายังอ่านได้ไหม**
ไม่ใช่แค่ว่าไม่ล้น · การซ่อนเนื้อหาไว้ในกล่องจิ๋วแย่กว่าปล่อยให้ทั้งแผงเลื่อน

## 8. หน้าสรุป — โดนัทสัดส่วนแหล่งที่มา (v0.4.2.0)

พี่กันทักว่ากล่องซ้าย (กราฟรายเดือน) เหลือที่ว่างด้านล่างเยอะ ขอมอคอัปมาเลือก
เสนอ 3 แบบ: โดนัทใช้ต่อ/ทำลาย · กราฟเหตุผลการทำลาย · ย้ายโดนัทแหล่งที่มามาซ้าย

**พี่กันเลือกแบบ ค (ย้ายโดนัทแหล่งที่มา)** และปฏิเสธแบบ ข ด้วยเหตุผลที่ดีมาก
*"ไม่รู้ว่าคนใช้จะกรอกเรื่องทำลายครบไหม"* — ช่องเหตุผลไม่ได้บังคับกรอก
ถ้ากรอกไม่ครบ กราฟจะบอกความจริงครึ่งเดียว **ซึ่งอันตรายกว่าไม่มีกราฟ**
→ เก็บลง ROADMAP หัวข้อ "เสนอแล้ว พี่กันบอกให้เก็บไว้ก่อน" ข้อ 0

### ที่ทำ

- ย้าย "สัดส่วนตามแหล่งที่มา" จากกล่องขวา → กล่องซ้าย แล้วทำเป็นโดนัท
- ลบของเดิมออกจากกล่องขวา ไม่ให้ซ้ำ

**เคล็ดลับการวาดโดนัทด้วย SVG** — ตั้งรัศมี `15.9` จะได้เส้นรอบวง ≈ 100 พอดี
ใส่เปอร์เซ็นต์ลง `stroke-dasharray` ได้ตรง ๆ ไม่ต้องคูณหาร
`stroke-dashoffset` เริ่มที่ 25 เพื่อให้ชิ้นแรกเริ่มที่ตำแหน่ง 12 นาฬิกา
คำนวณใน `vals/summary.js` (`dash` / `dashOffset`) ตามสัญญาโครงสร้าง — pages วาดอย่างเดียว

### ผลเทส (จอ 1366×768 พร้อมข้อมูลตัวอย่าง)

| เช็คอะไร | ผล |
|---|---|
| กล่องซ้าย–ขวาสูงต่างกัน | **0px** เท่ากันเป๊ะ (446px ทั้งคู่) |
| โดนัทครบ 5 แหล่งที่มา | ครบ |
| รวมเปอร์เซ็นต์ | 100% ไม่ขาดไม่เกิน |
| สัดส่วนซ้ำในกล่องขวาไหม | ไม่ซ้ำ |
| ไล่สีเขียวเข้ม→อ่อน | `#2f7d5d → #a8d3bd` |

กันไว้ด้วย: ยังไม่มีข้อมูลเลย → วาดวงเทาว่าง ๆ แทนวงบิดเบี้ยว (`srcEmpty`)

## 9. หน้าประวัติ — แสดง Lot + แบ่งกลุ่มด้วยสีจาง

พี่กันถาม *"หน้าประวัติ แสดง lot รึยัง เพราะถ้ามี lot มันจะเรียงกันยังไง"*

**ตรวจแล้วพบว่า:** หน้าคอม **ไม่เคยแสดง Lot เลย** — `lotLabel` ถูกคำนวณไว้ใน
`vals/history.js` แต่ไม่มีใครวาด · ฝั่งมือถือแสดงแต่ยัดรวมในบรรทัดรายละเอียดจนอ่านยาก

**การเรียงไม่ต้องแก้อะไร** — `mr_history` เรียง `return_date desc, id desc`
แถวใน Lot เดียวกันบันทึกพร้อมกัน จึงติดกันเองอยู่แล้วโดยธรรมชาติ

### ทำมอคอัป 3 แบบให้เลือก

ก. แถบสีจางอย่างเดียว · ข. สีจาง + คอลัมน์ Lot · ค. แถวหัวคั่นแต่ละ Lot
**พี่กันเลือกแบบ ข** และสั่งว่าไม่ต้องตัดคอลัมน์ HN ออก

### ที่ทำ

| | คอม | มือถือ |
|---|---|---|
| แสดง Lot | คอลัมน์ที่ 10 ท้ายตาราง | ป้ายเขียวเล็กข้างสถานะ |
| แบ่งกลุ่ม | พื้นสลับ ขาว ↔ `#f4faf7` | แถบสีซ้าย 3px เข้ม ↔ จาง |
| กดได้ | กดเลข = กรองดูเฉพาะ Lot นั้น | กดป้าย = เหมือนกัน |

🚨 **สีจางทำงานเฉพาะตอนเรียงตามวันที่**
ถ้ากดเรียงตามราคา/ชื่อยา แถวจาก Lot ต่าง ๆ จะสลับกันมั่ว
สีจะกลายเป็นลายพร้อยที่ **หลอกตาว่าเป็นกลุ่มทั้งที่ไม่ใช่** → ปิดอัตโนมัติ

**แถว "ทำลาย" คงสีแดงจางไว้เหมือนเดิม** เพราะเป็นสัญญาณที่สำคัญกว่าการจับกลุ่ม

**ฝั่งมือถือใช้แถบซ้ายแทนพื้นสี** เพราะการ์ดมีพื้นขาวกับกรอบอยู่แล้ว เปลี่ยนพื้นอีกจะเลอะ
และดึงเลข Lot ออกจากบรรทัดรายละเอียด (เดิม `60 เม็ด × 0.45 · HN 6418302 · L690806-03`
ยาวจนอ่านยาก) เหลือ `60 เม็ด × 0.45 · HN 6418302`

### สิ่งที่ปลดล็อกไปด้วย

ช่องค้นหารับ "เลข Lot" กับปุ่มกรอง Lot มีมาตั้งแต่แรก แต่หน้าคอมไม่เคยโชว์เลข
**คนใช้จึงไม่มีทางรู้ว่าเลขคืออะไร กลายเป็นฟีเจอร์ที่มีแต่ใช้ไม่ได้** — ตอนนี้ใช้ได้จริง

### ผลเทส

**คอม (1366×768 + ข้อมูลตัวอย่าง 59 แถว)**

```
L690806-04  ทำลาย      แดงจาง
L690806-04  ใช้ต่อได้  ขาว      ┐ กลุ่มเดียวกัน
L690806-04  ใช้ต่อได้  ขาว      ┘
L690806-03  ใช้ต่อได้  เขียวจาง ┐ สลับกลุ่ม
L690806-03  ใช้ต่อได้  เขียวจาง ┘
L690806-02  ใช้ต่อได้  ขาว      ┐ สลับกลับ
L690806-02  ทำลาย      แดงจาง   ┘
```

หัวตารางครบ 10 คอลัมน์: วันที่ · ยา · จำนวน · ราคา/หน่วย · มูลค่า · สถานะ · แหล่งที่มา · HN · ผู้บันทึก · Lot

**มือถือ (375×812)** — การ์ด 59 ใบ · Lot เดียวกันแถบซ้ายสีเดียวกัน · ขึ้น Lot ใหม่สลับสี
`rgb(168,211,189)` ↔ `rgb(47,125,93)`
