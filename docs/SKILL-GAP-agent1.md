# ผลกวาดหากฎที่ยังไม่ขึ้นสกิลกลาง — เอเจนต์ 1 (CLAUDE.md + session ME-DRP)

> ตรวจ 5 ก.ย. 2569 · ✅ ตรวจครบทุกไฟล์แล้ว (4 CLAUDE.md + 34 SESSION) · เจอ 108 รายการ

**อ่านสกิลกลางครบ 11 ตัวแล้วก่อนเริ่มตรวจ** — working-with-gun (943 บรรทัด) · SCOLDING-LOG ·
pharmacy-web-logic (1,045) · drug-display (327) · search-system (567) · web-craft (359) ·
medical-data-safety (151) · button-feedback (386) · loading-skeleton (190) ·
mobile-web-app (598) · audit-report (213) · making-documents (233)

**เกณฑ์ที่ใช้** — ถามคำถามเดียวทุกแถว: "เว็บอื่นเจอเรื่องนี้ได้ไหม"
ทุกแถวยกข้อความจริงจากเอกสารมา ไม่สรุปเป็นคำพูดตัวเอง

---

## 1) `C:\Users\PKH\ME-DRP-fresh\CLAUDE.md` (600 บรรทัด)

ไฟล์นี้เป็นแหล่งกฎที่รั่วมากที่สุด — เจอ **32 เรื่อง** ที่ใช้ได้ทุกเว็บแต่ยังอยู่ที่นี่ที่เดียว

| # | กฎ/บทเรียนที่เจอ | ยกข้อความจริงมา | ควรไปสกิลไหน | ทำไมถึงควรขึ้นสกิล |
|---|---|---|---|---|
| 1 | 🔴🔴🔴 **heredoc กลืนแบ็กสแลช — และสกิลกลางกลับแนะนำให้ใช้ heredoc** | *"บทเรียนรอบนี้: heredoc กลืนแบ็กสแลช — เขียนไฟล์ผ่าน bash heredoc แล้ว `/-?\d+/` กลายเป็น `/-?d+/` **หาตัวเลขไม่เจอเลย** · typecheck ผ่าน · build ผ่าน · ตรรกะพังสนิท จับได้จากการอ่านโค้ดเท่านั้น → **แก้ไฟล์ที่มีนิพจน์ค้นหา ให้เขียนสคริปต์ด้วย Write แทน heredoc เสมอ**"* | `working-with-gun` ข้อ 5 (ไฟล์+เครื่องมือ) | **ขัดกับสกิลกลางตรง ๆ** — ตอนนี้สกิลเขียนว่า *"วิธีปลอดภัย: ... เขียนบรรทัดใหม่ด้วย heredoc ลงไฟล์ชั่วคราว"* คือแนะนำวิธีที่ ME-DRP พิสูจน์แล้วว่าพัง · ใครอ่านสกิลอย่างเดียวจะเดินเข้ากับดักนี้ทุกโปรเจกต์ |
| 2 | 🔴 **ดึงข้อมูลได้ไม่เกิน 1,000 แถวแบบเงียบ ๆ** | *"**ทุกเส้นที่ข้อมูลโตได้ต้องวนดึงทีละ 1,000 แถว** (`ดึงครบทุกแถว` ใน `supabaseAdmin.ts`) — PostgREST ตัดที่ 1,000 แถว **แบบเงียบ ๆ ไม่มี error**"* | `pharmacy-web-logic` (ฐานข้อมูล) | ทุกเว็บที่ใช้ Supabase เจอเหมือนกัน · ตัวเลขจะเริ่มผิดเงียบ ๆ ตอนข้อมูลโต · สกิลไม่มีคำว่า PostgREST หรือ 1,000 แถวเลยสักที่ (มีแต่ใน `audit-report` ที่ยกเป็น**ตัวอย่างวิธีเขียนหัวข้อ** ไม่ใช่กฎ) |
| 3 | 🔴 **ลำดับปล่อยงานที่แตะฐาน — ห้ามสลับ** | *"push → รอ deploy → ตรวจเว็บจริงว่าใช้งานได้จริง → ค่อยปิดประตูฐาน"* · *"แคลร์รัน SQL ถอนสิทธิ์ตอนที่โค้ดใหม่ยังไม่ขึ้นเว็บจริง → เว็บที่รันรุ่นเก่าคุยกับฐานไม่ได้ทันที"* (เว็บจริงล่ม 40 นาที) | `working-with-gun` ข้อ 4 (push) | สกิลมีแค่ *"ปิด RLS เป็นขั้นสุดท้ายเสมอ"* ซึ่งไม่ได้บอกว่า "สุดท้าย" คือหลัง deploy เสร็จและตรวจเว็บจริงแล้ว · เว็บไหนก็ทำพังแบบนี้ได้ |
| 4 | 🔴 **ตัวแปรที่เพิ่งเพิ่มบน Cloudflare ไม่มีผลจนกว่าจะ deploy ใหม่ + สองที่ต้องใส่แยกกัน** | *"**ตัวแปรที่เพิ่งเพิ่มบน Cloudflare ไม่มีผลจนกว่าจะ deploy ใหม่** (Deployments → Retry build)"* · *"**`.env.local` กับ Cloudflare เป็นคนละที่ ต้องใส่แยกกัน** — บอกพี่กันให้ชัดทุกครั้งว่า 'ทั้งสองที่'"* | `working-with-gun` ข้อ 4 | สกิลบอกแค่ *"ก่อน push ต้องเตือนพี่กัน 2 เรื่องเสมอ ตั้งตัวแปรลับบน production (Cloudflare) แล้วหรือยัง"* แต่ไม่บอกว่าตั้งแล้วยังไม่มีผล และไม่บอกว่ามีสองที่ · ทุกเว็บที่ deploy บน Cloudflare เจอ |
| 5 | 🔴 **บันทึกทับสถานะถังขยะเงียบ ๆ** | *"**สถานะถังขยะเปลี่ยนได้ทางเดียว คือปุ่มลบกับปุ่มกู้คืน** ... เดิม: เครื่อง A เปิดเคสค้าง → เครื่อง B ลบลงถังขยะ → เครื่อง A กดบันทึก = เคสเด้งกลับออกจากถังขยะเงียบ ๆ เพราะภาพถ่ายเก่าถือค่าว่างของช่องนี้อยู่"* | `pharmacy-web-logic` ข้อ 19 (ถังขยะ) | สกิลมีแค่วิธี soft delete (`deleted_at`) ไม่มีกฎว่า **ทางเขียนต้องตัดช่องนี้ทิ้งเสมอ** · เว็บไหนที่มีถังขยะ + ใช้หลายเครื่องพร้อมกัน เจอแน่นอน |
| 6 | 🔴 **ตัวลบต้องตอบว่าโดนจริงกี่แถว** | *"**ตัวลบต้องตอบว่าโดนจริงกี่แถว ไม่ใช่แค่ว่าไม่มีข้อผิดพลาด** ... ไม่ขึ้น 'ย้ายไปถังขยะแล้ว' ทั้งที่ถังขยะอ่านจากฐานอย่างเดียว"* | `pharmacy-web-logic` ข้อ 19 | ตระกูลเดียวกับ "บันทึกไม่เข้าแต่ขึ้นว่าบันทึกแล้ว" ที่สกิลถือว่าร้ายแรงที่สุด แต่ฝั่ง **ลบ** ยังไม่มีกฎนี้ |
| 7 | 🔴 **หน้าแก้ไขใช้ตัวควบคุมคนละชนิดกับหน้ากรอก → ข้อมูลหาย** | *"หน้าแก้ไข error_type/error_nature เป็นปุ่มเลือกหลายอัน ... เดิมเป็น `<select>` อันเดียว → **array หด**"* | `web-craft` (ฟอร์ม) | เป็นการสูญข้อมูลเงียบ ๆ ที่เกิดกับทุกเว็บที่มีทั้งหน้ากรอกและหน้าแก้ไข · กฎกลางควรเป็น "หน้าแก้ไขต้องใช้ช่องชุดเดียวกับหน้ากรอกเสมอ" |
| 8 | 🔴 **ช่องบังคับที่ไม่มีช่องให้กรอกในหน้าแก้ไข = กดบันทึกไม่ได้ตลอดกาล** | *"ช่อง 'ระบุลักษณะอื่น ๆ' ในหน้าต่างแก้ไข — เคยขาดไปทั้งหน้าต่าง ตัวตรวจช่องบังคับสั่งให้ระบุ แต่ไม่มีช่องให้กรอก = ติ๊ก 'อื่น ๆ' แล้วกดบันทึกไม่ได้ตลอดกาล"* | `web-craft` | อาการที่ผู้ใช้แก้เองไม่ได้เลย · ทุกเว็บที่มีตัวตรวจช่องบังคับสองหน้าจอเสี่ยงเหมือนกัน |
| 9 | 🔴 **ข้อมูลสดข้ามเครื่องห้ามทับสิ่งที่ผู้ใช้กำลังแก้อยู่** | *"`refreshRecords` ไม่ทับ detail ตอน `editMode`"* | `web-craft` (realtime) · `pharmacy-web-logic` | สกิลสั่งให้ทำ realtime เป็น "พื้นฐานของเว็บเรา" แต่ไม่มีกฎกันกรณีที่ข้อมูลใหม่วิ่งมาทับฟอร์มที่คนกำลังพิมพ์ |
| 10 | 🔴 **สลับประเภทฟอร์ม — ช่องที่ปลายทางไม่มีต้องล้างทิ้งจริง ห้ามซ่อนไว้** | *"ช่องที่ประเภทปลายทางไม่มี ... **ล้างทิ้งจริง ไม่ซ่อนไว้** เพราะคอลัมน์พวกนี้อยู่ใน `COLS` ค่าค้างจะถูกบันทึกลง DB ไปด้วย"* · *"เปลี่ยน intervention ไปค่าอื่น → ล้าง `outcome` ทิ้ง (กันค่าค้างติดเคส)"* | `web-craft` (ฟอร์ม) | "ช่องที่ซ่อนแต่ยังมีค่า" ถูกบันทึกลงฐานโดยไม่มีใครรู้ = ข้อมูลปลอมในฐาน · เกิดได้ทุกเว็บที่มีฟอร์มแบบมีเงื่อนไข |
| 11 | 🟠 **dropdown ต้องเริ่มที่ค่าว่าง ห้ามค้างค่าแรก** | *"ค่าเริ่มต้นว่าง '— เลือกการแก้ไข —' · **เดิมค้างค่าแรกไว้ทำให้บันทึกค่าที่ไม่ได้ตั้งใจ**"* | `web-craft` (ฟอร์ม) | ผู้ใช้ไม่ได้เลือก แต่ระบบบันทึกให้ = ข้อมูลผิดโดยไม่มีใครรู้ · ใช้กับทุกฟอร์มทุกเว็บ |
| 12 | 🟠 **ค่าว่างต้องแปลงเป็น `null` ก่อนส่งฐาน** | *"`toRow` แปลง `severity`/`outcome` ที่เป็นค่าว่าง เป็น `null` ก่อนส่ง (คอลัมน์มี CHECK constraint · ค่าว่างไม่ผ่าน → error 400 บันทึกไม่เข้า) — บั๊กนี้เคยทำให้ 'กรอกแล้วไม่เข้า Supabase'"* | `pharmacy-web-logic` | อาการ "บันทึกแล้วไม่เข้า" ที่สกิลถือว่าร้ายแรงที่สุด · ต้นเหตุตัวนี้ยังไม่ถูกจดไว้ที่ไหนเลย |
| 13 | 🔴 **ห้ามยัดก้อนข้อมูลจากเบราว์เซอร์ลงฐานตรง ๆ** | *"**ห้ามยัดก้อนข้อมูลจากเบราว์เซอร์ลงฐานตรง ๆ** ต้องผ่าน `toRow`/`drugRow` เสมอ (ทำหน้าที่เป็นรายการคอลัมน์ที่อนุญาตด้วย)"* | `pharmacy-web-logic` (ความปลอดภัย) | เป็นกฎกันการเขียนคอลัมน์ที่ไม่ควรให้เขียน · สกิลมีแค่ "เบราว์เซอร์ไม่ถือกุญแจฐาน" ยังไม่พอ เพราะหลังบ้านที่รับก้อนดิบก็อันตรายเท่ากัน |
| 14 | 🔴 **เส้นทางที่มีรหัสในที่อยู่ ต้องยึดรหัสจากเส้นทาง ไม่เชื่อรหัสในตัวข้อมูล** | *"`PATCH /api/incidents/[id]` **ยึด id จากเส้นทางเสมอ** ไม่เชื่อ id ที่ปนมาในตัวข้อมูล ไม่งั้นคำขอเดียวแก้รายงานใบอื่นได้"* | `pharmacy-web-logic` (ความปลอดภัย) | ช่องโหว่แก้ข้อมูลข้ามคน · ทุกเว็บที่มีหลังบ้านของตัวเองเจอได้ |
| 15 | 🔴 **หน้าตรวจสุขภาพระบบห้ามคืนค่ากุญแจ** | *"`GET /api/health` (ต้องมีบัตรผ่าน) — บอกว่าตัวแปรแต่ละตัวตั้งไว้ไหม ยาวกี่ตัวอักษร ... **ห้ามให้คืนค่าของกุญแจเด็ดขาด** บอกได้แค่ 'มีไหม'"* | `pharmacy-web-logic` (ความปลอดภัย) | เป็นทั้งเครื่องมือไล่บั๊กที่ควรมีทุกเว็บ และเป็นข้อห้ามที่ถ้าทำพลาดคือกุญแจหลุด |
| 16 | 🔴 **`run_worker_first: true` — ไม่ตั้งแล้วมีทางเข้าที่ไม่ผ่านหน้าล็อกอิน** | *"`wrangler.jsonc` เพิ่ม **`run_worker_first: true`** ห้ามเอาออก — ค่าตั้งต้นของ Cloudflare คือ false ซึ่งแปลว่าตัวจ่ายไฟล์นิ่งทำงาน**ก่อน**โค้ดเรา = มีทางเข้าที่ไม่ผ่านหน้าล็อกอิน"* | `pharmacy-web-logic` (ประตูรหัสผ่าน) | สกิลมีเรื่อง "กั้น 2 ด่าน" แล้ว แต่ไม่มีข้อนี้ · เว็บที่ล็อกด้วยรหัสผ่านทุกตัวบน Cloudflare รั่วได้ทันทีถ้าไม่ตั้ง |
| 17 | 🟠 **ไม่ได้ตั้งรหัสผ่าน = ต้องพังแบบเห็นชัด + ห้ามมีทางลัดปลดล็อกตอนรันในเครื่อง** | *"**เว็บจริง (`NODE_ENV=production`) ล็อกเสมอ** ไม่ได้ตั้ง `MEDRP_PASSWORD` = ตอบ 503 ทั้งเว็บ (ตั้งใจให้พังแบบเห็นชัด ดีกว่าเปิดโล่งเงียบ ๆ)"* · *"**ห้ามทำทางลัดปลดล็อกตอนรันในเครื่อง** — เว็บยาคืนเคยมีแล้วถอดทิ้ง เพราะทำให้เครื่องทดสอบไม่เหมือนเว็บจริง"* | `pharmacy-web-logic` (ประตูรหัสผ่าน) | หลักการออกแบบความปลอดภัยที่ใช้ได้ทุกเว็บ · ข้อหลังอ้างถึงเว็บยาคืนอยู่แล้ว แต่กฎกลับไม่ได้อยู่ในสกิล |
| 18 | 🔴 **rAF และ `scrollIntoView` แบบนุ่ม ไม่ทำงานเมื่อแท็บถูกซ่อน** | *"`focusRow`/`scrollIntoView` ใช้ `setTimeout(...,0)` ไม่ใช่ `requestAnimationFrame` — rAF **ไม่ยิงเลย**เมื่อแท็บถูกซ่อน/ไม่ได้วาดจอ (เจอตอนเทสจริง: แถวใหม่เพิ่มได้แต่เคอร์เซอร์ไม่กระโดด)"* · *"`scrollIntoView` แบบ smooth ไม่ทำงานเมื่อแท็บถูกซ่อน/ไม่ได้วาดจอ (เคสเดียวกับ rAF) → มีตัวสำรอง: เช็คหลัง 600ms"* | `web-craft` (กับดักเบราว์เซอร์) | กับดักเบราว์เซอร์ล้วน ๆ ไม่ผูกกับเว็บไหน · สกิลไม่มีคำว่า `requestAnimationFrame` เลยสักตัว · อาการคือ "ทำงานตอนเทสในมือ แต่ไม่ทำงานตอนเทสอัตโนมัติ" ซึ่งไล่หายากมาก |
| 19 | 🔴 **`performance.getEntriesByType("resource")` = วิธีแยก "ไม่ได้เรียก" ออกจาก "เรียกแล้วพัง"** | *"**วิธีจับบั๊กแบบนี้: `performance.getEntriesByType(\"resource\")` ในหน้าเว็บจริง** — เห็นทุกคำขอที่เกิดขึ้นจริง (Network log ของเครื่องมือทดสอบจับ fetch ของ supabase-js ไม่ได้) · เห็นว่ามีแต่ `/rest/v1/drugs` ไม่มี `/rest/v1/incidents` → ชี้ชัดว่าไม่ได้เรียก ไม่ใช่เรียกแล้วพัง"* | `working-with-gun` ข้อ 3.6 (ไล่บั๊กเครื่องคนอื่น) | สกิลข้อ 3.6 สั่งให้ขอแท็บ Network เสมอ แต่ไม่ได้บอกว่า **แท็บ Network ของเครื่องมือทดสอบจับคำขอบางชนิดไม่ได้** และมีท่าสำรองที่ใช้ได้จริง |
| 20 | 🔴 **`stateRef.current` ยังไม่ commit ตอนถูกเรียกจาก mount effect → ข้ามงานเงียบ ๆ** | *"`loadRecords`/`loadTrash` อ่าน `stateRef.current.cfg` ตอนถูกเรียกจาก mount effect ผ่าน `setTimeout(...,0)` แต่ `setState({cfg})` ยัง commit ไม่เสร็จ → `isConfigured()` เป็น false → **ข้ามการ fetch เงียบ ๆ** (มี catch ครอบอีกชั้น เลยไม่มีอะไรฟ้อง) · เครื่องที่มีข้อมูลค้างใน localStorage ไม่เห็นอาการ แต่เครื่องใหม่/หน้าต่างไม่ระบุตัวตนเห็น 0 เคส"* | `web-craft` (React) | บั๊กที่ **เครื่องคนทำไม่เห็น แต่เครื่องผู้ใช้ใหม่เห็น** ซึ่งเป็นชนิดที่แพงที่สุด · แก้ด้วยการส่งค่าตรง ๆ แทนอ่านจาก ref |
| 21 | 🟠 **`const` ไม่ hoist — helper ต้องอยู่นอกคอมโพเนนต์** | *"helper พวกนี้ต้องอยู่นอกคอมโพเนนต์ — เคยวางไว้หลัง `return` แล้วเจอ `Cannot access 'makeStColorOf' before initialization` (const ไม่ hoist เหมือน function declaration)"* | `web-craft` (React) | กับดักภาษา ไม่ผูกกับเว็บไหน · เว็บพังทั้งหน้า |
| 22 | 🔴 **`useMemo` ห้ามใส่ state ทั้งก้อน + ตัวคำนวณห้ามอ่านค่าที่วิ่งอนิเมชัน** | *"`computeDashData` เคยอ่าน `S.kpiAnim` → ตัวเลขวิ่ง 112 รอบ = คำนวณใหม่ทั้งก้อน 112 รอบ (กวาดรายงาน 22 รอบ + สร้างตารางค้นหายา 417 ตัวใหม่ ≈ 414,000 ครั้งต่อการเข้าหน้า 1 ครั้ง)"* · *"**`useMemo` 3 จุด ... ห้ามใส่ `S` ทั้งก้อนในรายการที่เฝ้าดู** setState สร้าง object ใหม่ทุกครั้ง = ไม่ได้จำอะไรเลย"* | `web-craft` (ความเร็ว) | สกิลมีแค่ *"useMemo คำนวณหนัก"* ลอย ๆ · สองกับดักนี้ทำให้ useMemo ที่เขียนไว้ **ไม่ทำงานเลย** โดยไม่มีอะไรฟ้อง เกิดได้ทุกเว็บ React |
| 23 | 🟠 **ตัวช่วยที่เก็บผลไว้ใช้ซ้ำ ห้ามแก้ object ที่ได้คืนมา** | *"`lib/style.ts` `css()` เก็บผลไว้ใช้ซ้ำ (เรียกราว 720 จุด) **ห้ามแก้ object ที่ได้จาก `css()`**"* | `web-craft` | กฎคู่กับข้อ 22 · เว็บไหนที่ทำแคชแบบนี้ต้องรู้ ไม่งั้นแก้ที่เดียวเพี้ยนทั้งเว็บ |
| 24 | 🔴 **ตัวกรองต้องเทียบค่าจริง ห้ามยัดลงช่องค้นหาข้อความ** | *"**ตัวกรองต้องเทียบค่าจริง ห้ามส่งไปช่องค้นหา** — เดิมหน่วยงานต้นเหตุกับยาส่งเป็น `q` (ค้นข้อความ) ทำให้ผลปนกัน: 'แพทย์' ได้ 85 เคส (ปน 'ทันตแพทย์' + เคสที่มีคำนั้นในรายละเอียด/ชื่อผู้รายงาน) ทั้งที่จริง 57"* | `search-system` · `web-craft` | ตัวเลขบนจอผิดโดยไม่มีอะไรฟ้อง · ใช้ได้ทุกเว็บที่มีทั้งตัวกรองและช่องค้นหา |
| 25 | 🔴 **ตัวกรองกับกราฟต้องใช้กติกาเดียวกันเสมอ** | *"**ตัวกรองกับกราฟต้องใช้กติกาเดียวกันเสมอ** ไม่งั้นกดแท่งที่บอก 20 เคส แล้วเห็นแค่ 14"* | `web-craft` | หัวใจของหน้าสรุปทุกเว็บ · ผู้ใช้จับได้ทันทีและเสียความเชื่อถือทั้งหน้า |
| 26 | 🟠 **กดกราฟแล้วกรอง — ต้องสืบทอดตัวกรองเดิม · แถว 0 กดไม่ได้ · กราฟที่กรองไม่ได้ต้องกดไม่ได้** | *"`goRecords(patch)` ... **สืบทอดตัวกรอง Dashboard ไปด้วย ตัวเลขจึงตรงกับกราฟ**"* · *"แถวที่ count > 0 ห่อด้วย HDiv ... **แถว 0 กดไม่ได้**"* · *"**Heatmap เวร × วัน ตั้งใจให้กดไม่ได้** — ระบบไม่มีตัวกรองวันในสัปดาห์ ถ้ากรองแค่เวรจะได้ตัวเลขไม่ตรงกับช่องที่กด"* | `web-craft` | ชุดกฎที่ทำให้ "กดกราฟไปหน้ารายการ" ไม่โกหก · เว็บไหนที่มีหน้าสรุปควรได้ไปทั้งชุด |
| 27 | 🟠 **ค่าตั้งต้นของเครื่องนี้ — จำได้บางช่อง ห้ามจำช่องที่ต้องคิดใหม่ทุกครั้ง** | *"จำ **จุดที่พบ** กับ **ผู้รายงาน** ... **หน่วยงานต้นเหตุไม่อยู่ในนี้โดยตั้งใจ** ... ต้องเลือกใหม่ทุกเคส เพราะแต่ละเคสมาจากคนละวิชาชีพ **จำค่าไว้ = เดาแทนคนกรอก อันตรายกว่าการต้องกด**"* · *"**เปลี่ยนค่าที่หน้าตั้งค่าไม่ไปแตะฟอร์มที่กรอกค้างอยู่** มีผลกับใบถัดไปเท่านั้น"* | `web-craft` (ฟอร์ม) | เส้นแบ่ง "ช่องไหนจำได้ ช่องไหนห้ามจำ" เป็นการตัดสินใจเชิงความปลอดภัย ไม่ใช่ความสะดวก · ทุกเว็บที่กรอกซ้ำ ๆ ทุกวันต้องใช้ |
| 28 | 🟠 **เตือนว่าอาจกรอกซ้ำ — เตือนอย่างเดียว ห้ามบล็อก** | *"กุญแจ = HN + วันที่เกิดเหตุ · ใช้ตัวเดียวทั้ง 3 หน้า ... **พี่กันสั่งไม่เอาป๊อปตอนกรอก** — ใช้แถบอ่อนใต้ช่อง HN · ป้าย 'ซ้ำ N' ในตาราง ... **เตือนอย่างเดียว ห้ามบล็อก** — คนไข้คนเดียววันเดียวเจอความคลาดเคลื่อนคนละเรื่องได้จริง"* | `web-craft` · `pharmacy-web-logic` | เป็นคำสั่งของเจ้าของงานเรื่องรูปแบบการเตือน (ไม่เอาป๊อป · ต้องโชว์ HN ไม่ใช่แค่จำนวน) ซึ่งใช้กับทุกเว็บที่มีการกรอกซ้ำได้ |
| 29 | 🟠 **ค้นหาต้องกวาดเฉพาะช่องที่ผู้ใช้เห็น ห้ามกวาดทั้งก้อนข้อมูล** | *"ค้นหาใช้ `matchSearch` (whitelist ช่องที่เห็น) แทน `JSON.stringify` ทั้งก้อน"* | `search-system` | ค้นด้วยการแปลงทั้งก้อนเป็นข้อความทำให้เจอของที่ผู้ใช้มองไม่เห็น (รหัสภายใน · ธง true/false) แล้วผลลัพธ์อธิบายไม่ได้ · สกิลค้นหาไม่มีข้อนี้เลย |
| 30 | 🟠 **ย่อเฉพาะตอนแสดงในตาราง ค่าจริงต้องเต็มเสมอ** | *"**ย่อเฉพาะตอนแสดงในตาราง — DB/หน้ารายละเอียด/CSV/ค้นหา ยังเป็นค่าเต็ม**"* (`shortLocation`/`shortReporter`/`drpLabelEn`) | `web-craft` | ถ้าย่อที่ต้นทาง ค้นหาและไฟล์ส่งออกจะเพี้ยนตาม · เป็นกฎที่ใช้ได้ทุกตารางทุกเว็บ |
| 31 | 🟠 **ส่งออก CSV ต้องกันสูตรฝังในไฟล์** | *"ปุ่มส่งออก CSV (`exportDrugsCsv` · **กัน formula injection**)"* | `making-documents` · `pharmacy-web-logic` | ไฟล์ที่คนเปิดด้วย Excel รันสูตรที่ฝังมาได้ · เว็บไหนมีปุ่มส่งออกก็เสี่ยงเท่ากัน · ไม่มีคำนี้ในสกิลใดเลย |
| 32 | 🟠 **มอคอัปห้ามวางใน `public/`** | *"มอคอัปที่พี่กันเลือกเก็บไว้ที่ `docs/mockups/login-2026-09-02.html` (**ห้ามวางไว้ใน `public/`** คนนอกโหลดได้ — บทเรียนจากเว็บ HCV)"* | `web-craft` (มอคอัป) · `medical-data-safety` | สกิล `web-craft` สั่งให้ทำมอคอัปเป็นไฟล์ HTML เดี่ยวใน `mockups/` แต่**ไม่ได้บอกว่าห้ามอยู่ใน `public/`** · เว็บ HCV เคยมีข้อมูลผู้ป่วยจริงหลุดทางนี้มาแล้ว |

### เรื่องที่ตรวจแล้วว่า **มีในสกิลอยู่แล้ว** (ไม่ต้องยกขึ้น)

ยืนยันด้วย `grep -rn` ใน `C:\Users\PKH\.claude\skills\` ทุกข้อ

| เรื่องใน CLAUDE.md | อยู่ในสกิลไหนแล้ว |
|---|---|
| ห้ามเขียน `border-color` เดี่ยวใน hover (บั๊กขอบดำ) | `button-feedback` กฎข้อ 7 |
| `APP_LAST_UPDATE` ต้องอยู่ติดกับ `APP_VERSION` | `working-with-gun` ข้อ 4 |
| ห้ามรัน `npm run build` ขณะตัวรันในเครื่องเปิดอยู่ | `web-craft` ข้อ 6 |
| iPhone ซูมเองตอนแตะช่องกรอก → `font-size:16px` | `mobile-web-app` |
| ห้ามมี `overflow-x` ครอบตาราง (หัวตารางลอยไปโผล่กลางตาราง) | `web-craft` ข้อ 6.2 |
| ลูกศรเรียงต้องเป็น Roboto Mono + ลูกศรโกหก + `useMemo` ต้องมีตัวแปรการเรียง | `web-craft` ข้อ 6.3 |
| ห้ามใช้ `String(e)` กับข้อผิดพลาดของ Supabase · ช่อง `code` ห้ามปิดบัง | `pharmacy-web-logic` ข้อ 31 |
| ล็อก body scroll ตอนเปิดหน้าต่างซ้อน · คลิกนอกป๊อปไม่ปิด | `web-craft` ข้อ 6.4 · `working-with-gun` ข้อ 8 |
| 1 channel ต่อ 1 ตาราง · refetch ตอน visibility/online | `web-craft` ข้อ 6 · `pharmacy-web-logic` ข้อ 29 |
| ห้ามเปลี่ยนชื่อ `middleware.ts` เป็น `proxy.ts` | `web-craft` ข้อ 6 |
| ตัวย่อยาห้ามคิดเอง (127 ตัว) | `medical-data-safety` · `search-system` ข้อ 4 |
| ไฟล์ที่แชร์ข้ามเว็บ แก้ที่ไหนต้องก๊อปไปอีกที่ | `drug-display` · `pharmacy-web-logic` |
| เว็บไม่มีหลังบ้าน เปิด Observability แล้วบันทึกว่างเปล่า | `pharmacy-web-logic` ข้อ 31 |
| กั้น 2 ด่าน (middleware + requireAuth) · บัตรผ่านนับจากครั้งสุดท้ายที่ใช้ | `pharmacy-web-logic` ข้อ 24 |

---

## 2) `C:\Users\PKH\dm-remission\CLAUDE.md` (254 บรรทัด)

ไฟล์นี้ต่างจาก ME-DRP ตรงที่**เขียนเป็นหลักการมากกว่าเล่าเหตุการณ์** จึงยกขึ้นสกิลได้เกือบทั้งดุ้น
เจอ **14 เรื่อง** ที่ใช้ได้ทุกเว็บแต่ยังอยู่ที่นี่ที่เดียว

| # | กฎ/บทเรียนที่เจอ | ยกข้อความจริงมา | ควรไปสกิลไหน | ทำไมถึงควรขึ้นสกิล |
|---|---|---|---|---|
| 33 | 🔴🔴 **กฎ 5 ข้อของระบบคำแนะนำทางคลินิก** | *"1. ทุกคำแนะนำ**ต้องแสดงแหล่งอ้างอิง** ระบุชื่อแนวทาง หัวข้อ และหน้า · 2. ต้องอ่านออกชัดว่าเป็น**คำแนะนำ ไม่ใช่คำสั่ง** และการตัดสินใจเป็นของแพทย์ · 3. ต้อง**กดรับหรือปฏิเสธได้** ไม่ใช่แสดงเฉยๆ · 4. **ห้ามให้ระบบคำนวณขนาดยาขึ้นเอง** ใช้ได้เฉพาะตัวเลขที่ปรากฏในแนวทางตรงตัว · 5. สีของกล่องคำแนะนำต้องไม่ใช่สีเตือนภัย"* | `medical-data-safety` | เว็บ รพ. ตัวไหนก็มีวันที่ต้องขึ้นคำแนะนำให้บุคลากร · สกิลตอนนี้มีแค่ "ห้ามเดาข้อมูลยา" ยังไม่มีกฎว่า**แสดงคำแนะนำยังไงถึงจะไม่กลายเป็นคำสั่ง** ซึ่งเป็นเรื่องความรับผิดชอบทางวิชาชีพ |
| 34 | 🔴 **ระบบต้องเตือน ไม่ใช่ห้าม** | *"**ห้ามล็อกเกณฑ์คัดเข้าแบบตายตัว** แนวทางเปิดทางให้ดุลยพินิจของแพทย์ ระบบต้องเตือน ไม่ใช่ห้าม"* | `medical-data-safety` · `web-craft` | ตรงกันกับกฎ "เตือนอย่างเดียว ห้ามบล็อก" ที่ ME-DRP ตั้งไว้เรื่องเคสซ้ำ (ข้อ 28) — **สองโปรเจกต์ค้นพบกฎเดียวกันแยกกัน** แปลว่าเป็นกฎกลางชัด ๆ ที่ยังไม่มีใครยกขึ้น |
| 35 | 🔴 **กรอกไม่ครบต้องบันทึกได้** | *"**กรอกไม่ครบต้องบันทึกได้** โครงการนี้ยังไม่เริ่มดำเนินการจริง ทีมยังไม่ได้ตกลงวิธีทำงานกัน ถ้าบังคับกรอกทุกช่องแล้วหน้างานทำไม่ได้ **จะเลิกใช้ระบบ**"* | `web-craft` (ฟอร์ม) | เป็นเส้นแบ่งว่าเมื่อไหร่บังคับกรอก เมื่อไหร่ห้ามบังคับ · เว็บยาคืนเจอเรื่องเดียวกัน (เคยกั้นยาที่ยังไม่มีราคา แล้วต้องเลิกกั้นเพราะห้องยาบันทึกงานประจำวันไม่ได้) |
| 36 | 🔴 **`rls_enabled_no_policy` เป็นสถานะที่ตั้งใจ ห้ามไป "แก้"** | *"**เปิด RLS ทุกตารางและไม่ใส่ policy เลย** คือปฏิเสธทุกคำขอจากเบราว์เซอร์ ตัวตรวจของ Supabase จะขึ้น `rls_enabled_no_policy` ระดับ INFO **อันนี้คือสถานะที่ตั้งใจ ห้ามไปเพิ่ม policy ให้ anon**"* | `pharmacy-web-logic` (ความปลอดภัย) | 🚨 **นี่คือกับดักที่ Claude ตัวถัดไปจะเดินเข้าแน่นอน** — เห็นคำเตือนของ Supabase แล้วนึกว่าตั้งค่าผิด เลยไปเพิ่ม policy ให้ `anon` = เปิดประตูฐานคืนโดยตั้งใจดี · สกิลไม่มีคำนี้เลย |
| 37 | 🟠 **ฐานเดียวใช้หลายเว็บ ต้องแยกด้วยคำนำหน้าชื่อตาราง** | *"ซึ่ง**ใช้ร่วมกับเว็บอื่นของโรงพยาบาล** จึงต้องแยกด้วยคำนำหน้าชื่อตารางเสมอ"* (`dm_` · `mr_` · `wb_` · `incidents` · `drugs` `drug_audit` = ตารางกลาง) | `pharmacy-web-logic` (ฐานข้อมูล) | ทุกเว็บของพี่กันอยู่ในโปรเจกต์ Supabase เดียวกัน (46 ตารางจาก 6 เว็บ) · สกิลมีรายชื่อตารางแต่ไม่มี**กฎการตั้งชื่อ** · เว็บใหม่ตัวถัดไปตั้งชื่อชนกันเมื่อไหร่ก็แก้ยาก |
| 38 | 🟠 **ชื่อ Worker ใน `wrangler.jsonc` ต้องตรงเป๊ะ** | *"ค่า `name` ใน `wrangler.jsonc` ต้องตรงกับชื่อ Worker บน Cloudflare เป๊ะ **ไม่ตรง = ไปสร้างเว็บใหม่อีกอันแทนที่จะทับของเดิม**"* | `web-craft` (deploy) | พลาดแล้วได้เว็บซ้ำสองตัวโดยไม่มีอะไรเตือน แล้วคนเข้าลิงก์เก่าเห็นของเก่าตลอด · สกิลพูดถึง Cloudflare Workers แต่ไม่มีข้อนี้ |
| 39 | 🔴 **ทุกอย่างใน `public/` เสิร์ฟออกไปโดยไม่ผ่านการลงชื่อเข้าใช้** | *"**ทุกอย่างใน `public/` เสิร์ฟออกไปโดยไม่ผ่านการลงชื่อเข้าใช้** ... **ก่อนขึ้นใช้จริงต้องย้ายออกจาก `public/`** แล้วเสิร์ฟผ่านเส้นทางฝั่งเซิร์ฟเวอร์ที่ตรวจว่าลงชื่อเข้าใช้แล้วก่อนส่งไฟล์ เพราะเป็นเอกสารสงวนลิขสิทธิ์"* | `web-craft` · `medical-data-safety` | **เรื่องเดียวกับข้อ 32 ของ ME-DRP** — สองโปรเจกต์เขียนเตือนกันเอง แต่สกิลกลางไม่มี · HCV เคยมีข้อมูลผู้ป่วยจริงหลุดทางนี้แล้ว |
| 40 | 🟠 **เก็บข้อมูลส่วนบุคคลเท่าที่จำเป็น และตัดออกอัตโนมัติเมื่อส่งออก** | *"เก็บข้อมูลส่วนบุคคลเท่าที่จำเป็น และ**ตัดออกอัตโนมัติเมื่อส่งออกข้อมูล**"* | `medical-data-safety` | สกิลมีเรื่องปิดบัง HN ใน**บันทึกข้อผิดพลาด** แต่ไม่มีเรื่องปิดบังใน**ไฟล์ส่งออก** ซึ่งเป็นทางที่ข้อมูลออกนอกโรงพยาบาลได้ง่ายที่สุด |
| 41 | 🟠 **ทุกบันทึกติดผู้บันทึกอัตโนมัติ ผู้ใช้แก้ไม่ได้** | *"**ทุกบันทึกติดผู้บันทึกอัตโนมัติ** ชื่อ วิชาชีพ เลขใบประกอบ และเวลา **ผู้ใช้แก้ไขไม่ได้**"* · *"**ลายเซ็นผู้บันทึกแช่ไว้ในแถว** ไม่ใช่อ้างอิงตารางผู้ใช้"* | `pharmacy-web-logic` | สกิลมีเรื่องแช่ข้อมูลในแถว (ข้อ 9) แต่ไม่มีกฎว่า**ลายเซ็นต้องแก้ไม่ได้** ซึ่งเป็นหัวใจของการสืบกลับ |
| 42 | 🔴 **ห้ามเขียนรหัสสีลงในไฟล์ส่วนประกอบ** | *"**ห้ามเขียนรหัสสีลงในไฟล์ส่วนประกอบ** ใช้ชื่อสีของระบบเสมอ เช่น `bg-surface` `text-ink-mute` `border-line` เพราะโหมดมืดทำงานผ่านตัวแปรเหล่านี้ **ถ้าเขียนรหัสสีตรงๆ โหมดมืดจะพัง**"* | `web-craft` | สกิล `drug-display` เจอปัญหานี้ของจริงแล้ว (*"ชื่อยาหายไปทั้งบรรทัด เพราะสีเกือบดำกลืนพื้นมืด"*) แต่แก้เป็นราย ๆ ไป · กฎกันไว้แต่ต้นตัวนี้ยังไม่มีในสกิลไหนเลย |
| 43 | 🟠 **ทาสีทับพื้นที่มีสีอยู่แล้ว ห้ามใช้สีโปร่งบนตัวเดียวกัน** | *"จะทาสีทับพื้นที่มีสีอยู่แล้ว ห้ามใช้ `bg-สี/เปอร์เซ็นต์` บนตัวเดียวกัน เพราะมัน **แทนที่** พื้นเดิม ไม่ได้ซ้อนทับ **ผลคือได้สีจางกว่าเดิม** ให้คำนวณสีปลายทางเป็นสีทึบแล้วเก็บเป็นตัวแปรของระบบแทน"* | `web-craft` · `button-feedback` | กับดัก CSS ที่ทำให้ "ชี้แล้วสีจางลงแทนที่จะเข้มขึ้น" ซึ่งเป็นความผิดข้อ 1 ในตารางความผิดของ `button-feedback` พอดี แต่ยังไม่มีใครบันทึก**ต้นเหตุ** |
| 44 | 🟠 **ตัวเลขสำคัญต้องใช้ตัววาดกลาง เพื่อให้เรียงหลักตรงกัน** | *"ตัวเลขสำคัญใช้ `<StatValue>` ไม่เขียนเอง **เพราะต้องใช้ฟอนต์มีเชิงและเรียงหลักตรงกัน**"* | `web-craft` | ตัวเลขที่หลักไม่ตรงกันในคอลัมน์เดียวคือสิ่งที่ตาจับได้ทันทีว่า "ไม่เนี้ยบ" · เกี่ยวข้องกับกฎ Roboto Mono ที่มีอยู่แล้ว แต่ยังไม่มีกฎว่า **ตัวเลขต้องเรียงหลัก** |
| 45 | 🟠 **ฉากสามมิติ/ผืนผ้าใบต้องรอเบราว์เซอร์จัดวางเสร็จหนึ่งรอบก่อนสร้าง** | *"เคยเจอปัญหาฉากไม่ขึ้นเลยที่หน้าเข้าสู่ระบบ ต้นเหตุคือ**ฉากถูกสร้างก่อนที่กล่องครอบจะมีขนาดจริง จึงจับขนาดได้เป็นศูนย์แล้วไม่วาดใหม่อีก** ทางแก้คือรอให้เบราว์เซอร์จัดวางเสร็จหนึ่งรอบก่อนค่อยสร้างฉาก"* | `web-craft` | คู่กับบทเรียน three.js ของ ME-DRP (*"ช่วงที่กล้องมองเห็นแคบกว่าที่คิดมาก · วัดด้วย `.project(cam)` อย่ากะเอา"*) — **สองเว็บเจอเรื่องฉาก 3 มิติคนละแง่ ทั้งคู่ไม่ได้ขึ้นสกิล** |
| 46 | 🔵 **ตัวบล็อก push ด้วยเครื่อง — วิธีทำงานที่มีอยู่ที่เดียว** | *"🔒 **การ push มีตัวบล็อกด้วยเครื่อง ไม่มีตั๋วจะ push ไม่ได้** และจบงานไม่ได้ถ้า FLOW การ push ยังไม่ครบ 4 ขั้น (ขั้นสุดท้ายคือไฟล์ session) ขอตั๋วด้วย `node .claude/hooks/allow-push.mjs` เฉพาะตอนพี่กันสั่งให้ push ตรงๆ"* · *"**ไฟล์นี้คือบริบทของโปรเจกต์ ไม่ใช่กฎ** กฎการทำงานทั้งหมดอยู่ที่ `RULES.md` ที่เดียว ระบบเด้งให้อัตโนมัติทุกครั้งที่เปิดงาน ผ่าน `.claude/hooks/session-start.mjs` **กฎใหม่ทุกข้อเขียนลงที่นั่น ห้ามกระจายมาไว้ที่นี่**"* | `working-with-gun` ข้อ 4 (push) | 🔑 **ตรงกับปัญหาที่พี่กันเพิ่งจับได้พอดี** — โปรเจกต์นี้แก้ปัญหา "จดกฎผิดที่" กับ "push โดยไม่ได้สั่ง" ด้วย**กลไกของเครื่อง** ไม่ใช่ด้วยการเตือนตัวเอง · เว็บอื่นทั้งหมดยังไม่มี และกลไกนี้ไม่เคยถูกจดลงสกิลเลย |

### เรื่องที่ตรวจแล้วว่า **มีในสกิลอยู่แล้ว**

| เรื่องใน CLAUDE.md | อยู่ในสกิลไหนแล้ว |
|---|---|
| ห้ามใช้ `?` ในข้อความบนหน้าจอ · ภาษาทางการ · ไม่ทับศัพท์ | `working-with-gun` ข้อ 2 |
| หน่วยวัดเป็นอังกฤษ · ตัวย่อค่าแล็บ · หัวการ์ดใช้ชื่อเต็ม (วงเล็บ) | `working-with-gun` ข้อ 2 (ยกมาทั้งดุ้น) |
| ห้ามใช้กล่องแจ้งเตือนของเบราว์เซอร์ · ย้อนยากต้องมีหน้าต่างยืนยัน | `working-with-gun` ข้อ 2 · กฎทอง 9 |
| ของที่คล้ายกันรวมเป็นส่วนประกอบกลางตัวเดียว | `working-with-gun` กฎทอง 10 |
| ห้ามรวม/ย่อ/ตัดเกณฑ์ทางการแพทย์เอง (Stage 4+5) | `medical-data-safety` |
| ห้ามสร้างข้อมูลทางการแพทย์ขึ้นเอง รวมถึงตัวอย่างในเอกสาร | `medical-data-safety` |
| โทนสีไล่ต่อเนื่อง เขียว เหลือง ส้ม แดง ห้ามข้ามขั้น | `web-craft` ข้อ 6.6 (3) |
| ทุกเกณฑ์ต้องเขียนกำกับว่ามาจากไหน | `web-craft` ข้อ 6.6 (3) |
| วันที่ส่งจากเบราว์เซอร์ ห้ามใช้ `now()` ของฐาน | `pharmacy-web-logic` ข้อ 23 |
| ลบคือประทับ `deleted_at` · ทุกคำสั่งอ่านกรอง `deleted_at is null` | `pharmacy-web-logic` ข้อ 19 |
| ยาเก็บทั้ง `drug_id` และ `drug_name` | `pharmacy-web-logic` ข้อ 1 · 9 |
| ห้ามใส่ `NEXT_PUBLIC_` นำหน้ากุญแจ | `pharmacy-web-logic` ข้อ 24 |
| push ขึ้น GitHub ≠ เว็บจริงอัปเดต | `working-with-gun` ข้อ 7 |
| ห้าม rename `middleware.ts` เป็น `proxy.ts` | `web-craft` ข้อ 6 |
| ก่อน build ต้องปิดเซิร์ฟเวอร์ทดสอบ | `web-craft` ข้อ 6 |
| ไฟล์ที่ก๊อปมาห้ามแก้ในโปรเจกต์นี้ ต้องแก้ที่ต้นทาง | `drug-display` · `pharmacy-web-logic` (แต่สกิลเขียนเป็น "ซิงก์สองทาง" ส่วนที่นี่เขียนเป็น "ทางเดียวจากต้นทาง" — **สองสกิลบอกไม่ตรงกัน ควรเคาะให้เหลือแบบเดียว**) |

---

## 3) `C:\Users\PKH\tb-calculator\CLAUDE.md` (74 บรรทัด)

ไฟล์สั้นและเกือบทั้งหมดเป็นตรรกะยาวัณโรคเฉพาะโปรเจกต์ (สูตร eGFR · CrCl · ขนาดเม็ดยา)
แต่มี **5 เรื่อง** ที่เป็นหลักการทั่วไป

| # | กฎ/บทเรียนที่เจอ | ยกข้อความจริงมา | ควรไปสกิลไหน | ทำไมถึงควรขึ้นสกิล |
|---|---|---|---|---|
| 47 | 🔴 **เส้นแบ่ง "เตือนสีส้ม" กับ "กั้นสีแดง"** | *"**ช่วงอายุที่ validate ได้ใน Cockcroft-Gault** — 18–92 ปีเท่านั้น อายุนอกช่วงนี้จะ**แสดง warning สีส้มแต่ไม่ block การคำนวณ** (ต่างจากส่วนสูง/น้ำหนัก/Cr ที่ block พร้อม error สีแดง)"* | `medical-data-safety` · `web-craft` | **เรื่องเดียวกับข้อ 28 และ 34** — สามโปรเจกต์ค้นพบเส้นแบ่งเดียวกันแยกกัน (เตือน vs กั้น) และไม่มีสกิลไหนเขียนไว้ · ตรงนี้ให้ตัวอย่างที่ชัดที่สุดว่าเมื่อไหร่ใช้อันไหน |
| 48 | 🔴 **ข้อจำกัดของยาจริงต้องสะท้อนในการคำนวณ และต้องเขียนกำกับว่าตั้งใจ** | *"**ข้อจำกัดแคป R** — แคป Rifampicin หักไม่ได้ ดังนั้น `rDose()` ใช้ขนาดเต็มแคป (R300 หรือ R450) ซึ่งอาจทำให้ผู้ป่วยน้ำหนัก <25 kg ได้รับ mg/kg เกิน range **นี่เป็น trade-off ทางคลินิกที่ตั้งใจไว้และ document ไว้ใน source code**"* | `medical-data-safety` | ตัวเลขที่ "ดูผิด" แต่ถูกโดยตั้งใจ · ถ้าไม่เขียนกำกับ Claude ตัวถัดไปจะ "แก้ให้ถูก" แล้วได้ขนาดยาที่หักเม็ดไม่ได้จริง · ตระกูลเดียวกับกฎ "สงสัย/ดูแปลก = ถามก่อน ห้ามเหมาว่าบั๊ก" แต่ที่นี่แก้ด้วยการ**เขียนเจตนาไว้ในโค้ด** |
| 49 | 🟠 **ค่าที่ "แสดงผลเท่านั้น" ต้องเขียนแยกจากค่าที่ใช้ตัดสินใจ** | *"eGFR · CKD-EPI 2021 · **แสดงผลเท่านั้น ไม่ได้ใช้ปรับขนาดยา**"* (ส่วน CrCl · Cockcroft-Gault ใช้ปรับจริง) | `medical-data-safety` · `web-craft` | หน้าจอเดียวโชว์สองค่าที่หน้าตาคล้ายกันมาก คนอ่านต้องรู้ว่าค่าไหนคือค่าที่ระบบใช้ตัดสิน · ผิดแล้วคนคิดว่าระบบปรับยาตาม eGFR |
| 50 | 🟠 **ค่าที่ผู้ใช้กรอกต้องรอดข้ามการวาดจอใหม่** | *"**`doseCache`** — `calculate()` จะไม่ล้างค่านี้เด็ดขาด ล้างได้เฉพาะใน `resetAll()` เท่านั้น เพื่อให้โดสจริงที่ผู้ใช้กรอกอยู่รอดข้าม re-render"* | `web-craft` (ฟอร์ม) | ตระกูลเดียวกับ "ข้อมูลสดข้ามเครื่องห้ามทับสิ่งที่ผู้ใช้กำลังแก้อยู่" (ข้อ 9) — คนละต้นเหตุ ผลเดียวกันคือของที่พิมพ์ไปหายเงียบ ๆ |
| 51 | 🟡 **สถานะล็อกต้องตั้งใหม่ทุกครั้งที่โหลดหน้า เพราะเบราว์เซอร์เติมค่าให้เอง** | *"`calcFdcReverse()` / `lockFdc(active)` ... **lock state ต้องถูก initialize ใหม่เมื่อโหลดหน้า (กรณี browser autofill)**"* | `web-craft` (ฟอร์ม) | เบราว์เซอร์เติมค่าเก่าลงช่องหลังโหลดหน้า โดยไม่ยิงเหตุการณ์ที่โค้ดฟังอยู่ = สถานะบนจอกับค่าในช่องไม่ตรงกันตั้งแต่วินาทีแรก · ไม่มีสกิลไหนพูดถึง autofill เลย |

**เรื่องที่ตรวจแล้วว่ามีในสกิลอยู่แล้ว** — ตารางข้อมูลอ้างอิงยาที่มีแหล่งอ้างอิงกำกับทุกแถว (`web-craft` ข้อ 6.6 ข้อย่อย 3)
· ปุ่มสลับดูแบบมือถือบนคอมที่ทำให้ `@media` ใช้ตัดสินไม่ได้ (`mobile-web-app` กฎข้อ ②)

**ที่ไม่ยกขึ้น (เฉพาะโปรเจกต์)** — สูตร CKD-EPI/Cockcroft-Gault · การเลือก BW · `display:contents`
ที่ใช้ทำผัง L ของ CrCl+BMI · `window.updateMgKg` ที่ต้องอยู่บน `window` เพราะเรียกจาก inline attribute

---

## 4) `C:\Users\PKH\listless70\CLAUDE.md` (164 บรรทัด)

**ไฟล์ที่มีของทั่วไปหนาแน่นที่สุดในสี่ไฟล์** — เป็นเว็บของเพื่อน ใช้ Google Sheet ไม่ใช่ Supabase
แต่บทเรียนเกือบทั้งหมดเป็นเรื่อง **ภาษาไทย · ข้อมูลซ้ำ · แคช · การแก้ข้อมูลจริง** ซึ่งใช้ได้ทุกเว็บ
เจอ **17 เรื่อง**

| # | กฎ/บทเรียนที่เจอ | ยกข้อความจริงมา | ควรไปสกิลไหน | ทำไมถึงควรขึ้นสกิล |
|---|---|---|---|---|
| 52 | 🔴🔴🔴 **`normalize('NFC')` ไม่แก้ลำดับวรรณยุกต์ไทย — ต้องเรียงเครื่องหมายเอง** | *"It strips zero-width/BOM/soft-hyphen, folds exotic spaces, collapses runs of whitespace, and **sorts the Thai vowel/tone marks attached to each consonant**. That last one matters: `กี่` และ `ก่ี` render identically but are different strings, and **`normalize('NFC')` does not fix it** (verified — Unicode has no canonical order for Thai marks). It is a **comparison key only**; never display it, never write it to the sheet."* | `search-system` (ภาษาไทย) | 🚨 **นี่คือความรู้ภาษาไทยที่ไม่มีที่ไหนในสกิลเลย** · สองสตริงที่หน้าตาเหมือนกันเป๊ะแต่ระบบมองเป็นคนละคน = ค้นไม่เจอ · นับซ้ำ · จับกลุ่มพลาด · เว็บห้องยาที่ค้นชื่อยาไทยหรือชื่อ รพ.สต. เจอได้ทันที |
| 53 | 🔴🔴 **ทำความสะอาดข้อความที่ต้นเหตุ ตอนรับเข้า ไม่ใช่ตอนแสดงผล** | *"🚨 **Fixes the root cause, so the report side stops having to mop up.** Every text field ... goes through it before the payload is built: NFC, strip zero-width/BOM/soft-hyphen, fold exotic spaces, collapse whitespace runs, trim. **It only ever removes characters that change nothing visually — it must never touch the teacher's spelling** ... Before this, a teacher hitting the space bar twice created a second copy of a student in every card, chart, table and printed document"* | `search-system` · `web-craft` | เคาะเว้นวรรคเกินหนึ่งที = ข้อมูลแตกเป็นสองคนทั้งระบบ · เส้นแบ่ง "ลบได้เฉพาะสิ่งที่ตาไม่เห็น ห้ามแตะตัวสะกด" คือหัวใจที่ทำให้ปลอดภัยพอจะทำตอนรับเข้า |
| 54 | 🔴 **ตัวเลขสองที่บนจอห้ามขัดกันเงียบ ๆ — ต่างได้แต่ต้องเขียนอธิบาย** | *"**Charts count people, not rows.** Counting raw rows made ม.1–ม.6 sum to 737 while the card said 148, because one student reported in 5 subjects is 5 rows. The tooltip shows both numbers (`41 คน (203 รายการ)`) so the row count is not lost. When the bars legitimately exceed the card ... `renderChart` appends a `.chart-note` explaining why, **rather than letting two numbers contradict each other in silence**"* · *"**The student popup counts 'รายชื่อ', not 'คน'.** One person keyed into two rooms is 2 รายชื่อ but 1 คน; labelling both 'คน' made the card (148) contradict the popup it opens"* | `web-craft` | ต่อยอดจากข้อ 25 (ตัวกรองกับกราฟต้องใช้กติกาเดียวกัน) — ที่นี่ไปไกลกว่า: **ถ้าต่างกันจริงตามธรรมชาติของข้อมูล ต้องแสดงทั้งสองเลขและเขียนบอกว่าทำไม** · หน่วยนับต้องเรียกให้ตรงความหมาย (คน ≠ รายชื่อ ≠ รายการ) |
| 55 | 🔴 **กฎการนับตัวตนต้องมีชุดเดียวในระบบ** | *"**Counting people: there is exactly one identity rule, and it lives in `visKey()`** ... `report.js` delegates to it via `FIXES.visKey` — **never write a second copy**; if the two files normalise differently, the KPI card and the charts silently disagree and nobody can tell which is right"* | `web-craft` · `pharmacy-web-logic` | เป็นรูปธรรมของกฎทอง 10 ในกรณีที่แพงที่สุด — เขียนซ้ำสองที่แล้วตัวเลขไม่ตรง โดยไม่มีทางรู้ว่าอันไหนถูก |
| 56 | 🔴🔴 **แคชต้องมีป้ายบอกว่าเป็นของเก่า และห้ามเด้งกลับหน้าล็อกอินตอนเน็ตล้ม** | *"**A `.stale-bar` is mandatory whenever cached data is on screen — unlabelled stale data is worse than a slow page.** When the network leg fails while the cache is showing, **do not bounce back to the gate**; keep the data up with the amber bar"* · *"the cache is keyed by a **SHA-256 of the password** (never the password itself) so a wrong password shows nothing and wipes it; it expires after `CACHE_TTL` (12 h); 'ออกจากระบบ' clears it, **since closing the browser does not**. If `crypto.subtle` is unavailable, **caching is skipped entirely rather than falling back to a weak key**"* · `CACHE_MAX = 4 MB` (skip caching rather than blow the localStorage quota) | `working-with-gun` ข้อ 5.6 (ข้อมูลต้องเก็บในเครื่อง) | 🚨 สกิลข้อ 5.6 สั่งให้เก็บแคชทุกอย่าง **แต่ไม่มีกฎความปลอดภัยของแคชสักข้อ** — ป้ายบอกของเก่า · กุญแจแคชผูกกับรหัสผ่านแบบย่อย · ล้างตอนออกจากระบบ · เพดานขนาด · ไม่มีตัวเข้ารหัสก็ไม่แคชเลย · ทั้งหมดนี้ใช้ได้ทุกเว็บที่มีประตูรหัสผ่าน |
| 57 | 🔴 **ปฏิทิน พ.ศ. บนเครื่องผู้ใช้ทำให้ช่องวันที่มาตรฐานส่งปีผิด (บั๊ก 3112)** | *"the Sheet stores `วันที่กรอก` as **CE** and every display path adds 543. **Some teachers' machines are set to the Buddhist calendar, and `<input type="date">` there emitted BE** (`2569-08-08`), which then got +543 → **3112** on screen, in the sort order, in the printed A4 document, and in the CSV. 34 of 174 rows in the Sheet are still stored that way ... **Never re-introduce a native date input here**, and keep the 'year ≥ 2400 means BE' rule"* | `web-craft` (ไทยเฉพาะ) | สกิลมีแค่ *"วันที่ timezone +7 อย่าลืมบวก"* · เรื่องนี้อันตรายกว่ามากเพราะ**เกิดเฉพาะบนเครื่องบางคน** คนทำไม่มีทางเจอเอง และข้อมูลเสียถาวรในฐาน 34 แถว · ทางแก้คือทำช่องวัน/เดือน/ปีเอง + กฎ "ปี ≥ 2400 คือ พ.ศ." |
| 58 | 🔴 **แก้ข้อมูลให้ผู้ใช้ ต้องเก็บค่าเดิมไว้และแสดงว่าแก้แล้ว** | *"`timestamp` is stamped server-side ... so it is always trustworthy (**verified: correct in all 174 rows while `วันที่กรอก` was wrong in 34**). If `วันที่กรอก` is more than 365 days away from it ... Overwrite it with the server date, **keep the original in `_dateRaw`, flag `_dateFixed`**"* · *"**365 is deliberate: teachers legitimately backdate by weeks** — a form covering 15 Jun–7 Jul is filed in Aug — so anything under a year must not be touched"* · `dateFixMark()` แสดง ⚠ ที่ชี้ค้างแล้วเห็นค่าเดิม | `pharmacy-web-logic` · `medical-data-safety` | ต่างจากกฎ "ห้ามแก้ข้อมูลในฐานโดยไม่ได้สั่ง" ตรงที่นี่เป็นการ**แก้ตอนแสดงผล** ซึ่งทำได้ แต่ต้องครบ 3 อย่าง — เก็บของเดิม · ติดธง · โชว์ให้เห็นบนจอ · และเกณฑ์ต้องกว้างพอที่จะไม่ไปแตะพฤติกรรมปกติของผู้ใช้ |
| 59 | 🔴🔴 **เขียนทับข้อมูลจริงอัตโนมัติ ต้องมีด่าน 3 ชั้นต่อหนึ่งช่อง** | *"Three guards per cell: the column must be in the `MAIN_EDITABLE` **whitelist**, the row must be **≥2 and inside the real range**, and **the cell's current value must still equal the `from` the browser sent**. That last one is what makes it safe to fire against a sheet that may have moved under us"* · *"**`_row` is the whole reason the write is safe** ... it is sent back paired with the value it expects to find there. **Drop either half and the write becomes a guess**"* · *"contains no `deleteRow`/`insertRow` and no whole-sheet `setValues`; row count, row order and every untouched column stay exactly as they were"* | `pharmacy-web-logic` (ความปลอดภัย) | เป็นสูตรสำเร็จของ "แก้ข้อมูลจริงโดยไม่พังของคนอื่น" — ตรงกับข้อ 5 (ถังขยะเด้งกลับ) และข้อ 13 (รายการคอลัมน์ที่อนุญาต) ของ ME-DRP แต่เขียนครบกว่าและมีชั้นเทียบค่าเดิมซึ่งไม่มีที่ไหนเลย |
| 60 | 🔴 **ห้ามเพิ่มคอลัมน์ในตารางที่คนอื่นใช้งานอยู่ · ของเดิมเก็บตารางแยก** | *"**Originals go to a separate `ค่าเดิม` sheet, never into `รายงาน`.** Adding a 23rd column to `รายงาน` would break what the teachers do with it downstream — **that was an explicit product decision, not an implementation detail**"* · *"`undoMainEdits` replays that sheet backwards to restore, **skipping any cell a human has since hand-edited**, and stamps `สถานะ = ย้อนแล้ว` **rather than deleting the log**"* | `pharmacy-web-logic` (ตารางร่วม) | สกิลบอกว่า *"เพิ่มคอลัมน์ใหม่ = ปลอดภัย"* ซึ่ง**จริงเฉพาะเมื่อไม่มีคนเปิดตารางนั้นทำงานเอง** · ที่นี่คือกรณีที่ตารางเป็นเอกสารทำงานของคน แล้วคอลัมน์ที่ 23 ทำพังทั้งกระบวนการ |
| 61 | 🔴 **ระบบเสนอ คนตัดสิน — และต้องบอกก่อนเขียนว่าจะกระทบอะไรบ้าง** | *"The engine proposes, **a human always decides**, and decisions overlay the data at display time"* · *"**Every button that touches data goes through `confirmBox` first — it names the value, the rows and the cell count before anything is written**"* · *"Decided items live in their own **แก้ไปแล้ว** tab (**not appended to the bottom of the pending list, which is where they used to get lost**)"* · *"`restore()` runs first on every apply, **making it idempotent and making undo instant**"* | `pharmacy-web-logic` · `web-craft` | เว็บมูลค่ายาคืนตั้งกฎเดียวกันเรื่องนำเข้าราคาจาก HIS (*"ระบบเสนอ คนตัดสิน ห้ามนำเข้าอัตโนมัติทั้งดุ้น"*) — **สองโปรเจกต์ ไม่มีในสกิล** · ส่วน "ป๊อปต้องบอกจำนวนแถว/ช่องที่จะเปลี่ยน" ต่อยอดจากกฎ "ป๊อปต้องบอกว่ากำลังจะเสียอะไร" ที่มีอยู่แล้ว |
| 62 | 🔴 **ไฮไลต์ต่างตัวอักษรในข้อความไทย ต้องลากพยัญชนะฐานมาด้วย** | *"**`diffHTML()` highlights the differing characters between two options** and is the card's whole reason for working. The real cases differ by one character — `พิมพะวงศ์` vs `พิมพะวงศ` is one ไม้ทัณฑฆาต — and **a teacher cannot see that unaided, so they were picking blind** ... **Thai vowels/tone marks can't stand alone, so a differing mark drags its base consonant into the highlight** — otherwise it renders detached and unreadable"* · *"drops a `.fx-df-gap` tick where the other option has something this one is *missing* (**without it the shorter option looks clean, as if nothing were wrong**)"* | `search-system` (ภาษาไทย) | คู่กับข้อ 52 · ทุกเว็บที่ให้คนเลือกระหว่างสองข้อความไทยที่คล้ายกัน (รวมชื่อยา LASA) ต้องใช้ · ไฮไลต์แบบตัวต่อตัวธรรมดาทำให้สระลอยอ่านไม่ออก |
| 63 | 🟠 **ของที่ตาแยกไม่ออก ห้ามไฮไลต์ ต้องอธิบายเป็นข้อความไทย** | *"**a red cell border only ever goes on a cell that already displays two values** (`36, 37`). **Bordering a cell whose contents look perfectly normal tells the reader nothing** — that case gets a plain-Thai chip (`ชื่อในชีตไม่ตรงกัน (ตาดูเหมือนกัน)`)"* · *"Each option carries `describeOdd()` output — `เคาะเว้นวรรค 2 ที ติดกัน`, `มีอักขระที่มองไม่เห็นปนอยู่ 1 ตัว` — **because `diffHTML()` cannot highlight something with no visible glyph**, and a card showing two identical-looking options with no explanation just makes people guess"* | `web-craft` | หลักการทั่วไป: **เครื่องหมายเตือนที่ชี้ไปยังสิ่งที่ตามองไม่เห็น = เตือนแล้วคนยิ่งงง** ต้องเปลี่ยนเป็นคำอธิบายภาษาคนแทน |
| 64 | 🔴 **เอกสารที่พิมพ์ออกกระดาษต้องมีค่าเดียวต่อช่องเสมอ** | *"**The printed document shows one value per field, always.** `buildStudentListDoc` deliberately does **not** use `dispVar` — **printing `เลขที่ 22, 23` on a government form reads as a broken document. Conflicts belong on screen, where they get fixed**"* | `making-documents` | สกิลมีกฎเอกสารราชการครบหลายข้อแต่ไม่มีข้อนี้ · เอกสารที่ออกนอกหน่วยงานห้ามโชว์ความไม่ลงรอยของข้อมูล ต้องแก้ให้จบบนจอก่อน |
| 65 | 🔴 **ทุกช่องทางที่คุยกับหลังบ้านต้องมีตัวลองใหม่ และตัวใหม่ที่เพิ่มก็ต้องมีด้วย** | *"🚨 **Every call retries — do not remove it.** `jsonp()` ... retries up to 6 times (backoff 1s, 2s, 4s, capped at 4s) on `network`/`timeout` only; auth failures fire once ... Real-world failure: `ERR_NAME_NOT_RESOLVED` then `ERR_NETWORK_CHANGED` on the first two attempts, the third succeeding — **Edge and Safari resolve DNS through their own resolver, so they stumble where Chrome does not.** Without retry that reads as 'permanently broken on Edge/Safari, fine on Chrome'"* · *"On timeout the callback is **replaced with a no-op instead of deleted**, so a late response is swallowed quietly"* · *"**any new transport added anywhere must carry it too**"* | `pharmacy-web-logic` ข้อ 30 (เน็ตหลุด) | สกิลข้อ 30 มี "ตัวจับเวลาตัด" แต่**ไม่มีตัวลองใหม่แบบถอยห่าง** · และคำอธิบายว่าทำไมอาการ "พังเฉพาะ Edge/Safari" มักไม่ใช่บั๊กของเบราว์เซอร์นั้น เป็นความรู้ไล่บั๊กที่หาเองยากมาก |
| 66 | 🔴 **ส่งแล้วต้องถามกลับว่าลงจริงไหม — คำตอบที่ resolve ไม่ได้แปลว่าสำเร็จ** | *"**`no-cors`:** submit fetch always resolves; **never trust it for success** — use the `verify` round-trip. Both write paths do this: the entry form via `action=verify`, the fix panel via re-reading `action=fixes`"* · *"A 15s timeout + a follow-up `action=verify` GET (counts rows by `submissionId`) confirm the write actually landed"* | `pharmacy-web-logic` ข้อ 20-21 | ตระกูลเดียวกับบั๊ก "ขึ้นว่าบันทึกแล้วหลอก ๆ" ที่สกิลถือว่าร้ายแรงที่สุด (UUID สำรอง ข้อ 21) แต่**ท่าส่งแล้วถามกลับ** ยังไม่เคยถูกเขียนเป็นกฎ · ใช้ได้กับทุกช่องทางเขียนที่อ่านคำตอบตัวเองไม่ได้ |
| 67 | 🔴 **แยก "บั๊กของเรา" ออกจาก "ปัญหาเครื่องผู้ใช้" ด้วยรายการสัญญาณ** | *"**Popup paint-freeze on some machines (environmental, NOT a code bug):** ... **Telltales:** only Chrome/Edge (Firefox/Opera fine), main thread stays responsive (JS/longtask measured tiny), **freeze disappears with DevTools open**, not reproducible on a clean machine / the deployed site on another PC, and it persists through every code-level mitigation ... **do not keep chasing this freeze in the code** — point the user to update Windows/GPU drivers or use Firefox"* · *"**'เชื่อมต่อไม่สำเร็จ' on a user's machine is usually their Google session, not the network** ... Proof from a real device: **same machine, same network, same minute — normal window `302 → 404`, InPrivate window `302 → 200`**"* | `working-with-gun` ข้อ 3.6 (ไล่บั๊กเครื่องคนอื่น) | สกิลข้อ 3.6 บอกให้ขอ F12 แต่ไม่ได้บอกว่า**เมื่อไหร่ควรเลิกไล่ในโค้ด** · สองเคสนี้ให้ทั้งรายการสัญญาณและท่าพิสูจน์ (**เทียบหน้าต่างปกติกับหน้าต่างไม่ระบุตัวตนในนาทีเดียวกัน**) ซึ่งใช้ได้ทุกโปรเจกต์และประหยัดเวลาได้ทั้งวัน |
| 68 | 🟠 **ทำความสะอาดข้อมูลที่ประตูเดียว ก่อนอะไรก็ตามแตะมัน** | *"**`normalizeRows()`** runs on every fetch (login + realtime poll) **before anything else touches the data. One gate, so table/sort/filter/PDF/CSV are all fixed at once**"* | `web-craft` · `pharmacy-web-logic` | รูปธรรมของกฎทอง 10 ฝั่งข้อมูล (สกิลพูดถึงแต่ฝั่งหน้าจอ) · แก้ที่ประตูเดียวแล้วตาราง เรียง กรอง ไฟล์พิมพ์ และไฟล์ส่งออก ตรงกันหมดโดยไม่ต้องไล่ |
| 68ก | 🟠 **บางอย่างที่ดูซ้ำกัน ห้ามรวม — ข้อยกเว้นของกฎ "รวมเป็นตัวกลาง"** | *"**`buildDoc`/`fitNames`/`thaiDate` are NOT shared** — `js/index.js` และ `js/report.js` each keep their own copy and **they genuinely differ** ... **They were checked during the split and deliberately left separate — do not try to merge them into a shared file**"* | `working-with-gun` กฎทอง 10 | 🔑 กฎทอง 10 สั่งให้รวมของที่คล้ายกันเสมอ · ข้อนี้บอกวิธีตัดสินว่าเมื่อไหร่**ไม่ควรรวม** และที่สำคัญคือ **ต้องเขียนกำกับไว้ว่าตรวจแล้วและตั้งใจแยก** ไม่งั้นคนถัดไปจะมารวมให้ |
| 68ข | 🟠 **ตัวเขียนต้องยกเลิกตัวเองเมื่อไม่มีอะไรจะเขียน** | *"`saveToSheet` ... **aborts if `students` is empty (never deletes old data with nothing to write back)**"* | `pharmacy-web-logic` | ตัวเขียนที่ "ลบก่อนแล้วเขียนใหม่" เจอข้อมูลว่างเมื่อไหร่ = ลบของเก่าทิ้งฟรี · เป็นด่านบรรทัดเดียวที่กันข้อมูลหายทั้งก้อน |

### เรื่องที่ตรวจแล้วว่า **มีในสกิลอยู่แล้ว**

| เรื่องใน CLAUDE.md | อยู่ในสกิลไหนแล้ว |
|---|---|
| เปิด URL ตรง ๆ ในเบราว์เซอร์ไม่ใช่การพิสูจน์ว่าหลังบ้านทำงาน | `working-with-gun` ข้อ 3.6 (เขียนไว้ตรงกันเป๊ะ) |
| เวลาในฐานเป็น UTC ต้อง +7 ก่อนแสดงผลไทย | `web-craft` ข้อ 6 (ไทยเฉพาะ) |
| ฝังฟอนต์ไทยเอง (`@font-face` woff2) กันเครื่องไม่มีฟอนต์ | `web-craft` ข้อ 6 · `making-documents` |
| `overflow-x` ที่ครอบตารางทำให้ `overflow-y` กลายเป็น auto แล้ว sticky พัง | `web-craft` ข้อ 6.2 |
| `window.print()` อย่างเดียว ไม่ลงไลบรารีสร้าง PDF | `making-documents` |
| `print-color-adjust:exact` ให้สีหัวตารางรอดตอนพิมพ์ | `web-craft` · `making-documents` |
| ร่างเก็บในเครื่อง + ถามก่อนกู้ + ล้างเมื่อบันทึกสำเร็จ | `pharmacy-web-logic` ข้อ 30 |
| virtual scroll ตอนแถวเยอะ | `web-craft` ข้อ 6 (ความเร็ว) |
| เลขรุ่นแสดง 2 ที่ ต้องแก้ให้ตรงกันตอนปล่อยรุ่น | `working-with-gun` ข้อ 4 |
| บันทึกวิธีที่ลองแล้วไม่ได้ผล ห้ามเสียเวลาซ้ำ ("Do not try to proxy Apps Script") | `search-system` ข้อ 11 ใช้รูปแบบเดียวกัน |
| UI ห้ามมีเครื่องหมาย `?` | `working-with-gun` ข้อ 2 |

---

## 5) `C:\Users\PKH\ME-DRP-fresh\docs\SESSION-*.md` — 34 ไฟล์ (3,270 บรรทัด)

อ่านทั้ง 34 ไฟล์ · เนื้อหาส่วนใหญ่เป็นบันทึกงานรายรอบซึ่งซ้ำกับ `CLAUDE.md` อยู่แล้ว
ตรงนี้เก็บเฉพาะ **ของที่ไม่ได้ถูกยกขึ้น `CLAUDE.md` ด้วยซ้ำ** หรือมีรายละเอียดครบกว่า

### 5.1 ไฟล์ 07-08 ถึง 08-12 (20 ไฟล์แรก)

| # | กฎ/บทเรียนที่เจอ | ยกข้อความจริงมา | ควรไปสกิลไหน | ทำไมถึงควรขึ้นสกิล |
|---|---|---|---|---|
| 69 | 🔴🔴 **ค่าที่บันทึกลงฐาน ห้ามเป็นข้อความที่โชว์บนจอ** | *"**`KeyDesc.label`** = ป้ายที่โชว์ (ไทย + วงเล็บอังกฤษ) แยกจาก **`key`** = ค่าที่บันทึกจริง → **เปลี่ยนข้อความบนจอได้โดยเคสเก่าไม่เพี้ยน** · helper `drpLabel(key)` ใช้ทุกจุดแสดงผล"* (07-14) · CLAUDE.md ต่อยอดเป็น *"ห้ามแก้ key ของหมวดเดิม ... ไม่งั้นเคสเก่าเพี้ยน"* | `pharmacy-web-logic` (ฐานข้อมูล) | เว็บทุกตัวมีรายการตัวเลือก (ลักษณะ · หมวด · แหล่งที่มา · เหตุผล) · ถ้าเก็บข้อความที่โชว์ลงฐาน วันที่อยากแก้คำให้อ่านง่ายขึ้นจะทำไม่ได้เลย เพราะข้อมูลเก่าจะเพี้ยนทั้งหมด · เป็นการตัดสินใจที่**ย้อนกลับไม่ได้ ต้องทำถูกตั้งแต่ตารางแรก** |
| 70 | 🔴 **`NEXT_PUBLIC_*` ต้องมีตอน build ไม่ใช่ตอนรัน** | *"**ต้นเหตุ:** `NEXT_PUBLIC_SUPABASE_*` ไม่ถูก inline เข้า client bundle (**env ต้องมีตอน build ไม่ใช่ runtime**) → `envConfig()` คืนค่าว่าง → `isConfigured()` = false → **แอปคิดว่ายังไม่เชื่อม DB → ไม่ fetch เลยตกไปโชว์ demo**"* (07-10) | `web-craft` (deploy) | อาการคือ "เว็บจริงโชว์ข้อมูลปลอม" ซึ่งไล่หายากมากเพราะโค้ดถูกทุกบรรทัด · ตั้งตัวแปรถูกแล้วก็ยังพัง ถ้าตั้งหลัง build · ต่อยอดจากข้อ 4 ที่ว่าต้อง deploy ใหม่ |
| 71 | 🔴 **ตัวแปรบน Cloudflare ต้องเป็นชนิด Secret ไม่ใช่ข้อความธรรมดา** | *"ผู้ใช้ตั้ง env 2 ตัวเป็น **Secret** ใน Cloudflare ... — **plaintext จะโดน deploy ล้างทุกครั้ง, Secret ไม่หาย**"* (07-09) | `working-with-gun` ข้อ 4 · `pharmacy-web-logic` | ตั้งเป็นข้อความธรรมดาแล้วหายทุกรอบ deploy = เว็บจริงพังเป็นระยะโดยไม่มีรูปแบบ · ปัจจุบันสกิลบอกแค่ "ตั้งตัวแปรลับบน Cloudflare แล้วหรือยัง" ไม่ได้บอกว่าตั้งชนิดไหน |
| 72 | 🟠 **คำสั่ง build บน Cloudflare ของ Next.js ต้องเป็นตัวแปลง ไม่ใช่ `npm run build`** | *"**ต้นเหตุ:** Build command บน Cloudflare เป็น `npm run build` (ได้แค่ `.next/` ไม่ได้ `.open-next/`) → OpenNext deploy หาไฟล์ compile ไม่เจอ (`Could not find compiled Open Next config`) · **วิธีแก้: Build command = `npx opennextjs-cloudflare build` · Deploy command = `npx wrangler deploy`**"* (07-09) | `web-craft` (deploy) | ทุกเว็บ Next.js ของพี่กันขึ้น Cloudflare Workers ด้วยตัวแปลงตัวเดียวกัน · สกิลบอกแค่ว่า "Workers = Next.js มี server" ไม่ได้บอกคำสั่ง |
| 73 | 🔴 **ค่าที่ระบบเติมให้อัตโนมัติ ต้องแยกจากค่าที่ผู้ใช้ตั้งเอง** | *"**ธง High-alert auto ค้าง** — state `hadAuto` แยก auto จากยา HAD vs ผู้ใช้ติ๊กเอง · `clearAutoHad` (ปลดธงเมื่อลบยา HAD หมด · **ถ้า auto**) · `toggleHighAlert` ติ๊กเอง = ปลด `hadAuto` (**ไม่ลบธงที่ตั้งใจ**)"* (07-14c ข้อ 13) | `web-craft` (ฟอร์ม) | ระบบที่เติมค่าให้แล้วถอนคืนเองไม่ได้ = ค้างผิด · ถอนแบบไม่แยก = ลบสิ่งที่ผู้ใช้ตั้งใจติ๊ก · ใช้กับทุกช่องที่มี "ระบบเดาให้ก่อน แล้วผู้ใช้แก้ได้" |
| 74 | 🔴 **คิวที่เก็บในเครื่องต้องแก้ทีละรายการ ห้ามเขียนทับทั้งชุด** | *"**คิว/แคช race** — `mutatePending` (**อ่าน-แก้-เขียนทีละรายการ ไม่ทับทั้งชุด**) + `dedupUnsynced` (คงเคสที่ยังไม่ซิงก์)"* (07-14c ข้อ 6) | `pharmacy-web-logic` ข้อ 30 (เน็ตหลุด) | สองงานที่วิ่งพร้อมกัน (ตัวส่งซ้ำอัตโนมัติ กับ ผู้ใช้กดบันทึกใหม่) อ่านคิวชุดเดียวกันแล้วเขียนทับกัน = รายงานหายจากคิวเงียบ ๆ · สกิลมีเรื่องคิวส่งซ้ำแต่ไม่มีกฎเรื่องการเขียนทับ |
| 75 | 🟠 **ตัวเลือกในช่องกรอกต้องดึงจากค่าที่มีจริงในฐาน ไม่ให้พิมพ์เอง** | *"หน่วย/รูปแบบ/ทางให้ยา เป็น dropdown — helper `distinct(key)` จากค่าที่มีจริงในคลัง (**+ คงค่าปัจจุบันถ้าไม่อยู่ในลิสต์**) **กันพิมพ์ไม่ตรงกัน**"* (07-16c) | `web-craft` · `pharmacy-web-logic` | ต้นเหตุเดียวกับข้อ 52-53 ของ listless (ข้อมูลแตกเพราะคนพิมพ์ไม่เหมือนกัน) แต่แก้จากอีกด้าน — **ปิดทางพิมพ์ตั้งแต่แรก** · ข้อ "คงค่าปัจจุบันถ้าไม่อยู่ในลิสต์" สำคัญมาก ไม่งั้นแถวเก่าโดนล้าง |
| 76 | 🟠 **ร่องรอยการแก้ต้องทำที่ฐาน ไม่ใช่ที่แอป** | *"ตาราง `drug_audit` + trigger `log_drug_change` (**SECURITY DEFINER**) จับทุก insert/update/delete → เก็บ old/new (jsonb)+เวลา · **จับได้แม้แก้ตรงใน Supabase/MCP**"* · *"แอปไม่มี login เลยระบุ 'ใคร' ไม่ได้ · **แต่เก็บ 'อะไร+เมื่อไหร่' ครบ**"* (07-16b) | `pharmacy-web-logic` | ร่องรอยที่เขียนจากฝั่งแอป ดักไม่ได้ตอนคนเข้าไปแก้ในหน้าฐานโดยตรง · เว็บยาคืนก็ยกตารางนี้ไปใช้แล้ว (อ่าน `drug_audit` ที่ ME-DRP สร้าง) แสดงว่าเป็นของกลางจริง แต่กฎไม่ได้อยู่ในสกิล |
| 77 | 🟠 **เสียงเตือนบน iPhone ต้องปลดล็อกในจังหวะที่นิ้วแตะ** | *"`unlockAudio()` (**ปลด AudioContext ตอนกดปุ่ม — iOS ต้องปลดในจังหวะ gesture**) + `alertFail()` = `navigator.vibrate` (เฉพาะ Android) + บี๊บ 2 จังหวะ Web Audio · **iOS สั่นไม่ได้**"* (07-14b) | `mobile-web-app` | สกิลมีข้อห้าม `navigator.vibrate` บน iPhone แล้ว **แต่ไม่มีทางออกว่าใช้อะไรแทน** · เสียงใช้ได้ทั้งสองระบบ แต่ต้องปลดล็อกให้ถูกจังหวะ ไม่งั้นเงียบสนิทเฉพาะ iPhone |
| 78 | 🟠 **ป้องกันสูตรฝังในไฟล์ส่งออก ทำยังไง** | *"**CSV formula injection** — `esc` เติม `'` นำหน้าช่องที่ขึ้นต้น `= + - @`"* (07-14c ข้อ 18) | `making-documents` | เป็นวิธีทำของข้อ 31 · สี่ตัวอักษรที่ต้องดัก (`= + - @`) กับวิธีแก้ (เติมเครื่องหมายคำพูดเดี่ยวนำหน้า) เขียนไว้ที่นี่ที่เดียว |
| 79 | 🟠 **รื้อโครงโค้ดต้องทำเป็นเฟส เรียงจากเสี่ยงน้อยไปมาก และพิสูจน์ว่าไม่เปลี่ยนพฤติกรรมทุกเฟส** | *"**pure move (ไม่เปลี่ยนพฤติกรรม/หน้าตา)** · build เขียว + **verify runtime จริง (Playwright ทุกหน้า)**"* (ทุกไฟล์ 07-19b ถึง 07-19g) · *"ลำดับ: Settings/Manage → DrugsAdmin → Dashboard → Records → Detail → **Form (คู่ state มากสุด ทำท้ายสุด)**"* · *"**แนะนำ: ทำ Step 6–9 เป็นเซสชันใหม่สด ๆ · เสี่ยงกว่า** (closure/ref/effect-deps ของ save/คิว)"* | `working-with-gun` (วิธีทำงาน) | ผลลัพธ์จริง **5,202 → 824 บรรทัด (−84%) โดยไม่มีบั๊กหลุด** เพราะทำตามวิธีนี้ · เป็นวิธีทำงานที่พิสูจน์แล้วกับงานใหญ่ที่สุดของโปรเจกต์ และใช้ได้ทุกเว็บ · สกิลไม่มีเรื่องวิธีรื้อโครงเลย |
| 80 | 🟡 **typecheck จับ import ที่ตายแล้วไม่ได้** | *"`strict: true` แต่ **ไม่มี** `noUnusedLocals` → **import ที่ไม่ใช้ไม่ทำ build พัง**"* (07-19b) | `working-with-gun` ข้อ 4.8 (เทสด้วยการอ่านโค้ด) | ตรงกับกฎ 4.8 ที่ว่า "เทสเขียวหมดไม่ได้แปลว่าโค้ดถูก" — นี่คือตัวอย่างที่ชัดว่า **หลังย้ายโค้ด ตัวตรวจไม่ฟ้องของที่ตายแล้ว ต้องกวาดด้วยตา** |
| 81 | 🟡 **เซิร์ฟเวอร์ทดสอบค้างแบบ "พอร์ตเปิดแต่ไม่ตอบ"** | *"หลังแก้ไฟล์หลายรอบติดกัน **พอร์ต 3001 ยัง LISTENING แต่ HTTP ไม่ตอบเลย** (`curl` ได้ code `000` timeout 20 วิ) · **วิธีตรวจ:** `netstat -ano \| grep :3001` เห็นพอร์ตเปิด แต่ `curl -m 20` ไม่ตอบ · **วิธีแก้:** `Stop-Process -Id <pid> -Force` แล้วเปิดใหม่**บนพอร์ตเดิม** (ห้ามสลับพอร์ต)"* (08-12) | `working-with-gun` ข้อ 5.9 (เปิดเซิร์ฟเวอร์ทดสอบ) | สกิลข้อ 5.9 สอนแค่วิธีเช็คว่าพอร์ตว่างไหม · **พอร์ตที่เปิดอยู่แต่ตายแล้ว** จะทำให้เข้าใจผิดว่า "มีของโปรเจกต์เดียวกันรันอยู่ ใช้ตัวนั้น" แล้วเทสไม่ได้ทั้งวัน · เช็คด้วย `curl` ไม่ใช่แค่ `netstat` |
| 82 | 🟡 **ห้าม `pkill -f` ด้วยคำที่ตรงกับคำสั่งของตัวเอง** | *"⚠️ **ห้าม `pkill -f \"next dev\"`** — pattern ไปเจอ cmdline ของคำสั่งตัวเอง **เลยฆ่า shell ตัวเอง (exit 144)** · ปิด dev ใช้ `fuser -k 3100/tcp` แทน"* (07-09) | `working-with-gun` ข้อ 5 (ไฟล์+เครื่องมือ) | ฆ่าเชลล์ตัวเองแล้วงานที่ค้างอยู่หายหมด · ตระกูลเดียวกับข้อห้าม `cd` ใน Bash tool ที่มีอยู่แล้ว |
| 83 | 🟡 **เครื่องอาจล้าหลังหลายสิบ commit — ต้องเทียบก่อนเริ่มงาน** | *"เริ่ม session ด้วยการ **ดึงโค้ดล่าสุดลงเครื่อง** — `C:\Users\PKH\ME-DRP-fresh` ค้างที่ v0.9.7.2 **ล้าหลัง origin/main 47 commit** (งาน refactor 3 เฟส + ถังขยะ + คลังยา + หน่วยงานต้นเหตุ ฯลฯ)"* (08-07) | `working-with-gun` ข้อ 5 | แก้โค้ดบนฐานที่ล้าหลัง 47 commit = งานชนกันทั้งหมด · พี่กันทำงานหลายเครื่องและมี session บนคลาวด์ด้วย · สกิลบอกให้เช็ค `git log` ก่อน bump version แต่ไม่ได้บอกให้ `git fetch` เทียบก่อน**เริ่มแก้** |
| 84 | 🟡 **มอคอัปห้ามเอาของคนละหน้ามารวมกัน** | *"**mockup อย่าเอาของคนละหน้ามารวมกัน** จนดูเหมือนอยู่หน้าเดียวกัน (Heatmap อยู่ Dashboard ไม่ใช่ท้ายฟอร์ม)"* (07-09) | `web-craft` (มอคอัป) | สกิลบอกว่ามอคอัปต้องมีของครบเหมือนหน้าจริง · ข้อนี้เป็นด้านกลับ — **ครบเกินจนผิดที่ ก็หลอกตาเหมือนกัน** พี่กันเคาะแบบไปแล้วจะได้ของที่ไม่ตรงกับที่คิด |
| 85 | 🟡 **รายการหมวดทางวิชาการต้องเช็คว่าครบตามตำราตั้งแต่แรก** | *"**บทเรียน:** แคลร์เติมวงเล็บอังกฤษให้หมวดเดิม 7 อันโดย**ไม่เช็คว่าครบตามหลักวิชาการไหม** → พี่กันทัก *'เอามาตามตำราป่าวเนี่ย มันเยอะกว่านี้'* → เสนอ Hepler & Strand vs PCNE ให้เลือก"* (07-14) | `medical-data-safety` | สกิลมีกฎ "ห้ามรวม ห้ามย่อ ห้ามตัดเกณฑ์เอง" ซึ่งคุมตอน**แสดงผล** · ข้อนี้คุมตอน**สร้างรายการตั้งแต่แรก** — เจอรายการหมวดทางวิชาการเมื่อไหร่ ต้องถามก่อนว่าอิงตำราไหน ครบหรือยัง ไม่ใช่แต่งเพิ่มให้สวย |
| 86 | 🟡 **ประกาศชนิดของไฟล์ที่แชร์ข้ามโปรเจกต์ ต้องเป็น generic** | *"🚨 **`.d.ts` ใช้ generic `<T>` เปล่า ๆ** ไม่ผูกกับ `SearchableDrug` เพราะ `Drug` ไม่มี index signature จะติด TS2345"* (08-12) | `search-system` · `drug-display` | `drugSearch.js` ถูกก๊อปข้าม 3 เว็บที่ชนิดข้อมูลยาไม่เหมือนกัน · ผูกชนิดกับโปรเจกต์ใดโปรเจกต์หนึ่ง = อีกเว็บ typecheck ไม่ผ่านทันทีที่ก๊อปไป |

### 5.2 ไฟล์ 08-15 ถึง 09-04-part2 (14 ไฟล์หลัง)

| # | กฎ/บทเรียนที่เจอ | ยกข้อความจริงมา | ควรไปสกิลไหน | ทำไมถึงควรขึ้นสกิล |
|---|---|---|---|---|
| 87 | 🔴🔴🔴 **เอกสารที่จดค่าไว้ผิด ทำให้โค้ดผิดตามไปเรื่อย ๆ — ค่าที่คัดลอกมาจากอีกเว็บ ต้องวัดจากของที่รันจริง** | *"🔴 **ต้นตอคือ CLAUDE.md เขียนตารางสีไว้ผิดตั้งแต่แรก** ทั้งที่เขียนกำกับไว้เองว่า *'ต้องตรงกับเว็บมูลค่ายาคืนทุกจุด'* — **ค่าที่จดไม่ตรงของจริง แล้วโค้ดก็ทำตามที่จดผิด**"* · *"ค่าชุดใหม่**วัดจากเว็บยาคืนที่รันจริง**"* (09-04-part2) | `working-with-gun` (วิธีทำงาน) · `drug-display` | 🔑 **เป็นโรคเดียวกับที่กำลังตรวจอยู่นี้เป๊ะ** — จดกฎไว้ผิดที่ กับ จดค่าไว้ผิดค่า อาการเหมือนกันคือ "มีเอกสารกำกับ แต่เอกสารไม่ตรงของจริง" · กฎที่ควรตั้ง: **ยกค่าจากเว็บอื่นต้องเปิดเว็บนั้นวัด ห้ามลอกจากเอกสาร** และ **เอกสารที่อ้างว่า 'ตรงกับ X' ต้องมีวันที่ตรวจกำกับ** |
| 88 | 🔴🔴 **"ไปดูเว็บนั้นมาว่าเป็นยังไง" = ไปดูแล้วทำให้เหมือน จบในรอบเดียว** | *"1. **'ไปดูเว็บนั้นมาว่าเป็นยังไง' = ไปดูแล้วทำให้เหมือน จบในรอบเดียว** ห้ามกลับมาถามว่า 'จะให้แก้ไหม' เมื่อเจอความต่าง · 3. **พี่กันถามซ้ำ = ยังไม่ได้สิ่งที่ขอ ต้องเปลี่ยนวิธี** ไม่ใช่ตอบเดิมด้วยคำอื่น"* (09-04-part2 · หลังโดนด่า *"เราบอกอะไร เราให้ดูขนาดลูกศรรรรรรรรรร"*) | `working-with-gun` ข้อ 9 (งานที่สั่งแล้วห้ามถามซ้ำ) | สกิลข้อ 9 มี "สั่งแล้ว = ทำจนจบ ห้ามถามว่าจะให้ทำต่อไหม" แล้ว **แต่ไม่ครอบคำสั่งรูปแบบ "ไปดู X มา"** ซึ่งฟังเหมือนสั่งให้ไปสำรวจอย่างเดียว · ข้อ 3 (ถามซ้ำ = ต้องเปลี่ยนวิธี) เป็นสัญญาณเตือนที่ใช้ได้ทุกบทสนทนา |
| 89 | 🔴🔴 **ฟังก์ชันฝั่งอ่านต้องโยนข้อผิดพลาด ห้ามคืนค่าว่าง** | *"**ฟังก์ชันฝั่งอ่านต้องโยนข้อผิดพลาด ห้ามคืนค่าว่าง** → ค่าว่างไปทับสำเนาในเครื่อง **รายงานหายหมดจอ** (บั๊กเดิมของ v0.9.21.0)"* (09-03) | `pharmacy-web-logic` | "ดึงไม่สำเร็จ" กับ "ดึงได้แล้วไม่มีข้อมูล" ต้องแยกจากกันให้ได้ ไม่งั้นความล้มเหลวจะกลายเป็นการล้างข้อมูลบนจอ · เป็นต้นเหตุเดียวกับ "โหลดไม่สำเร็จต้องไม่เหมือนไม่มีข้อมูล" ที่เว็บยาคืนตั้งกฎไว้ แต่ยังไม่มีในสกิล |
| 90 | 🔴 **สำรองข้อมูลก่อนแตะฐาน และโฟลเดอร์สำรองต้องไม่ขึ้น git** | *"**สำรองข้อมูลก่อนแตะอะไร** (`backups/2026-09-03-1542/`) — ถ้าพลาดหนักกว่านี้ยังกู้ได้"* · *"🚨 โฟลเดอร์ `backups/` อยู่ใน `.gitignore` แล้ว **มี HN ห้ามขึ้น git**"* (09-03) | `pharmacy-web-logic` · `medical-data-safety` | รอบที่เว็บจริงล่ม 40 นาที สิ่งที่ช่วยไว้คือของสองอย่าง — คิวรายงานค้างส่ง กับ**ไฟล์สำรอง** · สกิลไม่มีขั้นตอน "สำรองก่อนแตะฐาน" เลย และไม่มีคำเตือนว่าไฟล์สำรองมี HN |
| 91 | 🔴 **ค่าตั้งค่าที่เปลี่ยนได้ ห้ามเอามาผสมในลายเซ็นบัตรผ่าน** | *"`SUPABASE_URL` · Secret · **ห้ามมี `/` ต่อท้าย** — ผสมอยู่ในลายเซ็นบัตรผ่าน **เปลี่ยนแล้วทุกคนหลุดออกจากระบบ**"* (09-03) | `pharmacy-web-logic` (ประตูรหัสผ่าน) | เติมทับ `/` ตัวเดียวแล้วทั้งห้องยาถูกเด้งออกพร้อมกัน โดยไม่มีอะไรอธิบาย · เว็บอื่นที่ยกกลไกบัตรผ่านไปใช้จะเจอเหมือนกัน |
| 92 | 🔴 **หน้าจอผู้ใช้ไม่บอกสาเหตุโดยตั้งใจ = เว็บจริงพังต้องดูบันทึกเสมอ** | *"**หน้าจอผู้ใช้เห็นแค่ 'โหลดรายงานไม่สำเร็จ'** เพราะ `apiFail` ไม่ส่งข้อความภายในออกไป ซึ่งถูกแล้ว **แต่แปลว่าต้องดู log เสมอเวลาเว็บจริงพัง**"* · *"พี่กันส่ง log จาก Cloudflare มาให้ → **ชี้ต้นเหตุได้ในนาทีเดียว**"* (09-03) | `working-with-gun` ข้อ 3.6 · `pharmacy-web-logic` ข้อ 31 | สกิลข้อ 31 บอกว่าทำไมต้องมีบันทึก · ข้อนี้เป็นด้านกลับ — **พอทำแล้ว หน้าจอจะเงียบโดยตั้งใจ** คนไล่บั๊กจึงห้ามเชื่อข้อความบนจอ ต้องเปิดบันทึกทุกครั้ง · ครั้งนี้เป็นครั้งแรกที่ได้ใช้จริงและช่วยไว้จริง |
| 93 | 🔴 **เลื่อนไปหาช่องที่กรอกไม่ครบ — ชุดกฎครบชุด** | *"`FIELD_ORDER` — ลำดับช่อง **ตามที่ตาเห็นบนฟอร์มจริง** (ไม่ใช่ลำดับใน `validateIncident`)"* · *"เติมคลาส `.fx-err` (**ถอดก่อน + บังคับ reflow เพื่อให้กดซ้ำแล้วเรืองใหม่ได้** · ถอดออกเองใน 3.2s)"* · *"โฟกัสช่องพิมพ์ด้วย `preventScroll:true` (**ปุ่ม/ชิปไม่โฟกัส กันกรอบโฟกัสค้าง**)"* · *"**เดิมโหมดแก้ไขไม่มีขอบแดงและไม่มีข้อความเตือนใต้ช่องเลย** บอกแค่ 'กรุณากรอกช่องที่จำเป็นให้ครบก่อนบันทึก' ลอย ๆ"* (08-15b) | `web-craft` (ฟอร์ม) | ฟอร์มยาว ๆ ที่บอกแค่ "กรอกไม่ครบ" คือปัญหาของทุกเว็บ · ชุดนี้ทำครบทุกรายละเอียดแล้วและไม่ผูกกับ ME-DRP เลย (ไฟล์ `lib/focusError.ts` ยกไปได้ทั้งไฟล์) · **สี่รายละเอียดเล็ก ๆ ในกฎนี้คือสิ่งที่ทำให้มันใช้ได้จริง** |
| 94 | 🟠 **ตัวอย่างในหัวข้อช่อง ต้องเรียกฟังก์ชันจริงมาสร้าง ห้ามพิมพ์ตายตัว** | *"หัวข้อช่อง → `AN ผู้ป่วยใน · ระบบจะเติม {formatAn(\"1234\", occurred_at)} ให้เอง` — **เรียก `formatAn` ตัวเดียวกับตอนกรอกจริง ไม่ได้พิมพ์เลข 69 ลงไปตรง ๆ** → **ปีเลื่อนตามวันที่เกิดเหตุเสมอ** · เทสจริง: เปลี่ยนวันที่เป็น 2027 → ข้อความเปลี่ยนเป็น '70-01234' ทันที"* (08-15b) | `web-craft` (ฟอร์ม) | ตัวอย่างที่พิมพ์ตายตัวจะกลายเป็นข้อมูลผิดในวันขึ้นปีใหม่ โดยไม่มีใครนึกถึง · ท่านี้ทำให้ตัวอย่างถูกเสมอโดยไม่ต้องดูแล — ใช้ได้กับทุกช่องที่มีรูปแบบ (วันที่ · เลขเอกสาร · รหัส) |
| 95 | 🟠 **รายการตัวเลือกที่ใกล้กัน ต้องเขียน "เส้นแบ่ง" กำกับ** | *"โครงที่ใช้กับทุกหมวด: เข้าเกณฑ์เมื่อไร → ตัวอย่างจากงานห้องยาจริง → **'เส้นแบ่ง:' กับหมวดที่ใกล้กันจนสับสนบ่อย**"* · *"**พี่กันซึ่งเป็นเภสัชกรยังต้องถาม แปลว่าคนกรอกคนอื่นก็จะเลือกผิดหมวดเหมือนกัน**"* (08-15c) | `medical-data-safety` · `web-craft` | สัญญาณที่ใช้ตัดสินชัดมาก — **ถ้าเจ้าของงานที่เป็นผู้เชี่ยวชาญยังต้องถาม แปลว่าคำอธิบายไม่พอ ไม่ใช่คนอ่านไม่เก่ง** · ใช้ได้กับทุกรายการตัวเลือกในเว็บ ไม่ใช่แค่ DRP |
| 96 | 🟠 **ขนาดตัวอักษรของช่องกรอกใช้กฎกลางได้ · สีตอนชี้ปุ่มใช้ไม่ได้ — เส้นแบ่งอยู่ตรงไหน** | *"**ครอบคลุมกว่าที่รายงานเสนอ** — รายงานบอกให้แก้ 6 ช่องที่วัดเจอ แต่**วิธีนี้ครอบทุกช่องในเว็บ รวมช่องที่จะสร้างวันหน้าด้วย**"* (09-04 · กฎ 16px) เทียบกับสกิล `button-feedback` ที่สั่งว่า *"ห้ามสร้างโค้ดแล้วให้ทุกปุ่มเปลี่ยน ต้องแก้มือทุกปุ่มเท่านั้น"* | `web-craft` · `button-feedback` | 🔑 **สองสกิลกำลังบอกตรงข้ามกัน โดยไม่มีใครเขียนเส้นแบ่งไว้** · เส้นแบ่งจริงคือ **เรื่องที่มีคำตอบเดียวถูกต้องเสมอ (16px กันซูม) ใช้กฎกลางได้ · เรื่องที่คำตอบขึ้นกับบริบทของแต่ละชิ้น (สีตอนชี้) ห้ามใช้** · ไม่เขียนไว้แล้ว Claude ตัวถัดไปจะเลือกผิดข้างหนึ่งแน่นอน |
| 97 | 🟠 **เพิ่มคอลัมน์ใหม่ต้องเติมในรายการคอลัมน์ที่ส่ง ไม่งั้นบันทึกเงียบ** | *"🚨 **`DRUG_COLS` ไม่ครบ = แก้จากหน้าคลังยาแล้วค่าไม่ถูกบันทึกแบบเงียบ ๆ ไม่มี error** เป็นจุดที่พลาดง่ายที่สุดเวลาเพิ่มคอลัมน์ใหม่"* (08-25) · *"แก้ฟิลด์ใหม่ต้องอัปเดต 5 จุดให้ครบ: form state → save → `toRow`/`COLS` → schema → detail/records/CSV"* (CLAUDE.md) | `pharmacy-web-logic` | รายการคอลัมน์ที่อนุญาต (ข้อ 13) มีสองด้าน — ด้านความปลอดภัย (กันเขียนของที่ไม่ควร) กับ**ด้านนี้ (ลืมเติมแล้วของที่ควรเขียนกลับไม่ถูกเขียน)** · เช็คลิสต์ 5 จุดใช้ได้ทุกเว็บที่มีตารางฟอร์ม |
| 98 | 🟠 **แทรกช่องข้อมูลต้องตรงลำดับกับที่ประกาศคอลัมน์ไว้** | *"รอบแรกวางช่องไว้ก่อน Renal แต่ประกาศไว้หลัง → **ทั้งตารางเหลื่อมกันทุกแถว** · จับได้ก่อนเทส เพราะเป็นบั๊กตระกูลเดียวกับตอนเพิ่มคอลัมน์ชื่อเต็มใน v0.9.19.0"* (08-25) · *"ต่อ `<col>`/`<th>` ท้าย `cols.map()` แยก → **หัวไปท้ายสุด แต่ช่องข้อมูลอยู่ก่อนปุ่ม**"* (08-19) | `web-craft` (ตาราง) | เจอซ้ำ 2 รอบในเว็บเดียว · ตารางเหลื่อมทั้งใบเป็นอาการที่ตาจับได้ทันทีแต่ไล่หาสาเหตุยาก · คู่กับกฎ `min-width` ต้องเท่าผลรวม `<col>` |
| 99 | 🟠 **แก้ปัญหา "ทำให้เหมือนกัน" ด้วยการกลับด้าน แทนที่จะไล่ทำให้คล้าย** | *"พี่กันสั่ง *'โลโก้เว็บ เอา 128 แทนที่เลย'* — แทนที่จะไล่ทำไอคอนให้เหมือนโลโก้ **เอาไอคอนไปเป็นโลโก้ซะเลย ทั้งสองที่จึงเป็นไฟล์เดียวกันจริง ไม่ใช่ทำให้คล้าย**"* (08-25 · หลังพยายาม 2 รอบไม่สำเร็จ) | `working-with-gun` กฎทอง 10 · `web-craft` | รอบ 1 วาดเส้นเอง ❌ รอบ 2 ใช้ตัวอักษรจริง ❌ รอบ 3 กลับด้าน ✅ · เป็นตัวอย่างของกฎ "แก้เรื่องเดิมเกินสองรอบแล้วยังไม่หาย = วิธีที่ใช้ผิดทาง" ที่มีอยู่แล้ว แต่**ให้ทางออกที่ชัดว่าเปลี่ยนวิธียังไง — ทำให้เป็นของชิ้นเดียวกัน แทนที่จะทำสองชิ้นให้เหมือนกัน** |
| 100 | 🟠 **ตัวอักษรพิเศษในไฟล์ไอคอน ต้องแปลงเป็นเส้นวาด** | *"**ไฟล์ไอคอนถูกวาดนอกหน้าเว็บ ฟอนต์ที่เว็บโหลดมาใช้ไม่ได้** ถ้าใช้ `<text>` เครื่องที่ฟอนต์ต่างกันจะได้ตัว ℞ คนละแบบ หรือได้กล่องสี่เหลี่ยมเปล่า · วิธีที่ใช้: **ดึงรูปทรง U+211E ออกจากไฟล์ฟอนต์ด้วย opentype.js แล้วฝังเป็นเส้นถาวร**"* · *"**สัดส่วนวัดจากโลโก้เดิมของจริง** — มุมโค้ง 11/36 = 0.306 · ตัวอักษร 20/36 = 0.556"* (08-25 + CLAUDE.md) | `web-craft` · `mobile-web-app` | สกิลมีกฎ "ไอคอนใช้ SVG วาดเอง ห้ามใช้ตัวอักษรพิเศษ" แล้ว **แต่ครอบเฉพาะไอคอนในหน้าเว็บ** · ไฟล์ไอคอนแท็บ/หน้าจอโฮมเป็นคนละเรื่อง เพราะไม่มีทางโหลดฟอนต์ของเว็บมาใช้ได้เลย · พร้อมวิธีดึง glyph ที่ทำได้จริง |
| 101 | 🟠 **แท็บที่ถูกซ่อนทำให้เครื่องมือทดสอบวัดขนาดหน้าต่างเป็น 0** | *"`window.innerWidth` คืน 0 ทำให้ `renderer.setSize(0,0)` และ `cam.aspect = NaN` · **เวลาอ่านค่าจากหน้าเว็บระหว่างทดสอบ ต้องบังคับขนาดจอก่อน (`resize_window`)** ไม่งั้นตัวเลขที่อ่านได้ไม่ใช่ของจริง"* (09-02-part2) | `working-with-gun` (การเทส) · `web-craft` | ญาติของข้อ 18 (rAF/scroll ไม่ทำงานตอนแท็บซ่อน) — **สภาพ "แท็บถูกซ่อน" ทำให้ค่าที่วัดได้ผิดหลายทางพร้อมกัน** · ตรงกับบทเรียน "ตัวตรวจที่วัดคนละสภาพกับที่พี่กันดู คือตัวตรวจที่โกหก" |
| 102 | 🟠 **พิสูจน์ว่าปุ่มเลื่อนหน้าจอทำงาน ตอนแท็บซ่อน** | *"⚠️ **เทสปุ่มเลื่อนหน้าจอในเครื่องมือทดสอบ ต้องเช็ค `document.visibilityState` ก่อน** — แท็บที่ถูกซ่อนอยู่ `scrollTo({behavior:\"smooth\"})` **ไม่ขยับเลยสักพิกเซล** · **วิธียืนยันว่าปุ่มทำงานจริง = สลับ `window.scrollTo` เป็นตัวดักชั่วคราวแล้วดูว่าถูกเรียกด้วยค่าอะไร** · การเลื่อนแบบทันที (ไม่ smooth) ทำงานปกติแม้แท็บซ่อน"* (09-01) | `working-with-gun` (การเทส) | เป็นท่าพิสูจน์ที่ใช้ได้กับทุกอย่างที่เบราว์เซอร์ "ไม่ยอมทำตอนแท็บซ่อน" — **แทนที่จะวัดผล ให้ดักดูว่าถูกเรียกด้วยค่าอะไร** · ไม่มีในสกิลเลย |
| 103 | 🟠 **รันชุดทดสอบทั้งหมดหลังแก้ ไม่ใช่แค่ชุดที่เกี่ยวกับงานรอบนี้** | *"`scripts/client-log-proof.mjs` — **พังมาก่อนแล้ว รันไม่ขึ้นเลย** ... **เจอเพราะรันชุดทดสอบทั้งหมดหลังแก้ ไม่ใช่แค่ชุดที่เกี่ยวกับงานรอบนี้**"* (09-04) | `working-with-gun` ข้อ 4.8 | ชุดทดสอบที่พังเงียบ ๆ ให้ผลแย่กว่าไม่มี เพราะทุกคนคิดว่ามีตาข่ายอยู่ · ตรงกับกฎ "ตัวตรวจที่รายงานผิดแย่กว่าไม่มีตัวตรวจ" ใน `button-feedback` แต่ยังไม่ถูกยกเป็นวินัยการรัน |
| 104 | 🟡 **บัตรผ่านของเครื่องที่ใช้ร่วมกันต้องอายุสั้น** | *"เหตุผลที่ไม่ใช้ 30 วันแบบเว็บแรก ๆ · 1. **คอมห้องยาเป็นเครื่องใช้ร่วมกัน** มีคนล็อกอินทิ้งไว้แล้วลืม อีกหลายสัปดาห์ข้างหน้าใครเปิดเครื่องนั้นก็เห็นข้อมูลผู้ป่วยโดยไม่ต้องรู้รหัส · 2. วันที่ต้องเปลี่ยนรหัสเพราะสงสัยว่ารหัสหลุด **เครื่องที่ล็อกอินค้างไว้ยังเข้าได้ต่อจนครบอายุบัตร การเปลี่ยนรหัสจึงไม่ได้ปิดประตูจริง**"* (09-02-part2) | `pharmacy-web-logic` (ประตูรหัสผ่าน) | สกิลระบุ `AUTH_DAYS = 7` ไว้แล้วแต่**ไม่มีเหตุผล** · เหตุผลข้อ 2 สำคัญที่สุด — วันที่รหัสหลุดจริง จะรู้ทันทีว่าต้องทำอะไรเพิ่มนอกจากเปลี่ยนรหัส |
| 105 | 🟡 **ประตูรหัสผ่านปิดหน้าเว็บ ไม่ได้ปิดฐานข้อมูล** | *"⚠️ **ประตูนี้ปิดหน้าเว็บ ไม่ได้ปิดฐานข้อมูล** — กุญแจ anon ที่เคยอยู่ใน repo สาธารณะ ยังยิงเข้า Supabase ตรงได้โดยไม่ผ่านหน้าเว็บ"* (09-02-part2) | `pharmacy-web-logic` | กันการเข้าใจผิดว่า "ใส่รหัสผ่านแล้วปลอดภัยแล้ว" ซึ่งเป็นความรู้สึกที่เกิดขึ้นทันทีหลังทำประตูเสร็จ · ต้องเขียนคู่กันเสมอว่ายังเหลือด่านไหน |
| 106 | 🟡 **วิธียืนยันว่าตั้งตัวแปรลับสำเร็จ — ดูรหัสตอบกลับ** | *"🔑 **ข้อ 4 คือตัวยืนยันว่าตั้งรหัสสำเร็จจริง** — ถ้ายังไม่ได้ตั้งจะได้ **503** ไม่ใช่ **401**"* (09-02-part2) | `pharmacy-web-logic` | ท่าตรวจจากภายนอกที่ไม่ต้องเข้าหน้า Cloudflare · คู่กับ `/api/health` (ข้อ 15) ที่ตรวจจากภายใน |
| 107 | 🟡 **ตัวรันในเครื่องเปิดอยู่ ให้ใช้ `tsc` ไม่ใช่ `npm run build`** | *"ใช้ `tsc` ตรง ๆ ไม่ใช่ `npm run build` **เพราะ dev server เปิดอยู่ ห้ามชน `.next`**"* (08-25) · แต่ยังพลาดซ้ำอีก 2 รอบ (09-04 · 09-04-part2) *"🚨 **พลาดซ้ำอีกรอบ** ... ทั้งที่เขียนเตือนไว้เองใน CLAUDE.md"* | `web-craft` ข้อ 6 | สกิลบอกแค่ "ห้ามรัน build ขณะ dev เปิด" แต่**ไม่ได้บอกว่าแล้วจะตรวจ type ยังไง** · คนที่อยากตรวจก่อน commit เลยไม่มีทางเลือกนอกจากรัน build · เจอซ้ำ **3 รอบใน 3 สัปดาห์** เพราะขาดประโยคเดียวนี้ |
| 108 | 🟡 **ตั้งค่าให้ตัวเปิดเซิร์ฟเวอร์หาพอร์ตว่างเอง** | *"⚠️ **เว็บโลคอลย้ายพอร์ต** — พอร์ต 3000 มี dev server ของโปรเจกต์ dm-remission (อีกแชท) ใช้อยู่ → เพิ่ม **`\"autoPort\": true`** ใน `C:\\Users\\PKH\\.claude\\launch.json` ของ entry `me-drp-dev` ระบบจะหาพอร์ตว่างให้เอง"* (08-15d · เจอซ้ำ 09-01) | `working-with-gun` ข้อ 5.9 | สกิลข้อ 5.9 (ตั้ง 5 ก.ย.) สอนให้เช็คพอร์ตแล้วเปิดพอร์ตอื่นเอง · **มีวิธีให้ระบบทำให้เองอยู่แล้ว** และไฟล์ `launch.json` ที่สกิลอ้างถึงก็ตัวเดียวกัน แค่ยังไม่ได้จดตัวเลือกนี้ไว้ |

---

## 📌 สรุปรวมทั้งหมด

| ไฟล์ที่ตรวจ | เจอกี่เรื่อง |
|---|---:|
| `ME-DRP-fresh/CLAUDE.md` | 32 |
| `dm-remission/CLAUDE.md` | 14 |
| `tb-calculator/CLAUDE.md` | 5 |
| `listless70/CLAUDE.md` | 17 |
| `ME-DRP-fresh/docs/SESSION-*.md` (34 ไฟล์) | 40 |
| **รวม** | **108** |

### 🔴 สิบเรื่องที่ควรยกขึ้นสกิลก่อนใคร (เรียงตามความเสียหายถ้าไม่ทำ)

| ลำดับ | ข้อ | เรื่อง | ไปสกิล |
|:--:|:--:|---|---|
| 1 | 1 | **heredoc กลืนแบ็กสแลช — และสกิลกลางแนะนำวิธีที่พังอยู่ตอนนี้** | `working-with-gun` ข้อ 5 |
| 2 | 87 | **เอกสารที่จดค่าไว้ผิดทำให้โค้ดผิดตาม — ยกค่าจากเว็บอื่นต้องเปิดวัดของจริง** | `working-with-gun` |
| 3 | 52 | **`normalize('NFC')` ไม่แก้ลำดับวรรณยุกต์ไทย — สองคำที่ตาเห็นเหมือนกันเป๊ะเป็นคนละสตริง** | `search-system` |
| 4 | 2 | **PostgREST ตัดที่ 1,000 แถวเงียบ ๆ** | `pharmacy-web-logic` |
| 5 | 16 | **`run_worker_first: true` — ไม่ตั้งแล้วมีทางเข้าที่ไม่ผ่านหน้าล็อกอิน** | `pharmacy-web-logic` |
| 6 | 36 | **`rls_enabled_no_policy` เป็นสถานะที่ตั้งใจ ห้ามไป "แก้"** | `pharmacy-web-logic` |
| 7 | 3 · 4 | **ลำดับปล่อยงานที่แตะฐาน + ตัวแปร Cloudflare ต้อง deploy ใหม่ + ต้องใส่สองที่** | `working-with-gun` ข้อ 4 |
| 8 | 57 | **ปฏิทิน พ.ศ. บนเครื่องผู้ใช้ทำให้ช่องวันที่มาตรฐานส่งปีผิด (บั๊ก 3112)** | `web-craft` |
| 9 | 18 · 101 · 102 | **แท็บที่ถูกซ่อนทำให้ rAF · smooth scroll · การวัดขนาดจอ ผิดหมด** | `web-craft` · `working-with-gun` |
| 10 | 5 · 6 · 59 | **ชุดกฎ "เขียนทับข้อมูลจริงโดยไม่พังของคนอื่น"** (ตัดช่องสถานะ · ตอบว่าโดนกี่แถว · ด่าน 3 ชั้น) | `pharmacy-web-logic` |

### 🔑 ข้อสังเกตที่สำคัญกว่าตัวเลข

**1. หลายกฎถูกค้นพบซ้ำแยกกันใน 2-3 โปรเจกต์** โดยไม่มีใครยกขึ้นสกิลสักครั้ง — เช่น
"เตือน ไม่ใช่ห้าม" (ME-DRP ข้อ 28 · dm-remission ข้อ 34 · tb-calculator ข้อ 47) ·
"มอคอัปห้ามอยู่ใน `public/`" (ME-DRP ข้อ 32 · dm-remission ข้อ 39) ·
"ระบบเสนอ คนตัดสิน" (listless ข้อ 61 · เว็บยาคืนเรื่องนำเข้าราคา HIS)
**การค้นพบซ้ำคือหลักฐานที่แข็งที่สุดว่าเป็นกฎกลาง**

**2. มีอย่างน้อย 2 จุดที่สกิลกับของจริงขัดกันอยู่ตอนนี้**
· ข้อ 1 — สกิลแนะนำ heredoc ซึ่งพิสูจน์แล้วว่ากลืนแบ็กสแลช
· ข้อ 96 — `button-feedback` ห้ามใช้กฎกลางครอบทุกปุ่ม แต่กฎ 16px ครอบทุกช่องกลับเป็นทางที่ถูก
**สองข้อนี้อันตรายกว่ากฎที่หายไป เพราะคนอ่านสกิลจะทำผิดโดยเชื่อว่าทำถูก**

**3. `listless70` เป็นเหมืองที่ยังไม่มีใครขุด** — เป็นเว็บของเพื่อน ใช้ Google Sheet
ไม่ใช่กองเดียวกับเว็บห้องยา จึงไม่เคยถูกนับเป็นแหล่งกฎ **แต่บทเรียนเรื่องภาษาไทย
ข้อมูลซ้ำ แคช และการแก้ข้อมูลจริง ละเอียดกว่าทุกเว็บในกอง**
