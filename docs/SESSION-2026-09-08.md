# บันทึกงาน 8 กันยายน 2569 — v0.19.3.0

> ชื่อยาในประวัติใช้ชื่อปัจจุบันจากคลังครบทุกทาง
> commit `040795e` → `3a9ae61` · push ขึ้น `main` แล้ว

---

## 🎯 งานที่ฝากข้ามแชทมา

พี่กันเคาะเรื่องนี้ในแชท ME-DRP แล้วส่งคำสั่งข้ามมาที่เว็บยาคืน

> *"ชื่อยาควรเปลี่ยนนะ เพราะชื่อยาที่เปลี่ยนควรเป็นปัจจุบัน เพราะไม่งั้นมันไม่สวย
>  และการเปลี่ยนชื่อยา **เราไม่ได้เปลี่ยนชนิดยาสักหน่อย เราอาจจะแค่ใส่บางคำเพิ่มแค่นั้น**"*

---

## 🔴 สิ่งที่พบตอนตรวจ — คำสั่งตั้งอยู่บนข้อมูลที่ไม่ตรง

คำสั่งบอกว่าเว็บนี้ยังแสดงชื่อที่แช่ไว้ **ความจริงคือทำไปแล้ว 5 จาก 6 จุด**

**สาเหตุที่เข้าใจผิด** — เว็บนี้ทำเรื่องนี้ที่ **ชั้นฐานข้อมูล ไม่ใช่ในโค้ด**
คนละที่กับ ME-DRP ที่ทำในโค้ด `grep` โค้ดจึงหาไม่เจอเลยสักจุด

```sql
-- ฟังก์ชันที่ทำงานนี้อยู่แล้ว พร้อมตาข่ายสำรองในตัว
mr_drug_label(d, ชื่อที่แช่ไว้)
  → ไม่มี d.generic (ยาถูกลบ / ไม่มีรหัสยา) = คืนชื่อที่แช่ไว้
  → มี = ประกอบชื่อจากคลัง ชื่อสามัญ + ตัวย่อ + ความแรง + % + รูปแบบ + ออกฤทธิ์ + ยี่ห้อ + สีเม็ด
```

| ทาง | ก่อนรอบนี้ |
|---|---|
| `mr_history` (ประวัติ + CSV) | ✅ |
| `mr_lot_rows` (หน้าล็อต) | ✅ |
| `mr_summary` (หน้าสรุป) | ✅ |
| `mr_top_returned` (ยาที่คืนบ่อย) | ✅ |
| **`mr_lot_log`** (ประวัติการแก้ท้ายหน้าล็อต) | ⬜ **จุดเดียวที่เหลือ** |

🔑 **บทเรียน** — งานที่ฝากข้ามแชทมา ต้องตรวจของจริงก่อนลงมือเสมอ
ถ้าเชื่อคำสั่งแล้วแก้ตามเลย จะไปเขียนทับของที่ทำถูกอยู่แล้วทั้ง 5 จุด

---

## 🔧 จุดที่ปิดในรอบนี้

**ติดตรงที่** ตาราง `mr_lot_audit` ไม่มี `drug_id` จึงหาในคลังไม่ได้

### `scripts/018_lot_audit_drug_id.sql` (รันบนฐานแล้ว)

- เพิ่มคอลัมน์ `drug_id` — **ปล่อยว่างได้ ไม่กระทบโค้ดเก่าระหว่างรอ deploy**
- `mr_lot_log` join `drugs` แล้วคืน `mr_drug_label(d, a.drug_name)`
- เผื่อคำสั่งเติมย้อนหลังจาก `return_id` ไว้ด้วย (ตอนนี้ไม่มีแถวที่เข้าเงื่อนไข)

🚨 **ปลอดภัยกับลำดับปล่อยงานที่แตะฐาน** — คอลัมน์ใหม่ว่างได้ และฟังก์ชันคืนชื่อช่องเดิม
โค้ดที่ยังไม่ได้ deploy จึงทำงานต่อได้ตามปกติ ไม่เข้าข่ายกรณีที่เคยทำเว็บจริงล่ม 40 นาที

### `app/api/lots/[lot]/route.js` บรรทัด 187 · 198

ส่ง `drug_id` ไปด้วยตอนจดบันทึกการแก้จำนวนกับสถานะ

### ไม่ต้องเติมข้อมูลย้อนหลัง

**ตรวจฐานก่อนตัดสินใจ** — 4 แถวที่มีอยู่เป็นการแก้ระดับล็อตทั้งหมด (แหล่งที่มา · วันที่)
ไม่ผูกกับยาตัวไหน `return_id` กับ `drug_name` จึงว่างอยู่แล้วโดยการออกแบบ
ส่วนการแก้รายแถวที่มีชื่อยา **ยังไม่เคยเกิดขึ้นเลยสักครั้ง**

---

## ✅ พิสูจน์จากฐานจริง

**251 แถวที่ยังไม่ถูกลบ มีรหัสยาครบทุกแถว และชื่อในคลังต่างจากชื่อที่แช่ไว้ทั้ง 251 แถว**

```
ชื่อที่แช่ไว้ตอนบันทึก              ชื่อที่หน้าจอแสดงตอนนี้
Domperidone 10 mg              →  Domperidone 10 mg tab
Sodium bicarbonate 300 mg      →  Sodium bicarbonate (NaHCO3) 300 mg tab
Paracetamol + Orphenadrine ... →  ... tab (Norgesic)
```

**ตรงกับที่พี่กันบอกเป๊ะ** — เติมคำให้ครบ ไม่ได้เปลี่ยนตัวยา

**มูลค่ารวมไม่ขยับ** เพราะคิดจากราคาที่แช่ไว้ ไม่เกี่ยวกับชื่อ
นำกลับใช้ **28,331.50** · ทำลาย **660.00** · รวม **28,991.50**

---

## ⚠️ ข้อที่ทำไม่ได้ ต้องพี่กันทำเอง

**เปิดดูในโครมจริง** — เปิด dev server ที่ `localhost:3000` ให้แล้ว
**แต่ติดหน้ากรอกรหัสผ่านห้องยา ซึ่งแคลร์กรอกรหัสเองไม่ได้**

พี่กันเข้าไปดูได้เลยที่หน้าล็อต (กล่องประวัติการแก้ท้ายหน้า) กับไฟล์ CSV

---

## 📝 เอกสารกับสกิล

| ไฟล์ | แก้อะไร |
|---|---|
| `lib/drugName.js` บรรทัด 69-79 | คอมเมนต์เดิมบอกว่า *"แถวที่บันทึกไปแล้วยังเป็นชื่อเดิม"* — ไม่จริงมานานแล้ว เขียนใหม่ |
| `CLAUDE.md` ข้อ 3.78 | บันทึกรอบนี้ทั้งชุด |
| สกิล `pharmacy-web-logic` | สถานะข้อ 9.1 เป็น ✅ + แก้ 2 ข้อความที่ล้าสมัย |

🚨 **พี่กันสั่งห้ามเขียนสกิลใหม่ในรอบนี้** — แตะเฉพาะช่องสถานะกับข้อความที่ผิดความจริง
ไม่ได้เพิ่มกฎหรือหัวข้อใหม่เลยสักบรรทัด

---

## 📤 push รอบนี้ · v0.19.3.0

| repo | ผล |
|---|---|
| **มูลค่ายาคืน** | `74cfec8` → **`3a9ae61`** · bump เป็น v0.19.3.0 (แก้ครบ 2 จุด) |
| DM Remission · TB Dashboard · Word Bank | ซิงก์สกิล repo ละ 1 commit |
| **HCV · ME-DRP** | **ไม่แตะ ตามที่พี่กันสั่ง** |

ทุกตัวตรวจซ้ำหลัง push ได้ `0/0` คือตรงกับ remote จริง
