# ผลกวาดหากฎที่ยังไม่ขึ้นสกิลกลาง — เอเจนต์ 2 (session เว็บมูลค่ายาคืน)

> ตรวจ 5 ก.ย. 2569 · **เสร็จแล้ว** — อ่านครบ 31 ไฟล์ · เจอ 132 รายการ (สรุปรวมอยู่ท้ายไฟล์)

อ่านสกิลกลางครบ 12 ตัวก่อนเริ่ม (`working-with-gun` + `SCOLDING-LOG` · `pharmacy-web-logic` · `drug-display` ·
`search-system` · `web-craft` · `medical-data-safety` · `button-feedback` · `loading-skeleton` ·
`mobile-web-app` · `audit-report` · `making-documents` · `image-viewer`)

ไล่ไฟล์ `docs/SESSION-*.md` จากใหม่ไปเก่า 31 ไฟล์

---

## SESSION-2026-09-04-part2.md (v0.19.0.0 · ตารางจริงทั้งเว็บ)

| # | กฎ/บทเรียนที่เจอ | ยกข้อความจริงมา | ควรไปสกิลไหน | ทำไมถึงควรขึ้นสกิล |
|---|---|---|---|---|
| 1 | เทสตารางต้องครบ 3 สภาพ ไม่ใช่แค่เปิดดูในเบราว์เซอร์ | *"🔑 **เทสตารางต้องครบ 3 อย่าง** ข้อมูลเยอะพอให้เลื่อน · เลื่อนจริง · ย่อจอ"* และ *"แคลร์เปิดดูในโครมจริงแล้วทั้งคู่ แต่ดูในสภาพที่บั๊กไม่โผล่"* (ตารางที่ดูมี 2 แถว · จอกว้าง 1517 อย่างเดียว) | **web-craft** (ต่อท้าย 6.1/6.2 เรื่องตาราง) | ตรวจแล้วสกิลไม่มีคำว่า "ย่อจอ" นอกจาก image-viewer · เว็บไหนมีตารางก็เจอบั๊กหัวลอย/ตารางทะลุกรอบแบบเดียวกันหมด |
| 2 | ย่อหน้าต่างด้วยเครื่องมือเบราว์เซอร์ไม่ได้ผล ต้องบีบความกว้างพื้นที่เลื่อนด้วยสคริปต์ | *"⚠️ **ย่อหน้าต่างโครมด้วย `resize_window` ไม่ได้ผล** เพราะแผงที่แคลร์คุมขนาดคงที่ ใช้วิธีบีบความกว้างพื้นที่เลื่อนด้วยสคริปต์แทน ซึ่งจับบั๊กได้จริง"* | **web-craft** หรือ **working-with-gun** (หัวข้อวิธีเทส) | grep `resize_window` ในสกิลทั้งหมด = ไม่เจอเลย · เป็นข้อจำกัดของเครื่องมือ ไม่ใช่ของโปรเจกต์ ใช้กับทุกเว็บ |
| 3 | ขอบเขตที่เจ้าของงานระบุชัด ชนะกฎ "อะไรคล้ายกันทำให้เหมือนกัน" | *"🔑 **กฎ "อะไรคล้ายกันทำให้เหมือนกัน" ใช้กับของที่พี่กันไม่ได้ระบุขอบเขต** พอพี่กันบอกขอบเขตชัด ("ตารางนี้") กฎนั้นไม่ใช้ — คำสั่งที่ชัดชนะกฎทั่วไปเสมอ"* · ต้นเหตุ: *"เราบอก ชื่อเต็ม เฉพาะตารางนี้โว้ย"* | **working-with-gun** (ข้อ 3 กฎทอง ข้อ 10 มีกฎ "อะไรที่คล้ายกัน ต้องทำให้เหมือนกัน" แต่**ไม่มีข้อยกเว้นนี้**) | ตอนนี้สกิลเขียนกฎ "ทำให้เหมือนกัน" ไว้เฉย ๆ ไม่มีเงื่อนไขจำกัด → เอเจนต์ตัวอื่นจะเอาไปอ้างแล้วทำเกินคำสั่งเหมือนกันอีก |
| 4 | คำสั่งที่ชี้กลับไปข้อตกลงเดิม ต้องอ่านทั้งประโยค ห้ามอ่านแค่ท่อนแรก | *"② ตีความคำสั่งผิด — พี่กันเขียน *"เอาแค่ชื่ออย่างเดียวตามที่เราตกลง"* แคลร์อ่านแค่ท่อนแรกแล้วตัดคำนำหน้าออก ทั้งที่ "ตามที่เราตกลง" ชี้กลับไปที่คำสั่งแรก"* | **working-with-gun** (ข้อ 3 กฎทอง / ข้อ 7 ข้อผิดพลาดห้ามซ้ำ) | เป็นวิธีอ่านคำสั่ง ไม่ผูกโปรเจกต์ · เกิดกับทุกงานที่มีคำสั่งต่อเนื่องหลายรอบ |
| 5 | ห้ามใช้ตัวแปรชื่อภาษาไทยใน bash (สร้างโฟลเดอร์ขยะ) | *"③ **ใช้ตัวแปรชื่อไทยใน bash** สร้างโฟลเดอร์ขยะ (พลาดซ้ำจากรอบเช้า)"* | **working-with-gun** (ข้อ 5 ไฟล์+เครื่องมือ) | grep "ชื่อไทยใน bash" ในสกิล = ไม่เจอ · เป็นกับดักของเครื่องมือ เกิดได้ทุกโปรเจกต์ และพลาดซ้ำแล้ว 2 รอบในวันเดียว |
| 6 | คอมเมนต์ JSX วางผิดที่ทำเว็บพัง — เพิ่มอีก 2 รูปแบบที่สกิลยังไม่ได้ระบุ | *"\| 1 \| ระหว่าง attribute ของ `<th>` \| ย้ายออกไปเหนือแท็ก \|"* · *"\| 2 \| นอกบล็อก JSX (ก่อน element แรก) \| ใช้ `//` ไม่ใช่ `{/* */}` \|"* | **button-feedback** บรรทัด 372 มีกฎนี้แล้ว แต่ยังไม่ครบ 2 กรณีนี้ · ควรย้ายไป **web-craft** ด้วยเพราะไม่ใช่เรื่องปุ่ม | กฎมีอยู่แล้วแต่**อยู่ผิดสกิล** (อยู่ในสกิลปุ่ม) คนทำตารางจะไม่เปิดอ่าน · และวันนี้พลาดซ้ำ 2 รอบทั้งที่เขียนเตือนไว้เอง |

**ไม่ควรขึ้นสกิล (เฉพาะโปรเจกต์):** ตาราง 4 หน้ากี่คอลัมน์ · ชื่อไฟล์ `.tbl` ในเว็บนี้ · รายการบั๊ก 8 ตัว · ค่า `min-width:fit-content` ของหน้าคลังยา

---

## SESSION-2026-09-04.md (v0.18.0.0 · ช่องค้นหา + หัวเว็บเป็นตัวกลาง)

| # | กฎ/บทเรียนที่เจอ | ยกข้อความจริงมา | ควรไปสกิลไหน | ทำไมถึงควรขึ้นสกิล |
|---|---|---|---|---|
| 7 | 🔴🔴 ตัวตรวจที่วัดคนละสภาพกับที่เจ้าของงานดู = ตัวตรวจที่โกหก | *"**ตัวตรวจที่วัดคนละสภาพ = ตัวตรวจที่โกหก** ต้องเทสตามลำดับที่พี่กันเล่าเป๊ะ ๆ เสมอ"* · ตารางเทียบ *"แคลร์ เดินจากหน้าบันทึก (ข้อมูลโหลดมาแล้ว) แล้วกดสรุป / พี่กัน **รีเฟรชสด** แล้วกดสรุปทันที"* และ *"ไม่มีแถบเลื่อนกินที่ / ดูใน F12 ซึ่งมีแถบเลื่อนแบบเดสก์ท็อป"* | **working-with-gun** (ข้อ 4.8 "เทสด้วยการอ่านโค้ด" มีแล้ว แต่ยังไม่มีข้อนี้) | grep "คนละสภาพ" · "ตัวตรวจที่โกหก" ในสกิลทั้งหมด = **ไม่เจอเลย** · เป็นวิธีเทส ใช้ได้ทุกเว็บ และเป็นเหตุที่รายงานผิดไป 2 รอบ |
| 8 | แก้ไฟล์ที่มีทั้งฝั่งคอมและฝั่งมือถือในไฟล์เดียว ห้ามค้นด้วยข้อความ ต้องจำกัดช่วงบรรทัดก่อน | *"ใช้การค้นข้อความเพื่อระบุจุดแทรก **แต่ข้อความชุดเดียวกันมีทั้งฝั่งคอมและฝั่งมือถือ** ตัวแรกที่เจอคือฝั่งคอม โค้ดจึงลงผิดฝั่ง"* · *"คืนไฟล์ด้วย `git checkout` แล้วทำใหม่โดยอ้างเลขบรรทัดในช่วงของฟังก์ชันมือถือเท่านั้น"* | **mobile-web-app** (หัวข้อ "แยกไฟล์มือถือ" · กฎ 3 ข้อ) | สกิล mobile-web-app บอกให้แยกไฟล์แล้ว แต่ไฟล์ `pages/*.jsx` ยังมี 2 ฟังก์ชันในไฟล์เดียว (Narrow/Wide) ทุกโปรเจกต์ · grep "ห้ามค้นด้วยข้อความ" = ไม่เจอ · พลาดซ้ำ 2 รอบ และเป็นการแตะฝั่งที่ห้ามแตะ |
| 9 | กับดักเครื่องมือ 4 ข้อที่ไม่ผูกกับโปรเจกต์ | *"- **ไฟล์ในโปรเจกต์เป็น CRLF** ต้องแปลงเป็น LF ก่อนเทียบข้อความ แล้วแปลงกลับตอนเขียน"* · *"- **`/tmp` ใช้กับ node ในเครื่องนี้ไม่ได้** (กลายเป็น `C:\tmp`) วางสคริปต์ชั่วคราวใน `scripts/`"* · *"- **heredoc ใน Bash กลืน backslash** และตัดที่ 10KB"* · *"- **หน้าเว็บ 500 หลายรอบ** จาก `</div>` ขาดหรือเกิน · นับสมดุลด้วย node ก่อนเสมอ"* | **working-with-gun** (ข้อ 5 ไฟล์+เครื่องมือ) | grep `CRLF` · `heredoc`(เรื่อง backslash) · `C:\tmp` = **ไม่เจอ** (ที่มีคือเรื่อง `sed` กับ `\U` คนละเรื่อง) · เป็นข้อจำกัดของเครื่องพี่กัน ไม่ใช่ของเว็บยาคืน เจอซ้ำหลายโปรเจกต์แล้ว (ME-DRP · HCV) |
| 10 | ตัวตรวจต้องหาปุ่มด้วย `aria-label` ไม่ใช่ข้อความบนปุ่ม | *"`ui-check.mjs` หาปุ่มด้วยข้อความ พอปุ่มเปลี่ยนชื่อก็กดไม่ติด แล้ว**ถ่ายหน้าผิดมาให้เงียบ ๆ** · ตอนนี้หาด้วย `aria-label`"* · และ *"`tap-check.mjs` ไม่ตั้งชื่อเครื่องก่อนวัด หน้าต่างถามชื่อเครื่องบังจอ แล้วไปวัดปุ่มในนั้น ฟ้องว่าพื้นที่กดทับกัน 3 คู่ทั้งที่โค้ดถูก"* | **working-with-gun** (หัวข้อเครื่องมือตรวจ) หรือ **audit-report** ข้อ 11 | สกิลมีกฎ "ตัวตรวจที่ฟ้องผิดแย่กว่าไม่มีตัวตรวจ" แล้ว (button-feedback) แต่**ไม่มีวิธีป้องกัน** · ข้อนี้เป็นวิธีเขียนตัวตรวจให้ไม่โกหก ใช้กับทุกเว็บที่มีสคริปต์เทส |
| 11 | พื้นปุ่มต้องระบุสีตรง ๆ ห้ามปล่อยโปร่งใส | *"🚨 **พื้นปุ่มต้องระบุ `#fff` ตรง ๆ** ปล่อยโปร่งใสแล้วสีที่ตาเห็นขึ้นกับพื้นของหน้านั้น หน้าสรุปไม่มีแถบขาวรอง ปุ่มเลยกลายเป็นเทา"* (พี่กันสั่ง *"เอาสีเหมือนกันสิ สีขาว"*) | **button-feedback** | สกิล button-feedback คุมเรื่องสีปุ่มตอนชี้ครบแล้ว แต่**ไม่มีกฎเรื่องสีพื้นตอนปกติ** · ปุ่มเดียวกันที่ใช้หลายหน้าจะเปลี่ยนสีเองตามพื้นหลัง = ปัญหาของทุกเว็บที่มีตัวกลางวาดปุ่ม |
| 12 | `scrollbar-gutter: stable` — จองที่ให้แถบเลื่อนเสมอ เพื่อให้ที่พี่กันเปิด F12 ดูตรงกับมือถือจริง | *"🚨 **`scrollbar-gutter: stable`** จองที่ให้แถบเลื่อนเสมอ มือถือจริงใช้แถบลอยทับจึงไม่เสียที่สักจุด (วัดแล้ว 0 ทั้งสองสภาพ) แต่ตอนพี่กันเปิด F12 ดูงานจะเห็นตรงกัน"* | **mobile-web-app** | grep `scrollbar-gutter` = ไม่เจอในสกิลเลย · เป็นค่า CSS จริงพร้อมเหตุผล ยกไปใช้ได้ทันทีทุกเว็บมือถือ · แก้ปัญหา "แคลร์เห็นไม่ตรงกับที่พี่กันเห็นใน F12" ซึ่งเป็นต้นเหตุของข้อ 7 |
| 13 | เขียนกฎไว้เองแล้วโค้ดจริงไม่ได้ทำตาม — ต้องไล่ตรวจว่าโค้ดทำตามกฎที่จดไว้จริงไหม | *"⚠️ **CLAUDE.md ข้อ 3.60 เขียนเตือนไว้เองแล้ว** ว่าปุ่มปีงบ "ไม่ต้องรอเซิร์ฟเวอร์" **แต่โค้ดจริงรออยู่**"* · และท้ายไฟล์ *"**เขียนกฎแล้วไม่ได้แปลว่าจะทำตาม** — ตระกูลเดียวกับข้อ 3.44 · 3.72 · 3.74"* | **working-with-gun** (ข้อ 4.8 เช็คลิสต์อ่านโค้ด) | เกิดซ้ำแล้ว **4 ครั้ง** ในโปรเจกต์เดียว (3.44 · 3.72 · 3.74 · 3.60) และเกิดที่ ME-DRP ด้วย · เป็นบทเรียนวิธีทำงาน ไม่ผูกโปรเจกต์ · grep "เขียนกฎแล้ว" = ไม่เจอในสกิล |

**มีในสกิลแล้ว (ตรวจจริง ไม่ต้องยก):** ตัวอักษรใน `<input>` ลอยไม่ตรงกับชั้นวาด → `search-system` ข้อ 12 · ทุกช่องต้องเรียกตัวกลางตัวเดียว + ไอคอนแว่นขยาย → `search-system` ข้อ 7/8 · ให้ดูภาพไม่ใช่โค้ด → `web-craft` 6.3 บทเรียนที่แพงที่สุด

**ไม่ควรขึ้นสกิล:** `HEAD_PAD` ของเว็บนี้ · `fyPicks` · เลข commit · FLOW push 5 ขั้น (อยู่ในสกิลแล้ว ข้อ 4.1)

---

## SESSION-2026-09-02-part4.md (v0.17.2.0 · ไม่มีอะไรพังเงียบ + บัตรผ่านต่ออายุ)

| # | กฎ/บทเรียนที่เจอ | ยกข้อความจริงมา | ควรไปสกิลไหน | ทำไมถึงควรขึ้นสกิล |
|---|---|---|---|---|
| 14 | 🔴🔴 **"สกิลกลาง คือกฎ"** — ทุกอย่างที่อยู่ในสกิลคือกฎ ไม่ว่าเขียนในรูปแบบไหน | *"แคลร์เจอเรื่องหนึ่งในสกิลแล้วรายงานว่า "อยู่ในสกิลจริง แต่เขียนไว้เป็นบันทึกว่า ME-DRP ทำอะไร ไม่ได้เป็นกฎกลาง" พี่กันตอบว่า **"จริง ๆ สกิลกลาง คือกฎนะ 555"**"* · *"เจอของที่เว็บอื่นทำแล้วเว็บนี้ยังไม่ทำ = ต้องทำ ไม่ใช่รอให้มีหัวข้อกฎเขียนกำกับ"* | **working-with-gun** (ควรอยู่บนสุด ข้อ 5.4 สกิลกลาง) | grep "สกิลกลาง คือกฎ" ในสกิลทั้ง 12 ตัว = **ไม่เจอเลย** · เป็นคำสั่งตรงจากพี่กันที่กำหนดวิธีอ่านสกิลทั้งระบบ ถ้าไม่มีข้อนี้ เอเจนต์จะข้ามตารางเช็ค/บันทึกในสกิลว่า "ไม่ใช่กฎ" อีก |
| 15 | แตะสกิลเมื่อไหร่ต้องซิงก์สำเนาในโปรเจกต์**ทุกไฟล์** ไม่ใช่แค่ไฟล์ที่แก้ | *"⚠️ **พบว่าสำเนาในโปรเจกต์ล้าหลังอีกครั้ง** — `SCOLDING-LOG.md` ขาดไป 64 บรรทัด (บันทึกวันที่ 3 ก.ย. จาก HCV) · **ต้องซิงก์ทุกครั้งที่แตะสกิล ไม่ใช่แค่ไฟล์ที่แก้เอง**"* | **working-with-gun** ข้อ 5.4 (มีกฎ "แก้ที่สำเนาอย่างเดียว = ของหาย" แล้ว แต่**ทิศตรงข้าม**ยังไม่มี) | สกิลคุมกรณี "แก้สำเนาแล้วลืมแก้ต้นฉบับ" แต่ไม่คุมกรณี "แก้ต้นฉบับแล้วสำเนาในโปรเจกต์อื่นล้าหลัง" · เป็นเหตุที่กฎใหม่ไปไม่ถึงเว็บอื่น = ปัญหาเดียวกับที่กำลังตรวจอยู่นี้ |
| 16 | ตัวตรวจขึ้น "ตก" ต้องอ่านสาเหตุก่อน อย่าเพิ่งไปแก้โค้ด | *"⚠️ `delete-who-check` เคยขึ้นตกครั้งหนึ่ง **เพราะเซิร์ฟเวอร์ทดสอบดับ ไม่ใช่โค้ดพัง** (`ERR_CONNECTION_REFUSED`) · เห็นตกแล้วต้องอ่านสาเหตุก่อน อย่าเพิ่งไปแก้โค้ด"* | **working-with-gun** (ข้อ 4.8 / หัวข้อเครื่องมือตรวจ) | grep "ตัวตรวจตก" = ไม่เจอ · เป็นวิธีอ่านผลตัวตรวจ ใช้ได้ทุกโปรเจกต์ที่มีสคริปต์เทส · ถ้าไม่มีข้อนี้จะไปแก้โค้ดที่ไม่ได้พัง |
| 17 | 🟠 สีในสกิล `drug-display` ยังขาด "พื้นหลังตัวย่อสีม่วงอ่อน" | *"**ที่สกิลยังไม่มี** … **พื้นหลังตัวย่อ** ม่วงอ่อน — สกิลบอกแค่สีตัวอักษร"* | **drug-display** (หัวข้อ "สีที่ใช้ (ห้ามสลับ)") | ตรวจสกิลจริงแล้ว: โหมดมืดกับสีเม็ดยา**ถูกเพิ่มเข้าสกิลแล้ว** แต่ grep "พื้นหลังตัวย่อ" = ยังไม่เจอ · เป็น 1 ใน 3 เรื่องที่รายงานว่า "สกิลตามไม่ทัน" แล้วเก็บไม่ครบ |
| 18 | สกิลอาจ**เขียนผิด**ได้ ไม่ใช่แค่ขาด — ต้องตรวจสกิลเทียบโค้ดจริงเป็นรอบ ๆ | *"**ที่สกิลเขียนผิดแล้ว** — สีหน่วยนับ ถูกเปลี่ยนทั้งเว็บไปแล้วตอนแก้เรื่องตัวหนังสือจางเกินอ่านยาก และตัววาดชื่อยาก็ไม่ได้วาดบรรทัดล่างนั้นแล้วด้วย"* | **working-with-gun** ข้อ 5.4 หรือ **audit-report** | เป็นบทเรียนวิธีดูแลสกิล — ตระกูลเดียวกับ HCV *"รายงานที่ไม่ตรวจซ้ำจะล้าสมัยโดยไม่มีใครรู้"* · ตอนนี้ไม่มีกฎว่าต้องทวนสกิลเทียบของจริง |

**มีในสกิลแล้ว (ตรวจจริง):** รหัส Postgres 5 ตัวห้ามปิดบัง → `pharmacy-web-logic` บรรทัด 1020 · ฝั่งเบราว์เซอร์ต้องร้อง (`clientLog`) → `pharmacy-web-logic` ข้อ 31 อย่างที่ 5 · บัตรผ่านนับอายุจากครั้งสุดท้ายที่ใช้ → `pharmacy-web-logic` ข้อ 24 · catch ที่เงียบได้ถูกต้อง 3 จุด → ข้อ 31 · ตัวตรวจฟ้องผิดแย่กว่าไม่มีตัวตรวจ → `pharmacy-web-logic` + `button-feedback`

**ไม่ควรขึ้นสกิล:** ชื่อไฟล์ `lib/maskHn.js` ของเว็บนี้ · ผลตัวตรวจกี่ข้อผ่าน · รายการงานค้างของเว็บอื่น

---

## SESSION-2026-09-02-part3.md (v0.17.1.1 · ชื่อเว็บไม่เหลื่อม + ปุ่มกดไม่ได้บอกเหตุผล)

| # | กฎ/บทเรียนที่เจอ | ยกข้อความจริงมา | ควรไปสกิลไหน | ทำไมถึงควรขึ้นสกิล |
|---|---|---|---|---|
| 19 | 🔴 ปุ่มที่กดไม่ได้ ต้องบอก**เหตุผล**บนตัวปุ่มเอง และห้ามโชว์ตัวเลขที่คำนวณจากค่าที่เกินเพดาน | *"🚨 **ตัวเลขหลักพันล้านสะดุดตากว่าบรรทัดเตือนสีแดงเสียอีก** คนเห็นแวบแรกตกใจว่าระบบคิดเงินผิด ทั้งที่เป็นยอดที่ไม่มีวันบันทึกได้จริง"* · *"🚨 **ต้องเช็ค `qty > MAX_QTY` ทั้ง `sheetCta` และ `sheetValueLabel`** ซ่อนแค่ยอดแล้วปุ่มจะเหลือคำว่า "เพิ่ม" ลอย ๆ **ไม่บอกว่าทำไมกดไม่ได้**"* | **button-feedback** (หัวข้อ "🚫 ปุ่มที่กดไม่ได้") | อ่านสกิลจริงแล้ว หัวข้อนั้น**พูดแค่เรื่องสีตอนชี้** ไม่มีเรื่องข้อความบนปุ่มเลย · พี่กันเลือก "แบบ ก" ด้วยตัวเอง = คำตัดสินที่ควรเป็นกฎกลาง · ทุกเว็บที่มีเพดานค่ากรอกจะเจอเรื่องนี้ |
| 20 | ระยะขอบติดลบดึงตัวถัดไปเข้ามาทับ — ต้องถอดทั้งระยะขอบขวาและระยะในขวาพร้อมกัน | *"**ระยะขอบติดลบดึงตัวถัดไปเข้ามาในระยะในของตัวเอง** กรอบจึงยื่นไปทับตัวถัดไป **เท่ากับเลขที่ติดลบเสมอ**"* · *"🚨 **ต้องถอดทั้งระยะขอบขวาและระยะในขวาพร้อมกัน** ความกว้างของกล่องรวมระยะขอบจึงเท่าเดิมเป๊ะ"* · *"⚠️ **ถอดแค่ระยะขอบอย่างเดียวไม่ได้** จะดันช่องวันที่กับปุ่มไอคอนไปทางขวา 7 จุด"* | **web-craft** (หัวข้อกับดัก CSS ข้อ 6) | สกิลมีคำว่า "ระยะขอบติดลบ" อยู่ที่เดียวคือ web-craft:197 ซึ่งเป็น**วิธีใช้ให้ถูก**ในแถบตรึง ไม่ใช่**กับดัก**ตัวนี้ · เป็นกฎ CSS ล้วน มีค่าจริงและวิธีแก้ที่ไม่ทำให้อะไรขยับ ยกไปใช้ได้ทุกเว็บ |
| 21 | ถามว่า "ออกมายังไง" = ถ่ายภาพ ห้ามอธิบายเป็นตัวหนังสือ | *"🔑 **บทเรียนของรอบนี้ — พี่กันถามว่า "ออกมายังไง" ให้ถ่ายภาพ อย่าอธิบาย**"* · *"พอถ่ายภาพมาแล้วพี่กันตัดสินใจได้ในข้อความเดียว ไม่ต้องถามกลับไปมา"* | **working-with-gun** (ข้อ 10 "อ้างว่าทำแล้ว ต้องแนบภาพครอปเฉพาะจุด") | ข้อ 10 คุมกรณี "อ้างว่าทำแล้ว" · ข้อนี้คุมกรณี **"ถามล่วงหน้าว่าจะออกมายังไง"** ซึ่งเป็นคนละจังหวะ (ก่อนทำ vs หลังทำ) · เป็นวิธีสื่อสาร ใช้ได้ทุกโปรเจกต์ |
| 22 | เปิดหน้าต่างซ้อนเพื่อถ่ายภาพ ให้ตั้ง state ตรง ๆ ผ่าน React fiber ไม่ต้องไล่กดทีละขั้น | *"ตั้งค่าโดยตรงผ่าน React fiber เชื่อถือได้กว่าการหาช่องกรอกแล้วกด"* พร้อมโค้ด `app.setState({ sheet: {...}, sheetQty: '3000000000' })` · *"⚠️ **ช่องค้นยาฝั่งมือถือหาด้วย `placeholder` ไม่ได้** — ตัวอักษรโปร่งใสจากระบบแก้วรรณยุกต์"* | **search-system** (ข้อ 11 วรรณยุกต์ — เตือนผลข้างเคียงตอนเทส) + **working-with-gun** (วิธีถ่ายภาพ) | ข้อจำกัด "หาช่องด้วย placeholder ไม่ได้" เป็น**ผลข้างเคียงตรง**ของโครงสามชั้นที่สกิล `search-system` สั่งให้ทำ · เว็บไหนทำตามสกิลข้อ 11 จะเจอทันที แต่สกิลไม่ได้เตือนไว้ |
| 23 | สคริปต์ชั่วคราวต้องวางในโปรเจกต์ ไม่ใช่โฟลเดอร์ชั่วคราวนอกโปรเจกต์ · และต้องลบทิ้ง | *"🚨 **สคริปต์ชั่วคราวต้องวางใน `scripts/` ไม่ใช่โฟลเดอร์ชั่วคราวนอกโปรเจกต์** `puppeteer-core` หาไม่เจอเพราะ node ไล่หา `node_modules` จากที่อยู่ของไฟล์ ไม่ใช่จากที่รัน · **ลบทิ้งหลังใช้เสร็จทุกครั้ง**"* | **working-with-gun** (ข้อ 5 ไฟล์+เครื่องมือ) | grep `puppeteer-core` · "สคริปต์ชั่วคราว" ในสกิล = **ไม่เจอ** · ต่อยอดจากข้อ 9 (เรื่อง `/tmp` กลายเป็น `C:\tmp`) · ทุกโปรเจกต์ที่ใช้สคริปต์เทสเจอเหมือนกัน |

**ไม่ควรขึ้นสกิล:** ตัวเลข 52–160/155–280 ของหน้านี้ · ชื่อฟังก์ชัน `sheetCta` · ผลตัวตรวจ · งานค้างของเว็บอื่น

---

## SESSION-2026-09-02-part2.md (v0.17.1.0 · เก็บกวาด 4 เรื่องค้าง)

| # | กฎ/บทเรียนที่เจอ | ยกข้อความจริงมา | ควรไปสกิลไหน | ทำไมถึงควรขึ้นสกิล |
|---|---|---|---|---|
| 24 | 🔴 ก่อนขยายปุ่มให้ถึงเกณฑ์ 44px ต้องอ่านคอมเมนต์ในไฟล์ก่อนว่าที่เล็กอยู่นั้นเจ้าของงานสั่งเอง | *"**บทเรียน:** ก่อนขยายปุ่มให้ถึงเกณฑ์ ต้องอ่านคอมเมนต์ในไฟล์ก่อนว่าที่เล็กอยู่นั้นตั้งใจหรือเปล่า **ตัวตรวจไม่รู้ว่าอะไรคือคำสั่งของพี่กัน**"* · ตัวอย่าง *"ชิปแหล่งที่มา (66×30) พี่กันสั่ง *"บีบตรงนี้โว้ย"* จาก 44 เหลือ 30"* | **mobile-web-app** (หัวข้อ "พื้นที่กดทับกัน" · ต่อจากทางแก้ที่ถูก) | อ่านสกิลจริงแล้ว หัวข้อนั้นบอกแต่ *"ทางแก้ที่ถูก — ขยายตัวปุ่มเองให้ถึง 44px"* **ไม่มีข้อยกเว้นเลย** · ทำตามสกิลตรง ๆ = ไปแก้ปุ่มที่พี่กันสั่งให้เล็กเอง แล้วโดนด่า · ทุกเว็บมีปุ่มที่พี่กันสั่งเองทั้งนั้น |
| 25 | `min-width`/`min-height` ชนะ `width`/`height` ที่เขียนฝังในแท็ก — แก้จุดเดียวได้ทั้งเว็บ | *"**วิธี — คลาสเดียวในไฟล์มือถือ** `.mrv-hit` กับ `.mrv-hit-input` `min-width`/`min-height` ชนะ `width`/`height` ที่เขียนฝังในแท็ก จึงไม่ต้องไล่แก้ทีละจุด"* · และ *"🚨 **ช่องกรอกใช้คลาสแยก ห้ามใส่ `display:flex` ให้ `<input>`**"* | **mobile-web-app** (หัวข้อ "พื้นที่กดทับกัน") | เป็นเทคนิค CSS ที่ยกไปใช้ได้ทันทีทุกเว็บที่มีสไตล์ฝังในแท็ก (มอคอัปทุกอันเป็นแบบนี้) · grep "min-width.*ชนะ" ในสกิล = ไม่เจอ · สกิลบอกให้แก้ทีละปุ่มซึ่งช้ากว่ามาก |
| 26 | กับดัก 4 ข้อของสคริปต์เทสที่ต้องล็อกอินก่อน | *"**① เปิดตัวดักคำขอตั้งแต่แรก แล้วล็อกอินไม่ผ่าน** `setRequestInterception(true)` ก่อนเข้าสู่ระบบทำให้คุกกี้ไม่ติด"* · *"**② ตัดสินว่าล็อกอินสำเร็จจาก URL แล้วผิด** คุกกี้ถูกตั้งจาก `/api/auth` ไม่ใช่จากการเปลี่ยนหน้า"* · *"**③ เดาชื่อเมธอดเอง** — `app.pickScreen` ไม่มีจริง"* · *"**④ ลืมล้างชื่อผู้บันทึกก่อนเทสด่าน** … **ตัวทดสอบฟ้องว่าโค้ดพัง ทั้งที่โค้ดถูก**"* | **working-with-gun** (หัวข้อเครื่องมือตรวจ) หรือสกิลใหม่เรื่องเขียนสคริปต์เทส | grep `setRequestInterception` · "เดาชื่อเมธอด" = **ไม่เจอในสกิลเลย** · เว็บทุกตัวของพี่กันมีประตูรหัสผ่านแล้ว (ยาคืน · ME-DRP · HCV) สคริปต์เทสทุกตัวต้องล็อกอินก่อน = เจอ 4 ข้อนี้เหมือนกันหมด |
| 27 | ยกแพตเทิร์นที่เจ้าของงานเคาะหน้าตาแล้วมาทั้งดุ้น ห้ามออกแบบใหม่ | *"🚨 **ยกแพตเทิร์นจากหน้าต่างแก้ไขล็อตทั้งดุ้น ไม่ได้ออกแบบใหม่** — พี่กันเคาะหน้าตานั้นไปแล้ว"* · และ *"🚨 **ป้องกันสองชั้น** ปุ่มปิดไว้ (ตาเห็น) + `confirmRun` ตีกลับอีกชั้น"* | **working-with-gun** (ข้อ 3 กฎทอง) หรือ **web-craft** 6.6 | สกิล web-craft 6.6 ข้อ 8 มี *"มอคอัปที่พี่กันเคาะแล้ว = สัญญา ทำตามเป๊ะ"* แต่**ไม่ครอบคลุมกรณี "ทำของใหม่ที่ทำหน้าที่คล้ายของเดิมที่เคาะแล้ว"** · เป็นวิธีทำงานที่ประหยัดรอบถาม ใช้ได้ทุกโปรเจกต์ |
| 28 | 🔴 กฎที่เขียนไว้แล้วไม่ได้แปลว่าของทั้งหมดทำตาม ต้อง**วัดจริง**เสมอ | *"ตรงกับกฎข้อ 3.55 ที่เขียนเตือนไว้เองว่าปุ่มคู่ห่างน้อยกว่า 22px ห้ามใช้ `.tap` **แต่ปุ่มคู่นี้หลุดรอดมา** — กฎที่เขียนไว้แล้วไม่ได้แปลว่าของทั้งหมดทำตาม ต้องวัดจริงเสมอ"* | **working-with-gun** (ข้อ 4.8 เช็คลิสต์อ่านโค้ด) — **เรื่องเดียวกับข้อ 13** ในรายงานนี้ | ยืนยันซ้ำอีกรอบว่าเป็นรูปแบบความผิดที่เกิดซ้ำ ไม่ใช่อุบัติเหตุครั้งเดียว · เจอในไฟล์นี้ + 09-04 + 3.44 + 3.72 + 3.74 = **5 ครั้ง** |

**มีในสกิลแล้ว (ตรวจจริง):** ตารางเช็คล้าสมัย ห้ามเชื่อโดยไม่ตรวจโค้ด → `pharmacy-web-logic:120` · `drug-display:28` · `search-system:53` (ข้อนี้ถูกยกขึ้นสกิลเรียบร้อยแล้ว) · `overscroll-behavior:contain` → `mobile-web-app:174` · คอมเมนต์ JSX → `button-feedback:372` (ดูข้อ 6) · heredoc กลืน backslash → **ยังไม่มี ดูข้อ 9**

**ไม่ควรขึ้นสกิล:** ตารางสถานะรายเว็บ (ME-DRP เหลือ 3 ข้อ) · ชื่อคลาส `.mrv-hit` · ผลตัวตรวจ

---

## SESSION-2026-09-02.md (v0.17.0.0 · บันทึกของเซิร์ฟเวอร์ครบตามสกิลข้อ 31)

> รอบนี้ **เนื้อหาส่วนใหญ่ถูกยกขึ้นสกิลไปแล้ว** (ตรวจจริง — `pharmacy-web-logic` ข้อ 31 มีทั้งเซิร์ฟเวอร์ปลอม
> บรรทัด 954 · คืนค่าเดิม 964 · "ลืมเรียก" 923 · `api-fail-proof` 925) เหลือ 3 เรื่องที่ยังไม่ขึ้น

| # | กฎ/บทเรียนที่เจอ | ยกข้อความจริงมา | ควรไปสกิลไหน | ทำไมถึงควรขึ้นสกิล |
|---|---|---|---|---|
| 29 | ตัวอย่างโค้ดในสกิลกลางเองก็ผิดได้ — เจอแล้วต้องแก้**ต้นฉบับ**ทันที ไม่ใช่แก้แต่ในเว็บตัวเอง | *"**ตัวอย่างโค้ดในสกิลกลางเขียนผิดแบบเดียวกัน** แก้ต้นฉบับให้แล้ว ไม่งั้น DM Remission กับเว็บถัดไปลอกบั๊กไปใช้"* | **working-with-gun** ข้อ 5.4 (การดูแลสกิล) | grep "ลอกบั๊ก" · "ตัวอย่างโค้ดในสกิล" = **ไม่เจอ** · เป็นกฎการดูแลระบบสกิลโดยตรง และตรงกับหัวใจของงานตรวจครั้งนี้ (กฎที่ไม่ไปถึงเว็บอื่น) · ต่อยอดจากข้อ 18 |
| 30 | สกิลเขียนนามสกุลไฟล์เป็น `.ts` — ต้องแปลงตามภาษาของโปรเจกต์ ไม่ใช่ลอกทั้งดุ้น | *"ทำเป็น **`.js` ไม่ใช่ `.ts`** ตามที่สกิลเขียนไว้ เพราะโปรเจกต์นี้เป็น JavaScript ล้วน `jsconfig.json` ไม่มีไฟล์ `.ts` เลยสักไฟล์ · ใส่ `.ts` แล้ว build พัง"* | **pharmacy-web-logic** (ข้อ 31 อย่างที่ 1-2 · หัวไฟล์) | ตรวจแล้วสกิลยังเขียน `lib/apiError.ts` · `lib/maskHn.ts` ตรง ๆ ไม่มีหมายเหตุ · เว็บของพี่กันมีทั้ง TypeScript (ME-DRP · HCV · DM) และ JavaScript ล้วน (ยาคืน) → เว็บ JS ตัวถัดไปที่ทำตามสกิลจะ **build พัง** เหมือนกัน |
| 31 | ก่อนเปิดบันทึกของเซิร์ฟเวอร์ ต้องตรวจก่อนว่าฐานมีข้อมูลระบุตัวคนไหม แล้วไล่ชื่อช่องออกมา | คำสั่งพี่กันคำต่อคำ *"ก่อนเริ่ม ตรวจให้เราก่อนว่าเว็บนี้มีข้อมูลคนไข้ในฐานไหม (HN หรือเลขอะไรที่ระบุตัวคนได้) ถ้ามี ห้ามข้ามข้อ 2 เด็ดขาด และห้ามให้เราเปิดสวิตช์ก่อนที่ข้อ 2 จะเสร็จ"* · ผลตรวจไล่ช่อง *"`mr_return.hn` · `mr_draft.hn` · `recorded_by` · `deleted_by` · `failed_by` · `price_fixed_by` · `changed_by`"* | **pharmacy-web-logic** ข้อ 31 (เป็นขั้นตอนที่ 0 ก่อนอย่างที่ 1) | สกิลบรรทัด 1037 เขียนแค่ *"เว็บที่มี HN ต้องทำตัวปิดบังก่อนเปิดสวิตช์เสมอ"* แต่**ไม่มีวิธีตรวจ** และ**ไม่ได้บอกว่าชื่อคนก็ระบุตัวได้** · พี่กันสั่งเป็นเงื่อนไขบังคับด้วยตัวเอง = ควรเป็นกฎ · DM Remission ที่ยังไม่ทำจะต้องใช้ทันที |

**ไม่ควรขึ้นสกิล:** จำนวนแถวในฐานของเว็บนี้ · รายชื่อ 17 ไฟล์ที่แก้ · ตารางเช็ครายเว็บ (อยู่ในสกิลแล้ว)

---

## SESSION-2026-09-01-part5.md (v0.16.3.0 · กฎตารางจริง + เปิดเป็นแอป + ป๊อปเลื่อนขึ้น)

| # | กฎ/บทเรียนที่เจอ | ยกข้อความจริงมา | ควรไปสกิลไหน | ทำไมถึงควรขึ้นสกิล |
|---|---|---|---|---|
| 32 | 🔴🔴 **การแอบตัดค่าที่เกินเพดานอันตรายกว่าไม่กั้น** — ต้องบอกให้เห็น ห้าม `Math.min` เงียบ ๆ | พี่กันเจอเอง *"เราเพิ่งรู้ว่า เงินมันไม่เกิน 1 แสนเหรอ ทำไมแฮะ"* · *"ของเดิม `Math.min(MAX_QTY, n)` ตัดให้เงียบ ๆ โดยไม่บอกอะไรเลย"* · *"🚨 **การแอบตัดค่าอันตรายกว่าไม่กั้น** เพราะคนกรอกอาจไม่ทันสังเกตว่าเลขเปลี่ยน แล้วแถวนั้นถูกแช่ราคาไว้ ตัวเลขในรายงานผู้บริหารจะผิดโดยไม่มีอะไรเตือน"* | **web-craft** หรือ **pharmacy-web-logic** (กฎเรื่องข้อมูลที่เปลี่ยนเองโดยผู้ใช้ไม่รู้) | grep `Math.min` · "แอบตัด" ในสกิลทั้ง 12 ตัว = **ไม่เจอเลย** · ทุกเว็บที่มีช่องกรอกตัวเลขมีเพดาน · ตระกูลเดียวกับกฎ "ห้ามพังเงียบ" ที่สกิลให้ความสำคัญมาก · พี่กันเลือกทางแก้เองแล้ว (แบบ ก) |
| 33 | 🔴 `{...kb(...)}` ต้องวางก่อน `role=` เสมอ ไม่งั้นทับ `role="dialog"` แล้วระบบกันเลื่อนพัง | พี่กันเจอเอง *"ทำไมเราสโครเมาส์ลงไม่ได้วะ"* · *"`kb()` … **คืน `role="button"` มาด้วย** และถูกวางไว้ **หลัง** `role="dialog"` ทั้งแท็กจึงกลายเป็น button"* · *"🚨 **`{...kb(...)}` ต้องวางก่อน `role=` เสมอ**"* | **web-craft** ข้อ 6.5 (ที่นิยาม `kb()` และ `role="dialog"` อยู่ด้วยกันพอดี) | สกิล web-craft 6.5 สอนทั้ง `kb()` และตาราง `role=` **ในหัวข้อเดียวกัน** แต่ไม่เตือนว่าสองอย่างนี้ชนกัน · เว็บไหนทำตามสกิลทั้งสองข้อจะเจอบั๊กนี้แน่นอน และ**หาต้นเหตุยากมาก** (อาการคือหน้าต่างเลื่อนไม่ได้ ไม่ใช่ปุ่มพัง) |
| 34 | ฉากหลังห้ามเลื่อนบนมือถือ — ดัก `touchmove` ยังไม่พอ ต้องตรึง `body` ด้วย `position:fixed` และจำตำแหน่งเลื่อนไว้คืน | พี่กันเจอเอง *"ทำไมเราเปิด popup แล้วเราสามารถเลื่อนเพจลงล่างได้"* · *"ดักเหตุการณ์ `touchmove` อย่างเดียวไม่พอ เบราว์เซอร์บนมือถือส่งการเลื่อนต่อให้หน้าเว็บทั้งใบได้อยู่ดี → เพิ่ม `_lockBody()` ตรึงด้วย `position:fixed`"* · *"🚨 **ต้องจำตำแหน่งที่เลื่อนค้างไว้แล้วคืนตอนปิด** ไม่งั้นเด้งกลับบนสุด"* · *"🚨 **ทำเฉพาะฝั่งมือถือ**"* | **web-craft** ข้อ 6.4 "ฉากหลังห้ามเลื่อน" · และ **mobile-web-app** | สกิล web-craft 6.4 หยุดอยู่แค่ *"ปิด `overflow` อย่างเดียวพอสำหรับคอม แต่มือถือไม่พอ … ต้องห้ามที่เหตุการณ์ด้วย"* — **ซึ่งพิสูจน์แล้วว่ายังไม่พอ** · สกิลบอกทางแก้ที่ไม่ครบ = เว็บที่ทำตามจะยังมีบั๊กและคิดว่าทำครบแล้ว |
| 35 | ป๊อปฝั่งมือถือต้องเลื่อนขึ้นจากขอบล่าง พร้อมค่าจังหวะที่พี่กันเคาะแล้ว | *"**จังหวะ 0.42 วินาที · `cubic-bezier(.16, 1.16, .3, 1)`** ค่า 1.16 ที่เกิน 1 คือตัวที่ทำให้เลยจุดหมายนิดหนึ่งแล้วค่อยกลับมา"* · *"🚨 **ใส่แอนิเมชันที่กล่องขาวข้างใน ไม่ใช่ที่ฉากซ้อน** ป๊อปใส่จำนวนขยับตามความสูงแป้นพิมพ์อยู่แล้ว (`--kb`) ถ้าใส่ที่ฉากซ้อนจะทับกันจนกระตุก"* | **mobile-web-app** (หัวข้อ "ท่ามาตรฐานที่ทำให้รู้สึกเป็นแอป") | สกิล `web-craft` มีแค่ชั้น `sheet: 20` ในตาราง z-index ไม่มีหน้าตาหรือจังหวะเลย · `mobile-web-app` มี 2 ท่าคุ้มที่สุด (ดึงลงโหลดใหม่ · View Transitions) แต่ไม่มีแผ่นเลื่อนขึ้น · **เป็นค่าที่พี่กันเคาะจากมอคอัป 3 แบบ = สัญญา** ยกไปใช้ได้ทันที |
| 36 | เล่นแอนิเมชันซ้ำต้องบังคับคำนวณใหม่ด้วย `void el.offsetWidth` คั่น | *"⚠️ **มอคอัปที่กด "ลองอีกครั้ง" แล้วไม่เล่น** — ถอดคลาสแล้วใส่กลับในเฟรมเดียวกัน เบราว์เซอร์มองว่าไม่มีอะไรเปลี่ยน ต้องบังคับให้คำนวณใหม่ด้วย `void el.offsetWidth` คั่น"* | **button-feedback** (หัวข้อแอนิเมชัน) หรือ **web-craft** | กับดักเบราว์เซอร์ล้วน ๆ · สกิล `button-feedback` มีหัวข้อแอนิเมชันเต็ม ๆ พร้อม `@keyframes` แต่ไม่มีข้อนี้ · เจอทุกครั้งที่ทำมอคอัปที่มีปุ่ม "ลองอีกครั้ง" ซึ่งสกิล web-craft 6.6 สั่งให้ทำมอคอัปทุกครั้งอยู่แล้ว |
| 37 | 🟠 ห้ามใช้ `width` คงที่กับปุ่มที่มีข้อความ · ข้อความที่ขาดกลางให้แยกก้อนตามจุดคั่นแล้ว `nowrap` · กล่องขาวที่มีมุมมนต้องมี `overflow:hidden` | *"🚨 **ห้ามใช้ `width` คงที่กับปุ่มที่มีข้อความ** มันบีบจนตกบรรทัดเป็นแนวตั้ง"* · *""เหลืออีก 7 วัน" ขาดกลาง — แยกเป็นก้อนตามจุดคั่น แต่ละก้อน `nowrap`"* · *"มุมมนไม่ครบสี่มุม — กล่องขาวขาด `overflow:hidden` เนื้อหาเลยทับมุม"* | **button-feedback** (ปุ่ม) · **web-craft** (กล่อง/ข้อความ) | เรื่อง `width` คงที่**อยู่ใน `SCOLDING-LOG.md:1415` แล้ว** แต่**อยู่ผิดที่** — บันทึกคำด่าไม่ใช่ที่ที่คนทำปุ่มจะเปิดหา (ตระกูลเดียวกับข้อ 6) · ส่วนอีก 2 เรื่องยัง grep ไม่เจอที่ไหนเลย · `drug-display` ข้อ 27 มีกฎ "ก้อนห้ามขาดกลาง" แต่จำกัดเฉพาะชื่อยา ทั้งที่ใช้กับข้อความอะไรก็ได้ |

**มีในสกิลแล้ว (ตรวจจริง):** ตารางต้องเป็น `<table>` จริง → `web-craft` 6.1 (ยกขึ้นแล้ว ✅) · เปิดเป็นแอปบนหน้าจอโฮม + iOS ไม่รับ SVG + maskable 20% + short_name 11 ตัว → `mobile-web-app` หัวข้อ 📲 (ยกขึ้นแล้ว ✅) · `prefers-reduced-motion` → `button-feedback`

**ไม่ควรขึ้นสกิล:** สถานะตาราง 3 หน้าของเว็บนี้ · ชื่อไฟล์ `pages/confirm.jsx` · เลข commit

---

## SESSION-2026-09-01-part4.md (v0.16.2.0 · วรรณยุกต์ไทย + สามช่องบังคับ + ฟอนต์ Krub)

> เรื่องวรรณยุกต์ไทยทั้งก้อน (ต้นเหตุ · ตารางวิธีที่ลองแล้วไม่ได้ผล 8 แถว · โครงสามชั้น ·
> `pointer-events:none` · ระยะบรรทัด 1.5 เท่า) **ยกขึ้นสกิล `search-system` ข้อ 11-12 เรียบร้อยแล้ว**
> บทเรียนคำด่า 9 ข้อก็อยู่ใน `working-with-gun` หัวข้อ 7 + `SCOLDING-LOG` แล้ว (ตรวจจริง)

| # | กฎ/บทเรียนที่เจอ | ยกข้อความจริงมา | ควรไปสกิลไหน | ทำไมถึงควรขึ้นสกิล |
|---|---|---|---|---|
| 38 | 🔴 เปลี่ยนฟอนต์แล้วต้องไล่ระยะทุกจุดที่เคยเผื่อไว้ให้ฟอนต์เก่า | *"🚨 **เปลี่ยนฟอนต์แล้วต้องไล่ระยะทุกจุดที่เคยเผื่อไว้ให้ฟอนต์เก่า** ระยะบรรทัด 1.95 กับที่ว่างใต้ตัวอักษร 8-10 จุด เคยเผื่อไว้ให้หางลากยาวของ Charmonman Krub ไม่มีหาง ที่ว่างเลยกลายเป็นช่องว่างเปล่าดันชื่อเว็บลอยขึ้นจนไม่ตรงกลางกับโลโก้"* · พี่กันทัก *"ชื่อเว็บ ไม่เห้นกลางเลยยย ฟ้อนมันเปลี่ยนก็ต้องปรับตามฟ้อนสิ"* | **web-craft** (หัวข้อฟอนต์) | grep "เปลี่ยนฟอนต์แล้ว" · "ไล่ระยะ" = **ไม่เจอในสกิล** · พี่กันเปลี่ยนฟอนต์ชื่อเว็บมาแล้ว 4 ตัว (Charm → Thasadith → Charmonman → Krub) และเปลี่ยนฟอนต์ทั้งเว็บอีก 2 รอบ · เป็นงานที่เกิดซ้ำแน่นอน · ตระกูลเดียวกับกฎ Roboto Mono กว้างกว่า 15% ที่ก็ยังไม่ขึ้นสกิลเหมือนกัน |
| 39 | ลูกศร ▾ ของช่องเลือกต้องลอยทับขอบขวา ไม่กินที่ในแถว | *"🚨 **ลูกศร ▾ ของช่องผู้บันทึกต้องลอยทับขอบขวา** ไม่ใช่กินที่ในแถว ไม่งั้นข้อความที่จัดกึ่งกลางถูกดันไปทางซ้ายเท่าความกว้างลูกศร"* | **web-craft** (ช่องกรอก/ช่องเลือก) | grep "ลูกศร ▾" · `appearance:none` = ไม่เจอในสกิลเลย · ทุกเว็บมีดรอปดาวน์ที่วาดลูกศรเอง (CLAUDE.md ข้อ 3.52 เตือนเรื่อง `background:#fff` รวบล้างลูกศรไปแล้วอีกเรื่อง) · เป็นค่า CSS ยกไปใช้ได้ |
| 40 | สคริปต์ถ่ายภาพต้องรอ**สิ่งที่ต้องการโผล่จริง** ไม่ใช่รอเป็นเวลาตายตัว | *"⚠️ **สคริปต์ถ่ายภาพต้องรอช่องรหัสโผล่จริง** ไม่ใช่รอเป็นเวลาตายตัว หน้าเข้าสู่ระบบโหลดฉากหลัง three.js (~600 KB) ช้ากว่า 900 มิลลิวินาทีบ่อย"* | **working-with-gun** (หัวข้อเครื่องมือตรวจ) — รวมกับข้อ 26 | ต่อยอดจากข้อ 26 (กับดักสคริปต์เทสที่ต้องล็อกอิน) · grep "รอเป็นเวลาตายตัว" = ไม่เจอ · เป็นเหตุที่ตัวตรวจฟ้องผิด ซึ่งสกิลบอกว่า "แย่กว่าไม่มีตัวตรวจ" แต่ไม่บอกวิธีเลี่ยง |
| 41 | 🟠 ตัวตรวจวรรณยุกต์เชื่อเป็นคำตอบสุดท้ายไม่ได้ — ทั้งฟ้องผิดและบอกผ่านผิด | *"⚠️ **ตัวตรวจเชื่อเป็นคำตอบสุดท้ายไม่ได้** — `thai-clip-check` เคยฟ้องผิดว่าชิป "เยี่ยมบ้าน" มีปัญหาทั้งที่ปกติ (พี่กันทัก) และเคยบอกว่าผ่านทั้งที่พี่กันเห็นว่าตัด"* | **search-system** ข้อ 11 (ท้ายหัวข้อวรรณยุกต์) | สกิล `search-system` มีคำสั่งตรวจระยะบรรทัดให้ (บรรทัด 528) แต่**ไม่เตือนว่าตัวตรวจนี้ผิดได้ทั้งสองทาง** · เว็บถัดไปที่ทำตามจะเชื่อผลตัวตรวจแล้วรายงานว่าผ่าน ทั้งที่พี่กันยังเห็นตัด = เข้าทางที่โดนด่ามาแล้ว 4 รอบ |

**มีในสกิลแล้ว (ตรวจจริง):** แก้เรื่องเดิมเกินสองรอบ = วิธีผิดทาง → `working-with-gun:809` · "เชื่อตาเรา" → `working-with-gun:802` · โครงสามชั้นวรรณยุกต์ + `pointer-events:none` → `search-system:377,396` · `width` คงที่กับปุ่ม → `SCOLDING-LOG:1415` (ดูข้อ 37 — อยู่ผิดที่)

**ไม่ควรขึ้นสกิล:** ตารางสามช่องบังคับของหน้าบันทึกมือถือ · ชื่อฟอนต์ Krub (เป็นการเลือกของโปรเจกต์) · ชื่อสคริปต์เฉพาะเว็บนี้

---

## SESSION-2026-09-01-part3.md (v0.16.1.0 · ชั้นหน้าต่างซ้อน + ฉากหลังห้ามเลื่อน)

> ตารางชั้น `Z` 9 ระดับ + ฉากหลังห้ามเลื่อน **ยกขึ้นสกิล `web-craft` ข้อ 6.4 แล้ว** (ตรวจจริง)

| # | กฎ/บทเรียนที่เจอ | ยกข้อความจริงมา | ควรไปสกิลไหน | ทำไมถึงควรขึ้นสกิล |
|---|---|---|---|---|
| 42 | 🔴 การป้องกันที่ผูกกับคลาสซึ่งใส่ตอน `componentDidMount` = ช่วงแรกหลังโหลดไม่มีการป้องกันเลย | *"ทุกอย่างที่ทำในรอบก่อน (ตัวดีดตำแหน่งเลื่อน · ตัวห้ามซูม · กฎ CSS) **เช็คคลาส `.mrv-mobile` ก่อนทำงาน** แต่คลาสนั้นถูกใส่ใน `componentDidMount` ซึ่งบนมือถือเกิดช้ากว่าหน้าโหลดเสร็จหลายวินาที — **ช่วงนั้นไม่มีการป้องกันอะไรเลย**"* · แก้ด้วย `_isNarrowNow()` (ด่านสำรองใช้ความกว้างจอ) + `_bindEarlyGuards()` ผูกตั้งแต่ `constructor` | **mobile-web-app** (หัวข้อ "แยกไฟล์มือถือ · กฎ 3 ข้อ") | สกิลบอกให้ตัดสินด้วยธง `wide` ห้ามใช้ `@media` (บรรทัด ~60) — **ซึ่งเป็นต้นเหตุของบั๊กนี้พอดี** · สกิลสั่งวิธีที่มีรูโหว่แล้วไม่บอกวิธีอุด · เว็บมือถือทุกตัวที่ทำตามสกิลจะไถลได้ในช่วงแรกหลังรีเฟรช (พี่กันเจอเอง) |
| 43 | `touch-action` ไม่สืบทอดลงไปยังลูก — ใส่ที่ `html`/`body` อย่างเดียวไม่พอ | *"**ต้นเหตุ — `touch-action` ไม่สืบทอดลงไปยังลูก** เบราว์เซอร์ดูค่าของ element ที่นิ้วแตะโดยตรง ใส่ที่ `html`/`body` อย่างเดียวจึงไม่พอ"* · แก้ *"`@media (max-width: 1179px) { html, body, body * { touch-action: pan-y !important } }` พร้อม `text-size-adjust: 100%` กัน iOS ขยายตัวอักษรเองในกล่องแคบ"* | **mobile-web-app** (หัวข้อห้ามซูม / หน้าไถลซ้ายขวา) | สกิลมี `touch-action: pan-y` อยู่ในตาราง "สิ่งที่ลองแล้วไม่พอ" (บรรทัด 533) แต่**ไม่ได้บอกว่าที่ไม่พอเพราะไม่สืบทอด** · เขียนแค่ "ไม่พอ" ทำให้เว็บถัดไปเลิกใช้ไปเลย ทั้งที่ใช้ถูกวิธีแล้วได้ผล · grep `text-size-adjust` = ไม่เจอเลย |
| 44 | React โหมดเข้มงวดสร้างคอมโพเนนต์สองรอบ — ตัวดักที่ผูกใน `constructor` ต้องมีธงกันผูกซ้ำ | *"🚨 **กันการผูกซ้ำด้วยธง `_earlyBound`** React โหมดเข้มงวดสร้างคอมโพเนนต์สองรอบตอนพัฒนา"* | **web-craft** (ทักษะ web dev) | grep `StrictMode` · "โหมดเข้มงวด" = **ไม่เจอในสกิลเลย** · ทุกโปรเจกต์ Next.js ของพี่กันเปิดโหมดนี้ตอนพัฒนา · อาการคือตัวดักทำงานสองรอบ ซึ่งหาสาเหตุยากเพราะเกิดเฉพาะตอน dev |
| 45 | เปลี่ยนหน้าต่างซ้อนเป็นหน้าเต็มจอ ต้องเป็น "แทนที่" ไม่ใช่ "วาดเพิ่ม" | *"🚨 **ต้องเป็น "แทนที่" ไม่ใช่ "วาดเพิ่ม"** วาดคู่กับหน้าบันทึกแล้วของสองหน้าอยู่ใน DOM พร้อมกัน ปุ่มซ้ำ (จับได้ตอนตัวตรวจนับชิปได้ 10 อันแทนที่จะเป็น 5)"* · *"🚨 **ต้องเอาออกจาก `anyModalOpen`** ไม่งั้นฉากหลังถูกล็อกทั้งที่ไม่มีอะไรซ้อน"* | **web-craft** ข้อ 6.4 (ต่อจากตารางชั้นหน้าต่าง) | สกิล 6.4 มีกฎ *"เพิ่มหน้าต่างใหม่ต้องเติมชื่อในตัวนับหน้าต่างที่เปิดอยู่ด้วย"* แต่**ไม่มีทิศตรงข้าม** (เอาหน้าต่างออกจากตัวนับ) · เป็นงานที่เกิดทุกครั้งที่พี่กันสั่งเปลี่ยนหน้าต่างซ้อนเป็นหน้าเต็มจอ ซึ่งเกิดแล้ว 2 เว็บ |
| 46 | ตัวตรวจต้องอัปเดตตามเมื่อของเปลี่ยนสถานะ · แทนที่บล็อกด้วยสคริปต์ต้องนับวงเล็บให้ครบ | *"🚨 **ตัวตรวจต้องอัปเดตตามเมื่อของเปลี่ยนสถานะ** `modal-check` ฟ้อง 4 ข้อเพราะยังทดสอบหน้าตั้งค่าด้วยกติกาของหน้าต่างซ้อน ทั้งที่มันเป็นหน้าเต็มจอไปแล้ว — **ตัวตรวจที่ฟ้องผิดทำให้เสียเวลาไล่หาบั๊กที่ไม่มี**"* · *"🚨 **แทนที่บล็อกด้วยสคริปต์ต้องนับวงเล็บปิดให้ครบ** … ทางที่ปลอดภัยกว่าคือ `git checkout` ไฟล์นั้นแล้วเขียนสคริปต์ใหม่ที่หาขอบเขตจากย่อหน้า"* | **working-with-gun** (เครื่องมือตรวจ + ไฟล์/เครื่องมือ) | รวมกับข้อ 10 · 16 · 26 · 40 ให้เป็นหัวข้อเดียว "กฎการเขียนและดูแลตัวตรวจ" · ตอนนี้บทเรียนเรื่องตัวตรวจกระจายอยู่ในสกิลปุ่มกับสกิลห้องยา ไม่มีที่รวม ทั้งที่เป็นเรื่องวิธีทำงาน |

**มีในสกิลแล้ว (ตรวจจริง):** ตารางชั้น `Z` + `closest('[role="dialog"]')` + "ปิด overflow พอสำหรับคอม มือถือไม่พอ" → `web-craft` 6.4 (แต่**ทางแก้ยังไม่ครบ ดูข้อ 34**) · `user-scalable=no` ผิด WCAG → `mobile-web-app:208` · คอมเมนต์ JSX → `button-feedback:372`

**ไม่ควรขึ้นสกิล:** ตารางเลข z-index เดิมของเว็บนี้ · รายการคำสั่งเก็บกวาดหน้ามือถือ · พอร์ตของวัน

---

## SESSION-2026-09-01-part2.md (v0.16.0.0 · ยกเครื่องหน้าจอมือถือ)

> ไฟล์นี้ **ยกขึ้นสกิลไปเกือบหมดแล้ว** — ตรวจจริง: `_pinLeft` (`mobile-web-app:544`) ·
> ตาราง "สิ่งที่ลองแล้วไม่พอ" (527) · "อะไรขยับ อะไรไม่ขยับ" (19 · 399 · 571) · ffmpeg (376) ·
> ป้ายเลขในชื่อเว็บ (24 · 29) · ปิดกลับ `127.0.0.1` (263) · เอเจนต์ต้องบันทึกลงไฟล์ (`working-with-gun` 4.5)

| # | กฎ/บทเรียนที่เจอ | ยกข้อความจริงมา | ควรไปสกิลไหน | ทำไมถึงควรขึ้นสกิล |
|---|---|---|---|---|
| 47 | 🔑 **คำถามของเจ้าของงานคือคำตอบ** — เขาชี้จุดถูกก่อนแคลร์เสมอ | *"### 🔑 บทเรียนที่ควรทำตั้งแต่รอบแรก — **คำถามของพี่กันคือคำตอบ**"* · *"คำถามนี้ชี้ตรงจุด — ในคลิป **แถบล่างนิ่งสนิทขณะที่ส่วนบนไถล** ถ้าทั้งหน้าถูกลาก แถบล่างต้องขยับด้วย พอมันไม่ขยับ = **พื้นที่เลื่อนตัวเดียวที่เลื่อนข้างได้**"* (คำถามพี่กัน: *"ทำไมกรอบเเสดงค่ายา เเละกรอบปุ่มกด … ทำไมมันไม่เลื่อน ทำไมมเอาระบบนั้นมาไม่ได้"*) | **working-with-gun** (ข้อ 3 กฎทอง หรือข้อ 3.5 กฎ 10 ข้อ) | grep "คำถามของพี่กันคือคำตอบ" = **ไม่เจอในสกิลเลย** · สกิลมีข้อ ๘ *"คำถามไม่ใช่คำสั่ง"* ซึ่งพูดคนละเรื่อง (เรื่องอย่าเพิ่งลงมือ) · ข้อนี้บอกว่า**คำถามคือเบาะแสวินิจฉัย** ต้องหยุดคิดตามก่อนตอบ · เสียเวลาไป 7 รอบเพราะข้ามข้อนี้ |
| 48 | 🟠 ตัวตรวจ "ตก" เพราะฐานไม่มีข้อมูล ไม่ใช่โค้ดพัง — ต้องแยกให้ออก | *"ตัวตรวจ `nav-check` ตก 2 ข้อ (ใบสรุป Lot · หน้าต่างแก้ล็อต) เพราะวันนี้ยังไม่มี Lot ในฐานเลยไม่มีปุ่มให้กด **ไม่ใช่บั๊ก**"* | **working-with-gun** (หัวข้อเครื่องมือตรวจ) — รวมกับข้อ 16 | เป็นตัวอย่างที่สองของ "ตัวตรวจตกโดยที่โค้ดไม่พัง" (ข้อ 16 คือเซิร์ฟเวอร์ดับ) · สองกรณีรวมกันเป็นกฎเดียวได้: **อ่านสาเหตุก่อนแก้โค้ด** · ทุกเว็บที่มีสคริปต์เทสที่พึ่งข้อมูลจริงจะเจอ |

**ไม่ควรขึ้นสกิล:** ตารางแผน 6 ชุดของเว็บนี้ · รายการงานที่พี่กันสั่งเพิ่ม · ชื่อสคริปต์ · พอร์ตของวัน

---

## SESSION-2026-09-01.md (v0.15.1.0 · บั๊กกดยืนยันบันทึกแล้วไม่บันทึก)

| # | กฎ/บทเรียนที่เจอ | ยกข้อความจริงมา | ควรไปสกิลไหน | ทำไมถึงควรขึ้นสกิล |
|---|---|---|---|---|
| 49 | 🔴🔴🔴 **ค่าที่ถูกใช้ข้ามหน้า ห้ามอยู่ในไฟล์ที่ถูกข้ามการคำนวณได้** | *"🚨 **ค่าที่ถูกใช้ข้ามหน้าห้ามอยู่ในไฟล์ที่ถูกข้ามการคำนวณได้** ถ้าจำเป็นต้องอยู่ ต้องเติมเงื่อนไขให้คำนวณตอนของนั้นถูกใช้ด้วยเสมอ"* · ผล: *"กระทบใคร **ทุกเครื่อง ทั้งคอมและมือถือ**"* · *"ติดมาตั้งแต่ **v0.12.0.0** — อยู่บนเว็บจริงมาหลายวันโดยไม่มีใครจับได้"* | **web-craft** (หัวข้อ Performance — ต่อจาก lazy render / useMemo) | สกิล `web-craft` สอนให้ทำ lazy render + `useMemo` เพื่อความเร็ว **แต่ไม่เตือนกับดักนี้เลย** · เว็บไหนทำตามคำแนะนำเรื่องความเร็วในสกิลจะสร้างบั๊กนี้ได้ทันที และเป็นบั๊กที่ **ทำให้บันทึกข้อมูลไม่ได้ทั้งเว็บ** |
| 50 | 🔴 บั๊กที่ "โค้ดไม่ได้พัง มันแค่ไม่มีอะไรให้เรียก" — เงียบที่สุด ไม่มีสัญญาณเตือนเลย | *"🚨🔴 **โค้ดไม่ได้พัง มันแค่ไม่มีอะไรให้เรียก** จึงไม่มีสัญญาณเตือนใด ๆ เลย ไม่มี error ไม่มี warning ไม่มีอะไรในคอนโซล — เป็นบั๊กที่เงียบที่สุดเท่าที่เจอมาในโปรเจกต์นี้"* | **working-with-gun** ข้อ 4.8 (เช็คลิสต์อ่านโค้ด — มีข้อ ③ *"ของที่สร้างไว้แต่ไม่มีใครเรียก"* แต่**ไม่มีทิศตรงข้าม**) | สกิลตรวจ "สร้างแล้วไม่มีใครเรียก" ได้ แต่ตรวจ **"เรียกแล้วไม่มีของ"** ไม่ได้ ซึ่งอันตรายกว่ามาก · ตระกูลเดียวกับ `pharmacy-web-logic:131` (สร้าง `SearchBox.tsx` แล้วไม่มีใครเรียก) ที่พิสูจน์แล้วว่าเกิดหลายเว็บ |
| 51 | 🔴 เทสด้วยการเรียกฟังก์ชัน/อ่าน state ผ่านหมด — ต้อง**กดปุ่มจริงทีละขั้นเหมือนคนใช้** | ตารางในบันทึก *"\| `setState` แล้วอ่าน state \| ❌ ผ่านหมด \|"* · *"\| เรียก `app.save()` ตรง ๆ \| ❌ ผ่าน (ข้ามป๊อปไปเลย) \|"* · *"\| **กดปุ่มจริงทีละขั้นเหมือนคนใช้** \| ✅ **เจอ** \|"* · และ *"**การไล่ดูด้วยตาไม่พอ ต้องเทสเส้นทางจริงทุกเส้นหลังตัดการคำนวณ**"* | **working-with-gun** ข้อ 4.8 (คู่กับ "เทสด้วยการอ่านโค้ด") | grep "กดปุ่มจริงทีละขั้น" = **ไม่เจอในสกิลเลย** ทั้งที่เป็นบทเรียนที่ถูกอ้างซ้ำในบันทึกอีก 3 ไฟล์ ("บทเรียนข้อ 3.66") · สกิลข้อ 4.8 สอนให้ **อ่านโค้ด** เพิ่มจากการยิงเทส — ข้อนี้เป็นขาที่สาม (กดจริง) ที่จับบั๊กที่อีกสองวิธีจับไม่ได้ |
| 52 | 🟠 ตัวตรวจต้องหาปุ่มด้วยชื่อที่ตรงกับของจริง ไม่ใช่ชื่อที่เดาเอง | *"🚨 **ชื่อปุ่มในป๊อปคือ "ยืนยันบันทึก" ไม่ใช่ "บันทึก"** ตัวตรวจที่หาผิดชื่อจะรายงานว่า "ไม่ยิงอะไรเลย" ทั้งที่แก้ถูกแล้ว — เสียเวลาไล่ผิดทางไปหนึ่งรอบ"* | **working-with-gun** (หัวข้อเครื่องมือตรวจ) — รวมกับข้อ 10 | ยืนยันซ้ำข้อ 10 (หาปุ่มด้วยข้อความแล้วพลาด → ต้องใช้ `aria-label`) · เกิดคนละรอบคนละสคริปต์ = เป็นรูปแบบ ไม่ใช่อุบัติเหตุ |
| 53 | 🟠 ห้ามเดาว่าข้อมูลในฐานที่ไม่รู้จักเป็นขยะจากการเทสของตัวเอง แล้วเสนอลบ | *"**เดาว่าแถวที่พี่กันบันทึกเองเป็นขยะจากการเทสของแคลร์** แล้วเสนอลบ"* · ความจริง *"แถว Metformin 20 เม็ด Lot L690901-01 — พี่กันทดสอบเอง จะลบเอง ไม่ใช่ขยะจากการเทสของแคลร์"* | **medical-data-safety** (ต่อจาก "ห้ามแก้ข้อมูลในฐานจริงโดยไม่ได้สั่ง") | สกิลคุมเรื่อง "ห้ามแก้/ห้ามคืนค่า" แล้ว แต่**ไม่คุมเรื่องการเดาว่าแถวไหนเป็นขยะ** · แคลร์เคยสร้างล็อตขยะเข้าฐานจริงจริง ๆ มาแล้ว 4 ล็อต (CLAUDE.md 3.63) จึงมีเหตุให้เดาแบบนี้ซ้ำ · ผลถ้าเดาผิดคือลบข้อมูลจริงของโรงพยาบาล |

**ไม่ควรขึ้นสกิล:** ตัวเลข 205→186px ของแถบบันทึกมือถือ · ชื่อสคริปต์ · เรื่องเข้าใจผิดเรื่องตัวกรอง "เดือนนี้"

---

## SESSION-2026-08-31-part3.md (v0.15.0.0 · เก็บกวาดหน้าบันทึก 8 เรื่อง + 3 สวิตช์ดูตัวอย่าง)

> ตรวจแล้วยกขึ้นสกิลไปหลายข้อ — "แตะหน้าจอต้องเปิดภาพดูก่อนรายงาน" (`mobile-web-app:264,592`) ·
> "งานแทรกไม่ได้ยกเลิกงานเก่า" (`working-with-gun:915-916`) · เปิดโครมตัวจริง (`working-with-gun` ข้อ 9)

| # | กฎ/บทเรียนที่เจอ | ยกข้อความจริงมา | ควรไปสกิลไหน | ทำไมถึงควรขึ้นสกิล |
|---|---|---|---|---|
| 54 | 🔴🔴 **กฎ "แตะหน้าจอต้องเปิดภาพดูก่อนรายงาน" ถูกเก็บไว้ในสกิลมือถืออย่างเดียว** ทั้งที่เกิดกับหน้าคอม | *"🚨 **กฎใหม่: แตะหน้าจอเมื่อไหร่ ต้องเปิดภาพดูก่อนรายงานเสมอ** ใช้ `computer screenshot` หรือ `zoom` ผ่าน Claude in Chrome การวัดค่าใช้ยืนยัน**หลัง**เห็นภาพแล้วว่าถูก ไม่ใช่ใช้แทนการดู"* · เคสจริงเป็น**หน้าบันทึกฝั่งคอม** *"กรอบตาราง 424px / เห็นยาแค่ **3 แถว**"* | **working-with-gun** (ข้อ 7 หรือ 4.8) — ตอนนี้อยู่ใน **mobile-web-app** เท่านั้น | ตรวจจริง: grep เจอเฉพาะ `mobile-web-app:264` และ `:592` · **แต่บั๊กทั้ง 4 ตัวในตารางนี้เป็นหน้าคอม** · คนแก้หน้าคอมจะไม่เปิดสกิลมือถืออ่าน = กฎไปไม่ถึง (ตรงกับปัญหาที่รายงานนี้ตามหาอยู่พอดี ตระกูลเดียวกับข้อ 6 · 37) |
| 55 | 🔴 ต้องเช็คสถานะอีกครั้ง **หลัง `await`** ก่อน `setState` — คำขอเก่าเดินทางกลับมาทับของใหม่ | *"🚨🔴 **ต้องเช็ค `app.state.demo` อีกครั้ง "หลัง await" ก่อน setState** คำขอที่ยิงไปก่อนเข้าโหมดตัวอย่าง เดินทางกลับมาทีหลังแล้วทับข้อมูลปลอมจนหายเกลี้ยง **เจอจากภาพถ่ายหน้าจอ ไม่ใช่จากสคริปต์เทส**"* | **web-craft** (ทักษะ web dev — ต่อจาก optimistic UI / กันคำตอบเก่าทับใหม่) | grep "หลัง await" = **ไม่เจอในสกิลเลย** · เป็นตระกูลเดียวกับกฎที่เว็บนี้เจอซ้ำ 3 รอบ (คำตอบเซิร์ฟเวอร์ทับสิ่งที่ผู้ใช้พิมพ์ 3.50 · คำตอบเก่าทับคำตอบใหม่ในหน้าประวัติ · ข้อนี้) · สกิลไม่มีกฎกลางของตระกูลนี้เลยแม้แต่ข้อเดียว |
| 56 | โหมดดูตัวอย่าง/ข้อมูลสาธิต ต้องเป็นของปลอม**ทั้งหมด** ทุกหน้า ไม่ใช่แค่หน้าที่นึกออก | *"🔴 **เดิมกดแท็บคลังยาในโหมดตัวอย่างแล้วโหลดยาจริง 417 ตัวพร้อมราคามาโชว์** `loadCatalog` กับ `loadPrices` ไม่รู้จักโหมดนี้เลย (ตัวกัน fetch ดักเฉพาะที่ไม่ใช่ GET)"* | **pharmacy-web-logic** หรือ **web-craft** | ตัวกัน GET/non-GET เป็นการออกแบบที่ดูปลอดภัยแต่มีรูโหว่ชัดเจน · ทุกเว็บที่มีโหมดสาธิตให้ผู้บริหารดู (ยาคืน มี · เว็บอื่นกำลังจะมี) เจอเหมือนกัน · grep "โหมดดูตัวอย่าง" ในสกิล = **ไม่เจอเลยสักตัว** |
| 57 | จอเตี้ยต้องบีบ**แถว** ไม่ใช่รีดช่องไฟ · และต้องวัดความสูงจอ ไม่ใช่แค่ความกว้าง | *"⚠️ **รีดช่องไฟรอบ ๆ ทีละ 5px ไม่ช่วยอะไร** ตัวที่กินที่จริงคือแถวยาที่คูณจำนวนแถว"* · *"🚨 ต้องวัด**ความสูงจอ**ด้วย ไม่ใช่แค่ความกว้าง"* · ผล *"จอ 577px: ตารางเห็นยาจาก 3 แถวเป็น 5 แถว"* | **web-craft** (ตาราง) หรือ **mobile-web-app** | grep "ความสูงจอ" = ไม่เจอ · สกิลทุกตัวตัดสินสภาพจอด้วย**ความกว้าง**อย่างเดียว (ธง `wide`) · โน้ตบุ๊กจอ 768px หรือหน้าต่างที่ย่อลง = จอเตี้ย ซึ่งคอมห้องยาเป็นแบบนั้น |
| 58 | 3 ทางที่ลองแล้วผิดสำหรับข้อความยาวเกินช่องที่ต้องเห็นท้ายสุด | ตาราง *"\| `direction:rtl` เพื่อให้เห็นท้ายสุด \| สลับลำดับ span → `90 15+15+15…` \|"* · *"\| `position:absolute` ตรึงผลลัพธ์ขวา \| ทับสูตรที่ไหลมาชนพอดี → `= 105·15+` \|"* · *"\| `text-overflow:ellipsis` \| ตัดตัวเลขที่พิมพ์มาทิ้ง เลื่อนดูไม่ได้ \|"* · ทางที่ถูก *"สูตรกับผลลัพธ์เรียงต่อกันตามปกติ (`flex:none` ทั้งคู่) แล้วเลื่อนดูเอา"* | **search-system** (ข้อ 11-12 โครงสามชั้น — เป็นท่าเดียวกัน) | โครงสามชั้นในสกิล `search-system` ใช้กับช่องนี้ด้วย (บันทึกบอกเอง *"ท่าเดียวกับช่องจำนวนฝั่งคอม"*) แต่สกิลไม่มีตาราง "ลองแล้วไม่ได้ผล" ของ**ข้อความยาวเกินช่อง** · ตารางแบบนี้คือของที่ประหยัดเวลาที่สุด สกิลอื่นก็ใช้รูปแบบนี้อยู่แล้ว |

**ไม่ควรขึ้นสกิล:** ชื่อไฟล์ `parkedsheet.jsx` · ค่า `qtyLong = 14 ตัวอักษร` ของเว็บนี้ · ตาราง 3 สวิตช์ดูตัวอย่าง · สถานะฐานข้อมูล

---

## SESSION-2026-08-31-part2.md (v0.13.0.0 · ส-7 ร่างแยกรายหน้าต่าง)

> ยกขึ้นสกิลแล้วเกือบครบ — ข้อเสนอต้องคิดจนจบ (`working-with-gun` ข้อ ๕ พร้อมคำพูดพี่กัน) ·
> มอคอัปต้องเป็นหน้าเว็บจริง + ทำเสร็จต้องส่งทันที (ข้อ ๖) · สคริปต์เทสห้ามแตะฐานจริง (ข้อ ๑ บรรทัด 146)

| # | กฎ/บทเรียนที่เจอ | ยกข้อความจริงมา | ควรไปสกิลไหน | ทำไมถึงควรขึ้นสกิล |
|---|---|---|---|---|
| 59 | 🔴 ร่าง/ของที่กรอกค้างต้องแยกราย**หน้าต่าง** ไม่ใช่รายเครื่อง — พร้อมกลไกทะเบียนหน้าต่างครบชุด | คำสั่งพี่กัน *"ไม่เอา ก ทำยังไงมาก็ได้ ที่กรอก 100 เครื่องก็ต้องแยกกัน และกรอกโครมเดียวกัน แต่คนละคนต้องแยกกัน **ให้ทุกอย่างมันเอกเทศกัน**"* · กลไก *"`myTabId()` · `draftKeyOf(id)` · `touchTab()` · `releaseTab()` · `tabAlive(id)` · `orphanDrafts()`"* · กฎ 4 ข้อ *"ห้ามแตะร่างของหน้าต่างที่ยังเปิดอยู่ … เอากลับมาแล้วต้องลบต้นทาง … มีของในมืออยู่แล้วห้ามดึงมาทับ"* | **pharmacy-web-logic** (ข้อ 30 ชุดรับมือเน็ตหลุด — ร่างเก็บในเครื่อง) หรือ **web-craft** | grep "รายหน้าต่าง" · `pagehide` = **ไม่เจอในสกิลเลย** · สกิล `pharmacy-web-logic` ข้อ 30 สั่งให้ทุกเว็บมี "ร่างเก็บในเครื่อง" — **ซึ่งเป็นของที่พังตอนเปิดหลายหน้าต่างพอดี** · เว็บอื่นที่ทำตามสกิลจะเจอบั๊ก "ยาหายเงียบ ๆ" และ "บันทึกซ้ำ มูลค่าถูกนับซ้ำ" เหมือนกันทุกตัว · คำสั่งพี่กันเป็น "ให้ทุกอย่างมันเอกเทศกัน" = เจตนากว้างกว่าเว็บเดียว |
| 60 | 🔴🔴 รีเฟรชกับทำสำเนาแท็บ **แยกไม่ออกจากมุมโค้ด** — ตัวแยกคือการปล่อยรหัสคืนตอน `pagehide` | *"ทั้งคู่คือ "มีรหัสเดิมใน sessionStorage และทะเบียนบอกว่ารหัสนั้นยังมีคนถืออยู่" สิ่งที่แยกสองอย่างนี้ออกจากกันคือ **หน้าต่างที่กำลังจะรีเฟรชปล่อยรหัสคืนก่อนหายไป**"* · *"ตอนแรกไม่มีการปล่อยคืน ทุกการรีเฟรชจึงได้รหัสใหม่ … = **กด F5 ทีของหายทั้งล็อต**"* | **web-craft** (ทักษะ web dev) — คู่กับข้อ 59 | เป็นความรู้เชิงกลไกเบราว์เซอร์ล้วน ๆ ไม่ผูกโปรเจกต์ · ถ้าไม่รู้ข้อนี้จะทำระบบแยกหน้าต่างแล้วได้บั๊ก "F5 ทีของหาย" ซึ่งร้ายแรงกว่าบั๊กเดิม |
| 61 | เครื่องมือทดสอบปิดหน้าต่างโดยไม่ยิง `pagehide` — สคริปต์เทสต้องจำลองเวลาผ่านไปเอง | *"⚠️ **เครื่องมือทดสอบปิดหน้าต่างโดยไม่ยิง `pagehide`** ทะเบียนจึงยังคิดว่าหน้าต่างนั้นเปิดอยู่ (เบราว์เซอร์จริงยิงเสมอ) สคริปต์เทสต้องจำลองว่าเวลาผ่านไปเกินเกณฑ์เอง"* | **working-with-gun** (หัวข้อเครื่องมือตรวจ) — รวมกับข้อ 26 · 40 · 46 | เป็นข้อจำกัดของ puppeteer ไม่ใช่ของเว็บนี้ · ต่อยอดชุดกับดักสคริปต์เทสที่กระจายอยู่หลายไฟล์ · ถ้าไม่รู้จะสรุปผิดว่าโค้ดพัง |
| 62 | 🟠 บั๊กหน้าตาเล็ก ๆ เห็นได้จากภาพเท่านั้น — ผลเทสไม่มีทางจับ | *"`money()` ใส่สัญลักษณ์บาทมาให้แล้ว แต่แคลร์เติมซ้ำอีกตัว แถบขึ้นว่า **80.00 ฿ ฿** เห็นตอนถ่ายภาพหน้าจอจริงมาดู — **ถ้าดูแต่ผลเทสจะไม่มีทางเจอ**"* | **working-with-gun** — หลักฐานเสริมของข้อ 54 | ตัวอย่างที่จับต้องได้ที่สุดของกฎ "ต้องเปิดภาพดูก่อนรายงาน" · เป็นเรื่องของ **ตัวช่วยกลางที่ใส่หน่วยมาให้แล้ว** ซึ่งทุกเว็บมี (`money()` · `thaiDate()`) |

**ไม่ควรขึ้นสกิล:** สถานะผลตรวจ 46 ข้อของเว็บนี้ · รายการงานค้าง · ชื่อสคริปต์ · รายการ 5 ข้อที่ให้พี่กันลองเทส

---

## SESSION-2026-08-31.md (v0.12.0.0 · กลุ่ม 3 ครบชุด — แคชแท็บ · อัปเดตสด · CSP)

**ไฟล์นี้เนื้อหนาที่สุดในบรรดาที่ยังไม่ขึ้นสกิล — เจอ 8 เรื่อง**

| # | กฎ/บทเรียนที่เจอ | ยกข้อความจริงมา | ควรไปสกิลไหน | ทำไมถึงควรขึ้นสกิล |
|---|---|---|---|---|
| 63 | 🔴🔴 **"โหลดไม่สำเร็จ" ต้องไม่หน้าตาเหมือน "ไม่มีข้อมูล"** และหน้าที่รายงานตัวเลขต้อง**ไม่วาดตัวเลขเลย** | *"**หน้าสรุปหนักสุด** — เดิมเซิร์ฟเวอร์ล่มแล้ววาด **฿0.00 ทุกช่อง** เงียบ ๆ ซึ่งเป็นหน้าที่เอาไปเสนอผู้บริหาร · หน้าประวัติขึ้น "ไม่พบรายการตามเงื่อนไขนี้" ทั้งที่เน็ตหลุด"* · *"🚨 **หน้าสรุปต้องไม่วาดตัวเลขเลย** ไม่ใช่แค่ขึ้นกล่องแจ้งไว้ข้างบน ตอนแรกแคลร์ทำแค่กล่องแจ้ง แล้วเทสจับได้ว่ายังเหลือ ฿0.00 อีก 3 จุดข้างล่าง ต้องกันทั้งฝั่งคอมและฝั่งมือถือ"* | **loading-skeleton** (เป็นสถานะที่สามคู่กับ "กำลังโหลด" และ "มีข้อมูล") | สกิล `loading-skeleton` คุมสถานะ "กำลังโหลด" กับ "ไม่มีข้อมูล" ครบ แต่**ไม่มีสถานะ "โหลดไม่สำเร็จ" เลย** · ฿0.00 ที่ค้างอยู่คือ **คำตอบที่ผิดในเอกสารเสนอผู้บริหาร** ซึ่งเป็นความเสียหายจริง · ทุกเว็บของพี่กันมีหน้าสรุป/แดชบอร์ด |
| 64 | 🔴 อัปเดตสดข้ามเครื่องด้วย "ลายเซ็นข้อมูล" — ต้องมี `updated_at` + trigger ในฐาน **ไม่ใช่แค่นับแถว** | *"🚨 **ต้องมีร่องรอยเวลาในฐาน ไม่ใช่แค่นับแถว** … การนับแถวจับได้แค่ **เพิ่ม** กับ **ลบ** — จับ "แก้จำนวน" หรือ "สลับใช้ต่อเป็นทำลาย" ไม่ได้เลย ซึ่งเป็นการแก้ที่กระทบมูลค่าโดยตรง"* · *"trigger ทำงานในฐานข้อมูล จึงจับได้แม้แก้ตรงในหน้า Supabase"* | **pharmacy-web-logic** (ข้อ 29 มีลายเซ็นคลังยาแล้ว — ข้อนี้คือฉบับทั่วไปสำหรับ**ข้อมูลทำรายการ**) | สกิลข้อ 29 มีลายเซ็นเฉพาะตาราง `drugs` (ใช้ `max(id)` ของ `drug_audit`) ซึ่ง**จับการแก้ค่าในแถวไม่ได้** · ข้อนี้เป็นรุ่นที่ครบกว่าและใช้กับตารางทำรายการของทุกเว็บ · เว็บอื่นยังไม่มีเลย |
| 65 | 🔴 ต้องแยกให้ออกว่าใครเป็นคนแก้ — และเผื่อเวลาต้อง**ไม่เท่ากับ**คาบของตัวจับเวลา | *"ถ้าไม่แยก กดบันทึกเองแล้วจะเด้งข้อความ "มีข้อมูลใหม่จากเครื่องอื่น" ทุกครั้ง"* · *"เผื่อเวลาไว้ **45 วินาที** ไม่ใช่ 20 เพราะตัวจับเวลาถามทุก 20 ถ้าเผื่อเท่ากันจะชนขอบพอดี แล้วเด้งผิดบ้างไม่ผิดบ้าง **ซึ่งเป็นบั๊กที่จับยากที่สุดแบบหนึ่ง**"* | **pharmacy-web-logic** หรือ **web-craft** (คู่กับข้อ 64) | ค่า 45 vs 20 พร้อมเหตุผลเป็นของที่ยกไปใช้ได้ทันที · บั๊ก "เด้งผิดบ้างไม่ผิดบ้าง" หาสาเหตุยากมาก เว็บถัดไปที่ทำระบบอัปเดตสดจะเสียเวลาซ้ำ |
| 66 | แท็บที่ซ่อนอยู่ไม่ต้องถามเซิร์ฟเวอร์ | *"🚨 **แท็บที่ซ่อนอยู่ไม่ถาม** คอมห้องยาเปิดเว็บค้างทั้งวันโดยไม่ได้มอง ถามตลอดจะได้คำขอเปล่าวันละสองพันกว่าครั้งต่อเครื่อง กลับมาที่แท็บเมื่อไหร่ถามทันที"* | **web-craft** (Performance / Realtime) | grep "แท็บที่ซ่อน" = ไม่เจอในสกิล · เป็นเงื่อนไขของสภาพใช้งานจริงในโรงพยาบาล (เปิดค้างทั้งวัน) ที่สกิล `web-craft` 6.6 ให้ความสำคัญอยู่แล้ว แต่ไม่มีข้อนี้ |
| 67 | 🔴🔴 **แตะ CSP เมื่อไหร่ ต้องเทส production build เสมอ** — หน้าที่ Next สร้างล่วงหน้าได้ HTML ไม่มีใบอนุญาต = จอขาวทั้งใบ | *"เทสใน dev ผ่านหมด แต่พอ build เป็นเว็บจริงแล้ว **สคริปต์ถูกบล็อกทั้งหมด จอขาวทั้งใบ**"* · *"**แก้** `export const dynamic = 'force-dynamic'` ทั้งสองหน้า · `app/page.js` ใส่ตรง ๆ · `app/login/` ต้องใส่ที่ `layout.js` (หน้านั้นเป็น client component)"* · *"🚨🔴 **แตะเรื่อง CSP เมื่อไหร่ ต้องเทส production build เสมอ** ตอนรันในเครื่องมี `'unsafe-eval'` อยู่ จึงไม่มีทางเจอปัญหานี้เลย"* | **web-craft** (Security + Deploy) | สกิลมีบรรทัด *"CSP headers (ไม่มี unsafe-eval)"* สั่งให้ทำ **แต่ไม่บอกว่าทำแล้วเว็บจริงจอขาว** · grep `force-dynamic` · `nonce` · "production build" = **ไม่เจอเลย** · เว็บถัดไปที่ทำตามสกิลจะพังบนเว็บจริงโดยไม่มีทางเจอตอน dev |
| 68 | 🔴 **วัดก่อนแก้เสมอ** — ผลตรวจบอกว่าอะไรหนัก แต่ไม่บอกว่าเวลาไปอยู่ที่ไหน | *"ผลตรวจข้อ ก-8 เขียนไว้ว่าหน้าคลังยาหนักที่สุด **วัดแล้วจริง** (1.81 ms) แต่ผลตรวจไม่ได้บอกว่า **95% ของเวลาอยู่ที่การวาด DOM ไม่ใช่การคำนวณ** ซึ่งเห็นได้จากการวัดเท่านั้น — ถ้าทำตามผลตรวจอย่างเดียวจะแก้ไม่ตรงจุด"* · *"มี 2 วิธีที่ลองแล้ววัดได้ว่าไม่ช่วย จึงถอยออก แทนที่จะเก็บไว้เพราะ "น่าจะดี""* พร้อมตัวเลข `content-visibility:auto` 78→76 ms · `table-layout:fixed` 78→78 ms | **web-craft** (Performance) · **audit-report** ข้อ 11 | grep "วัดก่อนแก้" = **ไม่เจอในสกิล** · สกิล `web-craft` มีรายการเทคนิคเร่งความเร็ว (lazy render · debounce · useMemo · virtual scroll) แบบ "ทำอันนี้สิ" โดยไม่มีขั้นตอนวัด · และมีตัวเลขจริงที่พิสูจน์ว่า 2 ใน 4 เทคนิคนั้นไม่ช่วย |
| 69 | 🔴🔴 **ห้ามแก้สคริปต์เทสให้ผ่านโดยไม่รู้ว่าทำไม** — รอบเดียวเจอวัดผิด 4 ครั้ง รวมทั้ง "ผ่านแบบหลอกตัวเอง" | *"รอบนี้เจอ 4 ครั้งที่สคริปต์รายงาน "ตก" ทั้งที่เว็บถูกต้อง"* พร้อมตาราง — *"\| รีเฟรชแล้วไม่โหลดซ้ำ (ผ่าน) \| **ผ่านแบบหลอกตัวเอง** — รีเฟรชแล้วเว็บเด้งกลับหน้าบันทึก ไม่ได้อยู่หน้าประวัติตั้งแต่แรก \|"* · *"ทุกครั้งต้องไล่หาสาเหตุจนเจอ **ห้ามแก้สคริปต์ให้ผ่านโดยไม่รู้ว่าทำไม**"* | **working-with-gun** (หัวข้อเครื่องมือตรวจ) — **หัวใจของชุดข้อ 10 · 16 · 26 · 40 · 46 · 48 · 52 · 61** | grep "ห้ามแก้สคริปต์ให้ผ่าน" = **ไม่เจอ** · นี่คือกฎแม่ของกับดักตัวตรวจทั้งหมดที่รายงานนี้เจอกระจายอยู่ 8 ไฟล์ · **"ผ่านแบบหลอกตัวเอง" อันตรายที่สุด** เพราะไม่มีใครไปตรวจข้อที่ผ่าน |
| 70 | 🟠 ธง/ค่าที่ใช้ตัดสินว่าจะคำนวณหน้าไหน ห้ามอยู่ในไฟล์ของหน้านั้นเอง (วงจรอุบาทว์) | *"`isPrices` `isLots` `isCatalog` เดิมอยู่ในไฟล์ของหน้าตัวเอง ซึ่งกลายเป็นวงจรอุบาทว์ทันทีที่ข้าม — **ไม่คำนวณหน้าราคา → ไม่มีธง → กดเข้าหน้าราคาแล้วจอว่างถาวร**"* · *"🚨 **หน้าที่ถูกข้ามได้ค่าเป็นว่างเปล่า ห้ามคืนค่าเก่าที่จำไว้** ธงอย่าง `slipOpen` จะค้างเป็นจริง แล้วใบสรุปจะเด้งขึ้นมาเอง"* | **web-craft** (Performance) — คู่กับข้อ 49 | เป็นกับดักคู่แฝดของข้อ 49 (ค่าที่ใช้ข้ามหน้า) · ทั้งสองข้อเกิดจากคำแนะนำเดียวกันในสกิล (lazy compute) · ถ้าจะเขียนกฎนี้ลงสกิลต้องเขียนคู่กันทั้งสองข้อ |

**มีในสกิลแล้ว (ตรวจจริง):** ที่เก็บของแท็บใช้ `sessionStorage` ลบเองตอนปิดแท็บ → `working-with-gun:727` · โหลดครั้งเดียว (load-once) → `web-craft:25` · เบราว์เซอร์ห้ามถือกุญแจฐาน → `pharmacy-web-logic` ข้อ 24

**ไม่ควรขึ้นสกิล:** ตัวเลข ms รายหน้าของเว็บนี้ · ชื่อไฟล์ `vals/index.js` · รายการงานค้าง 3 กอง · จำนวนแถว 417

---

## SESSION-2026-08-29.md (v0.11.6.0 · หน้าผลหลังกดส่ง + ของค้างต้องส่งเองจนสำเร็จ)

| # | กฎ/บทเรียนที่เจอ | ยกข้อความจริงมา | ควรไปสกิลไหน | ทำไมถึงควรขึ้นสกิล |
|---|---|---|---|---|
| 71 | 🔴🔴🔴 **รอสัญญาณ "เน็ตกลับมา" อย่างเดียวไม่พอ** — ต้องลองส่งเองเป็นระยะแบบถอยห่าง และ**ห้ามหยุดเอง** | *"🚨 **ทำไมรอสัญญาณ "เน็ตกลับมา" อย่างเดียวไม่พอ** เบราว์เซอร์ยิงสัญญาณนั้นเมื่อเครื่อง **หลุดจากเครือข่าย** เท่านั้น แต่อาการจริงในโรงพยาบาลคือไวไฟครบทุกขีดแต่ออกเน็ตไม่ได้ เครื่องคิดว่าตัวเองออนไลน์ตลอด ไม่มีสัญญาณไหนยิงเลย **ของค้างเงียบข้ามวันได้**"* · *"`RETRY_WAITS = [20, 40, 80, 160, 300]` วินาที ค้างที่ 5 นาทีไปเรื่อย ๆ **ไม่หยุดเอง** — หยุดเมื่อไหร่แปลว่าต้องมีคนมากด ซึ่งคือสิ่งที่เลี่ยงอยู่"* | **pharmacy-web-logic** ข้อ 30 (แก้ของเดิม) | 🔴 **สกิลข้อ 30 เขียนไว้ผิด/ไม่ครบอยู่ตอนนี้** — บรรทัด 683 เขียนว่า *"ส่งซ้ำอัตโนมัติเมื่อเน็ตกลับมา … **ผูกกับเหตุการณ์ `online`** — เน็ตกลับมาแล้วยิงของค้างต่อเอง"* ซึ่งเป็นวิธีที่บันทึกนี้พิสูจน์แล้วว่า**ไม่พอ** · HCV กำลังจะทำตามข้อนี้ (ตารางท้ายข้อ 30 เขียนว่ายังไม่มี) = จะได้ของที่ไม่ทำงานจริงในโรงพยาบาล |
| 72 | 🔴 หน้า "ส่งไม่สำเร็จ" ต้องตอบ 3 คำถาม และต้อง**นับถอยหลังจริงทุกวินาที** | ตาราง *"\| ของหายไหม \| ไม่หาย เก็บอยู่ในเครื่องนี้แล้ว ยังไม่ขึ้นระบบส่วนกลาง \|"* · *"\| ระบบทำอะไรอยู่ \| **นับถอยหลังจริงทุกวินาที** จนกว่าจะลองส่งเองครั้งถัดไป + ลองไปแล้วกี่ครั้ง \|"* · *"🚨 **ข้อความลอย ๆ ว่า "ระบบจะลองใหม่ให้" ใช้ไม่ได้** ไม่มีอะไรพิสูจน์ว่าระบบยังทำงานอยู่ คนจะไม่เชื่อแล้วกดเองรัว ๆ"* | **pharmacy-web-logic** ข้อ 30 (อย่างที่ 6) | สกิลข้อ 30 มีแต่ "แถบเตือนเน็ตหลุด" ที่เขียนไว้ดีมาก แต่**ไม่มีหน้าตอนส่งไม่สำเร็จ** ซึ่งเป็นจุดที่ข้อมูลผู้ป่วยเสี่ยงหายจริง · แนวคิด "ตอบ 3 คำถามในใจคนใช้" ยกไปใช้กับ UI อื่นได้ทั้งหมด |
| 73 | 🚨 ระบบที่ทำงานเองห้ามเด้งหน้าเต็มจอ · และตัวรับปุ่มที่ถูกเรียกอัตโนมัติต้องเทียบ `opts.auto === true` | *"🚨 **ระบบส่งเองห้ามเด้งหน้าเต็มจอ ทั้งตอนสำเร็จและตอนล้ม** … เภสัชกรอาจกำลังกรอกล็อตถัดไป และถ้าล้มซ้ำทุก 5 นาทีจะกลายเป็นจอเด้งทั้งวันจนคนเลิกอ่าน"* · *"⚠️ `app.save` ถูกผูกเป็นตัวรับการกดปุ่มด้วย จึงได้ event object มาเป็น `opts` **ต้องเทียบ `opts.auto === true` เท่านั้น**"* | **web-craft** (ทักษะ web dev) หรือ **pharmacy-web-logic** ข้อ 30 | กับดัก `opts` เป็น event object เป็นเรื่อง React ล้วน ไม่ผูกโปรเจกต์ · เช็คแค่ "มีค่าไหม" จะจริงเสมอเมื่อถูกกดจากปุ่ม = ระบบทำงานผิดทางเงียบ ๆ · grep `RETRY` · "ถอยห่าง" ในสกิล = ไม่เจอเลย |
| 74 | ก่อนออกแบบของที่เว็บอื่นมีอยู่แล้ว ให้ไป**โคลนโค้ดมาดูของจริง** ห้ามเดาจากคำอธิบาย | *"ก่อนออกแบบได้ไปโคลนโค้ด ME-DRP มาดูของจริง (`components/views/ResultOverlay.tsx`) ไม่ได้เดาจากคำอธิบาย"* (พี่กันสั่ง *"มันเปลี่ยนหน้าเลย แบบโปรเจกต์ DRP"*) | **working-with-gun** (ข้อ 3 กฎทอง) | grep "โคลนโค้ด" = ไม่เจอ · พี่กันสั่งแบบ "เอาเหมือนเว็บนั้น" บ่อยมาก (DRP · ยาคืน · HCV) · การเดาจากคำอธิบายทำให้ได้ของคนละแบบแล้วต้องแก้ซ้ำ · เป็นวิธีทำงานที่ประหยัดรอบมากที่สุด |
| 75 | 🟠 วิธีทดสอบเมื่อเครื่องไม่มีกุญแจฐานข้อมูล (เครื่องคลาว) — 3 ท่า | *"เครื่องคลาว**ไม่มีกุญแจฐานข้อมูล** จึงกดบันทึกจนสุดทางไม่ได้ ใช้ 3 ท่าแทน"* — *"ยิงเข้าฟังก์ชันจริงในหน้าเว็บผ่าน React fiber"* · *"ดักคำขอแล้วตอบแทนเซิร์ฟเวอร์ (puppeteer intercept)"* · *"ถ่ายภาพหน้าจริงทั้งจอคอมและจอมือถือ"* · และ *"⚠️ `scripts/ui-check.mjs` ใช้บนคลาวไม่ได้ตรง ๆ เพราะ `CHROME_PATHS` ไม่มีพาธของลินุกซ์"* พร้อมพาธ `/opt/pw-browsers/chromium` | **working-with-gun** (ข้อ 5 ไฟล์+เครื่องมือ / หัวข้อเครื่องคลาว) | สกิลมีกฎเรื่องเครื่องคลาว (push ขึ้น `main` เท่านั้น) แต่**ไม่มีวิธีเทสบนเครื่องคลาว** · พี่กันใช้ Claude Code คลาวอยู่เรื่อย ๆ · พาธ chromium ของลินุกซ์เป็นของที่ต้องหาใหม่ทุกครั้งถ้าไม่จด |

**มีในสกิลแล้ว (ตรวจจริง):** ชุดรับมือเน็ตหลุด 5 อย่าง + `fetchT` + แถบเตือน → `pharmacy-web-logic` ข้อ 30 · กันบันทึกซ้ำด้วย `batch_id` → ข้อ 20 · ปุ่มคู่ห้ามใส่ `.tap` → `mobile-web-app` · ช่องผู้บันทึกต้องว่างเสมอ → เป็นกฎเฉพาะโปรเจกต์ (3.24)

**ไม่ควรขึ้นสกิล:** เนื้อหาการ์ดสรุปของหน้าผล · `DEMO_PCU` · เลขเวอร์ชัน · `package-lock.json`

---

## SESSION-2026-08-27-part3.md (v0.11.5.0 · โครงจางระหว่างรอข้อมูล)

> ไฟล์นี้ **ยกขึ้นสกิลครบแล้ว** — อ่านสกิล `loading-skeleton` เทียบทั้งไฟล์: กฎ 6 ข้อ · สวิตช์ไม่เก็บลงเครื่อง ·
> คลื่นแสง 1.4 วิ + `prefers-reduced-motion` · ตัววาด 6 ตัว · `skelTableTag` · เช็คลิสต์ 7 ข้อ ·
> ตารางความผิด 7 ข้อ · "เปิดดูทุกหน้าจริง ๆ ก่อนบอกว่าเสร็จ" (บรรทัด 190)

| # | กฎ/บทเรียนที่เจอ | ยกข้อความจริงมา | ควรไปสกิลไหน | ทำไมถึงควรขึ้นสกิล |
|---|---|---|---|---|
| 76 | 🟠 ล้างข้อมูลจริงต้องทำที่ **จุดรวมค่าจุดเดียว** ห้ามไปดักทีละหน้า | *"**แก้ที่ `vals/index.js` จุดเดียว** — หลังรวมค่าทุกไฟล์แล้วล้างรายการทุกหน้าพร้อมกัน … 🚨 **ทำที่จุดรวมค่าจุดเดียว** ถ้าไปดักทีละหน้าจะตกหล่นแน่นอน (มี 6 หน้าที่มีรายการ)"* | **loading-skeleton** (กฎข้อ 1-2) | สกิลบอกว่า *"ต้องไม่มีข้อมูลจริงเหลืออยู่เลย"* และ *"ซ่อนของจริงทุกชิ้น"* แต่**ไม่บอกว่าให้ทำที่จุดไหน** · เว็บถัดไปที่ไล่ดักทีละหน้าจะตกหล่นเหมือนกัน (พิสูจน์แล้วว่าตกหล่น) |
| 77 | 🟠 เครื่องมือถ่ายภาพโครงจางต้อง**หน่วงเฉพาะคำขอข้อมูล ไม่แตะไฟล์เว็บ** | *"`scripts/skel-shot.mjs` — หน่วงคำขอข้อมูล 4 วินาทีเพื่อให้ทันถ่ายภาพโครงจาง"* · *"🚨 **หน่วงเฉพาะคำขอข้อมูล ไม่แตะไฟล์เว็บ** ไม่งั้นหน้าไม่โหลดเลย"* | **loading-skeleton** (ต่อจากหัวข้อ 🎛 สวิตช์) | สกิลมีสวิตช์กดดูเองแล้ว แต่**ไม่มีวิธีถ่ายภาพโครงจางจริงตอนโหลด** ซึ่งเป็นสิ่งเดียวที่พิสูจน์ว่าโครงจางโผล่ตอนโหลดจริง (ไม่ใช่แค่ตอนเปิดสวิตช์) · grep `skel-shot` · "หน่วงคำขอ" = ไม่เจอในสกิล |
| 78 | 🟠 กฎ "เปิดดูทุกหน้าจริง ๆ" ถูกจดไว้ 2 สกิล แต่**ไม่ได้อยู่ในสกิลกลาง** | *"**เรื่องเดียวกับที่พลาดตอนฟอนต์เมื่อรอบก่อน** — ทำเสร็จแล้วดูไม่ครบ … รอบก่อน: เปิดดูทุกหน้าแล้ว แต่ไม่ได้เทียบว่าของที่สั่งเปลี่ยนจริงไหม … รอบนี้: ดูแค่หน้าเดียวแล้วเหมาว่าที่เหลือถูก"* | **working-with-gun** (ข้อ 4.8 / ข้อ 10) | ตรวจจริง: กฎนี้อยู่ที่ `loading-skeleton:190` และ `mobile-web-app:265` เท่านั้น · **สกิลกลาง `working-with-gun` ไม่มี** ทั้งที่ไม่ใช่เรื่องโครงจางหรือมือถือเลย · ปัญหาเดียวกับข้อ 6 · 37 · 54 — กฎกลางถูกฝังไว้ในสกิลเฉพาะทาง |

**ไม่ควรขึ้นสกิล:** รายการหน้าที่ใส่โครงจางในเว็บนี้ · ตารางงานค้าง · รายการกลุ่ม 3

---

## SESSION-2026-08-27-part2.md (v0.11.4.0 · ฟอนต์ Roboto Mono + ตารางหน้ารายการ Lot)

**ไฟล์นี้มีของหนัก 6 เรื่อง — เรื่องฟอนต์ทั้งชุดยังไม่ขึ้นสกิลเลย**

| # | กฎ/บทเรียนที่เจอ | ยกข้อความจริงมา | ควรไปสกิลไหน | ทำไมถึงควรขึ้นสกิล |
|---|---|---|---|---|
| 79 | 🔴🔴 **คำสั่งของเจ้าของงานต้องเก็บคำต่อคำ ห้ามจดเป็นสรุป** — จดย่อแล้วเงื่อนไขหาย | พี่กันสั่ง *"เอาประโยคที่เราพิมพ์เรื่องกลุ่ม 3 เอาไปใส่ในนั้นด้วย **เราไม่มั่นใจว่าเธอเก็บคำพูดเราครบไหม**"* · ผลตรวจ *"ไม่ครบจริง 2 จุด"* — ส-7 ที่จดว่า "เคาะแล้ว" ของจริงคือ *"อันนี้ต้องคุย ยังไม่เคาะ"* + *"เราคิดไม่ออกแฮะ เธอเสนอมาหน่อย"* — *"**แคลร์มีการบ้านค้างที่ไม่รู้ตัว**"* · *"🚨 **บทเรียน: คำสั่งของพี่กันต้องเก็บคำต่อคำ** การจดเป็นสรุปสั้น ๆ ทำให้เงื่อนไขหายไป แล้วรอบหน้าจะทำงานตามที่ตัวเองจดผิด **โดยไม่มีใครจับได้**"* | **working-with-gun** (ข้อ 4.5 เอเจนต์ต้องบันทึกลงไฟล์ / ข้อ 3 กฎทอง) · **audit-report** | grep "คำต่อคำ" · "จดสรุป" ในสกิลทั้ง 12 ตัว = **ไม่เจอเลย** · นี่คือ**สาเหตุรากของงานตรวจครั้งนี้ทั้งงาน** — กฎที่ถูกจดย่อแล้วเงื่อนไขหาย · สกิล `audit-report` มีแม่แบบรายงานแต่ไม่มีกฎว่าคำสั่งต้องยกมาทั้งดุ้น |
| 80 | 🔴🔴 ฟอนต์อังกฤษ/ตัวเลขแยกจากฟอนต์ไทยด้วยลำดับ fallback ตัวเดียว — ไม่ต้องเขียนเงื่อนไขแยกภาษา | *"```font-family: var(--font-en), var(--font-sarabun), Sarabun, sans-serif```"* · *"Roboto Mono ไม่มีตัวอักษรไทย เบราว์เซอร์จึงตกมาใช้ Sarabun ให้เอง **ไม่ต้องเขียนเงื่อนไขแยกภาษาเลยสักบรรทัด**"* · *"การสลับทำที่ตัวแปรตัวเดียว `--font-en`"* | **web-craft** (หัวข้อฟอนต์ / ไทยเฉพาะ) | grep `font-en` = **ไม่เจอในสกิลเลย** · ท่านี้ถูกยกมาจาก HCV แล้วยกไปยาคืน = ใช้แล้ว 2 เว็บ แต่ไม่มีใครจดเป็นกฎกลาง · `web-craft:227` มีกฎ "ลูกศรต้องเป็น Roboto Mono" ซึ่ง**ต้องพึ่งกลไกนี้** แต่กลไกไม่ได้อยู่ในสกิล |
| 81 | 🔴🔴 จุดที่เขียนชื่อฟอนต์ตรง ๆ ในโค้ด **ไม่ผ่านตัวแปลงกลาง** — ของที่สั่งเปลี่ยนจะไม่เปลี่ยน | พี่กันจับได้เอง *"นี่ขนาดเราให้เธอเปิดดู ยังไม่แก้ชื่อยาอีกนะ ฟอนต์อ่ะะะะ"* · *"**ต้นเหตุ** — `pages/drugname.jsx` กับ `pages/record.jsx` เขียนฟอนต์ตรง ๆ … จุดนี้ไม่ผ่าน `fontify()` เพราะเขียนเป็นชื่อตัวแปรไม่ใช่ชื่อฟอนต์"* · *"**และเจอของแถม 12 จุดที่ยังใช้ `var(--font-plex)`** ซึ่งเป็นฟอนต์ที่พี่กันสั่งเลิกใช้ไปตั้งแต่ 26 ส.ค. … **หลุดรอดมาเพราะเหตุผลเดียวกัน**"* | **web-craft** (ฟอนต์) · **drug-display** (ตัววาดชื่อยาเป็นจุดที่พลาด) | เป็นกับดักของ**ตัวกลางทุกชนิด** ไม่ใช่แค่ฟอนต์ — จุดที่ข้ามตัวกลางไปจะไม่ได้รับการแก้ และเงียบสนิท · ตระกูลเดียวกับ `pharmacy-web-logic:134` *"สร้างตัวกลางไว้แล้วไม่ได้ต่อสาย เท่ากับไม่ได้ทำ"* ที่ยกขึ้นสกิลแล้ว **แต่ฉบับนั้นพูดถึงตัวที่ไม่มีใครเรียก ไม่ใช่ตัวที่มีคนเรียกแต่บางจุดเลี่ยงไป** |
| 82 | เปลี่ยนฟอนต์แล้วต้องไล่ตรวจของที่ล้น — Roboto Mono กว้างกว่า Sarabun ~15% | *"**ผลข้างเคียง — ฟอนต์นี้กว้างกว่า Sarabun ราว 15%**"* พร้อมตาราง 3 จุด — คอลัมน์วันที่ `92px → 116px` · ตัวเลขใหญ่ `clamp(44,9.4vw,132) → clamp(38,7.8vw,108)` · ชื่อผู้บันทึกเลิกใช้ ellipsis · *"**จุด `.` ในคำไทยกว้างขึ้น** (`รพ.สต.` ดูเหมือน `รพ . สต .`)"* | **web-craft** (ฟอนต์) — คู่กับข้อ 38 | ตัวเลข 15% กับ 3 จุดที่พังเป็นของที่ยกไปตรวจเว็บอื่นได้ทันที (ME-DRP ก็ใช้ Roboto Mono แล้ว) · คู่กับข้อ 38 (เปลี่ยนฟอนต์ต้องไล่ระยะ) — ทั้งสองข้อควรอยู่ด้วยกัน |
| 83 | 🚨 ฟอนต์เว็บห้ามลามเข้าใบพิมพ์เอกสารราชการ — พิสูจน์ 2 ชั้นจากไฟล์จริง | พี่กันสั่ง *"จุดที่ห้ามเปลี่ยนฟอนต์ คือหน้าปรินต์เอกสารราชการ"* · *"ผล: ในไฟล์ PDF มีแค่ **THSarabunNew** กับ **THSarabunNew-Bold** ไม่มี Roboto Mono เลย แปลงเป็นภาพเปิดดูด้วยตาแล้วด้วย"* · *"ปลอดภัยเพราะ `.slip-paper` เขียน `"TH Sarabun New"` ตรง ๆ ใน `@media print` **ไม่ผ่าน `fontify()`**"* | **making-documents** (มี `pdf-font.mjs` และกฎฟอนต์ราชการแล้ว แต่ยังไม่มีข้อนี้) | สกิล `making-documents` บอกให้ใช้ TH Sarabun New และมีเครื่องมืออ่านฟอนต์จาก PDF แล้ว **แต่ไม่มีกฎว่า "แตะฟอนต์เว็บเมื่อไหร่ ต้องตรวจใบพิมพ์ทุกครั้ง"** · เว็บที่มีทั้งหน้าจอและใบพิมพ์จะเผลอทำเอกสารราชการเปลี่ยนฟอนต์โดยไม่รู้ตัว |
| 84 | 🔴 ปุ่มที่กดไม่ได้ต้องใช้**สีของปุ่มตัวเองที่จางลง** ห้ามยกชุดจากปุ่มอื่น · แอนิเมชันต้องได้รับคำสั่งรายปุ่ม | พี่กัน *"ปุ่มเพิ่ม ไม่เอาอนิเมชั่น ไม่ได้บอก แล้วปุ่มมันจริง ๆ สีเขียว ก็แค่เปลี่ยนสีเขียวปกติ **ปุ่มไหนอนิเมชั่น เราบอกเอง**"* · ต้นเหตุ *"แคลร์ยกชุด**ครีมอมเหลือง + สั่น** ของปุ่มบันทึกไปใส่ โดยอ้างกฎ "อะไรที่คล้ายกันต้องทำให้เหมือนกัน""* | **button-feedback** — ✅ **ยกขึ้นสกิลแล้ว** (ตรวจจริง: บรรทัด 195 "ใส่แอนิเมชันได้เฉพาะปุ่มที่พี่กันสั่งเอง" · ตาราง "ใช้สีของปุ่มตัวเองที่จางลง") · **แต่คลาส `.hv-off-green` ยังไม่มีในสกิล** | ตัวกฎขึ้นแล้ว ✅ ที่ยังขาดคือ**โค้ดของปุ่มเขียวที่กดไม่ได้** — สกิลมีแต่ตัวอย่าง `.hv-wait` (ครีมอมเหลือง) ซึ่งเป็นตัวที่ห้ามยกไปใช้ · เว็บถัดไปจะไม่มีตัวอย่างที่ถูกให้ลอก |

**มีในสกิลแล้ว (ตรวจจริง):** ลูกศร `↑↓` 16/19px → `web-craft:210-222` · หัวคอลัมน์ต้อง `!important` ทับ padding ฝังในแท็ก → รวมเข้า `.tbl` แล้ว · แอนิเมชันเฉพาะปุ่มที่สั่ง → `button-feedback:195` · เก็บข้อมูลในเครื่อง → `working-with-gun` ข้อ 5.6

**ไม่ควรขึ้นสกิล:** ตารางความกว้างคอลัมน์รายหน้าของเว็บนี้ · แผงขวาล้นที่จอ 641px · รายการงานค้างกลุ่ม 3-4

---

## SESSION-2026-08-27.md (v0.11.3.0 · สีตอนชี้ทั้งเว็บ + เส้นแบ่งคอลัมน์ · วันที่ทำผิด 12 ข้อ)

> ตรวจจริง: บทเรียนเทคนิค 4 ข้อ + "ตรวจแบบให้เธอเป็นผู้ใช้" + เครื่องมือ `hover-check`/`hue-check`
> **ยกขึ้นสกิล `button-feedback` ครบแล้ว** (บรรทัด 347 · 356 · 359 · 368 · 384) และอยู่ใน `SCOLDING-LOG` ด้วย

| # | กฎ/บทเรียนที่เจอ | ยกข้อความจริงมา | ควรไปสกิลไหน | ทำไมถึงควรขึ้นสกิล |
|---|---|---|---|---|
| 85 | 🔴 **มอคอัปที่ส่งให้ดูต้องตรงกับโค้ดจริงเสมอ** — แก้โค้ดหลังทำมอคอัปแล้วไม่อัปเดตไฟล์ | *"⚠️ **มอคอัปรอบแรกกับโค้ดจริงไม่ตรงกัน 3 จุด** เพราะแคลร์แก้โค้ดหลังทำมอคอัปแต่ไม่ได้อัปเดตไฟล์ (แสงค้าง → จางหาย · เงาจางเกินจนมองไม่เห็น → เข้มขึ้นเท่าตัว · ดาว 4 ดวงเล็ก → 5 ดวงใหญ่ขึ้น) แก้ให้ตรงกันทุกบรรทัดแล้ว **บทเรียน: มอคอัปที่ส่งให้ดูต้องตรงกับโค้ดจริงเสมอ**"* | **web-craft** 6.6 ข้อ 8 (เรื่องมอคอัป) หรือ **working-with-gun** ข้อ ๖ | สกิลมีกฎ *"มอคอัปที่ approve = สัญญา ทำตามเป๊ะ"* (ทิศจากมอคอัป→โค้ด) แต่**ไม่มีทิศกลับ** (โค้ดเปลี่ยนแล้วต้องอัปเดตมอคอัป) · พี่กันเคาะจากมอคอัป = ถ้ามอคอัปไม่ตรงโค้ด **สิ่งที่พี่กันเคาะไม่ใช่สิ่งที่ได้** |
| 86 | 🟠 ไฟล์มอคอัปที่วางไว้ในโฟลเดอร์สาธารณะถูกเสิร์ฟบนเว็บจริง ใครมีลิงก์เปิดดูได้ | *"**ไฟล์มอคอัป 3 ไฟล์อยู่ใน `public/`** เสิร์ฟบนเว็บจริง ใครมีลิงก์เปิดดูได้ · จะย้ายหรือลบ"* (ค้างเป็นงานรอเคาะข้ามหลาย session) | **web-craft** 6.6 ข้อ 8 (มอคอัป) · **medical-data-safety** | สกิล `web-craft:107` สั่งให้ *"ทำเป็นไฟล์ HTML เดี่ยวใน `mockups/` แล้วส่งให้ดู"* แต่**ไม่บอกว่าห้ามวางใน `public/`** · ที่ HCV เคยเจอของจริงหนักกว่านี้ — **ข้อมูลผู้ป่วยจริงอยู่ใน `public/mockups`** ซึ่งคนนอกโหลดได้ (ตามบันทึกความจำ 1 ก.ย.) · เป็นรูรั่วซ้ำ 2 เว็บแล้ว |
| 87 | 🟠 ตัวตรวจสีมี "จุดบอดที่ตัดสินแทนคนไม่ได้" ต้องแยกเป็นรายการให้คนอ่านเอง ห้ามเดา | *"🚨 **มีจุดบอดที่ตัดสินแทนคนไม่ได้** — ปุ่มที่ตอนปกติเป็นขาว/เทาไม่มีโทนให้เทียบ จึงแยกออกมาเป็นรายการ **"ต้องอ่านเอง"** แทนที่จะเดา ต้องตัดสินจาก**ความหมายของปุ่ม** (ทำลาย=แดง · บันทึก=เขียว · รอ=อำพัน)"* | **button-feedback** — ✅ **มีในสกิลแล้ว** (`button-feedback:311-322`) · แต่หลักการ "ตัวตรวจต้องแยกสิ่งที่ตัดสินไม่ได้ออกมาแทนที่จะเดา" ยังไม่ถูกยกเป็นกฎกลาง | ตัวกฎเรื่องสีขึ้นแล้ว ✅ แต่**หลักการทั่วไป**ยังไม่มี — เป็นชิ้นส่วนของ "กฎการเขียนตัวตรวจ" ที่ข้อ 69 เสนอไว้ · ตัวตรวจที่เดาแทนคนคือที่มาของ "ตัวตรวจโกหก" ทุกกรณีในรายงานนี้ |

**มีในสกิลแล้ว (ตรวจจริง ยกมาให้เห็น):** คอมเมนต์ CSS ปิดเกิน → `button-feedback:359` · `display:flex` ฆ่า `text-align` → `:368` · สไตล์ฝังในแท็กชนะ CSS → `:356` · "ตรวจแบบให้เธอเป็นผู้ใช้" → `:347` · `hover-check` ต้องอ่านค่าสีไม่ใช่เทียบภาพ → `:286-310` · `hue-check` รายงานเป็นชื่อสีภาษาไทย → `:311`

**ไม่ควรขึ้นสกิล:** ชื่อคลาส `.hv-des-off` ของเว็บนี้ · ตารางความผิด 12 ข้อ (อยู่ใน SCOLDING-LOG แล้ว) · รายการงานค้าง

---

## SESSION-2026-08-26-part5.md (v0.11.2.0 · สีตอนชี้ทั้งเว็บ · รอบที่ทำพลาดหนักที่สุด)

> ไฟล์นี้เป็นต้นกำเนิดของ **`SCOLDING-LOG.md`** และกฎ 9 ข้อ + แผนผังตัดสิน push
> ตรวจจริงแล้ว **ยกขึ้นสกิลครบทั้งหมด** — `working-with-gun` ข้อ 0 (หยุดทันที) · ข้อ 4 (แผนผัง push) ·
> `button-feedback` กฎ 6 ข้อ (ห้ามใช้กฎกลาง · ห้ามตั้งสีชี้เท่าพื้นเดิม · อันที่ถูกอยู่แล้วห้ามแตะ)

| # | กฎ/บทเรียนที่เจอ | ยกข้อความจริงมา | ควรไปสกิลไหน | ทำไมถึงควรขึ้นสกิล |
|---|---|---|---|---|
| 88 | 🔴 **จุดที่ควรหยุดแล้วไม่หยุด — ยิ่งทำต่อยิ่งแย่ลงทุกรอบ** | *"พี่กันเริ่มหงุดหงิดตั้งแต่ *"ทำไมปุ่มนี้ยังไม่เปลี่ยนสี"* **ถ้าหยุดตรงนั้นแล้วแก้ให้ถูก เรื่องจบตั้งแต่ตอนนั้น** แต่ทำต่ออีก 4 รอบ แต่ละรอบแย่ลง"* พร้อมตารางลำดับความผิด 6 รอบ (กฎกลางครอบทุกปุ่ม → สีชี้เท่าพื้นเดิม → ทำงานที่ไม่มีใครสั่ง → **ทำเว็บพัง** → ถอยเกินคำสั่ง → ตีความว่าถูกสั่ง push) | **working-with-gun** ข้อ 0 (จับสัญญาณให้ได้) | สกิลข้อ 0 มีหัวข้อ "จับสัญญาณให้ได้" อยู่แล้ว แต่**ไม่มีตัวอย่างลำดับความผิดที่บานปลาย** ซึ่งเป็นสิ่งที่ทำให้เห็นราคาของการไม่หยุด · *"ทำไม X ยังไม่ Y"* คือสัญญาณแรกที่แคลร์อ่านไม่ออก 6 รอบติด |
| 89 | 🟠 repo ของเว็บโรงพยาบาลที่เปิดสาธารณะ — มีชื่อเภสัชกรจริง ชื่อโรงพยาบาล และบันทึกคำด่า | *"⚠️ **repo นี้เปิดสาธารณะ** ตรวจเจอระหว่างทาง แจ้งพี่กันแล้ว พี่กันยืนยันให้เอาขึ้นทั้งหมดโดยไม่ตัดชื่อออก"* · *"ในนั้นมีชื่อเภสัชกรจริง ชื่อโรงพยาบาล และตอนนี้มีบันทึกความผิดพร้อมคำที่พี่กันตำหนิด้วย"* | **medical-data-safety** หรือ **working-with-gun** ข้อ 4 (ก่อน commit ต้องกวาดหากุญแจ) | สกิลมีกฎ *"ก่อน commit ต้องกวาดหากุญแจ"* (`working-with-gun` 4.x) และ `audit-report:213` บอกว่าต้องตรวจว่า repo เปิดสาธารณะไหม · **แต่ไม่มีกฎว่าเจอแล้วต้องแจ้งและถามก่อนใส่ชื่อคนจริง** · ME-DRP เคยเปิดสาธารณะ**พร้อมกุญแจฐาน**มาแล้ว (บันทึกความจำ 2 ก.ย.) = เกิดซ้ำ 2 repo · เป็นเรื่อง PDPA ของโรงพยาบาล |

**ไม่ควรขึ้นสกิล:** ตารางคลาส `.hv-bg-*` ของเว็บนี้ · ตัวเลข 99/166 ปุ่ม · รายการงานค้าง

---

## SESSION-2026-08-26-part4.md (v0.11.0.0-1.0 · ใบสรุปเอกสารราชการ + หน้ารายการ Lot เป็นตาราง)

> ยกขึ้นสกิลแล้ว: เอกสารต้องแขวนที่ `body` ด้วย portal + `html,body{background:#fff}` → `making-documents` ข้อ 3 ·
> เครื่องมือ `print-check`/`pdf-look`/`pdf-font`/`ui-check` → `making-documents:34-51` ·
> ชุดสีชื่อยาโหมดมืด → `drug-display:58-88` · สี `#6f7873` แทน `#9aa19c` → `search-system:284`

| # | กฎ/บทเรียนที่เจอ | ยกข้อความจริงมา | ควรไปสกิลไหน | ทำไมถึงควรขึ้นสกิล |
|---|---|---|---|---|
| 90 | 🔴 เขียนคุณสมบัติ CSS แบบ**รวบ** ล้างค่าที่กฎกลางตั้งไว้ทิ้ง — ต้องเขียนแบบแยกเสมอ | *"กฎกลางใน `globals.css` วาดลูกศร ▾ ด้วย `background-image` คู่กับ `appearance:none` เขียน `background:#fff` แบบรวบในสไตล์ฝังในแท็ก = ล้างรูปลูกศรทิ้งทั้งหมด **ต้องเขียน `background-color` แยกเสมอ** — ตระกูลเดียวกับบั๊ก `border-color` เดี่ยวใน hover ของ ME-DRP"* · พี่กันทัก *"ไหนตัว v ที่บ่งบอกว่ามันคือดรอปดาวน์"* | **web-craft** (กับดัก CSS) หรือ **button-feedback** (มีเคส `border-color` อยู่แล้ว) | grep `background-color` ในสกิล = เจอแค่ในโค้ด `.hv-wait` ไม่มีเป็นกฎ · **เกิดแล้ว 2 เว็บด้วยคุณสมบัติคนละตัว** (`background` ที่ยาคืน · `border-color` ที่ ME-DRP) = เป็นรูปแบบ ไม่ใช่เคสเดียว · ทุกเว็บที่มีดรอปดาวน์วาดลูกศรเองเจอเหมือนกัน |
| 91 | 🔴 ปุ่มห้ามย่อเหลือแต่ไอคอน · ตัวกรองที่เลือกอะไรไม่ได้เลย แย่กว่าตัวกรองที่ยังไม่มีข้อมูล · รายการตัวเลือกต้องมาจากการตั้งค่า ไม่ใช่จากที่โผล่ในข้อมูล | ตาราง 3 รอบที่ทำผิด — *"\| ดึงรายชื่อ รพ.สต. จากที่โผล่ในข้อมูล \| ยังไม่มีล็อตไหนกรอกชื่อแห่ง รายการเลยว่างทั้งหมด \| ดึงครบ 13 แห่งจากการตั้งค่า \|"* · *"\| ย่อปุ่มเหลือแต่ไอคอน \| ไอคอนเปล่าต้องเดาความหมาย คนใช้ไม่บ่อยไม่กล้ากด \| มีข้อความกำกับทุกปุ่ม \|"* · *"\| ยัดทั้ง 13 แห่งลงดรอปดาวน์เดียว \| รายการยาวเป็นหางว่าว 19 บรรทัด \| เลือกสองชั้น \|"* · **บทเรียนร่วม** *"แคลร์ตัดสินใจแทนผู้ใช้ด้วยเหตุผลเรื่อง "ประหยัดที่บนจอ" ทั้งที่ของที่ประหยัดไปคือสิ่งที่ทำให้ใช้งานได้จริง"* | **web-craft** 6.6 (เกณฑ์ 4 ข้อ / กฎที่ตกผลึกจากงานจริง) | บทเรียนร่วมอยู่ใน `SCOLDING-LOG:960` **แต่ไม่ได้อยู่ในสกิลออกแบบหน้าจอเลย** (ปัญหาเดิม — กฎอยู่ในบันทึกคำด่าที่คนทำ UI ไม่เปิดอ่าน) · ทั้ง 3 ข้อย่อยเป็นกฎ UI ที่ใช้ได้ทุกเว็บ และ `web-craft` 6.6 มีเกณฑ์ "ไม่งง" อยู่แล้วซึ่งเป็นที่ที่ควรอยู่ |
| 92 | 🚨 เครื่องมือสร้างไฟล์ต้องตรวจก่อนว่า "ของที่จะพิมพ์เปิดอยู่จริง" ไม่งั้นไล่ผิดทาง | *"🚨 **`print-check.mjs` ต้องตรวจก่อนเสมอว่าใบเปิดอยู่จริง** รอบแรกไม่ได้ตรวจ เลยได้ PDF ของหน้าเว็บทั้งหน้าโดยไม่รู้ว่าสาเหตุคือสคริปต์กดปุ่มไม่ติด หรือกฎการพิมพ์ไม่ทำงาน — เสียเวลาไล่ผิดทาง"* | **making-documents** — ✅ **มีในสกิลแล้ว** (ตรวจจริง `making-documents` หัวข้อเครื่องมือ) | ✅ ขึ้นแล้ว ไม่ต้องทำอะไร · บันทึกไว้เพื่อยืนยันว่าตรวจครบ |
| 93 | 🟠 ตรวจก่อนแก้สีให้เข้มขึ้นว่าสีนั้นถูกใช้บนพื้นมืดด้วยไหม | *"🚨 ตรวจก่อนแก้ว่าสีนี้ไม่ได้ใช้ในหน้าสรุป (หน้าเดียวที่มีโหมดมืด) ถ้าใช้อยู่ การทำให้เข้มขึ้นจะกลับแย่ลงบนพื้นมืด"* | **drug-display** (มีตารางสีสองพื้นแล้ว) หรือ **web-craft** | สกิล `drug-display` มีชุดสีสองพื้นครบ ✅ และ `search-system:284` มีค่าสี ✅ **แต่ไม่มีขั้นตอน "ตรวจก่อนแก้สี"** · เว็บที่มีโหมดมืดบางหน้า (ซึ่งเป็นสภาพจริงของหลายเว็บ) จะแก้ contrast ด้านหนึ่งแล้วพังอีกด้าน |

**มีในสกิลแล้ว:** แก้ข้อไหนเสร็จต้องอัปเดตไฟล์สถานะทันที → `audit-report` ข้อ 8 (ตารางเต็ม) · heredoc กลืน backslash → **ยังไม่มี ดูข้อ 9** (ไฟล์นี้ยืนยันว่าเกิดซ้ำอีกครั้ง จน `lib/csv.js` พังทั้งไฟล์ ขึ้น `Unterminated string constant`)

**ไม่ควรขึ้นสกิล:** ชื่อคอลัมน์ 9 ตัวของหน้ารายการ Lot · `mr_setting.pcu_full` · รายชื่อไฟล์ที่แตะ

---

## SESSION-2026-08-26-part3.md (v0.10.1.0 · ระบบ รพ.สต. + ฟอนต์มีหัว + ปิดผลตรวจ 6 ข้อ)

| # | กฎ/บทเรียนที่เจอ | ยกข้อความจริงมา | ควรไปสกิลไหน | ทำไมถึงควรขึ้นสกิล |
|---|---|---|---|---|
| 94 | 🔴🔴 **ซากฟังก์ชันทับถมกันในฐาน — เพิ่มพารามิเตอร์แล้ว `create or replace` ไม่ทับตัวเดิม** | *"ตอนแก้ `mr_history` พบว่าในฐานมี **4 ตัวพร้อมกัน** เพราะเคยเพิ่มพารามิเตอร์ทีละตัว (002 → 004 → 005 → 011) แล้ว postgres ถือว่าเป็นคนละฟังก์ชัน ตัวเก่าไม่เคยถูกลบ"* · *"ยังไม่พังเพราะ `/api/returns` ส่งครบ 7 ช่องเสมอ **แต่เป็นระเบิดเวลา** — วันไหนมีคนเรียกโดยไม่ส่ง `p_offset` จะไปโดนตัวเก่าที่ไม่รู้จัก `pcu_site` แล้วได้ข้อมูลชุดเก่ากลับมาโดยไม่มีอะไรเตือน · **ลบซากทิ้ง 3 ตัว เหลือตัวเดียว**"* | **pharmacy-web-logic** (หัวข้อฐานข้อมูล / กฎการแก้ฟังก์ชัน) | grep "ซากฟังก์ชัน" · "คนละฟังก์ชัน" ในสกิล = **ไม่เจอเลย** · **เกิดแล้ว 2 เว็บ** — ที่ HCV คือ *"ฟังก์ชันบันทึกซ้อนกัน 2 ตัวในฐาน เพิ่มพารามิเตอร์แล้ว create or replace ไม่ทับตัวเดิม (ลบแล้ว)"* (บันทึกความจำ 2 ก.ย.) · เว็บทุกตัวใช้ Supabase ฐานเดียวกัน ฟังก์ชันซ้อนกันได้หมด · ผลคือ**ได้ข้อมูลชุดเก่าโดยไม่มีอะไรเตือน** |
| 95 | 🚨 `<option>` ในดรอปดาวน์ต้องใส่สไตล์ที่ตัวมันเอง ไม่สืบทอดจาก `<select>` | *"🚨 **`<option>` ในดรอปดาวน์ต้องใส่สไตล์ที่ตัวมันเอง** — วินโดวส์วาดรายการที่กางออกด้วยระบบของตัวเอง ไม่สืบทอดฟอนต์จากตัวแม่ (พี่กันจับได้ทันทีจากภาพหน้าจอ)"* | **web-craft** (ช่องกรอก/ช่องเลือก) — คู่กับข้อ 39 · 90 | grep `<option>` ในสกิล = **ไม่เจอเลย** · เป็นพฤติกรรมของวินโดวส์ (เครื่องที่ห้องยาใช้ทั้งหมด) · เว็บทุกตัวมีดรอปดาวน์ และทุกเว็บบังคับใช้ฟอนต์มีหัว = เจอเหมือนกันหมด · เป็น 1 ใน 3 กับดักของ `<select>` ที่รายงานนี้เจอ (ลูกศร ▾ กินที่ · `background` รวบล้างลูกศร · `<option>` ไม่สืบทอด) |
| 96 | 🟠 ไม่สร้าง UI ใหม่ที่ไม่เข้าชุดกับของเดิม — แก้ข้อความให้ตรงความจริงแทน | *"**ไม่ทำ UI จัดการรายชื่อในหน้าตั้งค่า** เพราะรายชื่อผู้บันทึก (`staff`) ก็ไม่มี แก้ในฐานเหมือนกัน จะสร้างของใหม่ที่ไม่เข้าชุดกับของเดิม → แก้ข้อความในหน้าจอให้ตรงความจริงแทน"* | **web-craft** 6.6 (กฎที่ตกผลึกจากงานจริง) | เป็นการตัดสินใจแบบที่สกิลควรสอน — สกิลมีกฎ *"อะไรที่คล้ายกัน ต้องทำให้เหมือนกัน"* แต่ยังไม่มีรูปแบบตรงข้าม *"อย่าทำของใหม่ที่หลุดจากชุดเดิม"* · ตรงกับกฎ *"ถ้าเจอโค้ดเก่าที่ทำไว้แล้ว ให้บอกพี่กันว่าควรรื้อ อย่าเขียนกฎเพิ่มซ้อนไปเรื่อย ๆ"* (`web-craft:145`) ซึ่งเป็นเรื่องเดียวกัน |

**มีในสกิลแล้ว (ตรวจจริง):** ฝังฟอนต์เอง ห้ามลิงก์ CDN → `web-craft:37` และ `:109` · `role=` เติมบนแท็กเดิม ห้ามเปลี่ยนแท็ก → `web-craft:357` · `aria-label` บนปุ่มไอคอน → `web-craft:350` · บัตรผ่านนับอายุจากครั้งสุดท้ายที่ใช้ → `pharmacy-web-logic` ข้อ 24 · แก้บั๊กต้องพิสูจน์ด้วยพฤติกรรมจริง → ซ้ำกับข้อ 13 · 28 · 51 ในรายงานนี้

**ไม่ควรขึ้นสกิล:** รายชื่อ รพ.สต. 13 แห่ง · ชื่อฟอนต์ Charmonman · `scripts/014_pcu_site.sql` · จำนวนไฟล์ที่แก้

---

## SESSION-2026-08-26.md (v0.10.0.1-0.3 · ปิดรูความปลอดภัย 9 ข้อ + เก็บกวาด 9 ข้อ · ทำบนคลาว)

| # | กฎ/บทเรียนที่เจอ | ยกข้อความจริงมา | ควรไปสกิลไหน | ทำไมถึงควรขึ้นสกิล |
|---|---|---|---|---|
| 97 | 🔴🔴 **เปิด session บนคลาวต้อง `git fetch` ก่อนเสมอ ห้ามเชื่อสิ่งที่เห็นในเครื่อง** | *"เปิด session มาแล้วอยู่ที่ **v0.1.0.0 (4 ส.ค.)** ทั้งที่ `origin/main` ไปถึง **v0.10.0.0** แล้ว"* · *"**ผลคือแคลร์ตอบพี่กันผิดไป 2 รอบ** — บอกว่าไม่มีสกิลใน repo (ของจริงมี 2 ชุด) และบอกว่า `CLAUDE.md` มี 237 บรรทัด (ของจริง 1,447 บรรทัด · กฎเหล็ก 15 ข้อ)"* · *"🚨 **บทเรียน: บนคลาวต้อง `git fetch` ดูก่อนเสมอ ห้ามเชื่อสิ่งที่เห็นในเครื่อง** เครื่องคลาวโคลนไว้ตอนสร้าง environment ไม่ได้ตามของใหม่ให้อัตโนมัติ"* | **working-with-gun** (ข้อ 4 push/commit — ตอนนี้ `git fetch` ถูกใช้เฉพาะ**หลัง** push เพื่อ verify) | ตรวจจริง: สกิลบรรทัด 281 · 304 ใช้ `git fetch` **เพื่อยืนยันว่า push สำเร็จ** เท่านั้น · **ไม่มีกฎว่าต้อง fetch ก่อนเริ่มงาน** · **เกิดแล้ว 2 โปรเจกต์** — ME-DRP เคยล้าหลัง **47 commit** (บันทึกความจำ 7 ส.ค.) · ผลคือ**ตอบพี่กันผิด** และเสี่ยงเขียนทับงานใหม่ |
| 98 | 🔴 **อย่าเชื่อคำว่า "เติมอย่างเดียวจบ" ในผลตรวจ ต้องดูของจริงก่อน** — ผลตรวจบอก 12 จุด ของจริง 79 จุด | *"ผลตรวจเขียนว่า *"คลาส `.tap` มีอยู่แล้ว เติมอย่างเดียว ไม่ขยับหน้าตาสักพิกเซล"* **ไม่จริงทั้งหมด** — `.tap::before` ขยายพื้นที่กดออกด้านละ 11px ปุ่มที่วางติดกันอย่าง **✓ กับ ✕** และ **ใช้ต่อ/ทำลาย** จะมีพื้นที่กดทับกัน"* · *"🚨 **บทเรียน: อย่าเชื่อคำว่า "เติมอย่างเดียวจบ" ในผลตรวจโดยไม่ดูของจริง** ตรวจแล้วพบว่ามีปุ่มที่ยังไม่มีคลาสนี้ **79 จุด** ไม่ใช่ 12 จุดตามที่ผลตรวจบอก"* | **audit-report** (ข้อ 11 / ข้อ 9 ห้ามทำ) | สกิล `audit-report` ข้อ 11 บอกให้ *"มุมที่เอเจนต์ทำไม่ได้ แคลร์ต้องทำเอง"* แต่**ไม่มีกฎว่าต้องตรวจซ้ำก่อนลงมือแก้ตามผลตรวจ** · ผลตรวจประเมินขนาดงานผิด **6.5 เท่า** และประเมินความเสี่ยงผิด (บอกว่าไม่ขยับหน้าตา แต่ของจริงทำให้กดปุ่มผิด) · ทุกโปรเจกต์ของพี่กันทำงานจากไฟล์ผลตรวจ |
| 99 | 🟠 อ่านบันทึกหลายรอบในวันเดียวกัน ต้องเอาสถานะจากไฟล์รอบสุดท้ายเสมอ | *"แคลร์บอกพี่กันว่าผลตรวจยังไม่ได้แก้สักข้อ **ซึ่งผิด** … ที่ผิดเพราะไปลอกประโยคจากบันทึก**รอบ 3** ซึ่งเป็นสถานะก่อนรอบ 4"* · *"🚨 **บทเรียน: อ่านบันทึกหลายรอบในวันเดียวกัน ต้องเอาสถานะจากไฟล์รอบสุดท้ายเสมอ**"* | **working-with-gun** (ข้อ 4 บันทึก session) หรือ **audit-report** | โปรเจกต์นี้มีบันทึกวันละ 5 รอบ (`SESSION-YYYY-MM-DD-partN.md`) และ ME-DRP · HCV ก็ทำแบบเดียวกัน · **สกิลสั่งให้เขียน session ทุกรอบแต่ไม่บอกวิธีอ่านย้อน** · ผลคือรายงานสถานะผิดให้พี่กัน |
| 100 | 🚨 แก้ประตูรหัสผ่านต้องกัน "รหัสว่าง" ด้วย — ไม่งั้นได้รูที่ใหญ่กว่าเดิม · และ `matcher` มีรูให้เลี่ยงด้วยนามสกุลไฟล์ | *"🚨 **กับดักที่เกือบพลาด** — แก้แค่ `authEnabled()` ไม่พอ ถ้าไม่กันรหัสว่างใน `/api/auth` ด้วย `tokenOf('')` จะตรงกับ `expectedToken('')` พอดี = **กรอกช่องว่างแล้วเข้าเว็บได้ กลายเป็นรูที่ใหญ่กว่าเดิม**"* · *"เพิ่ม `'/api/:path*'` เป็น matcher ตัวแรก … พิสูจน์รูเดิมแล้ว — `/api/returns/12.png` กับ `/api/lots/L690826-01.svg` **ข้ามประตูจริง**"* | **pharmacy-web-logic** ข้อ 24 (เบราว์เซอร์ห้ามถือกุญแจ / ประตูรหัสผ่าน) | สกิลข้อ 24 มีเรื่องบัตรผ่านและอายุคุกกี้ แต่**ไม่มีเรื่องประตูตรวจรหัสเอง** · grep "รหัสว่าง" · `matcher` = ไม่เจอเลย · เว็บของพี่กันมีประตูรหัสผ่านแล้ว 3 เว็บ (ยาคืน · ME-DRP · HCV) ทุกตัวใช้ middleware แบบเดียวกัน = มีรูเดียวกันได้หมด · **เป็นรูที่ทำให้ข้อมูลผู้ป่วยทั้งปีงบหลุด** |

**มีในสกิลแล้ว (ตรวจจริง):** push ขึ้น `main` เท่านั้นบนคลาว → `working-with-gun` ข้อ 4 · push ≠ deploy เสร็จ → `working-with-gun:870` · เอเจนต์ต้องเขียนผลลงไฟล์ระหว่างทาง → `audit-report:207`

**ไม่ควรขึ้นสกิล:** รายการผลตรวจ 46 ข้อ · `MRV_PASSWORD` · `package-lock.json` ค้างเวอร์ชัน · คำสั่ง `git pull` ตอนเปลี่ยนเครื่อง

---

## SESSION-2026-08-25-part4.md + part3.md (v0.9.0.0-0.10.0.0 · ราคาเข้าคลังกลาง · ชื่อยาจากคลัง · แก้ราคาย้อนหลัง)

| # | กฎ/บทเรียนที่เจอ | ยกข้อความจริงมา | ควรไปสกิลไหน | ทำไมถึงควรขึ้นสกิล |
|---|---|---|---|---|
| 101 | 🔴🔴 **`upsert` กับตารางที่มีคอลัมน์เยอะ ล้างช่องที่ไม่ได้ส่งเป็น NULL** — ต้องใช้ `update` รายแถว | *"`/api/prices` เดิมใช้ `upsert` กับ `mr_drug_price` ซึ่งมีแค่ 6 คอลัมน์ พอย้ายมา `drugs` ที่มียี่สิบกว่าคอลัมน์ **ช่องที่ไม่ได้ส่งไปจะโดนล้างเป็น NULL ทั้งหมด** = ชื่อยา ความแรง รูปแบบ หายเกลี้ยงทั้งตาราง"* · *"แก้เป็น `update` รายแถว ยิงทีละ 20 แถวขนานกัน"* · และบั๊กพี่น้อง *"PostgREST รวมรายชื่อช่องจากทุกแถวเป็นชุดเดียว แถวไหนไม่มีช่องนั้นถูกเติม `NULL`"* (CLAUDE.md 3.6) | **pharmacy-web-logic** (หัวข้อ 🚨 กฎการแก้ตาราง `drugs` — บรรทัด 569-582) | สกิลมีหัวข้อ "กฎการแก้ตาราง" พร้อมลำดับย้ายข้อมูลที่ถูกต้องแล้ว ✅ **แต่ไม่มีกฎเรื่อง `upsert`** · grep `upsert` เจอที่เดียวคือกันบันทึกซ้ำ (บรรทัด 335) · **ตาราง `drugs` ใช้ร่วม 3 เว็บ** — `upsert` ผิดครั้งเดียว **ชื่อยาทั้ง 417 ตัวหายพร้อมกันทั้งสามเว็บ** = ความเสียหายสูงสุดที่เป็นไปได้ในระบบนี้ |
| 102 | 🔴 อย่าเดาชิ้นส่วนของข้อมูลจากการอ่านข้อความ — ให้ส่งข้อมูลดิบรายช่องมาแล้ววาดทีละส่วน | *"**ต้นเหตุ:** `nameParts` เดาชิ้นส่วนชื่อจากการอ่านข้อความ พอสูตรชื่อเปลี่ยนก็เดาผิดทันที และ `(IR)` หายไปเพราะไม่อยู่ในตำแหน่งที่ตัวเดาคาดไว้"* พร้อมผลจริง *"`Morphine sulfate 10 mg/mL injection injection` ← ซ้ำ"* · *"**แก้ถูกทาง:** เลิกเดา ให้เซิร์ฟเวอร์ส่ง**ข้อมูลดิบรายช่อง**มา แล้ววาดทีละส่วน"* | **drug-display** (หัวข้อ "ตัววาดชื่อยาต้องเป็นตัวกลางตัวเดียว") | grep "ห้ามเดาชิ้นส่วน" ในสกิล = **ไม่เจอ** (มีใน CLAUDE.md 3.34 ของโปรเจกต์นี้เท่านั้น) · `drug-display` เป็นสกิลที่บอกลำดับและสีของชื่อยา — **แต่ไม่บอกว่าชิ้นส่วนต้องมาจากไหน** · ME-DRP กับ DM Remission จะเขียนตัวแยกชื่อจากข้อความเหมือนกัน แล้วพังตอนสูตรชื่อเปลี่ยน |
| 103 | 🚨 ช่องกรอกกับกล่องธรรมดาคิดความสูงจาก padding คนละแบบ — ต้องกำหนด `height` ตายตัว | *"🚨 **ความสูงเท่ากันต้องกำหนด `height` ตายตัว ไม่ใช่อาศัย padding** ช่องกรอกกับ `<div>` คิดความสูงจาก padding คนละแบบ ตั้ง padding เท่ากันแล้วยังสูงไม่เท่ากัน วิธีที่ใช้: `height:38px` + `box-sizing:border-box` + `inline-flex` + `align-items:center` วัดจริงในโครม — ทุกช่องสูง 38 · ขอบบนอยู่ที่ 152 เท่ากันหมด"* | **web-craft** (กับดัก CSS) หรือ **search-system** (แถวช่องค้นหา+ตัวกรอง) | grep "ความสูงเท่ากัน" = ไม่เจอ · เป็นกับดักที่เจอทุกครั้งที่วางช่องค้นหาข้างชิปตัวกรอง ซึ่ง `search-system` สั่งให้ทำอยู่แล้ว · มีค่าจริงและวิธีวัดยืนยัน ยกไปใช้ได้ทันที · ตระกูลเดียวกับข้อ 39 (ลูกศร ▾) และ 95 (`<option>`) |
| 104 | 🟠 การกันสิ่งไม่พึงประสงค์ต้องดักที่ **ทางออกทางเดียว** ไม่ใช่ไล่กันทีละฟังก์ชัน | *"**แก้:** `handlers/index.js` → `installDemoGuard` ดักที่ทางออกทางเดียว ทุกคำขอที่ไม่ใช่ GET ถูกตีกลับ · เปลี่ยนทุก handler ให้เรียก `app.fetchT` (**29 จุด**) **เดิมกันทีละฟังก์ชัน 8 จุด ขาดไป 6 เพราะต้องจำใส่เอง**"* | **web-craft** (ทักษะ web dev) หรือ **pharmacy-web-logic** | เป็นหลักการเดียวกับที่สกิล `pharmacy-web-logic` ข้อ 31 ใช้ (`apiFail` ตัวกลางตัวเดียว) และข้อ 30 (`fetchT` ทุกจุด) **แต่ไม่เคยเขียนเป็นหลักการ** · ตัวเลข 8 → ขาด 6 → 29 จุด เป็นหลักฐานว่าการกันทีละจุด**ตกหล่นเสมอ** · ใช้กับทุกเรื่องที่ต้องกันครบ (โหมดตัวอย่าง · ตัวจับเวลาตัด · การร้องข้อผิดพลาด) |
| 105 | 🟠 ตั้ง git ไม่ให้เด้งหน้าต่างถามบัญชี — ไม่งั้นค้างรอตลอดกาลตอนพี่กันไม่อยู่ | *"**ตั้งค่า git ไม่ให้เด้งถามเลือกบัญชีอีก** (`credential.guiPrompt false` สำคัญสุด — ถ้าหน้าต่างเด้งตอนพี่กันไม่อยู่ แคลร์จะค้างรอตลอดกาล)"* | **working-with-gun** (ข้อ 4 push/commit หรือข้อ 5 เครื่องมือ) | grep `guiPrompt` = **ไม่เจอ** · เป็นการตั้งค่าเครื่องพี่กัน ไม่ผูกโปรเจกต์ · อาการ "ค้างรอตลอดกาล" กินเวลาทั้ง session และพี่กันไม่รู้ว่าเกิดอะไรขึ้น |

**มีในสกิลแล้ว (ตรวจจริง):** ลำดับย้ายข้อมูลระหว่างคอลัมน์ที่ปลอดภัย → `pharmacy-web-logic:575-582` · สีเม็ดยาเก็บในฐานไม่ฝังในโค้ด + ก๊อปไป ME-DRP → `drug-display:251-272` · ห้ามเดาข้อมูลยา ต้องมาจากพี่กัน → `medical-data-safety` · ชื่อยาดึงจากคลังเสมอ · แช่ราคาไว้ในแถว → `pharmacy-web-logic` ข้อ 9

**ไม่ควรขึ้นสกิล:** คอลัมน์ใหม่ในตาราง `drugs` ของโปรเจกต์นี้ · ราคายารายตัว · เคส MTV tab · เลข commit

---

## SESSION-2026-08-25-part2.md + SESSION-2026-08-25.md (v0.7.1.0-0.8.0.0 · แก้ไขล็อต · เครื่องคิดเลขในช่องจำนวน)

| # | กฎ/บทเรียนที่เจอ | ยกข้อความจริงมา | ควรไปสกิลไหน | ทำไมถึงควรขึ้นสกิล |
|---|---|---|---|---|
| 106 | 🔴🔴 **ห้ามใช้ `eval()` / `new Function()` เด็ดขาด — CSP ของเว็บจริงบล็อก แล้วพังเงียบ** | *"🚨🚨 **ห้ามใช้ `eval()` หรือ `new Function()` เด็ดขาด** ทั้งสองตัวรันโค้ดอะไรก็ได้ที่อยู่ในข้อความ และ **CSP ของเว็บจริงบล็อกไว้อยู่แล้ว** (`next.config.js` ใส่ `'unsafe-eval'` เฉพาะตอน dev — **เว็บจริงจะพังเงียบ ๆ หาสาเหตุยากมาก**)"* · ทางที่ถูก *"ไล่อ่านทีละชั้น: บวกลบ → คูณหาร → ตัวเลข/วงเล็บ"* | **web-craft** (Security — ต่อจากบรรทัด 34 ที่บอกว่า CSP ไม่มี unsafe-eval) | สกิลบรรทัด 34 บอกให้ *"CSP headers (ไม่มี unsafe-eval)"* **แต่ไม่บอกผลตามมา** ว่าโค้ดที่ใช้ `eval` จะพังเฉพาะบนเว็บจริง · คู่กับข้อ 67 (แตะ CSP ต้องเทส production build) — ทั้งสองข้อคือ **"บั๊กที่มีเฉพาะบนเว็บจริง"** ซึ่งเป็นประเภทที่หาสาเหตุยากที่สุด |
| 107 | 🔴 ปุ่มที่กดแล้วโดนตีกลับ + ข้อความเตือนหายใน 2 วินาที = **ผู้ใช้เห็นเป็นปุ่มเสีย** | *"ผลคือพี่กันกดยืนยัน → โดนตีกลับ → ข้อความเตือนหายใน 2 วินาที → **ดูเหมือนปุ่มเสีย** พี่กันพิมพ์มาว่า *"กดยืนยันแล้ว ทำไมไม่หาย"*"* · ทางแก้ *"ช่อง **"ผู้ที่แก้ไข"** ในหน้าต่างยืนยันเอง + **ปิดปุ่มยืนยันจนกว่าจะเลือก** + ป้ายแดงบอกเหตุผล ไม่มีทางกดแล้วเงียบอีก · **ปุ่มบอกตัวเองว่าทำไมกดไม่ได้**"* | **button-feedback** (หัวข้อปุ่มที่กดไม่ได้) — **เรื่องเดียวกับข้อ 19** | ยืนยันซ้ำว่าเป็นรูปแบบ ไม่ใช่เคสเดียว — เกิด 25 ส.ค. (แก้ไขล็อต) และ 2 ก.ย. (ป๊อปใส่จำนวนเกินเพดาน) · หลักการเดียวกัน **"ห้ามให้ปุ่มเงียบ ต้องปิดปุ่มไว้แล้วบอกเหตุผลบนจอถาวร ไม่ใช่ toast ที่หายไป"** · สกิลยังไม่มี |
| 108 | 🚨 แถวที่มีปุ่มสลับสถานะ ต้องมีจำนวนช่องปุ่มคงที่เสมอ ไม่งั้นกรอบเลื่อนตอนกด | *"4. 🚨 **โครงคงที่ ปุ่ม 2 ช่องเสมอ**"* พร้อมผัง *"ปกติ: `[เลข 150px][✎ 26px][ที่ว่าง 26px]` / แก้: `[ช่อง 150px][✓ 26px][✕ 26px]`"* · *"ถ้าจำนวนปุ่มไม่เท่ากัน **กรอบตัวเลขเลื่อนซ้ายตอนกดแก้** — วัดจริงหลังแก้: ขอบขวา **618px** เท่ากันทั้งสองสถานะ ความสูงแถว **50px คงที่** ทั้ง 3 สถานะ"* | **web-craft** (ตาราง) หรือ **button-feedback** | grep "โครงคงที่" · "จำนวนปุ่มไม่เท่า" = ไม่เจอ · ตารางที่แก้ค่าในแถวได้มีทุกเว็บ (ยาคืน · ME-DRP คลังยา · HCV) · ของที่ขยับตอนกด ทำให้ตาต้องไล่หาใหม่ทุกครั้ง = ปัญหาที่พี่กันทักบ่อย |
| 109 | 🟠 ค่าที่ผูกกับขนาดของอย่างอื่น ต้องเขียนกำกับว่ามาจากไหน | *"🚨 `padding-right:69px` = ปุ่ม 26 + ที่ว่าง 26 + ช่องไฟ 5×2 + ขอบตัวเลข 7 — **เปลี่ยนขนาดปุ่มต้องแก้เลขนี้ตาม**"* | **web-craft** (ตาราง / CSS) | เป็นวิธีเขียนโค้ดที่ป้องกันบั๊กในอนาคต — เลขวิเศษที่ไม่มีที่มาจะไม่มีใครกล้าแตะและจะผิดเมื่อของข้าง ๆ เปลี่ยน · สกิลมีตาราง `.tbl` พร้อมค่าจริงแล้ว แต่ไม่มีหลักการนี้ |

**มีในสกิลแล้ว (ตรวจจริง):** bump เวอร์ชันต้องแก้วันที่อัปเดตด้วย → `working-with-gun:268` · แทนตัวเองว่า "แคลร์" → `working-with-gun` ข้อ 1 · ห้ามมี `overflow-x`/`max-height` ครอบ `<table>` → `web-craft:181` · ปุ่มเปลี่ยนสีตามสถานะต้องสลับคลาส hover → `button-feedback` กฎข้อ 7 · เอเจนต์ต้องเขียนผลลงไฟล์ (ลืมเก็บไฟล์ผลตรวจ) → `audit-report:207`

**ไม่ควรขึ้นสกิล:** ตารางผลทดสอบเครื่องคิดเลข · รายชื่อยาที่แก้ในฐาน · `padding-right:69px` (ค่าเฉพาะเว็บนี้ — เก็บเฉพาะหลักการในข้อ 109) · สถานะราคายา 43 ตัว

---

## SESSION-2026-08-13.md (v0.7.0.0 · หน้าคลังยา + ซิงก์ข้ามเว็บ + สีความแรง)

> บทเรียนใหญ่ของวัน (เดาตัวย่อยา 127 ตัวแล้วเขียนลงฐานจริง) **ยกขึ้นสกิล `medical-data-safety` ครบแล้ว**
> (บรรทัด 18 · 30) และซ้ำใน `pharmacy-web-logic:560` + `SCOLDING-LOG:365,978`

| # | กฎ/บทเรียนที่เจอ | ยกข้อความจริงมา | ควรไปสกิลไหน | ทำไมถึงควรขึ้นสกิล |
|---|---|---|---|---|
| 110 | 🔴🔴 **`anon` แก้ตาราง `drugs` ได้ — ใครเปิด F12 ก็แก้คลังยาของ 3 เว็บได้จากที่ไหนก็ได้** | *"ตาราง `drugs` เปิดให้ `anon` **อ่าน · เพิ่ม · แก้** โดยไม่มีเงื่อนไข `anon` คือกุญแจสาธารณะที่ฝังในหน้าเว็บ ME-DRP — **ใครเปิด F12 ก็เอาไปแก้คลังยาได้จากที่ไหนก็ได้**"* · *"มีมาก่อนแล้ว … แจ้งพี่กันแล้ว · **ยังไม่ได้แก้**"* | **pharmacy-web-logic** (ข้อ 24 เบราว์เซอร์ห้ามถือกุญแจ · และหัวข้อตาราง `drugs` ที่ใช้ร่วมกัน) | grep `anon` ในสกิลทั้ง 12 ตัว = **ไม่เจอเลยสักคำ** · สกิลมีหัวข้อ *"🗄 ตาราง `drugs` — ใช้ร่วมกัน"* พร้อมกฎการแก้ตาราง **แต่ไม่มีเรื่องสิทธิ์เข้าถึงเลย** · เป็นรูที่ **ค้างมาตั้งแต่ 13 ส.ค. ถึงอย่างน้อย 2 ก.ย.** (ยังอยู่ในรายการค้างของทุก session) และกระทบ **ยาคืน · ME-DRP · TB Calculator พร้อมกัน** — ใครก็ได้แก้ชื่อยา ความแรง ราคา ของโรงพยาบาลจริง |
| 111 | 🟠 ยาตัวเดียวมีหลายตัวย่อ ให้เลือกตัวเดียวและใช้ชื่อสากล (INN) | ตารางที่พี่กันเคาะ — *"\| `RIF RMP` \| **RIF** \| INN = rifampicin \|"* · *"\| `AZT ZDV` \| **ZDV** \| INN = zidovudine · AZT เป็นชื่อเคมีเก่า \|"* · *"\| `SMZ-TMP TMP-SMX` \| **SMX-TMP** \| SMX เป็นตัวย่อปัจจุบันของ sulfamethoxazole \|"* | **drug-display** (หัวข้อตัวย่อ) หรือ **medical-data-safety** | grep `INN` = ไม่เจอในสกิลเลย · เป็นหลักการที่**ใช้ตัดสินใจซ้ำได้** ไม่ใช่แค่ข้อมูล 5 ตัวนี้ · ทุกเว็บห้องยาที่แสดงตัวย่อ (ยาคืน · ME-DRP) ต้องใช้เกณฑ์เดียวกัน ไม่งั้นตัวย่อไม่ตรงกันข้ามเว็บ · เข้าคู่กับกฎ "ห้ามเดาตัวย่อ" ที่ขึ้นสกิลแล้ว |
| 112 | 🟠 ข้อจำกัดเครื่องมือ — `visibilitychange` ทดสอบใน Browser pane ไม่ได้ ใช้ `online` แทน | *"⚠️ **ทดสอบ `visibilitychange` ใน Browser pane ไม่ได้** (ไม่ยิงตอนสลับแท็บ เหมือน `resize_window`) ใช้ `window.dispatchEvent(new Event('online'))` แทน — เส้นทางเดียวกัน พิสูจน์แล้วว่าทำงาน"* | **working-with-gun** (เครื่องมือ) — รวมกับข้อ 2 (`resize_window` ไม่ได้ผล) | สกิล `pharmacy-web-logic:509` สั่งให้ *"refetch ตอน `visibilitychange` และ `online`"* — **แต่ไม่บอกว่าทดสอบตัวแรกไม่ได้** · เว็บถัดไปที่ทำตามสกิลจะทดสอบไม่ผ่านแล้วนึกว่าโค้ดพัง · เข้าชุดกับข้อ 2 · 61 (ข้อจำกัดเครื่องมือทดสอบ) |
| 113 | 🟠 ตีความคำสั่งกลับด้าน — *"RIF สิยังจะถามอีก"* ถูกอ่านเป็น "ลบสิ" | *"⚠️ **RIF เคยลบไปแล้วคืนกลับ** — แคลร์อ่าน *"RIF สิยังจะถามอีก"* เป็น "ลบสิ" ทั้งที่พี่กันหมายถึงให้เก็บไว้ ไม่ต้องถาม"* | **working-with-gun** (ข้อ 3 กฎทอง — ไม่ชัด = ถามก่อน) | เป็นตัวอย่างที่สามของ "ตีความคำสั่งผิด" ในรายงานนี้ (คู่กับข้อ 4 และ 79) · ทั้งสามครั้งต้นเหตุเดียวกัน — **อ่านประโยคสั้น ๆ แล้วเดาความหมายแทนที่จะถาม** · สกิลมีกฎ "ไม่ชัด = ถามก่อน" แต่ไม่มีตัวอย่างว่าประโยคแบบไหนที่แคลร์มักอ่านผิด |

**มีในสกิลแล้ว (ตรวจจริง):** "ลิสต์มาให้ดู = ให้เสนอ" → `medical-data-safety:18,30` · ห้ามเดาข้อมูลยา → `medical-data-safety` ทั้งไฟล์ · สีความแรง `ST_COLORS` → `drug-display` ข้อ 26 · เรียงตามรูปแบบยา → `search-system` ข้อ 6 · ก้อนห้ามตัดขาดกลาง → `drug-display` ข้อ 27 · ลายเซ็นคลังยา + `drugRev` คู่กับ `drugs` → `pharmacy-web-logic` ข้อ 29 · ลบยาไม่ได้ ใช้ซ่อนแทน → `pharmacy-web-logic`

**ไม่ควรขึ้นสกิล:** ตารางตัวย่อ 38 แบบ · คอลัมน์ที่ 14 ของหน้าคลังยา · รายการงานค้าง

---

## SESSION-2026-08-06.md (v0.4.1.0-0.4.2.0 · โดนัทหน้าสรุป · Lot ในหน้าประวัติ · ประตูรหัสผ่าน)

**ไฟล์เก่าที่ยังมีของดีเหลืออยู่เยอะที่สุด — 7 เรื่อง**

| # | กฎ/บทเรียนที่เจอ | ยกข้อความจริงมา | ควรไปสกิลไหน | ทำไมถึงควรขึ้นสกิล |
|---|---|---|---|---|
| 114 | 🔴 **ไอคอนต้องบอก "สถานะตอนนี้" ไม่ใช่ "สิ่งที่จะเกิดถ้ากด"** | *"🚨 **ทำกลับด้านตอนแรก** — ใส่ขีดทับตอนที่กำลังโชว์รหัส (ไอคอนบอก "สิ่งที่จะเกิดถ้ากด") พี่กันทักว่ากลับด้าน → เปลี่ยนเป็นไอคอนบอก **"สถานะตอนนี้"**"* พร้อมตาราง *"ซ่อนอยู่ = ตามีขีดทับ / เห็นรหัส = ตาปกติ"* | **button-feedback** หรือ **web-craft** 6.6 | grep "สถานะตอนนี้" = **ไม่เจอในสกิลเลย** · เป็นกฎออกแบบที่ตัดสินได้ทันทีและเถียงกันบ่อยที่สุดเรื่องหนึ่ง (ปุ่มดวงตา · ปุ่มเปิด/ปิด · สวิตช์ธีม) · ทุกเว็บมีปุ่มสลับสถานะ |
| 115 | 🔴 **กราฟที่ข้อมูลอาจไม่ครบ อันตรายกว่าไม่มีกราฟ** — เหตุผลของพี่กันเอง | พี่กันปฏิเสธมอคอัปแบบ ข ด้วยเหตุผล *"ไม่รู้ว่าคนใช้จะกรอกเรื่องทำลายครบไหม"* — *"ช่องเหตุผลไม่ได้บังคับกรอก ถ้ากรอกไม่ครบ กราฟจะบอกความจริงครึ่งเดียว **ซึ่งอันตรายกว่าไม่มีกราฟ**"* | **web-craft** 6.6 (กฎที่ตกผลึกจากงานจริง — ต่อจากข้อ 2 "ค่าอะไรวัดซ้ำได้ต้องมีกราฟ") | สกิล 6.6 ข้อ 2 สั่งว่า *"ค่าอะไรที่วัดซ้ำได้ตามเวลา ต้องมีกราฟ"* — **ข้อนี้คือข้อยกเว้นที่จำเป็น** · ทำตามสกิลตรง ๆ โดยไม่มีข้อยกเว้น = ได้กราฟจากช่องที่ไม่บังคับกรอก แล้วผู้บริหารตัดสินใจจากข้อมูลครึ่งเดียว · **เป็นเหตุผลที่พี่กันคิดเอง ควรเก็บเป็นกฎ** |
| 116 | 🚨 ของที่ "ยุบได้ตอนที่ว่างไม่พอ" ต้องเช็คว่ายุบแล้วยังอ่านได้ — ซ่อนเนื้อหาในกล่องจิ๋วแย่กว่าปล่อยให้ทั้งแผงเลื่อน | *"**ต้นเหตุ:** แคลร์ใส่ `overflow-y:auto` + `min-height:0` ไว้ในกล่องเพื่อให้ยุบได้ตอนที่ว่างไม่พอ **เจตนาดีแต่ผลตรงกันข้าม** — มันยุบจนเนื้อหาหายแทนที่จะบอกว่าไม่พอ"* · *"**บทเรียน:** เวลาทำของให้ "ยุบได้ตอนที่ว่างไม่พอ" ต้องเช็คด้วยว่ายุบแล้ว **เนื้อหายังอ่านได้ไหม** ไม่ใช่แค่ว่าไม่ล้น · การซ่อนเนื้อหาไว้ในกล่องจิ๋วแย่กว่าปล่อยให้ทั้งแผงเลื่อน"* · อาการจริง *"กล่องถูกบีบจนเหลือ 23px แล้วซ่อนบรรทัดไว้ข้างใน … ดูเหมือนเว็บพัง"* | **web-craft** (กับดัก flex/overflow — ต่อจากบรรทัด 29) | สกิลบรรทัด 29 สอนสูตร `flex:1;min-height:0` + `overflow-y:auto` เพื่อให้กล่องลูกเลื่อนได้ — **ซึ่งเป็นสูตรที่สร้างบั๊กนี้พอดี** · สกิลให้สูตรแต่ไม่ให้ข้อควรระวัง · พี่กันเจอเองที่จอ 768 ซึ่งเป็นขนาดจอคอมห้องยา |
| 117 | 🔑 วิธีหาบั๊ก "กล่องยุบแล้วซ่อนเนื้อหา" — วัดพื้นที่เลื่อนทุกอันในหน้าแล้วหาตัวแปลกปลอม | *"**วิธีหา:** วัดพื้นที่เลื่อนทุกอันในหน้า แล้วเจอตัวแปลกปลอม `{ box: "411→434", w: 238, ซ่อนอยู่: 34 }` = กล่องกว้าง 238 สูง 23 แต่ซ่อนเนื้อหาไว้ 34px"* | **web-craft** (คู่กับข้อ 116) | เป็นเทคนิควินิจฉัยที่ใช้ซ้ำได้ — หากล่องที่ `scrollHeight > clientHeight` โดยไม่ตั้งใจ · ตระกูลเดียวกับ `overflow-check.mjs` ที่สกิล `mobile-web-app` มีแล้ว แต่ตัวนั้นหาของ**ล้นออกนอกจอ** ไม่ใช่ของ**ซ่อนอยู่ข้างใน** |
| 118 | 🚨 `flex:1` ย่อจาก `1 1 0%` — ฐานความสูงเป็นศูนย์ กล่องจึงไม่ยอมหด ต้องใช้ `flex:1 1 auto` | *"**ก. กล่องไม่ยอมยุบตอนจอเตี้ย** — ใส่ `flex:1` = `1 1 0%` ฐานความสูงเป็นศูนย์ การคำนวณการหดเลยได้ศูนย์ตาม กล่องดันปุ่มบันทึกตกขอบ → เปลี่ยนเป็น `flex:1 1 auto`"* | **web-craft** (กับดัก flex) | grep `flex:1 1 auto` = ไม่เจอ · สกิลมีกับดัก flex อยู่ 2 ข้อแล้ว (`margin:0 auto` + flex · `flex:1 0 auto` ทำให้ลูกไม่รู้ความสูง) — **ข้อนี้เป็นตัวที่สาม** ซึ่งคนละอาการและคนละทางแก้ · ควรอยู่ด้วยกันเป็นชุด |
| 119 | 🟠 ค่าที่คำนวณไว้แต่ไม่มีใครวาด = ฟีเจอร์ที่มีแต่ใช้ไม่ได้ | *"**ตรวจแล้วพบว่า:** หน้าคอม **ไม่เคยแสดง Lot เลย** — `lotLabel` ถูกคำนวณไว้ใน `vals/history.js` แต่ไม่มีใครวาด"* · *"ช่องค้นหารับ "เลข Lot" กับปุ่มกรอง Lot มีมาตั้งแต่แรก แต่หน้าคอมไม่เคยโชว์เลข **คนใช้จึงไม่มีทางรู้ว่าเลขคืออะไร กลายเป็นฟีเจอร์ที่มีแต่ใช้ไม่ได้**"* | **working-with-gun** ข้อ 4.8 (เช็คลิสต์อ่านโค้ด ③ "ของที่สร้างไว้แต่ไม่มีใครเรียก") | สกิลข้อ 4.8 มีคำสั่ง grep หา "ของที่สร้างไว้แต่ไม่มีใครเรียก" แล้ว ✅ **แต่กรอบคิดยังเป็นเรื่องโค้ดตาย** ไม่ใช่ **"ฟีเจอร์ที่ผู้ใช้เข้าไม่ถึง"** ซึ่งร้ายแรงกว่า · ตระกูลเดียวกับ `pharmacy-web-logic:131` (`SearchBox.tsx` 241 บรรทัดไม่มีใครเรียก) และข้อ 8.2 ในไฟล์นี้ (`openDeviceAsk` ไม่มีปุ่มเรียก) = **เกิดแล้ว 3 ครั้ง** |
| 120 | 🟠 เทคนิคที่ยกไปใช้ได้ทันที — โดนัท SVG รัศมี 15.9 · CSV ต้องมี BOM · เทสประตูรหัสโดยไม่ให้รหัสผ่านตา | *"**เคล็ดลับการวาดโดนัทด้วย SVG** — ตั้งรัศมี `15.9` จะได้เส้นรอบวง ≈ 100 พอดี ใส่เปอร์เซ็นต์ลง `stroke-dasharray` ได้ตรง ๆ ไม่ต้องคูณหาร `stroke-dashoffset` เริ่มที่ 25 เพื่อให้ชิ้นแรกเริ่มที่ตำแหน่ง 12 นาฬิกา"* · *"ส่งออก CSV พร้อม BOM (เปิดใน Excel แล้วภาษาไทยไม่เพี้ยน)"* · *"เทสด้วยสคริปต์ที่อ่านรหัสจาก `.env.local` เอง **รหัสไม่เคยผ่านตาแคลร์และไม่โผล่ในแชท**"* · และ *"ยังไม่มีข้อมูลเลย → วาดวงเทาว่าง ๆ แทนวงบิดเบี้ยว"* | **web-craft** (SVG/กราฟ · CSV) · **working-with-gun** (วิธีเทสประตูรหัส) | grep `15.9` · `BOM` · "รหัสจาก .env" = **ไม่เจอเลยสักตัว** · **ข้อที่มีค่าที่สุดคือวิธีเทสประตูรหัส** — แคลร์กรอกรหัสผ่านเองไม่ได้ (ข้อห้ามติดตัว) ซึ่งเป็นเหตุผลที่เว็บนี้เคยเปิดช่องโหว่ `MRV_DEV_OPEN` ทิ้งไว้ · **มีวิธีที่ถูกอยู่แล้วตั้งแต่ 6 ส.ค. แต่ไม่ได้จด แล้ววันหลังไปเปิดประตูทิ้งไว้แทน** |

**ไม่ควรขึ้นสกิล:** ตารางผลเทสของเว็บนี้ · ค่าสีโดนัท · เลข Lot ตัวอย่าง · รายการงานค้าง

---

## SESSION-2026-08-04.md (v0.1.0.0 · สร้างเว็บใหม่ทั้งอันในวันเดียว)

> ไฟล์ตั้งต้นของโปรเจกต์ · การตัดสินใจ 5 ข้อในนั้น **ยกขึ้นสกิลครบแล้ว** — แช่ราคาไว้ในแถว
> (`pharmacy-web-logic` ข้อ 9) · `drug_id` ไม่ผูก FK · ไม่มี API ค้นหายา โหลดครั้งเดียว
> (`web-craft:25`) · ตัวแปลง CSS `s()` (เฉพาะโปรเจกต์) · กันบันทึกซ้ำ `batch_id`+`client_rid` (ข้อ 20)

| # | กฎ/บทเรียนที่เจอ | ยกข้อความจริงมา | ควรไปสกิลไหน | ทำไมถึงควรขึ้นสกิล |
|---|---|---|---|---|
| 121 | 🟠 เพิ่มช่องที่ต้องสืบกลับได้ ต้องเพิ่มตั้งแต่วันแรก — เพิ่มทีหลังต้องย้ายข้อมูล | *"**ช่องคนบันทึก** — ไม่มีระบบเข้าสู่ระบบแปลว่าไม่รู้ว่าใครกรอก ถ้าอนาคตอยากรู้ **ควรเพิ่มตั้งแต่ตอนนี้ (เพิ่มทีหลังต้องย้ายข้อมูล)**"* | **pharmacy-web-logic** (ออกแบบฐานข้อมูล) — คู่กับกฎ *"เก็บข้อมูลเกินดีกว่าขาด"* ใน `web-craft:94` | สกิล `web-craft` ข้อ 6 มี *"เก็บข้อมูลเกินดีกว่าขาด … ข้อมูลที่ไม่ได้เก็บตั้งแต่แรกย้อนกลับไปเก็บไม่ได้"* ✅ **แต่ไม่มีฉบับฐานข้อมูล** ว่าช่องสืบกลับ (ใครทำ · เมื่อไหร่ · จากที่ไหน) ต้องมีตั้งแต่ตารางแรก · เว็บนี้ต้องกลับมาเพิ่ม `recorded_by` · `deleted_by` · `price_fixed_by` · `changed_by` ทีละรอบภายหลังทั้งหมด |

**ไม่ควรขึ้นสกิล:** เฟส 1-5 ของการสร้างเว็บ · ตารางผลทดสอบ · รายการยาฉีด 48 ตัว · ที่ต่างจากมอคอัป 5 จุด

---

## SESSION-2026-08-05.md (v0.2.0.0-0.4.0.0 · 4 รอบในวันเดียว — ล็อต · ถังขยะ · นำเข้าราคาจาก HIS)

**ไฟล์ที่ยาวเป็นอันดับสอง (422 บรรทัด) และยังมีของที่ยังไม่ขึ้นสกิลอีก 6 เรื่อง**

| # | กฎ/บทเรียนที่เจอ | ยกข้อความจริงมา | ควรไปสกิลไหน | ทำไมถึงควรขึ้นสกิล |
|---|---|---|---|---|
| 122 | 🔴 **บั๊กเลื่อนหน้าค้างข้ามแท็บ — พื้นที่เลื่อนก้อนเดียวใช้ร่วมทุกหน้า** · แก้ 2 ชั้น | พี่กันเจอเอง *"เราเลื่อนขึ้นลงในหน้า บันทึก ประวัติ เเล้วพอเปิดมาหน้า สรุป มันกลับโดนเลื่อนลงมาเอง"* · *"**แก้ 2 ชั้น เพราะชั้นเดียวไม่พอ** 1. `app.toTop()` — รีเซ็ต `scrollTop` ทันที **แล้วรีเซ็ตซ้ำในเฟรมถัดไป** (หน้าประวัติ/สรุปตอนเพิ่งเข้ายังไม่มีข้อมูล เนื้อหาสั้น พอข้อมูลมาถึงแล้วยืดยาวขึ้น) 2. `overflow-anchor:none` ที่พื้นที่เลื่อน — ปิดระบบยึดตำแหน่งเลื่อนของเบราว์เซอร์ที่ดึงตำแหน่งเดิมกลับมาหลังเนื้อหาโตขึ้น"* | **web-craft** (ทักษะ web dev / กับดัก CSS) | grep `overflow-anchor` = **ไม่เจอในสกิลเลย** · ทุกเว็บที่สลับหน้าโดยไม่โหลดใหม่ (ทุกเว็บของพี่กัน) มีพื้นที่เลื่อนก้อนเดียว = เจอบั๊กนี้เหมือนกัน · **ชั้นที่สอง (`overflow-anchor`) เป็นของที่ไม่มีทางเดาได้** ถ้าไม่เคยเจอมาก่อน |
| 123 | 🔴🔴 **ห้ามทำระบบที่โยนงานกลับไปให้เจ้าของงาน ในเมื่อทำเองได้อยู่แล้ว** | พี่กัน *"ไม่เข้าใจ เราต้องทำไร เราอยากได้รายการยาที่ให้เรากดยืนยันว่าราคานี้จริงไหม **เราจะเอาไฟล์เข้ามาทำไมม** เธอใส่ราคาเลยย เเล้วเราจะกดเลือกเองง"* · *"หน้านำเข้าไฟล์ที่ทำไปเป็นการโยนงานกลับไปให้พี่กันโดยไม่จำเป็น — **แคลร์มีไฟล์อยู่แล้ว ทำให้เองได้เลย**"* | **working-with-gun** (ข้อ 3 กฎทอง) · **medical-data-safety** (มีบรรทัด 109 *"การให้ไปไล่นับเองคือการโยนงานกลับ"* แล้ว แต่เป็นคนละบริบท) | สกิลมีคำว่า "โยนงานกลับ" อยู่ที่เดียวและเป็นเรื่องการนับข้อมูล · **ข้อนี้เป็นเรื่องการออกแบบระบบ** — สร้างหน้าจอที่ทำให้พี่กันต้องทำงานเอง ทั้งที่แคลร์ทำแทนได้ · เสียแรงทั้งหน้าจอไปเปล่า ๆ (`hisMatch.js` + 3 ไฟล์ + ตัวอ่าน Excel 952 KB) · เป็นเกณฑ์ตัดสินก่อนออกแบบทุกหน้า |
| 124 | 🔴 **ชัวร์ → ใส่ค่าเลย · ไม่ชัวร์ → ปล่อยเป็นศูนย์แล้วแนบตัวเลือกไว้ให้กด** (ห้ามใส่ค่าที่อาจผิด) | *"ตอนแรกแคลร์จะใส่ราคาที่เดาได้ลงไปทุกตัวแล้วให้พี่กันมาแก้ทีหลัง แต่เจอตัวอย่างนี้ก่อนกดรัน"* — `Amoxicillin + Clavulanic acid 500 + 125 mg → จับไปเจอ Amoxicillin 250 mg = 2 ฿` · *"**ถ้าใส่ 2 ฿ ลงไปแล้วพี่กันมองข้าม ตัวเลข KPI จะผิดเงียบ ๆ**"* · *"**กติกาที่ยึด: ชัวร์ → ใส่ราคาเลย · ไม่ชัวร์ → ราคา 0 แล้วแนบตัวเลือกไว้ให้กด**"* | **medical-data-safety** (ต่อจาก "ห้ามเดา") | สกิลบอก *"ห้ามเดา ห้ามคิดเอง ห้ามเติมเอง"* ซึ่งถูก แต่**ไม่บอกว่าแล้วต้องทำยังไงกับข้อมูลที่จับคู่ได้บางส่วน** · กติกานี้เป็นทางออกที่ใช้ได้จริงและปลอดภัย (ค่าที่ยังว่าง = ระบบกันไม่ให้บันทึกอยู่แล้ว) · ใช้ได้กับทุกงานนำเข้าข้อมูลทางการแพทย์ |
| 125 | 🔴🔴 **ตรวจความแรงเสมอ ไม่ใช่ตรวจเฉพาะตอนมีหลายตัวเลือก** — เคสจับคู่ผิดที่ระบบบอกว่า "มั่นใจ" | *"```✓ Acyclovir 500 mg ← ACYCLOVIR 400 mg tab. = 5.50 ฿ ผิด แถมติ๊ก "มั่นใจ" ให้```"* · *"**ต้นเหตุ:** ตรวจความแรงเฉพาะตอน `cand.length > 1` พอในไฟล์ทดสอบมี acyclovir แถวเดียว เลยข้ามการตรวจ ได้ราคายาคนละความแรงมาใส่แบบเงียบ ๆ — **อันตรายที่สุดในระบบนี้**"* | **medical-data-safety** (การจับคู่ข้อมูลยา) | grep "ตรวจความแรง" = **ไม่เจอในสกิล** (อยู่ใน CLAUDE.md 3.3 ของโปรเจกต์นี้เท่านั้น) · **ทุกเว็บที่นำเข้าข้อมูลยาจาก HIS จะเจอเหมือนกัน** — ME-DRP กับ DM Remission ยังไม่ทำแต่จะต้องทำ · ความแรงผิด = ราคาผิด = ตัวเลขที่เสนอผู้บริหารผิด และในบริบทอื่นคือ**ขนาดยาผิด** |
| 126 | 🚨 Tall Man Lettering ในไฟล์ HIS — ต้องเทียบชื่อแบบ "บีบติดกัน" ห้ามแปลงขีดเป็นช่องว่าง | พี่กันชี้เอง *"cef หาดีๆ มันมี - แบ่งชื่อ"* · *"HIS เขียน `CefTRI-axone` `ceFA-zolin` `CefTAZ-idime` `CeFO-taxime` เพื่อกันหยิบยาชื่อพ้องผิด แคลร์แปลงขีดเป็นช่องว่าง เลยได้ `cef triaxone` ที่ไม่มีวันตรงกับ `ceftriaxone` → เพิ่มการเทียบแบบ **บีบติดกัน** (`squashName`) **จับได้เพิ่มทันที 9 ตัว**"* | **medical-data-safety** หรือ **search-system** (การเทียบชื่อยา) | grep `Tall Man` · `squashName` = **ไม่เจอในสกิลเลย** · เป็นมาตรฐานความปลอดภัยทางยาที่ HIS ทุกโรงพยาบาลใช้ ไม่ใช่ของเฉพาะปรางค์กู่ · ใครที่เทียบชื่อยากับไฟล์ HIS โดยไม่รู้เรื่องนี้จะจับคู่พลาดเป็นสิบตัวโดยไม่รู้ตัว · รวมถึงกับดักอื่นในไฟล์เดียวกัน (ชื่อการค้า/ตัวย่อ · สะกดผิดในต้นทาง `bisacodil` · ยาตัวเดียวหลายบรรทัดราคาต่างกัน · ราคา 1 บาท = ยาที่ไม่มีในคลัง) |
| 127 | 🟠 ชื่อ Worker ใน `wrangler.jsonc` ต้องตรงกับของจริงบน Cloudflare ไม่งั้น deploy สร้างโปรเจกต์ใหม่ | *"**ชื่อ Worker บน Cloudflare = `returndrug-pkh`** — แก้ `wrangler.jsonc` ให้ตรง **ไม่งั้น deploy จากเครื่องจะสร้างโปรเจกต์ใหม่อีกอัน**"* · และ *"**ไม่เปลี่ยนชื่อบัญชี workers.dev** — เพราะจะกระทบเว็บอื่นทั้งบัญชี"* | **web-craft** (Deploy) | สกิลมีเรื่อง deploy (Pages vs Workers · env vars) แล้ว **แต่ไม่มีข้อนี้** · พี่กันมี 6 เว็บอยู่ในบัญชีเดียวกัน — สร้าง worker ซ้ำโดยไม่ตั้งใจแล้วเว็บจริงค้างเวอร์ชันเก่าโดยไม่มีอะไรเตือน (ต่อยอดจากกฎ "push ≠ deploy" ที่มีแล้ว) |

**มีในสกิลแล้ว (ตรวจจริง):** เว็บส่วนตัว ≠ เว็บที่ทำงาน → `working-with-gun:567` · redact secret ก่อน commit + เตือนพี่กัน rotate → `working-with-gun:578` · ระบบเสนอ คนตัดสิน ห้ามนำเข้าอัตโนมัติ → `medical-data-safety` · `margin:0 auto` + flex ทำหน้าเว็บหด → `web-craft:28` · `flex:1 0 auto` ทำให้ลูกไม่รู้ความสูง → `web-craft:29` · ตรึงหัวด้วย ResizeObserver → `web-craft:30` · โหมดตัวอย่างห้ามเขียนทับร่างจริง → ซ้ำกับข้อ 56

**ไม่ควรขึ้นสกิล:** ตารางบั๊ก 41 จุด · ผลจับคู่ 972 รายการ · ตารางผลเทส · รายชื่อ 16 ผู้บันทึก

---

## SESSION-2026-08-10.md (v0.4.3.0-0.6.0.0 · 5 รอบในวันเดียว · ยกเครื่องระบบค้นหา — 931 บรรทัด)

> ไฟล์ยาวที่สุดในชุด · เนื้อหาส่วนใหญ่ **ยกขึ้นสกิลไปแล้ว** — ระบบค้นหาทั้งชุด (7 ชั้นการจัดอันดับ ·
> พิมพ์หลายคำ · ลืมสลับแป้น · ตัวย่อ · ยาใช้บ่อย · ลูกศรวนรอบ · กรอบเลื่อนตาม) → `search-system` ·
> ER/IR + สีตัวย่อม่วง + ห้ามวาดวงเล็บซ้ำ → `drug-display` · จับกลุ่มด้วยรหัสยา → `pharmacy-web-logic` ข้อ 1
> (พร้อมกฎ "ต้องทำก่อนมีข้อมูลจริง" บรรทัด 209) · เซิร์ฟเวอร์ทดสอบตัวเดียว → `web-craft:42`

| # | กฎ/บทเรียนที่เจอ | ยกข้อความจริงมา | ควรไปสกิลไหน | ทำไมถึงควรขึ้นสกิล |
|---|---|---|---|---|
| 128 | 🔴🔴 **ช่องในฐานที่มีข้อมูลอยู่แล้วแต่ไม่มีโค้ดหยิบมาใช้เลย — เจอ 3 รอบในไฟล์เดียว** | *"**ข้อมูลมีอยู่แล้วแต่ไม่เคยถูกใช้เลย** — `drugs.brand` มี 37 ตัวจาก 417 ไม่มีโค้ดตรงไหนหยิบมาใช้"* · *"`drugs.release` มีข้อมูลอยู่ **12 ตัว** แต่ไม่มีโค้ดตรงไหนหยิบมาใช้เลย (**แบบเดียวกับ % และชื่อการค้าที่เพิ่งแก้ไปรอบก่อน**)"* · *"ความเข้มข้น % ไม่เคยขึ้นเลย ทั้งที่กรอกไว้ในฐาน"* · และผลของ `release` ที่หาย — *"`Morphine sulfate 10 mg ER` = ออกฤทธิ์ 12 ชั่วโมง · `Morphine sulfate 10 mg` = 4 ชั่วโมง"* · *"ในคลังมีคู่ที่ต่างกันแค่นี้จริง ๆ … **เดิมแสดงเหมือนกันทุกตัวอักษร**"* | **pharmacy-web-logic** (หัวข้อตาราง `drugs`) หรือ **drug-display** | grep "ไม่เคยถูกใช้" · "มีในฐานแต่" = **ไม่เจอในสกิลเลย** · **เกิด 3 ครั้งในวันเดียว** และครั้งที่สาม (`release`) เป็นข้อมูลความปลอดภัยที่ทำให้ยาสองตัวหน้าตาเหมือนกันทุกตัวอักษร · ควรมี**คำสั่งตรวจ**ในสกิล: ไล่ทุกคอลัมน์ของ `drugs` แล้วนับว่ามีโค้ดไหน select ไปใช้บ้าง · ME-DRP กับ DM Remission ใช้ตารางเดียวกัน = มีช่องที่ยังไม่ได้ใช้เหมือนกัน |
| 129 | 🔧 dev server ค้างแบบ "พอร์ตยังจองอยู่แต่ไม่ตอบ" — ต้องแยกจาก "พี่กันทำอะไรผิด" | *"พี่กันแจ้งว่าเปิด localhost ไม่ติด ตรวจแล้วพบว่า **พอร์ตยัง LISTENING อยู่แต่ไม่ตอบเลย** (`curl` รอจนครบ 20 วินาทีแล้วได้ HTTP 000) — **ไม่ใช่พี่กันทำอะไรผิด**"* พร้อมคำสั่ง *"`netstat -ano \| grep LISTENING \| grep :3000` # พอร์ตยังจองอยู่ไหม / `curl -s -o /dev/null -w "%{http_code}" -m 5 http://localhost:3000/login` # ตอบจริงไหม"* · *"พอร์ตจองอยู่ + ไม่ตอบ = ค้าง ต้อง kill แล้วเปิดใหม่ (พอร์ตเดิม)"* · เกิด **3 รอบในวันเดียว** หลัง `sed` แตะ 15 ไฟล์พร้อมกัน | **working-with-gun** ข้อ 5.9 (เปิดเซิร์ฟเวอร์ทดสอบ) หรือ **web-craft** | สกิลมีคำสั่ง `netstat` เพื่อดูว่า**มีอะไรรันอยู่ไหม** แล้ว ✅ แต่**ไม่มีขั้นที่สอง** (รันอยู่แต่ตายแล้ว) · ตรงกับกฎ `working-with-gun` ข้อ ๗ *"ระบบพัง = ความผิดของแคลร์ ห้ามโยนกลับไปที่พี่กัน"* — ข้อนี้คือ**วิธีพิสูจน์** ว่าไม่ใช่ความผิดพี่กัน แทนที่จะเดา |
| 130 | 🟠 แปลงโค้ดจำนวนมากด้วย `sed` ต้องเติม `import` ให้ทุกไฟล์ที่แตะ | *"แปลงอัตโนมัติ **103 จาก 107 จุด** · อีก 4 จุดเป็นเงื่อนไขสามทาง (`onClick={a ? b : undefined}`) แก้มือ"* · *"⚠️ ต้องเติม `import { kb }` ให้ทุกไฟล์ที่ sed แตะ ไม่งั้นขึ้น `kb is not defined` ตอนรัน"* · และ 2 ทางที่ไม่เลือกพร้อมเหตุผล (ไล่ใส่ทีละจุด 107 จุด · สแกน DOM หลังวาดจอ — *"React ไม่ได้ใส่ `onclick` จริงบน DOM (ใช้ event delegation ที่ root) จึงหาไม่เจอ"*) | **working-with-gun** (ไฟล์+เครื่องมือ) · **web-craft** ข้อ 6.5 (ที่นิยาม `kb()`) | สกิล `web-craft` 6.5 มีตัวช่วย `kb()` และบอกว่าเว็บนี้มี 107 จุด **แต่ไม่บอกวิธีแปลงทั้งหมด** · เว็บอื่นที่แปลงจากมอคอัปเหมือนกันมีจุดพอ ๆ กัน · และเหตุผลที่ **"สแกน DOM แล้วเติมทีหลังไม่ได้"** เป็นความรู้ที่กันเสียเวลาไปทั้งวัน |
| 131 | 🟠 ทำไมไม่เปิดหน้าต่างใหม่แล้วสั่งพิมพ์ · และกราฟห้ามปัดหลักพัน | *"**ทำไมไม่เปิดหน้าต่างใหม่แล้วพิมพ์:** ต้องโหลดฟอนต์ไทยซ้ำ และเบราว์เซอร์บางตัวบล็อกหน้าต่างที่เปิดเอง = **พิมพ์ไม่ออกโดยไม่มีอะไรเตือน**"* · *"**เรื่องกราฟไม่ปัดหลักพัน** — 3,499 กับ 3,500 เดิมกลายเป็น "3k" กับ "4k" **ห่างกันบาทเดียวแต่ป้ายต่างกัน 1,000 บาท** ยอดยาคืนต่อเดือนของโรงพยาบาลชุมชนอยู่หลักพันถึงหลักหมื่น จึงแสดงเลขเต็มถึงหลักแสน"* | **making-documents** (การพิมพ์) · **web-craft** 6.6 (ตัวเลขต้องบอกความหมาย) | grep "หน้าต่างใหม่แล้วพิมพ์" · "ปัดหลักพัน" = **ไม่เจอในสกิล** · สกิล `making-documents` บอกวิธีพิมพ์ที่ถูกแล้ว แต่ไม่บอกว่า**ทางที่ผิดคืออะไรและทำไม** — คนที่ยังไม่เคยทำจะเลือกทางเปิดหน้าต่างใหม่ก่อนเสมอ · ส่วนเรื่องกราฟตรงกับ `web-craft` 6.6 ข้อ 3 *"ทุกตัวเลขต้องบอกความหมาย"* แต่กรณี "ปัดจนคนละความหมาย" ยังไม่มี |

**มีในสกิลแล้ว (ตรวจจริง):** ระบบค้นหาทั้งชุด → `search-system` (รวมบั๊ก `met` แล้ว Metformin ไม่ขึ้น บรรทัด 116) · ER/IR สำคัญที่สุด → `drug-display` ข้อ 3 · ห้ามใช้ `nowrap`+`ellipsis` กับบรรทัดชื่อยา → `drug-display:132` · ยาผสมครอบวงเล็บ → `drug-display:142` · จับกลุ่มด้วยรหัสยา + ต้องทำก่อนมีข้อมูลจริง → `pharmacy-web-logic:209` · แยกระบบค้นหาเป็นโมดูลอิสระ → `search-system` · เซิร์ฟเวอร์ทดสอบตัวเดียว/พอร์ตของวัน → `web-craft:42` · `upsert` แถวชุดช่องไม่เท่ากัน → **ยังไม่มี ดูข้อ 101** · ค่าว่างมีหลายความหมาย ต้องแยกด้วยธง → **ยังไม่มี** (ดูด้านล่าง) · เทสห้ามเขียนทับข้อมูลจริง → `working-with-gun` ข้อ ๑ · โหมดตัวอย่างต้องมีข้อมูลพอให้เห็นของใหม่ → ซ้ำกับข้อ 56

| # | (เพิ่ม) | ยกข้อความจริงมา | ควรไปสกิลไหน | ทำไม |
|---|---|---|---|---|
| 132 | 🟠 **ค่าว่างมีได้หลายความหมาย — ห้ามเดาจากความยาวของผลลัพธ์** | *"**บทเรียน:** ค่าว่างมีได้หลายความหมาย — "ยังหาไม่เจอ" กับ "เลือกไปแล้ว" หน้าตาเหมือนกัน **ต้องแยกด้วยธงต่างหาก ไม่ใช่เดาจากความยาวของผลลัพธ์**"* · อาการ *"กดเลือกยาแล้ว ยาเข้าช่องจำนวนถูกต้อง แต่กล่อง "ไม่พบยาชื่อนี้ ลองพิมพ์ชื่อสามัญ" ยังค้างอยู่"* | **search-system** (ข้อ 7 หรือท้ายส่วนที่ 3 ผลค้นหา) | ทุกช่องค้นหามีสถานะ "ไม่พบ" และ "เลือกแล้ว" ซึ่งทั้งคู่ให้ผลลัพธ์ว่าง · เป็นตระกูลเดียวกับข้อ 63 (โหลดไม่สำเร็จ vs ไม่มีข้อมูล) — **สถานะที่หน้าตาเหมือนกันแต่ความหมายคนละเรื่อง** ซึ่งเป็นบั๊กที่เจอซ้ำที่สุดในโปรเจกต์นี้ |

**ไม่ควรขึ้นสกิล:** ตารางไฟล์ที่แตะ · ตารางผลเทส · รายการตัวย่อ 127/38 ตัว · รายการงานค้าง · เลข commit

---
---

# 📊 สรุปรวม — อ่านครบ 31 ไฟล์ · เจอ 132 รายการ

> **เสร็จแล้ว** 5 ก.ย. 2569 · อ่าน `docs/SESSION-*.md` ครบทั้ง 31 ไฟล์ (6,561 บรรทัด)
> ทุกข้อ grep ยืนยันในสกิลจริงแล้วว่ายังไม่มี (หรือมีแต่ไม่ครบ/อยู่ผิดที่)

## 🔴 10 อันดับที่ควรยกขึ้นสกิลก่อน (เรียงตามความเสียหายถ้าไม่ทำ)

| ลำดับ | ข้อ | เรื่อง | ไปสกิล | เพราะอะไร |
|---|---|---|---|---|
| 1 | **14** | **"สกิลกลาง คือกฎ"** — ทุกอย่างในสกิลคือกฎ ไม่ว่าเขียนรูปแบบไหน | working-with-gun | คำสั่งตรงจากพี่กัน · กำหนดวิธีอ่านสกิลทั้งระบบ · ไม่มีข้อนี้ เอเจนต์จะข้ามตาราง/บันทึกในสกิลว่า "ไม่ใช่กฎ" |
| 2 | **79** | **คำสั่งต้องเก็บคำต่อคำ ห้ามจดเป็นสรุป** | working-with-gun · audit-report | **เป็นสาเหตุรากของงานตรวจครั้งนี้ทั้งงาน** — จดย่อแล้วเงื่อนไขหาย |
| 3 | **110** | `anon` แก้ตาราง `drugs` ได้ — ใครก็แก้คลังยา 3 เว็บได้ | pharmacy-web-logic | ค้างมาตั้งแต่ 13 ส.ค. · กระทบยาคืน+ME-DRP+TB Calc พร้อมกัน · แก้ชื่อยา/ความแรง/ราคาของโรงพยาบาลจริงได้ |
| 4 | **101** | `upsert` กับตารางคอลัมน์เยอะ ล้างช่องที่ไม่ได้ส่งเป็น NULL | pharmacy-web-logic | ผิดครั้งเดียว **ชื่อยา 417 ตัวหายพร้อมกันทั้ง 3 เว็บ** |
| 5 | **71** | รอสัญญาณ "เน็ตกลับมา" อย่างเดียวไม่พอ (ไวไฟครบขีดแต่ออกเน็ตไม่ได้) | pharmacy-web-logic ข้อ 30 | **สกิลข้อ 30 เขียนวิธีที่ไม่พอไว้อยู่ตอนนี้** (บรรทัด 683) · HCV กำลังจะทำตาม |
| 6 | **69** | **ห้ามแก้สคริปต์เทสให้ผ่านโดยไม่รู้ว่าทำไม** (+ "ผ่านแบบหลอกตัวเอง") | working-with-gun | กฎแม่ของกับดักตัวตรวจ 9 ข้อที่เจอกระจายทั้ง 8 ไฟล์ (ข้อ 10·16·26·40·46·48·52·61·87) |
| 7 | **7** | ตัวตรวจที่วัดคนละสภาพกับที่พี่กันดู = ตัวตรวจที่โกหก | working-with-gun | รายงานผิดให้พี่กัน 2 รอบติด |
| 8 | **49 + 70** | ค่าที่ใช้ข้ามหน้า/ธงตัดสิน ห้ามอยู่ในไฟล์ที่ถูกข้ามการคำนวณ | web-craft | สกิลสอนให้ทำ lazy compute เพื่อความเร็ว — **แล้วสร้างบั๊กที่บันทึกไม่ได้ทั้งเว็บ** |
| 9 | **67 + 106** | แตะ CSP ต้องเทส production build · ห้ามใช้ `eval()` | web-craft | บั๊กที่มีเฉพาะบนเว็บจริง หาสาเหตุยากที่สุด · เคยได้ **จอขาวทั้งใบ** |
| 10 | **63** | "โหลดไม่สำเร็จ" ต้องไม่เหมือน "ไม่มีข้อมูล" · หน้าสรุปห้ามวาด ฿0.00 | loading-skeleton | ตัวเลขผิดในเอกสารที่เสนอผู้บริหาร |

## 🧩 รูปแบบที่เจอซ้ำ — ควรรวบเป็นหัวข้อเดียวในสกิล

| รูปแบบ | ข้อที่เกี่ยวข้อง | ข้อเสนอ |
|---|---|---|
| **กฎกลางถูกฝังไว้ในสกิลเฉพาะทาง คนที่ต้องใช้ไม่เปิดอ่าน** | 6 (คอมเมนต์ JSX อยู่ใน `button-feedback`) · 37 (`width` คงที่อยู่ใน `SCOLDING-LOG`) · 54 (เปิดภาพดูก่อนรายงาน อยู่ใน `mobile-web-app`) · 78 · 91 | **นี่คือปัญหาเดียวกับที่พี่กันจับได้** (แว่นขยาย · ฟอนต์ลูกศร) แค่คนละชั้น — ควรกวาดย้ายกฎกลางออกมาไว้ที่ `working-with-gun`/`web-craft` แล้วให้สกิลเฉพาะทางชี้กลับ |
| **กฎการเขียนและอ่านผลตัวตรวจ (ยังไม่มีหัวข้อนี้เลยในสกิลใด)** | 10 · 16 · 26 · 40 · 46 · 48 · 52 · 61 · 69 · 87 · 98 | ตั้งหัวข้อใหม่ใน `working-with-gun` — 11 ข้อพร้อมใช้ |
| **กับดักของ `<select>` / ช่องกรอก** | 39 (ลูกศร ▾ กินที่) · 90 (`background` รวบล้างลูกศร) · 95 (`<option>` ไม่สืบทอด) · 103 (ความสูงไม่เท่า) | รวมเป็นหัวข้อ "ช่องเลือกกับช่องกรอก" ใน `web-craft` |
| **กับดัก flex/overflow (สกิลมี 2 ข้อ ควรมี 5)** | 116 (ยุบแล้วซ่อนเนื้อหา) · 117 (วิธีหา) · 118 (`flex:1` = `1 1 0%`) · 122 (`overflow-anchor`) | ต่อท้าย `web-craft` บรรทัด 28-29 ให้ครบชุด |
| **สถานะที่หน้าตาเหมือนกันแต่ความหมายคนละเรื่อง** | 63 (โหลดไม่สำเร็จ vs ไม่มีข้อมูล) · 132 (ไม่พบ vs เลือกแล้ว) · 50 (โค้ดไม่พัง แค่ไม่มีอะไรให้เรียก) | หลักการเดียวกัน — ควรเขียนเป็นกฎกลางข้อเดียวใน `web-craft` |
| **เรื่องฟอนต์ (ยังไม่มีในสกิลเลยทั้งชุด)** | 38 (เปลี่ยนฟอนต์ต้องไล่ระยะ) · 80 (`--font-en` fallback) · 81 (จุดที่เขียนฟอนต์ตรง ๆ ไม่ผ่านตัวกลาง) · 82 (กว้างกว่า 15%) · 83 (ห้ามลามเข้าใบพิมพ์) | ตั้งหัวข้อ "ฟอนต์" ใน `web-craft` — 5 ข้อพร้อมค่าจริง |
| **การนำเข้า/จับคู่ข้อมูลยาจาก HIS** | 124 (ชัวร์/ไม่ชัวร์) · 125 (ตรวจความแรงเสมอ) · 126 (Tall Man Lettering) | `medical-data-safety` ยังไม่มีหัวข้อการจับคู่ข้อมูลเลย · เว็บอื่นจะต้องทำเรื่องนี้แน่ |
| **กับดักเครื่องมือของเครื่องพี่กัน** | 2 (`resize_window`) · 9 (CRLF · `/tmp` · heredoc · `</div>`) · 23 (`puppeteer-core` หา `node_modules`) · 105 (`credential.guiPrompt`) · 112 (`visibilitychange`) · 129 (พอร์ตค้างแต่ไม่ตอบ) | ตั้งหัวข้อ "กับดักของเครื่องนี้" ใน `working-with-gun` ข้อ 5 |

## ✅ ที่ยืนยันว่ายกขึ้นสกิลครบแล้ว (ไม่ต้องทำอะไร)

โครงจางรอข้อมูล (08-27-part3) · ระบบมือถือ `_pinLeft`/ffmpeg/ป้ายเลข (09-01-part2) ·
วรรณยุกต์ไทยโครงสามชั้น (09-01-part4) · ตารางชั้น `Z` + ฉากหลังห้ามเลื่อนฝั่งคอม (09-01-part3) ·
สีตอนชี้ + `hover-check`/`hue-check` + บทเรียนเทคนิค 4 ข้อ (08-27) · กฎ 9 ข้อ + แผนผัง push (08-26-part5) ·
บันทึกของเซิร์ฟเวอร์ + ปิดบัง HN + บัตรผ่าน (09-02 · 09-02-part4) · ระบบค้นหาทั้งชุด (08-10) ·
ห้ามเดาข้อมูลยา + "ลิสต์มาให้ดู = ให้เสนอ" (08-13) · ตารางจริง `<table>` + เปิดเป็นแอป (09-01-part5)
