# สถานะผลตรวจ 46 ข้อ — ไฟล์สรุปที่ต้องอัปเดตทุกครั้งที่แก้

> ผลตรวจต้นฉบับอยู่ที่ [`AUDIT-2026-08-25.md`](AUDIT-2026-08-25.md) (ตรวจบนฐาน v0.7.1.0)
> **ไฟล์นี้คือสถานะล่าสุด** — ต้นฉบับเป็นบันทึกวันที่ตรวจ ไม่ได้อัปเดตตาม
> อัปเดตล่าสุด **26 ส.ค. 2569 · v0.10.0.3**

| | ความหมาย |
|---|---|
| ✅ | แก้แล้ว |
| ◐ | แก้บางส่วน ที่เหลือรอเคาะ |
| ⬜ | ยังไม่แก้ |

**รวม 46 ข้อ — ✅ 21 · ◐ 3 · ⬜ 22**

| ระดับ | ทั้งหมด | ✅ | ◐ | ⬜ |
|---|---|---|---|---|
| 🔴 วิกฤต | 1 | 1 | 0 | 0 |
| 🟠 สูง | 8 | 5 | 0 | 3 |
| 🟡 กลาง | 15 | 5 | 1 | 9 |
| 🟢 ต่ำ | 19 | 10 | 2 | 7 |
| 📌 ต้องตัดสินใจ | 3 | 0 | 0 | 3 |

---

# 🔴 วิกฤต

### ✅ ว-1 · โหมดดูตัวอย่างกดลบ/แก้ แล้วไปโดนแถวจริงในฐาน
**แก้ 25 ส.ค. รอบ 4 (v0.10.0.0)** · ข้อมูลตัวอย่างใช้เลขแถว 1, 2, 3 ชุดเดียวกับของจริง
เปิดโหมดตัวอย่างให้ผู้บริหารดูแล้วกด "ลบ" → แถวจริงหมายเลขนั้นหายจากฐาน
**วิธีแก้:** `handlers/index.js` → `installDemoGuard` ดักที่ทางออกทางเดียว ทุกคำขอที่ไม่ใช่ GET ถูกตีกลับ
เปลี่ยนทุก handler ให้เรียก `app.fetchT` (29 จุด) แทนการไล่กันทีละฟังก์ชัน

---

# 🟠 สูง

### ✅ ส-1 · เซิร์ฟเวอร์ทดสอบเปิดข้อมูลผู้ป่วยให้ทั้งวงแลนโรงพยาบาล
**แก้ 26 ส.ค. (v0.10.0.1)** · `package.json` → `next dev -H 127.0.0.1`

### ✅ ส-2 · ประตูตรวจรหัสเป็นแบบ "พังแล้วเปิด"
**แก้ 26 ส.ค. (v0.10.0.1)** · `lib/auth.js` แยก `authConfigured()` จาก `authEnabled()`
production ล็อกเสมอ · ไม่มีรหัส = 503 ทั้งเว็บ · `/api/auth` กันรหัสว่างด้วย
⚠️ **ก่อน deploy ต้องมั่นใจว่า `MRV_PASSWORD` ตั้งบน Cloudflare แล้ว**

### ⬜ ส-3 · เดารหัสผ่านได้ไม่จำกัดครั้ง ไม่มีบันทึกความพยายาม
รหัสร่วมห้องยามักสั้น เครื่องอัตโนมัติยิงได้หลักพันครั้งต่อนาที ลองรหัสยอดนิยม 10,000 ตัวจบใน 2-3 นาที
**นี่คือกุญแจดอกเดียว** ผ่านแล้วได้ HN ทั้งปี + สิทธิ์ลบ + สิทธิ์แก้คลังยาที่ใช้ร่วม 3 เว็บ
**ทางที่คุ้มสุด: ตั้ง Cloudflare Rate Limiting ที่ `/api/auth` ไม่ต้องแก้โค้ดเลย** — รอพี่กันเคาะ

### ✅ ส-4 · เลือกปีงบย้อนหลัง → กราฟว่าง · ป้ายผิดปี · CSV ผิดปีทั้งไฟล์
**แก้ 25 ส.ค. รอบ 4** · `vals/summary.js` ใช้ `fyYear` ที่เซิร์ฟเวอร์ส่งมา · API รับ `fy=2568`

### ✅ ส-5 · ป๊อปยืนยันลบเขียนว่า "กู้คืนไม่ได้" ทั้งที่มีถังขยะ
**แก้ 25 ส.ค. รอบ 4**

### ✅ ส-6 · "ล้างทั้งหมด" ไม่ล้างเลข Lot → ยาคนใหม่ถูกยัดเข้า Lot คนเก่า
**แก้ 25 ส.ค. รอบ 4**

### ⬜ ส-7 · เปิด 2 แท็บ → ร่างหาย หรือมูลค่าถูกนับสองรอบ
- **เบา:** แท็บ 2 เพิ่มยา → แท็บ 1 เพิ่มยา → แท็บ 1 เขียนทับ ของแท็บ 2 หายถาวร
- **หนัก:** แท็บ 1 บันทึกสำเร็จได้ Lot A · แท็บ 2 ยังโชว์ปุ่ม "บันทึก 8 รายการ" ค้าง
  กดตาม → ออก Lot B → **แถวเดิมเข้าฐานอีกชุด มูลค่านับสองรอบ**

**ทำไมยังไม่แก้:** ต้องออกแบบวิธีให้สองแท็บคุยกัน และเคาะว่าแท็บที่ล้าหลังควรทำยังไง
(เตือน / ปิดปุ่ม / ดึงของใหม่มาทับ) ไม่ใช่แก้บรรทัดเดียว

### ⬜ ส-8 · เหตุผลการทำลายเก็บได้เฉพาะทางป๊อปมือถือ

| เส้นทาง | ได้เหตุผลไหม |
|---|---|
| มือถือ กดยา → ป๊อป → เลือก "ทำลาย" | ได้ |
| **คอม พิมพ์ → Enter → ใส่จำนวน → Enter** (เส้นทางหลัก) | **ไม่ได้** |
| **กดสลับ "ใช้ต่อ/ทำลาย" ในแถวที่เพิ่มแล้ว** | **ไม่ได้** |
| แก้แถวเก่าในหน้าประวัติ | ไม่ได้ |

ตอบผู้บริหารไม่ได้ว่า "ทำลายไป 40,000 บาท เพราะอะไร" ซึ่งเป็นเหตุผลเดียวที่ใส่ช่องนี้มา
**ทำไมยังไม่แก้:** ต้องเพิ่มช่องใหม่ในหน้าจอ = แตะหน้าตา

---

# 🟡 กลาง

### ⬜ ก-1 · โหลดไม่สำเร็จ กับ ไม่มีข้อมูลจริง หน้าจอเหมือนกัน
**หน้าสรุปหนักสุด** — `/api/summary` ล่ม → วาด **฿0.00 ทุกช่อง** ไม่มีอะไรบอก
**หน้าสรุปคือหน้าที่เอาไปเสนอผู้บริหาร** การโชว์ ฿0 ตอนโหลดไม่ผ่าน = บอกข้อมูลผิดแบบเงียบ ๆ
หน้าประวัติขึ้น "ไม่พบรายการตามเงื่อนไข" ทั้งที่ความจริงคือเน็ตหลุด

### ⬜ ก-2 · ข้อความแจ้งพังหายใน 2 วินาที และกดไม่ได้
ใช้ 2000ms ร่วมกันทั้ง "บันทึกสำเร็จ" และ "อ่านประวัติไม่สำเร็จ" · `pointer-events:none` แตะไม่ได้
มี 12 จุดที่ใช้แจ้งความล้มเหลว · หน้าบันทึกทำถูกแล้ว (แถบแดงค้าง + ปุ่มลองใหม่) เอาแนวนั้นมาใช้
⚠️ **กัดพี่กันจริงแล้ว 25 ส.ค.** ตอนกดยืนยันแก้ Lot แล้วโดนตีกลับ ข้อความหายก่อนอ่านทัน ดูเหมือนปุ่มเสีย

### ⬜ ก-3 · ไม่มีตัวรับข้อผิดพลาด พังทีเดียวได้จอขาว
ตรวจซ้ำ 26 ส.ค. แล้ว — ยังไม่มี `app/error.js` · ไม่มี `app/global-error.js` · ไม่มี `componentDidCatch`
หลักฐานว่าเคยเกิดอยู่ในโค้ดเอง มีคอมเมนต์ว่ากรองแถวเสียทิ้งเพราะเคยทำจอขาวถาวร
เภสัชกรกลางเวรเจอจอขาวกับข้อความอังกฤษ กู้เองไม่ได้ · แก้ด้วยไฟล์เดียวราว 30 บรรทัด
**ทำไมยังไม่แก้:** เป็นหน้าจอใหม่ที่พี่กันควรเห็นแบบก่อน

### ✅ ก-4 · ใบสรุป Lot ตัดที่ 60 แถวเงียบ ๆ
**แก้ 26 ส.ค. (v0.10.0.1)** · `handlers/lots.js` เติม `limit=all`

### ✅ ก-5 · `/api/catalog` เขียนคลังกลางโดยไม่จำกัดความยาว
**แก้ไปแล้วตอน v0.9.0.0 (25 ส.ค.)** · มี `MAXLEN` รายช่องแล้ว

### ✅ ก-6 · middleware มีรูให้เลี่ยง — เส้นทางลงท้าย `.png` `.svg` ไม่ถูกตรวจรหัส
**แก้ 26 ส.ค. (v0.10.0.1)** · เพิ่ม `'/api/:path*'` เป็น matcher ตัวแรก
พิสูจน์กับตัวกรองชุดเก่าแล้วว่า `/api/returns/12.png` และ `/api/lots/L690826-01.svg` ทะลุจริง

### ✅ ก-7 · HN ผู้ป่วยค้างในเครื่องกลาง ไม่ถูกล้างตอนออกจากระบบ
**แก้ 26 ส.ค. (v0.10.0.1)** · `doLogout` ล้างเฉพาะช่อง HN ไม่แตะรายการยาในร่าง
⚠️ อีกทางเลือกที่ผลตรวจเสนอ (ใส่อายุให้ร่าง 12-24 ชม.) **ยังไม่ได้ทำ** ถ้าอยากได้ด้วยบอกได้

### ⬜ ก-8 · `renderVals()` คำนวณค่าของทุกหน้าใหม่ทุกครั้งที่วาดจอ
หนักสุดคือคลังยา 417 ตัวถูกประกอบชื่อใหม่ทั้งชุดทุกรอบ
**หลังเข้าแท็บคลังยาไปครั้งหนึ่ง ทุกตัวอักษรที่พิมพ์ในช่องค้นยาจะสั่งคำนวณคลังใหม่ทั้งก้อน**
⚠️ v0.10.0.2 แก้ไปครึ่งหนึ่งแล้วโดยไม่ตั้งใจ — เลิกเรียก vals ซ้ำรอบสองตอน dev (ดู ต-9)
แต่ตัวปัญหาหลักคือเรียกทุกหน้าทุกรอบ ยังอยู่ · **ต้องรื้อโครง**

### ⬜ ก-9 · หน้าคลังยาวาดครบ 417 แถว × 14 คอลัมน์ทีเดียว
ต่างจากทุกหน้าในโปรเจกต์เดียวกัน (ประวัติ 60 · ราคา 40 · Lot 40)
คอมเมนต์ใน `MedReturnApp.jsx` เขียนเองว่า "วาดหมดทีเดียวจอค้าง"

### ◐ ก-10 · ป๊อปอัปไม่ดักโฟกัส และ Esc ปิดได้ไม่ครบ
**แก้บางส่วน 25 ส.ค.** — ใบสรุป Lot · หน้าต่างแก้ไข Lot 3 ชั้น · แป้นเครื่องคิดเลข · ช่องแก้จำนวน
**ยังเหลือ:** หน้านำเข้าราคาจาก HIS · 4 ป๊อปในหน้าคลังยา
จุดอันตรายสุดคือป๊อปยืนยัน — กฎเหล็กตั้งใจให้ "กดพื้นหลังไม่ปิด"
ผู้ใช้คีย์บอร์ดที่โฟกัสหลุดจะกดปุ่ม "ยืนยันลบ"/"ยกเลิก" ไม่ได้เลย

### ⬜ ก-11 · ช่องกรอกบนมือถือต่ำกว่า 16px ทุกช่อง iOS ซูมเองทุกครั้ง
ช่องค้นหายา 15.5px · วันที่ 14px · HN 14px · ยานอกบัญชี 13.5px · ค้นประวัติ 14.5px · รหัสผ่าน 15px
แตะช่องค้นยา → จอเด้งซูม → พิมพ์ → หุบนิ้วซูมออก → แตะช่องจำนวน → เด้งอีก **วนทุกรายการยา**
⚠️ ขยับเป็น 16px = ตัวหนังสือโตขึ้นจริง **แตะหน้าตา ต้องเคาะ** (แก้เฉพาะฝั่งมือถือได้ ไม่กระทบคอม)

### ⬜ ก-12 · สีข้อความจางเกินเกณฑ์ รวมถึงป้ายแท็บในแถบเมนู

| สี | บนพื้น | ได้ | เกณฑ์ | ใช้ที่ไหน |
|---|---|---|---|---|
| `#9aa19c` | ขาว | **2.4:1** | 4.5 | ป้ายแท็บที่ยังไม่เลือกในแถบเมนูคอม + ตัวอย่างในช่องกรอก |
| `#8d948f` | ขาว | **3.1:1** | 4.5 | ป้ายแท็บในแถบเมนูมือถือ |
| `#c0c5c1` | ขาว | **1.9:1** | 4.5 | ป้าย Lot ตอนไม่มีค่า |
| `#6b746e` | `#f6f7f4` | 4.49:1 | 4.5 | เฉียดตก · ใช้เยอะสุดในเว็บ |

ห้องยาแสงจ้า + จอคอมเก่าสีซีด + เจ้าหน้าที่อายุมาก
⚠️ สีมาจากมอคอัปตรง ๆ **เข้าข่ายเปลี่ยนดีไซน์ ต้องเคาะ** · ขยับ 1-2 ขั้นก็ผ่านแล้ว

### ✅ ก-13 · เทียบรหัสผ่านแบบไม่คงเวลา
**แก้ 26 ส.ค. (v0.10.0.1)** · เพิ่ม `safeEqual()` ใช้ทั้ง `middleware.js` และ `/api/auth`

### ⬜ ก-14 · `'unsafe-inline'` อยู่ใน `script-src` ของเว็บจริงด้วย
ตรวจซ้ำ 26 ส.ค. — ยังมีอยู่จริงที่ `next.config.js:18`
`style-src` ตัดไม่ได้ (ถูกต้อง ไม่แตะ) **แต่บรรทัดนี้คือ `script-src` คนละเรื่องกัน**
ทำให้ตาข่ายกัน XSS หมดความสามารถไปเกือบทั้งหมด · ตอนนี้โค้ดสะอาด แต่ตาข่ายมีไว้รับตอนพลาด
**ทำไมยังไม่แก้:** ตัดตรง ๆ ไม่ได้ Next ใช้สคริปต์ฝังในหน้าเพื่อวาดจอ ตัดแล้วเว็บพังทันที
ต้องทำระบบใบอนุญาตรายสคริปต์ (nonce) ก่อน เป็นงานชิ้นหนึ่งเลย

### ⬜ ก-15 · คุกกี้หมดอายุที่เบราว์เซอร์เท่านั้น + `needAuth` ส่งมาแต่ไม่มีใครรับ
ตรวจซ้ำ 26 ส.ค. — ค้นทั้งโปรเจกต์ยังไม่มีโค้ดฝั่งจอตัวไหนอ่าน `needAuth` เลย
คุกกี้ 30 วันหมดตอนแท็บเปิดค้าง → ทุกปุ่มขึ้นข้อความแดงวนไปเรื่อย ๆ ผู้ใช้ต้องเดาเองว่าให้กด F5

---

# 🟢 ต่ำ

### ⬜ ต-1 · หน้าเข้าสู่ระบบไม่ใช่ฟอร์มจริง
ตัวจำรหัสผ่านในเบราว์เซอร์ไม่เติมให้ · กด Enter ส่งไม่ได้ · โปรแกรมอ่านหน้าจอไม่บอกว่ากรอกผิด
**ทำไมยังไม่แก้:** เป็นประตูทางเข้าเว็บ พลาดแล้วเข้าไม่ได้เลย ต้องเทสในเบราว์เซอร์จริงตอนพี่กันอยู่ด้วย

### ⬜ ต-2 · มือถือ ประวัติเกิน 60 แถวแล้วไม่มีปุ่มโหลดเพิ่ม
ตรวจซ้ำ 26 ส.ค. — ปุ่มอยู่ใน `renderHistoryWide` เท่านั้น `renderHistoryNarrow` ยังไม่มี
**ทำไมยังไม่แก้:** เพิ่มปุ่มใหม่บนจอ

### ✅ ต-3 · `saveHisImport` ใช้ `fetch` ดิบไม่มีตัวจับเวลา
**แก้ 26 ส.ค. (v0.10.0.2)** · เปลี่ยนเป็น `app.fetchT` — ได้ตัวจับเวลา + ตัวกันโหมดตัวอย่างด้วย

### ✅ ต-4 · `loadMoreHistory` ไม่มีเลขลำดับกันคำตอบเก่า
**แก้ 26 ส.ค. (v0.10.0.2)**

### ✅ ต-5 · ตัวเลขใหญ่หน้าสรุปเพี้ยนได้ 1 บาท
**แก้ 26 ส.ค. (v0.10.0.2)** · ทดสอบแล้ว `999.999` เดิมได้ `999.00` ตอนนี้ได้ `1,000.00`

### ✅ ต-6 · `deleted_by` ไม่เคยถูกบันทึก
**แก้ 26 ส.ค. (v0.10.0.1)** · `deleteRecord` ส่ง `by` ไปด้วย
⚠️ ยังไม่บังคับกรอก — ถ้ายังไม่เลือกชื่อในหน้าบันทึกจะได้ค่าว่างเหมือนเดิม
การบังคับต้องเพิ่มช่องในหน้าต่างยืนยัน = แตะหน้าตา **รอเคาะ**

### ✅ ต-7 · `invalidate()` ไม่ล้างแคชรายการ Lot
**แก้ 26 ส.ค. (v0.10.0.1)**

### ✅ ต-8 · `app.loadLots` ประกาศซ้ำ 2 ไฟล์
**แก้ 26 ส.ค. (v0.10.0.2)** · ลบตัวตายใน `history.js` เหลือตัวเดียวใน `lots.js`

### ✅ ต-9 · คีย์ซ้ำข้ามไฟล์ และตัวตรวจตรวจไม่ครบ 3 ไฟล์
**แก้ 26 ส.ค. (v0.10.0.2)** · รวมเป็น `parts` ชุดเดียว ครบ 11 ไฟล์ · เรียก vals ไฟล์ละครั้งเดียว

### ⬜ ต-10 · เลือกวันที่ในอนาคตได้ กรอกครบ 20 รายการค่อยเด้งเตือน
`dateMax` กับ `isBackdated` คำนวณไว้แล้วไม่มีใครแสดง
**ทำไมยังไม่แก้:** ต้องเคาะว่าจะห้ามเลือกไปเลย หรือเลือกได้แต่ขึ้นป้ายเตือนตั้งแต่ตอนเลือก

### ✅ ต-11 · ปุ่มลอย "ขึ้นบนสุด" ลอยทับหน้าต่างซ้อนและกดโดนได้
**แก้ 26 ส.ค. (v0.10.0.2)** · z-index 40 → 15
เดิมทับ 6 ตัว: ป๊อปใส่จำนวน 20 · ตั้งค่า 24 · เลือกผู้บันทึก 29 · ข้อความเด้ง 30 · นำเข้า HIS 34 · แก้ไข Lot 36
🚨 **กฎ: z-index ปุ่มลอยต้องต่ำกว่าหน้าต่างซ้อนที่ต่ำสุดเสมอ** (ตอนนี้ต่ำสุด 20 → ปุ่มต้องไม่เกิน 19)

### ⬜ ต-12 · ปุ่มเล็กกว่า 44px
🔴 **ผลตรวจบอกว่า "คลาส `.tap` มีอยู่แล้ว เติมอย่างเดียว ไม่ขยับหน้าตาสักพิกเซล" — ไม่จริงทั้งหมด**
`.tap::before` ขยายพื้นที่กดออกด้านละ 11px ปุ่มที่วางติดกันอย่าง **✓ กับ ✕**
และ **ใช้ต่อ/ทำลาย** จะมีพื้นที่กดทับกัน = **กดพลาดสลับกันได้**
ซึ่งเป็นคู่ "ยืนยัน/ยกเลิก" กับ "ใช้ต่อ/ทำลาย" พอดี
นับจริง 26 ส.ค. — ปุ่มที่ยังไม่มีคลาสนี้มี **79 จุด ไม่ใช่ 12 จุด** ตามที่ผลตรวจบอก
**ต้องวัดในเบราว์เซอร์จริงทีละคู่ก่อน ห้ามเติมรวดเดียว**

### ⬜ ต-13 · ไม่มี `aria-label` เลยสักจุดทั้งโปรเจกต์
ข้อความเด้งไม่มี `role="status"` · ป๊อปไม่มี `role="dialog"`
ตรวจซ้ำ 26 ส.ค. — ยังเป็น 0 ไฟล์

### ⬜ ต-14 · ไม่มีหัวข้อ/โครงหน้าเว็บมาตรฐานเลย
`<h1>` = 0 · `<main>` = 0 · `<nav>` = 0 · เติม `role=` ได้โดยไม่แตะ CSS
**ทำไมยังไม่แก้ (ต-13 + ต-14):** มองไม่เห็นบนจอเลย แต่เป็นการกวาด ~12 ไฟล์
ขอทำเป็นรอบของตัวเองจะได้ตรวจง่าย ไม่ปนกับรอบอื่น

### ◐ ต-15 · ส่วนหัวความปลอดภัยและของประกอบเว็บที่ขาด
**แก้แล้ว 26 ส.ค. (v0.10.0.2):** เพิ่ม HSTS (2 ปี · includeSubDomains) · ปิด `X-Powered-By`
**ยังไม่แก้:** สีแถบเบราว์เซอร์มือถือ (`themeColor`) · manifest (กด "เพิ่มลงหน้าจอโฮม") · หน้า 404
ทั้งสามอย่างเห็นบนจอ — manifest ต้องตั้งชื่อกับไอคอนแอป · หน้า 404 เป็นหน้าใหม่ต้องออกแบบ

### ✅ ต-16 · `/api/drugs/rev` ส่งข้อความผิดพลาดดิบกลับเบราว์เซอร์
**แก้ 26 ส.ค. (v0.10.0.1)** · เป็นเส้นเดียวใน 13 เส้นที่ยังทำอยู่

### ⬜ ต-17 · ไม่มีป้ายบอกว่า "ตอนนี้ออฟไลน์"
ระบบฟัง `online` อยู่แล้ว แต่ไม่ฟัง `offline` · **ทำไมยังไม่แก้:** เพิ่มป้ายใหม่บนจอ

### ✅ ต-18 · โค้ดตาย 3 จุด
**แก้ 26 ส.ค. (v0.10.0.2)** · `qtyText` สองข้างเหมือนกัน · `(histOffset ? 0 : 0)` · `histOffset` ลบออกทั้งหมด

### ◐ ต-19 · เพดานที่หน้าจอไม่บอก และการดึงข้อมูลชุดใหญ่ไม่มีร่องรอย
**แก้แล้ว 26 ส.ค. (v0.10.0.2):** `/api/returns` บันทึกฝั่งเซิร์ฟเวอร์เมื่อมีคำขอ `limit=all`
(จำนวนแถว · ช่วงวัน · เลข Lot · ถังขยะหรือไม่)
**ยังไม่แก้:** `/api/lots` ตัดที่ 200 Lot โดยหน้าจอไม่บอก — ต้องเพิ่มข้อความบนจอ

---

# 📌 เรื่องที่ต้องตัดสินใจ ไม่ใช่บั๊ก

### ⬜ 1 · `mr_backfill_price` ไม่จำกัดช่วงวัน และไม่มีหน้าต่างยืนยัน
ใส่ราคายาวันนี้ **ยอดปีงบ 2568 ที่รายงานผู้บริหารไปแล้วขยับย้อนหลังได้**
ยังอยู่ในกฎเหล็กข้อ 12 จึงไม่นับเป็นบั๊ก แต่ควรเตือนก่อนว่าจะกระทบกี่แถว ปีไหนบ้าง
(`mr_zero_price_count` มีอยู่แล้ว หยิบมาใช้ได้เลย)

### ⬜ 2 · ฟังก์ชันในฐานข้อมูลบางตัวไม่มีไฟล์เก็บใน `scripts/`
`mr_lots` · `mr_next_lot_no` ไม่มีเลย · `mr_history` ที่ใช้จริงรับ 7 ช่อง แต่ไฟล์มีแค่รุ่น 5 ช่อง
**ยืนยันไม่ได้ว่ากรอง `deleted_at` ครบไหม และคืนช่อง `reason` มาหรือเปล่า**
`lib/csv.js` ใช้ `r.reason` อยู่ — ถ้าไม่ได้ส่งมา **คอลัมน์ "เหตุผลที่ทำลาย" ในไฟล์ส่งออกจะว่างทุกแถว**
✅ ยืนยันแล้ว 26 ส.ค. ด้วย SQL จริง — `mr_history` มี **4 เวอร์ชันซ้อนกันในฐาน**
🆕 **ทำจากคลาวได้แล้ว** Supabase MCP ต่อติด ไม่ต้องรอเครื่องพี่กัน

### ⬜ 3 · ตาราง `drugs` — `anon` แก้ได้โดยไม่มีเงื่อนไข
รายงานไว้ตั้งแต่ 13 ส.ค. ยังไม่แก้ · 🆕 ทำจากคลาวได้แล้วเหมือนกัน

---

# 📋 งานค้างอื่นที่ไม่ได้อยู่ในผลตรวจ

- **ยา 43 ตัวยังไม่มีราคา** — 15 ตัวรอแค่กดเลือกในแท็บ "รอกดเลือก" (ยืนยันด้วย SQL 26 ส.ค.)
- **`mr_drug_price`** ไม่มีโค้ดไหนอ่านแล้ว เก็บเป็นสำเนาสำรอง รอเคาะว่าจะลบเมื่อไร
- **`mr_history` 4 เวอร์ชันซ้อนกัน** ตัวเก่า 3 ตัวยังใช้ชื่อแบบเดิม รอเคาะว่าจะลบไหม
- **ระบบ LASA + ค้นหาแบบพิมพ์ผิดนิดหน่อย** ยังไม่ตัดสินใจ
- **คอลัมน์ราคายังไม่มีในการ์ดมือถือของหน้าคลังยา**
