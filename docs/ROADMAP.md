# 🗺 Roadmap — เว็บแอปมูลค่ายาคืน

> รวมทุกอย่างที่ "ยังไม่ได้ทำ" กับ "เสนอไว้แล้วรอตัดสินใจ" ไว้ที่เดียว
> อัปเดตล่าสุด **5 ส.ค. 2569** · ที่มาของรายการ: [REPORT-2026-08-05.md](REPORT-2026-08-05.md)

| สัญลักษณ์ | ความหมาย |
|---|---|
| 🔜 | เคาะแล้ว รอลงมือ |
| 💤 | เสนอแล้ว **พี่กันบอกให้เก็บไว้ก่อน** |
| 💭 | ยังไม่ได้คุย เป็นข้อเสนอเฉย ๆ |
| ✅ | ทำเสร็จแล้ว (เก็บไว้เป็นประวัติ) |

---

## 🚦 ก่อนเปิดใช้จริง — ต้องทำให้ครบ

| # | เรื่อง | สถานะ | หมายเหตุ |
|---|---|---|---|
| 1 | ตั้ง `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` บน Cloudflare | 🔜 | Workers → Settings → Variables and Secrets |
| 2 | ตั้ง `MRV_PASSWORD` (รหัสผ่านร่วมของห้องยา) บน Cloudflare | 🔜 | **ถ้าไม่ตั้ง เว็บจะไม่ล็อกเลย ใครมีลิงก์ก็เข้าได้** |
| 3 | ใส่ราคายาจริงจากไฟล์ HIS (คอลัมน์ `UnitPrice`) | 🛠 | **แคลร์ใส่ให้แล้ว 254 ตัว** · เหลือพี่กันเปิดหน้าจัดการราคา แท็บ **"รอกดเลือก"** แล้วกดเลือก 97 ตัว + พิมพ์ราคาเอง 66 ตัวที่ไม่มีในไฟล์ |
| 4 | เลือกยาที่คืนบ่อย 6 ตัวในหน้าตั้งค่า | 🔜 | ตอนนี้ว่าง แถบยาที่คืนบ่อยเลยยังไม่โผล่ |
| 5 | เช็คว่าเลขในหน้าสรุปตรงกับที่คาด หลังใส่ราคาจริง | 🔜 | |
| 6 | เทสบนมือถือจริง — โดยเฉพาะป๊อปอัปใส่จำนวนตอนแป้นพิมพ์ขึ้น | 🔜 | แก้โค้ดแล้วแต่ยังไม่ได้ยืนยันบนเครื่องจริง |

---

## 🔜 งานที่เคาะแล้ว รอลงมือ

### จากรายงานตรวจ — ส่วนที่ยังไม่ได้แก้

| เรื่อง | ทำไมต้องทำ |
|---|---|
| **ปุ่มทุกปุ่มกดด้วยคีย์บอร์ดได้** | ปุ่มทั้งหมดเป็น `div` · หน้าบันทึกมีของที่ Tab ถึงได้ 4 ชิ้น แต่กดด้วยเมาส์ได้ 15 ชิ้น → กรอกยาครบแล้วต้องคว้าเมาส์กดบันทึกทุกรอบ |
| **หน้าดูรายล็อตแบบเต็ม** | ตอนนี้มี API (`/api/lots`) + กรองด้วยเลขล็อตได้แล้ว แต่ยังไม่มีหน้ารายการล็อตให้กดเลือก |
| **ใบสรุปล็อตพิมพ์ได้** | เอาไปแปะกับถุงยาที่รอตรวจ หรือเก็บเข้าแฟ้มเป็นหลักฐาน |
| **ปุ่มล้างรายการทั้งหมด** | ตอนนี้ต้องกด ✕ ทีละแถว |
| **เตือนเมื่อร่างค้างข้ามกะ** | เวรเช้าทิ้งรายการค้างไว้ เวรบ่ายเปิดมาเจอของเก่า ไม่รู้ว่าของใคร แล้วกดบันทึกไปด้วย |
| **เตือนเมื่อยาซ้ำในรายการเดียวกัน** | โดนขัดจังหวะแล้วกรอกยาตัวเดิมซ้ำ ระบบสร้าง 2 แถวเงียบ ๆ |
| **ล้างราคาที่แก้ค้างตอนออกจากหน้าจัดการราคา** | กลับเข้ามาวันหลังเจอ "บันทึกราคา 3 รายการ" ค้างอยู่ ไม่รู้ว่าแก้อะไรไว้ |
| **ลบราคาจนว่าง = ต้องถามก่อน** | ตอนนี้ตั้งเป็น 0 เงียบ ๆ ทำให้ยานั้นกลายเป็น "ยังไม่ใส่ราคา" |
| **แถบเมนูจำกัดความกว้างบนจอใหญ่** | จอ 1920px โลโก้กับวันที่หลุดไปคนละมุมจอ ไม่ตรงแนวกับเนื้อหา |
| **ข้อความเด้งไม่ทับตัวเลขบนมือถือ** | ค่า `bottom:96px` ของมอคอัปไม่พอเมื่อมีแถบบันทึกสูง ~200px |
| **การ์ดยาสูตรผสมแยกชื่อผิดจุด** | `Amoxicillin + Clavulanic acid 875 +` / `125 mg` — มี `+` ห้อยท้าย |
| **สี "—" ในกราฟจางเกินอ่านไม่ออก** | คอนทราสต์ 1.8:1 จากเกณฑ์ 4.5:1 |
| **กราฟไม่ปัดหลักพัน** | 3,499 กับ 3,500 แสดงเป็น "3k" กับ "4k" ผู้บริหารบวกเองแล้วไม่ตรง |
| **security headers** | โปรเจกต์พี่น้อง (TB Dashboard) ใส่ CSP ไว้แล้ว ควรยกมาใช้แบบเดียวกัน |

### จากมุมคลังสินค้า — ข้อที่พี่กันเคาะว่าเอา

| เรื่อง | สถานะ |
|---|---|
| **ยาที่ถูกคืนบ่อยที่สุด** | ✅ **ทำแล้ว** — อยู่ในหน้าสรุป เรียงตามจำนวนครั้ง พร้อมคำอธิบายว่าเอาไปคุยกับแพทย์ได้ |

---

## 💤 เสนอแล้ว — พี่กันบอกให้เก็บไว้ก่อน

> เหตุผล: *"เราอยากให้มันง่ายในการกรอก"* — 2 ข้อนี้เพิ่มขั้นตอนตอนกรอก จึงพักไว้

### 1. วันหมดอายุ 💤

**ปัญหาที่จะแก้:** ตอนนี้ระบบบอกว่ายา 90 เม็ดนี้ "ใช้ต่อได้ ประหยัด 450 บาท"
แต่ไม่รู้ว่าหมดอายุเมื่อไหร่ → เอากลับเข้าคลังจริงไม่ได้ถ้าเหลืออายุไม่ถึงเกณฑ์
(ระบบคลังมาตรฐานใช้ 6 เดือน หรือ 1 ใน 3 ของอายุเดิม)

**ผลกระทบ:** ตัวเลข "ประหยัดได้" ที่รายงานผู้บริหาร **สูงเกินจริง**

**ถ้าทำ:** ช่องวันหมดอายุตอนกดเพิ่มยา (ไม่บังคับ) → ระบบเตือนถ้าเหลือน้อยกว่าเกณฑ์
→ หน้าสรุปแยก "ประหยัดได้จริง" ออกจาก "คืนมาแต่ใช้ไม่ได้"

### 2. สถานะ "รอตรวจสภาพ" (Quarantine) 💤

**ปัญหาที่จะแก้:** ตอนนี้มี 2 สถานะ — ใช้ต่อได้ / ทำลาย
แต่หน้าเคาน์เตอร์คนไข้รออยู่ เภสัชกรยังไม่ได้ตรวจสภาพยาละเอียด ต้องรีบรับไว้ก่อน
ระบบคลังจริงจึงมีสถานะกลาง "รอตรวจสอบ" — ของเข้ามาแล้วแต่ยังไม่ตัดสิน

**ถ้าทำ:** สถานะที่ 3 เป็นค่าเริ่มต้น → รับคืนเร็ว ตรวจทีหลังตอนว่าง
→ หน้าประวัติมีปุ่มเปลี่ยนเป็น ใช้ต่อ/ทำลาย → หน้าสรุปเตือน "ค้างรอตรวจ N รายการ"

---

## 💭 ยังไม่ได้คุย — ข้อเสนอมุมคลังสินค้า

เรียงตาม "คุ้มค่าที่สุดต่อแรงที่ลง"

| # | เรื่อง | ยากแค่ไหน | ได้อะไร |
|---|---|---|---|
| 1 | **นำกลับเข้าคลังแล้วหรือยัง** | กลาง | ยาที่ติด "ใช้ต่อได้" ถูกนับเป็นเงินประหยัดทันที แต่จริง ๆ ยังเป็นตัวเลขบนกระดาษจนกว่าจะมีคนเอากลับขึ้นชั้น · ในทางคลังคือ 2 ขั้นตอนแยกกัน (ตัดสินใจ / เคลื่อนย้ายจริง) |
| 2 | **ปิดงวดเดือน** | กลาง | ตอนนี้แก้รายการเก่าได้ตลอด แม้เป็นเดือนที่รายงานผู้บริหารไปแล้ว → ตัวเลขที่รายงานกับในระบบไม่ตรงกันโดยไม่มีใครรู้ · เป็นมาตรฐานของทุกระบบบัญชี/คลัง |
| 3 | **ยิงบาร์โค้ดแทนพิมพ์ชื่อ** | กลาง | ซองยา รพ. มีบาร์โค้ดอยู่แล้ว เครื่องอ่านราคาไม่กี่ร้อย เสียบ USB ใช้ได้เลย · **แก้ปัญหาเลือกยาผิดความแรงได้ในตัว** |
| 4 | **อนุมัติล็อตมูลค่าสูง** | ง่าย | ล็อตเกินเกณฑ์ (เช่น 5,000 บาท) ต้องมีคนที่ 2 กดรับรอง — ควบคุมภายในมาตรฐาน |
| 5 | **ล็อตของผู้ผลิต (Manufacturer Lot)** | ง่าย | ถ้าบริษัทเรียกคืนยา (Drug Recall) จะตามได้ว่ายาล็อตนั้นเคยถูกคืนมาบ้างไหม |
| 6 | **ตัวชี้วัดแบบคลังเพิ่มเติม** | ง่าย–กลาง | อัตราการกู้คืน (%) · มูลค่าเฉลี่ยต่อล็อต · แหล่งที่คืนมากที่สุด · เวลาเฉลี่ยจากรับคืนถึงตัดสินใจ |

---

## 💭 อื่น ๆ ที่ยังไม่ได้คุย

| เรื่อง | หมายเหตุ |
|---|---|
| **ระบบเข้าสู่ระบบเต็มรูปแบบ** | ตอนนี้เป็นรหัสผ่านร่วม 1 ชั้น + ช่องผู้บันทึกให้เลือกเอง · ถ้าอยากได้บัญชีรายคนจริง ๆ ต้องทำเพิ่ม |
| **โดเมนของเว็บ** | ตอนนี้เป็น `returndrug-pkh.siravitphoapha9928.workers.dev` · พี่กันเคาะว่ายังไม่เปลี่ยน · ถ้าอยากได้ชื่อสวยกว่านี้ ทางที่ตรงคือใช้โดเมนของโรงพยาบาล หรือแยกบัญชี Cloudflare สำหรับงาน |
| ~~**นำเข้าราคายาจากไฟล์ HIS**~~ | ✅ **ทำแล้ว v0.4.0.0** — หน้าตรวจทานในเว็บ อ่านไฟล์ในเครื่อง จับคู่ให้ แล้วให้เภสัชกรยืนยันก่อนบันทึก |
| **แจ้งเตือนทางอีเมล** | เช่น สรุปรายเดือนส่งอัตโนมัติ (โปรเจกต์อื่นของพี่กันใช้ Resend อยู่แล้ว) |
| **หน้ารายงานสำหรับผู้บริหารโดยเฉพาะ** | หน้าเดียวจบ เปิดฉายในห้องประชุมได้เลย ไม่มีปุ่มแก้ไขอะไรทั้งนั้น |

---

## ✅ ทำเสร็จแล้ว (ประวัติ)

<details>
<summary>กดดูรายการที่ทำเสร็จแล้ว</summary>

**5 ส.ค. 2569 — รอบใหญ่**

- ระบบล็อต — เลขล็อตที่ระบบสร้างเอง (`L690805-01`) · ค้นด้วยเลขล็อตได้ · กรองดูรายล็อตได้
- ช่องผู้บันทึก — 16 ชื่อจาก ME-DRP · เลือกครั้งเดียวค้างทั้งเวร · เพิ่มชื่อใหม่ได้
- ถังขยะ — ลบแล้วกู้คืนได้ · KPI กรองของที่ลบออกอัตโนมัติ
- จำนวนรับทศนิยม — ยาน้ำครึ่งขวด ยาแบ่งครึ่งเม็ด
- ราคา 4 ตำแหน่งทศนิยม — ตรงกับที่ HIS ให้มา
- ตีราคาย้อนหลัง — เติมเฉพาะแถวที่ราคาเป็น 0 ไม่ทับของเดิม
- HN + แหล่งที่มา ติดรายแถว — คนไข้ 2 คนในล็อตเดียวไม่ปนกัน
- เหตุผลการทำลาย — 5 ตัวเลือก
- ยานอกบัญชี รพ. — พิมพ์ชื่อ/หน่วย/ราคาเองได้
- รหัสผ่านร่วมของห้องยา 1 ชั้น — จำ 30 วัน
- เลือกปีงบย้อนหลังในหน้าสรุป
- เลือกช่วงวันที่เอง + ปุ่มดูเพิ่มในหน้าประวัติ
- ยาที่ถูกคืนบ่อยที่สุด (มุมคลังสินค้า ข้อ 3)
- หัวไฟล์ Export + บรรทัดยอดรวม + วันที่ พ.ศ.
- แก้บั๊กจากรายงานตรวจ 41 จุด (ดู [REPORT-2026-08-05.md](REPORT-2026-08-05.md))

**4 ส.ค. 2569** — แปลงมอคอัปเป็นเว็บจริงครบ 5 เฟส (v0.1.0.0)

</details>
