# SESSION 2026-08-26 — v0.10.0.1 ปิดรูความปลอดภัย 9 ข้อ + กฎใหม่เรื่อง push บนคลาว

> **commit `d635178`** (v0.10.0.1) · **`77e3a74`** (กฎใหม่) · push ขึ้น `main` แล้ว ยืนยัน `0 0`
> **ทำงานบนคลาว** (Claude Code บนเว็บ) ไม่ใช่เครื่องพี่กัน · ไม่ได้เปิดเซิร์ฟเวอร์ทดสอบของวัน

---

## 🔴 เรื่องแรกที่เจอ — สำเนาในเครื่องคลาวค้าง 3 สัปดาห์

เปิด session มาแล้วอยู่ที่ **v0.1.0.0 (4 ส.ค.)** ทั้งที่ `origin/main` ไปถึง **v0.10.0.0** แล้ว

**ผลคือแคลร์ตอบพี่กันผิดไป 2 รอบ** — บอกว่าไม่มีสกิลใน repo (ของจริงมี 2 ชุด)
และบอกว่า `CLAUDE.md` มี 237 บรรทัด (ของจริง 1,447 บรรทัด · กฎเหล็ก 15 ข้อ)

🚨 **บทเรียน: บนคลาวต้อง `git fetch` ดูก่อนเสมอ ห้ามเชื่อสิ่งที่เห็นในเครื่อง**
เครื่องคลาวโคลนไว้ตอนสร้าง environment ไม่ได้ตามของใหม่ให้อัตโนมัติ

---

## รายงานผิดอีกจุด — "ยังไม่ได้แก้สักข้อ"

แคลร์บอกพี่กันว่าผลตรวจยังไม่ได้แก้สักข้อ **ซึ่งผิด** เมื่อคืนรอบ 4 แก้ไปแล้ว 4 ข้อ
(ว-1 · ส-4 · ส-5 · ส-6) ที่ผิดเพราะไปลอกประโยคจากบันทึก**รอบ 3** ซึ่งเป็นสถานะก่อนรอบ 4

🚨 **บทเรียน: อ่านบันทึกหลายรอบในวันเดียวกัน ต้องเอาสถานะจากไฟล์รอบสุดท้ายเสมอ**

---

## แก้อะไรไปบ้าง — 9 ข้อ

เกณฑ์ที่พี่กันให้: *"ข้อไหนมันอันตรายแล้วเราไม่ต้องเคาะ ให้แก้เลย"*
= อันตรายจริง + ไม่แตะหน้าตา + ไม่ต้องตัดสินใจเชิงออกแบบ

### ส-2 · ประตูตรวจรหัสเปลี่ยนเป็น "พังแล้วปิด"

`lib/auth.js` แยก `authConfigured()` ออกจาก `authEnabled()`
`authEnabled()` คืน `true` เสมอเมื่อ `NODE_ENV === 'production'`

ของเดิมถ้าอ่าน `MRV_PASSWORD` ไม่ได้ → ปล่อยผ่านทันที **เว็บเปิดโล่งทั้งใบไม่มีสัญญาณเตือน**

🚨 **กับดักที่เกือบพลาด** — แก้แค่ `authEnabled()` ไม่พอ
ถ้าไม่กันรหัสว่างใน `/api/auth` ด้วย `tokenOf('')` จะตรงกับ `expectedToken('')` พอดี
= **กรอกช่องว่างแล้วเข้าเว็บได้ กลายเป็นรูที่ใหญ่กว่าเดิม**

⚠️ **ก่อน deploy ต้องมั่นใจว่า `MRV_PASSWORD` ตั้งบน Cloudflare แล้ว** ไม่งั้นเว็บจริง 503 ทั้งใบ

### ก-6 · `matcher` มีรูให้เลี่ยงด้วยนามสกุลไฟล์

เพิ่ม `'/api/:path*'` เป็น matcher ตัวแรกใน `middleware.js`
พิสูจน์รูเดิมด้วยตัวกรองชุดเก่าแล้ว — `/api/returns/12.png` กับ `/api/lots/L690826-01.svg` ข้ามประตูจริง

### ก-13 · `safeEqual()` เทียบแบบใช้เวลาเท่ากัน

สำคัญกับเว็บนี้เป็นพิเศษเพราะค่าที่เทียบ = ค่าเดียวกับที่ฝังเป็นคุกกี้

### ส-1 · `npm run dev` ผูกกับ `127.0.0.1`

เดิม `next dev` เปล่า ๆ + `MRV_DEV_OPEN=1` + `.env.local` ชี้ฐานตัวจริง
= ใครต่อไวไฟโรงพยาบาลได้ เปิดดู HN ทั้งปีงบและกดลบได้

### ก-4 · ใบสรุป Lot เติม `limit=all`

รับคืน 80 รายการแล้วพิมพ์ ได้กระดาษ 60 แถว ยอดท้ายใบไม่ตรงกับหน้ารายการ Lot

### ต-7 · `invalidate()` ล้างแคชรายการ Lot ด้วย
### ต-6 · `deleteRecord` ส่งชื่อผู้ลบไปด้วย
### ก-7 · `doLogout` ล้างเฉพาะช่อง HN ออกจากร่าง (ไม่แตะรายการยา)
### ต-16 · `/api/drugs/rev` ไม่ส่งข้อความผิดพลาดดิบกลับเบราว์เซอร์

---

## ✅ ตรวจแล้ว — ยิงจริงบนเซิร์ฟเวอร์โหมด production

| ทดสอบ | ผล |
|---|---|
| `/api/returns/12.png` ไม่มีคุกกี้ | **401** (เดิมข้ามประตู) |
| `/api/returns/12` ไม่มีคุกกี้ | 401 |
| หน้าแรกไม่มีคุกกี้ | 307 → `/login` |
| `/login` | 200 |
| กรอกรหัสว่าง | **401** |
| กรอกรหัสถูก | ok 200 |
| **ไม่ตั้ง `MRV_PASSWORD` ในโหมด production** | **ทุกเส้นทาง 503** พร้อมข้อความไทย |

`npm run build` ผ่าน · `node --check` ผ่านครบทุกไฟล์ `.js`
⚠️ **ยังไม่ได้เทสในโครมจริง** เพราะทำบนคลาว ไม่มีเบราว์เซอร์ของพี่กัน

---

## 🔴 กฎใหม่ที่พี่กันสั่ง — ทำงานบนคลาว push ขึ้น `main` เท่านั้น

setup ของ session บนคลาวตั้งให้ push ขึ้นสาขา `claude/read-all-files-naa71t`
แคลร์บอกพี่กันไปว่าติดข้อจำกัด **พี่กันสั่งกลับมาว่าให้ขึ้น `main` เดี๋ยวนี้ และตั้งเป็นกฎไว้เลย**

**เหตุผล** — Cloudflare ดู `main` อย่างเดียว · สาขา `claude/xxx` ไม่ใช่ตัว deploy ·
พี่กันไม่ได้เปิด GitHub ไล่ merge เอง · เครื่องคลาวเป็นเครื่องชั่วคราว ปิด session แล้วหาย

บันทึกไว้ที่ **`.claude/skills/working-with-gun/SKILL.md` ข้อ 4** และ **`CLAUDE.md` กฎเหล็กข้อ 16**
วิธี: `git push -u origin HEAD:main` แล้วยืนยัน `git rev-list --left-right --count origin/main...HEAD` = `0 0`

---

## เวอร์ชัน

`0.10.0.0` → **`0.10.0.1`** (W ขยับ — แก้บั๊กล้วน ไม่มีฟีเจอร์ใหม่ ไม่แตะหน้าตา)
แก้ 4 จุด: `APP_VERSION` · `APP_LAST_UPDATE` เป็น 26 สิงหาคม 2569 · `package.json` · `package-lock.json`

🚨 **`package-lock.json` ค้างที่ `0.1.0.0` มาตั้งแต่ commit แรก** เพิ่งเจอวันนี้ตอน `npm install`
แก้เฉพาะ 2 บรรทัดเลขเวอร์ชัน ไม่เอา metadata `libc` ที่ npm รุ่นในเครื่องคลาวจะตัดทิ้ง 152 บรรทัด

---

## ⏭ งานค้าง — ผลตรวจที่ยังไม่แก้ (รอพี่กันเคาะ)

| ข้อ | เรื่อง | ทำไมยังไม่แก้ |
|---|---|---|
| ส-3 | เดารหัสได้ไม่จำกัดครั้ง | ทำที่ Cloudflare Rate Limiting ไม่ต้องแก้โค้ด |
| ส-7 | เปิด 2 แท็บ ร่างหาย/มูลค่านับสองรอบ | ต้องออกแบบวิธีซิงก์ข้ามแท็บ |
| ส-8 | เหตุผลทำลายเก็บไม่ได้บนเส้นทางคอม | ต้องเพิ่มช่องในหน้าจอ |
| ก-1 ก-2 ก-3 | โหลดพลาดโชว์ ฿0 · ข้อความหาย 2 วิ · ไม่มีตัวรับข้อผิดพลาด | แตะหน้าตา |
| ก-8 ก-9 | คำนวณใหม่ทุกครั้งที่วาดจอ · วาด 417 แถวทีเดียว | ต้องรื้อโครง |
| ก-10 | ป๊อปไม่ดักโฟกัส (เหลือ 5 ป๊อป) | แตะหน้าตา |
| ก-11 | ช่องกรอกมือถือต่ำกว่า 16px iOS ซูมเอง | ตัวหนังสือโตขึ้นจริง |
| ก-12 | สีข้อความจางเกินเกณฑ์ | สีมาจากมอคอัป |
| ก-14 | `'unsafe-inline'` ใน `script-src` | ตัดตรง ๆ ไม่ได้ Next ใช้สคริปต์ฝังในหน้า ต้องทำระบบ nonce ก่อน |
| ก-15 | คุกกี้หมดอายุแล้วไม่พาไปหน้าเข้าสู่ระบบ | แตะหน้าตา |
| ต-1 ถึง ต-19 | ที่เหลือ 15 ข้อ | ระดับต่ำ เก็บทีหลังได้ |

**อื่น ๆ** — ยา 43 ตัวยังไม่มีราคา (15 ตัวรอกดเลือก) · `mr_drug_price` ไม่มีโค้ดไหนอ่านแล้ว
รอเคาะว่าจะลบไหม · `mr_history` มี 4 เวอร์ชันซ้อนกันในฐาน · ระบบ LASA ยังไม่ตัดสินใจ
